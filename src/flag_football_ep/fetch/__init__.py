"""Network fetch stage: downloads raw JSON snapshots to disk and stops there.

No re-exports on purpose — sibling plans import directly from
`flag_football_ep.fetch.http`, `flag_football_ep.fetch.sportapp` and
`flag_football_ep.fetch.ifaf`, so this file never needs editing.
"""
