---
phase: M3-04-player-analysis-report
plan: 05
type: execute
wave: 4
depends_on: [M3-04-04]
files_modified:
  - src/flag_football_ep/reports/player_analysis.py
  - src/flag_football_ep/templates/player_analysis.html.j2
  - src/flag_football_ep/reports/build.py
  - src/flag_football_ep/cli.py
  - tests/test_reports_player_analysis_page.py
  - tests/test_cli_report.py
  - docs/coaching-reports.md
autonomous: true
requirements: [HC-05]

must_haves:
  truths:
    - "`ffep report` produces a German HTML page that reads like his Player-Analysis tab, from canonical plays, without any manual step"
    - "The page shows his column and our column next to each other for every player, with n on every rate and muted cells greyed rather than hidden"
    - "Columns that cannot be computed appear as a named German state in the table, not as a blank or a zero"
    - "A failure inside this product is caught and reported as a German notice — the other four report products still get built"
    - "The full report run stays inside the ten-minute REQ-S1-16 budget with the new product added"
  artifacts:
    - path: "src/flag_football_ep/templates/player_analysis.html.j2"
      provides: "German report page extending base.html.j2, one section per split, HC columns next to M3-3 columns"
      contains: "{% extends \"base.html.j2\" %}"
      min_lines: 90
    - path: "src/flag_football_ep/reports/build.py"
      provides: "player-analysis as the fifth product with its own per-product failure isolation"
      contains: "player-analysis"
    - path: "docs/coaching-reports.md"
      provides: "The fifth product documented with its command, its splits and its known gaps"
      contains: "player-analysis"
  key_links:
    - from: "src/flag_football_ep/reports/build.py"
      to: "flag_football_ep.reports.player_analysis"
      via: "PRODUCTS entry plus a try/except dispatch branch mirroring the own-team branch"
      pattern: "build_player_analysis_data|PLAYER_ANALYSIS_FILENAME"
    - from: "src/flag_football_ep/templates/player_analysis.html.j2"
      to: "base.html.j2"
      via: "extends the shared base so Jinja2 autoescape and the single accent colour apply unchanged"
      pattern: "extends"
---

<objective>
Turn the data object into the thing he actually opens: one standalone German HTML page, built by
the same `ffep report` command that already produces the other four products, written into the same
dated run folder, inside the same ten-minute budget.

The layout carries the whole trust argument. His column and ours stand next to each other, every
rate carries its n, thin samples are greyed rather than removed, and the three columns we cannot
compute yet say why in his language — "nicht verfügbar (Drop-Spalte noch nicht kanonisch)" — instead
of showing a number he would then have to distrust.

Purpose: this is the deliverable the October handout links to.
Output: page builder, template, product registration, CLI wiring, documentation, and a timed real
run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-04-player-analysis-report/M3-04-CONTEXT.md
@.planning/phases/M3-04-player-analysis-report/M3-04-RESEARCH.md
@.planning/phases/M3-04-player-analysis-report/M3-04-04-SUMMARY.md
@src/flag_football_ep/reports/player_analysis.py
@src/flag_football_ep/reports/own_team.py
@src/flag_football_ep/reports/build.py
@src/flag_football_ep/reports/render.py
@src/flag_football_ep/templates/own_team_report.html.j2
@src/flag_football_ep/templates/base.html.j2
@src/flag_football_ep/charts/explosiveness.py
@src/flag_football_ep/cli.py
@docs/coaching-reports.md
@tests/test_cli_report.py
</context>

<file_collision_guard>
Owned by this plan: `src/flag_football_ep/reports/player_analysis.py` (page-builder half),
`src/flag_football_ep/templates/player_analysis.html.j2` (new file),
the `PRODUCTS` tuple and the one new dispatch branch in `src/flag_football_ep/reports/build.py`,
the `--product` help string in `src/flag_football_ep/cli.py`,
`tests/test_reports_player_analysis_page.py` (new file),
the product assertions in `tests/test_cli_report.py`, and the new product section in
`docs/coaching-reports.md`.

- `src/flag_football_ep/reports/{aggregate,own_team,render,opponent,decisions,wp_review}.py`:
  READ-ONLY. `render_page`, `fig_to_data_uri` and `write_report_run` are used exactly as they are —
  no new rendering or writing mechanism, no second templating environment.
- `src/flag_football_ep/templates/{base,own_team_report,opponent_report,decisions_cheatsheet,wp_review}.html.j2`:
  READ-ONLY. The new template EXTENDS `base.html.j2`; it does not modify it and adds no `|safe`
  filter anywhere.
- `src/flag_football_ep/charts/explosiveness.py`: READ-ONLY — its two renderers are called, not
  changed.
- `features/explosiveness.py`, `reference.py`, `config.py`, `canonical.py`, `ingest/**`: READ-ONLY.
- `tests/test_cli_report.py` is edited ONLY where the product list is asserted (the
  `test_products_tuple_is_the_four_locked_families` case and any list-length expectation) plus one
  added isolation test; every other test in that file stays untouched.
- `tests/conftest.py`: never edited.
- `docs/hc-rueckfragen-2026-09.md`, `docs/explosiveness-vorschlag.md`,
  `docs/epa-refinement-2026-10.md`: READ-ONLY here (M3-04-07 and M3-2 own them).
- `src/flag_football_ep/cv/**`, `scripts/hackathon/**`, `models/**`, `data/labels/**`,
  `data/video/**`: never touched.
- `reports/**` output is gitignored: the rendered HTML with real player names must never be
  committed. Verify with `git status --porcelain reports` before finishing.
</file_collision_guard>

<research_correction>
`M3-04-RESEARCH.md`'s Validation Architecture names `tests/test_reports_build.py` as an existing
file to extend. **That file does not exist.** The build/`PRODUCTS` coverage lives in
`tests/test_cli_report.py` (which asserts `PRODUCTS == ("opponents", "own-team", "decisions",
"wp-review")` verbatim, so it WILL fail the moment the fifth product is added — that assertion is
this plan's to update). Do not create `tests/test_reports_build.py`.
</research_correction>

<interfaces>
From M3-04-04 (read the summary for the final field names):

```python
PLAYER_ANALYSIS_FILENAME: str = "player-analysis.html"

@dataclass(frozen=True) class HcColumnTable: table; unavailable; notices; basis
@dataclass(frozen=True) class PlayerAnalysisSplit: key; heading; label_status; columns; m3_table; basis; empty_notice
@dataclass(frozen=True) class PlayerAnalysisReportData: team; splits; unresolved_games; unmapped_players; notices; n_hc_rows; overall_basis

def build_player_analysis_data(plays, *, config, scored=None) -> PlayerAnalysisReportData
```

Phase 1.4 rendering machinery, used unchanged:

```python
# flag_football_ep.reports.render
def build_environment() -> Environment     # cached, autoescape on for *.html.j2
def render_page(template_name, /, **context) -> str
def fig_to_data_uri(fig, *, dpi=EMBED_DPI) -> str    # closes the figure in a finally block
def write_report_run(rendered: dict[str, str], reports_root: Path) -> tuple[Path, Path]
ACCENT_COLOR = "#1f4e79"

# flag_football_ep.reports.own_team -- the formatting conventions to mirror exactly
_NO_DATA = "keine Daten"
_NOT_APPLICABLE = "–"      # en dash: does not apply to this row, never "missing"
def _format_epa(value, n) -> str
def _format_pct(value, n) -> str
# build_own_team_page: every numeric cell is pre-formatted to a display string plus a `muted`
# flag BEFORE it reaches the template; the template does no arithmetic.

# flag_football_ep.charts.explosiveness  (M3-3, built for this handoff)
def render_cliff_zone(table) -> Figure
def render_definition_comparison(table) -> Figure
```

Current dispatch in `reports/build.py` to mirror (per-product try/except, German notice, batch
continues):

```python
PRODUCTS: tuple[str, ...] = ("opponents", "own-team", "decisions", "wp-review")

if "own-team" in resolved_products:
    try:
        own_team_data = build_own_team_data(plays, config=config, scored=scored)
        rendered[OWN_TEAM_FILENAME] = build_own_team_page(own_team_data)
    except Exception as exc:  # noqa: BLE001 - per-product isolation, see docstring
        notices.append(f"Eigenteam-Auswertung fehlgeschlagen: {exc}")
```

`cli.py::report` validates `--product` against `PRODUCTS` and lists the valid names in its help
string — that literal list must be updated too, or the CLI help will disagree with the code.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The German page and its template</name>
  <files>src/flag_football_ep/reports/player_analysis.py, src/flag_football_ep/templates/player_analysis.html.j2, tests/test_reports_player_analysis_page.py</files>
  <read_first>
    - `src/flag_football_ep/reports/own_team.py` lines 756-1058 — the formatter helpers, the per-section context builders and `build_own_team_page`. This is the shape to mirror; the template must do no arithmetic.
    - `src/flag_football_ep/templates/own_team_report.html.j2` in full and `base.html.j2` lines 1-80 — the block structure, the `.muted`/`.warning-block`/`.section-basis` classes and the accent colour already defined there.
    - `tests/test_reports_own_team_page.py` — the page-test conventions (assert on rendered strings, not on HTML structure minutiae).
    - `.planning/phases/M3-04-player-analysis-report/M3-04-04-SUMMARY.md` — the exact data-object fields.
  </read_first>
  <action>
    Add `build_player_analysis_page(data: PlayerAnalysisReportData, *, generated_on: date | None = None) -> str`
    to `reports/player_analysis.py` and create `src/flag_football_ep/templates/player_analysis.html.j2`
    extending `base.html.j2`.

    Page structure (layout is Claude's discretion per CONTEXT; this is the shape that carries the
    argument):
    - Title `Player Analysis — {team}`, subtitle with the generation date and
      `data.overall_basis.text`.
    - A summary block naming, in German, how many head-coach rows the corpus contains
      (`data.n_hc_rows`, zero is a legitimate answer stated plainly), and which columns are
      currently unavailable and why.
    - `data.notices` and `data.unmapped_players` in the existing warning-block style.
    - One `<h2>` section per `PlayerAnalysisSplit`, in the order the data object provides:
      heading, `basis.text`, then a two-part table — his columns (in his tab's order, with his
      German labels: Comps, Incs, Attempts, TDs, Comp %, Adj Comp %, INTs, Sacks, Pass Yards,
      Air Yards, YPA, adj Pass Yards, adj YPA, Exp Plays, Explosive %, Efficiency, Carries, Rush
      Yards, Rush TDs) followed by our columns (Success Rate, Explosiveness kalibriert,
      Kontinuierlicher Score, each as `rate (n=…)` with the CI in a title/secondary cell and the
      shrunk rate beside it).
    - A section whose `label_status == "conflict"` carries its dispute note visibly in the heading
      area (e.g. `Camp IV/VI (unklar benannt, Zeilen 3001-4000)`).
    - A section with an `empty_notice` renders the notice instead of an empty table.
    - Footnotes: the Air-Yards deviation, the per-opponent limitation, the muting threshold
      (`MUTED_MIN_N`), and one sentence naming which M3-3 definitions produced our columns with a
      pointer to `docs/explosiveness-vorschlag.md`.

    Every numeric cell is pre-formatted in Python into `{"text": ..., "muted": bool}` before it
    reaches the template — reuse `own_team.py`'s `_NO_DATA` / `_NOT_APPLICABLE` vocabulary and add
    one shared `_UNAVAILABLE` German string for columns in `HcColumnTable.unavailable`, so an
    unavailable cell can never be visually confused with a zero or with "no plays".

    Embed the two existing M3-3 charts once per page (not per section) through `fig_to_data_uri`:
    `render_definition_comparison` over the corpus-wide split's comparison table, and
    `render_cliff_zone` over `cliff_zone_table` on the same frame. If either renderer raises, catch
    it, skip the chart and add a German notice — a chart is never worth failing a page for.

    Write `tests/test_reports_player_analysis_page.py` covering: the page renders to a non-empty
    HTML string on a synthetic data object; an unavailable column renders the named German state and
    never `0` or an empty cell; a `conflict` split heading contains both camp names; an empty split
    renders its notice and no table body; a player name containing `<script>` is escaped (autoescape
    proof — no `|safe` anywhere in the template); and the page contains no `data:image` entry when
    both chart renderers are stubbed to raise.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reports_player_analysis_page.py -q</automated>
    <automated>grep -c '|safe' src/flag_football_ep/templates/player_analysis.html.j2 | grep -qx 0 && echo "no unsafe filter"</automated>
  </verify>
  <done>A standalone German HTML page renders from the data object with his columns beside ours, named unavailable states, the conflict label visible, escaped player names and the two M3-3 charts embedded as data URIs.</done>
</task>

<task type="auto">
  <name>Task 2: Fifth product, CLI wiring, documentation and the timed real run</name>
  <files>src/flag_football_ep/reports/build.py, src/flag_football_ep/cli.py, tests/test_cli_report.py, docs/coaching-reports.md</files>
  <read_first>
    - `src/flag_football_ep/reports/build.py` lines 40-70 (imports, `PRODUCTS`) and 133-175 (the dispatch branches and the isolation discipline).
    - `src/flag_football_ep/cli.py` lines 468-517 — the `report` command, its `--product` help string and its validation.
    - `tests/test_cli_report.py` lines 300-320 and 470-495 — the two places the product list is asserted.
    - `docs/coaching-reports.md` sections "The four products", "Commands" and "Output layout" — the German documentation shape to extend.
  </read_first>
  <action>
    Register the product: append `"player-analysis"` to `reports.build.PRODUCTS` and add one
    dispatch branch mirroring the own-team branch exactly — build the data, build the page, store it
    under `PLAYER_ANALYSIS_FILENAME`, and catch `Exception` into the German notice
    `f"Player-Analysis-Auswertung fehlgeschlagen: {exc}"`. Per-product isolation is the point: a
    failure here must not cost the coaching staff the other four products the evening before a
    tournament.

    Update `cli.py`'s `--product` help string to list all five names. The validation code already
    reads `PRODUCTS`, so it needs no change — confirm that by reading it rather than editing it.

    Update `tests/test_cli_report.py`: extend the locked-products assertion to the five-tuple
    (rename the test to say five), and add one test that a raising
    `build_player_analysis_data` (monkeypatched) produces the German notice while the other
    products still render — the isolation guarantee, asserted rather than assumed.

    Update `docs/coaching-reports.md` in German: rename "The four products" to five and add the new
    product's paragraph (what it reproduces, that it follows his own formula cells, which splits it
    shows), one command example (`uv run ffep report --product player-analysis --skip-ingest`), the
    output filename, and a short "Was heute noch fehlt" list naming (a) the fact that his tab carries QB rows only — a receiver/WR table exists nowhere in his workbook, so none is reproduced here (a WR-side extension reusing `reports/own_team.py::player_efficiency` is deferred; REQ HC-05's "QB/WR" wording is reconciled in CONTEXT.md Deferred Ideas) — and (b) the three drop-dependent
    columns, the Air-Yards deviation and the Camp IV/VI naming conflict, each with the question it
    waits on.

    Then run the real thing end to end and record it:
    `uv run ffep report --product player-analysis --skip-ingest` (skip-ingest keeps the concurrent
    M3-2 ingest work untouched). Confirm the page lands in `reports/<date>/` and `reports/latest/`,
    read it once in a browser or via `python -m http.server`, and record the stage durations the
    command prints plus the total for a full five-product run in the summary — REP-D05's ten-minute
    budget is a claim that has to be measured, not asserted. Verify `git status --porcelain reports`
    is empty (the output is gitignored and must stay uncommitted).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_report.py -q</automated>
    <automated>uv run ffep report --product player-analysis --skip-ingest 2>&1 | tail -20</automated>
  </verify>
  <done>`ffep report` builds five products; a player-analysis failure is isolated to its own German notice; the CLI help, the product tuple and the documentation agree; the real page has been generated, opened and timed, and nothing under `reports/` is staged for commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-charted free text → rendered HTML | player names and descriptions typed into a spreadsheet reach a browser document |
| rendered report output → git | the rendered page carries real player display names |
| one product's failure → the whole report run | a batch the staff depends on the evening before a tournament |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-04-18 | Tampering (stored XSS via charted cells) | `player_analysis.html.j2` | mitigate | The template extends `base.html.j2` and inherits `select_autoescape([... "j2"])`; no `|safe` filter is allowed, asserted by a grep gate and by a `<script>`-in-name render test |
| T-M3-04-19 | Information Disclosure (PII in git) | rendered page under `reports/` | mitigate | `reports/**` is gitignored; task 2 verifies `git status --porcelain reports` is empty before finishing, and no fixture in this plan carries a real name |
| T-M3-04-20 | Denial of Service (batch abort) | `build_reports` dispatch | mitigate | The new branch uses the same per-product try/except and German notice as the other four, with a monkeypatched-failure test proving the batch continues |
| T-M3-04-21 | Path/filename injection | `write_report_run` key | mitigate | `PLAYER_ANALYSIS_FILENAME` is a fixed module constant, never built from opponent or player input; `write_report_run`'s existing key validation is the second layer |
| T-M3-04-22 | Repudiation (undocumented product) | `docs/coaching-reports.md` | mitigate | The fifth product, its command, its splits and its three named gaps are documented in the same file as the other four |
| T-M3-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing (M3-04-RESEARCH Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `uv run pytest tests/test_reports_player_analysis_page.py tests/test_cli_report.py tests/test_reports_render.py -q` green.
- `uv run pytest -q` green.
- `uv run ffep report --product player-analysis --skip-ingest` writes `player-analysis.html` into `reports/<date>/` and `reports/latest/`, prints its stage timings and exits 0.
- `grep -v '^#' src/flag_football_ep/templates/player_analysis.html.j2 | grep -c '|safe'` returns 0.
- `git status --porcelain reports` is empty.
</verification>

<success_criteria>
1. One command produces his tab as a German HTML page from canonical plays.
2. His numbers and ours are on the same row, with n on every rate and unavailable columns named.
3. The new product cannot take the other four down with it, and the run stays inside the budget.
</success_criteria>

<output>
Create `.planning/phases/M3-04-player-analysis-report/M3-04-05-SUMMARY.md` when done. Record the
measured stage durations, the rendered filename, and the exact list of columns that showed a
"nicht verfügbar" state on the real run — M3-04-07's handout quotes all three.
</output>
