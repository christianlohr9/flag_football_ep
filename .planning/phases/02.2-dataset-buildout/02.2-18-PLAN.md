---
phase: 02.2-dataset-buildout
plan: 18
type: execute
wave: 11
depends_on: ["02.2-17"]
files_modified:
  - docs/dataset-buildout.md
  - docs/cv-setup.md
autonomous: false
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "A detector trained on dataset v2 is registered with its dataset hash"
    - "Per-domain metrics for the 2.1 baseline, iteration 1 and iteration 2 are reported side by side on the identical frozen eval clips"
    - "The final stopping-rule verdict states whether further labelling is justified, with the measured deltas"
    - "The champion alias reflects a measured improvement, and the hackathon-frozen alias is unchanged"
  artifacts:
    - path: "docs/dataset-buildout.md"
      provides: "The three-way per-domain comparison and the final labelling verdict"
      contains: "mAP_50_95"
    - path: "docs/cv-setup.md"
      provides: "Updated Detector-Training section for the multi-domain detector"
  key_links:
    - from: "docs/dataset-buildout.md"
      to: "data/reference/frozen_eval_clips.csv"
      via: "all three detectors measured on the same clips"
      pattern: "frozen_eval"
---

<objective>
Close the dataset strand's measurement loop: train on dataset v2, evaluate per domain on the same
frozen eval clips as every previous detector, put all three detectors in one table, and state the
final labelling verdict against the threshold committed in plan 02.2-06.

Purpose: REQ-S2-03's success condition (D-14) is a measurable per-domain improvement over the pilot
detector, reported transparently. This plan produces that report or documents honestly that it did
not materialise.
Output: a registered multi-domain detector, a three-way per-domain comparison, the final verdict,
and the updated setup documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-15-SUMMARY.md
@.planning/phases/02.2-dataset-buildout/02.2-17-SUMMARY.md
@docs/dataset-plan.md
@docs/dataset-buildout.md
@docs/cv-setup.md

<interfaces>
Entry points (all implemented by now):
  ffep cv train --dataset data/labels/dataset --device {cuda|mps} ...
  ffep cv eval-domains --run <run id> --split data/reference/frozen_eval_clips.csv --out <path>
  ffep cv promote --run <run id>

Runs to compare (all on the identical frozen eval clips):
  Phase 2.1 champion  87a8a5222f7a472787875e974d089c44   (drone only, 304 frames)
  Iteration 1 run     <from plan 02.2-15 SUMMARY>
  Iteration 2 run     <produced by this plan>

Stopping rule (docs/dataset-plan.md): +0.010 absolute mAP_50_95 per domain, mAP_50 same direction.
Training machine policy (D-21): Dell 8 GB CUDA primary, M5 Max MPS proven fallback, AWS optional.
Variant policy: Nano/Small/Medium/Large only; XLarge/2XLarge forbidden (C-06).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the dataset-v2 training</name>
  <files>none — the run is recorded in MLflow, not in tracked files</files>
  <action>
    Pause execution and hand the user the training command with the same hyperparameters as iteration 1, so the comparison is not confounded by a schedule change. Do not start the run inside the agent session (D-21). When the user reports back, verify the MLflow run logged the dataset v2 `content_sha256`, and record machine, device, wall clock, metrics and any deviation from the iteration-1 settings.
  </action>
  <what-built>
    Dataset v2 (validated, DVC-tracked, hash recorded) and the per-domain evaluation verb. This is
    the second multi-hour GPU run of the phase and belongs on your hardware (D-21).
  </what-built>
  <how-to-verify>
    1. On the Dell box: `uv sync --extra cv --extra versioning`, then
       `uv run dvc pull data/labels/dataset.dvc` (or copy the directory if the remote is pending).
    2. Run `uv run --extra cv ffep cv train --dataset data/labels/dataset --batch-size 4
       --grad-accum 4 --resolution <per docs/dataset-plan.md> --device cuda` with the same epoch
       count as iteration 1, so the comparison is not confounded by a schedule change. If you change
       any hyperparameter, say which and why.
    3. Fall back to `--device mps` on the M5 Max if CUDA is unavailable. Do not wait for AWS.
    4. Report the MLflow run id, machine, device, wall clock and printed metrics.
  </how-to-verify>
  <acceptance_criteria>
    - An MLflow run id is reported and the run logs the dataset v2 `content_sha256` as a parameter
    - The hyperparameters match iteration 1, or every deviation is stated with its reason
    - The RF-DETR variant is one of Nano/Small/Medium/Large
  </acceptance_criteria>
  <resume-signal>Type the MLflow run id plus the machine and wall clock</resume-signal>
  <verify>
    <automated>uv run pytest tests/test_cv_detect_train.py -x -q</automated>
  </verify>
  <done>A registered MLflow run on dataset v2 exists with its dataset hash logged, and every hyperparameter deviation from iteration 1 is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Three-way per-domain evaluation and the final verdict</name>
  <files>docs/dataset-buildout.md</files>
  <read_first>
    - docs/dataset-buildout.md (the iteration-1 comparison table this extends)
    - docs/dataset-plan.md §Stopping rule and its resolvability caveat
    - data/reference/frozen_eval_clips.csv
    - docs/pilot-accuracy.md (the project's precedent for reporting a measurement with its caveats rather than a headline alone)
  </read_first>
  <action>
    Run `ffep cv eval-domains` for the iteration-2 run and re-use the recorded results for the 2.1
    champion and the iteration-1 run (re-run them if the split or the evaluation code changed since,
    and say so). Build one table in docs/dataset-buildout.md with a row per (domain, detector) and
    columns `mAP_50`, `mAP_50_95`, `AP_player`, `AP_referee`, `n_images`, `n_boxes`, followed by the
    iteration-over-iteration deltas per domain.

    State the final verdict per domain against the +0.010 threshold: whether a third iteration would
    be justified (it is out of scope for this phase either way — REQ-S2-03 specifies two) and
    whether the second domain helped or diluted the drone domain. Carry the phase-level success
    statement (D-14): a measurable per-domain improvement over the pilot detector, or an explicit
    statement that it did not materialise, with the numbers and the likely reason. Never report a
    pooled figure without the per-domain rows beside it.

    Promote the iteration-2 run to `champion` only if it improves the drone domain without
    regressing another in-scope domain; otherwise leave the alias where it is and write down why.
    The `hackathon-frozen` alias must be unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_freeze.py tests/test_cv_detect_train.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The table contains one row per (domain, detector) for all three detectors with `n_images`/`n_boxes` on every row
    - Deltas are stated per domain with the +0.010 threshold named next to them
    - A single sentence states whether the phase's D-14 success condition was met, with the numbers
    - `resolve_frozen('cv_detector_model', config)` still returns `87a8a5222f7a472787875e974d089c44` (asserted and quoted)
    - The promotion decision (moved or not moved) is recorded with its reasoning
  </acceptance_criteria>
  <done>The phase's central claim is either demonstrated with per-domain numbers or honestly reported as unmet.</done>
</task>

<task type="auto">
  <name>Task 3: Update the detector documentation</name>
  <files>docs/cv-setup.md</files>
  <read_first>
    - docs/cv-setup.md `## Detector-Training` (the 2.1 record whose structure this section mirrors, including its explicit limitations paragraph)
    - docs/dataset-buildout.md (the final numbers)
  </read_first>
  <action>
    Append a `## Detector-Training (Multi-Domain, Phase 2.2)` section to docs/cv-setup.md in German,
    mirroring the 2.1 section's structure: machine and device, settings, dataset hash, wall clock,
    per-class and per-domain metrics with their `n`, MLflow run id, promotion decision and its
    rationale, and an explicit limitations paragraph (single annotator, no inter-annotator agreement,
    one game per domain, eval clips from the same sessions as the training clips — clip-level split,
    not game-level, which is the strongest split this material allows). Leave the 2.1 section intact
    as history and cross-reference it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The new section states machine, device, settings, dataset hash, run id and per-domain metrics with `n`
    - The limitations paragraph names the clip-level (not game-level) split and the single-annotator caveat
    - The Phase 2.1 `## Detector-Training` section is unchanged (`git diff` shows insertions only in that region)
  </acceptance_criteria>
  <done>The multi-domain detector is documented to the same standard as the pilot detector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metric reporting → project decisions | Phase 2.3 and the gate re-run rely on these numbers |
| champion alias → hackathon-frozen alias | The benchmark must stay put |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-55 | Repudiation | improvement claim | mitigate | All three detectors measured on the identical frozen clips; per-domain rows with `n`; pooled-only reporting forbidden; the threshold was fixed before any number existed |
| T-2.2-56 | Tampering | hackathon-frozen alias | mitigate | Acceptance check re-reads `resolve_frozen` and asserts the 2.1 run id |
| T-2.2-57 | Elevation of Privilege (licence) | detector variant | mitigate | Variant policy restated in the checkpoint; source-level PML guard already in the suite |
</threat_model>

<verification>
- `uv run pytest tests -q` green
- Three-way per-domain table present with n on every row
- `resolve_frozen` unchanged; promotion decision documented

</verification>

<success_criteria>
- Dataset-v2 detector trained on user hardware and registered
- Per-domain comparison across all three detectors on the same frozen clips
- Final stopping-rule verdict and the D-14 success statement written down
- Detector documentation updated with its limitations
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-18-SUMMARY.md` when done
</output>
