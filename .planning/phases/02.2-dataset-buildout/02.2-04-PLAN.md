---
phase: 02.2-dataset-buildout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - ffep.toml
  - .env.example
  - .gitignore
  - .dvc/config
  - .dvcignore
  - src/flag_football_ep/config.py
  - tests/test_cv_config.py
  - tests/test_cv_dependencies.py
  - tests/test_dvc_layout.py
  - docs/cv-setup.md
autonomous: false
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "`uv run dvc --version` works after `uv sync --extra versioning`"
    - "A DVC add/push/pull round trip succeeds against a local directory remote, with no cloud credentials"
    - "OTC OBS credentials are referenced by env-var name only; no literal key can enter ffep.toml"
    - "An automated test fails if any source file references a PML-1.0-licensed RF-DETR variant"
  artifacts:
    - path: ".dvc/config"
      provides: "DVC repo config incl. the OTC OBS S3-compatible remote (endpoint only, no credentials)"
      contains: "endpointurl"
    - path: "tests/test_dvc_layout.py"
      provides: "Local-remote smoke test + guard that no .dvc file tracks raw video"
    - path: "src/flag_football_ep/config.py"
      provides: "CvSettings fields dvc_remote_name/dvc_remote_url/dvc_remote_endpoint/otc_obs_access_key_env/otc_obs_secret_key_env"
      contains: "otc_obs_access_key_env"
  key_links:
    - from: "src/flag_football_ep/config.py"
      to: "OTC OBS credentials"
      via: "secret(config.cv.otc_obs_access_key_env)"
      pattern: "secret\\("
---

<objective>
Add dataset versioning (D-18) as installable, testable infrastructure before any dataset exists:
`dvc` + `dvc-s3` in their own extras group, an OTC-OBS-shaped remote configured by endpoint only,
credentials resolved through the existing env-var-name-only `secret()` pattern, a local-remote
round-trip smoke test, and the two supply-chain guards this phase needs (RF-DETR PML variants,
package legitimacy).

Purpose: DVC-on-OTC-OBS is unverified in this environment (RESEARCH Pitfall 3). Validating the DVC
mechanics against a local remote now means the only untested variable later is the cloud endpoint,
and the phase never blocks on credentials that do not exist yet.
Output: a working `dvc` install, `.dvc/config`, five new config fields, three test gates, and the
German setup documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md
@.planning/phases/02.2-dataset-buildout/02.2-PATTERNS.md

<interfaces>
From src/flag_football_ep/config.py (existing):

  @dataclass(frozen=True)
  class CvSettings:  # fields are positional; _CV_KEYS must stay in the same order
      pilot_session_id: str; detector_model: str; detector_experiment: str; resolution: int
      sahi: bool; sahi_slice: int; sahi_overlap: float; train_epochs: int; train_batch_size: int
      train_grad_accum: int; device: str; label_frame_target: int; cvat_host: str
      cvat_username_env: str; cvat_password_env: str
      field_length_yards: float; field_width_yards: float; endzone_yards: float

  def secret(env_var_name: str) -> str   # resolves a value from env/.env; raises ConfigError if unset

Existing secret-name-only precedent (copy exactly):
  ffep.toml: cvat_username_env = "CVAT_USERNAME"
  cv/dataset.py::create_cvat_task: secret(config.cv.cvat_username_env)

Package legitimacy (02.2-RESEARCH.md §Package Legitimacy Audit):
  dvc     3.67.1  Apache-2.0  slopcheck [OK]  — user-locked by D-18
  dvc-s3  3.3.0   Apache-2.0  slopcheck [OK]  — [ASSUMED] (researcher-discovered name)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for dvc-s3 (and dvc) before install</name>
  <files>none — verification only, no file is modified by this task</files>
  <action>
    Pause execution before running any `uv add`/`uv sync` that would install `dvc` or `dvc-s3`. Present the two PyPI pages and the RESEARCH audit result, wait for the user's verdict, and record the confirmed versions and the verdict in the SUMMARY. If the user rejects `dvc-s3`, stop the plan and report back rather than substituting a package on your own judgement.
  </action>
  <what-built>
    Nothing yet — this checkpoint runs before the first `uv add`. RESEARCH tagged `dvc-s3` as
    `[ASSUMED]` because the package name was discovered by the researcher rather than named by you,
    even though registry and slopcheck checks came back `[OK]`.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/dvc-s3/ — confirm: the project links to the DVC/iterative
       project family (homepage or source repo pointing at the DVC organisation, today
       `github.com/treeverse/dvc` after the November 2025 lakeFS acquisition), the release history
       spans years rather than weeks, and the licence is Apache-2.0.
    2. Open https://pypi.org/project/dvc/ — same check for the main package (version 3.67.x,
       Apache-2.0, very high download counts).
    3. Confirm neither page shows the hallmarks of a typosquat: single release, no source repo, a
       description that mentions an unrelated project.
  </how-to-verify>
  <acceptance_criteria>
    - The user states `dvc-s3 verified` (and `dvc verified`), or names an alternative S3-compatible DVC backend
    - The verified versions are recorded in the SUMMARY
    - No install command has run before this checkpoint resolved
  </acceptance_criteria>
  <resume-signal>Type "dvc-s3 verified" or describe what you found on PyPI</resume-signal>
  <verify>
    <automated>uv run pytest tests/test_cv_dependencies.py -x -q</automated>
  </verify>
  <done>The user has confirmed both package identities (or named an alternative), and no install command ran before that confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Install DVC in its own extras group, init the repo, add the config surface</name>
  <files>pyproject.toml, uv.lock, ffep.toml, .env.example, .gitignore, .dvc/config, .dvcignore, src/flag_football_ep/config.py, tests/test_cv_config.py</files>
  <read_first>
    - pyproject.toml lines 41-80 (the `[project.optional-dependencies].cv` block and its per-entry licence-rationale comment convention)
    - src/flag_football_ep/config.py lines 93-180 (CvSettings dataclass, _CV_KEYS tuple) and lines 285-310 (`secret()`)
    - ffep.toml `[cv]` block (the comment style that explains why a setting has its value)
    - .env.example (existing CVAT_USERNAME/CVAT_PASSWORD entries)
    - .gitignore (the `data/video/*` and `data/labels/*` PII rules and their explanatory comments)
    - tests/test_cv_config.py (how CvSettings fields are asserted)
  </read_first>
  <action>
    Add a new extras group `versioning = ["dvc>=3.67.1", "dvc-s3>=3.3.0"]` to pyproject.toml — a
    separate group, not `cv`, so a `dvc pull`-only workflow does not drag in the multi-GB torch
    stack (RESEARCH "Note on install surface"). Follow the file's own convention: a comment block
    above the group naming package, purpose, licence (Apache-2.0 both) and why it is split from
    `cv`. Run `uv sync --extra versioning` and commit the refreshed `uv.lock`.

    Run `dvc init --subdir` is NOT appropriate here (the repo root is the DVC root): run
    `uv run dvc init` at the repo root, which creates `.dvc/config`, `.dvc/.gitignore` and
    `.dvcignore`. Configure the remote without credentials:
    `dvc remote add -d otc-obs s3://<bucket>/flag-football-datasets`,
    `dvc remote modify otc-obs endpointurl https://obs.eu-de.otc.t-systems.com`,
    `dvc remote modify otc-obs region eu-de`. Leave the bucket name as the placeholder
    `s3://ffep-datasets-PLACEHOLDER/flag-football-datasets` with a comment in docs/cv-setup.md that
    plan 02.2-14 replaces it once the real bucket exists. Never run `dvc remote modify --local` with
    a literal key in this plan.

    Extend CvSettings with five appended fields — `dvc_remote_name`, `dvc_remote_url`,
    `dvc_remote_endpoint`, `otc_obs_access_key_env`, `otc_obs_secret_key_env` — appended at the end
    of both the dataclass and `_CV_KEYS` (order matters: `load_config` splats positionally). Add the
    matching keys to `ffep.toml`'s `[cv]` block with the values `otc-obs`,
    `s3://ffep-datasets-PLACEHOLDER/flag-football-datasets`,
    `https://obs.eu-de.otc.t-systems.com`, `OTC_OBS_ACCESS_KEY_ID`, `OTC_OBS_SECRET_ACCESS_KEY`.
    Add the two env-var names (names only, no values) to `.env.example`.

    Extend .gitignore with `data/bundles/*` (+ `!data/bundles/.gitkeep`) carrying the same PII
    rationale comment as the `data/labels/*` rule, since hackathon bundles contain frames and crops.
    Extend tests/test_cv_config.py to assert all five new fields load from ffep.toml and that
    ffep.toml contains no value that looks like a literal AWS/OTC key.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra versioning dvc --version` prints a 3.67.x version
    - `uv run python -c "from flag_football_ep.config import load_config; c=load_config('ffep.toml'); print(c.cv.dvc_remote_endpoint, c.cv.otc_obs_access_key_env)"` prints `https://obs.eu-de.otc.t-systems.com OTC_OBS_ACCESS_KEY_ID`
    - `grep -c "AKIA\|access_key\s*=\|secret_key\s*=" ffep.toml .dvc/config` returns 0 for both files
    - `grep -c "data/bundles" .gitignore` returns >= 1
    - `grep -c "dvc" pyproject.toml` shows the entries inside a `versioning` group, and `grep -c "dvc" ` inside the `cv` group block returns 0
    - `uv run pytest tests/test_cv_config.py -q` passes
  </acceptance_criteria>
  <done>DVC is installed in its own group, the repo is DVC-initialised, and the OTC remote is described by endpoint with credentials referenced by env-var name only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Local-remote round-trip smoke test, PML-variant guard, and the German setup section</name>
  <files>tests/test_dvc_layout.py, tests/test_cv_dependencies.py, docs/cv-setup.md</files>
  <read_first>
    - tests/test_cv_dependencies.py (full — `test_boxmot_absent_from_pyproject`/`test_boxmot_absent_from_uv_lock` are the exact string-absence gate pattern to copy)
    - .dvc/config (as written in Task 2)
    - docs/cv-setup.md `## Installierte Versionen` and `## Pro-Maschine-torch-Falle` (the sections a new `## Dataset-Versionierung (DVC)` section sits beside; note the two-machine `uv sync` caveat that applies to the new group as well)
    - 02.2-RESEARCH.md §Common Pitfalls, Pitfall 2 (PML-1.0 RF-DETR variants) and Pitfall 3 (OTC endpoint uncertainty)
  </read_first>
  <behavior>
    - A DVC add + push + pull round trip against a `tmp_path` directory remote succeeds without any network or credentials
    - `test_rfdetr_pml_variants_absent_from_source` fails if `RFDETRXLarge`, `RFDETR2XLarge` or `rfdetr_plus` appears anywhere under `src/`
    - `test_no_dvc_file_tracks_raw_video` fails if any `*.dvc` file in the repo references a path under `data/video/`
    - `test_versioning_group_lists_dvc` asserts `dvc` and `dvc-s3` are in the `versioning` extras group and absent from `cv`
  </behavior>
  <action>
    Create tests/test_dvc_layout.py. The round-trip test builds a throwaway git+dvc repo inside
    `tmp_path` (`git init`, `dvc init`, write a small text file, `dvc add`, `dvc remote add -d local
    <tmp remote dir>`, `dvc push`, delete the cache and workspace copy, `dvc pull`, assert the
    content matches). Invoke DVC through `subprocess.run([sys.executable, "-m", "dvc", ...])` so the
    test uses the environment's own install, and `pytest.importorskip("dvc")` so the suite still
    passes on a machine without the `versioning` extra. Keep the test under 20 seconds.

    Add the repo-level guards to the same file: no `.dvc` file may reference `data/video/`, and the
    `.dvc/config` remote must carry an `endpointurl` but no `access_key_id`/`secret_access_key`.

    Extend tests/test_cv_dependencies.py with `test_rfdetr_pml_variants_absent_from_source`
    (scanning `src/**/*.py` for `RFDETRXLarge`, `RFDETR2XLarge`, `rfdetr_plus`, failure message
    naming C-06 and PML 1.0) and `test_versioning_group_lists_dvc`. When counting occurrences in a
    file that also documents the forbidden strings in comments, filter comment lines first — never
    assert `== 0` on an unfiltered whole-file grep.

    Append `## Dataset-Versionierung (DVC)` to docs/cv-setup.md (German): install command
    (`uv sync --extra versioning`), why it is a separate group, the chosen layout decision — one
    single growing DVC-tracked dataset directory `data/labels/dataset/` rather than per-iteration
    silos (RESEARCH Open Question 1, planner's call), the relationship between DVC's own MD5 hash
    and the project's existing `dataset_hash()` SHA-256 (both kept, neither replaces the other), the
    placeholder bucket, and the two-machine `uv sync` caveat.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dvc_layout.py tests/test_cv_dependencies.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_dvc_layout.py -q` passes and the round-trip test completes in under 20 s
    - `uv run pytest tests/test_cv_dependencies.py -q` passes, including the new PML guard
    - Introducing the string `RFDETRXLarge` into any file under `src/` makes `tests/test_cv_dependencies.py` fail (verify during development, then remove)
    - docs/cv-setup.md contains a `## Dataset-Versionierung (DVC)` section naming `data/labels/dataset/` as the tracked root and stating that `dataset_hash()` is kept alongside the DVC hash
    - `uv run pytest tests -q` green
  </acceptance_criteria>
  <done>DVC mechanics are proven locally, the licence trap is guarded by a test, and the layout decision is written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → OTC OBS remote | `dvc push` moves label data to third-party object storage |
| PyPI → local environment | New packages enter the dependency tree |
| PII data tree → DVC tracking | DVC could be pointed at raw video by accident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-SC | Tampering | `uv add dvc dvc-s3` | mitigate | slopcheck audit in RESEARCH plus the blocking human legitimacy checkpoint (Task 1) before any install; `[ASSUMED]` name confirmed on pypi.org/project/dvc-s3 |
| T-2.2-10 | Information Disclosure | `.dvc/config`, `ffep.toml` | mitigate | Endpoint/region/bucket only in tracked config; credentials solely via `secret(config.cv.otc_obs_access_key_env)`; test asserts no key-shaped literal in either file |
| T-2.2-11 | Information Disclosure | DVC-tracked paths | mitigate | `test_no_dvc_file_tracks_raw_video` forbids any `.dvc` file referencing `data/video/`; raw footage stays outside DVC entirely |
| T-2.2-12 | Elevation of Privilege (licence) | rfdetr variants | mitigate | `test_rfdetr_pml_variants_absent_from_source` blocks XLarge/2XLarge/`rfdetr_plus` (PML 1.0, violates C-06) |
</threat_model>

<verification>
- `uv run pytest tests -q` green
- `uv run --extra versioning dvc remote list` shows `otc-obs` with the endpoint
- `git status` shows `.dvc/config`, `.dvcignore` and the updated `uv.lock` tracked; `.dvc/cache` ignored
</verification>

<success_criteria>
- DVC installs from a dedicated extras group and round-trips against a local remote
- Five config fields exist; credentials are env-var names only
- PML-variant and raw-video guards are enforced by tests
- The DVC layout decision is documented in German in docs/cv-setup.md
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-04-SUMMARY.md` when done
</output>
