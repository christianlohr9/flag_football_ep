---
phase: 02.2-dataset-buildout
plan: 12
subsystem: cv
tags: [bundle, hackathon, private-test-set, transfer-set, leak-guard, label-vault, polars]

# Dependency graph
requires:
  - phase: 02.2-dataset-buildout
    provides: "02.2-10 build_bundle/bundle_manifest dev-kind implementation, declarative _BundleItem content table, test-kind name-based label-leak guard (T-2.2-28), score_tracks.py; 02.2-08 export_detections_parquet + bundle-inputs pattern; 02.2-06 frozen_eval_clips.csv private_test=true split (D-07); 02.2-07 hackathon_freeze.json pin"
provides:
  - "cv/bundle.py::_test_items/_private_test_clip_numbers/_vault_withheld_labels/_assert_no_label_leak_in_tree -- the private test-set content table (18 private_test=true drone clips), its local gitignored label vault, and a column-level (not just name-based) leak guard"
  - "cv/bundle.py::_transfer_items/_TRANSFER_DOMAINS -- the transfer-set content table (60 GoPro sideline + 51 TV broadcast clips with per-domain detections)"
  - "data/bundles/test-set_2026-09-01_448c681c6e5c.zip -- the built, hashed private test-set bundle (18 clips, 41 files, gitignored)"
  - "data/bundles/transfer-set_2026-09-01_82c955898fe4.zip -- the built, hashed transfer-set bundle (111 clips across 2 domains, 115 files, gitignored)"
  - "data/private/test-labels/{continuity_review.csv,flag_pull_events.csv} -- the withheld-label vault, gitignored, outside every bundle"
  - "data/labels/{2026-08-14_WC-GER-vs-MEX-GOPRO,2026-08-14_WC-USA-vs-AUS-TV}/bundle-inputs/detections.parquet -- real per-domain detections for the transfer material, pinned to the frozen detector run"
  - "docs/hackathon-bundles.md -- completed test-set/transfer-set sections (contents, schemas, leak-guard design, reproduction commands, hashes)"
affects: [02.2-14-otc-delivery, hackathon-challenge-reid]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two independent leak guards, not one: a pre-assembly, declarative, name-based check over the content table (_assert_no_test_kind_label_leak, from plan 02.2-10) PLUS a post-assembly, column-level check that reads every staged file's actual header/schema off disk (_assert_no_label_leak_in_tree, this plan) -- a name-only check alone is defeated by a renamed file, so the column check reads real headers instead of trusting filenames"
    - "Local gitignored vault pattern for withheld labels (data/private/test-labels/), written by the same build that omits them from the shipped archive -- keeps the organizer's own post-event grading material available without it ever entering a bundle's staging tree or git history"
    - "Per-kind dispatch inlined into build_bundle (dev/test/transfer each resolve their own clip set and call their own _*_items function) replacing the prior single _content_table dispatcher, since the three kinds diverge too much (single session vs. two cross-domain sessions, pool filter vs. private_test filter vs. no filter) for one shared signature"

key-files:
  created:
    - data/bundles/test-set_2026-09-01_448c681c6e5c.zip (gitignored)
    - data/bundles/transfer-set_2026-09-01_82c955898fe4.zip (gitignored)
    - data/private/test-labels/continuity_review.csv (gitignored)
    - data/private/test-labels/flag_pull_events.csv (gitignored)
    - data/labels/2026-08-14_WC-GER-vs-MEX-GOPRO/bundle-inputs/detections.parquet (gitignored)
    - data/labels/2026-08-14_WC-USA-vs-AUS-TV/bundle-inputs/detections.parquet (gitignored)
  modified:
    - src/flag_football_ep/cv/bundle.py
    - tests/test_cv_bundle.py
    - .gitignore (data/private/* added)
    - docs/hackathon-bundles.md

key-decisions:
  - "Task 2's checkpoint:human-action (transfer-domain inference) was run directly instead of stopping for the user, per this session's explicit orchestrator override: the plan's own throughput documentation (docs/cv-setup.md's measured ~11 min for 61 similarly-sized pilot clips) put the combined 111-clip GoPro+TV run well under the 45-minute threshold that authorized running it in-session. Measured actual wall clock: sideline (60 clips) ~10 min, broadcast (51 clips) ~9 min, ~19 min combined -- confirming the estimate. The second-drone-game question (the other half of that checkpoint) was answered from the orchestrator-locked D-07 fallback context (no second game exists as of 2026-09-01), not from new user input."
  - "Transfer-set content-table implementation (_transfer_items, _TRANSFER_DOMAINS) was added during Task 1's bundle.py edit, not deferred to Task 3, even though the plan's Task 1 text names only the test-set. Task 3's own action requires running `ffep cv bundle --kind transfer`, which cannot succeed without the transfer content table existing in code -- this is a Rule 3 blocking-issue fix, implemented while already restructuring the same declarative-content-table machinery for the test-set rather than as a second pass over the same file."
  - "gt_positions.csv is excluded from the test-set content table entirely (not shipped, not vaulted) -- it is not one of the two files plan 02.2-10 named as the label tables to split (continuity_review.csv/flag_pull_events.csv), and the plan's own <interfaces> block explicitly lists 'NO gt positions' for the test set. Ground-truth foot positions for the withheld clips are exactly what a position-accuracy score would need, so they stay withheld from every bundle -- a narrower, more conservative reading than the dev set's pool-only gt_positions.csv."
  - "The per-domain inference settings question (docs/dataset-plan.md ## 4 vs. ffep.toml's drone defaults) resolved to 'no override needed': all three capture domains (drone/sideline/broadcast) independently measured into the same 20-40px band and share resolution=896/sahi=false, which is also ffep.toml's existing [cv] value. The transfer detections were still run without silently assuming this -- docs/dataset-plan.md's table was read and cross-checked against ffep.toml before running, not asserted from memory."
  - "_transfer_items wraps frames.clip_paths' ClipNotFound in BundleError (a pre-existing gap: _dev_items/_test_items rely on their own pool/private_test pre-validation before reaching clip_paths, but _transfer_items has no equivalent pre-check since transfer material isn't part of the eval-split). This keeps bundle.py's documented error contract (\"raises BundleError\") consistent across all three kinds; caught by test_build_bundle_transfer_missing_domain_detections_raises."

requirements-completed: [REQ-S2-03]

# Metrics
duration: ~56min
completed: 2026-09-01
---

# Phase 02.2 Plan 12: Private Test-Set + Transfer-Set Bundles Summary

**Private test-set bundle (18 withheld drone clips, D-07 fallback, content hash `448c681c...`, 41 files) with withheld labels vaulted to a local gitignored directory instead of shipped, plus a transfer-set bundle (60 GoPro + 51 TV clips, content hash `82c9558...`, 115 files) built from freshly run per-domain detections — both verified leak-free against a new column-level guard that reads every staged file's actual schema rather than trusting filenames.**

## Performance

- **Duration:** ~56 min
- **Started:** 2026-09-01T12:26:00Z (approx., prior wave-5 tracking commit)
- **Completed:** 2026-09-01T13:22:32Z
- **Tasks:** 3 (Task 1 `auto`/`tdd="true"`, Task 2 `checkpoint:human-action` resolved directly per orchestrator override, Task 3 `auto`)
- **Files modified:** 4 tracked (bundle.py, test_cv_bundle.py, .gitignore, docs/hackathon-bundles.md) + 6 gitignored real artifacts (2 bundles, 2 vault CSVs, 2 detections Parquets)

## Accomplishments

- `cv/bundle.py` gained the two remaining content tables promised by this plan: `_test_items` (clips/detections/baseline-tracks/overlays/homography for the 18 `private_test = true` drone clips, resolved from `frozen_eval_clips.csv` — never a hard-coded list) and `_transfer_items` (all 60 GoPro sideline + 51 TV broadcast clips with per-domain detections, no tracks/overlays/labels per the challenge doc's own Benchmark-Design table)
- Two independent leak guards protect the test-kind build: the pre-assembly, name-based check from plan 02.2-10, plus a new post-assembly, column-level guard (`_assert_no_label_leak_in_tree`) that reads every staged CSV/Parquet's actual header/schema off disk and fails on `verdict`/`id_switches`/`reviewer_note`/`pull_time_s`/`carrier_track_id`/`puller_track_id` regardless of filename — proven by a test that renames a label file to `notes.csv` and confirms the guard still catches it
- `_vault_withheld_labels` writes the 18 withheld clips' continuity/flag-pull rows to `data/private/test-labels/` (new gitignored path) at test-build time, atomically, outside every bundle's staging tree
- Ran the transfer-domain detection pass directly on this machine (M5 Max, `mps` backend): sideline (GoPro, 60 clips, 798 s footage) in ~10 min producing 155,732 detection rows; broadcast (TV, 51 clips, 633 s footage) in ~9 min producing 275,539 rows — both carrying `detector_run_id = 87a8a5222f7a472787875e974d089c44`, matching the frozen pin exactly
- 11 new tests added to `tests/test_cv_bundle.py` (19 total, up from 8): test-set content/exclusion/vault-correctness/dev-test-disjointness, the renamed-label-file leak-guard catch (and a clean-tree pass-through), and full transfer-set build/error-path coverage against a new `_populate_transfer_fixture` helper
- Real builds: `data/bundles/test-set_2026-09-01_448c681c6e5c.zip` (18 clips, 41 files, ~607 MB) and `data/bundles/transfer-set_2026-09-01_82c955898fe4.zip` (111 clips across 2 domains, 115 files, ~543 MB); verified by unzipping and grepping every CSV for `verdict` (no match) and reading every Parquet's schema for the six leak columns (none present)
- `docs/hackathon-bundles.md` completed: full test-set and transfer-set sections (contents, directory layout, schemas, leak-guard design, label-vault documentation, reproduction commands, measured hashes/file counts/wall-clock times, per-domain tier/apparent-player-size table quoted from `docs/material-sighting.md`)

## Task Commits

1. **Task 1: Test-set content table, label vault, leak guard (+ transfer content table)** - `dc6345c` (feat)
2. **Task 2: Transfer-domain detection pass** - no commit (gitignored `data/labels/` output only, per plan's own `<files>` declaration; run directly this session, see Deviations)
3. **Task 3: Build both bundles, complete documentation** - `b497a3b` (docs)

**Plan metadata:** (this commit, following SUMMARY.md write)

## Files Created/Modified

- `src/flag_football_ep/cv/bundle.py` — `_private_test_clip_numbers`, `_test_items`, `_transfer_items`, `_vault_withheld_labels`, `_assert_no_label_leak_in_tree`, `_render_test_readme`, `_render_transfer_readme`, `_LEAK_COLUMN_NAMES`, `_TRANSFER_DOMAINS`, `_TEST_LABEL_VAULT_DIRNAME`; `build_bundle` restructured to dispatch per kind instead of through the old single `_content_table` function
- `tests/test_cv_bundle.py` — 11 new tests, `_populate_transfer_fixture` helper, obsolete `test_build_bundle_transfer_kind_not_implemented`/`test_build_bundle_test_kind_refuses_label_bearing_file` replaced with real-behavior equivalents (transfer is now implemented; test-kind no longer refuses everything, it ships a real narrower content table)
- `.gitignore` — `data/private/*` (+ `.gitkeep` exception) added, same PII-adjacent policy comment style as `data/video/*`/`data/labels/*`
- `docs/hackathon-bundles.md` — test-set and transfer-set sections completed (were "AUSSTEHEND" placeholders), header status line updated
- `data/bundles/test-set_2026-09-01_448c681c6e5c.zip`, `data/bundles/transfer-set_2026-09-01_82c955898fe4.zip` — real built archives (gitignored)
- `data/private/test-labels/{continuity_review.csv,flag_pull_events.csv}` — real withheld-label vault (gitignored)
- `data/labels/2026-08-14_WC-GER-vs-MEX-GOPRO/bundle-inputs/detections.parquet`, `data/labels/2026-08-14_WC-USA-vs-AUS-TV/bundle-inputs/detections.parquet` — real transfer-domain detections (gitignored)

## Decisions Made

See frontmatter `key-decisions` for full rationale on: running Task 2's inference directly instead of checkpointing (throughput-based orchestrator override, confirmed by measurement); implementing the transfer content table during Task 1 (Rule 3 blocking-issue fix, needed for Task 3 to be executable at all); excluding `gt_positions.csv` entirely from the test-set (narrower than the dev set, per the plan's own interfaces block); confirming per-domain inference settings need no override (measured coincidence, checked not assumed); and wrapping `ClipNotFound` as `BundleError` in `_transfer_items` for error-contract consistency.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Transfer-set content table implemented in Task 1, not deferred**
- **Found during:** Task 1 (reading Task 3's own action text, which runs `ffep cv bundle --kind transfer` with no `<files>` entry for `bundle.py`)
- **Issue:** Task 3 cannot build a transfer bundle without `_transfer_items`/`_TRANSFER_DOMAINS` existing in `cv/bundle.py` — the prior state (`_content_table` raising `BundleError` naming "plan 02.2-12 -- no sideline/broadcast detections exist yet") would make Task 3's own action fail outright.
- **Fix:** Implemented `_transfer_items` and `_TRANSFER_DOMAINS` alongside the test-set work in Task 1's single `bundle.py` edit, since both are the same declarative-content-table refactor already underway.
- **Files modified:** `src/flag_football_ep/cv/bundle.py`, `tests/test_cv_bundle.py`
- **Verification:** `test_build_bundle_transfer_creates_archive_with_per_domain_detections` and `test_build_bundle_transfer_missing_domain_detections_raises`, both passing; the real `ffep cv bundle --kind transfer` run in Task 3 succeeded on the first attempt.
- **Committed in:** `dc6345c` (Task 1 commit)

**2. [Rule 1 - Bug] `_transfer_items` let `ClipNotFound` escape instead of `BundleError`**
- **Found during:** Task 1 (writing `test_build_bundle_transfer_missing_domain_detections_raises`)
- **Issue:** `frames.clip_paths` raises `ClipNotFound` (a `CvError`, not a `BundleError`) when no clips are registered for a domain/session. Every other guard in this module raises `BundleError`, and `build_bundle`'s own docstring documents "raises BundleError" as the module's error contract.
- **Fix:** Wrapped the `frames.clip_paths` call in `_transfer_items` in a `try`/`except frames.ClipNotFound` that re-raises as `BundleError`.
- **Files modified:** `src/flag_football_ep/cv/bundle.py`
- **Verification:** `test_build_bundle_transfer_missing_domain_detections_raises` passes.
- **Committed in:** `dc6345c` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug). Both necessary for Task 3 to be executable and for the module's error contract to hold; no scope creep beyond what Task 3's own action already required.

## Authentication Gates

None encountered.

## Checkpoint Resolution (Task 2)

Task 2 was declared `type="checkpoint:human-action"` in the plan, anticipating a multi-hour GPU job the user would need to run outside the agent session (D-21). This session's orchestrator explicitly authorized running it directly instead, conditioned on the plan's own documented throughput keeping the job under ~45 minutes — which it did (measured ~19 min combined). The other half of that checkpoint, the second-drone-game question, was answered from the orchestrator-locked D-07 fallback context rather than new user input (no second drone game exists as of 2026-09-01). No user interaction occurred for this task; it is documented here as a checkpoint resolved by explicit prior authorization, not skipped silently.

**Detections produced (row counts and provenance):**

| Domain | Session | Clips | Rows | `detector_run_id` | Wall clock |
|---|---|---:|---:|---|---|
| sideline (GoPro) | `2026-08-14_WC-GER-vs-MEX-GOPRO` | 60/60 | 155,732 | `87a8a5222f7a472787875e974d089c44` | ~10 min |
| broadcast (TV) | `2026-08-14_WC-USA-vs-AUS-TV` | 51/51 | 275,539 | `87a8a5222f7a472787875e974d089c44` | ~9 min |

Both `detector_run_id` values match `data/reference/hackathon_freeze.json`'s pin exactly (verified via `df["detector_run_id"].unique()`, not just spot-checked).

## Known Stubs

None. All shipped bundle content is real data produced this session (no placeholder rows, no empty defaults reaching a shipped artifact).

## Threat Flags

None beyond what the plan's own `<threat_model>` already covers (T-2.2-35/36/37) — no new network endpoints, auth paths, or trust-boundary-crossing file access were introduced.

## Issues Encountered

- Two consecutive full-suite `uv run pytest tests -q` runs (once via `nohup ... &`, once as a direct foreground-then-auto-backgrounded run) both terminated their captured log file immediately after the `-- Docs: https://docs.pytest.org/...` warnings-summary footer, with no final `N passed in Ts` line ever written to either log file — despite pytest's own `$?` being captured and printed as `0` (success) directly after the command in both cases. Investigated: no `\r`-based overwrite, no crash signal, no failing/erroring test anywhere in either log, `pytest --version` shows a clean single-plugin (`anyio`) install with no timeout/xdist plugin that could swallow the summary. Concluded this is a cosmetic quirk of `pytest 9.1.1 -q` under this session's non-TTY captured redirection when the warnings section is large, not a real failure — the `$?`-captured exit code of `0` is the authoritative signal, corroborated by the identical, complete, `test_cv_bundle.py`-scoped run (which does print its 19-passed summary line cleanly) and the two real bundle-build/inference runs all succeeding.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All three hackathon data packages (dev, test, transfer) now exist, are hashed, and are documented in `docs/hackathon-bundles.md`. `cv/bundle.py::build_bundle` is feature-complete for all three `BUNDLE_KINDS`.
- Plan 02.2-14 (OTC OBS delivery) can now `deliver_bundle` all three real archives once implemented — nothing in this plan blocks it.
- If a second drone game arrives before the hackathon (23.–27. November 2026), the test-set must be rebuilt: re-freeze `frozen_eval_clips.csv` for the new session, which returns the 18 pilot clips to the dev pool and requires a dev-set rebuild too (documented explicitly in `docs/hackathon-bundles.md`'s closing section).
- The transfer-set's optional "continuity judgments on a sample" (mentioned as "falls Zeit" in the challenge doc's Benchmark-Design table) remains unimplemented — not a blocker, explicitly scoped out of this bundle per the challenge doc's own wording.
- No blockers for wave 6's remaining work or for phase completion.

## Verification

- `uv run pytest tests/test_cv_bundle.py -x -q` — 19 passed (11 new)
- `uv run pytest tests/test_cv_export.py -x -q` — 21 passed, unaffected (Task 2's own stated verification)
- `uv run pytest tests -q` — exit code 0 (captured via `$?` directly), no failures in either full run (see Issues Encountered for the cosmetic missing-summary-line note)
- `uv run ruff check src/flag_football_ep/cv/bundle.py tests/test_cv_bundle.py` — clean
- Real run: `uv run --extra cv ffep cv bundle --kind test --out data/bundles` — `test-set_2026-09-01_448c681c6e5c.zip` (41 files, content_sha256 `448c681c6e5c6945e67e78d3007642dc32e2fc42990525ab87fe51d0ec342811`)
- Real run: `uv run --extra cv ffep cv bundle --kind transfer --out data/bundles` — `transfer-set_2026-09-01_82c955898fe4.zip` (115 files, content_sha256 `82c955898fe4fddac50557fac6e11537783b62e19156c1d5819cd5eba853bdc1`)
- `git status --porcelain data/bundles data/private` — empty (gitignore boundary respected)
- Unzipped test archive: `grep -r verdict` over all CSVs — no match; `pl.read_parquet_schema` over `detections.parquet`/`tracks.parquet` — no leak columns
- Vault (`data/private/test-labels/*.csv`) contains exactly the 18 `private_test = true` clip numbers (5, 6, 7, 11, 15, 16, 21, 22, 28, 33, 36, 40, 43, 49, 52, 54, 55, 56), disjoint from the 43 dev-set pool clips
- Transfer archive: 60 `sideline/clips/*.mp4` + 51 `broadcast/clips/*.mp4`, both `detections.parquet` files' `detector_run_id` equal to the frozen pin

## Self-Check: PASSED

- `src/flag_football_ep/cv/bundle.py` — FOUND, contains `_test_items`/`_transfer_items`/`_vault_withheld_labels`/`_assert_no_label_leak_in_tree`
- `tests/test_cv_bundle.py` — FOUND, 19 tests present and passing
- `docs/hackathon-bundles.md` — FOUND, both AUSSTEHEND placeholders replaced with real content/hashes
- `data/bundles/test-set_2026-09-01_448c681c6e5c.zip` — FOUND on disk (gitignored)
- `data/bundles/transfer-set_2026-09-01_82c955898fe4.zip` — FOUND on disk (gitignored)
- `data/private/test-labels/continuity_review.csv`, `data/private/test-labels/flag_pull_events.csv` — FOUND on disk (gitignored)
- `data/labels/2026-08-14_WC-GER-vs-MEX-GOPRO/bundle-inputs/detections.parquet`, `data/labels/2026-08-14_WC-USA-vs-AUS-TV/bundle-inputs/detections.parquet` — FOUND on disk (gitignored)
- Commits `dc6345c`, `b497a3b` — both FOUND in `git log --oneline`
- All plan `<acceptance_criteria>` re-checked this session for Tasks 1-3: pass (see Verification above)
- Plan-level `<verification>` block re-run: `tests/test_cv_bundle.py -q` green (19 passed), `tests -q` exit code 0, unzipped test bundle confirmed label-free

---
*Phase: 02.2-dataset-buildout*
*Completed: 2026-09-01*
