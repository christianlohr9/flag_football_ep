---
phase: 02.2-dataset-buildout
plan: 12
type: execute
wave: 6
depends_on: ["02.2-10"]
files_modified:
  - src/flag_football_ep/cv/bundle.py
  - tests/test_cv_bundle.py
  - docs/hackathon-bundles.md
autonomous: false
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "A private test-set bundle exists containing clips, detections and baseline tracks but no labels"
    - "The withheld labels live outside every bundle, in a local vault that is not tracked and not shipped"
    - "A transfer-set bundle exists with sideline and broadcast clips plus detections produced with per-domain inference settings"
  artifacts:
    - path: "docs/hackathon-bundles.md"
      provides: "Completed test and transfer sections with content hashes"
      contains: "test-set"
  key_links:
    - from: "src/flag_football_ep/cv/bundle.py"
      to: "data/reference/frozen_eval_clips.csv"
      via: "private_test rows define the test-set clip list"
      pattern: "private_test"
---

<objective>
Complete the hackathon data packages: the private test set from the 18 withheld drone clips (D-07,
with a second drone game used instead if one materialises), and the transfer set from the sideline
and broadcast material — each with detections produced under its own per-domain inference settings.

Purpose: without a private test set every team optimises against the dev clips. Without a transfer
set the challenge cannot answer the question that decides practical value: how much of the
improvement survives a camera change?
Output: two more bundles, their hashes, and the completed German bundle documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-10-SUMMARY.md
@.planning/phases/02.2-dataset-buildout/02.2-06-SUMMARY.md
@docs/hackathon-bundles.md
@docs/hackathon-challenge-reid.md
@docs/material-sighting.md

<interfaces>
Bundle contents by kind (this plan fills the two pending tables in cv/bundle.py):

  test set     — the 18 clips with `private_test = true` in data/reference/frozen_eval_clips.csv
                 (or, if a second drone session exists, that session's clips instead):
                 clips, detections Parquet, baseline BoT-SORT tracks, overlays, homography
                 calibration. NO continuity verdicts, NO flag-pull events, NO gt positions.
  transfer set — sideline (2026-08-14_WC-GER-vs-MEX-GOPRO) and broadcast
                 (2026-08-14_WC-USA-vs-AUS-TV) clips plus detections Parquet per domain.

Per-domain inference settings come from docs/dataset-plan.md (derived from
cv/sighting.py::recommend_inference_settings), not from the drone defaults in ffep.toml.

Label vault (never tracked, never bundled):
  data/private/test-labels/   — withheld continuity verdicts and flag-pull rows for the test clips
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-set content table, label vault and the leak guard</name>
  <files>src/flag_football_ep/cv/bundle.py, tests/test_cv_bundle.py</files>
  <read_first>
    - src/flag_football_ep/cv/bundle.py (the builder and its per-kind content table from plan 02.2-10, including the existing label-refusal check)
    - data/reference/frozen_eval_clips.csv (`private_test` rows)
    - data/reference/continuity_review.csv and data/reference/flag_pull_events.csv (the label tables to split)
    - .gitignore (where the vault path must be excluded)
  </read_first>
  <behavior>
    - The test-set content table resolves its clip list from `private_test = true` rows, never from a hard-coded list
    - Building the test bundle writes the withheld label rows to `data/private/test-labels/` and excludes them from the archive
    - The builder raises `BundleError` if any file in the assembled test archive matches a label-table name or contains a `verdict`/`pull_time_s` column
    - `data/private/` is gitignored
  </behavior>
  <action>
    Fill the `test` content table in `cv/bundle.py`, resolving clips from `frozen_eval_clips.csv`.
    Add a post-assembly leak guard that walks the staged archive tree and fails on any CSV/Parquet
    carrying a `verdict`, `id_switches`, `reviewer_note`, `pull_time_s`, `carrier_track_id` or
    `puller_track_id` column — a name-based check alone is too weak, since a renamed file would slip
    through. Write the withheld rows to `data/private/test-labels/` with an atomic write, and add
    `data/private/*` (plus a `.gitkeep` exception) to `.gitignore` with the same PII-rationale
    comment style the file already uses.

    Extend tests/test_cv_bundle.py: a staged test bundle containing a renamed label file fails the
    guard; the vault receives exactly the withheld rows; the dev bundle and the test bundle share no
    clip number.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_bundle.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A test proves the column-level leak guard catches a label file renamed to `notes.csv`
    - A test asserts the dev-set clip list and the test-set clip list are disjoint
    - `grep -c "data/private" .gitignore` returns >= 1
    - `uv run pytest tests/test_cv_bundle.py -q` passes with at least 4 new cases
  </acceptance_criteria>
  <done>The private test set can be built without any path by which its labels reach a participant.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the transfer-domain detection pass on the user's machine</name>
  <files>none — the artifacts land under the gitignored data/labels/ tree</files>
  <action>
    Pause execution and hand the user the two exact commands below, together with the per-domain resolution and SAHI values from docs/dataset-plan.md. Do not start the multi-hour inference inside the agent session (D-21). When the user reports back, verify both Parquet files exist, check that every row's `detector_run_id` matches the freeze pin, and record the row counts, wall clock and the second-drone-game answer.
  </action>
  <what-built>
    The detections export verb and the per-domain inference settings are in place; this pass is a
    long-running inference job (111 clips across two domains) that belongs on your hardware, not
    inside an agent session (D-21).
  </what-built>
  <how-to-verify>
    1. Run, with the settings from `docs/dataset-plan.md` (do not fall back to the drone defaults):
       `uv run --extra cv ffep cv detections --session 2026-08-14_WC-GER-vs-MEX-GOPRO --domain sideline --out data/labels/2026-08-14_WC-GER-vs-MEX-GOPRO/bundle-inputs/detections.parquet`
       `uv run --extra cv ffep cv detections --session 2026-08-14_WC-USA-vs-AUS-TV --domain broadcast --out data/labels/2026-08-14_WC-USA-vs-AUS-TV/bundle-inputs/detections.parquet`
    2. Expect roughly the per-clip runtime documented in `docs/cv-setup.md ## Inferenz-Durchsatz`,
       scaled to 60 and 51 clips; the Dell CUDA box or the M5 Max both work.
    3. Report the wall-clock time and the row counts printed by each command.
    4. If a second drone game has arrived by now, say so — the test set then uses that session
       instead of the 18 withheld clips, and the withheld clips return to the dev pool.
  </how-to-verify>
  <acceptance_criteria>
    - Both detection Parquet files exist with non-zero row counts, reported in the SUMMARY
    - Each file's rows carry `detector_run_id` equal to the freeze pin's run id
    - The user reports whether a second drone session exists (yes/no), and the answer is recorded in docs/hackathon-bundles.md
  </acceptance_criteria>
  <resume-signal>Type "detections done" with the two row counts, plus "second game: yes/no"</resume-signal>
  <verify>
    <automated>uv run pytest tests/test_cv_export.py -x -q</automated>
  </verify>
  <done>Both transfer-domain detection Parquet files exist with verified provenance, and the second-drone-game question is answered on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Build both bundles and complete the German bundle documentation</name>
  <files>docs/hackathon-bundles.md</files>
  <read_first>
    - docs/hackathon-bundles.md (the dev section written by plan 02.2-10 and the two pending sections)
    - docs/material-sighting.md (the tier verdicts that explain what teams can expect from each transfer domain)
    - docs/hackathon-challenge-reid.md §Benchmark-Design (the promised per-set contents)
  </read_first>
  <action>
    Run `uv run --extra cv ffep cv bundle --kind test --out data/bundles` and
    `uv run --extra cv ffep cv bundle --kind transfer --out data/bundles`. Record each archive's
    name, content hash, file count and size.

    Complete docs/hackathon-bundles.md: fill the test and transfer sections with contents, layout,
    schemas, reproduction commands and hashes; state explicitly that the test-set labels live in
    `data/private/test-labels/` and are released only after the event; state, for the transfer set,
    the measured tier and apparent player size per domain so teams know what they are looking at;
    and note whether a second drone session replaced the withheld-clip fallback.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_bundle.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Test and transfer archives exist under `data/bundles/`, and `git status --porcelain data/bundles data/private` is empty
    - Unzipping the test archive and grepping for `verdict` across all its files yields no match
    - docs/hackathon-bundles.md lists a content hash and file count for all three bundle kinds
    - The transfer section quotes the per-domain tier and apparent player size from docs/material-sighting.md
  </acceptance_criteria>
  <done>All three hackathon data packages exist, are hashed, are documented, and the private labels are outside every one of them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| withheld labels → any shipped artifact | The whole value of the private test set |
| PII footage of two World Cup games → participants | Covered by the 2026-08-31 federation approval, purpose-bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-35 | Information Disclosure | test-set bundle | mitigate | Column-level leak guard over the staged tree plus an unzip-and-grep acceptance check; labels written to a gitignored vault outside the bundle tree |
| T-2.2-36 | Information Disclosure | transfer-set footage | mitigate | Distribution rests on the documented federation approval (docs/capture-legal.md Nachtrag 2026-08-31); the bundle README repeats the purpose-bound, no-redistribution, delete-after-event terms |
| T-2.2-37 | Tampering | per-domain inference settings | mitigate | Settings are read from docs/dataset-plan.md; the acceptance check verifies the detections carry the frozen run id, so a stray drone-default run is visible |
</threat_model>

<verification>
- `uv run pytest tests/test_cv_bundle.py -q` green
- `uv run pytest tests -q` green
- Unzipped test bundle contains no label column

</verification>

<success_criteria>
- Test and transfer bundles built, hashed, documented
- Withheld labels vaulted outside every archive and outside git
- Transfer detections produced with per-domain settings on user hardware
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-12-SUMMARY.md` when done
</output>
