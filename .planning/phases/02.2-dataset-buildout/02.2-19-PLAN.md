---
phase: 02.2-dataset-buildout
plan: 19
type: execute
wave: 11
depends_on: ["02.2-14", "02.2-18", "02.2-20"]
files_modified:
  - docs/dataset-publication.md
  - docs/dataset-card.md
  - docs/dataset-buildout.md
  - tests/test_phase22_docs.py
autonomous: false
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "The publication option is assessed in writing: platform, licence, contents, timing, and the legal basis"
    - "A dataset card documents composition, splits, labelling convention, provenance hashes and limitations"
    - "The licence choice is confirmed by the user, not decided by the researcher"
    - "The phase's closing record states what exists, what was deferred, and what the post-hackathon gate re-run inherits"
  artifacts:
    - path: "docs/dataset-publication.md"
      provides: "Publication assessment: platforms, licence recommendation, PII basis, publication checklist"
      min_lines: 70
    - path: "docs/dataset-card.md"
      provides: "Dataset card: composition, splits, convention, hashes, limitations, intended use"
      min_lines: 70
  key_links:
    - from: "docs/dataset-publication.md"
      to: "docs/capture-legal.md"
      via: "the 2026-08-31 federation approval as the publication basis"
      pattern: "2026-08-31"
---

<objective>
Close the phase: assess the publication option (D-19 — assessment now, publication after the
hackathon), settle the licence question with the user rather than for them (RESEARCH assumption
A5), write the dataset card that any publication or hand-over needs, and record what this phase
produced and what the post-hackathon steps inherit.

Purpose: REQ-S2-03's third success criterion is "the publication option has been assessed". This
plan produces that assessment — and stops short of publishing, deliberately.
Output: a German publication assessment, a dataset card, a user-confirmed licence choice, and the
phase closing record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md
@.planning/phases/02.2-dataset-buildout/02.2-18-SUMMARY.md
@docs/capture-legal.md
@docs/dataset-buildout.md
@docs/cv-setup.md

<interfaces>
Locked constraints this assessment works within:
  D-19  publication happens AFTER the hackathon; this phase assesses and prepares only
  D-20  full frames are published (no blurring, no crops-only); licence choice is open
  D-07  the 18 private-test clips and their labels stay withheld until after the event
  D-06  federation approval (2026-08-31) covers hackathon use, TV-material distribution and publication
  A5    a CC BY-NC-4.0-style licence is the researcher's default recommendation, explicitly flagged
        as needing user confirmation — it is a rights decision about identifiable persons, not a
        technical one

Platforms to compare (RESEARCH, LOW confidence — surface-level, state that):
  HuggingFace Datasets, Zenodo, Roboflow Universe

Provenance to cite in the dataset card:
  dataset `content_sha256` per version, the DVC pointer commits, the detector run ids,
  the frozen eval split file, the labelling convention in docs/cv-setup.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the publication assessment</name>
  <files>docs/dataset-publication.md</files>
  <read_first>
    - docs/capture-legal.md (the DSGVO posture and the 2026-08-31 approval Nachtrag)
    - docs/hackathon-challenge-reid.md §Datenschutz (the participant-facing terms already agreed)
    - .planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md §Assumptions Log (A5) and §State of the Art
    - docs/dataset-buildout.md (what the dataset actually contains after two iterations)
  </read_first>
  <action>
    Write docs/dataset-publication.md in German with a dated status header. Sections:

    (1) Was veröffentlicht würde: full frames plus COCO annotations per domain, the split
    definition, and explicitly what is withheld (the private test clips and their labels until after
    the event, the raw video where it is not part of the frame set).

    (2) Rechtsgrundlage: the federation approval of 2026-08-31 as recorded in docs/capture-legal.md,
    what it covers and what it does not, plus the standing project rule that this document is not
    legal advice.

    (3) Plattformvergleich: HuggingFace Datasets, Zenodo, Roboflow Universe — each with storage
    model, licence handling, citability (DOI), discoverability for the CV community, and the
    practical cost of maintaining it solo. Mark the comparison as surface-level research
    (RESEARCH rates it LOW confidence) rather than a decided evaluation.

    (4) Lizenzempfehlung: CC BY-NC 4.0 as the recommendation with its reasoning (non-commercial
    limits secondary exploitation of identifiable persons' images while keeping research value),
    the alternatives (CC BY 4.0, CC BY-NC-SA 4.0, a custom research-use licence) and what each would
    mean. State clearly that this is a recommendation pending the user's decision.

    (5) Zeitpunkt: after the hackathon (D-19), with the reasoning (dataset plus benchmark is a
    stronger package; no conflict with the private test set).

    (6) Checkliste für den Veröffentlichungszeitpunkt: the concrete steps to execute later
    (finalise licence file, produce the English dataset card, strip the withheld clips, publish
    hashes, register a DOI if Zenodo, announce).
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All six sections are present and the platform comparison covers all three platforms with the same criteria
    - The licence section names at least three options and marks the recommendation as pending user confirmation
    - The document cites `docs/capture-legal.md` and the date 2026-08-31 as the legal basis
    - The publication checklist has at least five concrete, executable steps
  </acceptance_criteria>
  <done>The publication option is assessed on paper, with the decision left where it belongs.</done>
</task>

<task type="auto">
  <name>Task 2: Write the dataset card</name>
  <files>docs/dataset-card.md, tests/test_phase22_docs.py</files>
  <read_first>
    - docs/dataset-buildout.md (per-domain and per-split counts, hashes, both iterations)
    - docs/cv-setup.md `### Datensatz` (the labelling convention and the single-annotator caveat) and the new multi-domain training section
    - docs/material-sighting.md (per-domain capture characteristics: apparent player size, tiers, camera positions)
    - data/reference/frozen_eval_clips.csv (the split definition to describe)
  </read_first>
  <action>
    Write docs/dataset-card.md in German (an English translation is produced at publication time —
    state that at the top). Sections: purpose and intended use; sources and capture conditions per
    domain with the measured apparent player size; composition (images, boxes per class and per
    domain, splits); the split methodology (clip-level, never frame-level, with the reason:
    adjacent frames are near-duplicates; game-level splits are impossible with one game per domain);
    the labelling convention verbatim in substance; provenance (dataset `content_sha256` per
    version, DVC pointer commits, detector run ids); known limitations (single annotator, no
    inter-annotator agreement, one game per domain, missing conditions such as rain and floodlight,
    the polygon-to-box artifact in the pilot seed if it was included); privacy statement (full
    frames of identifiable persons, the approval basis, the withheld clips); and a licence
    placeholder pointing at docs/dataset-publication.md.

    Extend tests/test_phase22_docs.py with assertions that both new documents exist and contain
    their required section headings, so a later edit cannot silently gut them.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Every composition figure in the card matches the corresponding figure in docs/dataset-buildout.md (spot-checked and stated in the SUMMARY)
    - The limitations section names at least four distinct limitations
    - The split section states the clip-level rule and its reason
    - `uv run pytest tests/test_phase22_docs.py -q` passes with new assertions covering both documents
  </acceptance_criteria>
  <done>Anyone receiving this dataset can tell what it is, how it was built and where it is weak.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Licence decision and phase closing record</name>
  <files>docs/dataset-publication.md, docs/dataset-buildout.md</files>
  <action>
    Pause execution and present the four licence options below with the assessment's reasoning. Do not pick a licence on the user's behalf — RESEARCH assumption A5 flags this as a rights decision. When the user selects, record the choice as a dated decision in docs/dataset-publication.md, reflect it in the dataset card's licence section, and then append the phase closing section to docs/dataset-buildout.md listing deliverables, hashes, delivery state and every deferred item with its inheritor.
  </action>
  <decision>Which licence the dataset carries when it is published after the hackathon</decision>
  <context>
    The dataset shows identifiable persons in full frames (D-20), covered by the federation approval
    of 2026-08-31. The researcher's recommendation is CC BY-NC 4.0, but RESEARCH explicitly flagged
    this as a rights decision that needs your confirmation rather than a technical default (A5). The
    assessment in docs/dataset-publication.md lays out the options; publication itself happens after
    the hackathon either way.
  </context>
  <options>
    <option id="cc-by-nc-4">
      <name>CC BY-NC 4.0 (recommendation)</name>
      <pros>Research and teaching use stays free; commercial re-exploitation of players' images is excluded; standard, well-understood licence</pros>
      <cons>Excludes commercial research labs; "non-commercial" is famously fuzzy at the edges</cons>
    </option>
    <option id="cc-by-4">
      <name>CC BY 4.0</name>
      <pros>Maximum reach and citation; matches TeamTrack and SoccerTrack, the closest analogue datasets</pros>
      <cons>Permits commercial use of identifiable persons' images — a bigger ask of the federation's approval than it may have intended</cons>
    </option>
    <option id="custom-research">
      <name>Custom research-use licence with registration</name>
      <pros>Tightest control; usage can be tied to an acknowledged terms document, like several sports datasets do</pros>
      <cons>Bespoke licences depress adoption and create maintenance work for a solo project</cons>
    </option>
    <option id="defer">
      <name>Defer the decision to publication time</name>
      <pros>Nothing is lost — publication is post-hackathon anyway; more information may exist by then</pros>
      <cons>Leaves an open decision in the closing record and delays the dataset card's licence section</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The user selects one option id; the choice is recorded in docs/dataset-publication.md as a dated decision and reflected in the dataset card's licence section
    - The phase closing section is appended to docs/dataset-buildout.md listing: dataset versions and hashes, the detector runs and aliases, the three hackathon bundles with their hashes and delivery state, the frozen eval/private-test split, and the documentation produced
    - The closing section names explicitly what this phase did NOT do and who inherits it: the gate re-run (post-hackathon, D-02), the ReID work (the hackathon itself), the actual publication (D-19), and any conditional item that did not materialise (a second drone game, OTC access)
  </acceptance_criteria>
  <resume-signal>Select: cc-by-nc-4, cc-by-4, custom-research, or defer</resume-signal>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <done>The licence choice is recorded as a dated decision in both documents, and the phase closing section lists deliverables and deferrals with their inheritors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset → public internet (later) | Publication is irreversible; the assessment is where the conditions are set |
| approval scope → publication scope | The published scope must not exceed what the approval covers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-58 | Information Disclosure | future publication of identifiable persons | mitigate | Publication deferred by D-19; the assessment records the approval basis, the withheld clips, and a licence decision made by the user rather than the researcher |
| T-2.2-59 | Repudiation | licence basis | mitigate | The decision is recorded as a dated choice in docs/dataset-publication.md with the options that were on the table |
| T-2.2-60 | Information Disclosure | private test labels | mitigate | The publication checklist requires stripping the withheld clips before any release |
</threat_model>

<verification>
- `uv run pytest tests/test_phase22_docs.py -q` green
- `uv run pytest tests -q` green
- Closing section present in docs/dataset-buildout.md with deliverables and deferrals

</verification>

<success_criteria>
- Publication assessed in writing with platform, licence, timing and legal basis
- Dataset card complete and consistent with the measured counts
- Licence decision taken by the user and recorded
- Phase closing record states what exists and what the post-hackathon steps inherit
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-19-SUMMARY.md` when done
</output>
