---
phase: 02.2-dataset-buildout
plan: 06
type: execute
wave: 3
depends_on: ["02.2-02", "02.2-05"]
files_modified:
  - src/flag_football_ep/cv/frames.py
  - tests/test_cv_frames.py
  - data/reference/frozen_eval_clips.csv
  - docs/dataset-plan.md
autonomous: true
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "A frozen, clip-level eval split per domain exists before any active-learning frame is selected"
    - "18 drone clips are withheld as the private hackathon test set and are the same clips as the drone eval split (D-07 + D-13)"
    - "The numeric mAP-improvement threshold that ends labelling is written down before the first iteration runs"
    - "The domain mix and per-domain inference settings are derived from the measured sighting, not assumed"
  artifacts:
    - path: "data/reference/frozen_eval_clips.csv"
      provides: "The immutable clip-level eval/test split, one row per clip, with role and stratum"
      contains: "frozen_eval"
    - path: "docs/dataset-plan.md"
      provides: "German dataset plan: mix, frame targets, stopping rule, inference settings, hard-case strategy, seed-set verdict"
      min_lines: 90
  key_links:
    - from: "src/flag_football_ep/cv/active_learning.py"
      to: "data/reference/frozen_eval_clips.csv"
      via: "AL pool excludes every clip with role frozen_eval"
      pattern: "read_eval_split"
---

<objective>
Fix the measurement design before the measuring starts: freeze the per-domain eval clips (D-13),
carve the private hackathon test set out of the same drone clips (D-07), pre-commit the numeric
threshold that ends labelling (D-12, RESEARCH Open Question 3), and settle the domain mix and
per-domain inference settings from the measured sighting (D-10).

Purpose: every one of these decisions becomes unfalsifiable if it is made after seeing results. The
gate document earned its credibility by fixing criteria first; this plan does the same for the
dataset.
Output: `freeze_eval_clips` implemented, a frozen split CSV, and a German dataset plan that later
plans execute rather than reinterpret.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md
@.planning/phases/02.2-dataset-buildout/02.2-PATTERNS.md
@.planning/phases/02.2-dataset-buildout/02.2-02-SUMMARY.md
@docs/material-sighting.md
@docs/cv-setup.md

<interfaces>
Contract to implement (frozen by plan 02.2-05):

  @dataclass(frozen=True)
  class EvalSplit:
      clips_by_domain: dict[str, list[int]]
      fraction: float
      seed: int
      frozen_at: str

  def freeze_eval_clips(config, domains, fraction, seed, out_csv) -> EvalSplit
  def read_eval_split(path) -> EvalSplit

Existing precedent to extend (frames.py):
  _VAL_FRACTION = 0.2 ; _MIN_VAL_CLIPS = 6
  sample_training_frames(...) — clip-level split via random.Random(seed).shuffle, grouped by
  hover_ids.get(n, "all"), then _allocate_proportional per group. Eval freezing is the same
  idea one level up: freeze clip ids BEFORE the AL pool is drawn from the remainder.

Detector metrics logged by 2.1 (the stopping rule is expressed in these keys):
  mAP_50 = 0.9571, mAP_50_95 = 0.8112, AP_player = 0.8266, AP_referee = 0.7958
  (cv_detector_model@champion, run 87a8a5222f7a472787875e974d089c44)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement freeze_eval_clips and write the immutable split</name>
  <files>src/flag_football_ep/cv/frames.py, tests/test_cv_frames.py, data/reference/frozen_eval_clips.csv</files>
  <read_first>
    - src/flag_football_ep/cv/frames.py lines 342-532 (`sample_training_frames`'s clip-level split, `_allocate_proportional`, `write_manifest`/`read_manifest`) plus the domain parameter added by plan 02.2-02
    - data/reference/hover_positions.csv (drone strata: hp-01 = 30 clips, hp-02 = 31 clips)
    - data/reference/sighting_2026-08-14_WC-GER-vs-MEX-GOPRO.csv and data/reference/sighting_2026-08-14_WC-USA-vs-AUS-TV.csv (the strata for the other domains, written by plan 02.2-02)
    - src/flag_football_ep/cv/sighting.py::_write_hover_positions_csv (the atomic CSV write pattern to reuse)
  </read_first>
  <behavior>
    - `freeze_eval_clips` allocates eval clips per domain proportionally across that domain's camera/hover strata, using `random.Random(seed)` so the split is reproducible from the seed alone
    - The drone domain yields exactly 18 eval clips, 9 from hp-01 and 9 from hp-02
    - A domain with fewer than `_MIN_VAL_CLIPS` clips raises `ManifestError` naming the domain and its clip count
    - Re-running with the same seed produces a byte-identical CSV; re-running with a different seed against an existing CSV raises rather than overwriting (the split is frozen by definition)
    - `read_eval_split` round-trips the CSV back into an `EvalSplit`
  </behavior>
  <action>
    Implement `freeze_eval_clips(config, domains, fraction, seed, out_csv)` in `frames.py` following
    `sample_training_frames`'s two-level "group by stratum, allocate per stratum" pattern. Write
    `data/reference/frozen_eval_clips.csv` with columns
    `domain,session_id,clip_number,stratum_id,role,private_test,frozen_at,seed` where `role` is
    `frozen_eval` or `pool` and `private_test` is `true` only for the drone `frozen_eval` rows
    (D-07: the withheld hackathon test clips and the frozen drone eval clips are deliberately the
    same 18 clips, so the hackathon test set is never contaminated by AL training data). Use the
    atomic `.tmp` + `os.replace` write. Refuse to overwrite an existing file whose `seed` differs —
    raise `ManifestError` naming both seeds.

    Run it for the domains the D-11 verdict left in scope: `drone` with `fraction=0.30` (18 of 61)
    and the second training domain with `fraction=0.20` (minimum 6 clips). Implement
    `read_eval_split` as the inverse.

    Add tests covering: stratified counts, seed reproducibility, the overwrite refusal, the
    too-few-clips error, and that the union of `frozen_eval` and `pool` rows equals the domain's
    full clip set with no clip appearing twice.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_frames.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F, '$1=="drone" && $5=="frozen_eval"' data/reference/frozen_eval_clips.csv | wc -l` returns 18
    - `awk -F, '$1=="drone" && $5=="frozen_eval" {print $4}' data/reference/frozen_eval_clips.csv | sort | uniq -c` shows 9 for hp-01 and 9 for hp-02
    - `awk -F, '$5=="frozen_eval" && $6!="true" && $1=="drone"' data/reference/frozen_eval_clips.csv | wc -l` returns 0
    - `cut -d, -f1,3 data/reference/frozen_eval_clips.csv | sort | uniq -d` produces no output (no clip listed twice)
    - Re-running the command with the same seed leaves the file byte-identical (`git diff --exit-code data/reference/frozen_eval_clips.csv` succeeds)
    - `uv run pytest tests/test_cv_frames.py -q` passes with at least 5 new test cases
  </acceptance_criteria>
  <done>The eval and private-test clips are fixed, reproducible from a seed, and removable from the AL pool by a single read.</done>
</task>

<task type="auto">
  <name>Task 2: Write the dataset plan — mix, targets, stopping rule, inference settings</name>
  <files>docs/dataset-plan.md</files>
  <read_first>
    - docs/material-sighting.md (measured tiers, apparent player px, `recommend_inference_settings` output per domain, and the D-11 ratification block)
    - .planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md §Material & mix (D-10, D-11, D-12), §Eval design (D-13, D-14), §Claude's Discretion
    - docs/cv-setup.md `## Detector-Training` (the 2.1 recipe, machine, metrics and their stated limitations) and `## Datensatz` (the 304-frame baseline)
    - data/reference/continuity_review.csv `reviewer_note` column (the documented failure modes that define "hard cases")
    - 02.2-RESEARCH.md §Open Questions item 3 and §Common Pitfalls Pitfall 2
  </read_first>
  <action>
    Write docs/dataset-plan.md in German with a dated status header. It fixes, in advance:

    (1) Domain mix: the concrete split of the 1,500-frame floor across domains, derived from the
    measured sighting and stated as a number of frames per domain with the reasoning (D-10's
    "60/40 is orientation only"). If the D-11 verdict changed the domain set, the mix follows the
    verdict, not the original orientation.

    (2) Frame targets: 1,500 verified frames as the mandatory floor, up to 3,000, with the pilot's
    304 frames counted explicitly (or excluded, per Task 3's verdict).

    (3) Stopping rule (pre-committed, D-12): labelling of a domain continues past the floor only
    while that domain's `mAP_50_95` on the frozen eval clips improves by at least **+0.010
    absolute** over the previous iteration's registered detector, with `mAP_50` moving in the same
    direction. Below that, labelling for that domain stops even if the 3,000 ceiling is unreached.
    State the honest caveat: with an eval set of this size a delta below ~0.01 is not resolvable, so
    the threshold is a resolvability floor, not a claim of significance.

    (4) Per-domain inference settings (D-04/C-05): resolution (multiple of 224) and SAHI on/off per
    domain, quoted from `recommend_inference_settings` output, plus the rule that pooled mAP is
    never reported alone.

    (5) Hard-case mining strategy (discretion): which clips/timestamps the AL pool prioritises,
    derived from the `reviewer_note` failure modes (line-of-scrimmage congestion, crossing routes,
    occlusion, shadow) and how diversity sampling prevents collapse onto one scenario. Document
    which conditions the material cannot supply (rain, floodlight) as explicit dataset gaps.

    (6) RF-DETR variant decision (discretion): Small stays the default; Nano/Medium/Large remain
    permissible; XLarge/2XLarge are forbidden (PML 1.0, C-06) and are guarded by
    `tests/test_cv_dependencies.py::test_rfdetr_pml_variants_absent_from_source`. State whether a
    size comparison happens this phase and why (8 GB Dell VRAM as the binding constraint, D-21).

    (7) The eval-split contract: pointer to `data/reference/frozen_eval_clips.csv`, the statement
    that AL selects only from `role = pool`, and that the drone `frozen_eval` clips double as the
    private hackathon test set.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - docs/dataset-plan.md contains the literal threshold `+0.010` together with `mAP_50_95` and the resolvability caveat
    - The document states a frame count per domain that sums to at least 1,500
    - The per-domain inference settings table lists a resolution divisible by 224 for every in-scope domain
    - The document names `data/reference/frozen_eval_clips.csv` and states that AL draws only from `role = pool`
    - The document states the RF-DETR variant policy including the XLarge/2XLarge prohibition and cites C-06
    - tests/test_phase22_docs.py is extended with an assertion that the stopping-rule threshold string is present
  </acceptance_criteria>
  <done>Every number the later plans need is fixed in writing before any of them can be influenced by results.</done>
</task>

<task type="auto">
  <name>Task 3: Spot-check the 304 pilot labels before seeding them as v0</name>
  <files>docs/dataset-plan.md</files>
  <read_first>
    - docs/cv-setup.md `### Datensatz` (full — the labelling convention, the polygon-to-box conversion at ~frame 103, the single-annotator/no-IAA caveat, the `content_sha256` `ab3a9673…`)
    - src/flag_football_ep/cv/dataset.py::validate_coco (lines 85-222) and `_BBOX_BOUNDS_EPSILON_PX`
    - data/labels/2026-05-16_FRIENDLY-GER-vs-PANAMA-ROJO-DRONE/corrected/instances.json (structure only; it is gitignored PII)
    - 02.2-RESEARCH.md §Common Pitfalls, Pitfall 4
  </read_first>
  <action>
    Run a programmatic spot-check over the 304-frame COCO export rather than trusting it: compare
    box-geometry statistics for the polygon-derived range (images up to ~frame 103) against the
    directly-rectangled remainder — median box height/width, aspect-ratio distribution, count of
    boxes exceeding the frame bounds within `_BBOX_BOUNDS_EPSILON_PX`, per-class counts, and the
    fraction of images with zero annotations. Sample 15 images across both ranges and check them
    against the documented convention (every clearly visible person boxed; `referee` only for active
    on-field officials; box bottom tight at the feet, shadow excluded).

    Append a `## Seed-Set-Prüfung (Pilot, 304 Frames)` section to docs/dataset-plan.md reporting the
    measured statistics with n, and a verdict of exactly one of: `als v0 übernommen`,
    `als v0 übernommen mit Nachkorrektur von <n> Frames`, or `nicht übernommen`. If the two ranges
    differ materially in box geometry, the verdict must say so and name the affected frame range.
    State unchanged that `data/reference/gt_positions.csv` stays eval-only regardless of this
    verdict (locked in 2.1).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_dataset.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The new section reports at least: n images per range, median box height per range, per-class box counts, and the count of out-of-bounds boxes
    - The verdict is one of the three literal strings above
    - The section states that the GT position set stays eval-only
    - If the verdict is `nicht übernommen`, the frame targets in the mix section are restated so the 1,500 floor is still met without the seed
  </acceptance_criteria>
  <done>The decision to reuse or drop the pilot labels rests on measurement, not on convenience.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval split → hackathon private test set | The same clips serve both; leaking them into training invalidates both |
| PII COCO export → tracked docs | The seed-set check reads gitignored label data and writes only aggregates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-16 | Tampering | frozen_eval_clips.csv | mitigate | `freeze_eval_clips` refuses to overwrite an existing split with a different seed; the file is tracked in git so any change is visible in review |
| T-2.2-17 | Information Disclosure | seed-set spot-check output | mitigate | Only aggregate geometry statistics are written to `docs/`; no frame, crop or annotation payload is copied out of the gitignored tree |
| T-2.2-18 | Repudiation | stopping rule | mitigate | The numeric threshold is committed before the first AL iteration; later plans cite the commit rather than re-deriving it |
</threat_model>

<verification>
- `uv run pytest tests/test_cv_frames.py tests/test_cv_dataset.py tests/test_phase22_docs.py -q` green
- `uv run pytest tests -q` green
- `data/reference/frozen_eval_clips.csv` committed and reproducible from its recorded seed
</verification>

<success_criteria>
- Frozen per-domain eval split exists, 18 drone clips double as the private test set
- Domain mix, frame targets, stopping rule, inference settings and variant policy are written down
- The pilot seed set is accepted or rejected on measured evidence
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-06-SUMMARY.md` when done
</output>
