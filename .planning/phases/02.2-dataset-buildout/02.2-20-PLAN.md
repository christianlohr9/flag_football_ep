---
phase: 02.2-dataset-buildout
plan: 20
type: execute
wave: 9
depends_on: ["02.2-13", "02.2-14"]
files_modified:
  - .dvc/config
  - ffep.toml
  - docs/cv-setup.md
autonomous: true
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "The DVC remote for the dataset is resolved: either the real OTC bucket or an explicitly documented local fallback, never a placeholder that fails silently"
    - "The iteration-1 dataset pointer committed in plan 02.2-13 is pushed to that remote and the outcome is recorded"
    - "RESEARCH assumption A1 is answered in writing: whether `endpointurl` alone sufficed for OTC OBS, or which remedy was needed"
    - "No credential value ever enters .dvc/config or ffep.toml — only env-var names"
  artifacts:
    - path: ".dvc/config"
      provides: "Resolved DVC remote (real bucket or documented fallback), endpoint only"
      contains: "endpointurl"
    - path: "docs/cv-setup.md"
      provides: "Dataset-Versionierung section completed with the resolved remote, the env-var names and the A1 finding"
  key_links:
    - from: "ffep.toml"
      to: ".dvc/config"
      via: "the same bucket URL in both places, credentials in neither"
      pattern: "s3://"
---

<objective>
Resolve the dataset side of the storage story: point the DVC remote at the bucket that plan 02.2-14
established (or at the documented local fallback if OTC access is still pending), push the
iteration-1 dataset pointer from plan 02.2-13, and write down what actually worked against Open
Telekom Cloud OBS.

This is the concern that genuinely depends on the labelling weekend — it needs a dataset v1 to push.
It was split out of plan 02.2-14 so that hackathon bundle delivery, which is deadline-critical, is
not blocked by AL-iteration work.

Purpose: RESEARCH assumption A1 (`dvc-s3`'s `endpointurl` mechanism suffices for OTC OBS) is still
open. Pushing a real dataset is what answers it, and the answer belongs in the setup documentation
rather than in someone's memory.
Output: a resolved DVC remote, a pushed dataset pointer (or a recorded fallback push), and the A1
finding in docs/cv-setup.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md
@.planning/phases/02.2-dataset-buildout/02.2-13-SUMMARY.md
@.planning/phases/02.2-dataset-buildout/02.2-14-SUMMARY.md
@docs/cv-setup.md

<interfaces>
State inherited from plan 02.2-14's SUMMARY:
  the bucket name and region endpoint the user reported, or the explicit "no OTC access yet"
  the verbatim error text if the first bundle upload failed

State inherited from plan 02.2-13's SUMMARY:
  data/labels/dataset.dvc — the committed pointer for dataset v1
  whether that plan's `dvc push` already succeeded or fell back to a local remote

Config surface to update (plan 02.2-04 left the placeholder):
  .dvc/config    remote "otc-obs": url = s3://ffep-datasets-PLACEHOLDER/flag-football-datasets
                                   endpointurl = https://obs.eu-de.otc.t-systems.com
                                   region = eu-de
  ffep.toml      cv.dvc_remote_url mirrors the same URL

Credential rule (project-wide, no exceptions):
  credentials live only in OTC_OBS_ACCESS_KEY_ID / OTC_OBS_SECRET_ACCESS_KEY
  never `dvc remote modify --local` with a literal key, never a key in a tracked file

Remedy order for an S3-compatibility failure (RESEARCH Pitfall 3, assumption A1):
  1. `dvc remote modify <name> listobjects true`
  2. path-style addressing
  whichever one works is the A1 answer and must be written down
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the DVC remote and push the iteration-1 dataset</name>
  <files>.dvc/config, ffep.toml</files>
  <read_first>
    - .dvc/config (the placeholder remote from plan 02.2-04)
    - ffep.toml `[cv]` (the `dvc_remote_*` fields)
    - .planning/phases/02.2-dataset-buildout/02.2-14-SUMMARY.md (bucket name and endpoint, or the deferral)
    - .planning/phases/02.2-dataset-buildout/02.2-13-SUMMARY.md (whether the dataset push was already deferred)
  </read_first>
  <action>
    If plan 02.2-14 reported a real bucket: replace `ffep-datasets-PLACEHOLDER` in both `.dvc/config`
    and `ffep.toml` with that bucket and confirm the endpoint matches the region the user named.
    Then run `uv run --extra versioning dvc push` for the dataset pointer committed in plan 02.2-13
    and record the transferred-object count verbatim. If the push fails with a signature or
    addressing error, apply the two documented remedies in order (`dvc remote modify <name>
    listobjects true`, then path-style addressing), re-run after each, and record which one worked —
    that is the A1 answer.

    If OTC access is still pending: leave the placeholder in place, add the documented local fallback
    remote (`dvc remote add local-fallback <path>` on an external volume or a second directory), push
    the dataset there, and record that the OTC remote is pending together with the exact commands to
    switch. Do not delete the `otc-obs` remote — the switch must stay a one-line change.

    Never run `dvc remote modify --local` with a literal access key; credentials stay in the two
    environment variables.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dvc_layout.py tests/test_cv_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra versioning dvc remote list` output is quoted verbatim in the SUMMARY
    - `grep -v '^#' .dvc/config | grep -c PLACEHOLDER` returns 0 when a real bucket exists; when it does not, the SUMMARY states the placeholder is intentional and a `local-fallback` remote is listed
    - `grep -rn "OTC_OBS_SECRET\|access_key_id\|secret_access_key" .dvc/config ffep.toml` shows env-var *names* only, never a value
    - `uv run --extra versioning dvc push` (or the fallback push) reports a transferred-object count, quoted in the SUMMARY
    - `uv run pytest tests/test_dvc_layout.py -q` green (the guard that no `.dvc` file tracks raw video still holds)
  </acceptance_criteria>
  <done>The dataset lives on a real remote — cloud or documented fallback — and the switch between them is a one-line change.</done>
</task>

<task type="auto">
  <name>Task 2: Write the resolved remote and the A1 finding into the setup docs</name>
  <files>docs/cv-setup.md</files>
  <read_first>
    - docs/cv-setup.md `## Dataset-Versionierung (DVC)` (the section plan 02.2-04 opened and plan 02.2-13 extended)
    - .planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md §Assumptions Log (A1, A2) and §Pitfall 3
  </read_first>
  <action>
    Complete the `## Dataset-Versionierung (DVC)` section in German with a dated status line:
    the resolved remote configuration (name, bucket URL, endpoint, region), the two credential
    env-var names (names only, never values), the pull/push commands a second machine needs, and the
    A1 finding stated plainly — whether `endpointurl` alone sufficed against OTC OBS, or which remedy
    (`listobjects`, path-style addressing) was required, or that the question is still open because
    the push ran against the local fallback.

    Where the finding is "still open", say so in that wording rather than implying a verified cloud
    setup, and give the command sequence that answers it later.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_config.py tests/test_dvc_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - docs/cv-setup.md `## Dataset-Versionierung (DVC)` names the resolved remote and both credential env-var names
    - The A1 answer is stated explicitly — sufficed / needed remedy X / still open on the local fallback
    - `grep -c "OTC_OBS_SECRET_ACCESS_KEY=" docs/cv-setup.md` returns 0
    - `uv run pytest tests -q` green
  </acceptance_criteria>
  <done>The next person (or the next machine) can reproduce the remote setup from the documentation, and A1 is no longer an open question in someone's head.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → OTC OBS remote | `dvc push` moves label data and frames to third-party object storage |
| environment → tracked config | Credentials must not cross this line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-61 | Information Disclosure | `.dvc/config` / `ffep.toml` | mitigate | Bucket and endpoint only; `dvc remote modify --local` with a literal key is explicitly forbidden; grep acceptance check over both files and over docs/cv-setup.md |
| T-2.2-62 | Information Disclosure | pushed dataset content | mitigate | Only frames and labels are DVC-tracked, never raw video — guarded by `tests/test_dvc_layout.py`, re-run as this plan's verify; push stays an explicit command, never automatic |
| T-2.2-63 | Denial of Service (schedule) | missing OTC access | accept | Local fallback remote keeps dataset versioning functional; the `otc-obs` remote entry is preserved so the switch is one line |
</threat_model>

<verification>
- `uv run pytest tests/test_dvc_layout.py tests/test_cv_config.py -q` green
- `uv run pytest tests -q` green
- `dvc remote list` and the push result quoted in the SUMMARY

</verification>

<success_criteria>
- The DVC remote is resolved to a real target and the dataset v1 pointer is pushed to it
- No credential value in any tracked file
- The A1 finding is documented in German, including the honest "still open" case

</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-20-SUMMARY.md` when done
</output>
