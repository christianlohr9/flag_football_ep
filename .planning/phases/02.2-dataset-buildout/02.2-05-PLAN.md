---
phase: 02.2-dataset-buildout
plan: 05
type: execute
wave: 2
depends_on: ["02.2-02", "02.2-04"]
files_modified:
  - src/flag_football_ep/cv/freeze.py
  - src/flag_football_ep/cv/bundle.py
  - src/flag_football_ep/cv/active_learning.py
  - src/flag_football_ep/cv/frames.py
  - src/flag_football_ep/cv/schema.py
  - src/flag_football_ep/cv/export.py
  - src/flag_football_ep/cv/detect.py
  - src/flag_football_ep/cv/prelabel.py
  - src/flag_football_ep/cv/commands.py
  - tests/test_cv_contracts.py
autonomous: true
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "`ffep cv --help` lists the eight new verbs freeze, bundle, active-learn, eval-split, detections, crops, deliver, eval-domains"
    - "Every new module imports without the cv extras group installed"
    - "Every function later plans call exists with its final parameter names, raising NotImplementedError"
  artifacts:
    - path: "src/flag_football_ep/cv/freeze.py"
      provides: "FROZEN_ALIAS + freeze/resolve_frozen/write_freeze_pin/read_freeze_pin contracts"
      contains: "FROZEN_ALIAS"
    - path: "src/flag_football_ep/cv/active_learning.py"
      provides: "frame_uncertainty_score/select_al_frames/manifest round trip contracts"
    - path: "src/flag_football_ep/cv/bundle.py"
      provides: "build_bundle/bundle_manifest/deliver_bundle contracts"
    - path: "src/flag_football_ep/cv/schema.py"
      provides: "DETECTION_COLUMNS/DETECTION_SCHEMA + write_detections_parquet"
      contains: "DETECTION_COLUMNS"
  key_links:
    - from: "tests/test_cv_contracts.py"
      to: "src/flag_football_ep/cv/*.py"
      via: "FUNCTION_CONTRACTS signature guard"
      pattern: "FUNCTION_CONTRACTS"
---

<objective>
Freeze the interfaces of everything this phase still has to build, before any of it is built: three
new `cv/` modules as stubs with final signatures, the detection-Parquet schema, the two export
entry points, the fine-tuned prelabel backend seam, the frozen-eval-clip function, eight new CLI
verbs, and the contract-guard test that makes all of it non-negotiable for later plans.

Purpose: eight downstream plans call into this surface. Without contracts up front they would each
invent their own parameter names and the parallel waves would not compose. This is the
interface-first step, not the implementation.
Output: importable stubs raising `NotImplementedError`, wired CLI verbs, and an extended
`tests/test_cv_contracts.py` that fails on any signature drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-RESEARCH.md
@.planning/phases/02.2-dataset-buildout/02.2-PATTERNS.md
@.planning/phases/02.2-dataset-buildout/02.2-02-SUMMARY.md

<interfaces>
Contracts this plan creates (exact names and parameter order — later plans depend on them):

  cv/freeze.py
    FROZEN_ALIAS = "hackathon-frozen"      # sibling of registry.CHAMPION_ALIAS, never reused
    def freeze(name, run_id, config) -> str                     # returns model version
    def resolve_frozen(name, config) -> str                     # returns run_id
    def write_freeze_pin(config, run_id, dataset_hash, path) -> Path
    def read_freeze_pin(path) -> FreezePin                      # dataclass: run_id, dataset_hash, frozen_at, model_version
    class FreezeError(RegistryError)                            # reuse registry's error type family

  cv/active_learning.py
    class ActiveLearningError(CvError, ValueError)
    def frame_uncertainty_score(detections) -> float
    def diversity_key(row) -> tuple
    def select_al_frames(config, session_ids, iteration, target, seed, out_dir) -> ALSelection
    def write_selection_manifest(manifest, path) -> Path
    def read_selection_manifest(path) -> ALSelection

  cv/bundle.py
    class BundleError(CvError, RuntimeError)
    BUNDLE_KINDS = ("dev", "test", "transfer")
    def build_bundle(config, kind, pin, out_dir) -> BundleResult   # BundleResult: archive_path, manifest_path, content_sha256, n_files
    def bundle_manifest(root) -> dict
    def deliver_bundle(config, archive, remote) -> str             # returns remote URI

  cv/frames.py (extension)
    def freeze_eval_clips(config, domains, fraction, seed, out_csv) -> EvalSplit
    def read_eval_split(path) -> EvalSplit                         # EvalSplit: clips_by_domain, seed, frozen_at

  cv/schema.py (extension, mirrors TRACKING_COLUMNS/TRACKING_SCHEMA exactly)
    DETECTION_COLUMNS = ("session_id","clip_number","frame_index","timestamp_s","det_index",
                         "class_name","confidence","bbox_x1","bbox_y1","bbox_x2","bbox_y2",
                         "detector_run_id","detected_at")
    def empty_detection_frame() -> pl.DataFrame
    def conform_detections(df) -> pl.DataFrame
    def write_detections_parquet(df, path) -> Path

  cv/export.py (extension)
    def export_detections_parquet(config, session_id, domain, run_id, out_path) -> Path
    def export_track_crops(config, session_id, tracks, out_dir) -> int      # returns n crops written

  cv/detect.py (extension)
    def evaluate_per_domain(config, run_id, eval_split_path, out_path) -> dict
    # runs the registered detector over the frozen eval clips and returns
    # {domain: {mAP_50, mAP_50_95, AP_player, AP_referee, n_images, n_boxes}} —
    # never a pooled number alone (C-05/D-04)

  cv/prelabel.py (extension)
    def prelabel_frames(config, frames_dir, out_dir, force, backend) -> PrelabelResult
    def _load_finetuned_backend(device, config, run_id) -> Callable[[Path], list[Detection]]
    # _BACKENDS gains "finetuned"; it is selected explicitly, never via _resolve_backend's
    # try-in-order fallback chain (RESEARCH Pitfall 1)

Existing patterns to copy (do not reinvent):
  registry.py::promote/resolve_champion  — alias set/read shape for freeze.py
  frames.py::write_manifest/read_manifest — `.tmp` + os.replace atomic JSON round trip
  schema.py::conform_tracking            — cast/reorder/validate discipline
  commands.py::train/promote             — Typer verb shape, `--config` first, function-local imports
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three new modules as contract stubs</name>
  <files>src/flag_football_ep/cv/freeze.py, src/flag_football_ep/cv/bundle.py, src/flag_football_ep/cv/active_learning.py</files>
  <read_first>
    - src/flag_football_ep/cv/registry.py (full — module docstring style, `RegistryError`, `promote`, `resolve_champion`, the `mlflow_store.configure(config)`-first rule)
    - src/flag_football_ep/cv/frames.py lines 37-130 (error classes, dataclasses) and lines 453-532 (`write_manifest`/`read_manifest` atomic JSON round trip)
    - src/flag_football_ep/cv/dataset.py lines 43-90 (error class + `DatasetStats` dataclass convention)
    - src/flag_football_ep/cv/__init__.py (`CvError` base class)
    - .planning/phases/02.2-dataset-buildout/02.2-PATTERNS.md §`freeze.py`, §`bundle.py`, §`active_learning.py`
  </read_first>
  <action>
    Create the three modules with `from __future__ import annotations`, a module docstring naming
    the plan that implements each function, the dataclasses and error classes from the
    `<interfaces>` block, and function bodies that raise
    `NotImplementedError("implemented by plan 02.2-NN")` with the correct plan number
    (freeze → 02.2-07, bundle → 02.2-10, active_learning → 02.2-09). Type annotations must be
    complete and `TYPE_CHECKING`-guarded for `Config`, exactly as `frames.py` does.

    Every heavy import (`mlflow`, `numpy`, `supervision`, `cv2`) stays inside function bodies —
    tests/test_cv_contracts.py's lazy-import guard runs against every listed module. `polars` may be
    imported at module level (core dependency), matching `schema.py`'s documented exception.

    `freeze.py` defines `FROZEN_ALIAS = "hackathon-frozen"` as a module constant and reuses
    `flag_football_ep.model.registry.RegistryError` as the base of `FreezeError` rather than
    inventing a parallel error family.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_contracts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import flag_football_ep.cv.freeze, flag_football_ep.cv.bundle, flag_football_ep.cv.active_learning"` succeeds in an environment without the `cv` extra
    - Every public function raises `NotImplementedError` naming its implementing plan
    - `grep -c "^from mlflow\|^import mlflow\|^import numpy\|^import supervision\|^import cv2" src/flag_football_ep/cv/freeze.py src/flag_football_ep/cv/bundle.py src/flag_football_ep/cv/active_learning.py` returns 0 for all three
    - `FROZEN_ALIAS` equals `"hackathon-frozen"` and is not equal to `flag_football_ep.model.registry`'s champion alias constant
  </acceptance_criteria>
  <done>The three new modules exist with final signatures and no implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Extend frames, schema, export and prelabel with their new contract surfaces</name>
  <files>src/flag_football_ep/cv/frames.py, src/flag_football_ep/cv/schema.py, src/flag_football_ep/cv/export.py, src/flag_football_ep/cv/detect.py, src/flag_football_ep/cv/prelabel.py</files>
  <read_first>
    - src/flag_football_ep/cv/schema.py (full — `TRACKING_COLUMNS`, `TRACKING_SCHEMA`, `NULLABLE_COLUMNS`, `CLASS_VOCABULARY`, `conform_tracking`, `write_tracking_parquet`; the new detection schema mirrors this structure exactly)
    - src/flag_football_ep/cv/export.py (full, 30 lines — the thin export-module convention)
    - src/flag_football_ep/cv/prelabel.py lines 37-100 (`Detection` dataclass, `PrelabelResult`) and lines 183-232 (`_BACKENDS`, `_BACKEND_ORDER`, `_resolve_backend`)
    - src/flag_football_ep/cv/frames.py lines 100-130 (`FrameSample`, `FrameSampleManifest`)
  </read_first>
  <action>
    In `schema.py` add `DETECTION_COLUMNS`, `DETECTION_SCHEMA`, `DETECTION_NULLABLE_COLUMNS`
    (empty), `empty_detection_frame`, `conform_detections` and `write_detections_parquet`,
    mirroring the tracking equivalents column-for-column: same dtypes for the shared columns
    (`session_id` Utf8, `clip_number`/`frame_index`/`det_index` Int32, `timestamp_s`/`confidence`/
    `bbox_*` Float64, `detector_run_id`/`detected_at` Utf8), the same `CLASS_VOCABULARY`
    enforcement, and the same atomic write discipline as `write_tracking_parquet`. Reuse the
    existing exception types (`MissingTrackingColumns` etc.) only if their docstrings still read
    true; otherwise add detection-specific siblings.

    In `frames.py` add `EvalSplit` (dataclass: `clips_by_domain: dict[str, list[int]]`, `fraction`,
    `seed`, `frozen_at`), `freeze_eval_clips(config, domains, fraction, seed, out_csv)` and
    `read_eval_split(path)` as stubs raising `NotImplementedError("implemented by plan 02.2-06")`.

    In `export.py` add `export_detections_parquet` and `export_track_crops` stubs raising
    `NotImplementedError("implemented by plan 02.2-08")`. In `detect.py` add an
    `evaluate_per_domain` stub raising `NotImplementedError("implemented by plan 02.2-15")`,
    keeping every existing `detect.py` signature untouched.

    In `prelabel.py` add the keyword-only `backend: str | None = None` parameter to
    `prelabel_frames` (when given, it selects a `_BACKENDS` entry directly and bypasses
    `_resolve_backend`'s ordered fallback; when `None`, current behaviour is unchanged), register a
    `"finetuned"` key in `_BACKENDS` pointing at a `_load_finetuned_backend` stub raising
    `NotImplementedError("implemented by plan 02.2-09")`, and raise
    `PrelabelBackendUnavailable` naming the valid backend keys if an unknown name is passed. Do not
    add `"finetuned"` to `_BACKEND_ORDER` — it must never be auto-selected.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_prelabel.py tests/test_cv_track_schema.py tests/test_cv_export.py tests/test_cv_frames.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from flag_football_ep.cv import schema; print(len(schema.DETECTION_COLUMNS)); print(schema.empty_detection_frame().schema)"` prints 13 and a schema matching `DETECTION_SCHEMA`
    - `uv run python -c "import inspect; from flag_football_ep.cv import prelabel; print(list(inspect.signature(prelabel.prelabel_frames).parameters))"` ends with `backend`
    - Calling `prelabel_frames(..., backend="nonsense")` raises `PrelabelBackendUnavailable` whose message lists `finetuned`
    - `"finetuned" not in prelabel._BACKEND_ORDER` and `"finetuned" in prelabel._BACKENDS`
    - All existing prelabel/schema/export/frames tests still pass unchanged
  </acceptance_criteria>
  <done>The four extended modules expose their new surfaces without changing any existing behaviour.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the eight CLI verbs and extend the contract guard</name>
  <files>src/flag_football_ep/cv/commands.py, tests/test_cv_contracts.py</files>
  <read_first>
    - src/flag_football_ep/cv/commands.py (module docstring rules at lines 1-17, plus `sight`, `train`, `promote`, `overlay` as the shape templates)
    - tests/test_cv_contracts.py (full — `CV_VERBS`, `FUNCTION_CONTRACTS`, `CV_CONTRACT_MODULES`, `FORBIDDEN_CV_MODULES`, and the four guard groups)
    - .planning/phases/02.2-dataset-buildout/02.2-PATTERNS.md §`commands.py`
  </read_first>
  <action>
    Add eight verbs to `cv/commands.py`, each with `config: Path = typer.Option(DEFAULT_CONFIG,
    "--config", ...)` as the first parameter, all business-logic imports function-local, output via
    `typer.echo`, and no logic beyond argument resolution and one delegating call:
      `freeze`       --run, --dataset-hash            → cv.freeze.freeze + write_freeze_pin
      `bundle`       --kind {dev,test,transfer}, --out → cv.bundle.build_bundle
      `deliver`      --archive, --remote              → cv.bundle.deliver_bundle
      `active-learn` --iteration, --target, --seed, --session, --out-dir → cv.active_learning.select_al_frames
      `eval-split`   --domain (repeatable), --fraction, --seed, --out → cv.frames.freeze_eval_clips
      `detections`   --session, --domain, --run, --out → cv.export.export_detections_parquet
      `crops`        --session, --tracks, --out-dir    → cv.export.export_track_crops
      `eval-domains` --run, --split, --out              → cv.detect.evaluate_per_domain
    Verbs whose delegate is still a stub must surface the `NotImplementedError` unchanged — do not
    swallow it into a friendly message.

    Extend tests/test_cv_contracts.py: add the eight verbs to `CV_VERBS`, add `freeze`, `bundle`,
    `active_learning` to `CV_CONTRACT_MODULES`, add every function from the `<interfaces>` block to
    `FUNCTION_CONTRACTS` with its exact parameter tuple, and update the module docstring's verb
    count from 18 to 26.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_contracts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ffep cv --help` lists all 26 verbs; `uv run pytest tests/test_cv_contracts.py -q` passes every parametrised verb case
    - The lazy-import guard passes: `import flag_football_ep.cli` still loads none of `rfdetr`, `torch`, `cv2`, `supervision`, `sahi`, `transformers`, `umap`
    - Renaming any parameter of a contract function makes `tests/test_cv_contracts.py` fail (verify during development, then restore)
    - `uv run ffep cv freeze --run abc123` exits with the stub's `NotImplementedError` mentioning plan 02.2-07
    - `uv run pytest tests -q` green
  </acceptance_criteria>
  <done>Eight verbs are discoverable and contract-guarded; downstream plans implement behind fixed signatures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI surface → cv modules | New verbs become the entry points for data movement and cloud delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-13 | Information Disclosure | `ffep cv deliver` output | mitigate | The verb echoes the remote URI only; no credential value is ever echoed (commands.py module docstring rule, enforced by review of the echo strings) |
| T-2.2-14 | Tampering | `ffep cv freeze` | mitigate | The verb writes a distinct alias (`hackathon-frozen`), never the rolling `champion`; the alias constant lives in one place |
| T-2.2-15 | Denial of Service (self-inflicted) | stub verbs | accept | Calling an unimplemented verb raises NotImplementedError naming the implementing plan — deliberate, no silent no-op |
</threat_model>

<verification>
- `uv run pytest tests/test_cv_contracts.py -q` green with 25 verbs
- `uv run pytest tests -q` green
- `uv run ffep --help` still lists `cv` and imports no cv-extras module
</verification>

<success_criteria>
- Three new modules and four extended modules expose final signatures
- Eight CLI verbs wired with the established shape
- Contract guard extended so later plans cannot drift the interface
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-05-SUMMARY.md` when done
</output>
