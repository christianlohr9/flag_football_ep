---
phase: 02.2-dataset-buildout
plan: 10
subsystem: cv
tags: [bundle, hackathon, content-hash, scoring, dataset-delivery, polars]

# Dependency graph
requires:
  - phase: 02.2-dataset-buildout
    provides: "02.2-05 build_bundle/bundle_manifest contract stubs + BUNDLE_KINDS; 02.2-06 frozen_eval_clips.csv role=pool/frozen_eval split; 02.2-07 hackathon_freeze.json pin + freeze.read_freeze_pin; 02.2-08 bundle-inputs detections.parquet + crops (all 61 clips); 02.2-03 complete continuity_review.csv/flag_pull_events.csv (61/61) + the measured 15/61 baseline"
provides:
  - "cv/bundle.py::build_bundle -- real dev-kind archive assembly (role=pool filter, content-hash determinism, German README) + a fail-closed test-kind label-leak guard (T-2.2-28) ready for plan 02.2-12"
  - "cv/bundle.py::bundle_manifest -- manifest.json reader/validator"
  - "scripts/hackathon/score_tracks.py -- the single scoring script every hackathon team is measured with (continuity + optional flag-pull bonus)"
  - "data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip -- the built, hashed, real dev-set bundle (43 pool clips, 11834 files, gitignored)"
  - "docs/hackathon-bundles.md -- German bundle documentation (dev complete; test/transfer marked pending for plan 02.2-12)"
  - "docs/hackathon-challenge-reid.md Baseline-Zahlen refreshed to the measured 15/61 = 24.59% (supersedes the 77% upper bound over n=20)"
affects: [02.2-12-test-transfer-bundles, 02.2-14-otc-delivery, hackathon-challenge-reid]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Declarative bundle content table (_BundleItem: dest/source/category/writer) built once per kind, letting the test-kind label-leak guard inspect source paths before any writer runs (no I/O needed to prove refusal)"
    - "Content-hash generalization: dataset.dataset_hash's sorted (relative_path, sha256) pattern reused verbatim but widened from 'images + one instances.json' to 'every file in the tree' -- a hackathon bundle ships Parquet/CSV/MP4/JPEG/JSON/Markdown, not only images"
    - "Defense-in-depth freeze-pin verification: build_bundle re-reads hackathon_freeze.json itself and cross-checks run_id against the caller-supplied FreezePin, rather than trusting an injected dataclass blindly (T-2.2-24)"
    - "Standalone scoring script imports only the private per-clip fragment-logic function it needs (continuity._measure_clip) plus the public summarise_review, deliberately avoiding Config/video_inventory.csv so it never has an avenue to read project-internal reference paths it wasn't explicitly given"

key-files:
  created:
    - src/flag_football_ep/cv/bundle.py (real implementation, was a contract stub)
    - tests/test_cv_bundle.py
    - scripts/hackathon/score_tracks.py
    - tests/test_hackathon_scoring.py
    - docs/hackathon-bundles.md
    - data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip (gitignored)
    - data/bundles/dev-set/ (gitignored staging tree)
  modified:
    - docs/hackathon-challenge-reid.md (Baseline-Zahlen section only)

key-decisions:
  - "Radar renders are NOT part of the dev bundle: no per-clip radar-rendering run exists on disk (cv/radar.py can technically produce them, but generating ~43 renders was out of this plan's files_modified/time budget and not required by any acceptance criterion). Documented as a known gap in both the generated README.md and docs/hackathon-bundles.md rather than fabricated or silently omitted."
  - "frozen_eval_clips.csv itself is NOT shipped inside the dev bundle -- it would tell participants exactly which 18 clip numbers are the private test set, an unnecessary information leak beyond what shipping only 43 of 61 clips already implies by omission. build_bundle reads it internally to compute the role=pool filter, but it never becomes bundle content."
  - "The test-kind label-leak guard (T-2.2-28) is scoped exactly to continuity_review.csv/flag_pull_events.csv per the plan's own <behavior> text, not extended to gt_positions.csv (which IS legitimately shipped in dev). A real test-kind build (plan 02.2-12) will need its own withholding decision for GT positions -- noted here, not decided by this plan."
  - "score_tracks.py's flag-pull bonus reports precision/recall on the +-0.5s time window always, and additionally applies the ~2-yard location window only when both ground-truth and predicted rows carry optional x_yards/y_yards columns (neither the shipped flag_pull_events.csv schema nor the team submission schema guarantees them) -- a location-blind match is reported with an explicit printed caveat rather than a fabricated distance number, since the challenge doc itself records this design as 'offen' (Teil 3)."
  - "The dev bundle documents its own pool-only baseline (10/43 = 23.26%, reproduced live via score_tracks.py against the shipped tracks+continuity_review.csv) in docs/hackathon-bundles.md, distinct from the full-pilot 15/61 = 24.59% headline in docs/hackathon-challenge-reid.md's Baseline-Zahlen -- both are correct, they measure over different denominators (43 pool-only vs. 61 including the withheld private-test clips) for the same underlying reason: the private test set isn't in the dev bundle."

requirements-completed: [REQ-S2-03]

# Metrics
duration: ~140min
completed: 2026-09-01
---

# Phase 02.2 Plan 10: Hackathon Bundle Builder + Scoring Script Summary

**Real dev-set bundle (43 role=pool clips, 11,834 files, content hash `ad412c5c...`) built from `cv/bundle.py::build_bundle`'s declarative content table, plus `scripts/hackathon/score_tracks.py` reproducing the bundle's own pool-only baseline (10/43 = 23.26%) live against the shipped tracks and human verdicts.**

## Performance

- **Duration:** ~140 min (includes a real ~27s bundle build against 11,834 files and a second rebuild to verify content-hash determinism on real data)
- **Started:** 2026-09-01T~11:40:00Z (approx.)
- **Completed:** 2026-09-01T~14:00:00Z (approx.)
- **Tasks:** 3 (Tasks 1-2 `auto`/`tdd="true"`, Task 3 `auto`)
- **Files modified:** 7 (2 code + 2 test files created, 1 doc created, 1 doc modified, 1 real bundle archive/tree built)

## Accomplishments

- `cv/bundle.py::build_bundle`/`bundle_manifest` implemented for real against the frozen 02.2-05 contract: a declarative per-item content table (`_BundleItem`), a content-hash scheme generalizing `dataset.dataset_hash`'s sorted-`(path, sha256)` pattern from COCO images to arbitrary bundle file types, atomic manifest/archive writes, and a fail-closed `role = pool` filter sourced from `data/reference/frozen_eval_clips.csv`
- Test-kind label-leak guard (T-2.2-28) implemented and exercised now, even though no real second-drone-game test bundle exists yet: `build_bundle(config, "test", pin, out_dir)` always raises `BundleError` naming `continuity_review.csv`/`flag_pull_events.csv`
- 14 new tests in `tests/test_cv_bundle.py` against a synthetic tmp_path fixture (4 clips, 1 withheld as `private_test`): archive/manifest creation, private-clip exclusion (staged tree + parquet + CSV + the actual zip's `namelist()`), content-hash determinism across two independent builds AND idempotent rebuild into the same `out_dir`, missing/stale freeze-pin refusal, test-kind refusal, unknown/transfer-kind refusal, German-README-with-English-columns spot check, manifest round-trip
- `scripts/hackathon/score_tracks.py` implemented: standalone argparse CLI (`--tracks`, `--review`, `--flag-pulls`, `--out`), schema-validates the submission before computing anything, reuses `cv.continuity._measure_clip` (per-clip fragment logic) and `cv.continuity.summarise_review` (human-verdict headline) rather than reimplementing either, gates the flag-pull bonus behind `--flag-pulls` with a time+optional-location precision/recall match
- 6 new subprocess-driven tests in `tests/test_hackathon_scoring.py`: `--help` documents all four flags, missing-`track_id` failure names the column, every printed rate matches a `k/n (p%)` regex, a degraded (fragmented-track) submission scores strictly worse than a perfect one on the aggregate automatic indicator, and the flag-pull precision/recall path
- Ran `uv run --extra cv ffep cv bundle --kind dev --out data/bundles` against the real pilot session: `data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip` (43 pool clips, 11,834 files, `content_sha256 = ad412c5cffb9a9b54a49e4e7c0b3075c0e013304cf8d1b7fb45d7ed5e5db2a56`, ~1.5 GB); rebuilt a second time and confirmed the identical hash and file count on real data (not just the synthetic test fixture)
- `docs/hackathon-bundles.md` created: per-kind sections (dev complete with content list/layout/schemas/reproduction command/content hash/pool-only baseline/delivery rules; test/transfer marked pending for plan 02.2-12, with the already-live label-leak guard called out explicitly)
- `docs/hackathon-challenge-reid.md` `## Baseline-Zahlen` refreshed: the 77%-upper-bound-over-n=20 wording replaced with the measured 15/61 = 24.59% over all 61 clips (from plan 02.2-03's completed labelling), with the two dominant failure modes and their rates named

## Task Commits

1. **Task 1: Implement the bundle builder** - `0f2d0eb` (feat)
2. **Task 2: The shared scoring script** - `035c9fb` (feat)
3. **Task 3: Build the dev-set bundle and document it** - `776e049` (docs)

**Plan metadata:** (this commit, following SUMMARY.md write)

## Files Created/Modified

- `src/flag_football_ep/cv/bundle.py` - `build_bundle`/`bundle_manifest` real implementations (was a `NotImplementedError` contract stub), `_BundleItem`/`_content_table`/`_dev_items`/`_hash_tree`/`_render_readme`/`_write_archive` and per-artifact writer closures
- `tests/test_cv_bundle.py` - 14 tests against a synthetic 4-clip fixture mirroring the real bundle-inputs layout
- `scripts/hackathon/score_tracks.py` - standalone scoring CLI
- `tests/test_hackathon_scoring.py` - 6 subprocess tests
- `docs/hackathon-bundles.md` - new, German, per-bundle-kind documentation
- `docs/hackathon-challenge-reid.md` - `## Baseline-Zahlen` section only, rest untouched
- `data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip`, `data/bundles/dev-set/` - real built artifacts (gitignored, never committed)

## Decisions Made

See frontmatter `key-decisions` for full rationale on:
- Radar renders excluded from the dev bundle (documented gap, not fabricated).
- `frozen_eval_clips.csv` itself never shipped as bundle content (used internally only, to avoid telling participants exactly which clip numbers are withheld).
- Test-kind label-leak guard scoped exactly to the two files the plan names (`continuity_review.csv`, `flag_pull_events.csv`), not `gt_positions.csv`.
- Flag-pull bonus's location criterion is conditional on optional columns neither shipped schema guarantees; a time-only match is reported with an explicit caveat when location data is absent.
- The dev bundle's own pool-only baseline (10/43) is documented separately from the full-pilot 61-clip headline (15/61); both are correct over their own denominators.

## Deviations from Plan

### Auto-fixed Issues

None — no bugs, missing-critical-functionality, or blocking issues were found during implementation; all real-data runs (the two live `ffep cv bundle --kind dev` builds, the score_tracks.py reproduction run) succeeded on the first attempt against the already-verified plan 02.2-03/02.2-06/02.2-07/02.2-08 outputs.

### Scope Decisions (documented, not silent)

**1. Radar renders omitted from the dev bundle.** The plan's `<interfaces>` block's idealized dev-set content list includes "radar renders" alongside overlay videos. No per-clip radar-rendering run exists anywhere in the repository (`cv/radar.py::render_radar_frame`/`render_showcase_reel` exist but are driven by no script that has ever run per-pool-clip; only a single curated `showcase.mp4` exists, built from an unknown/unverified clip subset that could not be safely confirmed leak-free against the private test set). Generating ~43 new per-clip radar renders was outside this plan's `files_modified` and time budget, and no acceptance criterion in the plan requires it. Documented explicitly as a known gap in both the bundle's own generated `README.md` and `docs/hackathon-bundles.md`, per the "no fabricated bundle contents" instruction in this session's objective — omission-with-documentation was chosen over either fabricating a placeholder or silently shipping less than promised without saying so.

---

**Total deviations:** 0 auto-fixed bugs/missing-critical/blocking issues. 1 documented scope decision (radar renders), not a deviation rule 1-3 case since nothing was broken or blocking -- the dev bundle's stated purpose (development/tuning against detections, tracks, crops, and human-verdict labels) is fully served without it.
**Impact on plan:** No scope creep; the omission is narrower than what a literal reading of the interfaces block's content list would suggest, documented rather than hidden.

## Issues Encountered

- The full `uv run pytest tests -q` suite (all CV + EP/WP tests) took long enough that this plan's targeted verification ran first against the specific files it touches (`tests/test_cv_bundle.py`, `tests/test_hackathon_scoring.py`, `tests/test_cv_contracts.py`, `tests/test_phase22_docs.py`, all green) while the full run continued in the background. It has since completed: exit code 0, no failures reported. Full-suite green is confirmed for this plan's changes.
- Otherwise none. The real bundle build (~27s wall clock for 11,834 files, ~1.5 GB) and rebuild both completed well within budget; `score_tracks.py`'s reproduction run against the shipped baseline tracks matched the documented number exactly on the first attempt.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `cv/bundle.py::build_bundle`/`bundle_manifest` are real, tested, and already reused by `ffep cv bundle` (wired in plan 02.2-05).
- The dev-set bundle is built, hashed, and documented; ready for OTC OBS delivery once plan 02.2-14 provisions the bucket (`deliver_bundle` remains a stub, explicitly deferred to that plan, not this one).
- Plan 02.2-12 (test/transfer bundles) inherits the test-kind label-leak guard (T-2.2-28) already implemented and tested here — it does not need to add this guard itself, only real second-game/transfer-domain content tables that pass it.
- `scripts/hackathon/score_tracks.py` is ready to ship inside the dev bundle's starter-kit / be referenced by the challenge doc's submission instructions.
- Known gap for a later plan: per-clip radar renders (see Deviations above) and `gt_positions.csv`'s withholding treatment for a real test-kind bundle (plan 02.2-12).
- No blockers for wave 5's remaining work.

## Verification

- `uv run pytest tests/test_cv_bundle.py -q` — 14 passed
- `uv run pytest tests/test_hackathon_scoring.py -q` — 6 passed
- `uv run pytest tests/test_cv_contracts.py -q` — green, `build_bundle`/`bundle_manifest` signatures unchanged from the plan 02.2-05 freeze
- `uv run pytest tests/test_cv_bundle.py tests/test_phase22_docs.py -q` — 25 passed (Task 3's stated verification command)
- `uv run ruff check src/flag_football_ep/cv/bundle.py tests/test_cv_bundle.py scripts/hackathon/score_tracks.py tests/test_hackathon_scoring.py` — clean
- Real run: `uv run --extra cv ffep cv bundle --kind dev --out data/bundles` — `data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip` (11834 files, `ad412c5cffb9a9b54a49e4e7c0b3075c0e013304cf8d1b7fb45d7ed5e5db2a56`); rebuilt a second time — identical hash and file count
- `git status --porcelain data/bundles` — empty (gitignore boundary respected)
- `unzip -l` of the real archive contains zero files matching any of the 18 `private_test = true` clip numbers (5,6,7,11,15,16,21,22,28,33,36,40,43,49,52,54,55,56), verified individually
- `uv run python scripts/hackathon/score_tracks.py --tracks data/bundles/dev-set/data/tracks.parquet --review data/bundles/dev-set/data/continuity_review.csv` — prints `Referenz-Baseline (Human-Urteile, aus --review): 10/43 (23.26%)`, exactly matching `docs/hackathon-bundles.md`'s documented pool-only baseline
- `grep -c "obere Schranke 47/61" docs/hackathon-challenge-reid.md` — 0
- `docs/hackathon-challenge-reid.md` `## Baseline-Zahlen` contains `15/61`
- `uv run pytest tests -q` (full suite, plan-level `<verification>` block) — completed after this session's other work, exit code 0, no failures (only pre-existing benign `RuntimeWarning`s from the `trackers` package's Kalman filter, matching prior sessions' notes)

## Self-Check: PASSED

- `src/flag_football_ep/cv/bundle.py` — FOUND, contains real `build_bundle`/`bundle_manifest` (no `NotImplementedError` for either)
- `tests/test_cv_bundle.py` — FOUND, 14 tests present and passing
- `scripts/hackathon/score_tracks.py` — FOUND, `--help` exits 0 and documents all four flags
- `tests/test_hackathon_scoring.py` — FOUND, 6 tests present and passing
- `docs/hackathon-bundles.md` — FOUND, states the archive's `content_sha256` and file count matching the real manifest
- `data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip` — FOUND on disk (gitignored)
- Commits `0f2d0eb`, `035c9fb`, `776e049` — all FOUND in `git log --oneline`
- All plan `<acceptance_criteria>` re-checked this session for Tasks 1-3: pass (see Verification above for exact commands/outputs)
- Plan-level `<verification>` block re-run: `tests/test_cv_bundle.py tests/test_hackathon_scoring.py -q` green; dev bundle rebuilt twice with an identical content hash on real data

---
*Phase: 02.2-dataset-buildout*
*Completed: 2026-09-01*

## Self-Check: PASSED (re-verified)

- `test -f` confirmed on disk: `src/flag_football_ep/cv/bundle.py`, `tests/test_cv_bundle.py`, `scripts/hackathon/score_tracks.py`, `tests/test_hackathon_scoring.py`, `docs/hackathon-bundles.md`, `data/bundles/dev-set_2026-09-01_ad412c5cffb9.zip`
- `git log --oneline --all | grep` confirmed commits `0f2d0eb`, `035c9fb`, `776e049` all present
