---
phase: 02.2-dataset-buildout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/pilot-gate-decision.md
  - docs/capture-protocol.md
  - docs/capture-legal.md
  - docs/hackathon-challenge-reid.md
  - docs/hackathon-challenge-prep.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - tests/test_phase22_docs.py
autonomous: true
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "A reader of docs/pilot-gate-decision.md sees a dated Nachtrag explaining why Phase 2.2 started on a TEILWEISE verdict and that the continuity remediation path is untouched"
    - "ROADMAP.md and REQUIREMENTS.md no longer claim Phase 2.2/REQ-S2-03 requires a passed pilot"
    - "docs/capture-protocol.md carries the three drone capture wishes from the gate findings"
    - "docs/capture-legal.md and docs/hackathon-challenge-reid.md record the federation approval as a dated user assurance (2026-08-31)"
  artifacts:
    - path: "tests/test_phase22_docs.py"
      provides: "Structural gate over the four doc edits and the two planning-file wording fixes"
      contains: "Nachtrag"
  key_links:
    - from: "docs/pilot-gate-decision.md"
      to: ".planning/ROADMAP.md"
      via: "same dated rationale, one commit"
      pattern: "2026-08-31"
---

<objective>
Reconcile the documentation with the recorded decision to start Phase 2.2 on the TEILWEISE gate
verdict (D-01), carry the three drone findings into the capture Wunschzettel (D-03), and record
the federation approval as a dated user assurance (D-06).

Purpose: the gate document is the project's honesty artifact. Starting 2.2 without amending it
would leave three files claiming a precondition that was consciously waived, and would leave the
hackathon doc claiming a blocking approval that has been granted.
Output: dated addenda in three `docs/` files, corrected wording in ROADMAP/REQUIREMENTS, and a
structural pytest gate that keeps the six edits from silently drifting apart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 — gate-decision Nachtrag plus ROADMAP/REQUIREMENTS wording, one commit</name>
  <files>docs/pilot-gate-decision.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/pilot-gate-decision.md (full — the existing section order is `## Zweck & Abgrenzung`, `## Ausgangslage`, `## Gate-Kriterien und Messung`, `## Extrapolationsformel`, `## Fehlerzerlegung und Vorbehalte`, `## Entscheidung`, `## Konsequenzen`, `## Demo`; match its German register and its habit of naming measured values inline)
    - .planning/ROADMAP.md lines 15-30 (phase list) and lines 311-323 (`### Phase 2.2: Dataset Buildout`)
    - .planning/REQUIREMENTS.md lines 39-45 (REQ list) and lines 80-88 (traceability table)
    - .planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md §Gate reconciliation (D-01, D-02)
  </read_first>
  <action>
    Append a new final section `## Nachtrag 2026-08-31 — Phase 2.2 trotz TEILWEISE freigegeben` to
    docs/pilot-gate-decision.md (German, dated, appended — never rewrite the existing verdict text).
    It states: (a) Phase 2.2 was released for detection robustness, which is gate criterion 2
    (PASSED), not for the missed track-continuity criterion; (b) D-06 of PROJECT.md ("clear miss →
    capture setup, never more training labels") is therefore not violated, because these labels do
    not answer the continuity failure; (c) the two continuity remediation paths stay untouched and
    run in parallel — steeper hover angle via the capture Wunschzettel (docs/capture-protocol.md)
    and appearance-based ReID via the BWI hackathon (docs/hackathon-challenge-reid.md); (d) the
    gate re-run (re-track plus full 61-clip continuity review) is explicitly NOT part of Phase 2.2
    and happens as its own step after the hackathon, triggered by the re-trigger clause (D-02).

    In .planning/ROADMAP.md replace the phrase `(only after passed pilot)` in the Phase 2.2
    summary bullet with `(started 2026-08-31 on the TEILWEISE verdict — detection robustness only,
    see docs/pilot-gate-decision.md Nachtrag 2026-08-31)`, and replace the `**Depends on**: Phase
    2.1 (gate PASSED)` line under `### Phase 2.2: Dataset Buildout` with `**Depends on**: Phase 2.1
    (gate TEILWEISE; released for detection robustness per D-01, 2026-08-31)`.

    In .planning/REQUIREMENTS.md replace the REQ-S2-03 lead-in `Only after passed pilot:` with
    `Released 2026-08-31 on the TEILWEISE pilot verdict (detection robustness, gate criterion 2):`
    and change the REQ-S2-03 traceability row status from `Pending (gated on 2.1)` to
    `In progress (Phase 2.2, released 2026-08-31 despite TEILWEISE — D-01)`.

    All three edits land in a single commit (D-01 requires them consistent), commit subject
    `docs(02.2): reconcile gate verdict with the Phase 2.2 start`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Nachtrag 2026-08-31" docs/pilot-gate-decision.md` returns >= 1
    - `grep -c "only after passed pilot" .planning/ROADMAP.md` returns 0
    - `grep -c "Only after passed pilot" .planning/REQUIREMENTS.md` returns 0
    - `grep -c "gate PASSED" .planning/ROADMAP.md` returns 0
    - The Nachtrag section names all four points: detection robustness, D-06 non-violation, the two remediation paths, and the deferred gate re-run
    - `git log -1 --name-only` for this task lists exactly the three files above
  </acceptance_criteria>
  <done>The gate document, ROADMAP and REQUIREMENTS tell the same story about why 2.2 started, in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: D-03 — capture Wunschzettel update, drone points only</name>
  <files>docs/capture-protocol.md</files>
  <read_first>
    - docs/capture-protocol.md (full, 123 lines — note the `Ideal / Brauchbar / Unbrauchbar` tier vocabulary, the `### Hover-Position & Winkel`, `### Feldabdeckung` subsections under `## Domäne 1 — Drohne (Primärdomäne)`, and the `## Ratifizierungs-Block` at the end)
    - docs/pilot-gate-decision.md §Fehlerzerlegung und Vorbehalte and §Konsequenzen (the three findings: hover angle too flat, east endzone never covered, in-clip camera cuts)
  </read_first>
  <action>
    Append a dated section `## Nachtrag 2026-08-31 — Wünsche aus dem Piloten-Gate (nur Drohne)`
    ahead of the `## Ratifizierungs-Block` section, carrying exactly three wishes and nothing else:
    (1) steilerer, stärker senkrechter Hover-Winkel (rationale: flat angle maximises mutual
    occlusion, the dominant ID-switch mode); (2) Abdeckung der Ost-Endzone, die in der
    Piloten-Session nie im Bild war; (3) keine Kameraschnitte innerhalb eines Clips.
    Keep the existing tier vocabulary verbatim (`Ideal`/`Brauchbar`/`Unbrauchbar`) and phrase the
    section as a wish list, matching the doc's own `## Zweck & Ton — Wunschliste, kein Pflichtenheft`
    framing. Do NOT add a side-camera request — the user rejected it (CONTEXT.md Deferred Ideas).
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Nachtrag 2026-08-31" docs/capture-protocol.md` returns >= 1
    - The new section mentions all three of: Hover-Winkel, Endzone, Kameraschnitt
    - `grep -ci "seitenkamera" ` on the new section only matches the pre-existing `## Domäne 2` section, i.e. the Nachtrag adds no new side-camera request
    - No existing line of docs/capture-protocol.md is deleted (`git diff --stat` shows insertions only)
  </acceptance_criteria>
  <done>The capture protocol carries the three drone findings as wishes for the next session, nothing more.</done>
</task>

<task type="auto">
  <name>Task 3: D-06 — approval state in capture-legal and the hackathon docs, plus the doc gate test</name>
  <files>docs/capture-legal.md, docs/hackathon-challenge-reid.md, docs/hackathon-challenge-prep.md, tests/test_phase22_docs.py</files>
  <read_first>
    - docs/capture-legal.md (full, 28 lines — short register, `## DSGVO — Einverständnis liegt beim Verband` is the section the addendum attaches to)
    - docs/hackathon-challenge-reid.md §Datenschutz and the status header at the top (currently: "Voraussetzung vor Einreichung: Freigabe des Verbands/Teams")
    - docs/hackathon-challenge-prep.md §1 (the `[ ] [NUTZER] Verbands-Freigabe` checklist item)
    - tests/test_cv_gate_artifacts.py (the structural-doc-gate test style this new test file copies: read the file, assert section headings and cited evidence exist)
  </read_first>
  <action>
    In docs/capture-legal.md append `## Nachtrag 2026-08-31 — Freigabe des Verbands` recording the
    user assurance verbatim in substance: the federation confirmed on 2026-08-31 ("die Anfrage ist
    positiv, wir haben alle Befugnisse") covering hackathon use, distribution of the TV broadcast
    material, and publication of the dataset. Mark it explicitly as a dated user assurance, not a
    legal opinion, and keep the existing "Verwendungs-Scope: interne Analyse, keine Veröffentlichung"
    sentence intact by adding the qualifier that publication is now covered but deliberately deferred
    until after the hackathon (D-19).

    In docs/hackathon-challenge-reid.md: update the status header so the approval is recorded as
    granted (2026-08-31) instead of an open precondition, and extend §Datenschutz with the same
    dated assurance plus the unchanged operational rules (no cloud upload, usage agreement for
    participants, deletion after the event).

    In docs/hackathon-challenge-prep.md tick the `[NUTZER] Verbands-Freigabe` item to `[x]` with the
    date and leave the Bundeswehr and BWI-infrastructure items open.

    Create tests/test_phase22_docs.py as a structural gate over all three tasks of this plan: assert
    the `Nachtrag 2026-08-31` heading exists in pilot-gate-decision.md, capture-protocol.md and
    capture-legal.md; assert the forbidden strings `only after passed pilot`, `Only after passed
    pilot` and `gate PASSED` are absent from ROADMAP/REQUIREMENTS; assert the capture-protocol
    Nachtrag names Hover-Winkel, Endzone and Kameraschnitt; assert the hackathon doc contains
    `2026-08-31` in its Datenschutz section. Use `REPO_ROOT = Path(__file__).resolve().parent.parent`
    and plain `read_text(encoding="utf-8")` assertions, exactly like tests/test_cv_gate_artifacts.py.
    Where a count is asserted over a Markdown file, strip comment/heading noise the way that file
    does — never assert `== 0` on an unfiltered whole-file grep.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_phase22_docs.py -q` passes with at least 6 assertions/test functions
    - docs/capture-legal.md contains the strings `2026-08-31` and `alle Befugnisse`
    - docs/hackathon-challenge-reid.md status header no longer states the approval as a pending precondition (`grep -c "Voraussetzung vor Einreichung: Freigabe" docs/hackathon-challenge-reid.md` returns 0)
    - docs/hackathon-challenge-prep.md line for `Verbands-Freigabe` starts with `- [x]`
    - The test file fails if any one of the three Nachtrag sections is removed (verify by temporary deletion during development, restore afterwards)
  </acceptance_criteria>
  <done>The approval state is documented in both the legal doc and the challenge doc, and a test keeps all six edits from drifting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public hackathon submission | Text copied from these docs may end up in a public challenge form |
| project docs → federation/legal claims | Statements about approval must be traceable to a dated user assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-01 | Repudiation | docs/capture-legal.md approval claim | mitigate | Record the approval as a dated user assurance with the quoted statement and its date; never phrase it as an independent legal assessment |
| T-2.2-02 | Information Disclosure | doc addenda | mitigate | No player names, no file paths under `data/video/` or `data/labels/` with identifiable content are added to any tracked doc |
| T-2.2-03 | Tampering | ROADMAP/REQUIREMENTS wording | mitigate | tests/test_phase22_docs.py asserts the reconciled wording, so a later edit cannot silently restore the "only after passed pilot" claim |
</threat_model>

<verification>
- `uv run pytest tests/test_phase22_docs.py -q` green
- `uv run pytest tests -q` green (no regression in tests/test_cv_gate_artifacts.py)
- `git log --oneline -3` shows the D-01 edits as one commit touching exactly the three D-01 files
</verification>

<success_criteria>
- Three dated Nachtrag sections exist (gate decision, capture protocol, capture legal)
- ROADMAP and REQUIREMENTS carry the reconciled Phase 2.2 / REQ-S2-03 wording
- The hackathon challenge doc and prep checklist record the federation approval as granted
- tests/test_phase22_docs.py gates all of the above
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-01-SUMMARY.md` when done
</output>
