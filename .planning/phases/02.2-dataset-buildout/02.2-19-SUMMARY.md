---
phase: 02.2-dataset-buildout
plan: 19
subsystem: cv
tags: [dataset, publication, licence, dataset-card, gdpr, phase-closing, model-registry, promotion]

# Dependency graph
requires:
  - phase: 02.2-dataset-buildout
    provides: dataset v2 (755 verified frames, plan 02.2-17/18), the frozen eval-GT and its bias-test correction (02.2-EVAL-GT, 02.2-BIAS-TEST), the champion-leak diagnosis (02.2-DIAG)
provides:
  - "docs/dataset-publication.md -- publication assessment: platform comparison (LOW confidence), four licence options with reasoning, 2026-08-31 federation approval as legal basis, post-hackathon timing, publication-time checklist, dated `defer` licence decision"
  - "docs/dataset-card.md -- dataset card for v2: v1/v1.1/v1.2/v2 provenance table, per-domain sources and measured player size, clip-level split rationale, labelling convention, eval-GT provenance with the prelabel-bias caveat, seven named limitations, privacy statement, deferred licence section"
  - "tests/test_phase22_docs.py -- structural guards for both new documents"
  - "cv_detector_model champion alias promoted to iteration-1 (be854a1adebf4eb4b01d98dc39022ee1), hackathon-frozen left unchanged -- owner decision 2026-09-11"
  - "docs/dataset-buildout.md phase closing record: deliverables/hashes, promotion record, REQ-S2-03 scored against its three criteria, what this phase did not do and who inherits it"
affects: [phase-2.2-closing, future-dataset-publication, post-hackathon-gate-rerun, cv-tracking-and-inference-downstream-of-champion]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inline cross-references to other Markdown headings inside a doc must not repeat the literal '## Heading' string before the real heading -- the shared _section() heading-extraction helper (test_phase22_docs.py, test_cv_gate_artifacts.py) does a plain text.index() and will silently grab the wrong slice otherwise; use quoted plain text for inline refs instead"
    - "The M3-05 promotion gate (model/gate.py) is wired only into the EP/WP model registry (model/registry.py::promote, CLI `ffep promote`) via its EP/WP-only metric-key resolution -- it is not imported by, and does not apply to, the CV detector registry's promote path (cv/registry.py::promote, CLI `ffep cv promote`), which has no gate and no --force/--reason flag"

key-files:
  created:
    - docs/dataset-publication.md
    - docs/dataset-card.md
  modified:
    - tests/test_phase22_docs.py
    - docs/dataset-buildout.md

key-decisions:
  - "Task 3's licence decision (CC BY-NC 4.0 / CC BY 4.0 / custom research-use / defer) is NOT the same decision as docs/lizenz-inventur.md's 2026-09-09 Entscheidung -- that decision covers the CODE repository (Apache-2.0, repo goes private) and is already closed; the dataset licence is a separate, still-open rights decision over identifiable persons, exactly as RESEARCH assumption A5 flagged"
  - "Owner decision 2026-09-11 (verbatim 'defer, runde1'): dataset licence choice postponed to the actual publication moment; recorded dated in docs/dataset-publication.md §7 and mirrored in docs/dataset-card.md §Lizenz, together with the owner's clarification that whether/when the dataset is published at all remains open (the code repo stays private) -- D-19 sets only the earliest possible timing, not a commitment to publish then"
  - "Owner decision 2026-09-11 (explicit instruction, not an automatic stopping-rule outcome): promote iteration-1 (be854a1adebf4eb4b01d98dc39022ee1) to the cv_detector_model champion alias, based on the 2026-09-11 prelabel-bias test showing the previously measured drone 'regression' and the GoPro gap were largely measurement bias, not a real model regression. hackathon-frozen intentionally left unchanged at the pilot run (87a8a5222f7a472787875e974d089c44) -- the three already-built hackathon bundles stay pinned to it"
  - "Verified the M3-05 promotion gate (model/gate.py) does not apply to the CV detector registry by reading its source (EP/WP-only metric-key dict, never imported by cv/registry.py or cv/commands.py::promote) -- promoted directly via `ffep cv promote --run <id>`, no --force/--reason needed, no gate bypass occurred because no gate exists on this path"

requirements-completed: [REQ-S2-03]

# Metrics
duration: two sessions (paused at checkpoint, resumed after owner decision)
completed: 2026-09-11
---

# Phase 02.2 Plan 19: Publication Assessment, Dataset Card, Licence Decision and Champion Promotion Summary

**Publication assessment and dataset card written in German for dataset v2 (755 frames); owner deferred the dataset licence decision (code repo stays private, publication itself undecided); owner separately instructed promoting iteration-1 (be854a1a...) to the `cv_detector_model` champion alias after the prelabel-bias test showed the earlier "regression" was largely measurement bias -- hackathon-frozen stays pinned to the original pilot run; phase closing record appended to docs/dataset-buildout.md.**

## Performance

- **Started:** 2026-09-11
- **Tasks completed:** 3 of 3 (Task 3's checkpoint was resolved by the owner mid-session; a champion-promotion instruction was added by the owner beyond the plan's original Task 3 scope and executed as a course correction)
- **Files created:** 2
- **Files modified:** 2 (test file extended; dataset-buildout.md appended)

## Accomplishments

- **`docs/dataset-publication.md`** (final, with §7 filled in): what would be published (dataset
  v2 full frames + COCO annotations, explicitly excluding the private Puerto Rico testset and the
  frozen eval clips until after the event); the legal basis (2026-08-31 federation approval,
  `docs/capture-legal.md`); a platform comparison (HuggingFace Datasets, Zenodo, Roboflow
  Universe) explicitly marked LOW confidence per RESEARCH; four licence options with reasoning;
  the post-hackathon timing rationale (D-19); a seven-item publication-time checklist; and §7,
  the dated owner decision (`defer`, verbatim "defer, runde1"), including the owner's clarification
  that publication itself -- not just the licence -- remains undecided while the code repository
  stays private.
- **`docs/dataset-card.md`** (final, with §Lizenz updated): purpose/intended use; per-domain
  sources with measured apparent-player-size figures; a full version-history table (v1/v1.1/v1.2/
  v2) with box counts and `content_sha256` per version, spot-checked against
  `docs/dataset-buildout.md`'s own merge tables; clip-level split rule and reason; the labelling
  convention verbatim in substance; a provenance table (four `content_sha256` values, four DVC
  pointer commits, four detector MLflow run ids); seven named limitations; a privacy statement;
  and a licence section mirroring the deferred decision.
- **`tests/test_phase22_docs.py`** extended with 13 new structural assertions across both
  documents -- `uv run pytest tests/test_phase22_docs.py -q` -- 40 passed.
- **Champion promotion:** `ffep cv promote --run be854a1adebf4eb4b01d98dc39022ee1` executed
  directly (no gate applies to this path, see Deviations/Decisions). Verified before/after with
  `registry.resolve_champion`/`freeze.resolve_frozen`:

  | Alias | Before | After |
  |---|---|---|
  | `champion` | `87a8a5222f7a472787875e974d089c44` (v1) | `be854a1adebf4eb4b01d98dc39022ee1` (v2) |
  | `hackathon-frozen` | `87a8a5222f7a472787875e974d089c44` | `87a8a5222f7a472787875e974d089c44` (unchanged) |

- **`docs/dataset-buildout.md`** phase closing record appended: a `## Champion-Beförderung`
  section (owner decision, gate applicability check, before/after table) and a
  `## Phase-2.2-Abschlussprotokoll` section (delivered artefacts with hashes, detector registry
  table with all six versions, hackathon bundle status, the eval/private-test split explained
  across its two governing CSVs, documentation produced, a "what this phase did NOT do and who
  inherits it" table with eight rows, and REQ-S2-03 scored criterion-by-criterion).

## Task Commits

1. **Task 1: Write the publication assessment** - `7c7e8d8` (docs)
2. **Task 2: Write the dataset card** - `27eaf38` (docs, includes the test-file extension)
3. **Task 3a: Record the deferred licence decision** - `3e66b02` (docs)
4. **Task 3b (owner course correction): Promote champion + close out phase** - `e57cd79` (docs)

## Files Created/Modified

- `docs/dataset-publication.md` -- publication assessment, licence decision recorded in §7
- `docs/dataset-card.md` -- dataset card for v2, §Lizenz mirrors the deferred decision
- `tests/test_phase22_docs.py` -- 13 new assertions guarding both documents
- `docs/dataset-buildout.md` -- champion-promotion record + phase closing record appended

## Decisions Made

- **Licence: `defer`** (owner, 2026-09-11, verbatim "defer, runde1"). None of the four options is
  locked in; the decision itself is to decide later, at the actual publication moment. The owner
  also clarified that publication itself -- not only the licence -- is undecided, since the code
  repository stays private (`docs/lizenz-inventur.md`); recorded honestly in both documents rather
  than implying a publication commitment that doesn't exist.
- **Champion promotion: iteration-1 (`be854a1adebf4eb4b01d98dc39022ee1`)** (owner, 2026-09-11,
  explicit instruction after reviewing the prelabel-bias test). This is not the automatic
  stopping-rule outcome from Plan 02.2-18 (which measured "not promoted" on the original,
  prelabel-biased comparison) -- it is a direct owner override based on the 2026-09-11 bias-test
  finding that the measured gap was largely measurement bias. `hackathon-frozen` stays pinned to
  the original pilot run by explicit owner instruction, keeping the three already-built and
  hashed hackathon bundles untouched.
- **Clarification, not itself a decision:** `docs/lizenz-inventur.md`'s 2026-09-09 "privat +
  Apache" decision covers the code repository, not the dataset -- confirmed by reading it in full
  before treating Task 3 as pre-answered. The two licence questions (code, dataset) are
  independent.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed a false-positive heading-match footgun in inline cross-references**
- **Found during:** Task 2 (writing docs/dataset-card.md, running its structural tests)
- **Issue:** `docs/dataset-card.md` used inline references like `` `## Split-Methodik` `` (the
  literal heading string, in backticks) to point readers at other sections. The shared
  `_section()` test helper (used by `tests/test_phase22_docs.py` and
  `tests/test_cv_gate_artifacts.py`) extracts a section via a plain `text.index(heading)` --
  the first occurrence of that literal string, not necessarily the real `##`-prefixed heading
  line. Because the inline reference appeared earlier in the file than the actual heading, two
  new tests extracted the wrong slice and failed.
- **Fix:** Replaced all inline heading-style references with quoted plain text (e.g.
  „Split-Methodik") that reads identically to a human but no longer collides with the
  heading-extraction helper.
- **Files modified:** `docs/dataset-card.md`
- **Verification:** `uv run pytest tests/test_phase22_docs.py -x -q` -- 40 passed (was failing on
  2 tests before the fix)
- **Committed in:** `27eaf38` (Task 2 commit)

### Scope addition (owner-directed, not a Rule 1-3 auto-fix)

**2. Champion promotion added to Task 3's scope by explicit owner instruction**

The plan's Task 3 as written covers only the licence decision and phase closing record --
`<action>` explicitly says "record the recommendation for the owner", and the objective this
executor was launched with said "do NOT move any alias here unless the plan explicitly tells you
to." The owner's mid-session message added a second, separate instruction (promote iteration-1 to
champion) that goes beyond the plan's written scope. This was not treated as a Rule 1-4 deviation
(no bug, no missing-critical-functionality, no blocker, no architectural ambiguity) but as a
direct course correction from the party that can authorize scope changes -- executed as instructed,
with the requested verification (gate-applicability check, before/after `resolve_champion`/
`resolve_frozen`) and documented in full in `docs/dataset-buildout.md`.

---

**Total deviations:** 1 auto-fixed (1 bug, Rule 1) + 1 owner-directed scope addition (documented,
not a deviation rule).
**Impact on plan:** The bug fix is editorial-only, no scope creep. The scope addition is a
first-class owner decision, executed transparently with its own before/after verification and
closing-record entry -- not silently absorbed into "Task 3 as originally scoped."

## Issues Encountered

None outstanding. The full test suite (`uv run pytest tests -q`) was started in the background
during Task 2 and finished with exit code 0 (no failures/errors, pre-existing warnings only)
before Task 3 began. The CV contract tests requested for Task 3
(`tests/test_cv_contracts.py tests/test_cv_registry.py tests/test_cv_freeze.py`) and
`tests/test_phase22_docs.py` were re-run after the champion promotion and closing-record commit
-- all green.

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- **REQ-S2-03 satisfied** on all three of its stated criteria except the 1,500-frame floor
  (partially met, honestly reported as not reached -- 755/1,500). Dataset versioning, per-domain
  eval, and the publication assessment are all complete.
- **Champion alias now points at iteration-1** (`be854a1adebf4eb4b01d98dc39022ee1`) -- any future
  plan or script that resolves `champion` without an explicit `--run` (e.g. `ffep cv detect`/
  `ffep cv track` outside the frozen bundle pipeline) will load this run from now on, not the
  Phase-2.1 pilot. `hackathon-frozen` is unaffected.
- **Post-hackathon gate re-run (D-02)** inherits: whether the promoted champion changes the
  61-clip continuity review outcome is untested by this phase.
- **Publication (D-19)** inherits: both the licence and the publish/don't-publish question itself,
  now explicitly recorded as open rather than implied-eventual.
- **Real OTC-OBS delivery (Plan 02.2-20)** remains not run -- credentials still unavailable;
  bundles and dataset are pushed only to the local DVC fallback remote.
- Full inheritance table with all eight open items: `docs/dataset-buildout.md
  ## Phase-2.2-Abschlussprotokoll §Was diese Phase NICHT getan hat, und wer es erbt`.

## Self-Check

```
FOUND: docs/dataset-publication.md
FOUND: docs/dataset-card.md
FOUND: docs/dataset-buildout.md (champion-promotion + closing-record sections present)
FOUND commit 7c7e8d8 (docs(02.2-19): assess dataset publication option)
FOUND commit 27eaf38 (docs(02.2-19): write dataset card for v2 and guard both docs with tests)
FOUND commit 3e66b02 (docs(02.2-19): record deferred dataset licence decision)
FOUND commit e57cd79 (docs(02.2-19): promote iteration-1 to champion, close out phase 2.2)
PASSED: uv run pytest tests/test_phase22_docs.py -q (40 passed)
PASSED: uv run pytest tests -q (full suite; exit code 0)
PASSED: uv run pytest tests/test_cv_contracts.py tests/test_cv_registry.py tests/test_cv_freeze.py -q (all green)
VERIFIED: registry.resolve_champion('cv_detector_model', cfg) == 'be854a1adebf4eb4b01d98dc39022ee1'
VERIFIED: freeze.resolve_frozen('cv_detector_model', cfg) == '87a8a5222f7a472787875e974d089c44'
```

## Self-Check: PASSED

---
*Phase: 02.2-dataset-buildout*
*Completed: 2026-09-11*
