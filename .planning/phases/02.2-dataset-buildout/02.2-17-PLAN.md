---
phase: 02.2-dataset-buildout
plan: 17
type: execute
wave: 9
depends_on: ["02.2-16"]
files_modified:
  - data/labels/dataset.dvc
  - docs/dataset-buildout.md
autonomous: false
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "Every iteration-2 frame was seen and confirmed or corrected by the user in CVAT"
    - "The merged dataset reaches at least the 1,500-frame floor with per-domain coverage intact"
    - "Dataset v2 is DVC-tracked with its pointer committed, and both hashes are recorded"
  artifacts:
    - path: "data/labels/dataset.dvc"
      provides: "Updated DVC pointer for dataset v2"
      contains: "md5"
    - path: "docs/dataset-buildout.md"
      provides: "Iteration-2 dataset record: counts, hashes, floor status"
  key_links:
    - from: "data/labels/dataset.dvc"
      to: "git history"
      via: "one pointer commit per dataset version"
      pattern: "outs"
---

<objective>
Turn the second labelling block into dataset v2: 100 % human verification (D-17) within the weekend
budget (D-16), merged and domain-validated, DVC-versioned, and measured against the mandatory
1,500-frame floor.

Purpose: this is the plan where REQ-S2-03's headline number becomes real — and where it must be
reported honestly if the floor was not reached.
Output: an updated dataset directory, its content hash, a second committed `.dvc` pointer, and the
German record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-13-SUMMARY.md
@.planning/phases/02.2-dataset-buildout/02.2-16-SUMMARY.md
@docs/dataset-buildout.md
@docs/dataset-plan.md
@docs/cv-setup.md

<interfaces>
Identical pipeline to plan 02.2-13, one iteration later:
  ffep cv cvat-pull --task <id> --out <dir>
  merge into the single growing directory data/labels/dataset/  (ids renumbered)
  ffep cv dataset --coco data/labels/dataset --manifest <merged manifest>
      → validate_coco(..., min_images=_MIN_IMAGES_MULTIDOMAIN, max_images=_MAX_IMAGES_MULTIDOMAIN)
  dvc add data/labels/dataset ; git add data/labels/dataset.dvc ; dvc push

Trim precedent (docs/cv-setup.md ### Datensatz): frames pushed but not corrected are removed from
`instances.json` and the manifest before validation and kept beside it as `instances.full-<n>.json`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Labelling weekend 2 — correct and confirm every iteration-2 frame</name>
  <files>none — corrections are made inside CVAT, not in the repo</files>
  <action>
    Pause execution and point the user at docs/dataset-buildout.md `## Labelling-Anleitung Iteration 2` plus the iteration-2 CVAT task ids. Do not pull or merge before the user reports back. Record the stop point if any, and the user's observation on whether the correction burden per frame dropped relative to iteration 1 — that observation is the practical test of the model-in-the-loop claim and belongs in Task 3's record.
  </action>
  <what-built>
    CVAT tasks `al-2-<domain>-<index>` prelabelled by the retrained detector; the ids and the
    labelling instruction are in `docs/dataset-buildout.md ## Labelling-Anleitung Iteration 2`.
    Prelabels should be noticeably better than in iteration 1 — most frames confirm-only.
  </what-built>
  <how-to-verify>
    1. Start CVAT as documented in `docs/cv-setup.md ## CVAT`.
    2. Work through every iteration-2 task, applying the same binding convention as before
       (every clearly visible person boxed; `referee` only for active on-field officials; everyone
       else `player`; boxes tight at the feet, shadow excluded; full visible body).
    3. Mark each task complete only when every frame in it has been reviewed (D-17).
    4. Note whether the correction burden actually dropped compared with iteration 1 — that
       observation belongs in the record, since it is the practical test of the model-in-the-loop
       claim.
    5. If you stop early, say exactly where. The frame count adapts; the convention does not.
  </how-to-verify>
  <acceptance_criteria>
    - Every iteration-2 CVAT task is complete, or the stop point is stated exactly
    - The user reports whether the correction burden per frame fell relative to iteration 1
    - Any new convention question is recorded for the docs update in Task 3
  </acceptance_criteria>
  <resume-signal>Type "iteration 2 labelled" or "stopped at task X frame Y"</resume-signal>
  <verify>
    <automated>uv run pytest tests/test_cv_dataset.py -x -q</automated>
  </verify>
  <done>Every iteration-2 CVAT task is complete or an exact stop point is recorded, and the correction-burden observation is captured.</done>
</task>

<task type="auto">
  <name>Task 2: Pull, merge, validate against the floor</name>
  <files>docs/dataset-buildout.md</files>
  <read_first>
    - src/flag_football_ep/cv/dataset.py (`export_cvat_task`, `validate_coco`, `dataset_hash`)
    - data/labels/al-iteration-2/selection.json
    - docs/dataset-buildout.md (iteration-1 counts, so the deltas can be reported)
    - docs/dataset-plan.md (the mix and the floor/ceiling)
  </read_first>
  <action>
    Pull every iteration-2 task, merge into `data/labels/dataset/` with renumbered ids, apply the
    trim precedent for any uncorrected remainder, and validate with the multi-domain band. Record
    the full `DatasetStats`: images per domain, boxes per class and per domain, empty images,
    train/val split sizes, and the new `content_sha256`.

    Report the total against the 1,500-frame floor explicitly. If the floor is not reached, state
    the shortfall, the reason (labelling-time budget, material exhaustion, stopping-rule verdict)
    and what would close it — do not round up, and do not quietly redefine "verified".
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_dataset.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ffep cv dataset` exits 0 against the merged directory and prints the stats
    - The total image count and its relation to the 1,500 floor is stated in one unambiguous sentence
    - `boxes_by_domain` shows a non-zero player count for every in-scope domain
    - The new `content_sha256` differs from the iteration-1 hash and is recorded
  </acceptance_criteria>
  <done>Dataset v2 is merged, validated and measured against the requirement's floor.</done>
</task>

<task type="auto">
  <name>Task 3: Version dataset v2 and update the dataset documentation</name>
  <files>data/labels/dataset.dvc, docs/dataset-buildout.md</files>
  <read_first>
    - data/labels/dataset.dvc (the v1 pointer)
    - docs/cv-setup.md `## Dataset-Versionierung (DVC)`
    - .planning/phases/02.2-dataset-buildout/02.2-14-SUMMARY.md (whether the OTC remote is live or the local fallback applies)
  </read_first>
  <action>
    Run `dvc add data/labels/dataset`, commit the updated pointer with subject
    `feat(02.2): version dataset v2 after AL iteration 2`, and `dvc push` to whichever remote plan
    02.2-14 established. Append to docs/dataset-buildout.md: the new DVC MD5, the pointer commit
    hash, the `content_sha256`, the per-domain and per-split counts, the correction-burden
    observation from Task 1, and any convention clarification that arose.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dvc_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `data/labels/dataset.dvc` has a new `md5` differing from the v1 pointer, and the change is committed
    - `dvc status` reports the workspace up to date
    - `git status --porcelain data/labels` shows only the pointer file
    - docs/dataset-buildout.md carries both hashes and the pointer commit for v2
  </acceptance_criteria>
  <done>Both dataset versions are reproducible from git plus the DVC remote.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CVAT export → tracked repo | Only pointers and statistics may be tracked |
| dataset claims → REQ-S2-03 | The frame count is the requirement's headline number |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-52 | Information Disclosure | label data in git | mitigate | `dvc add` ignores the data; acceptance check asserts only the pointer appears in `git status` |
| T-2.2-53 | Repudiation | frame-count claim | mitigate | Uncorrected frames trimmed per the 2.1 precedent; the floor status is stated in one unambiguous sentence, shortfall included |
| T-2.2-54 | Tampering | dataset provenance | mitigate | Both hashes and the pointer commit recorded per version |
</threat_model>

<verification>
- `uv run pytest tests/test_cv_dataset.py tests/test_dvc_layout.py -q` green
- `uv run pytest tests -q` green
- Two dataset pointer commits exist in git history

</verification>

<success_criteria>
- 100 % of iteration-2 frames human-verified
- Dataset v2 validated, floor status stated honestly
- DVC pointer committed and pushed
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-17-SUMMARY.md` when done
</output>
