---
phase: 02.2-dataset-buildout
plan: 17
subsystem: cv
tags: [cvat, dvc, dataset-versioning, active-learning, multi-domain, coco, d-17]

# Dependency graph
requires:
  - phase: 02.2-dataset-buildout
    provides: "02.2-16's AL iteration-2 selection/prelabel/CVAT push (tasks 8/9/10, drone 183/sideline 150/broadcast 101), dataset v1.2 (572 images) from 02.2-13/EVAL-GT's D-19 guard (assert_no_frozen_eval_clips wired unconditionally into validate_coco)"
provides:
  - "Dataset v2: data/labels/dataset/ (755 images: drone 514, sideline 57, broadcast 184), DVC-tracked, md5 4b1652c97f6ca4e2f0032a6a7e2334a3.dir, content_sha256 d87dd04cb7ed53cc3436e02596233937971df0edfbcf9cff628192a9d8963dce"
  - "A real, previously-latent NameError bug in ffep cv dataset fixed (cfg never assigned before use)"
  - "docs/dataset-buildout.md's iteration-2 merge record: per-domain touched/untouched counts, exclusion assertions, validation stats, both hashes, honest floor status"
affects: [02.2-18-iteration-2-detector-training-and-evaluation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Domain-uniform D-17 touched-frame rule: unlike iteration 1's original (later corrected) assumption that a fully-reviewed CVAT task means every frame counts, this merge applies the file-diff-against-prelabel test (box count, category_id, bbox within 1px) identically to all three domains -- no 'drone/broadcast are trusted, only sideline gets diffed' exception. A frame only enters the dataset if the diff proves an edit happened."
    - "Zero-box frames (touched or untouched) are never treated as a confirmed empty -- every AL frame is drawn from a live play with players on it, so 0 detections after correction is read as an unreviewed far-field skip, consistent with plans 02.2-13/02.2-16's precedent"

key-files:
  created: []
  modified:
    - data/labels/dataset.dvc
    - docs/dataset-buildout.md
    - src/flag_football_ep/cv/commands.py

key-decisions:
  - "Applied the strict, domain-uniform touched-frame filter (only file-diff-confirmed edits count) to all three iteration-2 domains, not just sideline -- the D-17 violation that produced v1.1/v1.2 came from trusting 'the user says she reviewed it' for drone/broadcast without a diff check; this merge does not repeat that assumption even though the owner reported all three iteration-2 tasks as fully reviewed"
  - "183 of 434 pulled frames (42.2%) were touched and merged; the resulting 755/1500 (50.3%) does not close the 1,500-frame floor after both planned AL iterations -- reported as an explicit shortfall, not rounded up or reinterpreted"
  - "Pushed dataset v2 to the local-fallback DVC remote only; the real OTC-OBS push was attempted and failed with the same 403 Forbidden against the still-unprovisioned placeholder bucket documented in every prior plan (02.2-20, which resolves the real remote, has not run yet -- no SUMMARY exists for it)"
  - "Fixed cv/commands.py's dataset command (Rule 1 bug: cfg referenced before assignment) rather than working around it, since it blocked the plan's own required validation command from running at all"

requirements-completed: [REQ-S2-03]

# Metrics
duration: ~45min
completed: 2026-09-10
---

# Phase 02.2 Plan 17: AL Iteration-2 Merge, Domain-Uniform D-17 Filter & DVC Dataset v2 Summary

**Merged the 183 file-diff-confirmed touched frames from CVAT tasks 8/9/10 into a 755-image, three-domain COCO dataset (drone 514 / sideline 57 / broadcast 184), applying the D-17 touched-frame rule uniformly across all domains this time, and versioned it with DVC (md5 `4b1652c97f6ca4e2f0032a6a7e2334a3.dir`, content_sha256 `d87dd04cb7ed53cc3436e02596233937971df0edfbcf9cff628192a9d8963dce`) -- the 1,500-frame floor is not reached (755/1500, 50.3%).**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-09-10 (gate cleared by owner same day)
- **Completed:** 2026-09-10
- **Tasks:** 2 (Task 1, the labelling checkpoint, was already cleared by the owner before this session started per the objective's gate statement; Task 2: pull/merge/validate; Task 3: DVC versioning + docs)
- **Files modified:** 3 (`data/labels/dataset.dvc`, `docs/dataset-buildout.md`, `src/flag_football_ep/cv/commands.py`)

## Accomplishments

- Pulled all three iteration-2 CVAT tasks via `ffep cv cvat-pull` (task 8 `al-2-drone-1-1`, 183 images; task 9 `al-2-sideline-1-1`, 150 images; task 10 `al-2-broadcast-1-1`, 101 images)
- Ran a file-diff (box count, `category_id`, `bbox` within 1.0px tolerance) between each pulled image and its own prelabel COCO package, applied identically across all three domains (no drone/broadcast exception this time, unlike iteration 1's original, later-corrected assumption): drone 64/183 touched (35.0%), sideline 35/150 touched (23.3%, plus 10 confirmed far-field zero-detection frames left untouched, matching the champion prelabel's own zero-detection set exactly), broadcast 84/101 touched (83.2%)
- Merged the 183 touched frames into `data/labels/dataset/`, domain-prefixed and renumbered, alongside the existing 572-frame v1.2 base -- dataset v2: 755 images (drone 514, sideline 57, broadcast 184), 12139 `player` + 1427 `referee` boxes, 0 images without annotation, every domain carrying a non-zero `player` count
- Verified exclusions: 0 Puerto Rico frames across all 755 merged frames; `assert_no_frozen_eval_clips` (the D-19 guard) ran cleanly inside `validate_coco` with no exception -- no frozen eval clip entered the training dataset
- Found and fixed a real, previously-undiscovered bug in `ffep cv dataset`: `cv/commands.py`'s `dataset` command called `load_config(config)` without binding the result, then referenced the unbound `cfg` a few lines later for the `eval_split_path` D-19 guard wired in by plan 02.2-EVAL-GT -- `NameError` on every real invocation, only ever masked because no test fixture exercised the eval-guard code path
- Versioned dataset v2 with DVC (`dvc add`, new pointer committed) and pushed to the proven `local-fallback` remote (191 files); the real OTC-OBS push was attempted and failed identically to every prior plan (`403 Forbidden`, placeholder bucket `ffep-datasets-PLACEHOLDER` still unprovisioned -- plan 02.2-20 has not run)
- `docs/dataset-buildout.md` extended with the full iteration-2 merge record (touched/untouched table per domain, exclusion assertions, validation stats, both hashes, honest floor statement) and the top status header updated

## Task Commits

1. **Task 2 (blocking bug found while running the plan's own acceptance command): fix `cfg` unassigned in `ffep cv dataset`** - `dc555db` (fix)
2. **Task 2 + Task 3: merge AL iteration-2 into dataset v2, version with DVC** - `dbe6a10` (feat)

**Plan metadata:** (this commit, following SUMMARY.md write)

## Files Created/Modified

- `data/labels/dataset.dvc` - dataset v2 DVC pointer (md5 `4b1652c97f6ca4e2f0032a6a7e2334a3.dir`, 757 files)
- `docs/dataset-buildout.md` - `## Iteration-2-Merge, Validierung und Dataset v2 (Plan 02.2-17)` section (diff methodology, per-domain touched/untouched table, merge stats, DVC record, CLI-bug note); top status header updated
- `src/flag_football_ep/cv/commands.py` - `dataset` command: `cfg = load_config(config)` (was unbound `load_config(config)`)

## Decisions Made

- Applied the domain-uniform D-17 touched-frame filter to all three iteration-2 domains, not repeating iteration 1's original "drone/broadcast are fully reviewed, trust the task-complete flag" assumption that the 2026-09-02 correction had to walk back for sideline -- the owner reporting all three tasks complete does not substitute for the file-diff proof this project's own precedent requires.
- Did not attempt to close the 1,500-frame floor gap within this plan -- REQ-S2-03's floor is a two-iteration cumulative target (`docs/dataset-plan.md`), and this is the second of the two planned iterations; 755/1500 is reported as the honest, final post-two-iteration state, not padded or reinterpreted.
- Pushed only to the `local-fallback` DVC remote; did not attempt to provision or fix the OTC-OBS bucket (out of this plan's scope, belongs to plan 02.2-20).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `ffep cv dataset` raised `NameError` on every real invocation**
- **Found during:** Task 2, first real `ffep cv dataset --coco data/labels/dataset --manifest data/labels/dataset/manifest.json --min-images 1 --max-images 3000` run against the merged v2 directory (the plan's own required acceptance command)
- **Issue:** `cv/commands.py`'s `dataset` typer command called `load_config(config)` without assigning the return value, then referenced the (unbound) name `cfg` a few lines later to resolve `eval_split_path` for the D-19 guard -- a real, blocking `NameError`, not a test-only gap. It went undetected because no existing test exercised the real CLI entry point with the eval-guard parameter wired through.
- **Fix:** `cfg = load_config(config)`.
- **Files modified:** `src/flag_football_ep/cv/commands.py`
- **Verification:** real re-run against the 755-image merged directory succeeds (exit 0, prints stats); `uv run pytest tests/test_cv_dataset.py -x -q` (27 passed); `uv run pytest tests/ -k cv_ -q` full cv-scoped suite green, 100% collected, no FAILED/ERROR lines
- **Committed in:** `dc555db`

---

**Total deviations:** 1 auto-fixed (Rule 1, a real blocking bug found by running the plan's own required command against real data, not anticipated by any test fixture). No architectural changes.
**Impact on plan:** Necessary for Task 2's own acceptance criteria to be satisfiable at all; no scope creep.

## Issues Encountered

None beyond the deviation documented above. CVAT was reachable at `http://localhost:8080` for the full pull session; all three task exports round-tripped cleanly.

## User Setup Required

None -- no external service configuration required. CVAT credentials resolved from the existing gitignored `.env`; the DVC local fallback remote lives outside the repo (`~/.dvc-local-remote/`), already configured from prior plans.

## Next Phase Readiness

- Dataset v2 (755 images, DVC-tracked, `content_sha256 d87dd04cb7ed53cc3436e02596233937971df0edfbcf9cff628192a9d8963dce`) is ready to be consumed by plan 02.2-18's training run: `ffep cv train --dataset data/labels/dataset` points directly at it.
- **The 1,500-frame floor is not reached after both planned AL iterations** (755/1500, 50.3%) -- this is REQ-S2-03's headline number and plan 02.2-18 must report it honestly as part of the phase's final labelling verdict, not as a pending item still to be closed within this phase's scope (D-16 bounds labelling to two weekend iterations; a third iteration is explicitly out of REQ-S2-03's scope per plan 02.2-18's own objective).
- The real OTC-OBS `dvc push` remains untested against actual credentials/bucket -- unchanged gap from every prior plan, still owned by plan 02.2-20 (not yet run, no SUMMARY exists).
- `champion`/`hackathon-frozen` aliases were not touched by this plan (no training happened here) -- both remain at `87a8a5222f7a472787875e974d089c44`, unaffected.

## Verification

- `uv run --extra cv pytest tests/test_cv_dataset.py -x -q` -- 27 passed
- `uv run --extra cv pytest tests/test_cv_dataset.py tests/test_dvc_layout.py -x -q` (plan-level `<verification>` block) -- all passed
- `uv run pytest tests/ -k "cv_" -q` -- 100% collected, all green, zero FAILED/ERROR lines, only pre-existing benign warnings (trackers Kalman-filter RuntimeWarning, mlflow pyfunc type-hint UserWarning, `TestsetError` collection warning) matching every prior session's notes in this phase
- `ffep cv dataset --coco data/labels/dataset --manifest data/labels/dataset/manifest.json --min-images 1 --max-images 3000` -- exit 0, 755 images, `content_sha256 d87dd04cb7ed53cc3436e02596233937971df0edfbcf9cff628192a9d8963dce`, `player: 12139 boxes`, `referee: 1427 boxes`
- `boxes_by_domain` (direct Python call to `validate_coco`): drone `{player: 9046, referee: 1035}`, sideline `{player: 656, referee: 133}`, broadcast `{player: 2437, referee: 259}` -- every domain non-zero
- `dvc status` -- "Data and pipelines are up to date"
- `dvc status -r local-fallback -c data/labels/dataset.dvc` -- "Cache and remote 'local-fallback' are in sync"
- `git check-ignore -q data/labels/dataset` -- succeeds (data stays ignored)
- `git status --porcelain data/labels` -- only `data/labels/dataset.dvc`
- Puerto Rico exclusion: 0 hits across all 755 merged frames (direct path search)
- Frozen-eval exclusion: `assert_no_frozen_eval_clips` ran with no exception inside `validate_coco`

## Self-Check: PASSED

- `data/labels/dataset.dvc` -- FOUND, tracked by git (commit `dbe6a10`), `outs[0].md5 == 4b1652c97f6ca4e2f0032a6a7e2334a3.dir`
- `docs/dataset-buildout.md` -- FOUND, contains `## Iteration-2-Merge, Validierung und Dataset v2 (Plan 02.2-17)`
- `src/flag_football_ep/cv/commands.py` -- FOUND, contains `cfg = load_config(config)` in the `dataset` command
- Commits `dc555db`, `dbe6a10` -- both FOUND in `git log --oneline`
- `data/labels/dataset/` on disk -- FOUND, 757 files (755 images + `instances.json` + `manifest.json`)
- All plan `<acceptance_criteria>` re-checked this session for Tasks 2-3: pass (see Verification above)
- Plan-level `<verification>` block: `tests/test_cv_dataset.py tests/test_dvc_layout.py` green; two dataset pointer commits exist in git history (`7b528cd`/`8e52101`/etc. from earlier plans plus `dbe6a10` this plan)

---
*Phase: 02.2-dataset-buildout*
*Completed: 2026-09-10*
