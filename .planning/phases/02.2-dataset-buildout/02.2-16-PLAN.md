---
phase: 02.2-dataset-buildout
plan: 16
type: execute
wave: 8
depends_on: ["02.2-15", "02.2-21"]
files_modified:
  - docs/dataset-buildout.md
  - data/labels/al-iteration-2/selection.json
autonomous: true
requirements: [REQ-S2-03]

must_haves:
  truths:
    - "The iteration-2 selection is drawn with the iteration-1 detector, so it targets what the model still gets wrong"
    - "No frame selected in iteration 1 is selected again, and no frozen eval clip is touched"
    - "The per-domain frame targets follow the stopping-rule verdict from iteration 1 and the 1,500-frame floor"
  artifacts:
    - path: "data/labels/al-iteration-2/selection.json"
      provides: "The iteration-2 selection manifest with exclusions from iteration 1"
      contains: "iteration"
  key_links:
    - from: "data/labels/al-iteration-2/selection.json"
      to: "data/labels/al-iteration-1/selection.json"
      via: "previously-selected frame exclusion"
      pattern: "excluded"
---

<objective>
Run the second active-learning selection against the retrained detector: draw from the remaining
pool, exclude everything iteration 1 already used, size the per-domain targets by the iteration-1
stopping-rule verdict and the mandatory floor, prelabel with the newest detector, and push
weekend-sized CVAT tasks.

Purpose: the point of a second iteration is that the model changed. Selecting with the iteration-1
detector is what makes the second weekend buy something the first could not.
Output: an iteration-2 selection manifest, CVAT tasks, and the German record of the hand-off.

Constraint from plan 02.2-21: `2026-05-16_FRIENDLY-GER-vs-PUERTORICO-DRONE-WIDE` is the private
hackathon test game and is excluded from the training pool (DATA-04, game-disjoint dev/test).
`select_al_frames` refuses that session by way of `data/reference/al_excluded_sessions.csv`;
never pass it in `--session`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-dataset-buildout/02.2-CONTEXT.md
@.planning/phases/02.2-dataset-buildout/02.2-11-SUMMARY.md
@.planning/phases/02.2-dataset-buildout/02.2-15-SUMMARY.md
@docs/dataset-plan.md
@docs/dataset-buildout.md

<interfaces>
Same entry points as iteration 1 (plan 02.2-11), with iteration set to 2:
  ffep cv active-learn --iteration 2 --target <n> --seed <s> --session <id> --out-dir <dir>
  ffep cv prelabel --frames <dir> --out <dir> --backend finetuned     # resolves the CURRENT champion
  ffep cv cvat-push --coco <dir> --name al-2-<domain>-<index>

Selection excludes (cv/active_learning.py):
  clips with role `frozen_eval` in data/reference/frozen_eval_clips.csv
  every frame id present in data/labels/al-iteration-1/selection.json

Target sizing inputs:
  the iteration-1 per-domain stopping-rule verdict (docs/dataset-buildout.md)
  the mandatory 1,500-frame floor (REQ-S2-03) — binding even where the metric verdict was `nein`
  the 3,000-frame ceiling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Size and draw the iteration-2 selection</name>
  <files>data/labels/al-iteration-2/selection.json</files>
  <read_first>
    - docs/dataset-buildout.md (iteration-1 counts, the per-domain stopping-rule verdict, the current dataset size against the floor)
    - docs/dataset-plan.md (mix, ceiling, hard-case strategy)
    - data/labels/al-iteration-1/selection.json (the exclusion set)
    - src/flag_football_ep/cv/active_learning.py (the selector and its exclusion handling)
  </read_first>
  <action>
    Compute per-domain targets: for a domain whose iteration-1 verdict was `ja`, continue toward the
    planned mix; for a domain whose verdict was `nein`, label only as much as the 1,500-frame floor
    still requires and state that explicitly. Never exceed the 3,000-frame ceiling across the
    dataset.

    Run `ffep cv active-learn --iteration 2` per in-scope session with a recorded seed, passing the
    iteration-1 manifest so previously selected frames are excluded. Report the uncertainty
    distribution and compare it against iteration 1: if the median uncertainty dropped, say so —
    that is the expected signature of a detector that improved, and it is evidence for the stopping
    decision at the end of the phase.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_active_learning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The intersection of iteration-1 and iteration-2 selected frame ids is empty (command output quoted in the SUMMARY)
    - No selected clip appears in the `frozen_eval` rows of `data/reference/frozen_eval_clips.csv`
    - Per-domain targets are justified in the SUMMARY against the verdict and the floor, with arithmetic
    - The projected total dataset size after iteration 2 is stated and lies within [1500, 3000]
  </acceptance_criteria>
  <done>Iteration 2 targets the model's current weaknesses without re-labelling anything.</done>
</task>

<task type="auto">
  <name>Task 2: Prelabel with the current champion and push CVAT tasks</name>
  <files>data/labels/al-iteration-2/selection.json</files>
  <read_first>
    - src/flag_football_ep/cv/prelabel.py (`_load_finetuned_backend` and its documented run-id resolution: AL prelabels use the rolling champion, bundles use the freeze pin)
    - .planning/phases/02.2-dataset-buildout/02.2-15-SUMMARY.md (whether the champion alias moved after iteration 1 — if it did not, state which run is being used and why)
    - docs/cv-setup.md `## CVAT`
  </read_first>
  <action>
    Prelabel the iteration-2 frames with `--backend finetuned`. If the iteration-1 run was not
    promoted to champion (a regression was recorded), pass its run id explicitly and state the
    reasoning — prelabelling with the newest weights is the goal, promotion is a separate decision.
    Push CVAT tasks of at most 300 frames named `al-2-<domain>-<index>` and record the task ids.
    Report per-domain prelabel statistics and compare the boxes-per-frame and zero-detection rates
    against iteration 1.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_prelabel.py tests/test_cv_cvat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Task ids recorded, each task at most 300 frames
    - The run id used for prelabelling is stated explicitly in the SUMMARY together with why it was chosen
    - Zero-detection rate per domain reported next to the iteration-1 figure
    - No credential value appears in any output; `git status --porcelain data/labels` is empty
  </acceptance_criteria>
  <done>The second labelling block is prepared with the best available prelabels.</done>
</task>

<task type="auto">
  <name>Task 3: Record the iteration-2 hand-off</name>
  <files>docs/dataset-buildout.md</files>
  <read_first>
    - docs/dataset-buildout.md (the iteration-1 sections this mirrors)
    - docs/cv-setup.md `### Datensatz` (the labelling convention, including any clarification added after iteration 1)
  </read_first>
  <action>
    Append the iteration-2 section to docs/dataset-buildout.md: target derivation with its
    arithmetic, seed, per-domain frame and clip counts, uncertainty distribution compared with
    iteration 1, CVAT task ids and sizes, prelabel statistics, and a
    `## Labelling-Anleitung Iteration 2` subsection restating the binding convention plus anything
    that caused hesitation during the first session.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase22_docs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The iteration-2 section states the target arithmetic, the seed and the per-domain counts
    - The labelling instruction restates the convention rules and any iteration-1 clarification
    - Every iteration-2 CVAT task id and frame count is listed
  </acceptance_criteria>
  <done>The second weekend can start without a conversation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen eval clips → AL pool | Same leakage risk as iteration 1 |
| iteration-1 frames → iteration-2 selection | Re-labelling the same frames would waste the scarce labelling budget and inflate the "verified frames" count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-49 | Tampering | selection pool | mitigate | Post-selection intersection checks against both the frozen eval clips and the iteration-1 manifest, quoted in the SUMMARY |
| T-2.2-50 | Repudiation | frame-count claims | mitigate | Duplicate exclusion means the dataset's frame count is a count of distinct verified frames |
| T-2.2-51 | Information Disclosure | CVAT | mitigate | Loopback-bound instance, credentials via `secret()`, frames stay under the gitignored label tree |
</threat_model>

<verification>
- `uv run pytest tests -q` green
- Empty intersections with both the eval split and the iteration-1 selection
- CVAT tasks visible with prelabels

</verification>

<success_criteria>
- Iteration-2 selection drawn against the retrained detector, deduplicated and pool-safe
- Targets justified by the stopping-rule verdict and the floor
- Hand-off documented
</success_criteria>

<output>
Create `.planning/phases/02.2-dataset-buildout/02.2-16-SUMMARY.md` when done
</output>
