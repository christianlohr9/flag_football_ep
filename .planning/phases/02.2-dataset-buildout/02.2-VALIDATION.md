---
phase: 2.2
slug: dataset-buildout
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-31
---

# Phase 2.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing, uv-managed) |
| **Config file** | pyproject.toml |
| **Quick run command** | `uv run pytest tests -x -q -k "cv or dataset or bundle or active"` |
| **Full suite command** | `uv run pytest tests -q` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests -x -q -k "cv or dataset or bundle or active"`
- **After every plan wave:** Run `uv run pytest tests -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by planner) | — | — | REQ-S2-03 | — | No PII-bearing data committed to git | unit | `uv run pytest tests -q` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements (pytest + `flag_football_ep.testing` frame factories + schema-gate test style from `tests/test_capture_artifacts.py`). New test modules for new code (active-learning selection, freeze/bundle, DVC layout checks) are created alongside their implementation tasks.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Frame label verification (100% human check) | REQ-S2-03 | D-17 requires human sighting of every dataset frame in the annotation tool | User reviews/corrects prelabels in CVAT per AL iteration |
| Sideline/broadcast sighting verdict | REQ-S2-03 | Material quality judgment against capture-protocol tiers is human | Review sighting outputs; D-11 checkpoint if material unusable |
| Hackathon bundle sanity | REQ-S2-03 | Archive completeness/readability judged by opening the bundle | Unpack bundle, read German README, run eval script on dev set |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
