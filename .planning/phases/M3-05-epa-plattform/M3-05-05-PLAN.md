---
phase: M3-05-epa-plattform
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0001-modell-plattform.md
autonomous: false
requirements: [PROD-06]

must_haves:
  truths:
    - "The platform question (single machine vs. containerised OTC vs. Kubernetes, feature store yes/no) is answered once, in writing, signed by the user -- not defaulted by Claude"
    - "Each option carries a cost/effort/operational-burden/migration-path assessment, with cost figures explicitly flagged where they are order-of-magnitude, not a quote"
    - "The feature-store and multi-tenant-data-model questions are answered with a stated reason, not silently skipped"
    - "The document ends with a dated decision line the user actually signed, not a placeholder"
  artifacts:
    - path: "docs/adr/0001-modell-plattform.md"
      provides: "The signed platform decision record"
      contains: "Entscheidung"
  key_links:
    - from: "docs/adr/0001-modell-plattform.md"
      to: "the user's checkpoint answer"
      via: "the signed decision line names the option chosen and the date"
      pattern: "Entscheidung"
---

<objective>
CONTEXT is explicit: "Feature Store and Kubernetes are not dismissed -- the user wants the
trade-off worked through once, properly, because a wrong default now could hurt when
U17-Seniors adopt the tooling." This is a document, not code -- deciding the actual migration
(if any) is out of this phase's scope; this plan drafts the ADR and gets it signed.

Reconciliation with CONTEXT's locked "quick wins first, then hygiene" ordering: this plan's own scheduling is unchanged by that ordering (it was always wave 1, independent of everything else) -- it sits alongside the new coach-facing quick win (M3-05-02) rather than before or after it, since a platform decision and a model card serve different audiences and neither blocks the other.

RESEARCH already did the legwork this plan drafts from: three named options (single machine now
/ small OTC VM with docker-compose MLflow+Postgres+OBS / Kubernetes on OTC CCE), each with a
cost/burden/migration-path/breaks-first-at assessment (RESEARCH Q6's table), a feature-store
non-decision grounded in a 5-signal framework where this project is currently at 0-of-5 (Q7), a
multi-tenant data-model recommendation (shared model with a tier covariate over per-programme
models, evidence-backed by this phase's own re-run: `womens-international` log-loss 0.881 vs.
`mixed-other` 0.946 -- pooling captures a real tier effect a per-programme model would lose,
Q8), and MLflow deployment realities including its 2026 CVE history for unauthenticated servers
(Q9). RESEARCH's own recommendation (Q10): stay on Option (i) through this phase and BL-02's
first cut, with an explicit, cheap migration trigger (a second write-access user, or BL-02
needing to run independent of the developer's laptop) that promotes to Option (ii).

Every cost figure RESEARCH produced for OTC (ECS/CCE/OBS) is order-of-magnitude, not a
calculator quote -- OTC publishes a price calculator, not fixed list prices, for these SKUs
(RESEARCH Assumption A2). This plan's draft must carry that caveat forward, not launder it into
a false precision.

Purpose: answer the platform question once, with the user, in writing, before BL-02 is built on
top of whatever answer this gives.
Output: `docs/adr/0001-modell-plattform.md`, drafted then signed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-05-epa-plattform/M3-05-CONTEXT.md
@.planning/phases/M3-05-epa-plattform/M3-05-RESEARCH.md
@docs/hc-notes-2026-09-03.md
@docs/hackathon-otc-upload.md
</context>

<file_collision_guard>
Owned by this plan: `docs/adr/0001-modell-plattform.md` only. This plan does not touch
`ffep.toml`, `src/**`, or any other doc -- the ADR is a decision record, not an implementation;
executing a migration (if the decision calls for one) is explicitly out of scope here.
</file_collision_guard>

<tasks>

<task type="auto">
  <name>Task 1: Draft the ADR from RESEARCH's Part B findings</name>
  <files>docs/adr/0001-modell-plattform.md</files>
  <read_first>
    - `M3-05-RESEARCH.md` sections Q6-Q10 (Platform Options, Feature Store, Multi-tenant Data
      Model, MLflow Deployment Realities, Recommendation) -- the entire evidentiary basis for
      this draft. Do not re-derive costs or trade-offs from scratch; cite this research.
  </read_first>
  <action>
    Write `docs/adr/0001-modell-plattform.md` in German, standard ADR shape (Kontext, Optionen,
    Entscheidungskriterien, Empfehlung, offene Entscheidung): (1) Kontext -- one paragraph
    stating the multi-team future this decides for (U17-Seniors, men's + women's programmes,
    other coaches' Excel -> web app / BL-02), current scale (~30k plays, batch scoring under
    10 min, no real-time requirement, solo developer), and that this phase's own hygiene work
    (M3-05-02/03/06) is Option-(i)-native and unaffected regardless of which option is chosen.
    (2) The three options from RESEARCH Q6's table verbatim (single machine / OTC VM
    docker-compose / Kubernetes), each with cost (flagged `[ASSUMED, order-of-magnitude]` where
    RESEARCH flagged it), operational burden, migration path from today, and what breaks first
    at 10x scale. (3) The feature-store question (Q7): state the 5-signal framework, that this
    project is at 0-of-5 today, and the recommendation (no feature store now; reconsider
    alongside Kubernetes if programme count exceeds ~3 with materially divergent feature needs
    or a real-time requirement emerges). (4) The multi-tenant data model question (Q8): the
    `source`/`competition_tier`/team-mapping scoping already in place, the shared-model-with-
    covariate recommendation and the specific evidence for it (`womens-international` 0.881 vs.
    `mixed-other` 0.946 EP log-loss). (5) MLflow deployment realities relevant to a future
    Option-(ii)/(iii) execution (Q9): no auth by default, the 2026 CVE history for unauthenticated
    servers, the default-credential rotation requirement if basic auth is ever enabled. (6) A
    "## Empfehlung" section stating RESEARCH's staged recommendation verbatim (stay on Option i
    through this phase and BL-02's first cut; migrate to Option ii only when a concrete trigger
    fires -- a second write-access user, or BL-02 needing independent uptime; defer Kubernetes
    and a feature store together, not independently). (7) An empty
    "## Entscheidung (vom Nutzer zu unterschreiben)" section with placeholder text -- this is
    filled in task 2, not here.
  </action>
  <verify>
    <automated>test -f docs/adr/0001-modell-plattform.md && grep -q "Empfehlung" docs/adr/0001-modell-plattform.md && grep -q "Entscheidung" docs/adr/0001-modell-plattform.md && echo OK</automated>
  </verify>
  <done>
    The ADR draft exists in German, covers all three named options plus the feature-store and
    multi-tenant questions with RESEARCH's cited evidence, and ends in an unfilled decision
    section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sign the platform decision</name>
  <files>docs/adr/0001-modell-plattform.md</files>
  <action>
    Pause here. Do not fill the "## Entscheidung" section before the resume signal -- task 3
    applies exactly what comes back from this checkpoint, nothing more.
  </action>
  <what-built>
    A drafted ADR (task 1) covering: (a) three platform options with cost/burden/migration-path,
    cost figures explicitly flagged as order-of-magnitude where RESEARCH could not get a fixed
    OTC price; (b) a feature-store non-recommendation grounded in a 0-of-5 trigger-signal
    framework; (c) a multi-tenant data-model recommendation (shared model, tier covariate) with
    the specific measured evidence for it; (d) MLflow's unauthenticated-server CVE history,
    relevant only if a server-based option is later executed; (e) a staged recommendation
    (stay single-machine now, migrate on a concrete trigger, defer Kubernetes/feature store
    together).
  </what-built>
  <how-to-verify>
    1. Read `docs/adr/0001-modell-plattform.md` in full.
    2. Decide: (a) which option to run on now -- almost certainly Option (i), per the
       recommendation, but this is your call; (b) whether the feature-store/Kubernetes deferral
       is acceptable as stated; (c) whether the multi-tenant shared-model-with-covariate
       recommendation is the direction to build BL-02 on later; (d) if you want the migration
       trigger's wording changed (what exactly should force a move to Option ii).
    3. If any cost figure needs a real number before you're comfortable signing, run the OTC
       price calculator yourself first (RESEARCH flags every OTC cost as order-of-magnitude,
       not a quote) -- this plan does not do that for you.
  </how-to-verify>
  <resume-signal>
    Reply with the option you choose (i / ii / iii), any wording changes to the recommendation
    or migration trigger, and confirm you're signing -- this becomes the "## Entscheidung"
    section's content, dated to today.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the signed decision back</name>
  <files>docs/adr/0001-modell-plattform.md</files>
  <read_first>The checkpoint response from task 2 -- the only source of what goes here.</read_first>
  <action>
    Fill the "## Entscheidung (vom Nutzer zu unterschreiben)" section with: the date, the chosen
    option (i/ii/iii) named explicitly, any wording changes to the recommendation or migration
    trigger the user requested (apply them to the relevant section above too, not just quoted in
    the decision line), and an explicit signed statement (e.g. "Entscheidung getroffen und
    freigegeben von <Nutzer> am <Datum>: Option (i), Migrations-Trigger wie oben beschrieben.").
    Apply nothing beyond what the checkpoint response said.
  </action>
  <verify>
    <automated>(grep -q "^Entscheidung getroffen" docs/adr/0001-modell-plattform.md || grep -qi "unterschrieben\|signiert\|freigegeben" docs/adr/0001-modell-plattform.md) && echo OK</automated>
  </verify>
  <done>
    The ADR carries a dated, explicit, signed decision line naming the chosen option -- not a
    placeholder.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| architecture decision -> multi-year commitment | this document determines what BL-02 (the coach-facing web app) gets built on; a wrong default is expensive to unwind |
| cost estimate -> user's mental budget | an order-of-magnitude figure presented as a quote could mislead a real commitment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-05-11 | Repudiation | platform decision | mitigate | The decision is made only via the explicit checkpoint, never defaulted, and the signed line names the date, the option and the user |
| T-M3-05-12 | Information Disclosure (false precision) | OTC cost figures | mitigate | Every order-of-magnitude figure carries its `[ASSUMED]` flag through to the final document; the checkpoint explicitly tells the user to run the real calculator before signing if precision matters to their decision |
| T-M3-05-13 | Spoofing/Information Disclosure (future) | unauthenticated MLflow server, if Option ii/iii is later executed | accept | Out of this plan's code scope (a document, not an implementation) -- the ADR names the 2026 CVE history and the default-credential rotation requirement so a future execution plan inherits the warning, not a false sense of safety |
| T-M3-05-SC | Tampering | package installs | accept | This plan installs nothing (`boto3` stays gated behind an ADR-triggered future execution, never installed here) |
</threat_model>

<verification>
- `docs/adr/0001-modell-plattform.md` exists, covers all three options plus feature-store and
  multi-tenant sections, and ends with a dated, signed decision line.
- The signed option matches what the checkpoint response actually said.
</verification>

<success_criteria>
1. The platform question is answered once, in writing, with the user's explicit sign-off.
2. Every cost figure that RESEARCH could not pin to a real quote stays flagged as such.
3. The feature-store and multi-tenant questions have stated, evidence-backed answers, not
   silence.
</success_criteria>

<output>
Create `.planning/phases/M3-05-epa-plattform/M3-05-05-SUMMARY.md` when done. Record the
checkpoint answer and the exact signed decision line.
</output>
