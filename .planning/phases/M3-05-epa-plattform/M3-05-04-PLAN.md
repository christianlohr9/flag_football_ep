---
phase: M3-05-epa-plattform
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [PROD-05]

must_haves:
  truths:
    - "A pull request or push to this repo now runs the fixture-based test suite automatically, without a developer remembering to run it locally"
    - "The workflow needs zero secrets -- it never touches SPORTAPP_API_KEY, CPX_API_KEY or any OTC credential"
    - "The CV/hackathon test suites (multi-GB torch/rfdetr dependencies, out of this phase's scope) are excluded, not silently slowed into the same job"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI running the fixture-based pytest suite on push/PR"
      contains: "pytest"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/conftest.py"
      via: "uv run pytest -q -k \"not cv and not hackathon\""
      pattern: "pytest"
---

<objective>
No `.github/workflows/` directory exists in this repo today -- verified this session
(`ls .github/workflows` -> not found). `tests/conftest.py` already provides a `tmp_path`-scoped
`Config` fixture (every path redirected away from the real `mlruns/`) and a synthetic canonical
corpus, deliberately small "so the suite stays fast" per that file's own docstring --
`tests/test_model_train.py` already exercises `train_ep`/`train_wp` against it, and every
fixture-based test in this project already uses a `tmp_path`-scoped MLflow store, never the
real one. This is a runner-config problem, not a new-fixture problem.

Purpose: catch a training/registry/gate regression before it reaches the real corpus.
Output: `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-05-epa-plattform/M3-05-CONTEXT.md
@.planning/phases/M3-05-epa-plattform/M3-05-RESEARCH.md
</context>

<interfaces>
`pyproject.toml`'s `[dependency-groups]` has `dev = ["pytest>=8"]` -- `uv sync --dev` installs
exactly this, the CORE dependency group only. Do NOT `uv sync --all-extras` or otherwise pull in
the `cv`/`versioning` extras (`[project.optional-dependencies]` `cv`/`versioning` -- multi-GB
`torch`/`rfdetr`/`dvc` stacks this phase's tests do not need).

`pyproject.toml`'s `[tool.pytest.ini_options]` already sets `testpaths = ["tests"]`,
`addopts = "-q"` -- `uv run pytest` alone already targets the right directory.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `.github/workflows/ci.yml` -- fixture suite on push/PR, no secrets</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create `.github/workflows/ci.yml`: triggers on `push` and `pull_request`; one job on
    `ubuntu-latest`; steps: checkout, install `uv` (`astral-sh/setup-uv@v3` or the documented
    curl installer, pin a specific action version, not `@latest`), `uv sync --dev` (core group
    only, per `<interfaces>` above -- no `--all-extras`), then
    `uv run pytest -q -k "not cv and not hackathon"` to skip the heavy CV/hackathon suites this
    phase is out of scope for. Set no `env:` secrets anywhere in the workflow -- every
    fixture-based test this job runs already uses a `tmp_path`-scoped MLflow store and never
    calls `SPORTAPP_API_KEY`/`CPX_API_KEY`/any `OTC_OBS_*` variable (verified this session:
    every `-k "not cv and not hackathon"` test path in `tests/` reads only `tests/conftest.py`
    fixtures or committed `data/reference/*` files). Name the job something identifiable, e.g.
    `test`.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "pytest" .github/workflows/ci.yml && grep -qv "SPORTAPP_API_KEY\|CPX_API_KEY\|OTC_OBS" .github/workflows/ci.yml && python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK</automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` exists, is valid YAML, runs `uv sync --dev` + the filtered
    fixture-based pytest suite on push/PR, and references no secret environment variable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pull request -> CI runner | an untrusted PR's code runs inside the CI job; a workflow that echoes secrets or fetches unpinned actions widens this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-05-09 | Information Disclosure | CI workflow secrets | mitigate | Zero secrets declared in the workflow -- every test this job runs is fixture-based, never touches a real API key or OTC credential (verified: the excluded `-k "not cv and not hackathon"` filter is exactly the set of suites that would need one) |
| T-M3-05-10 | Tampering (supply chain) | third-party GitHub Actions | mitigate | Actions pinned to a specific released version, not `@latest`/an unpinned branch |
| T-M3-05-SC | Tampering | package installs | accept | `uv sync --dev` installs only the already-declared `pytest>=8` dev group -- no new package this plan introduces |
</threat_model>

<verification>
- `.github/workflows/ci.yml` is valid YAML and references `pytest`.
- No secret/credential environment variable name appears anywhere in the file.
- `uv sync --dev && uv run pytest -q -k "not cv and not hackathon"` succeeds locally (same
  command the workflow runs).
</verification>

<success_criteria>
1. A push or PR to this repo now runs the fixture-based suite automatically.
2. The job needs no secrets and excludes the CV/hackathon suites.
</success_criteria>

<output>
Create `.planning/phases/M3-05-epa-plattform/M3-05-04-SUMMARY.md` when done.
</output>
