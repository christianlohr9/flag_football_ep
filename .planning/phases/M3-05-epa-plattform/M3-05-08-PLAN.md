---
phase: M3-05-epa-plattform
plan: 08
type: execute
wave: 4
depends_on: [M3-05-01, M3-05-03, M3-05-07]
files_modified:
  - scripts/render_model_card.py
  - docs/epa-modellkarte.md
  - docs/mlflow-ui-howto.md
  - tests/test_m3_epa_docs.py
autonomous: true
requirements: [PROD-07, PROD-08]

must_haves:
  truths:
    - "A coach reading docs/epa-modellkarte.md can answer: which model is in production, why, how good it is, and since when"
    - "Every number on the card comes from the MLflow store or a committed reference CSV, resolved at generation time -- never hand-typed"
    - "Regenerating the card after a later promotion produces a different card without any code change"
    - "A coach can follow docs/mlflow-ui-howto.md and see the registered models/aliases without being handed a raw metrics dump"
  artifacts:
    - path: "scripts/render_model_card.py"
      provides: "A script that reads the MLflow registry + data/reference/epa_refinement CSVs and writes docs/epa-modellkarte.md"
      exports: ["main"]
    - path: "docs/epa-modellkarte.md"
      provides: "The generated, coach-readable German model card"
      contains: "Champion-Alias"
    - path: "docs/mlflow-ui-howto.md"
      provides: "German how-to for the coach session -- what to show, what not to show"
      contains: "mlflow ui"
  key_links:
    - from: "scripts/render_model_card.py"
      to: "MLflow registry (resolve_champion, get_run)"
      via: "flag_football_ep.model.registry.resolve_champion"
      pattern: "resolve_champion"
    - from: "docs/epa-modellkarte.md"
      to: "tests/test_m3_epa_docs.py"
      via: "a doc-guard test asserting every quoted number/run id traces back to the live registry or a committed CSV"
      pattern: "epa-modellkarte"
---

<objective>
`docs/epa-modellkarte.md` exists today as a hand-written, one-off artifact from the 2026-09-08
re-run (commit `ee46207`) -- CONTEXT's own framing is that it becomes a generated artifact this
phase, following the exact "generated from source, never hand-typed" discipline
`docs/epa-refinement-2026-10.md` already established (doc-guard tests, `data/reference/*` CSVs
as the source of truth). By the time this plan runs, the champion has been resolved twice over
in this phase (M3-05-01's initial decision, M3-05-07's extra-point-fix decision) -- generating
the card from the LIVE registry, not a snapshot, is what makes it correct regardless of which
decision landed last.

The coach's actual question, per CONTEXT: "welches Modell ist produktiv, warum, wie gut, seit
wann" (`docs/hc-notes-2026-09-03.md`). This plan answers it from data every later regeneration
can reproduce: `registry.resolve_champion` for "which", the resolved run's own MLflow
params/metrics (now including M3-05-03's lineage/calibration/tier/no-play metrics, and
M3-05-06's freeze citation if present) for "how good"/"since when", and the promotion decision
recorded in `docs/epa-refinement-2026-10.md` for "why".

RESEARCH Q4 is explicit that raw per-run metric tables and hyperparameter dumps are NOT
coach-appropriate -- the live `mlflow ui` (already documented, `docs/model-training.md` section
2, unchanged by this plan) is a demo tool the developer walks the coach through once, not
something to hand over unsupervised. This plan's second artifact is that walkthrough script, in
German.

Purpose: the model card becomes a durable, regenerable, coach-readable artifact; the MLflow UI
gets a German operator script for the coach session.
Output: `scripts/render_model_card.py`, a regenerated `docs/epa-modellkarte.md`,
`docs/mlflow-ui-howto.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-05-epa-plattform/M3-05-CONTEXT.md
@docs/epa-modellkarte.md
@docs/epa-refinement-2026-10.md
@docs/model-training.md
</context>

<execution_note>
This script reads the real `mlruns/` MLflow store to resolve the current champion and its
metrics -- run on the main tree, not this worktree, if `mlruns/` is not shared between the two.
</execution_note>

<interfaces>
From `src/flag_football_ep/model/registry.py` (unchanged by this plan, read in full earlier
this phase):
```python
def resolve_champion(name: str, config: Config) -> str:
    """Returns the run id currently aliased `champion` under `name`. Raises RegistryError
    (naming `name`) if no alias is set."""
```
`registered_model_name("ep")` -> `"ep_model"`, `registered_model_name("wp")` -> `"wp_model"`.

Resolve a champion run's full param/metric set via
`MlflowClient().get_run(run_id).data.{params,metrics}` -- after M3-05-03/06/07 land, this
includes `git_commit`, `corpus_fingerprint`, `calibration_max_deviation_<class>`,
`per_tier_logloss_<tier>`, `no_play_share`, and (if the run cited a freeze)
`freeze_manifest_path`/`freeze_date` -- pull what is present, degrade gracefully (omit a
section, do not crash) for an older run missing some of these, since the champion after
M3-05-01 alone (before M3-05-07 lands) may predate some of them.

`tests/test_m3_epa_docs.py` already defines `MODELLKARTE = REPO_ROOT / "docs" /
"epa-modellkarte.md"` and includes it in the existing roster/PII scan (`for path in (DOC,
RUECKFRAGEN, MODELLKARTE): ...` near the file's PII-gate section) -- this plan extends that same
file with a run-id/figure-match guard for the regenerated card, it does not need a new test
file.

The promotion decision and its reason live in `docs/epa-refinement-2026-10.md`'s
`## Champion-Entscheidung` section (M3-05-01) and, if it fired, the
`## Methodenaenderung: Extrapunkt-Ausschluss` section (M3-05-07) -- the "why" section of the
generated card should read from there, not re-derive a reason.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `scripts/render_model_card.py` -- generate the card, never hand-type</name>
  <files>scripts/render_model_card.py, docs/epa-modellkarte.md, tests/test_m3_epa_docs.py</files>
  <behavior>
    - `main()` loads config, resolves the champion run id for both `ep_model`/`wp_model` via
      `registry.resolve_champion`, reads each run's params/metrics, and writes
      `docs/epa-modellkarte.md` deterministically from that data plus
      `data/reference/epa_refinement/*/ablation_summary.csv` (whichever dated subfolder the
      champion's `run_id` actually appears in -- resolved by searching, not hard-coded to one
      date).
    - Every number in the written file is either a value read directly from
      `run.data.metrics`/`run.data.params`, or a cell from a committed CSV row matched by
      `run_id` -- no numeric literal is typed into the script's template strings.
    - No player name, position, or any PII appears anywhere in the generated file (existing
      roster-based PII gate in `tests/test_m3_epa_docs.py` continues to pass against it
      unmodified).
    - If `registry.resolve_champion` raises (no champion set for a model), the script writes a
      card section stating that explicitly (`"kein Champion gesetzt"`) rather than crashing --
      the card must always be regenerable, even mid-phase before a promotion has happened.
  </behavior>
  <action>
    Write `scripts/render_model_card.py` following the `scripts/hc_corpus_ablation.py`/
    `scripts/epa_comparison.py` driver convention already established in this project (imports
    from `src/`, never the reverse; `argparse` for `--config`/`--out`). Card sections, mirroring
    the existing hand-written `docs/epa-modellkarte.md`'s structure (read it first for the
    German prose tone and section order to preserve): Wozu das Modell da ist (static prose,
    unchanged), Eingaben (static prose, unchanged), Trainingskorpus (per-source row/game counts
    -- pull from the champion run's own logged `n_plays` plus the freeze manifest's per-source
    counts if the run cited one, else state "kein Freeze referenziert"), Methode (static prose
    plus a sentence citing whether the corpus this run trained on excludes extra-point rows --
    read from the run's params/commit history if determinable, else state it plainly as unknown
    for that run), Performance (the champion run's own `logo_*`/`naive_*`/`logloss_improvement`
    metrics, per-tier breakdown from `per_tier_logloss_*` if present), Kalibrierung (the
    `calibration_max_deviation_*` metrics if present, else "nicht gemessen fuer diesen Lauf"),
    Bekannte Grenzen (static prose from the existing card, updated only if a fact it states --
    e.g. the extra-point leak -- has actually changed per M3-05-07's outcome), Versionierung
    (champion run ids, corpus fingerprint, promotion date and reason read from
    `docs/epa-refinement-2026-10.md`'s `Champion-Entscheidung`/`Methodenaenderung` sections),
    Naechste Schritte (static prose, updated to drop any item this phase resolved). Run the
    script for real (main tree, per `<execution_note>`) and commit the regenerated
    `docs/epa-modellkarte.md`. Extend `tests/test_m3_epa_docs.py` with a guard scoped to
    `MODELLKARTE`: every run id it quotes resolves to a real run in
    `data/reference/epa_refinement/*/ablation_summary.csv` (any dated subfolder), and every
    log-loss-shaped figure matches that run's row within the same tolerance convention
    `_assert_figure_matches` already implements -- reuse that helper, do not write a second one.
  </action>
  <verify>
    <automated>uv run pytest tests/test_m3_epa_docs.py -x -q</automated>
  </verify>
  <done>
    `scripts/render_model_card.py` exists and was run for real; `docs/epa-modellkarte.md` is
    regenerated, sourced entirely from the MLflow store and committed CSVs; the doc-guard test
    covers it; no player name/PII appears in it.
  </done>
</task>

<task type="auto">
  <name>Task 2: German MLflow UI how-to for the coach session</name>
  <files>docs/mlflow-ui-howto.md</files>
  <read_first>
    - `docs/model-training.md` section 2 -- the exact, already-proven
      `mlflow ui --backend-store-uri sqlite:///$(pwd)/mlruns/mlflow.db` command and what it
      exposes.
  </read_first>
  <action>
    Write `docs/mlflow-ui-howto.md` in German: the exact command to run locally (copy verbatim
    from `docs/model-training.md`, do not restate it differently), what to open
    (`http://127.0.0.1:5000`), what IS coach-appropriate to show live (the registered-model list,
    the `champion` alias and version, when it was promoted) per RESEARCH Q4, and what NOT to show
    (raw per-run metric tables, hyperparameter dumps, the tuning trial-loss curves) -- direct the
    coach to `docs/epa-modellkarte.md` for those numbers instead. Frame it explicitly as "a
    walkthrough the developer runs live during the October sync", not a link to hand the coach
    unsupervised, matching RESEARCH's own recommendation.
  </action>
  <verify>
    <automated>test -f docs/mlflow-ui-howto.md && grep -q "mlflow ui" docs/mlflow-ui-howto.md && echo OK</automated>
  </verify>
  <done>
    `docs/mlflow-ui-howto.md` exists, cites the proven command verbatim, and states clearly what
    to show live versus what to redirect to the model card instead.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated coach-facing doc -> a colleague/external reader | a wrong or stale number here is read by someone who did not build the model |
| MLflow UI walkthrough -> what a coach sees live | showing the wrong panel could leak more than intended (raw hyperparameters, unrelated experiment runs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-05-21 | Tampering (stale numbers) | `docs/epa-modellkarte.md` | mitigate | Generated from the live registry and committed CSVs at run time, never hand-typed; a doc-guard test pins every quoted run id/figure back to its source, same discipline as `docs/epa-refinement-2026-10.md` |
| T-M3-05-22 | Information Disclosure (PII) | generated model card | mitigate | The existing roster-based PII gate in `tests/test_m3_epa_docs.py` already scans `MODELLKARTE`; this plan's generation script never reads or interpolates any player-identifying field |
| T-M3-05-23 | Information Disclosure (over-sharing in a live demo) | MLflow UI walkthrough | mitigate | `docs/mlflow-ui-howto.md` explicitly scopes what to show (registered models/aliases) versus what stays in the model card instead (raw metric tables, hyperparameters) |
| T-M3-05-SC | Tampering | package installs | accept | This plan installs nothing new |
</threat_model>

<verification>
- `uv run pytest tests/test_m3_epa_docs.py -q` green, including the new model-card guard.
- `docs/epa-modellkarte.md` was regenerated by `scripts/render_model_card.py`, not hand-edited.
- `docs/mlflow-ui-howto.md` exists and cites the exact proven `mlflow ui` command.
- No player name or PII in either new document.
</verification>

<success_criteria>
1. The model card answers "which model, why, how good, since when" from data traceable to the
   live MLflow store and committed CSVs.
2. Regenerating the card after a future promotion changes its numbers with no code edit.
3. The coach gets a German how-to that scopes exactly what to show live versus what stays in
   the card.
</success_criteria>

<output>
Create `.planning/phases/M3-05-epa-plattform/M3-05-08-SUMMARY.md` when done.
</output>
