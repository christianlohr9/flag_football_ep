---
phase: M3-05-epa-plattform
plan: 08
type: execute
wave: 4
depends_on: [M3-05-02, M3-05-03, M3-05-07]
files_modified:
  - docs/epa-modellkarte.md
  - tests/test_m3_epa_docs.py
  - scripts/render_model_card.py
autonomous: true
requirements: [PROD-09]

must_haves:
  truths:
    - "docs/epa-modellkarte.md now reflects the post-leak-fix champion, not the pre-fix one M3-05-02 first generated"
    - "The regeneration needs no new code -- M3-05-02's script was already built to degrade gracefully and now finds every field populated"
    - "Every number on the regenerated card still comes from the MLflow store or a committed reference CSV, never hand-typed"
  artifacts:
    - path: "docs/epa-modellkarte.md"
      provides: "The regenerated, coach-readable German model card reflecting the final champion for this phase"
      contains: "Champion-Alias"
  key_links:
    - from: "scripts/render_model_card.py"
      to: "docs/epa-modellkarte.md"
      via: "a re-run against the post-M3-05-07 MLflow store"
      pattern: "resolve_champion"
    - from: "docs/epa-modellkarte.md"
      to: "tests/test_m3_epa_docs.py"
      via: "the doc-guard extended in M3-05-02 continues to pass against the regenerated card"
      pattern: "epa-modellkarte"
---

<objective>
This is the phase's LAST step for the model card, and it is deliberately thin: M3-05-02 (wave
1) already built `scripts/render_model_card.py` to read whatever the live MLflow registry
currently has and degrade gracefully for anything absent. By the time this plan runs, the
champion has potentially moved twice (M3-05-01's initial decision, M3-05-07's extra-point-fix
decision) and the champion run now carries every metric M3-05-03/06 added (lineage, calibration,
per-tier, no-play share, freeze citation) that did not exist when M3-05-02 first generated the
card. Re-running the SAME script picks all of this up automatically -- by design, not by a code
change.

This plan's job is therefore: re-run the script for real, verify the regenerated card actually
reflects the post-fix champion and the newly-populated metrics (not just re-emit the same
content), and extend the doc-guard for anything M3-05-07 added to
`docs/epa-refinement-2026-10.md` (the `Methodenaenderung: Extrapunkt-Ausschluss` section) that
the card's "why" section now cites. `scripts/render_model_card.py` is listed in this plan's
`files_modified` only in case the real run surfaces a genuine bug against data shapes that did
not exist at M3-05-02's execution time (e.g. a field name mismatch) -- fixing that here is
in-scope; redesigning the script's structure is not.

Purpose: close the loop CONTEXT's ordering opened -- the quick-win card from wave 1 becomes the
accurate, final card once the hygiene and methodology work has landed.
Output: a regenerated `docs/epa-modellkarte.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-05-epa-plattform/M3-05-CONTEXT.md
@docs/epa-modellkarte.md
@docs/epa-refinement-2026-10.md
@.planning/phases/M3-05-epa-plattform/M3-05-02-PLAN.md
@.planning/phases/M3-05-epa-plattform/M3-05-07-PLAN.md
</context>

<execution_note>
This script reads the real `mlruns/` MLflow store to resolve the current champion and its
metrics -- run on the main tree, not this worktree, if `mlruns/` is not shared between the two.
</execution_note>

<file_collision_guard>
Owned by this plan: `docs/epa-modellkarte.md`, `tests/test_m3_epa_docs.py`,
`scripts/render_model_card.py` (touch only to fix a genuine bug surfaced by the real run, not to
redesign it). Nothing else.
</file_collision_guard>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the card against the post-fix champion</name>
  <files>docs/epa-modellkarte.md, tests/test_m3_epa_docs.py, scripts/render_model_card.py</files>
  <read_first>
    - `M3-05-07-SUMMARY.md` (once it exists) or `docs/epa-refinement-2026-10.md`'s
      `Champion-Entscheidung`/`Methodenaenderung` sections -- confirms which run is now champion
      and why, so you can verify the regenerated card actually changed to match, not just that
      the script ran without error.
  </read_first>
  <action>
    Run `uv run python scripts/render_model_card.py` (main tree, per `<execution_note>`) for
    real. Diff the regenerated `docs/epa-modellkarte.md` against its M3-05-02 version: confirm
    the champion run ids, and at least the calibration/per-tier/no-play/freeze fields that were
    previously "nicht verfuegbar"/"nicht gemessen fuer diesen Lauf", now show real values (if
    M3-05-07 promoted a new candidate) or that the card still correctly and honestly describes
    the M3-05-01 champion if M3-05-07's decision was `none`. If the real run reveals a genuine
    bug in the script (e.g. a field name that does not match what M3-05-03/06/07 actually log),
    fix it minimally here -- do not restructure the card's sections. Extend
    `tests/test_m3_epa_docs.py`'s existing `MODELLKARTE` guard (from M3-05-02) to also cover any
    new run id the card now cites from M3-05-07's dated CSV subfolder, using the same
    `_assert_figure_matches` helper -- never loosen the M3-05-02 guard, only extend its coverage.
  </action>
  <verify>
    <automated>uv run pytest tests/test_m3_epa_docs.py -x -q</automated>
  </verify>
  <done>
    `docs/epa-modellkarte.md` is regenerated and demonstrably reflects the post-M3-05-07
    champion state (not a byte-identical re-emit of the M3-05-02 version, unless M3-05-07's
    decision was genuinely `none` and nothing changed); the doc-guard test still passes and
    covers the new run id if one was promoted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated coach-facing doc -> a colleague/external reader | a stale card after the leak fix would misrepresent the actual production model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-05-24 | Tampering (stale numbers) | `docs/epa-modellkarte.md` after M3-05-07 | mitigate | Regenerated from the live registry at this plan's own execution time, not carried forward from M3-05-02's snapshot; the doc-guard test is extended, never loosened |
| T-M3-05-SC | Tampering | package installs | accept | This plan installs nothing new |
</threat_model>

<verification>
- `uv run pytest tests/test_m3_epa_docs.py -q` green.
- `docs/epa-modellkarte.md` was regenerated by `scripts/render_model_card.py`, not hand-edited.
- The regenerated card's champion run ids match `docs/epa-refinement-2026-10.md`'s recorded
  post-M3-05-07 decision.
</verification>

<success_criteria>
1. The final model card reflects the phase's actual final champion, not an intermediate one.
2. No code change was needed beyond fixing a genuine bug, if any surfaced.
3. The doc-guard test coverage only grew, never shrank.
</success_criteria>

<output>
Create `.planning/phases/M3-05-epa-plattform/M3-05-08-SUMMARY.md` when done.
</output>
