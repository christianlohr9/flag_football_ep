---
phase: 02.0-capture-protocol-material-inventory
plan: 02
type: execute
wave: 2
depends_on: ["02.0-01"]
files_modified:
  - docs/material-inventory.md
  - docs/sync-convention.md
  - tests/test_capture_reference_docs.py
autonomous: true
requirements: [REQ-S2-01]

must_haves:
  truths:
    - "Der Nutzer kann einen neu erhaltenen Clip in wenigen Minuten registrieren: Domäne bestimmen, `ffprobe` und `shasum` laufen lassen, eine Zeile in `video_inventory.csv` schreiben (D-07)"
    - "Es ist dokumentiert, welches Material existiert, wer es hält und was davon gesichert ist — der unsichere Teil trägt einen DEFERRED-ANALYST-Block statt einer erfundenen CSV-Zeile (D-01)"
    - "Ein Video-Play lässt sich einem bestehenden Hudl-PBP-Play zuordnen, ohne dass Staff irgendetwas am Spielfeld tut (D-09, D-11 Variante B)"
    - "Die Konvention definiert nie einen Play neu — sie hängt Videopositionen an das, was in Hudl schon existiert (`PLAY #` → `play_id`, Plausibilitätscheck über `DN`/`DIST`/`RESULT`) (D-10)"
    - "Variante A (Upload nach Hudl) steht als begründeter Wunsch mit DEFERRED-ANALYST-Block da, nicht als Voraussetzung (D-11)"
    - "Beide Dokumente nennen die Join-Dtypes `game_id: Utf8` / `play_id: Int32` explizit, damit ein stiller 0-Zeilen-Join nicht als Datenproblem missverstanden wird"
    - "Beide Dokumente sind deutschsprachig mit englischen Identifiern und folgen dem Stil von `docs/data-contract.md` (D-06)"
  artifacts:
    - path: "docs/material-inventory.md"
      provides: "Materialinventur: Domänen, Bestand, Spaltenmodell, Registrier-Anleitung"
      contains: "## Domänen & Bestand"
      min_lines: 70
    - path: "docs/sync-convention.md"
      provides: "Video-Play ↔ Hudl-PBP-Play Join-Vertrag, Variante A und B"
      contains: "## Variante B — manuelle Snap-Anker (immer verfügbar)"
      min_lines: 70
    - path: "tests/test_capture_reference_docs.py"
      provides: "Section- und Drift-Gates für beide Dokumente gegen die committeten CSV-Header"
      min_lines: 80
  key_links:
    - from: "docs/material-inventory.md"
      to: "data/reference/video_inventory.csv"
      via: "Spaltenmodell-Tabelle, per Test gegen den echten Header geprüft"
      pattern: "content_sha256"
    - from: "docs/sync-convention.md"
      to: "data/processed/plays.parquet"
      via: "dokumentierter Join auf game_id + play_id"
      pattern: "Int32"
---

<objective>
Schreibe die beiden Dokumente, die die in Plan 01 angelegten Referenz-CSVs erklären und verbindlich machen: die Materialinventur und die Sync-Konvention.

Purpose: Ohne `docs/material-inventory.md` ist `video_inventory.csv` ein Header ohne Bedeutung — niemand weiß, welche `domain`-Werte erlaubt sind oder wie ein `content_sha256` entsteht. Ohne `docs/sync-convention.md` bleiben Tracking-Daten von Strang 1 entkoppelt (Roadmap-Erfolgskriterium 4). Beide Dokumente sind die Übergabe an Phase 2.1 (Pilotmaterial auswählen) und Phase 2.3 (XY-Tracks an `plays.parquet` joinen).
Output: `docs/material-inventory.md`, `docs/sync-convention.md`, `tests/test_capture_reference_docs.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.0-capture-protocol-material-inventory/02.0-CONTEXT.md
@.planning/phases/02.0-capture-protocol-material-inventory/02.0-PATTERNS.md
@.planning/phases/02.0-capture-protocol-material-inventory/02.0-01-SUMMARY.md

<interfaces>
<!-- Committete Header aus Plan 01 — die Doku muss exakt diese Spaltennamen nennen. -->

data/reference/video_inventory.csv:
  domain,session_id,game_id,capture_date,resolution,fps,duration_seconds,local_path,content_sha256,notes

data/reference/video_sync.csv:
  game_id,play_id,video_file,snap_frame,snap_seconds,end_seconds,notes

<!-- Kanonische Join-Key-Dtypes, aus src/flag_football_ep/canonical.py -->
game_id : pl.Utf8
play_id : pl.Int32

<!-- Hudl-Seite des Joins, aus docs/data-contract.md §Spaltenmodell -->
Kern-Spalten: `PLAY #`, `ODK`, `DN`, `DIST`, `YARD LN`, `PLAY TYPE`, `RESULT`, `GN/LS`
`play_id` entsteht aus `PLAY #`; `game_id` aus der Dateinamens-Konvention
`YYYY-MM-DD_{TEAM1}-vs-{TEAM2}_{COMP}` (Beispiel: `2026-06-14_GER-vs-AUT_EM-QUALI`).

<!-- Werkzeuge, verifiziert auf der Maschine des Nutzers (RESEARCH §Environment Availability) -->
ffprobe -v quiet -print_format json -show_format -show_streams <datei>
  → streams[0].width, streams[0].height, streams[0].r_frame_rate (Bruch, z. B. "30000/1001"), format.duration
shasum -a 256 <datei>   (macOS)  /  sha256sum <datei>  (Linux)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/material-inventory.md — Materialinventur (D-06, D-07, D-08, D-01)</name>
  <files>docs/material-inventory.md</files>
  <read_first>
    - `docs/data-contract.md` (vollständig: Status-/Versionskopf Zeilen 1-7, Dialekt-Dualitäts-Notiz, §„Charting-Protokoll & Gesprächsagenda" mit dem Ratifizierungs-Block Zeile 167-169, §„Datenschutz-Hinweis")
    - `docs/ifaf-field-mapping.md` (Provenance-Kopf und Evidenz-Tabellenstil; Länge/Register sind das nähere Vorbild als data-contract.md)
    - `data/reference/video_inventory.csv` (der committete Header aus Plan 01)
    - `.gitignore` (der `data/video/*`-Block aus Plan 01)
    - `docs/plan-2-cv-tracking.md` §„Phase 2.0" (die drei genannten Kameradomänen: Drohne / TV-Seite / GoPro-Längsseite)
  </read_first>
  <action>
    Schreibe `docs/material-inventory.md` auf Deutsch, englische Identifier/Spaltennamen/Football-Vokabular bleiben englisch. Struktur mit genau diesen Überschriften (H1 + sieben H2):

    `# Materialinventur — Video-Bestand pro Kameradomäne`

    Direkt darunter ein kurzer Status-Block im Stil von `docs/data-contract.md` Zeile 1-7: eine Zeile „Maschinenlesbares Gegenstück: `data/reference/video_inventory.csv`", plus eine Status-Zeile in der Form „**Status: einseitig erstellt am {Datum} — Analysten-Ergänzung ausstehend (siehe Ratifizierungs-Block).**" (D-01).

    `## Zweck & Abgrenzung` — was die Inventur leistet (Phase 2.1 wählt daraus das Pilotmaterial aus) und was nicht (keine CV-Verarbeitung, kein Ingest-Code, kein Archiv-Durchlauf; gescopet auf die 2–3 Beispielclips pro Domäne aus dem Roadmap-Erfolgskriterium 1).

    `## Domänen & Bestand` — Tabelle mit den Spalten `domain (CSV-Wert) | Beschreibung | Wer hält das Material | Stand {Datum}`. Genau drei Zeilen, das feste `domain`-Vokabular:
    - `drone` — Drohne in fester Hover-Position, Primärdomäne (D-03 im PROJECT.md); bei offiziellen Spielen verboten (C-01)
    - `sideline` — erhöhte Seitenkamera / Stativ / GoPro-Längsseite / Veo-Klasse, Zweitdomäne für offizielle Spiele
    - `broadcast` — TV-/Seitenansicht Fremdmaterial; nur registriert, nicht verarbeitet (Phase 2.5, zurückgestellt)
    Spalte „Stand": ehrlich `unbekannt — beim Videoanalysten/Staff, nicht gesichtet` für alle drei, mit Verweis auf den Ratifizierungs-Block. Nichts erfinden.

    `## Spaltenmodell von data/reference/video_inventory.csv` — Tabelle mit den Spalten `Spalte | Typ | Pflicht | Hinweise`, eine Zeile pro Spalte in der Reihenfolge des committeten Headers: `domain`, `session_id`, `game_id`, `capture_date`, `resolution`, `fps`, `duration_seconds`, `local_path`, `content_sha256`, `notes`. Inhalte: `domain` string/Pflicht/nur `drone`|`sideline`|`broadcast`; `session_id` string/Pflicht/Kennung der Session im Stil `YYYY-MM-DD_{LABEL}`, z. B. `2026-05-12_TRAINING-DUS`; `game_id` string/nur wenn ein Hudl-Spiel existiert/muss exakt dem `game_id` in `data/processed/plays.parquet` entsprechen (Dateinamens-Konvention aus `docs/data-contract.md` §Dateinamenskonvention); `capture_date` string ISO `YYYY-MM-DD`/Pflicht; `resolution` string `BREITExHÖHE`/Pflicht/aus `ffprobe`; `fps` float/Pflicht/aus `r_frame_rate` ausgerechnet, z. B. `30000/1001` → `29.97`; `duration_seconds` float/Pflicht/`format.duration`; `local_path` string/Pflicht/repo-relativ unter `data/video/`, nie ein absoluter Benutzerpfad; `content_sha256` string/Pflicht wenn `local_path` gesetzt/64 Kleinbuchstaben-Hex; `notes` string/optional/Freitext **ohne Personennamen**.

    Darunter ein Absatz zur Dialekt-Dualität, formuliert wie `docs/data-contract.md` Zeile 31: diese Datei ist handgepflegt und verwendet Komma + UTF-8 **ohne** BOM; der Hudl-Export-Dialekt (`;` + `utf-8-sig`) gilt hier ausdrücklich nicht.

    `## Clip registrieren — Schritt für Schritt` — nummerierte Anleitung: (1) Clip nach `data/video/` legen (oder repo-relativen Pfad vergeben), (2) `ffprobe -v quiet -print_format json -show_format -show_streams <datei>` ausführen und `width`, `height`, `r_frame_rate`, `format.duration` ablesen, (3) `shasum -a 256 <datei>` (macOS) bzw. `sha256sum <datei>` (Linux) ausführen, (4) eine Zeile an `data/reference/video_inventory.csv` anhängen, (5) `uv run pytest tests/test_capture_artifacts.py -q` laufen lassen — das Gate prüft Domänen-Vokabular, Hash-Form und Pfad-Form. Mit der Zeit-Erwartung aus RESEARCH Pitfall 2 als expliziter Hinweis: SHA-256 über einen mehrere GB großen 4K-Clip dauert wenige Minuten je nach Speichermedium — das ist normal, nicht hängend. Und: eine Zeile mit gefülltem `local_path`, aber leerem `content_sha256` gilt als unvollständig, nicht als akzeptabel.

    `## Wo die Clips liegen` — `data/video/` ist per `.gitignore` (`data/video/*`, `!data/video/.gitkeep`) von Git ausgeschlossen, gleiche PII-Policy wie `data/raw/hudl/*`; die Aufnahmen zeigen Gesichter von Spielerinnen. Die CSV ist die Zeiger- und Verifikationsschicht: `local_path` + `content_sha256` machen einen verschobenen oder umbenannten Clip wiederauffindbar und einen beschädigten erkennbar. Clips außerhalb des Repos sind zulässig, brauchen aber trotzdem einen repo-relativen Symlink unter `data/video/`, damit `local_path` keinen Benutzerpfad enthält.

    `## Was nicht in die CSV kommt` — die Auflösung von RESEARCH Open Question 2: Material, das nur der Analyst hält und das der Nutzer nie gesehen hat, bekommt **keine** CSV-Zeile (jede Zeile muss hashbar und damit verifizierbar sein). Solches Material wird in der Tabelle unter „Domänen & Bestand" in Prosa geführt und im Ratifizierungs-Block adressiert.

    `## Ratifizierungs-Block` — DEFERRED-ANALYST-Blockquote im Wortlaut-Schema von `docs/data-contract.md` Zeile 167-169: „> DEFERRED-ANALYST: Gespräch auf unbestimmte Zeit verschoben (User-Entscheid 2026-08-17). Owner: Nutzer. Follow-up-Trigger: sobald der Videoanalyst wieder verfügbar ist, spätestens vor dem nächsten Filmtausch. Zu ratifizieren: (a) …, (b) …, (c) …". Die drei Punkte hier: (a) welches Material pro Domäne tatsächlich existiert (Spalte „Stand"), (b) Übergabe von 2–3 Beispielclips pro Domäne zur Sicherung und Hashung, (c) ob `broadcast`-Material überhaupt zugänglich ist. „Owner: Nutzer" beibehalten.

    Keine Personennamen im Dokument. Keine erfundenen Bestandszahlen.
  </action>
  <acceptance_criteria>
    - `docs/material-inventory.md` existiert und enthält die H1 `# Materialinventur — Video-Bestand pro Kameradomäne`
    - Enthält alle sieben H2-Überschriften: `## Zweck & Abgrenzung`, `## Domänen & Bestand`, `## Spaltenmodell von data/reference/video_inventory.csv`, `## Clip registrieren — Schritt für Schritt`, `## Wo die Clips liegen`, `## Was nicht in die CSV kommt`, `## Ratifizierungs-Block`
    - Enthält den String `DEFERRED-ANALYST` und den String `Owner: Nutzer`
    - Enthält alle zehn Spaltennamen des committeten `video_inventory.csv`-Headers
    - Enthält die Strings `ffprobe`, `shasum -a 256`, `sha256sum`, `data/video/*`, `drone`, `sideline`, `broadcast`
    - Enthält keinen `player_name`-Wert aus `data/reference/roster.csv`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_capture_reference_docs.py -q -k inventory</automated>
  </verify>
  <done>Die Materialinventur erklärt jede CSV-Spalte, das Registrier-Verfahren mit konkreten Shell-Kommandos, die PII-Ablage und trägt den DEFERRED-ANALYST-Block.</done>
</task>

<task type="auto">
  <name>Task 2: docs/sync-convention.md — Video-Play ↔ Hudl-PBP-Play (D-09, D-10, D-11)</name>
  <files>docs/sync-convention.md</files>
  <read_first>
    - `docs/data-contract.md` (§„Abgeleitete Felder" Zeilen 62-87 für den Ableitungs-Tabellenstil `Feld | Quelle | Regel | Randfälle`; §„Charting-Protokoll & Gesprächsagenda" Zeilen 157-169 für die „Festgelegt (Datum):"-Struktur plus konsolidierten Ratifizierungs-Block; §Dateinamenskonvention für die `game_id`-Form)
    - `data/reference/video_sync.csv` (der committete Header aus Plan 01)
    - `data/reference/half_boundaries.csv` (der strukturell nächste Präzedenzfall: kleine, handgepflegte Anker-CSV, die eine Lücke schließt, die der Export nicht liefert)
    - `tests/test_capture_artifacts.py` (`SYNC_SCHEMA` und `test_video_sync_schema_joins_plays_shaped_frame` — die Doku muss dieselben Dtypes nennen wie das Gate prüft)
  </read_first>
  <action>
    Schreibe `docs/sync-convention.md` auf Deutsch, englische Identifier bleiben englisch. Struktur mit genau diesen Überschriften (H1 + acht H2):

    `# Sync-Konvention — Video-Play ↔ Hudl-PBP-Play`

    Darunter ein Status-Block wie in Task 1: „Maschinenlesbares Gegenstück: `data/reference/video_sync.csv`" plus „**Status: Variante B einseitig festgelegt am {Datum}; Variante A ist ein Wunsch — Analysten-Ratifizierung ausstehend (siehe Ratifizierungs-Block).**"

    `## Zweck & Abgrenzung` — die Konvention hängt Videopositionen an Plays, die in Hudl **schon existieren**; sie definiert niemals einen Play neu (D-10). Ohne diesen Join bleiben Tracking-Daten von Strang 1 entkoppelt (Roadmap-Erfolgskriterium 4). Nicht in Scope: automatische Snap-Erkennung (Phase 2.3), XY-Extraktion (Phase 2.1).

    `## Referenzseite: was in Hudl schon existiert` — `PLAY #` wird beim Ingest zu `play_id`; `game_id` kommt aus der Export-Dateinamens-Konvention `YYYY-MM-DD_{TEAM1}-vs-{TEAM2}_{COMP}`. `ODK`, `DN`, `DIST` und `RESULT` dienen als Plausibilitätscheck, dass beim manuellen Markieren der richtige Play getroffen wurde (D-10) — nenne konkret: nach dem Setzen eines Ankers die `DN`/`DIST`/`RESULT`-Werte des Ziel-Plays gegen das anschauen, was im Video passiert.

    `## Was von Staff verlangt wird: nichts` — expliziter Abschnitt zu D-09. Kein Board, kein Klatschen, kein Timestamp-Overlay ist Voraussetzung. Ein sichtbares Drive-Signal steht in `docs/capture-protocol.md` ausschließlich als Wunsch mit Begründung; die Konvention funktioniert bei null Kooperation.

    `## Variante A — Upload nach Hudl (Wunsch, DEFERRED-ANALYST)` — das Drohnenmaterial wird wie Spielfilm nach Hudl hochgeladen und vom Analysten ohnehin pro Play geschnitten; dann sind die Hudl-Clipgrenzen die Anker und es entfällt jedes manuelle Markieren. Mit Begründung formuliert als Bitte, nicht als Erwartung. Ausdrücklich: die Konvention hängt nicht davon ab.

    `## Variante B — manuelle Snap-Anker (immer verfügbar)` — Rohvideodatei außerhalb von Hudl; der Nutzer markiert jeden Snap selbst. **Ein Anker pro Play, nie pro Drive** — mit der Begründung aus D-11: robust gegen ungefilmte Plays, Akku-Lücken und Penalties, die eine Drive-relative Zeitrechnung sofort auseinanderlaufen lassen. Es wird kein verlässlicher Timestamp im Material erwartet; Anker sind Framenummern bzw. Sekunden-Offsets **innerhalb einer benannten Videodatei**. Verweis auf `data/reference/half_boundaries.csv` als Präzedenzfall derselben Bauart: kleiner manueller Input, der eine Lücke schließt, die der Export nicht liefert.

    `## Spaltenmodell von data/reference/video_sync.csv` — zwei Tabellen. Erstens die Ableitungstabelle im Stil von `docs/data-contract.md` §„Abgeleitete Felder" mit den Spalten `Feld | Quelle | Regel | Randfälle`, eine Zeile pro Spalte des committeten Headers (`game_id`, `play_id`, `video_file`, `snap_frame`, `snap_seconds`, `end_seconds`, `notes`). Konkret: `game_id` ← Hudl-Exportdateiname, muss exakt dem `plays.parquet`-Wert entsprechen; `play_id` ← Hudl `PLAY #`, ganzzahlig, ohne führende Nullen und ohne Leerzeichen; `video_file` ← ein `local_path`-Wert aus `video_inventory.csv`; `snap_frame` ← Framenummer des Snaps in dieser Datei; `snap_seconds` ← Sekunden-Offset des Snaps, mindestens eine der beiden Spalten muss gefüllt sein; `end_seconds` optional, Ende des relevanten Ausschnitts; `notes` Freitext ohne Personennamen. Randfälle benennen: Play im Video nicht vorhanden (Akku-Wechsel, Kamera aus) → keine Zeile statt Zeile mit Nullwerten; Play über zwei Videodateien verteilt → Anker auf die Datei setzen, die den Snap enthält.
    Zweitens die Dtype-Tabelle, identisch zu `SYNC_SCHEMA` in `tests/test_capture_artifacts.py`: `game_id: Utf8`, `play_id: Int32`, `video_file: Utf8`, `snap_frame: Int64`, `snap_seconds: Float64`, `end_seconds: Float64`, `notes: Utf8`. Plus derselbe Dialekt-Hinweis wie in `docs/material-inventory.md` (Komma, UTF-8 ohne BOM, nicht der Hudl-Dialekt).

    `## Join gegen data/processed/plays.parquet` — der Join läuft als inner join auf `["game_id", "play_id"]`. Deutliche Warnung: bei Dtype-Mismatch (etwa `play_id` als String `"03"` statt Integer `3`) liefert der Join **null Zeilen ohne Fehlermeldung** — das sieht aus wie fehlendes Material, ist aber ein Typfehler. Deshalb wird beim Lesen immer explizit `schema_overrides` gesetzt, und `tests/test_capture_artifacts.py::test_video_sync_schema_joins_plays_shaped_frame` bewacht genau diesen Fall. Nenne auch, dass `data/processed/*` gitignored ist: `plays.parquet` existiert in keinem frischen Clone und wird in Tests durch ein inline gebautes Fixture ersetzt.

    `## Kompatibilität mit automatischer Snap-Erkennung (Phase 2.3)` — dieselbe Zeilenform (`game_id`, `play_id`, `video_file`, `snap_frame`/`snap_seconds`) ist das, was eine Snap-Erkennung später vorschlagen würde; automatische Vorschläge ersetzen also nur die Befüllung, nicht das Format. Empfehlung: maschinell erzeugte Zeilen in `notes` kennzeichnen, damit sie von handgesetzten Ankern unterscheidbar bleiben.

    `## Ratifizierungs-Block` — DEFERRED-ANALYST-Blockquote im Wortlaut-Schema von `docs/data-contract.md` Zeile 167-169, „Owner: Nutzer" beibehalten. Zu ratifizieren: (a) Bereitschaft, Drohnenmaterial wie Spielfilm nach Hudl hochzuladen und pro Play zu schneiden (Variante A), (b) ob die Hudl-Clipgrenzen exportierbar/auslesbar sind, (c) ob `PLAY #` im Drohnen-Charting identisch zum bestehenden Charting vergeben wird.
  </action>
  <acceptance_criteria>
    - `docs/sync-convention.md` existiert und enthält die H1 `# Sync-Konvention — Video-Play ↔ Hudl-PBP-Play`
    - Enthält alle acht H2-Überschriften, darunter wörtlich `## Variante A — Upload nach Hudl (Wunsch, DEFERRED-ANALYST)`, `## Variante B — manuelle Snap-Anker (immer verfügbar)` und `## Was von Staff verlangt wird: nichts`
    - Enthält alle sieben Spaltennamen des committeten `video_sync.csv`-Headers
    - Enthält die Strings `Utf8`, `Int32`, `plays.parquet`, `DEFERRED-ANALYST`, `Owner: Nutzer`
    - Enthält keinen `player_name`-Wert aus `data/reference/roster.csv`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_capture_reference_docs.py -q -k sync</automated>
  </verify>
  <done>Die Sync-Konvention beschreibt beide Varianten, das vollständige Spaltenmodell mit Dtypes, den Join-Vertrag inklusive stiller Fehlermodus und den DEFERRED-ANALYST-Block.</done>
</task>

<task type="auto">
  <name>Task 3: Section- und Drift-Gates für beide Dokumente</name>
  <files>tests/test_capture_reference_docs.py</files>
  <read_first>
    - `tests/test_repo_hygiene.py` (Zeilen 1-80: `REPO_ROOT`-Konstante, Substring-Assertions auf Dateiinhalte, Fehlermeldungs-Stil)
    - `tests/test_capture_artifacts.py` (Plan 01: Konstanten-Layout, `INVENTORY_COLUMNS`/`SYNC_COLUMNS`, Roster-PII-Helper)
    - `docs/material-inventory.md` und `docs/sync-convention.md` (Ergebnis von Task 1 und 2)
    - `data/reference/roster.csv`
  </read_first>
  <action>
    Neue Datei `tests/test_capture_reference_docs.py` mit Modul-Docstring, der erklärt: diese Gates bewachen die beiden CSV-begleitenden Dokumente gegen Section-Verlust und gegen Drift zwischen dokumentiertem und committetem CSV-Header.

    Modul-Konstanten: `REPO_ROOT`, `DOCS_DIR = REPO_ROOT / "docs"`, `INVENTORY_DOC`, `SYNC_DOC`, `INVENTORY_CSV`, `SYNC_CSV`, sowie zwei Tupel `INVENTORY_DOC_SECTIONS` und `SYNC_DOC_SECTIONS` mit den in Task 1/2 festgelegten H2-Überschriften im Wortlaut.

    Diese Tests, mit exakt diesen Namen:

    1. `test_inventory_doc_has_required_sections` — `INVENTORY_DOC` existiert; für jede Überschrift aus `INVENTORY_DOC_SECTIONS` gilt: sie kommt als eigene Zeile im Dokument vor. Zeilenweise vergleichen (`line.strip() == section`), nicht per `in`-Substring — sonst zählt eine Erwähnung im Fließtext als Überschrift.
    2. `test_sync_doc_has_required_sections` — analog für `SYNC_DOC_SECTIONS`, inklusive der beiden Varianten-Überschriften.
    3. `test_docs_document_every_committed_csv_column` — parametrisiert über die Paare (`INVENTORY_DOC`, `INVENTORY_CSV`) und (`SYNC_DOC`, `SYNC_CSV`): liest die erste Zeile der CSV, splittet auf `,`, und asserted für jeden Spaltennamen, dass er im Dokumenttext vorkommt. Das ist das Drift-Gate: wer eine Spalte hinzufügt, ohne sie zu dokumentieren, bekommt einen roten Test.
    4. `test_deferred_analyst_blocks_present_and_owned` — beide Dokumente enthalten `DEFERRED-ANALYST` und `Owner: Nutzer` (D-01: der Nutzer besitzt den Follow-up-Trigger, nicht der Analyst).
    5. `test_sync_doc_states_join_dtypes` — `SYNC_DOC` enthält `Utf8`, `Int32` und `plays.parquet`; zusätzlich enthält es beide Varianten-Marker `Variante A` und `Variante B` (RESEARCH Pitfall 1 + D-11).
    6. `test_inventory_doc_documents_registration_commands` — `INVENTORY_DOC` enthält `ffprobe`, `shasum -a 256`, `sha256sum` und `data/video/*`, und alle drei `domain`-Vokabelwerte `drone`, `sideline`, `broadcast`.
    7. `test_reference_docs_contain_no_roster_names` — wie in `tests/test_capture_artifacts.py`: eindeutige `player_name`-Werte aus `data/reference/roster.csv` lesen und asserten, dass keiner (case-insensitiv, vollständiger Name) in einem der beiden Dokumente vorkommt (T-2.0-02).

    Style wie `tests/test_repo_hygiene.py`: `from __future__ import annotations`, Typannotationen, `pytest.mark.parametrize` für Test 3, plain `assert` mit sprechender Fehlermeldung. Der `-k inventory` / `-k sync`-Filter aus den Verify-Kommandos der Tasks 1 und 2 muss greifen — deshalb tragen die Testnamen die Wörter `inventory` bzw. `sync`; Test 3, 4 und 7 laufen bewusst unter beiden Filtern nicht vollständig und werden über den ungefilterten Lauf abgedeckt.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_capture_reference_docs.py -q` exit code 0
    - `uv run pytest tests/test_capture_reference_docs.py --collect-only -q` listet alle sieben oben genannten Testnamen
    - Ein künstlich entfernter H2-Header in einem der Dokumente lässt Test 1 bzw. 2 fehlschlagen (manuell einmal gegengeprüft, Änderung danach zurückgenommen)
    - `uv run pytest -q` exit code 0 (volle Suite bleibt grün)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_capture_reference_docs.py -q</automated>
  </verify>
  <done>Beide Dokumente sind gegen Section-Verlust, CSV-Header-Drift, fehlende DEFERRED-ANALYST-Blöcke und Klarnamen-Leaks mechanisch abgesichert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dokument → geteiltes Artefakt | `docs/*.md` sind versionierte, potenziell weitergegebene Dateien; personenbezogene Details darin sind dauerhaft |
| Dokumentierter Vertrag → späterer Join | Was hier als Spaltenmodell steht, ist der einzige Vertrag, gegen den Phase 2.3 joint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.0-02 | Information Disclosure | `docs/material-inventory.md`, `docs/sync-convention.md` (Beispielzeilen, `notes`-Erläuterungen) | mitigate | Beispiele verwenden ausschließlich fiktive `game_id`/Dateinamen ohne Personenbezug; `notes` ist als „ohne Personennamen" dokumentiert; `test_reference_docs_contain_no_roster_names` prüft gegen die Klarnamen aus `data/reference/roster.csv` |
| T-2.0-05 | Tampering | Dokumentierte Dtypes laufen von `SYNC_SCHEMA` und `plays.parquet` weg → stiller 0-Zeilen-Join | mitigate | Dtype-Tabelle in `docs/sync-convention.md` ist wortgleich zu `SYNC_SCHEMA`; `test_sync_doc_states_join_dtypes` und `test_docs_document_every_committed_csv_column` bewachen Drift in beide Richtungen |
| T-2.0-06 | Information Disclosure | `local_path`-Erläuterung verleitet zum Eintragen absoluter Benutzerpfade | mitigate | `## Wo die Clips liegen` schreibt repo-relative Pfade (ggf. via Symlink unter `data/video/`) als Norm fest; das Gate `test_video_inventory_paths_stay_repo_relative` aus Plan 01 erzwingt es |
| T-2.0-SC | Tampering | npm/pip/uv-Installationen | accept | Diese Phase installiert kein Paket; `pyproject.toml` bleibt unverändert (Gate `test_phase_adds_no_video_python_dependency` aus Plan 01) |
</threat_model>

<verification>
- `uv run pytest tests/test_capture_reference_docs.py -q` grün
- `uv run pytest -q` grün (volle Suite)
- Jede Spalte aus beiden committeten CSV-Headern kommt im zugehörigen Dokument vor (Test 3)
- Beide Dokumente tragen einen DEFERRED-ANALYST-Block mit `Owner: Nutzer`
</verification>

<success_criteria>
1. `docs/material-inventory.md` erklärt alle zehn `video_inventory.csv`-Spalten, das Registrier-Verfahren mit `ffprobe`/`shasum` inklusive Zeit-Erwartung, die gitignorete Ablage und trägt den DEFERRED-ANALYST-Block
2. `docs/sync-convention.md` beschreibt Variante A (Wunsch) und Variante B (immer verfügbar, ein Anker pro Play), das vollständige Spaltenmodell mit Dtypes und den Join-Vertrag inklusive des stillen 0-Zeilen-Fehlermodus
3. Beide Dokumente sind deutschsprachig mit englischen Identifiern und folgen dem Status-/Ratifizierungs-Schema von `docs/data-contract.md`
4. `tests/test_capture_reference_docs.py` hält beide Dokumente gegen Section-Verlust und CSV-Drift grün
</success_criteria>

<output>
Create `.planning/phases/02.0-capture-protocol-material-inventory/02.0-02-SUMMARY.md` when done
</output>
