---
phase: 02.0-capture-protocol-material-inventory
plan: 03
subsystem: docs
tags: [capture-protocol, drone, dsgvo, eu-drone-regulation, pytest]

# Dependency graph
requires:
  - phase: 02.0-01
    provides: "data/reference/video_inventory.csv header (D-07), .gitignore data/video/ block, tests/test_capture_artifacts.py mechanical gate style"
provides:
  - "docs/capture-protocol.md — the Staff-facing capture wish list, drone primary domain (8 parameters x 3 tiers) + elevated side-camera second domain (compact table), sync signal marked as pure wish"
  - "docs/capture-legal.md — short responsibility-split note: EU drone regulation with the operator (D-12), DSGVO consent held by the federation (D-13), no new compliance artifacts"
  - "tests/test_capture_protocol_docs.py — 10 mechanical gates: section completeness, all-eight-parameters, three-tier coverage, mandate-language ban, [ASSUMED]/Richtwert tagging, sync-signal optionality, legal-doc section/policy-level/roster-name gates"
affects: [02.0-04, 02.1]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tiered wish-list table pattern (Ideal/Brauchbar/Unbrauchbar per parameter, one-line justification), mirrored from docs/data-contract.md's Kern/Optional density"
    - "DEFERRED-ANALYST ratification blockquote reused verbatim from docs/data-contract.md §8, 'Owner: Nutzer' preserved"
    - "Mandate-language ban is a line-level, case-insensitive substring gate with a single named exception (lines containing 'Pflichtenheft')"

key-files:
  created:
    - docs/capture-protocol.md
    - docs/capture-legal.md
    - tests/test_capture_protocol_docs.py

key-decisions:
  - "Second-domain (elevated side-camera) height/standpoint values are explicitly tagged [ASSUMED] rather than given the same confidence as the drone Richtwerte — nothing in the project fixes these numbers, unlike the drone's 30-60m which traces to research-notes.md"
  - "Sync-Signal parameter intentionally has no Unbrauchbar tier ('entfällt') — D-09 requires the sync convention to work at zero cooperation, so nothing about capture is disqualifying here"
  - "capture-legal.md kept to 28 lines with no version/changelog apparatus (unlike the 180-line data-contract.md) since D-12 explicitly scopes it as an orientation pointer, not a specification"

requirements-completed: [REQ-S2-01]

# Metrics
duration: ~15min
completed: 2026-08-22
---

# Phase 02.0 Plan 03: Capture Protocol & Legal Note Summary

**`docs/capture-protocol.md` (Staff-facing drone + second-camera capture wish list, 8 tiered drone parameters) and `docs/capture-legal.md` (EU-drone/DSGVO responsibility-split note), both backed by a 10-gate `tests/test_capture_protocol_docs.py`.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-08-22T13:00:00+02:00 (approx, after worktree base correction)
- **Completed:** 2026-08-22T13:03:28+02:00
- **Tasks:** 3/3 completed
- **Files modified:** 3 (all created)

## Accomplishments
- `docs/capture-protocol.md`: German wish-list document, drone as primary domain with all eight parameters (hover position/angle, altitude, resolution, frame rate, exposure/white balance, field coverage, recording duration/battery swap, sync signal) each carrying an Ideal/Brauchbar/Unbrauchbar tier with one-line reasoning grounded in `docs/research-notes.md` points 6-7 (TeamTrack mAP evidence, small-object regime, static-hover homography); second domain (elevated side camera) compressed into one table; ratification block, sync-convention/inventory-CSV cross-references, and mandate-language ban all verified inline
- `docs/capture-legal.md`: 28-line responsibility-split note — EU drone regulation stays fully with the operator (D-12, `[ASSUMED]` orientation pointer only, no checklist), DSGVO consent stays with the federation (D-13, analysis-only scope, no new consent artifact), practical repo tie-in to the `.gitignore`'d `data/video/` PII pattern
- `tests/test_capture_protocol_docs.py`: 10 named pytest gates covering both documents — section completeness, all-eight-drone-parameter coverage, tier-count floor, mandate-language ban (with the single `Pflichtenheft`-line exception), `[ASSUMED]`/`Richtwert` tagging, sync-signal optionality, legal-doc section completeness, responsibility-split token presence, policy-level (no checkbox syntax, <60 lines) gate, and a shared roster-name leak check across both docs

## Task Commits

Each task was committed atomically:

1. **Task 1: docs/capture-protocol.md — der Wunschzettel (D-02 bis D-05, D-09)** - `d1a3608` (docs)
2. **Task 2: docs/capture-legal.md — Zuständigkeitsnotiz (D-12, D-13)** - `dd3a554` (docs)
3. **Task 3: Stufen-, Tonfall- und Policy-Gates** - `43238a1` (test)

**Plan metadata:** committed by this SUMMARY commit (worktree mode — orchestrator handles STATE.md/ROADMAP.md after merge)

## Files Created/Modified
- `docs/capture-protocol.md` - Drone + second-domain capture wish list, 123 lines, all acceptance checks (headings, tier counts >=9 each, DEFERRED-ANALYST/Owner: Nutzer/[ASSUMED]/Richtwert tokens, mandate-language ban, roster-name-free) pass
- `docs/capture-legal.md` - EU-drone/DSGVO responsibility note, 28 lines, no checkbox syntax, roster-name-free
- `tests/test_capture_protocol_docs.py` - 10 named gates, all collected and green individually and via `-k protocol`/`-k legal`; manually verified the mandate-language gate fails on an injected "ihr müsst" line and passes again after revert

## Decisions Made
- Second-domain height/standpoint values tagged `[ASSUMED]` distinctly from the drone's `Richtwert` tagging, to signal a lower confidence tier for numbers with zero grounding in `docs/research-notes.md`
- Sync-Signal deliberately has no Unbrauchbar row (`entfällt`) per D-09 — documented explicitly in-doc and asserted by `test_protocol_doc_keeps_sync_signal_optional`

## Deviations from Plan

None - plan executed exactly as written. All acceptance criteria for all three tasks verified directly:
- Task 1 inline Python check: `PROTOCOL_DOC_OK`
- Task 2 inline Python check: `LEGAL_DOC_OK` (28 lines)
- Task 3: `uv run pytest tests/test_capture_protocol_docs.py -q` — 10/10 passed; `--collect-only -q` lists all 10 named tests; `-k protocol` and `-k legal` both non-empty and green; injected "ihr müsst" line confirmed to fail `test_protocol_doc_uses_no_mandate_language`, reverted cleanly (`git diff --stat docs/capture-protocol.md` empty before the Task 3 commit)
- Combined run `uv run pytest tests/test_capture_protocol_docs.py tests/test_capture_artifacts.py -q` — 22/22 passed

## Issues Encountered

None. Per project convention (full suite runtime exceeds 5 minutes), `uv run pytest -q` was not run for the full repository; this plan only adds two new doc files and one new self-contained test file, touching no existing source or test files, so regression risk against the rest of the suite is minimal. The plan-scoped test file plus the Plan 01 capture-artifacts test file were run directly and passed cleanly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 04 (material inventory doc, if scoped to reference these) and Phase 2.1 (pilot footage selection) can treat `docs/capture-protocol.md` and `docs/capture-legal.md` as committed, gate-verified fact
- Both documents are ready to hand to Staff/the federation as-is; DEFERRED-ANALYST ratification remains open per the blockquote in `docs/capture-protocol.md`
- No blockers

---
*Phase: 02.0-capture-protocol-material-inventory*
*Completed: 2026-08-22*
