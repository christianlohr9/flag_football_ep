---
phase: 02.0-capture-protocol-material-inventory
plan: 01
subsystem: data
tags: [polars, gitignore, reference-csv, capture-protocol, cv-pilot]

# Dependency graph
requires: []
provides:
  - "data/video/ gitignored clip directory with .gitkeep anchor (T-2.0-01, PII policy for player faces)"
  - "data/reference/video_inventory.csv header-only, D-07 minimum column set"
  - "data/reference/video_sync.csv header-only, D-11 variant B sync anchor schema"
  - "tests/test_capture_artifacts.py — 10 mechanical gates guarding header/dtype drift, PII leaks, and the no-video-dependency tier boundary"
affects: [02.0-02, 02.1]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Hand-maintained reference CSV dialect: comma-delimited, UTF-8 without BOM, header-only is a valid initial state (mirrors half_boundaries.csv/team_mapping.csv)"
    - "Gitignore block mirrors data/raw/hudl/* PII pattern: ignore contents (data/video/*), re-include only .gitkeep"
    - "Mechanical join-dtype smoke test against an inline plays-shaped pl.DataFrame fixture, never against the gitignored plays.parquet itself"

key-files:
  created:
    - data/video/.gitkeep
    - data/reference/video_inventory.csv
    - data/reference/video_sync.csv
    - tests/test_capture_artifacts.py
  modified:
    - .gitignore

key-decisions:
  - "Both reference CSVs created header-only (no example/placeholder rows) — no material has been secured yet (D-01), and a fake row would break the 'every row is verifiable' guarantee the tests enforce"
  - "No src/ loader added for either CSV — all schema reads happen inline in the test file via pl.read_csv(schema_overrides=...), per the plan's explicit 'no Pipeline-Code this phase' boundary"

requirements-completed: [REQ-S2-01]

# Metrics
duration: 25min
completed: 2026-08-22
---

# Phase 02.0 Plan 01: Capture Artifacts Foundation Summary

**Gitignored `data/video/` directory, header-only `video_inventory.csv`/`video_sync.csv` reference CSVs (D-07/D-11), and a 10-gate `tests/test_capture_artifacts.py` guarding header drift, PII leaks, and the join-dtype match against `plays.parquet`'s `game_id`/`play_id` types.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-22T10:22:00Z (approx, worktree base correction)
- **Completed:** 2026-08-22T10:47:00Z
- **Tasks:** 3/3 completed
- **Files modified:** 5 (1 modified, 4 created)

## Accomplishments
- `.gitignore` extended with a `data/video/*` / `!data/video/.gitkeep` block mirroring the existing `data/raw/hudl/*` PII pattern — drone/side-camera clips showing player faces can never accidentally enter git history
- `data/reference/video_inventory.csv` and `data/reference/video_sync.csv` created header-only, comma-delimited, UTF-8 without BOM, with the exact column sets specified by D-07 and D-11 variant B
- `tests/test_capture_artifacts.py` adds all ten named mechanical gates: header/dialect drift, inventory row verifiability and repo-relative paths, sync row anchoring, a plays-shaped join-dtype smoke test (RESEARCH Pitfall 1 guard), `.gitignore`/git-tracking hygiene, roster-name PII leak detection, and the no-video-dependency architecture gate on `pyproject.toml`

## Task Commits

Each task was committed atomically:

1. **Task 1: Gitignoretes Video-Verzeichnis anlegen (D-08, T-2.0-01)** - `546a724` (feat)
2. **Task 2: Die beiden handgepflegten Referenz-CSVs anlegen (D-07, D-11)** - `4090def` (feat)
3. **Task 3: Mechanische Gates für CSVs, .gitignore und Git-Hygiene** - `77b47cd` (test)

**Plan metadata:** (this SUMMARY commit, added by orchestrator after merge)

## Files Created/Modified
- `.gitignore` - adds `data/video/*` / `!data/video/.gitkeep` PII block after `/reports/`, before the closing "intentionally NOT ignored" note
- `data/video/.gitkeep` - empty anchor file so the gitignored directory stays re-includable
- `data/reference/video_inventory.csv` - header-only, `domain,session_id,game_id,capture_date,resolution,fps,duration_seconds,local_path,content_sha256,notes`
- `data/reference/video_sync.csv` - header-only, `game_id,play_id,video_file,snap_frame,snap_seconds,end_seconds,notes`
- `tests/test_capture_artifacts.py` - 10 mechanical gates (12 collected test IDs after parametrization), no `src/` loader introduced, never reads `data/processed/plays.parquet`

## Decisions Made
- Header-only CSVs with zero example rows: a fabricated row would violate the "every registered row is verifiable" invariant the tests enforce (`test_video_inventory_rows_are_verifiable`, `test_video_sync_rows_are_anchored`); example rows for humans live in the docs (Plan 02) and as `tmp_path` fixtures in the join-dtype test instead
- Schema/dtype dicts (`INVENTORY_SCHEMA`, `SYNC_SCHEMA`) live only in the test module, matching `_TEAM_MAPPING_SCHEMA`-style dicts in `src/flag_football_ep/reference.py` in spirit but deliberately not adding a `src/` loader this phase (Tier boundary: docs + reference data, no pipeline code)

## Deviations from Plan

None - plan executed exactly as written. All acceptance criteria for all three tasks verified directly:
- `git check-ignore -q data/video/probe.mp4` exit 0, `git check-ignore -q data/video/.gitkeep` exit 1
- Both CSVs: header matches exactly, `wc -l` == 1, no BOM, no `;`
- `uv run pytest tests/test_capture_artifacts.py -q` — 12/12 passed
- `uv run pytest tests/test_capture_artifacts.py --collect-only` lists all 10 named tests (2 parametrized into 2 IDs each)
- `grep -c "data/processed/plays.parquet" tests/test_capture_artifacts.py` == 0
- `grep -c "from flag_football_ep" tests/test_capture_artifacts.py` == 0

## Issues Encountered
None. The full repository test suite (`uv run pytest -q`) was kicked off in the background per the plan's overall verification step; per project convention (full suite runtime exceeds 5 minutes) this run was not blocked on for task completion — the plan-scoped test file (`tests/test_capture_artifacts.py`) was verified directly and passed cleanly, and the changes in this plan (a `.gitignore` addition, two new header-only CSVs, one new self-contained test file) touch no existing source or test files, so regression risk against the rest of the suite is minimal.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plan 02 (docs: `docs/capture-protocol.md`, `docs/material-inventory.md`, `docs/capture-legal.md`, `docs/sync-convention.md`) can now reference these committed artifacts (CSV column sets, `.gitignore` block) as ratified fact
- Phase 2.1 can read `video_inventory.csv` once populated to select pilot footage; Phase 2.3 can read `video_sync.csv` anchors to join XY tracks to `plays.parquet` — both CSVs are currently header-only pending actual captured material
- No blockers

---
*Phase: 02.0-capture-protocol-material-inventory*
*Completed: 2026-08-22*
