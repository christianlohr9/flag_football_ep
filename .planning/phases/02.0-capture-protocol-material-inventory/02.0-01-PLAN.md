---
phase: 02.0-capture-protocol-material-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - data/video/.gitkeep
  - data/reference/video_inventory.csv
  - data/reference/video_sync.csv
  - tests/test_capture_artifacts.py
autonomous: true
requirements: [REQ-S2-01]

must_haves:
  truths:
    - "Ein Video-Clip unter `data/video/` kann nicht versehentlich committet werden — Gesichter von Spielerinnen bleiben aus der Git-Historie (D-08, T-2.0-01)"
    - "`data/reference/video_inventory.csv` existiert mit dem in D-07 geforderten Mindest-Spaltensatz im handgepflegten Repo-Dialekt (Komma, UTF-8 ohne BOM)"
    - "`data/reference/video_sync.csv` existiert und trägt `game_id`/`play_id` in genau den Typen, mit denen `data/processed/plays.parquet` joinbar ist (D-11 Variante B)"
    - "Ein Dtype-Fehler in einer handgepflegten Sync-Zeile fällt als roter Test auf, nicht als stiller 0-Zeilen-Join (RESEARCH Pitfall 1)"
    - "Die Phase fügt keine Python-Video-/CV-Abhängigkeit hinzu — die Tier-Grenze „Dokumentation, kein Pipeline-Code" ist mechanisch bewacht"
  artifacts:
    - path: ".gitignore"
      provides: "PII-Ignore-Block für das Video-Verzeichnis"
      contains: "data/video/*"
    - path: "data/video/.gitkeep"
      provides: "Verzeichnis-Anker, damit `data/video/*` re-inkludierbar bleibt"
    - path: "data/reference/video_inventory.csv"
      provides: "Maschinenlesbare Materialinventur (D-07)"
      contains: "domain,session_id,game_id,capture_date,resolution,fps,duration_seconds,local_path,content_sha256,notes"
    - path: "data/reference/video_sync.csv"
      provides: "Sync-Anker Variante B (D-11)"
      contains: "game_id,play_id,video_file,snap_frame,snap_seconds,end_seconds,notes"
    - path: "tests/test_capture_artifacts.py"
      provides: "Mechanische Gates für beide CSVs, .gitignore und Git-Hygiene"
      min_lines: 120
  key_links:
    - from: "data/reference/video_sync.csv"
      to: "data/processed/plays.parquet"
      via: "inner join auf game_id (Utf8) + play_id (Int32)"
      pattern: "play_id.*Int32"
    - from: ".gitignore"
      to: "data/video/"
      via: "git check-ignore"
      pattern: "data/video/\\*"
---

<objective>
Lege das maschinenlesbare Fundament der Phase: die beiden handgepflegten Referenz-CSVs, das gitignorete Video-Verzeichnis und die mechanischen Tests, die beides bewachen.

Purpose: Phase 2.1 liest `video_inventory.csv`, um das Pilot-Material auszuwählen; Phase 2.3 liest `video_sync.csv`, um XY-Tracks an `plays.parquet` zu hängen. Beide Dateien sind hand-editiert — ohne Dtype-/Header-Gate scheitert der Join später still (RESEARCH Pitfall 1), und ohne `.gitignore`-Block landen PII-behaftete Clips in der Git-Historie (T-2.0-01).
Output: `.gitignore`-Erweiterung + `data/video/.gitkeep`, zwei Header-only Referenz-CSVs, `tests/test_capture_artifacts.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.0-capture-protocol-material-inventory/02.0-CONTEXT.md
@.planning/phases/02.0-capture-protocol-material-inventory/02.0-PATTERNS.md

<interfaces>
<!-- Kanonische Join-Key-Dtypes, aus src/flag_football_ep/canonical.py. Direkt verwenden, nicht neu erheben. -->

game_id : pl.Utf8      # Dateinamen-Konvention YYYY-MM-DD_{TEAM1}-vs-{TEAM2}_{COMP}
play_id : pl.Int32     # entspricht Hudl `PLAY #`

<!-- Handgepflegte Referenz-CSV-Konvention, aus data/reference/*.csv und docs/data-contract.md -->
Komma-Delimiter, UTF-8 **ohne** BOM, reine Header-Zeile + Datenzeilen, keine Kommentar-/Metazeilen.
(Nur Hudl-Rohexporte verwenden `;` + `utf-8-sig` — dieser Dialekt gilt hier NICHT.)

<!-- Bestehender PII-Ignore-Block, aus .gitignore (Zeilen 7-11) -->
data/raw/hudl/*
!data/raw/hudl/.gitkeep

<!-- WICHTIG: data/processed/* ist selbst gitignored. plays.parquet existiert in keinem
     frischen Clone. Tests dürfen die Datei NIE lesen — plays-förmige Fixtures werden
     inline als pl.DataFrame gebaut. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gitignoretes Video-Verzeichnis anlegen (D-08, T-2.0-01)</name>
  <files>.gitignore, data/video/.gitkeep</files>
  <read_first>
    - `.gitignore` (vollständig — den bestehenden `data/raw/hudl/*`-Block und die abschließende „intentionally NOT ignored"-Erläuterung kennen)
  </read_first>
  <action>
    Lege das Verzeichnis `data/video/` mit einer leeren Datei `data/video/.gitkeep` an.

    Ergänze `.gitignore` um einen neuen Block, eingefügt **nach** dem `/reports/`-Block und **vor** dem abschließenden Erläuterungs-Kommentar („data/raw/legacy/, data/raw/sportapp/, ... sind intentionally NOT ignored"). Der Block besteht aus einem Kommentar plus den zwei Pattern-Zeilen `data/video/*` und `!data/video/.gitkeep`, exakt in dieser Reihenfolge.

    Der Kommentar nennt drei Dinge: (a) dass Drohnen-/Seitenkamera-Clips Gesichter von Spielerinnen zeigen (PII, gleiche Policy wie `data/raw/hudl/*`), (b) die Phase-/Requirement-Referenz `Phase 2.0, REQ-S2-01, T-2.0-01`, (c) die Wiederhol-Notiz aus dem `data/raw/hudl/*`-Block, dass Inhalte (`data/video/*`) und nicht das Verzeichnis selbst ignoriert werden, weil Git eine Datei innerhalb eines pauschal ausgeschlossenen Verzeichnisses nicht re-inkludieren kann.

    Ergänze außerdem `data/video/` in der Aufzählung des abschließenden Erläuterungs-Kommentars NICHT — dieser Absatz listet ausdrücklich die *nicht* ignorierten Verzeichnisse und bleibt unverändert.

    Kein Anker-Slash voranstellen: `data/video/*` ist bereits pfad-eindeutig (im Gegensatz zum `/reports/`-Fall, wo `reports/` auch `src/flag_football_ep/reports/` getroffen hätte).
  </action>
  <acceptance_criteria>
    - `.gitignore` enthält die Zeile `data/video/*`
    - `.gitignore` enthält die Zeile `!data/video/.gitkeep`
    - `data/video/.gitkeep` existiert und ist leer (0 Bytes)
    - `git check-ignore -v data/video/probe.mp4` exit code 0
    - `git check-ignore data/video/.gitkeep` exit code 1 (Datei ist NICHT ignoriert)
    - `git status --porcelain data/video/` zeigt `data/video/.gitkeep` als einzigen untracked/added Pfad
  </acceptance_criteria>
  <verify>
    <automated>touch data/video/probe.mp4 && git check-ignore -q data/video/probe.mp4 && ! git check-ignore -q data/video/.gitkeep && rm -f data/video/probe.mp4 && echo GITIGNORE_OK</automated>
  </verify>
  <done>`data/video/probe.mp4` wird von Git ignoriert, `data/video/.gitkeep` nicht.</done>
</task>

<task type="auto">
  <name>Task 2: Die beiden handgepflegten Referenz-CSVs anlegen (D-07, D-11)</name>
  <files>data/reference/video_inventory.csv, data/reference/video_sync.csv</files>
  <read_first>
    - `data/reference/half_boundaries.csv` (kleinster handgepflegter CSV-Präzedenzfall)
    - `data/reference/final_scores.csv` (`game_id`-gekeyter Präzedenzfall mit Freitext-`note`-Spalte)
    - `data/reference/team_mapping.csv` (Dialekt-Referenz)
    - `.planning/phases/02.0-capture-protocol-material-inventory/02.0-PATTERNS.md` §`data/reference/video_inventory.csv` und §`data/reference/video_sync.csv`
  </read_first>
  <action>
    Lege beide Dateien als **Header-only** an (Header-Zeile + abschließender Newline, keine Datenzeilen). Header-only ist der gültige Ausgangszustand: es ist derzeit kein Material gesichert (D-01 — der Analyst ist nicht verfügbar), und die CSV ist per RESEARCH Open Question 2 strikt auf real gesicherte, hashbare Clips gescopet.

    Kodierung: UTF-8 **ohne** BOM, Komma-Delimiter, Unix-Zeilenenden — wie `data/reference/team_mapping.csv`. Kein `;`, kein `utf-8-sig`.

    `data/reference/video_inventory.csv`, Header exakt:
    `domain,session_id,game_id,capture_date,resolution,fps,duration_seconds,local_path,content_sha256,notes`

    Bedeutung der Spalten (wird in Plan 02 in `docs/material-inventory.md` ausformuliert; hier nur als Begründung des Spaltensatzes): `domain` aus dem festen Vokabular `drone` | `sideline` | `broadcast`; `session_id` als Pflicht-Kennung der Trainings-/Spiel-Session im Stil `YYYY-MM-DD_{LABEL}`; `game_id` nur gefüllt, wenn ein Hudl-Spiel existiert, dann exakt der `plays.parquet`-Wert; `capture_date` ISO `YYYY-MM-DD`; `resolution` als `BREITExHÖHE` (z. B. `3840x2160`); `fps` und `duration_seconds` als Fließkommazahlen aus `ffprobe`; `local_path` repo-relativ unter `data/video/`; `content_sha256` als 64 Kleinbuchstaben-Hex aus `shasum -a 256`; `notes` Freitext ohne Personennamen.

    `data/reference/video_sync.csv`, Header exakt:
    `game_id,play_id,video_file,snap_frame,snap_seconds,end_seconds,notes`

    Bedeutung (ausformuliert in Plan 02 in `docs/sync-convention.md`): eine Zeile pro manuell markiertem Play (nie pro Drive, D-11); `game_id` Utf8 und `play_id` Int32 exakt wie in `plays.parquet`; `video_file` verweist auf einen `local_path`-Wert aus `video_inventory.csv`; `snap_frame` (Int64) und/oder `snap_seconds` (Float64) tragen den Anker, mindestens eine der beiden Spalten ist gefüllt; `end_seconds` optional; `notes` Freitext.

    Keine Beispiel-/Platzhalterzeile committen — eine Fake-Zeile mit erfundener `game_id` würde die Zeilen-Garantie „jede Zeile ist verifizierbar" brechen. Beispielzeilen leben in der Doku (Plan 02) und im Test (Task 3, `tmp_path`).
  </action>
  <acceptance_criteria>
    - `head -1 data/reference/video_inventory.csv` == `domain,session_id,game_id,capture_date,resolution,fps,duration_seconds,local_path,content_sha256,notes`
    - `head -1 data/reference/video_sync.csv` == `game_id,play_id,video_file,snap_frame,snap_seconds,end_seconds,notes`
    - `wc -l` beider Dateien == 1
    - Erste Bytes beider Dateien sind NICHT `EF BB BF` (kein BOM)
    - Keine der beiden Dateien enthält ein Semikolon
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import sys
exp={'data/reference/video_inventory.csv':'domain,session_id,game_id,capture_date,resolution,fps,duration_seconds,local_path,content_sha256,notes','data/reference/video_sync.csv':'game_id,play_id,video_file,snap_frame,snap_seconds,end_seconds,notes'}
for p,h in exp.items():
    raw=open(p,'rb').read()
    assert not raw.startswith(b'\xef\xbb\xbf'), p+': BOM'
    lines=raw.decode('utf-8').splitlines()
    assert lines==[h], p+': '+repr(lines)
print('CSV_HEADERS_OK')"</automated>
  </verify>
  <done>Beide CSVs existieren Header-only, BOM-frei, mit exakt den oben genannten Spaltennamen.</done>
</task>

<task type="auto">
  <name>Task 3: Mechanische Gates für CSVs, .gitignore und Git-Hygiene</name>
  <files>tests/test_capture_artifacts.py</files>
  <read_first>
    - `tests/test_repo_hygiene.py` (Zeilen 1-60: Modul-Docstring-Stil, `REPO_ROOT`-Konstante, plain-assert-Idiom, „assert named files exist"-Muster)
    - `tests/test_reference.py` (Zeilen 1-40 und der Dtype-Test `test_load_half_boundaries_dtypes`: `_write_csv`-Helper, explizite Schema-Assertions)
    - `data/reference/roster.csv` (Spalte `player_name` — Quelle der PII-Negativliste)
    - `pyproject.toml` (`[tool.pytest.ini_options]`, `[project] dependencies`)
    - `.gitignore` (Ergebnis von Task 1)
  </read_first>
  <action>
    Neue Datei `tests/test_capture_artifacts.py` mit Modul-Docstring im Stil von `tests/test_repo_hygiene.py`: erklärt, dass diese Gates die handgepflegten Phase-2.0-Artefakte bewachen — Header-Drift, Dtype-Drift gegen `plays.parquet`, PII-Leaks und die Tier-Grenze „keine Pipeline-/CV-Abhängigkeit".

    Modul-Konstanten: `REPO_ROOT = Path(__file__).resolve().parent.parent`, `REFERENCE_DIR = REPO_ROOT / "data" / "reference"`, `INVENTORY_CSV`, `SYNC_CSV`, sowie die beiden erwarteten Header als Tupel `INVENTORY_COLUMNS` und `SYNC_COLUMNS` (Werte aus Task 2), `INVENTORY_SCHEMA` und `SYNC_SCHEMA` als `dict[str, pl.DataType]` im Stil von `_TEAM_MAPPING_SCHEMA` in `src/flag_football_ep/reference.py`:
    `SYNC_SCHEMA` = `game_id: pl.Utf8`, `play_id: pl.Int32`, `video_file: pl.Utf8`, `snap_frame: pl.Int64`, `snap_seconds: pl.Float64`, `end_seconds: pl.Float64`, `notes: pl.Utf8`.
    `INVENTORY_SCHEMA` = `domain: pl.Utf8`, `session_id: pl.Utf8`, `game_id: pl.Utf8`, `capture_date: pl.Utf8`, `resolution: pl.Utf8`, `fps: pl.Float64`, `duration_seconds: pl.Float64`, `local_path: pl.Utf8`, `content_sha256: pl.Utf8`, `notes: pl.Utf8`.
    Ferner `DOMAIN_VOCABULARY = frozenset({"drone", "sideline", "broadcast"})` und `VIDEO_SUFFIXES` mit `.mp4 .mov .mkv .avi .m4v .mts .insv .lrf .braw`.

    **Kein Loader in `src/`.** Alle Reads erfolgen inline mit `pl.read_csv(path, schema_overrides=...)` im Test. Es wird nie `data/processed/plays.parquet` gelesen — die Datei ist gitignored und existiert in keinem frischen Clone; plays-förmige Fixtures werden inline als `pl.DataFrame(...).cast(...)` gebaut.

    Diese Tests, mit exakt diesen Namen:

    1. `test_reference_csv_headers_are_exact` — parametrisiert über beide CSVs: liest die Datei binär, asserted `not raw.startswith(b"\xef\xbb\xbf")` (kein BOM) und dass die erste Zeile exakt dem erwarteten Header-Tupel entspricht, gesplittet auf `,`. Fehlermeldung nennt Ist- und Soll-Header.
    2. `test_reference_csvs_use_comma_dialect_not_hudl_dialect` — asserted, dass keine der beiden Dateien ein `;` enthält (Abgrenzung gegen den Hudl-Export-Dialekt aus `docs/data-contract.md`).
    3. `test_video_inventory_rows_are_verifiable` — liest `INVENTORY_CSV` mit `INVENTORY_SCHEMA`; für jede Zeile: `domain` ∈ `DOMAIN_VOCABULARY`, `session_id` nicht leer, und wenn `local_path` gefüllt ist, matcht `content_sha256` den Regex `^[0-9a-f]{64}$` (D-07/D-08: jede registrierte Datei ist re-lokalisierbar und verifizierbar). Bei Header-only-Datei läuft die Schleife leer durch — das ist gewollt.
    4. `test_video_inventory_paths_stay_repo_relative` — für jede Zeile mit gefülltem `local_path`: der Pfad beginnt nicht mit `/`, nicht mit `~`, enthält kein `/Users/` und kein `/home/` (T-2.0-02: keine lokalen Benutzerpfade in Git).
    5. `test_video_sync_rows_are_anchored` — liest `SYNC_CSV` mit `SYNC_SCHEMA`; asserted `schema["game_id"] == pl.Utf8` und `schema["play_id"] == pl.Int32`; für jede Zeile: `game_id` und `play_id` nicht null, `video_file` nicht leer, und mindestens eine von `snap_frame`/`snap_seconds` nicht null (D-11: ein Anker pro Play).
    6. `test_video_sync_schema_joins_plays_shaped_frame` — schreibt via `_write_csv`-Helper (Form aus `tests/test_reference.py`) eine Sync-CSV nach `tmp_path` mit dem Header aus `SYNC_COLUMNS` und einer Beispielzeile für `game_id` `2026-06-14_GER-vs-AUT_EM-QUALI`, `play_id` `3`, `video_file` `data/video/2026-06-14_GER-vs-AUT_drone_01.mp4`, `snap_frame` `1450`, `snap_seconds` `48.3`; liest sie mit `SYNC_SCHEMA`; baut inline `pl.DataFrame({"game_id": [...], "play_id": [3]}).cast({"game_id": pl.Utf8, "play_id": pl.Int32})` als plays-förmiges Fixture; asserted `sync.join(plays, on=["game_id", "play_id"], how="inner").height == 1`. Das ist das Gate gegen RESEARCH Pitfall 1 (stiller 0-Zeilen-Join bei Dtype-Mismatch).
    7. `test_gitignore_covers_video_directory` — `subprocess.run(["git", "check-ignore", "-q", "data/video/probe.mp4"], cwd=REPO_ROOT)` gibt returncode 0; derselbe Aufruf für `data/video/.gitkeep` gibt returncode 1. Kein Anlegen echter Dateien nötig — `git check-ignore` arbeitet rein auf Pfadnamen.
    8. `test_no_video_binaries_tracked_by_git` — `git ls-files` in `REPO_ROOT`, asserted, dass kein getrackter Pfad (case-insensitiv) auf einen Eintrag aus `VIDEO_SUFFIXES` endet (T-2.0-01).
    9. `test_capture_artifacts_contain_no_roster_names` — liest `data/reference/roster.csv` mit polars, sammelt die eindeutigen `player_name`-Werte, und asserted für beide Phase-2.0-CSVs, dass kein vollständiger Name (case-insensitiv) im Dateitext vorkommt (T-2.0-02). Nur vollständige `player_name`-Werte prüfen, keine einzelnen Tokens — sonst produzieren gängige Vornamen Falschtreffer.
    10. `test_phase_adds_no_video_python_dependency` — liest `pyproject.toml` als Text und asserted, dass keiner der Tokens `opencv`, `decord`, `ffmpeg-python`, `imageio-ffmpeg`, `moviepy` vorkommt (Architektur-Gate: Phase 2.0 ist Doku + Referenzdaten, kein Pipeline-/CV-Code; RESEARCH Anti-Pattern „Python-Ingestion-Skript für die CSVs").

    Style: `from __future__ import annotations`, Typannotationen an allen Testfunktionen, `pytest.mark.parametrize` für Test 1 und 2, plain `assert` mit sprechender Fehlermeldung als zweitem Argument — wie in `tests/test_repo_hygiene.py`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_capture_artifacts.py -q` exit code 0
    - `uv run pytest tests/test_capture_artifacts.py --collect-only -q` listet alle zehn oben genannten Testnamen
    - `grep -c "data/processed/plays.parquet" tests/test_capture_artifacts.py` == 0 (Tests lesen die gitignorete Parquet nie)
    - `grep -c "from flag_football_ep" tests/test_capture_artifacts.py` == 0 (kein `src/`-Loader wird eingeführt oder importiert)
    - `uv run pytest -q` exit code 0 (volle Suite bleibt grün)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_capture_artifacts.py -q</automated>
  </verify>
  <done>Alle zehn Gates laufen grün gegen die in Task 1 und 2 erzeugten Artefakte; die volle Suite bleibt grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokales Dateisystem → Git-Repo | Videoclips mit Gesichtern von Spielerinnen und lokale Benutzerpfade können beim `git add` die Repo-Grenze überschreiten |
| Handgepflegte CSV → Pipeline-Join | Hand-editierte Zeilen sind unvalidierte Eingaben für den späteren Join gegen `plays.parquet` (Phase 2.3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.0-01 | Information Disclosure | `data/video/*` (Clips zeigen Gesichter von Spielerinnen) | mitigate | `.gitignore`-Block `data/video/*` + `!data/video/.gitkeep` gespiegelt von `data/raw/hudl/*`; Gates `test_gitignore_covers_video_directory` und `test_no_video_binaries_tracked_by_git` |
| T-2.0-02 | Information Disclosure | `data/reference/video_inventory.csv` Spalten `local_path`, `notes` | mitigate | Repo-relative Pfade unter `data/video/` als dokumentierte Norm; `test_video_inventory_paths_stay_repo_relative` verbietet `/Users/`, `/home/`, `~`, absolute Pfade; `test_capture_artifacts_contain_no_roster_names` verbietet Klarnamen aus `roster.csv` |
| T-2.0-05 | Tampering | Stiller Dtype-Drift in `video_sync.csv` führt zu 0-Zeilen-Join statt Fehler → Tracking-Daten werden lautlos falschen Plays zugeordnet | mitigate | `SYNC_SCHEMA` mit explizitem `schema_overrides`; `test_video_sync_schema_joins_plays_shaped_frame` erzwingt einen nicht-leeren Join gegen ein plays-förmiges Fixture |
| T-2.0-SC | Tampering | npm/pip/uv-Installationen | accept | Diese Phase installiert kein einziges Paket (RESEARCH §Package Legitimacy Audit: „Not applicable"); `pyproject.toml` bleibt unverändert, `test_phase_adds_no_video_python_dependency` bewacht das |
</threat_model>

<verification>
- `uv run pytest tests/test_capture_artifacts.py -q` grün
- `uv run pytest -q` grün (volle Suite)
- `git check-ignore -q data/video/probe.mp4` exit 0, `git check-ignore -q data/video/.gitkeep` exit 1
- `git status --porcelain` zeigt außer den fünf geplanten Pfaden nichts Unerwartetes
</verification>

<success_criteria>
1. `.gitignore` ignoriert `data/video/*` und re-inkludiert `data/video/.gitkeep` (mechanisch geprüft, nicht per Augenschein)
2. Beide Referenz-CSVs existieren Header-only, BOM-frei, Komma-delimitiert, mit exakt den in Task 2 genannten Spaltennamen
3. `tests/test_capture_artifacts.py` enthält alle zehn benannten Gates und läuft grün
4. Weder `src/flag_football_ep` noch `pyproject.toml` wurden angefasst
</success_criteria>

<output>
Create `.planning/phases/02.0-capture-protocol-material-inventory/02.0-01-SUMMARY.md` when done
</output>
