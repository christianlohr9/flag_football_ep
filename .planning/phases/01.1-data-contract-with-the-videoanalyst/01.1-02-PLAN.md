---
phase: 01.1-data-contract-with-the-videoanalyst
plan: 02
type: execute
wave: 2
depends_on: [01.1-01]
files_modified:
  - docs/data-contract.md
  - docs/data-contract.schema.json
  - .gitignore
autonomous: false
requirements: [REQ-S1-03]

must_haves:
  truths:
    - "The COVERAGE / DEF FRONT / BLITZ canonical vocabulary is derived from real sighted export values — not invented before seeing data"
    - "No PENDING-SIGHTING marker remains in docs/data-contract.md or docs/data-contract.schema.json"
    - "Sample exports containing player names are NOT committed to the repo"
    - "Observed-variant → canonical-value normalization mapping is documented in the spec"
  artifacts:
    - path: "docs/data-contract.schema.json"
      provides: "COVERAGE, DEF FRONT, BLITZ columns with concrete values arrays from sighted data"
      contains: "COVERAGE"
    - path: "docs/data-contract.md"
      provides: "Defense-field section with canonical vocabulary, variant mapping, and remaining gaps flagged for the analyst conversation"
      contains: "COVERAGE"
    - path: ".gitignore"
      provides: "data/samples/ excluded from version control (PII in sample exports)"
      contains: "data/samples/"
  key_links:
    - from: "docs/data-contract.schema.json"
      to: "docs/data-contract.md"
      via: "every canonical defense value in the schema appears in the spec's defense section"
      pattern: "COVERAGE"
---

<objective>
Sight real distinct values of the defense columns (`DEF FRONT`, `COVERAGE`, `BLITZ`) from actual Hudl exports the user provides, derive the canonical vocabulary from that data, and fill the PENDING-SIGHTING slots in both contract artifacts.

Purpose: REQ-S1-03 — the locked decision is data-first: no premature Cover-0/1/2/3-vs-man/zone schema before seeing what the analyst actually charts. The repo contains no file with these columns (verified in RESEARCH.md Pitfall 6), so a human-provided sample is the hard prerequisite.

Output: updated `docs/data-contract.md` + `docs/data-contract.schema.json` (PENDING-SIGHTING cleared), `.gitignore` entry for `data/samples/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-CONTEXT.md
@.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-RESEARCH.md
@docs/data-contract.md
@docs/data-contract.schema.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide real Hudl sample exports with defense columns</name>
  <read_first>
    - docs/data-contract.md (defense-field section — what the sighting will fill)
    - .planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-RESEARCH.md (§Pitfall 6, §Open Question 3 — why repo data cannot answer this)
  </read_first>
  <action>
    HUMAN ACTION REQUIRED (no CLI/API exists for Hudl — Claude cannot export). The repo contains no file with `DEF FRONT`, `COVERAGE`, or `BLITZ` values; sighting them needs your Hudl account.

    Option A (preferred): Export 2–3 games with the real 19-column preset, spanning the season and ideally mixing one own game and one exchanged opponent film. Save them as CSV into `data/samples/` (any filenames). These files contain player names and will be gitignored, not committed.
    Option B (fallback): Paste the distinct values (with rough counts if visible) of the `DEF FRONT`, `COVERAGE`, and `BLITZ` columns directly into the chat.
    Note: if the exports arrive as .xlsx, either save-as CSV from Excel (German locale is fine — the `;`+BOM dialect is expected) or say so and paste values instead.
  </action>
  <verify>
    <automated>ls data/samples/*.csv 2>/dev/null || echo "no sample files — expect pasted values in chat (Option B)"</automated>
  </verify>
  <acceptance_criteria>
    - Either ≥ 1 CSV file exists under `data/samples/` containing at least the `DEF FRONT`, `COVERAGE`, `BLITZ` columns, or the distinct values for all three columns were provided in chat
  </acceptance_criteria>
  <done>Real defense-column values (files or pasted list) are available to Task 2.</done>
  <resume-signal>Type "provided" plus where (data/samples/ or pasted values above)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Audit sighted values, derive canonical vocabulary, fill PENDING-SIGHTING slots</name>
  <files>docs/data-contract.md, docs/data-contract.schema.json, .gitignore</files>
  <read_first>
    - data/samples/*.csv (if Option A — read headers first; files use `;` delimiter + utf-8-sig encoding)
    - docs/data-contract.schema.json (current defense-column entries to fill)
    - docs/data-contract.md (defense section and agenda item (c) to fill)
  </read_first>
  <action>
    1. Add `data/samples/` to `.gitignore` (create the file if it does not exist; append the line otherwise). This is the PII mitigation (T-1.1-03): sample exports carry player names and never enter version control.
    2. If samples were provided as files: audit them with stdlib only (`csv` module, `delimiter=';'`, `encoding='utf-8-sig'`) — collect distinct value → count for each of `DEF FRONT`, `COVERAGE`, `BLITZ`, including the empty-string count and total row count per file. Use a throwaway `python3 -c` invocation or an uncommitted scratch script; do not add pipeline code (Phase 1.2 scope) and do not install anything. If values were pasted in chat, use those directly.
    3. Derive the canonical vocabulary per column from the observed values: normalize casing/spelling variants to one canonical form each (e.g. unify `man`/`Man`), keep the analyst's actual terminology — do NOT map onto a textbook Cover-0/1/2/3 scheme unless the observed values already use it. For `BLITZ`, if values are binary-ish (e.g. yes/empty), canonicalize to an explicit two-value vocabulary.
    4. Update `docs/data-contract.schema.json`: replace the `["PENDING-SIGHTING"]` values arrays of `DEF FRONT`, `COVERAGE`, `BLITZ` with the canonical value lists. Keep `required: false` — fill rate is a charting-consistency topic for the analyst, not a hard validation gate yet.
    5. Update `docs/data-contract.md` defense section: canonical vocabulary per column, a variant → canonical mapping table for every observed non-canonical spelling, observed fill rates (n filled / n total), and — if fill rates are low or values are inconsistent — a concrete bullet list of gaps to raise in agenda item (c) of the analyst conversation (Plan 03). Remove every `PENDING-SIGHTING` marker; agenda item (c) keeps its PENDING-ANALYST outcome slot (consistency going forward is confirmed in Plan 03).
    6. Do not commit anything under `data/samples/`.
  </action>
  <verify>
    <automated>python3 -c "import json,pathlib; s=json.load(open('docs/data-contract.schema.json')); t=pathlib.Path('docs/data-contract.md').read_text(encoding='utf-8'); assert 'PENDING-SIGHTING' not in json.dumps(s), 'schema still has PENDING-SIGHTING'; assert 'PENDING-SIGHTING' not in t, 'spec still has PENDING-SIGHTING'; dcols={c['name']: c for c in s['columns'] if c['name'] in ('DEF FRONT','COVERAGE','BLITZ')}; assert len(dcols)==3; bad=[n for n,c in dcols.items() if not c.get('values')]; assert not bad, ('empty values for', bad); miss=[v for c in dcols.values() for v in c['values'] if v not in t]; assert not miss, ('schema values missing in spec', miss); gi=pathlib.Path('.gitignore').read_text(); assert 'data/samples/' in gi, '.gitignore missing data/samples/'; import subprocess; out=subprocess.run(['git','ls-files','data/samples'],capture_output=True,text=True).stdout.strip(); assert out=='', 'sample exports are tracked by git!'; print('defense vocab OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `PENDING-SIGHTING` appears in neither `docs/data-contract.md` nor `docs/data-contract.schema.json`
    - `DEF FRONT`, `COVERAGE`, `BLITZ` each have a non-empty `values` array in the schema, and every value appears verbatim in the spec
    - Spec documents observed fill rates and a variant → canonical mapping for the sighted data
    - `.gitignore` contains `data/samples/` and `git ls-files data/samples` returns nothing
  </acceptance_criteria>
  <done>Defense vocabulary is fixed from real data in both artifacts; remaining defense questions are queued as concrete agenda bullets for the analyst conversation; no PII entered the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's Hudl exports → repo working tree | Sample files contain player names (PII) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.1-03 | Information Disclosure | Sample exports in `data/samples/` (player names) | mitigate | `.gitignore data/samples/` added BEFORE any audit; verify step asserts `git ls-files data/samples` is empty; only aggregated distinct values (team-tactic terms, no player names) enter the committed artifacts |
| T-1.1-04 | Tampering (vocabulary drift) | Canonical defense values in schema vs spec | mitigate | Automated parity check: every schema value must appear in the spec; deviations fail the verify command |
</threat_model>

<verification>
- Verify command of Task 2 green (markers cleared, values parity, gitignore, no tracked samples)
- Plan 01 checks still green (schema parses, columns/tokens intact — this plan must not regress them)
</verification>

<success_criteria>
- Success criterion 4 (defense-field decision) is data-backed: adopted vocabulary derived from sighted exports, flag-pull skip already recorded in Plan 01
- The analyst conversation (Plan 03) receives concrete gap bullets instead of an open-ended vocabulary discussion
</success_criteria>

<output>
Create `.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-02-SUMMARY.md` when done.
</output>
