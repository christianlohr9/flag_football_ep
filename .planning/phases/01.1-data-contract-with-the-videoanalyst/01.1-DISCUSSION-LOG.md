# Phase 1.1: Data Contract with the Videoanalyst - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-02
**Phase:** 1.1-Data Contract with the Videoanalyst
**Areas discussed:** Manual fields: chart vs derive, Contract artifact & export scope, Time data: format & fallback, Defense fields: which & vocabulary

---

## Manual fields: chart vs derive

| Option | Description | Selected |
|--------|-------------|----------|
| Pipeline derives max | Analyst charts only what only they know; pipeline derives game_id, play_id, yardline_50, posteam | ✓ |
| Analyst charts all six | Six fields become explicit preset columns filled during charting | |
| Keep current split | User keeps appending the six columns manually after export | |

**User's choice:** Pipeline derives max

| Option | Description | Selected |
|--------|-------------|----------|
| One file per game, naming convention | Filename parsed into game_id + metadata; matches 1.2 ingest-CLI folder design | ✓ |
| Single combined export + game column | One yearly file with a game/opponent column per clip | |
| Sidecar manifest file | Free filenames + manifest mapping filename → game metadata | |

**User's choice:** One file per game, naming convention

| Option | Description | Selected |
|--------|-------------|----------|
| Derive drives from ODK, chart half only | drive_id from possession flips; analyst charts a single half marker | ✓ |
| Analyst charts both | Drive number and half as preset columns | |
| Derive both, validate hard | Infer half too; rely on 1.2 validation | |

**User's choice:** Derive drives from ODK, chart half only
**Notes:** Later revised by the export-format correction — half now comes from the user's manually maintained halftime boundary per game (see Time data), since QTR is unfilled and the analyst charting ask was dropped.

| Option | Description | Selected |
|--------|-------------|----------|
| Filename carries both teams + perspective | Naming convention date_home_away; contract defines ODK perspective | |
| Chart team abbreviation per play | 'Team' column with offense abbreviation each play | |
| GER-perspective only for now | Define ODK→posteam only for GER games | |

**User's choice:** Free text: "cant exactly tell right now"
**Notes:** posteam derivation for exchanged opponent-vs-opponent film stays OPEN; to be resolved in the analyst conversation, with the filename+perspective convention as the proposal to bring.

---

## Contract artifact & export scope

| Option | Description | Selected |
|--------|-------------|----------|
| Markdown spec + machine-readable schema | docs/data-contract.md + schema file consumed by 1.2 validator | ✓ |
| Markdown spec only | Single doc; 1.2 re-encodes as validation code by hand | |
| Schema file only | Machine-readable single source of truth | |

**User's choice:** Markdown spec + machine-readable schema

| Option | Description | Selected |
|--------|-------------|----------|
| German spec, English schema | Spec in German for the analyst; schema/columns stay technical English | ✓ |
| All English | Consistent with codebase | |
| Bilingual spec | German + English side by side | |

**User's choice:** German spec, English schema

| Option | Description | Selected |
|--------|-------------|----------|
| Re-export history under new preset | Bulk re-export of past games; one code path | |
| Forward-only, grandfather data_raw.csv | New preset from now on; 1.2 maintains legacy ingest path | ✓ |
| Ask analyst, decide by effort | Defer the call to the conversation | |

**User's choice:** Forward-only, grandfather data_raw.csv

| Option | Description | Selected |
|--------|-------------|----------|
| Preset mirrored in repo + sample export | Analyst owns preset; repo stores definition + golden sample | |
| Contract doc is the only record | No golden sample file | |
| You own the preset | User configures and maintains the export preset in Hudl | ✓ |

**User's choice:** You own the preset

---

## Time data: format & fallback

| Option | Description | Selected |
|--------|-------------|----------|
| Clip timestamps, pipeline converts | Hudl clip timestamps mapped to game clock via per-half anchor | |
| Game clock charted per play | Scoreboard clock read into a preset column | |
| Drive-level times only | Start/end per drive, plays interpolated | |

**User's choice:** Free text (German): user has Hudl access and exports everything themselves; the real export format is `PLAY #, ODK, DN, DIST, HASH, YARD LN, PLAY TYPE, RESULT, GN/LS, OFF FORM, OFF PLAY, OFF STR, PLAY DIR, GAP, PASS ZONE, DEF FRONT, COVERAGE, BLITZ, QTR` and is the given baseline.
**Notes:** Follow-up (plain text): no time information beyond QTR exists, and QTR is essentially never filled.

| Option | Description | Selected |
|--------|-------------|----------|
| Not available + QTR request to charting | Document unavailability; ask analyst to tag QTR/half boundary going forward | |
| Document only, request nothing | Record unavailability, no charting change | |
| Manually maintain per game | User adds time anchors themselves per game | ✓ |

**User's choice:** Manually maintain per game

| Option | Description | Selected |
|--------|-------------|----------|
| Only halftime boundary | One value per game (PLAY # where half 2 starts); time stays synthetic per half | ✓ |
| Halftime + drive times | Also rough start/end per drive, plays interpolated | |
| Halftime + play times for key games | Real play timestamps only for selected games | |

**User's choice:** Only halftime boundary

---

## Defense fields: which & vocabulary

| Option | Description | Selected |
|--------|-------------|----------|
| Empty today — request filling with fixed vocab | Ask analyst to tag COVERAGE/BLITZ with a small fixed vocabulary | |
| Partially filled — document actual state | Document and normalize the really-used vocabulary; ask only about gaps | ✓ |
| Deliberately skip | Record REQ-S1-03 as explicitly skipped | |

**User's choice:** Partially filled — document actual state

| Option | Description | Selected |
|--------|-------------|----------|
| Sight actual vocabulary first | Pull distinct values from existing exports, then fix canonical vocabulary | ✓ |
| Shell scheme Cover 0/1/2/3 | Classic shell vocabulary as target schema | |
| Man/Zone only | Coarsest sensible level at 5v5 | |

**User's choice:** Sight actual vocabulary first

| Option | Description | Selected |
|--------|-------------|----------|
| Request in the conversation | Propose flag-pull causer as new charting column | |
| Explicitly skip | Record as deliberate skip in the charting protocol | ✓ |
| Tag it yourself | User adds causer from video | |

**User's choice:** Explicitly skip

---

## Claude's Discretion

- Exact filename convention format (order/separator of date, teams, competition tag)
- File format and location of the per-game halftime-boundary record
- Schema file format (YAML vs JSON) and structure, as long as the 1.2 validator can consume it
- How derivation rules are documented in the markdown spec

## Deferred Ideas

- Flag-pull causer charting field — deliberately skipped; revisit if player-level defense scouting becomes a priority
- `PASS ZONE` / `GAP` / `PLAY DIR` semantics in the canonical schema — sight these columns like COVERAGE during 1.2 schema work
