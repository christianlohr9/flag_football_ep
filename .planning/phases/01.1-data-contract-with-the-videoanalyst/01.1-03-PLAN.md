---
phase: 01.1-data-contract-with-the-videoanalyst
plan: 03
type: execute
wave: 3
depends_on: [01.1-01, 01.1-02]
files_modified:
  - docs/data-contract.md
  - docs/data-contract.schema.json
autonomous: false
requirements: [REQ-S1-01, REQ-S1-03]

must_haves:
  truths:
    - "The three analyst agenda items (ODK perspective / posteam, RESULT charting incl. Fumble semantics and mandatory non-empty, defense-column consistency) have recorded outcomes in the contract"
    - "No PENDING-ANALYST marker remains in either artifact; derived_fields.posteam.rule is a concrete rule, or carries the literal DEFERRED-ANALYST marker (owner + follow-up trigger) in BOTH artifacts — in which case the SUMMARY records phase success criterion 2 as PARTIAL, never as met"
    - "The contract status is agreed (version 1.0), satisfying success criterion 1: preset exists and is agreed with the Videoanalyst"
    - "Charting-protocol decisions (mandatory RESULT, defense-field adoption, flag-pull skip) are recorded — success criterion 4 fully met"
  artifacts:
    - path: "docs/data-contract.md"
      provides: "Agenda outcomes recorded, posteam rule filled, contract marked vereinbart with date"
      contains: "vereinbart"
    - path: "docs/data-contract.schema.json"
      provides: "posteam rule + filename_semantics concrete; contract_version 1.0, status agreed"
      contains: "contract_version"
  key_links:
    - from: "docs/data-contract.schema.json"
      to: "docs/data-contract.md"
      via: "posteam rule and filename TEAM1 semantics identical in both artifacts"
      pattern: "posteam"
---

<objective>
Hold the (single, minimal) Videoanalyst conversation on the three charting items, record the outcomes, fill every remaining PENDING-ANALYST slot, and finalize the contract as agreed v1.0.

Purpose: closes REQ-S1-01 (contract agreed with the analyst) and REQ-S1-03 (defense-field decisions confirmed). This is deliberately the LAST plan: everything else was drafted first so the conversation only confirms/fills marked slots (RESEARCH.md Pitfall 5 — do not stall the phase on an external person).

Output: finalized `docs/data-contract.md` + `docs/data-contract.schema.json` with zero unresolved PENDING markers (a legitimate deferral, if any, carries the machine-checkable `DEFERRED-ANALYST` marker and downgrades success criterion 2 to PARTIAL in the SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-CONTEXT.md
@docs/data-contract.md
@docs/data-contract.schema.json

<interfaces>
From Python/helper_add_hudl_mutations.py (lines 4–12, `get_games`) — the caveat to surface in the conversation, not a rule to lock:
- in-code comment: the team with the first drive is assumed to be "home_team" (only relevant for scores/differential)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Hold the analyst conversation and report the outcomes</name>
  <read_first>
    - docs/data-contract.md (§Charting-Protokoll & Gesprächsagenda — the three items with their PENDING-ANALYST slots, plus the defense gap bullets from Plan 02)
  </read_first>
  <action>
    HUMAN ACTION REQUIRED (external person — no automation possible). Hold the conversation with the Videoanalyst. Per the locked scope it covers ONLY charting — never export mechanics. The three items (agenda already drafted in the spec):
    1. ODK-Perspektive bei getauschtem Gegner-Film: whose perspective is ODK charted from? Proposal on the table: filename carries both teams, TEAM1 = charting-perspective team. Outcome defines the `posteam` derivation rule.
    2. RESULT-Charting going forward: RESULT mandatory (non-empty) on every play; Fumble semantics (does a charted Fumble imply possession change / how is it tagged); confirmation of the 13-token vocabulary incl. `No Good`.
    3. Defense-Spalten: the concrete gap/consistency bullets from the Plan-02 sighting; confirmation of the sighted canonical vocabulary for `DEF FRONT`, `COVERAGE`, `BLITZ` going forward; flag-pull causer stays skipped.

    Afterwards, report the outcome per item in chat (short free-form is fine — e.g. "1: TEAM1-Vorschlag angenommen ...; 2: verpflichtend ok, Fumble = ...; 3: Vokabular bestätigt"). If an item could not be resolved, say so explicitly — it will be recorded with the literal `DEFERRED-ANALYST` marker plus owner and follow-up trigger, not silently dropped, and the phase SUMMARY will report the affected success criterion as PARTIAL.
  </action>
  <verify>
    <automated>N/A — human-action checkpoint; downstream gate: Task 2 finalization check (no PENDING-ANALYST markers remain)</automated>
  </verify>
  <acceptance_criteria>
    - An outcome (resolution or explicit deferral) is reported in chat for each of the three agenda items
  </acceptance_criteria>
  <done>All three agenda outcomes are reported and available to Task 2.</done>
  <resume-signal>Report the three outcomes, then type "continue"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record outcomes, fill PENDING-ANALYST slots, finalize contract v1.0</name>
  <files>docs/data-contract.md, docs/data-contract.schema.json</files>
  <read_first>
    - docs/data-contract.md (all PENDING-ANALYST slots: filename TEAM1 semantics §3, Fumble semantics §4, posteam row in derivation table §5, agenda outcome slots §8)
    - docs/data-contract.schema.json (slots: file_format.filename_semantics, RESULT note, derived_fields.posteam.rule, status, contract_version)
  </read_first>
  <action>
    Using the reported outcomes verbatim as source of truth:
    1. Spec §8 (Gesprächsagenda): write the recorded outcome under each of the three items, with conversation date. Unresolved items (if any) become an explicit record carrying the literal marker `DEFERRED-ANALYST` plus owner and follow-up trigger (e.g. "DEFERRED-ANALYST: Owner Nutzer, Follow-up vor dem nächsten Filmtausch — Vorschlag TEAM1 = Perspektiv-Team bleibt auf dem Tisch") instead of a PENDING marker — machine-distinguishable from both open (PENDING) and resolved states; never free-form prose like "vertagt" without the marker.
    2. Spec §3 + §5 and schema `derived_fields.posteam.rule` + `file_format.filename_semantics`: replace PENDING-ANALYST with the concrete perspective rule as reported (e.g. "TEAM1 = charting-perspective team; posteam = TEAM1 when ODK == 'O', else TEAM2"). The rule text must be identical in substance in spec and schema. If item 1 could not be resolved, write the posteam rule in BOTH artifacts as a string starting with `DEFERRED-ANALYST:` (incl. owner and follow-up trigger) — the finalization gate distinguishes a deferral only via this literal marker. In that case phase success criterion 2 (deterministic derivation documented for all six fields) is NOT met: the SUMMARY must record criterion 2 as PARTIAL with the follow-up plan.
    3. Spec §4 and schema RESULT `note`: record the agreed Fumble semantics and the confirmation that RESULT is mandatory going forward; keep the C-07 amendment record.
    4. Spec §7: record the analyst's confirmation of the defense vocabulary / any agreed charting changes going forward.
    5. Finalize: schema `contract_version` = "1.0", `status` = "agreed" (with date); spec header gets a status line "Status: vereinbart am YYYY-MM-DD (Gespräch mit Videoanalyst)". Remove every remaining PENDING-ANALYST marker from both files.
    6. Re-run the full Plan-01 structural checks — finalization must not regress columns, tokens, or derived_fields.
  </action>
  <verify>
    <automated>python3 -c "import json,pathlib; s=json.load(open('docs/data-contract.schema.json')); t=pathlib.Path('docs/data-contract.md').read_text(encoding='utf-8'); assert 'PENDING-ANALYST' not in json.dumps(s), 'schema still has PENDING-ANALYST'; assert 'PENDING-ANALYST' not in t, 'spec still has PENDING-ANALYST'; assert 'PENDING-SIGHTING' not in t and 'PENDING-SIGHTING' not in json.dumps(s), 'sighting marker regressed'; assert s.get('contract_version')=='1.0', s.get('contract_version'); p=json.dumps(s['derived_fields']['posteam']); assert 'PENDING' not in p, 'posteam rule not concrete'; deferred='DEFERRED-ANALYST' in p; assert (not deferred) or ('DEFERRED-ANALYST' in t), 'posteam deferred in schema but deferral not recorded in spec'; assert 'vereinbart' in t, 'spec missing agreed status'; assert {'file_format','columns','derived_fields'} <= set(s); names={c['name'] for c in s['columns']}; assert len(names)>=19; r=[c for c in s['columns'] if c['name']=='RESULT'][0]; assert {'Rush','Penalty','KNEEL','Sack','Interception','Complete','Incomplete','TD','Def TD','Good','No Good','Safety','Fumble'} <= set(r['base_tokens'])|set(r['modifier_tokens']); assert {'play_id','yardline_50','drive_id','half','game_id','posteam'} <= set(s['derived_fields']); print('contract v1.0 OK — POSTEAM DEFERRED: phase success criterion 2 PARTIAL, must be recorded in SUMMARY' if deferred else 'contract v1.0 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Neither artifact contains `PENDING-ANALYST` or `PENDING-SIGHTING`
    - `contract_version` == "1.0" and spec contains the literal word `vereinbart` with a date
    - `derived_fields.posteam.rule` is a concrete rule string without "PENDING"; a legitimate deferral instead starts with the literal marker `DEFERRED-ANALYST` (owner + follow-up trigger) and appears in BOTH artifacts
    - If `DEFERRED-ANALYST` is present, the verify command prints the criterion-2-PARTIAL warning and the SUMMARY records phase success criterion 2 as PARTIAL (posteam deferred) — it is never reported as fully met
    - All Plan-01 structural invariants still hold (19+ columns, 13-token RESULT set, 6 derived_fields)
    - Each of the three agenda items in §8 has a written outcome (or an explicit `DEFERRED-ANALYST` record)
  </acceptance_criteria>
  <done>The contract is agreed v1.0; all four phase success criteria are satisfied — or, on a posteam deferral, criterion 2 is explicitly recorded as PARTIAL via the DEFERRED-ANALYST marker in both artifacts and in the SUMMARY — and machine-checkable where structural.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verbal analyst agreement → written contract | Agreement exists only in conversation until recorded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.1-05 | Repudiation | Agreed charting rules (mandatory RESULT, perspective rule, defense vocabulary) | mitigate | Outcomes written verbatim into the versioned, dated contract (git history = audit trail); unresolved items recorded as explicit machine-checkable DEFERRED-ANALYST records instead of disappearing |
</threat_model>

<verification>
- Task 2 verify command green (markers cleared, v1.0, posteam concrete or explicitly DEFERRED-ANALYST with the PARTIAL warning printed, structural invariants intact)
- Full phase check set green: Plan-01 schema/spec/boundary checks + Plan-02 defense parity check + this plan's finalization check
</verification>

<success_criteria>
- Phase success criterion 1: preset + vocabulary agreed with the analyst (recorded, dated)
- Criterion 2: six manual fields documented incl. the now-concrete posteam rule; on deferral, the explicit DEFERRED-ANALYST record stands in both artifacts and criterion 2 is reported PARTIAL in the SUMMARY — never silently claimed met
- Criterion 3: time-data unavailability recorded (Plan 01, unchanged)
- Criterion 4: defense-field decisions recorded — adopted vocabulary + flag-pull skip
</success_criteria>

<output>
Create `.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-03-SUMMARY.md` when done.
</output>
