---
phase: 01.1-data-contract-with-the-videoanalyst
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/data-contract.md
  - docs/data-contract.schema.json
  - data/half_boundaries.csv
autonomous: true
requirements: [REQ-S1-01, REQ-S1-02]

must_haves:
  truths:
    - "docs/data-contract.md exists (German) and documents all 19 real-export columns, the RESULT token grammar, and a deterministic rule for each of the six manual fields"
    - "docs/data-contract.schema.json parses with stdlib json and contains file_format, columns (all 19), and derived_fields (all 6)"
    - "Time-data unavailability is explicitly recorded in the spec, and data/half_boundaries.csv exists as the defined half input"
    - "Unresolved items carry literal markers PENDING-SIGHTING (defense vocabulary) and PENDING-ANALYST (posteam/perspective, Fumble semantics) — nothing is silently locked"
  artifacts:
    - path: "docs/data-contract.schema.json"
      provides: "Machine-readable contract: file_format (;/utf-8-sig/filename pattern), 19 columns with dtype/required/vocabulary, RESULT grammar, 6 derived_fields"
      contains: "derived_fields"
    - path: "docs/data-contract.md"
      provides: "German spec: baseline export, filename convention, RESULT vocabulary incl. C-07 amendment, per-field derivation table, time-data record, defense-field section, charting agenda, Nicht-Ziele, DSGVO note"
      min_lines: 80
    - path: "data/half_boundaries.csv"
      provides: "User-maintained halftime-boundary record (header row, plain comma CSV)"
      contains: "half2_first_play"
  key_links:
    - from: "docs/data-contract.md"
      to: "docs/data-contract.schema.json"
      via: "column/token parity — every schema column name and RESULT token appears in the spec"
      pattern: "data-contract.schema.json"
    - from: "docs/data-contract.md"
      to: "data/half_boundaries.csv"
      via: "half derivation rule references the boundary file"
      pattern: "half_boundaries.csv"
---

<objective>
Draft the complete data contract as two artifacts (German markdown spec + machine-readable JSON schema) plus the halftime-boundary record, with every externally-dependent item marked as an explicit PENDING slot so the analyst conversation (Plan 03) and the defense-column sighting (Plan 02) only fill marked gaps.

Purpose: REQ-S1-01 (contract exists, identical columns, fixed RESULT vocabulary) and REQ-S1-02 (time-data path settled: unavailability recorded, half boundary as the only manual time input). Drafting first prevents the phase from stalling on the external Videoanalyst (STATE.md blocker).

Output: `docs/data-contract.md`, `docs/data-contract.schema.json`, `data/half_boundaries.csv`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-CONTEXT.md
@.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-RESEARCH.md
@.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-PATTERNS.md

<interfaces>
<!-- The de-facto vocabulary contract this plan formalizes. Source: Python/helper_add_hudl_mutations.py (verified in PATTERNS.md). -->

From Python/helper_add_hudl_mutations.py (lines 49–93, play-type parsing — substring semantics to replace with exact-token semantics):
- `contains("Rush")` → run; `contains("Penalty")` → no_play; `contains("KNEEL")` → qb_kneel; `down == 0` → extra_point; otherwise → pass (empty RESULT lands here)
- TD = `contains("TD") & not contains("Def")`; Def TD = `contains("Def TD")`
- Case-sensitivity is load-bearing: `Incomplete` must NOT match `contains("Complete")`

From Python/helper_add_hudl_mutations.py (lines 95–107, PAT):
- PAT play: `RESULT == "Good"` (exact match) & `down == 0` & `yardline_50 == 45` (1pt) / `== 40` (2pt)

From Python/helper_add_hudl_mutations.py (lines 28–33, manual-field dtypes):
- `yardline_50`, `game_id`, `play_id`, `drive_id`, `half` are Int32; `posteam` is str

From Python/helper_add_hudl_mutations.py (lines 384–407, `prepare_wp_data` — synthetic time; NOTE: RESEARCH.md wrongly cites helper_add_ep_wp.py, PATTERNS.md corrects the location):
- `play_time = 1200 / max(play_id_half)` per (game_id, half); `half_seconds_remaining = 1200 - cumsum(play_time)`

From Python/helper_add_ep_wp.py (lines 57–78): hardcoded PAT baselines 50% (1pt) / 46% (2pt) — reference target for the spec's PAT section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/data-contract.schema.json (machine-readable contract)</name>
  <files>docs/data-contract.schema.json</files>
  <read_first>
    - .planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-RESEARCH.md (§Pattern 1 schema sketch, §Pitfall 3 vocabulary audit, §Pitfall 4 CSV dialect)
    - .planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-PATTERNS.md (§data-contract.schema.json load-bearing requirements, §Shared Patterns)
    - Python/helper_add_hudl_mutations.py (lines 28–110 — dtypes and parsing the schema must encode)
  </read_first>
  <action>
    Create `docs/data-contract.schema.json` (English, JSON — locked format choice per RESEARCH.md: uv env broken, PyYAML unavailable, stdlib json works). Top-level keys MUST be exactly `contract_version`, `status`, `file_format`, `columns`, `derived_fields` (the structural checks assert `{'file_format','columns','derived_fields'}` as subset).

    `contract_version`: "1.0-draft". `status`: "draft — PENDING-ANALYST items open".

    `file_format` object: `type` = "csv", `delimiter` = ";", `encoding` = "utf-8-sig" (German-Excel dialect, verified against data_raw.csv BOM per Pitfall 4), `filename_pattern` = "YYYY-MM-DD_{TEAM1}-vs-{TEAM2}_{COMP}.csv" (example: 2026-06-14_GER-vs-AUT_EM-QUALI.csv; ISO date first for chronological sort, `_` separates fields, `-` joins tokens within a field, uppercase 3-letter IFAF team codes), `filename_semantics` = "TEAM1 = charting-perspective team — PENDING-ANALYST", `files` = "one export file per game".

    `columns`: array of 19 objects covering exactly the real-export baseline from CONTEXT.md: `PLAY #, ODK, DN, DIST, HASH, YARD LN, PLAY TYPE, RESULT, GN/LS, OFF FORM, OFF PLAY, OFF STR, PLAY DIR, GAP, PASS ZONE, DEF FRONT, COVERAGE, BLITZ, QTR`. Each object: `name`, `dtype`, `required`; plus constraints:
    - `PLAY #`: int, required (identity source of play_id, contiguous 1..N per game)
    - `ODK`: str, required, `values` = ["O","D","K"] (forward-only hardening vs polluted legacy ODK, Pitfall 2)
    - `DN`: int, required, `range` = [0,4] (0 = PAT play)
    - `DIST`: int, required
    - `YARD LN`: int, required, `range` = [-50,50], `note` on signed convention (negative = own territory, positive = yards to opponent goal)
    - `RESULT`: str, required (non-empty forward-only — Pitfall 1: 23% of legacy plays are empty and silently become "pass"), `base_tokens` = ["Rush","KNEEL","Sack","Interception","Complete","Incomplete","Good","No Good","Fumble","Penalty"], `modifier_tokens` = ["TD","Def TD","Safety","Penalty"], `separator` = ", ", `matching` = "exact token match after split on separator, case-sensitive" (replaces the fragile str.contains substring semantics), `note` flagging the C-07 amendment: "No Good" and "Fumble" added to C-07; Fumble semantics PENDING-ANALYST
    - `GN/LS`: int, optional, `note` = "informational, non-authoritative; yards gained are derived from yardline_50 deltas"
    - `DEF FRONT`, `COVERAGE`, `BLITZ`: str, optional, `values` = ["PENDING-SIGHTING"] (locked decision: sight real values before fixing vocabulary)
    - `QTR`: str, optional, `note` = "essentially never filled; not consumed by the pipeline"
    - `HASH`, `PLAY TYPE`, `OFF FORM`, `OFF PLAY`, `OFF STR`, `PLAY DIR`, `GAP`, `PASS ZONE`: str, optional, no vocabulary (semantics sighting deferred to Phase 1.2 per CONTEXT.md deferred ideas)

    `derived_fields`: one entry per manual field, each with `source`, `rule`, `dtype`:
    - `play_id`: source "PLAY #", rule "identity", Int32
    - `yardline_50`: source "YARD LN", rule "-x if x < 0 else 50 - x", Int32, `verified` = "3656/3658 legacy rows (99.95%)"
    - `drive_id`: source "ODK + RESULT", rule "increment on O/D possession flip; scoring/turnover RESULT (TD, Def TD, Safety, Interception, Fumble-if-turnover) also closes a drive", Int32
    - `half`: source "data/half_boundaries.csv", rule "1 if PLAY # < half2_first_play else 2", Int32
    - `game_id`: source "filename", rule "parsed from filename_pattern", Int32-or-str per Phase 1.2 choice — document as "assigned by ingest from parsed filename"
    - `posteam`: source "filename + ODK", rule "PENDING-ANALYST: ODK charting perspective for exchanged opponent film unresolved; proposal on the agenda: TEAM1 in filename = perspective team", str

    Implementation is a literal JSON file — no code. Validate it parses before committing.
  </action>
  <verify>
    <automated>python3 -c "import json; s=json.load(open('docs/data-contract.schema.json')); assert {'file_format','columns','derived_fields'} <= set(s), 'missing top-level keys'; names={c['name'] for c in s['columns']}; req={'PLAY #','ODK','DN','DIST','HASH','YARD LN','PLAY TYPE','RESULT','GN/LS','OFF FORM','OFF PLAY','OFF STR','PLAY DIR','GAP','PASS ZONE','DEF FRONT','COVERAGE','BLITZ','QTR'}; assert req <= names, ('missing columns', req-names); r=[c for c in s['columns'] if c['name']=='RESULT'][0]; toks=set(r['base_tokens'])|set(r['modifier_tokens']); assert {'Rush','Penalty','KNEEL','Sack','Interception','Complete','Incomplete','TD','Def TD','Good','No Good','Safety','Fumble'} <= toks, 'RESULT tokens incomplete'; assert {'play_id','yardline_50','drive_id','half','game_id','posteam'} <= set(s['derived_fields']), 'derived_fields incomplete'; print('schema OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('docs/data-contract.schema.json'))"` exits 0
    - Schema contains all 19 baseline column names exactly as listed (including `PLAY #`, `YARD LN`, `GN/LS` with spaces/slash)
    - RESULT token union covers exactly the 13-token canonical set (C-07 + `No Good` + `Fumble`); `separator` == ", "
    - `file_format.delimiter` == ";" and `file_format.encoding` == "utf-8-sig"
    - All 6 derived_fields present; `posteam.rule` contains the literal string "PENDING-ANALYST"; COVERAGE/DEF FRONT/BLITZ `values` contain "PENDING-SIGHTING"
  </acceptance_criteria>
  <done>Schema file exists, parses with stdlib json, and encodes the full column canon, RESULT grammar, and all six derivation entries with PENDING markers on the two externally-dependent items.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/data-contract.md (German spec incl. charting protocol and agenda)</name>
  <files>docs/data-contract.md</files>
  <read_first>
    - docs/plan-1-analytics-refresh.md (style analog: German prose, English identifiers in backticks, `##` em-dash headings, `## Nicht-Ziele` section pattern)
    - docs/data-contract.schema.json (just created — the spec must mirror it 1:1)
    - .planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-RESEARCH.md (§Summary legacy-audit numbers, §Pattern 2 derivation table, §Common Pitfalls 1–6, §Open Questions)
    - .planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-PATTERNS.md (§Shared Patterns — code excerpts to transcribe; corrected synthetic-time location)
    - Python/helper_add_hudl_mutations.py (lines 4–12 get_games home-team assumption; lines 384–407 prepare_wp_data)
  </read_first>
  <action>
    Write `docs/data-contract.md` in German (working language with the analyst, matching `docs/plan-*.md` style: German prose, ALL file names / column names / code identifiers in English backticks, `#` title + `##` sections with em-dash subtitles). Cross-reference `data-contract.schema.json` and `data/half_boundaries.csv` as inline code paths. Required sections and load-bearing content:

    1. **Zweck & Geltungsbereich** — forward-only contract; the 47 legacy games (3,701 plays) in `data_raw.csv` are grandfathered (no re-export); Phase 1.2 gets two ingest paths (legacy + contract format). Export preset is owned and maintained by the user (Hudl access) — the analyst conversation covers charting only, never export mechanics.
    2. **Export-Baseline** — the 19-column preset (list all 19), one file per game, CSV dialect `;` + `utf-8-sig` (German-Excel; warn per Pitfall 4 that locale changes break parsing). Note the deliberate dialect duality: hand-maintained repo CSVs (`data/half_boundaries.csv`, `team_roster.csv`) use plain `,`/UTF-8; only Hudl exports use `;`/BOM.
    3. **Dateinamenskonvention** — `YYYY-MM-DD_{TEAM1}-vs-{TEAM2}_{COMP}.csv`, example `2026-06-14_GER-vs-AUT_EM-QUALI.csv`, uppercase 3-letter IFAF codes; `TEAM1` = Charting-Perspektive is a proposal marked PENDING-ANALYST.
    4. **`RESULT`-Vokabular & Grammatik** — canonical 13 tokens; grammar = base token + optional modifiers joined by `", "`, exact case-sensitive matching. Explicitly record the **C-07-Amendment**: `No Good` (97 legacy occurrences) and `Fumble` (9) added to the C-07 list; `KNEEL` kept although 0 legacy occurrences. `RESULT` is mandatory (non-empty) forward-only — document the legacy defect: 846/3,701 plays (23%) empty, silently parsed as `pass` by `helper_add_hudl_mutations.py`'s `.otherwise(pl.lit("pass"))`. Document malformed legacy variants (`Penalty (declined)`, `Complete Penalty` without comma) as grandfathered, invalid forward. PAT semantics: `down == 0` marks PAT, `Good` is exact match, `yardline_50` 45 = 1-Punkt-Spot, 40 = 2-Punkt-Spot (current baselines 50%/46% hardcoded in `helper_add_ep_wp.py`, replaced empirically in Phase 1.3). Fumble semantics (Possession-Wechsel?) marked PENDING-ANALYST.
    5. **Abgeleitete Felder** — markdown table with one row per manual field (locked expectation), columns: Feld → Quelle → Regel → Randfälle → Legacy-Verifikation. Rules verbatim from the schema: `play_id` ← `PLAY #` (identity; contiguous 1..N verified in all 47 games), `yardline_50` ← `YARD LN` (`-x if x < 0 else 50 - x`; 3,656/3,658 match, 2 goal-line rows manually set to 50), `drive_id` ← ODK possession flips + scoring/turnover results (requires clean `ODK ∈ {O, D, K}`; legacy ODK polluted with team names/formations → grandfathered path reads the manual `drive_id` column), `half` ← `data/half_boundaries.csv`, `game_id` ← filename, `posteam` ← filename + ODK perspective (PENDING-ANALYST; surface the `get_games()` caveat: first-drive possession is assumed "home_team"). Derivation CODE is Phase 1.2 scope — this spec documents rules only.
    6. **Zeitdaten (REQ-S1-02)** — explicit record: the export contains NO time information; `QTR` exists but is essentially never filled. Consequence: `half_seconds_remaining` stays synthetic per C-08 (`play_time = 1200 / max(play_id_half)` per half, `prepare_wp_data` in `helper_add_hudl_mutations.py` lines 384–407 — NOT `helper_add_ep_wp.py`); Phase 1.4 WP charts must be flagged "synthetic time". The only manual time input is the halftime boundary: define `data/half_boundaries.csv` (plain `,` CSV, header `filename,half2_first_play`, one row per game, keyed by export filename; example row `2026-06-14_GER-vs-AUT_EM-QUALI.csv,34`; rule `half = 1 if PLAY # < half2_first_play else 2`).
    7. **Defense-Felder (REQ-S1-03)** — `DEF FRONT`, `COVERAGE`, `BLITZ` exist in the export, partially filled; canonical vocabulary is PENDING-SIGHTING (locked: sight real distinct values from sample exports first — no repo file contains these columns). **Flag-Pull-Verursacher: bewusst übersprungen** — record the deliberate skip as a charting-protocol decision (success criterion 4), revisit-later note.
    8. **Charting-Protokoll & Gesprächsagenda** — the three (and only three) analyst items: (a) `ODK`-Perspektive bei getauschtem Gegner-Film → `posteam`-Regel + `TEAM1`-Semantik [PENDING-ANALYST], (b) `RESULT`-Charting: verpflichtend nicht-leer, Fumble-Semantik, Vokabular-Bestätigung [PENDING-ANALYST], (c) Defense-Spalten: Lücken/Konsistenz forward, Bestätigung des gesichteten Vokabulars [outcome recorded after sighting + conversation]. Each with a marked outcome slot.
    9. **Nicht-Ziele** — no re-export/re-charting of legacy games; no negotiation of export mechanics with the analyst; flag-pull causer skipped; no derivation/validation code in this phase (Phase 1.2).
    10. **Datenschutz-Hinweis** — export contains player-name columns (`RECEIVED BY`, `Thrown By`, `QB`); DSGVO handling is scoped to Strand 2 (REQ-S2-01), noted here for awareness.

    Use exactly the literal markers `PENDING-SIGHTING` and `PENDING-ANALYST` for unresolved slots (Plans 02/03 clear them; verification greps for them). Do not resolve `posteam` or the defense vocabulary — locked as open in CONTEXT.md.
  </action>
  <verify>
    <automated>python3 -c "import pathlib; t=pathlib.Path('docs/data-contract.md').read_text(encoding='utf-8'); missing=[f for f in ['play_id','drive_id','game_id','half','posteam','yardline_50','half_boundaries.csv','data-contract.schema.json','PENDING-SIGHTING','PENDING-ANALYST','-x if x < 0 else 50 - x','No Good','Fumble','utf-8-sig','1200'] if f not in t]; assert not missing, missing; assert len(t.splitlines()) >= 80, 'spec suspiciously short'; print('spec OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Spec is German prose with English identifiers in backticks (matches `docs/plan-1-analytics-refresh.md` conventions)
    - All six manual fields appear with a derivation table row each; the `yardline_50` rule appears as the literal expression `-x if x < 0 else 50 - x`
    - The C-07 amendment (`No Good`, `Fumble`) is recorded as an explicit decision, not silently applied
    - Time-data section states unavailability explicitly and references `prepare_wp_data` / `1200` synthetic construction and `data/half_boundaries.csv`
    - Flag-pull causer skip is recorded as a deliberate charting-protocol decision
    - Literal markers `PENDING-SIGHTING` and `PENDING-ANALYST` are present (they gate Plans 02/03)
    - Spec is ≥ 80 lines
  </acceptance_criteria>
  <done>German spec exists covering all 10 sections, mirrors the schema, and every externally-dependent item carries a PENDING marker.</done>
</task>

<task type="auto">
  <name>Task 3: Create data/half_boundaries.csv and run cross-artifact consistency checks</name>
  <files>data/half_boundaries.csv</files>
  <read_first>
    - docs/data-contract.md (just created — the boundary-file definition it must match)
    - docs/data-contract.schema.json (just created — `derived_fields.half.source` must point at this file)
    - team_roster.csv (repo root — plain-CSV convention analog: comma-delimited, plain UTF-8, no BOM, lowercase snake_case header)
  </read_first>
  <action>
    Create `data/half_boundaries.csv` with exactly one line: the header `filename,half2_first_play` (plain comma CSV, UTF-8 without BOM, LF line ending — the hand-maintained-CSV dialect per `team_roster.csv`, deliberately NOT the Hudl `;`+BOM dialect). No data rows: the contract is forward-only and no contract-format exports exist yet; the example row lives in the spec, not the data file.

    Then run the cross-artifact consistency checks (spec ⊇ schema): every schema column name and every RESULT token must appear in the spec text. Fix the spec (not the schema) if anything is missing.
  </action>
  <verify>
    <automated>python3 -c "import csv,json,pathlib; r=next(csv.reader(open('data/half_boundaries.csv'))); assert r==['filename','half2_first_play'], r; raw=pathlib.Path('data/half_boundaries.csv').read_bytes(); assert not raw.startswith(b'\xef\xbb\xbf'), 'BOM found — must be plain UTF-8'; s=json.load(open('docs/data-contract.schema.json')); t=pathlib.Path('docs/data-contract.md').read_text(encoding='utf-8'); mc=[c['name'] for c in s['columns'] if c['name'] not in t]; assert not mc, ('columns missing in spec', mc); rc=[c for c in s['columns'] if c['name']=='RESULT'][0]; mt=[x for x in set(rc['base_tokens'])|set(rc['modifier_tokens']) if x not in t]; assert not mt, ('tokens missing in spec', mt); print('consistency OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/half_boundaries.csv` first row parses (comma dialect) to exactly `['filename','half2_first_play']`
    - File has no UTF-8 BOM
    - Every column `name` in the schema appears verbatim in `docs/data-contract.md`
    - Every RESULT base/modifier token in the schema appears verbatim in `docs/data-contract.md`
  </acceptance_criteria>
  <done>Boundary record exists in the plain-CSV dialect and spec/schema are token- and column-consistent (they cannot drift at creation time).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hudl export CSV → Phase 1.2 ingest | Semi-trusted external file input; this plan's schema IS the future input-validation spec |
| User-maintained `data/half_boundaries.csv` → ingest | Hand-edited file consumed by pipeline code later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.1-01 | Tampering / DoS (malformed input) | `docs/data-contract.schema.json` as future validator input | mitigate | Encode `required`, `range` ([0,4] downs, [-50,50] YARD LN), closed `values` sets (ODK) and the exact RESULT token grammar so Phase 1.2 rejects malformed exports instead of silently corrupting features (ASVS V5 input-validation spec work) |
| T-1.1-02 | Information Disclosure | Player-name columns (`RECEIVED BY`, `Thrown By`, `QB`) documented in spec | accept | Docs-only phase adds no new data; spec's DSGVO note flags the columns; full DSGVO clearance is scoped to Strand 2 (REQ-S2-01) |
</threat_model>

<verification>
All structural checks from the three tasks green, run together from repo root:
1. Schema parses; top-level keys, 19 columns, 13 RESULT tokens, 6 derived_fields present
2. Spec contains all six manual fields, both PENDING markers, C-07 amendment tokens, time-data record
3. Boundary CSV header exact, no BOM; spec ⊇ schema columns/tokens
</verification>

<success_criteria>
- Contract draft complete: both artifacts exist and are mutually consistent
- Success criterion 2 (six manual fields documented) fully met; criteria 1/3/4 met up to the marked PENDING slots
- No decision locked that CONTEXT.md marked as open (posteam, defense vocabulary)
</success_criteria>

<output>
Create `.planning/phases/01.1-data-contract-with-the-videoanalyst/01.1-01-SUMMARY.md` when done.
</output>
