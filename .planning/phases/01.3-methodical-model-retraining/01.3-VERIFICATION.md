---
phase: 01.3-methodical-model-retraining
verified: 2026-08-19T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 01.3: Methodical Model Retraining Verification Report

**Phase Goal:** EP/WP models retrained on the full dataset with honest evaluation, calibrated
probabilities, and versioned artifacts.
**Verified:** 2026-08-19
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All reported metrics come from grouped CV over `game_id`; no game contributes to both train and test | VERIFIED | `src/flag_football_ep/model/evaluate.py::run_logo` uses `sklearn.model_selection.LeaveOneGroupOut`, pre-flight rejects <2 groups, post-loop asserts no unfilled (NaN) predictions. `model/train.py::_train` calls `run_logo` exclusively — `grep -rn "train_test_split" src/flag_football_ep/` returns zero matches. `tests/test_model_evaluate.py` (31/31 passing) and `tests/test_model_train.py` (37/37 passing) both exercise fold-disjointness. `_tune`'s inner CV also uses `GroupKFold` over `game_id`, never a play-level split. |
| 2 | Training report contains reliability curves per class and log-loss against a naive baseline | VERIFIED | `evaluate.py::naive_baseline_logloss` (class-frequency floor) and `reliability_curves` (one-vs-rest `sklearn.calibration.calibration_curve`, 10 bins) are real, tested implementations (31/31 tests green), wired into `_train` and logged as MLflow figure/metric artifacts (`reliability_{ep,wp}.png`, `logo_*`/`naive_*`/`logloss_improvement`). `01.3-TRAINING-REPORT.md` §3-4 reports both models' numbers narratively and honestly (EP does **not** beat naive; WP does by a wide margin) — this is the "prefer statistical honesty" finding, not a fabricated pass. |
| 3 | Each feature candidate (`half`, real `half_seconds_remaining`, competition tier, recency) has a documented grouped-CV evaluation | VERIFIED | `model/experiments.py` implements the harness (`run_candidate`, `run_recency_candidate`, `run_real_clock_experiment`, all built on `run_logo`); `half`/`competition_tier` were adopted into production and correctly removed from the live `CANDIDATES` registry (confirmed by reading the registry dict — only `recency`/`real_clock` remain as still-testable candidates). `01.3-TRAINING-REPORT.md` §6 documents all four verdicts with deltas, including the two that could not cleanly adopt/reject (`recency`'s no-op — `game_date` 100% null; `real_clock`'s not-viable — IFAF `yards_to_go` 100% null), which is an honest, not hidden, gap. `tests/test_model_experiments.py` (45/45 passing) and `tests/test_migration_equivalence.py` (7/7 passing, "Known, expected deltas" section names the same values) confirm the code matches the report's numbers. |
| 4 | PAT baselines are empirical estimates from the full dataset (replacing hard-coded 50%/46%), PAT break-even chart exists | VERIFIED | `features/mutations.py::estimate_pat_baselines` computes pooled 1-pt/2-pt rates with `scipy.stats.binomtest` Clopper-Pearson CIs, raising `InsufficientPatAttempts` rather than silently returning 0/NaN; the old hard-coded `0.5`/`0.92` values are retained only in `PatBaselines.legacy_notebook()`, explicitly documented as test-only and never called from production. `charts/pat_breakeven.py::write_pat_breakeven` + `ffep pat-breakeven` CLI command exist and are wired (`cli.py:198-222`). `tests/test_charts_pat_breakeven.py` + `tests/test_model_registry.py` (52/52 passing). Training report §7 cites the actual numbers (1-pt 49.00% n=251, 2-pt 41.18% n=102). |
| 5 | Model artifacts carry date + training-data hash in filename; `ep_model.pkl` never silently overwritten | VERIFIED | `model/train.py::_export_pickle` writes `{prefix}_model_{YYYYMMDD}_{hash8}.pkl` and raises `FileExistsError` on a repeat — no fixed filename anywhere in the write path. `model/registry.py` implements a full MLflow-registry versioning scheme (`register_production_model`, `promote`, `resolve_champion`, `champion`-alias, identifier-injection guards). `models/legacy/` holds only the pre-phase notebook pickles, explicitly quarantined with a README explaining they are unreachable by any scoring code path; `tests/test_repo_hygiene.py::test_models_dir_has_no_fixed_name_pickles` enforces this. `tests/test_model_train.py` (37/37 passing) asserts the dated-hash filename pattern and the never-overwrite guard directly. |

**Score:** 5/5 truths verified

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-S1-07 | 01.3-04, 01.3-09 | GroupKFold/LOGO over `game_id`, no train/test game leakage | SATISFIED | `evaluate.py::run_logo`, zero `train_test_split` usages, `logo_wall_seconds`/`cv_scheme` params logged |
| REQ-S1-08 | 01.3-05, 01.3-09 | Reliability curves + naive-baseline log-loss in report | SATISFIED | `evaluate.py::naive_baseline_logloss`/`reliability_curves`, MLflow-logged figures, `01.3-TRAINING-REPORT.md` §3-4 |
| REQ-S1-09 | 01.3-02, 01.3-07, 01.3-08, 01.3-09 | Feature candidates re-tested with documented outcomes | SATISFIED | `experiments.py` harness, `hyperparams.py` adoption comments citing deltas, `01.3-TRAINING-REPORT.md` §6 |
| REQ-S1-10 | 01.3-06, 01.3-09 | Empirical PAT baselines + break-even chart | SATISFIED | `mutations.py::estimate_pat_baselines`, `charts/pat_breakeven.py`, `ffep pat-breakeven` |
| REQ-S1-11 | 01.3-01, 01.3-03, 01.3-05, 01.3-09 | Model versioning, no silent overwrite | SATISFIED | `model/registry.py`, `train.py::_export_pickle`, `tests/test_repo_hygiene.py` hygiene guard |

REQUIREMENTS.md lists exactly these five IDs for Phase 1.3, all already marked Complete; cross-referencing every PLAN's `requirements:` frontmatter against REQUIREMENTS.md found no orphans (every ID declared by a plan appears in REQUIREMENTS.md, and every Phase-1.3 ID in REQUIREMENTS.md is claimed by at least one plan).

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/flag_football_ep/model/evaluate.py` | LOGO loop, calibration, OOF persistence | VERIFIED | 358 lines, real implementation, exports match plan frontmatter (`run_logo`, `LogoResult`, `EvaluationError`, `naive_baseline_logloss`, `reliability_curves`, `per_source_metrics`, `oof_frame`, `write_oof_predictions`, `render_reliability_figure`) |
| `src/flag_football_ep/model/train.py` | LOGO-wired `_train`/`_tune`, dated `.pkl` export | VERIFIED | 539 lines, no `train_test_split`, `_export_pickle` write-once guard present |
| `src/flag_football_ep/model/registry.py` | MLflow registry, `champion` alias, promote/resolve | VERIFIED | 157 lines, identifier-validated URI construction, `RegistryError` named failures |
| `src/flag_football_ep/model/experiments.py` | Candidate harness | VERIFIED | `CANDIDATES` registry correctly excludes adopted `half`/`competition_tier` |
| `src/flag_football_ep/features/mutations.py::estimate_pat_baselines` | Empirical PAT rates + CI | VERIFIED | Clopper-Pearson CI via `scipy.stats.binomtest`, `InsufficientPatAttempts` guard |
| `src/flag_football_ep/charts/pat_breakeven.py` | Break-even chart module | VERIFIED | `render_pat_breakeven`/`write_pat_breakeven` exist and are CLI-wired |
| `01.3-TRAINING-REPORT.md` | Consolidated evidence, 5-criteria mapping | VERIFIED | 324 lines, every headline number cites an MLflow run id, explicit success-criteria mapping section |
| `docs/model-training.md` | Operator guide | VERIFIED | Real, detailed guide matching the actual code's error messages and CLI flow verbatim |
| `models/{ep,wp}_model_*_*.pkl` | Dated/hashed exports | Not present in this worktree | `data/processed`/`mlruns`/dated pickles are gitignored generated artifacts (confirmed via `.gitignore`); their absence here reflects worktree state, not missing code — the writer/guard code and its tests are verified directly |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `train.py::_train` | `evaluate.py::run_logo` | direct call, replaces `train_test_split` | WIRED | Confirmed by reading `_train`'s body (line 254) and by `grep` showing zero `train_test_split` matches in `src/` |
| `train.py::_train` | `evaluate.py` calibration fns | `naive_baseline_logloss`/`reliability_curves`/`per_source_metrics` calls | WIRED | Lines 271-273, results logged via `_log_run`'s `figures=`/`texts=` |
| `train.py::_train` | `registry.py::register_production_model` | `_log_run(..., register_as=registry.registered_model_name(...))` | WIRED | Confirmed at `_train` line 340 and inside `_log_run` |
| `experiments.py::run_candidate` | `evaluate.py::run_logo` | each arm measured via the same LOGO protocol as production | WIRED | Confirmed by module inspection; `_build_current_production_base_features` keeps the control arm matching production feature set |
| `cli.py::pat_breakeven` | `mutations.py::estimate_pat_baselines` + `charts/pat_breakeven.py::write_pat_breakeven` | CLI command body | WIRED | `cli.py:215-222` |
| `cli.py::experiment` / `train` / `score` / `promote` | `experiments.py` / `train.py` / `score.py` / `registry.py` | CLI dispatch | WIRED | All 9 subcommands present in `cli.py` (`ingest`, `fetch-sportapp`, `fetch-ifaf`, `train`, `score`, `pat-breakeven`, `experiment`, `promote`, `run`) |

### Behavioral Spot-Checks (test suite, run live in this verification)

| Behavior | Command | Result | Status |
|---|---|---|---|
| LOGO fold-disjointness / OOF coverage | `pytest -q tests/test_model_evaluate.py` | 31/31 passed | PASS |
| Model registry / champion promotion | `pytest -q tests/test_model_registry.py` | (bundled, 52/52 with pat_breakeven) passed | PASS |
| PAT baseline + break-even chart | `pytest -q tests/test_charts_pat_breakeven.py` | (bundled above) passed | PASS |
| Scoring path (`score_plays`) | `pytest -q tests/test_model_score.py` | 22/22 passed | PASS |
| Feature-candidate harness | `pytest -q tests/test_model_experiments.py` | 45/45 passed | PASS |
| Frozen notebook-baseline equivalence + REQ-S1-09 deltas | `pytest -q tests/test_migration_equivalence.py` | 7/7 passed | PASS |
| `_train`/`_tune` LOGO wiring, dated-pkl export, no-overwrite guard | `pytest -q tests/test_model_train.py` | 37/37 passed | PASS |
| No `train_test_split` remains in `src/` | `grep -rn "train_test_split" src/flag_football_ep/` | no matches | PASS |
| No fixed-name pickle written in production path | code read of `_export_pickle` + `tests/test_repo_hygiene.py` | guard present, test asserts it | PASS |

**Total: 194 tests run live during this verification across the 7 phase-1.3-owned test modules, 0 failures.** (A full-repository `pytest -q` run was also attempted twice but was not completed to its final summary line within this session due to environment CPU contention from two concurrent full-suite invocations; both were terminated. This does not weaken the finding above — every module directly implementing or exercising the five success criteria was run individually to full completion with 0 failures, and `pytest --collect-only` confirmed no import/collection errors across the same modules.)

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `model/train.py` | 34 | "placeholder" in docstring, referring to the `.pkl` export existing only for legacy-consumer compatibility while the MLflow registry is the primary REQ-S1-11 mechanism | Info | Not a stub — the described mechanism (registry) is fully implemented and tested; the word describes scope, not missing work |
| `model/evaluate.py` | 240 | "todo" in docstring, referring to a CONTEXT-tracked follow-up item described as "realised" by this module | Info | Documentation cross-reference, not an open debt marker |

No `TBD`/`FIXME`/`XXX` markers found in any phase-1.3-owned source file. No `TODO`/`HACK`/`PLACEHOLDER` literal debt markers found either — the two "placeholder"/"todo" hits above are prose, not markers of incomplete work, and neither is attached to a stubbed code path.

The independent code review (`01.3-REVIEW.md`, same date) found 0 Critical, 10 Warning, 10 Info issues, concentrated in the opt-in `--tune` path, a train/serve encoding edge case for null-`down` legacy rows, and several non-atomic-write / error-message-quality gaps. None of these warnings contradict or undermine any of the five roadmap success criteria — they are legitimate code-quality follow-ups on paths outside this phase's must-haves (e.g., the `--tune` path is explicitly not exercised in the shipped training run per `01.3-TRAINING-REPORT.md` §8).

### Human Verification Required

None outstanding. The one item that structurally required human sign-off — Plan 01.3-09 Task 3's `checkpoint:human-verify` promotion gate — was already executed during phase execution: the user reviewed the training report and MLflow UI and responded "approved" (quoted verbatim in `01.3-09-SUMMARY.md`), after which both `ep_model` and `wp_model` were promoted to `champion`. This is a completed gate with a real approval record, not a pending item this verification needs to re-surface.

### Gaps Summary

No gaps. All five ROADMAP Phase 1.3 success criteria are backed by real, tested, non-stub code — not just SUMMARY.md narrative. 194 tests were run live in this verification session across every phase-1.3-owned module (LOGO evaluation, calibration/reliability, feature-candidate harness, PAT baselines, model registry/versioning) with zero failures, and static code inspection (import checks, `grep` for banned patterns, direct reading of the LOGO/registry/PAT implementations) confirms the training report's claims match what the code actually does — including its unflattering, honestly-reported finding that the EP model does not beat its naive baseline. That finding is a data/feature-coverage limitation the report documents plainly (§3/§4/§9), not a gap in this phase's deliverable — the roadmap's second success criterion asks for the naive-baseline comparison to exist and be reported, not for the model to win it.

One minor, non-blocking observation: `.planning/STATE.md` frontmatter still shows `status: executing` / `completed_phases: 2` / "Phase 01.3 execution started", stale relative to `ROADMAP.md`'s "9/9 plans complete, Phase 1.3 Complete 2026-08-19" and the actual commit history. This is a tracking-metadata staleness issue for the orchestrator to refresh before starting Phase 1.4, not a gap in Phase 1.3's own deliverable, and does not affect this verification's status.

---
*Verified: 2026-08-19*
*Verifier: Claude (gsd-verifier)*
