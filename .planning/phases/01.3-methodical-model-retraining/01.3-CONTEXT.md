# Phase 1.3: Methodical Model Retraining - Context

**Gathered:** 2026-08-18
**Status:** Ready for planning

<domain>
## Phase Boundary

EP/WP models retrained on the full canonical dataset with honest evaluation, calibrated probabilities, and versioned artifacts (REQ-S1-07 … REQ-S1-11): the play-level `train_test_split` in `model/train.py` is replaced by grouped cross-validation over `game_id` (D-07), the training report gains reliability curves per class and log-loss vs a naive baseline, the REQ-S1-09 feature candidates get documented grouped-CV outcomes, the hard-coded PAT baselines in `features/mutations.py` become empirical estimates with a break-even chart, and model artifacts are versioned so nothing is silently overwritten.

**Scope extension (folded todo):** The IFAF/cpx.studio WM-2026 data (ingested in 1.2) is explicitly used for calibration/benchmarking in this phase — via per-source metric breakdowns and an IFAF-only real-clock experiment.

Coaching-product delivery (tendency reports, decision-chart packaging for the HC, <10-minute turnaround) is **not** this phase — that is Phase 1.4. 1.3 delivers the measurement methodology, the retrained/versioned models, and the PAT analysis 1.4 consumes.

</domain>

<decisions>
## Implementation Decisions

### Grouped-CV protocol
- **Evaluation scheme:** Leave-one-game-out (LOGO) over `game_id` — every game is held out once; all reported metrics come from these held-out predictions. This is the maximal form of REQ-S1-07/D-07 (user chose it over GroupKFold K=5 knowing the ~50+ fits cost).
- **Tuning:** Hyperopt scores each trial on a cheap grouped inner CV (e.g. GroupKFold K=5 over games); the winning params are then evaluated once with the full LOGO loop. No play-level leakage anywhere in tuning; no fully-nested LOGO×trials explosion.
- **Shipped model:** LOGO is measurement only. The production model is refit once on all games with the tuned params — one artifact.
- **Out-of-fold EPA:** The LOGO held-out predictions are persisted; `ffep score` uses each historical play's out-of-fold EP/WP (nflfastR precedent) so own-team EPA in 1.4 is not flattered by in-sample predictions. New/unseen games are scored with the refit model.

### Calibration & training report
- **Naive baseline:** Class-frequency baseline — predict the marginal outcome distribution (training-data class rates) for every play; report model log-loss against it.
- **Report form:** MLflow run artifacts — reliability-curve PNGs per class + metrics table logged to the training run, browsable in the MLflow UI. No separate standalone report file required.
- **Miscalibration policy:** Report only. No isotonic/Platt layer in this phase; if curves look bad, calibration correction becomes an explicit follow-up decision.
- **Per-source breakdown:** LOGO metrics and reliability curves are reported per source/competition (legacy Hudl, new Hudl, sportapp, IFAF WM-2026) alongside pooled numbers — this is how the folded IFAF-benchmarking todo is realized. IFAF games train like any other source (no external holdout).

### Feature re-tests & training-data mix
- **Training pool:** Pool everything — all four sources train the production models. The per-source breakdown guards against a source hurting; competition level enters as a tested covariate.
- **Real `half_seconds_remaining`:** IFAF-only sub-experiment — one documented WP experiment on IFAF games comparing real `game_clock_ms`-derived time vs the synthetic construction on the same games. Quantifies what synthetic time costs; the production feature set stays synthetic until Hudl delivers real time (REQ-S1-02 outcome).
- **Adoption criterion:** A feature candidate is adopted when mean pooled LOGO log-loss improves over the current feature set. Every candidate experiment is logged in MLflow with its verdict (satisfies REQ-S1-09 "documented outcomes").
- **Recency weighting:** Exponential decay by game date (half-life tuned as part of the experiment); on EP it multiplies with the existing `Total_W_Scaled` weight.
- **Competition covariate:** Categorical competition tier from a small maintained tier vocabulary in reference data (e.g. womens-international / womens-national / mixed-other), one-hot encoded. Not source-as-proxy.
- **Existing EP weighting:** `Total_W_Scaled` (drive-score-distance × score-differential, nflfastR-style) stays as an untouched given — not re-tested.

### PAT baselines & model versioning
- **PAT estimation:** Pooled overall success rate per PAT type (1-pt, 2-pt) over the whole dataset, replacing `PAT_BASELINE_ONE_POINT = 0.5` / 46% constants in `features/mutations.py`; attempt counts and confidence intervals reported alongside.
- **Versioning (REQ-S1-11):** MLflow model registry is primary — registered model per EP/WP, the source of truth consumers resolve against. The dated + training-data-hash `.pkl` export from 1.2 is kept (satisfies the requirement's literal filename wording; offline use).
- **Promotion:** Registry champion alias per model with an explicit `ffep promote` step, used after reviewing the training report. `ffep score` resolves the champion alias — it never silently follows the latest run (changes today's latest-FINISHED-run behavior in `model/score.py`).

### Claude's Discretion
- PAT break-even chart mechanism — user chose "you decide"; requirement is a reproducible chart. Lean toward a reusable chart module (e.g. an `ffep` subcommand rendering PNG, also logged to MLflow) so Phase 1.4 can reuse it rather than a throwaway notebook plot.
- Inner-CV fold count for tuning (K=5 suggested, not locked), MLflow experiment naming/layout for LOGO runs and candidate experiments, reliability-curve binning, storage format/location of persisted out-of-fold predictions, exact tier vocabulary and its reference-data file format (consistent with `data/reference/` CSV style), recency half-life search grid.
- Runtime engineering for the ~50+ LOGO fits (parallelization, caching) as long as evaluation stays honest.

### Folded Todos
- **Tap IFAF/cpx.studio WM API as additional data source** (`.planning/todos/pending/2026-08-17-tap-ifaf-wm-api-as-data-source.md`) — the ingest half landed in 1.2; the remaining EP-calibration/benchmarking half folds into this phase as (a) the per-source metric/reliability breakdown incl. IFAF WM-2026 and (b) the IFAF-only real-clock WP experiment.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase sources & requirements
- `.planning/REQUIREMENTS.md` — REQ-S1-07 (split fix), REQ-S1-08 (calibration), REQ-S1-09 (feature re-tests), REQ-S1-10 (PAT baselines), REQ-S1-11 (versioning)
- `docs/plan-1-analytics-refresh.md` §"Phase 1.3" — original phase definition (German, authoritative detail)
- `.planning/PROJECT.md` §Key Decisions — D-07 (GroupKFold over game_id, locked), D-08 (Strand-1 non-goals)
- `.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md` — the 1.2 decisions this phase builds on (MLflow foundation, canonical Parquet, "obvious fixes only" cut-line that reserved all methodology for 1.3)
- `.planning/todos/pending/2026-08-17-tap-ifaf-wm-api-as-data-source.md` — folded todo (calibration/benchmarking half)

### Code this phase changes
- `src/flag_football_ep/model/train.py` — current `train_test_split(0.2, random_state=42)` pipeline, hyperopt `--tune` path, `_log_run` MLflow contract, `_export_pickle` dated+hash placeholder, `_hash_frame` training-data hash
- `src/flag_football_ep/model/hyperparams.py` — EP/WP feature lists, fixed params, shared `HYPEROPT_SPACE`; the single home for hyperparameters
- `src/flag_football_ep/model/score.py` — `resolve_run` latest-FINISHED-run resolution (replaced by champion-alias resolution), T-1.2-08 run-id validation pattern to preserve
- `src/flag_football_ep/features/mutations.py` — `PAT_BASELINE_ONE_POINT = 0.5` and the 0.46 two-point constant to replace; synthetic `half_seconds_remaining` construction (`1200 / max(play_id_half)`) in `prepare_wp_data`; `Total_W_Scaled` weighting
- `src/flag_football_ep/ingest/ifaf.py` — `game_clock_ms` field the real-clock experiment consumes

### Data inputs
- `data/processed/plays.parquet` (built by `ffep ingest`) — canonical training input with source/competition filter columns
- `data/reference/` — home for the new competition-tier reference data (style template: `data/reference/team_mapping.csv`, `data/half_boundaries.csv`)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `model/train.py` `_train` shared pipeline: filter → prepare → mutate → drop_nulls → split → fit/tune → evaluate → `_log_run` — the LOGO loop replaces the split/evaluate stage; the shared-shape design means EP and WP get the new protocol from one change
- `_hash_frame` (SHA-256 over Parquet bytes) — already the training-data hash REQ-S1-11 needs; reuse for registry version tags
- `_export_pickle` — already refuses overwrites and writes dated+hash filenames; keep as the secondary export
- MLflow foundation from 1.2: local `file:mlruns/` store (`MLFLOW_ALLOW_FILE_STORE` opt-in), per-run params/metrics/model logging, `phase` tag

### Established Patterns
- Hyperparameters live only in `model/hyperparams.py` — new tuning results must land there or in MLflow params, never inline in train.py
- `score.py` scopes all model loading to `config.paths.mlruns` with plain-hex run-id validation (T-1.2-08) — champion-alias resolution must preserve this no-arbitrary-path property
- Reference data as small maintained CSVs under `data/reference/` — the competition-tier vocabulary follows this shape
- Config via `flag_football_ep.config.Config` (`config.train.*` experiment names, `config.paths.*`) — new knobs (folds, half-life grid, promotion) belong there

### Integration Points
- `ffep train` / `ffep score` CLI (Typer) — new behavior (LOGO report, promote step) surfaces as subcommands/flags on the existing `ffep` entry point
- Persisted out-of-fold predictions become an input to Phase 1.4's own-team EPA reports — their location/format is a 1.4-facing contract
- The champion-alias resolution is what the 1.4 report pipeline (and `ffep run`) will call — keep resolution fast; the <10-minute goal lives downstream
- MLflow >=3.15 deprecates the local file store — the registry decision may interact with this (registry support on `file:` stores is limited); researcher should verify whether the registry requires the sqlite backend and what migrating `mlruns/` entails

</code_context>

<specifics>
## Specific Ideas

- The user consistently chose the maximally honest evaluation option (LOGO over K=5, out-of-fold EPA, explicit promotion) — when planning trade-offs arise, prefer statistical honesty over convenience.
- "Enterprise level with MLflow" (carried from 1.2) — registry, aliases, and experiment tracking are wanted as first-class, not as ceremony to minimize.
- nflfastR is the reference methodology the user recognizes (LOSO precedent, out-of-fold EPA, drive-score-distance weighting) — frame research and report language against it.

</specifics>

<deferred>
## Deferred Ideas

- Calibration correction layer (isotonic/Platt on out-of-fold predictions) — explicitly deferred; becomes a follow-up decision if this phase's reliability curves look bad.
- Real time data for Hudl games (REQ-S1-02 outcome) — the IFAF-only experiment produces the evidence for how much it matters; acting on it stays with the analyst conversation.
- Stronger down/distance-bucket baseline — considered for the report, not chosen; could join a later report iteration.
- Cleanup/retirement of the stale fixed-name `models/*.pkl` files (`ep_model.pkl` etc.) — not discussed; planner may fold it into the versioning work if trivial, otherwise leave.

### Reviewed Todos (not folded)
- **Ratify data contract v1.0 with the Videoanalyst** (`.planning/todos/pending/2026-08-17-ratify-data-contract-with-videoanalyst.md`) — analyst-blocked and user-owned; stays a tracked todo outside this phase.

</deferred>

---

*Phase: 1.3-Methodical Model Retraining*
*Context gathered: 2026-08-18*
