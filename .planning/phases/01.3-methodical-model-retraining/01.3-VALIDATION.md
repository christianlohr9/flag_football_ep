---
phase: 01.3
slug: methodical-model-retraining
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-18
---

# Phase 01.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (per `pyproject.toml` `[dependency-groups] dev = ["pytest>=8"]`) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` (`testpaths = ["tests"]`, `addopts = "-q"`) |
| **Quick run command** | `.venv/bin/pytest tests/test_model_train.py tests/test_model_score.py tests/test_features_mutations.py -q` |
| **Full suite command** | `.venv/bin/pytest -q` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/pytest tests/test_model_train.py tests/test_model_score.py -q`
- **After every plan wave:** Run `.venv/bin/pytest -q` (the migration from `file:` to `sqlite:` tracking touches shared fixtures used across `test_model_train.py` and `test_model_score.py`, so both must stay green together)
- **Before `/gsd:verify-work`:** Full suite must be green, plus a manual check that `mlflow ui --backend-store-uri sqlite:///.../mlflow.db` renders the reliability-curve PNGs and registered-model aliases (MLflow UI browsability is an explicit CONTEXT requirement)
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | REQ-S1-07 | — | LOGO evaluation: no game's plays in both train and test of any fold | unit | `pytest tests/test_model_train.py -k logo -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | REQ-S1-08 | — | Reliability curves + naive-baseline log-loss logged to the MLflow run | unit | `pytest tests/test_model_train.py -k calibration -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | REQ-S1-09 | — | Each feature candidate has a documented grouped-CV outcome | unit + manual | `pytest tests/test_model_train.py -k feature_candidate -x` (mechanics); MLflow UI review for the verdict record | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | REQ-S1-10 | — | PAT baselines computed from data, not hard-coded; break-even chart renders | unit | `pytest tests/test_features_mutations.py -k pat_baseline -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | REQ-S1-11 | — | Registered model + alias resolution; `ep_model.pkl`-equivalent never silently overwritten | unit | `pytest tests/test_model_train.py -k registry -x` + existing `test_train_ep_never_writes_fixed_ep_model_pkl` | Partial | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*(Task IDs to be filled in by the planner; the requirement→test mapping above is authoritative.)*

---

## Wave 0 Requirements

- [ ] `tests/test_model_train.py` — rewrite `_run_dirs` helper: it currently assumes FileStore's on-disk `params/` directory layout, which will not exist once the tracking URI is `sqlite:///...`
- [ ] `tests/test_model_train.py` — new tests for LOGO loop correctness (no game_id in both train and test of any fold; out-of-fold predictions cover every row exactly once)
- [ ] `tests/test_model_train.py` — new tests for registry: `register_model`/alias set/resolve round-trip against a `tmp_path`-scoped SQLite file (mirroring the existing `_make_config` pattern, never the real repo `mlruns/`)
- [ ] `tests/test_features_mutations.py` — new tests for empirical PAT baseline computation and small/zero PAT-attempt-count edge cases
- [ ] `tests/test_model_score.py` — update/extend for champion-alias resolution replacing `resolve_run`'s "latest FINISHED" path

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Feature-candidate verdicts are documented and browsable | REQ-S1-09 | The "documented outcome" is a human-readable record in MLflow, not a computable assertion | Open `mlflow ui --backend-store-uri sqlite:///...`; confirm each candidate run records an adopted/rejected verdict with grouped-CV metrics |
| MLflow UI renders reliability-curve PNGs and registered-model aliases | REQ-S1-08, REQ-S1-11 | Rendering in the UI cannot be asserted from pytest | Open MLflow UI; check run artifacts show reliability curves and the model registry shows champion alias |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
