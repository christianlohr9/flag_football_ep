---
phase: 01.3-methodical-model-retraining
plan: 08
type: execute
wave: 5
depends_on: [01.3-07]
files_modified:
  - src/flag_football_ep/model/hyperparams.py
  - src/flag_football_ep/features/mutations.py
  - src/flag_football_ep/model/experiments.py
  - tests/test_features_mutations.py
  - tests/test_model_experiments.py
autonomous: true
requirements: [REQ-S1-09]

must_haves:
  truths:
    - "Recency weighting has a documented grouped-CV outcome, with its half-life chosen by the same LOGO comparison rather than assumed"
    - "Rows whose source has no reliable game date get an explicitly chosen, documented weight instead of a silent NaN"
    - "The cost of synthetic `half_seconds_remaining` is quantified: one WP comparison on IFAF games only, real game clock versus the synthetic construction, on the same games and the same folds"
    - "The production WP feature set still uses synthetic time — the real-clock result is evidence for the analyst conversation, not a shipped change"
  artifacts:
    - path: "src/flag_football_ep/features/mutations.py"
      provides: "Exponential recency weighting and the real-clock `half_seconds_remaining` derivation"
      exports: ["add_recency_weight", "prepare_wp_data"]
    - path: "src/flag_football_ep/model/experiments.py"
      provides: "recency and real_clock candidate specs plus the IFAF-only sub-experiment runner"
      contains: "real_clock"
    - path: "src/flag_football_ep/model/hyperparams.py"
      provides: "Recency half-life search grid"
      contains: "RECENCY_HALF_LIFE_DAYS_GRID"
  key_links:
    - from: "src/flag_football_ep/features/mutations.py"
      to: "make_ep_model_mutations Total_W_Scaled"
      via: "the recency factor multiplies into the existing EP sample weight"
      pattern: "Total_W_Scaled"
    - from: "src/flag_football_ep/features/mutations.py"
      to: "game_clock_ms"
      via: "prepare_wp_data(real_clock=True) derives half_seconds_remaining from the IFAF game clock"
      pattern: "game_clock_ms"
---

<objective>
Close out REQ-S1-09's remaining two candidates: exponential recency weighting by game date
(with its half-life tuned by the same grouped-CV comparison), and the IFAF-only experiment
quantifying what synthetic `half_seconds_remaining` costs the WP model versus the real game
clock.

Purpose: CONTEXT §"Feature re-tests & training-data mix" locks recency weighting as exponential
decay by game date with the half-life tuned as part of the experiment, multiplying into the
existing `Total_W_Scaled` weight on EP; and locks the real-clock work as an IFAF-only
sub-experiment comparing real `game_clock_ms`-derived time against the synthetic construction on
the same games, with the production feature set staying synthetic until Hudl delivers real time
(REQ-S1-02 outcome). This is also the second half of the folded IFAF/cpx.studio todo.

Output: `add_recency_weight` and a `real_clock` path in `prepare_wp_data`, the half-life grid in
`hyperparams.py`, two more candidate specs, and their recorded verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md
@.planning/phases/01.3-methodical-model-retraining/01.3-RESEARCH.md
@.planning/phases/01.3-methodical-model-retraining/01.3-PATTERNS.md
@.planning/phases/01.3-methodical-model-retraining/01.3-DATA-PROFILE.md
@.planning/phases/01.3-methodical-model-retraining/01.3-07-SUMMARY.md

<interfaces>
<!-- Contracts this plan extends. Extracted from the codebase and plans 01.3-04/06/07. -->

src/flag_football_ep/features/mutations.py (current, before this plan):
  def prepare_wp_data(df: pl.DataFrame) -> pl.DataFrame
      # lines 190-231: builds play_id_half, play_time, then
      #   half_seconds_remaining = 1200 - play_time.cum_sum().over(["game_id","half"])
      #   where play_time = 0 on play_id_half==1 else 1200 / play_id_half.max() over (game_id, half)
      #   game_seconds_remaining = half==2 ? half_seconds_remaining : 2400 - play_time.cum_sum().over("game_id")
      #   elapsed_share = (2400 - game_seconds_remaining) / 2400
      #   Diff_Time_Ratio = score_differential / exp(-4 * elapsed_share)
      # module docstring lines 12-15 declare half_seconds_remaining SYNTHETIC and say
      #   "do not source it from game_clock_ms here" -- this plan adds an opt-in path and
      #   must update that wording rather than contradict it silently.

  def make_ep_model_mutations(df, selected_columns) -> pl.DataFrame
      # lines 548-568: Drive_Score_Dist_W, ScoreDiff_W, Total_W = sum of the two,
      #   Total_W_Scaled = (Total_W - Total_W.min()) / (Total_W.max() - Total_W.min())
      # guarded by DegenerateWeightRange when either input range is zero (lines 530-546)
  class DegenerateWeightRange(ValueError)
  class MissingFeatureColumns(ValueError)

src/flag_football_ep/canonical.py:
  NULLABLE_EXTRAS includes  "game_date": pl.Utf8            # nullable, per-source coverage varies
                            "game_clock_ms": pl.Int64        # populated by ingest/ifaf.py:329 from context["gameClockMs"]
                            "half_seconds_remaining": pl.Float64
  CORE_COLUMNS includes     "source": pl.Utf8, "half": pl.Int32, "game_id": pl.Utf8

src/flag_football_ep/model/experiments.py (plan 01.3-07):
  @dataclass(frozen=True) class CandidateSpec:
      name: str
      build: Callable[[pl.DataFrame, Config], tuple[pl.DataFrame, list[str]]]
      applies_to: tuple[str, ...]
  @dataclass(frozen=True) class CandidateResult:
      name, model_prefix, control_logloss, candidate_logloss, delta, adopted, n_folds, n_plays,
      control_features, candidate_features
  CANDIDATES: dict[str, CandidateSpec]        # already holds "half" and "competition_tier"
  class ExperimentError(ValueError)
  def candidate_experiment_name(model_prefix: str, config: Config) -> str
  def run_candidate(*, plays, config, model_prefix, spec, tune=False, max_evals=100) -> CandidateResult

src/flag_football_ep/model/hyperparams.py (plans 01.3-04 / 01.3-07):
  WP_FEATURES includes "half_seconds_remaining", "game_seconds_remaining", "Diff_Time_Ratio"
  GROUP_COLUMNS = ("game_id", "play_id", "source") ; LOGO_GROUP_COLUMN = "game_id"
  CANDIDATE_EXPERIMENT_SUFFIX ; CANDIDATE_ADOPTION_MIN_DELTA
</interfaces>

**Recency weighting is a weight change, not a feature change.** CONTEXT: "on EP it multiplies
with the existing `Total_W_Scaled` weight". So the recency candidate's two arms differ in
`sample_weight`, not in the feature list — `run_candidate`'s existing feature-list contract does
not express that, and Task 2 extends the harness for it. `Total_W_Scaled` itself stays
untouched (CONTEXT §"Existing EP weighting": "not re-tested").

**RESEARCH Pitfall 4 is live.** `game_date` is a nullable extra with per-source coverage that
`01.3-DATA-PROFILE.md` now measures. Read those numbers before writing the null policy — a
silent NaN weight for a whole source would invalidate the experiment.

**The real-clock arm must not change production.** CONTEXT: "the production feature set stays
synthetic until Hudl delivers real time (REQ-S1-02 outcome)". `WP_FEATURES` is not edited by
this plan.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exponential recency weighting with an explicit missing-date policy</name>
  <files>src/flag_football_ep/model/hyperparams.py, src/flag_football_ep/features/mutations.py, tests/test_features_mutations.py</files>
  <read_first>
    - src/flag_football_ep/features/mutations.py (whole file — `make_ep_model_mutations` lines ~492-575 with the `Total_W_Scaled` computation at 565-568 and the `DegenerateWeightRange` guards at 530-546)
    - src/flag_football_ep/model/hyperparams.py (the constant style and provenance-comment convention)
    - src/flag_football_ep/canonical.py (`NULLABLE_EXTRAS["game_date"]` — nullable Utf8, not a `CORE_COLUMNS` guarantee)
    - .planning/phases/01.3-methodical-model-retraining/01.3-DATA-PROFILE.md (§"game_date" — the measured per-source null rate and min/max dates that decide the null policy)
    - tests/test_features_mutations.py (`test_total_w_scaled_has_no_nan_no_inf_and_is_bounded` around line 700 — the invariant that must keep passing)
    - .planning/phases/01.3-methodical-model-retraining/01.3-RESEARCH.md (Pitfall 4)
    - .planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md (§"Recency weighting", §"Existing EP weighting")
  </read_first>
  <behavior>
    - `RECENCY_HALF_LIFE_DAYS_GRID` is a non-empty tuple of positive numbers spanning at least one order of magnitude.
    - `add_recency_weight(df, half_life_days)` adds a `Recency_W` Float64 column computed as `0.5 ** (age_days / half_life_days)`, where `age_days` is the number of days between a row's `game_date` and the most recent `game_date` in the frame.
    - The most recent game gets `Recency_W == 1.0`; a game exactly one half-life older gets `0.5` within 1e-9.
    - `Recency_W` is strictly positive and never NaN or infinite for every row, including rows with a null `game_date`.
    - Rows with a null `game_date` receive `RECENCY_NULL_DATE_WEIGHT`, and the count of such rows is returned so the caller can report it.
    - `add_recency_weight` raises `MissingFeatureColumns` naming `game_date` when the column is absent entirely.
    - `add_recency_weight` raises `DegenerateWeightRange` naming the observed date when every non-null `game_date` is identical, because a single-date corpus makes recency weighting meaningless.
    - `add_recency_weight` parses `game_date` values that are ISO `YYYY-MM-DD` strings and raises a named error listing the offending values for anything it cannot parse.
    - `Total_W_Scaled` remains bounded in [0, 1] with no NaN or inf after the recency factor is applied.
  </behavior>
  <action>
    Add to `src/flag_football_ep/model/hyperparams.py`, in the established constant style with a
    provenance comment citing REQ-S1-09 and CONTEXT §"Recency weighting" (half-life tuned as part
    of the experiment; the grid itself is Claude's discretion):
    - `RECENCY_HALF_LIFE_DAYS_GRID: tuple[float, ...] = (90.0, 180.0, 365.0, 730.0)` — roughly a
      season quarter, half a season, one year and two years, so the grid brackets both "only the
      last campaign matters" and "history barely decays" over a corpus whose date span is
      recorded in `01.3-DATA-PROFILE.md`.
    - `RECENCY_NULL_DATE_WEIGHT: float = 1.0` — the weight given to rows whose `game_date` is
      null, with a comment stating the policy explicitly: a dateless row is treated as
      *undiscounted* rather than dropped or zero-weighted, so a source with poor date coverage
      keeps its full influence instead of silently vanishing from the fit; RESEARCH Pitfall 4
      warns that the alternative is an invisible NaN. If `01.3-DATA-PROFILE.md` shows a
      meaningful null rate for a major source, say so in the comment and in the SUMMARY, and
      state that the recency verdict is therefore effectively measured on the dated subset.

    Add `add_recency_weight(df: pl.DataFrame, half_life_days: float) -> tuple[pl.DataFrame, int]`
    to `src/flag_football_ep/features/mutations.py`, returning the frame with the new `Recency_W`
    column and the count of null-date rows. Parse `game_date` with
    `pl.col("game_date").str.to_date(strict=False)`, compute the reference date as the maximum
    non-null parsed date across the frame, derive `age_days` as the whole-day difference, and
    compute `Recency_W = 0.5 ** (age_days / half_life_days)`, filling nulls with
    `RECENCY_NULL_DATE_WEIGHT`. Raise `MissingFeatureColumns` naming `game_date` when the column
    is missing, `DegenerateWeightRange` when all non-null dates are equal (mirroring the existing
    guards' message shape — name the value and say why the computation is meaningless), and a
    named error listing the distinct unparseable values when `str.to_date(strict=False)` produces
    nulls for rows whose raw `game_date` was non-null.

    Extend `make_ep_model_mutations` with an optional keyword-only parameter
    `recency_weight_column: str | None = None`. When it is None the function behaves exactly as
    today. When it is set, multiply that column into `Total_W` *before* the existing min-max
    rescale, i.e. `Total_W = (Drive_Score_Dist_W + ScoreDiff_W) * pl.col(recency_weight_column)`,
    leaving the `Total_W_Scaled` min-max formula and its `DegenerateWeightRange` guards
    untouched. Do not change `Drive_Score_Dist_W` or `ScoreDiff_W` — CONTEXT keeps the existing
    weighting as an untouched given.

    Extend `tests/test_features_mutations.py` with tests for every bullet in `<behavior>`,
    including a two-date frame where the expected `0.5` at exactly one half-life is computable by
    hand, and a re-run of the existing bounded/no-NaN `Total_W_Scaled` invariant with the recency
    factor applied.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_features_mutations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_features_mutations.py -k "recency or total_w_scaled" -q` passes and collects at least 9 tests.
    - `.venv/bin/python -c "import polars as pl; from flag_football_ep.features.mutations import add_recency_weight; d=pl.read_parquet('data/processed/plays.parquet'); out,n=add_recency_weight(d,180.0); w=out['Recency_W']; assert w.null_count()==0; assert w.min()>0; assert w.max()<=1.0+1e-9; print('RECENCY_OK', round(w.min(),6), round(w.max(),6), 'null_date_rows', n)"` prints `RECENCY_OK` with a positive minimum.
    - `.venv/bin/python -c "from flag_football_ep.model.hyperparams import RECENCY_HALF_LIFE_DAYS_GRID as g, RECENCY_NULL_DATE_WEIGHT as w; assert len(g)>=3 and min(g)>0 and max(g)/min(g)>=10; assert w>0; print('GRID_OK', g, w)"` prints `GRID_OK`.
    - `grep -n "Drive_Score_Dist_W" src/flag_football_ep/features/mutations.py` still shows the original max-minus-value formula unchanged.
    - The null-date row count printed above matches the `game_date` null totals in `01.3-DATA-PROFILE.md`.
    - `.venv/bin/pytest -q` passes (full suite).
  </acceptance_criteria>
  <done>An exponential recency factor multiplies into the EP sample weight with a documented, non-silent policy for rows lacking a game date, and the existing weight invariants still hold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the recency candidate across the half-life grid</name>
  <files>src/flag_football_ep/model/experiments.py, tests/test_model_experiments.py</files>
  <read_first>
    - src/flag_football_ep/model/experiments.py (whole file as written by plan 01.3-07 — `CandidateSpec`, `CandidateResult`, `run_candidate`, `CANDIDATES`, `candidate_experiment_name`)
    - src/flag_football_ep/features/mutations.py (as extended in Task 1 — `add_recency_weight`, `make_ep_model_mutations(..., recency_weight_column=...)`)
    - src/flag_football_ep/model/hyperparams.py (`RECENCY_HALF_LIFE_DAYS_GRID`, `CANDIDATE_ADOPTION_MIN_DELTA`)
    - src/flag_football_ep/model/evaluate.py (`run_logo`'s `weight_column` parameter)
    - .planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md (§"Recency weighting" — half-life tuned as part of the experiment; §"Adoption criterion")
  </read_first>
  <behavior>
    - `run_recency_candidate(plays, config, model_prefix)` evaluates one arm per entry of `RECENCY_HALF_LIFE_DAYS_GRID` plus the unweighted control, all on the same frame and the same folds.
    - It returns the best-performing `CandidateResult` together with the per-half-life results, and the returned result's `name` identifies the winning half-life.
    - `adopted` is True only when the best half-life's pooled log-loss beats the control by more than `CANDIDATE_ADOPTION_MIN_DELTA`.
    - Every arm — control and each half-life — is logged as its own MLflow run in the candidate experiment, tagged `candidate=recency` and carrying a `half_life_days` param.
    - The candidate arms differ from the control in sample weight only: their feature lists are identical.
    - For `model_prefix="wp"` the candidate is skipped with a recorded reason, because `train_wp` fits without sample weights (`weight_column=None`).
    - The recency entry is registered in `CANDIDATES` and its `applies_to` is `("ep",)`.
  </behavior>
  <action>
    Extend `src/flag_football_ep/model/experiments.py` so a candidate can vary the *sample weight*
    rather than the feature list. Recency weighting is such a candidate: CONTEXT specifies it
    multiplies into `Total_W_Scaled` on EP, and `run_candidate`'s current
    feature-list-only contract cannot express it.

    Add an optional `weight_build: Callable[[pl.DataFrame, Config, float], tuple[pl.DataFrame, str]] | None = None`
    field to `CandidateSpec` (defaulting to None so the existing `half` and `competition_tier`
    specs are unaffected), and a `run_recency_candidate(*, plays, config, model_prefix) -> tuple[CandidateResult, list[CandidateResult]]`
    function that:
    - Raises `ExperimentError` naming the prefix for anything other than `"ep"`, and is registered
      in `CANDIDATES` with `applies_to=("ep",)` so `ffep experiment --model both` skips it for WP
      with a printed reason rather than silently producing a meaningless arm.
    - Builds the model frame once: filter excluded ids, `add_recency_weight(plays, half_life)`,
      `prepare_ep_data`, `make_ep_model_mutations(..., recency_weight_column="Recency_W")` with
      `selected_columns = [*EP_TRAINING_COLUMNS]` (the weight column is `Total_W_Scaled`, already
      selected — `Recency_W` is consumed inside the mutation and does not need to survive), then
      `drop_nulls()`. Because `Total_W_Scaled` differs per half-life, build one frame per grid
      entry; assert every arm's frame height equals the control frame's height and raise
      `ExperimentError` naming the counts otherwise, so the comparison stays like-for-like.
    - Measures the control arm with the unmodified `make_ep_model_mutations` (no
      `recency_weight_column`) and each candidate arm with its half-life-specific frame, all via
      `run_logo(features=EP_FEATURES, weight_column="Total_W_Scaled", ...)`. The feature list is
      identical across arms.
    - Logs one MLflow run per arm in `candidate_experiment_name("ep", config)` with tag
      `candidate=recency`, params `half_life_days` (or `control` for the baseline arm) and
      `verdict`, and metrics `candidate_logloss` plus `delta` versus the control.
    - Picks the arm with the lowest pooled log-loss, sets `adopted` per
      `CANDIDATE_ADOPTION_MIN_DELTA`, and returns it plus the full per-half-life list.

    Run the real experiment: `.venv/bin/ffep experiment --candidate recency --model ep`. Record
    in this plan's SUMMARY a row per half-life (half-life, pooled log-loss, delta versus control)
    plus the winning half-life and the verdict, and the null-date row count from Task 1 so the
    reader can judge how much of the corpus the weighting actually discriminated.

    Do not edit `EP_FEATURES` or wire recency into `train_ep` — adoption is plan 01.3-09's
    decision once all candidates have verdicts.

    Extend `tests/test_model_experiments.py` with tests for every bullet in `<behavior>`, using
    the synthetic corpus with injected `game_date` values spanning more than one half-life.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_model_experiments.py -k recency -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_model_experiments.py -k recency -q` passes and collects at least 6 tests.
    - `.venv/bin/python -c "from flag_football_ep.model.experiments import CANDIDATES; s=CANDIDATES['recency']; assert s.applies_to==('ep',); print('RECENCY_SPEC_OK')"` prints `RECENCY_SPEC_OK`.
    - `.venv/bin/ffep experiment --candidate recency --model ep` exits 0 and prints one line per half-life plus a winning verdict.
    - The SUMMARY contains a table with one row per entry of `RECENCY_HALF_LIFE_DAYS_GRID` plus a control row, each with pooled log-loss and delta.
    - `grep -v '^#' src/flag_football_ep/model/hyperparams.py | grep -c "Recency_W"` is 0 (recency is not yet in the production feature/weight path).
    - `.venv/bin/pytest -q` passes (full suite).
  </acceptance_criteria>
  <done>Recency weighting has a grouped-CV verdict with its half-life selected by measurement across the grid, logged per arm in MLflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run the IFAF-only real-clock WP sub-experiment</name>
  <files>src/flag_football_ep/features/mutations.py, src/flag_football_ep/model/experiments.py, tests/test_features_mutations.py, tests/test_model_experiments.py</files>
  <read_first>
    - src/flag_football_ep/features/mutations.py (module docstring lines 12-15 declaring `half_seconds_remaining` SYNTHETIC and "do not source it from `game_clock_ms` here"; `prepare_wp_data` lines 178-231 with the exact synthetic construction)
    - src/flag_football_ep/ingest/ifaf.py (line ~329 — `"game_clock_ms": context.get("gameClockMs")`, the source of the real clock)
    - src/flag_football_ep/canonical.py (`NULLABLE_EXTRAS["game_clock_ms"]: pl.Int64`)
    - .planning/phases/01.3-methodical-model-retraining/01.3-DATA-PROFILE.md (§"game_clock_ms" — the measured per-source coverage that decides whether this experiment is viable at all)
    - src/flag_football_ep/model/experiments.py (as extended in Task 2)
    - .planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md (§"Real `half_seconds_remaining`" — IFAF-only sub-experiment, production stays synthetic; §"Folded Todos")
    - .planning/STATE.md (Blockers — REQ-S1-02 may not be deliverable by the analyst)
  </read_first>
  <behavior>
    - `prepare_wp_data(df)` with no keyword behaves exactly as before: `half_seconds_remaining` is the synthetic `1200 / max(play_id_half)` construction.
    - `prepare_wp_data(df, real_clock=True)` derives `half_seconds_remaining` from `game_clock_ms` as seconds remaining in the half, and derives `game_seconds_remaining`, `elapsed_share` and `Diff_Time_Ratio` from it with the same formulas the synthetic path uses.
    - `prepare_wp_data(df, real_clock=True)` raises `MissingFeatureColumns` naming `game_clock_ms` when that column is absent, and a named error reporting the null count when any row in the frame has a null `game_clock_ms`.
    - `half_seconds_remaining` from the real-clock path is non-increasing within each `(game_id, half)` group.
    - `run_real_clock_experiment(plays, config)` restricts the frame to `source == "ifaf"` before either arm runs, and raises `ExperimentError` naming the source and the row count when that subset is empty or has fewer than 2 distinct `game_id` values.
    - It measures two WP arms on the same IFAF games and the same folds: synthetic time versus real clock, with identical feature lists.
    - It logs both arms to the WP candidate experiment tagged `candidate=real_clock` with an `arm` param of `synthetic` or `real_clock`, and records the delta.
    - `WP_FEATURES` is unchanged by this plan.
  </behavior>
  <action>
    Add a keyword-only `real_clock: bool = False` parameter to `prepare_wp_data` in
    `src/flag_football_ep/features/mutations.py`. When False, the function's body is exactly what
    it is today — do not restructure the synthetic path. When True, replace only the
    `play_time`/`half_seconds_remaining` derivation: compute
    `half_seconds_remaining = pl.col("game_clock_ms") / 1000.0` (cast to Float64), then derive
    `game_seconds_remaining` with the existing rule (`half == 2` uses `half_seconds_remaining`
    directly, otherwise `1200 + half_seconds_remaining` for the first half, matching the
    synthetic path's 2400-second game frame), and keep `elapsed_share` and `Diff_Time_Ratio`
    computed by the same formulas so the only thing differing between the two arms is where time
    comes from. Raise `MissingFeatureColumns` naming `game_clock_ms` when absent, and a named
    error reporting the null count when any row's `game_clock_ms` is null — a partially populated
    clock would silently make the two arms incomparable.

    Update the module docstring (lines 12-15): keep the statement that the *default* and
    *production* `half_seconds_remaining` is synthetic and gates Phase 1.4's WP charts, and add
    that `real_clock=True` is an opt-in path used solely by the REQ-S1-09 IFAF sub-experiment to
    quantify what the synthetic construction costs, and that it must not be enabled in the
    production WP feature path until REQ-S1-02 delivers real Hudl time. Replace the current
    absolute "do not source it from `game_clock_ms` here" wording with this narrower rule so the
    docstring and the code agree.

    Add `run_real_clock_experiment(*, plays: pl.DataFrame, config: Config) -> tuple[CandidateResult, CandidateResult]`
    to `src/flag_football_ep/model/experiments.py`, registered in `CANDIDATES` under
    `real_clock` with `applies_to=("wp",)`. It filters `plays` to `source == "ifaf"` first (both
    arms see the same games — CONTEXT: "comparing real `game_clock_ms`-derived time vs the
    synthetic construction on the same games"), raises `ExperimentError` naming the source and
    counts when the subset is too small for LOGO, then builds two WP model frames from that
    subset — one with `prepare_wp_data(...)` and one with `prepare_wp_data(..., real_clock=True)` —
    runs `run_logo` on each with `features=WP_FEATURES`, `weight_column=None`, and logs both arms
    to `candidate_experiment_name("wp", config)` tagged `candidate=real_clock` with the `arm`
    param and the log-loss metrics plus the delta.

    Run it: `.venv/bin/ffep experiment --candidate real_clock --model wp`. Record in this plan's
    SUMMARY: the IFAF game and play counts the experiment ran on, both arms' pooled log-loss, the
    delta, and a one-paragraph reading of what synthetic time costs — this is the evidence
    STATE.md's REQ-S1-02 blocker and the analyst conversation need. If
    `01.3-DATA-PROFILE.md` §"game_clock_ms" shows the IFAF clock is not usably populated, do not
    fabricate a result: record that the experiment is not viable on the current corpus, say
    exactly which coverage number made it so, and leave the code path in place for when it is.

    Do not add `game_clock_ms` or the real-clock path to `WP_FEATURES` or to `train_wp`.

    Extend `tests/test_features_mutations.py` and `tests/test_model_experiments.py` with tests for
    every bullet in `<behavior>`, building a small synthetic IFAF-sourced frame with a
    monotonically decreasing `game_clock_ms`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_features_mutations.py tests/test_model_experiments.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_features_mutations.py -k real_clock -q` passes and collects at least 5 tests.
    - `.venv/bin/pytest tests/test_model_experiments.py -k real_clock -q` passes and collects at least 4 tests.
    - `.venv/bin/python -c "import inspect; from flag_football_ep.features.mutations import prepare_wp_data as p; assert 'real_clock' in inspect.signature(p).parameters; assert inspect.signature(p).parameters['real_clock'].default is False; print('SIG_OK')"` prints `SIG_OK`.
    - `.venv/bin/python -c "from flag_football_ep.model.hyperparams import WP_FEATURES; assert 'game_clock_ms' not in WP_FEATURES; print('PROD_UNCHANGED', WP_FEATURES)"` prints `PROD_UNCHANGED`.
    - `grep -n "REQ-S1-02" src/flag_football_ep/features/mutations.py` returns a match (the docstring still ties the synthetic default to the pending time-data requirement).
    - The SUMMARY records either both arms' log-loss with a delta, or an explicit not-viable finding citing the `game_clock_ms` coverage number from `01.3-DATA-PROFILE.md`.
    - `.venv/bin/pytest -q` passes (full suite).
  </acceptance_criteria>
  <done>The cost of synthetic time is quantified on IFAF games with real and synthetic arms on identical folds, or its non-viability is documented with the measurement that showed it; production WP features are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nullable `game_date` -> sample weight | A nullable source column now scales every EP training row's influence |
| Third-party `game_clock_ms` -> WP time features | IFAF-supplied clock values drive an experimental feature derivation |
| Experimental code path -> production feature set | `real_clock=True` and recency weighting exist in the same modules production uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.3-30 | Tampering | `add_recency_weight` null-date handling | mitigate | Null dates take an explicit `RECENCY_NULL_DATE_WEIGHT` constant and the null-row count is returned and reported, so RESEARCH Pitfall 4's silent NaN/zero-weight failure cannot occur unobserved; a single-date corpus raises `DegenerateWeightRange` |
| T-1.3-31 | Tampering | `game_clock_ms` -> `half_seconds_remaining` | mitigate | The real-clock path raises a named error on any null `game_clock_ms` rather than back-filling, so a partially populated third-party clock cannot silently make the two arms incomparable; monotonicity within each half is asserted by test |
| T-1.3-32 | Elevation of Privilege | experimental paths leaking into production | mitigate | `real_clock` defaults to False and `WP_FEATURES` is asserted unchanged; recency is asserted absent from `hyperparams.py`'s production path; both are gated behind explicit acceptance-criteria greps and adoption is deferred to plan 01.3-09 |
| T-1.3-33 | Repudiation | recency half-life choice | mitigate | Every half-life arm is its own MLflow run with its `half_life_days` param and log-loss, so the winning half-life is a recorded measurement rather than an unattributable constant |
| T-1.3-34 | Tampering | IFAF subset selection | mitigate | The subset filter is a fixed `source == "ifaf"` predicate applied before either arm, and `ExperimentError` names the source and row count when the subset cannot support LOGO — the comparison cannot silently run on a different or too-small population |
| T-1.3-SC | Tampering | package installs | mitigate | No package installs in this plan; RESEARCH `## Package Legitimacy Audit` records zero new external packages for this phase |
</threat_model>

<verification>
- `.venv/bin/pytest -q` (full suite) passes.
- `mlflow ui --backend-store-uri sqlite:///$(pwd)/mlruns/mlflow.db` shows `candidate=recency` runs (one per half-life plus control) in the EP candidate experiment and `candidate=real_clock` runs (two arms) in the WP candidate experiment.
- `WP_FEATURES` and `EP_FEATURES` are unchanged; no candidate has been folded into production.
- The SUMMARY records the recency half-life table with a verdict and the real-clock comparison (or its documented non-viability).
</verification>

<success_criteria>
- Recency weighting has a documented grouped-CV outcome with a measured half-life (REQ-S1-09).
- The real-clock versus synthetic-time comparison is documented on IFAF games, completing the folded IFAF/cpx.studio todo's calibration half.
- Missing `game_date` and missing `game_clock_ms` are handled by explicit, reported policies — never silently.
- Production feature sets and weights are unchanged; all four candidate verdicts now exist for plan 01.3-09 to act on together.
</success_criteria>

<output>
Create `.planning/phases/01.3-methodical-model-retraining/01.3-08-SUMMARY.md` when done
</output>
