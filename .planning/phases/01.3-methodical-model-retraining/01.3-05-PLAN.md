---
phase: 01.3-methodical-model-retraining
plan: 05
type: execute
wave: 3
depends_on: [01.3-03, 01.3-04]
files_modified:
  - src/flag_football_ep/model/evaluate.py
  - src/flag_football_ep/model/train.py
  - tests/test_model_evaluate.py
  - tests/test_model_train.py
autonomous: true
requirements: [REQ-S1-08, REQ-S1-11]

must_haves:
  truths:
    - "The training run carries a reliability curve per EP class and for the WP model, computed from out-of-fold predictions"
    - "The training run reports model log-loss against a class-frequency naive baseline, so a model that beats nothing is visible as such"
    - "Pooled metrics are broken down per source (legacy, hudl, sportapp, ifaf), so a source that hurts the model is visible"
    - "Out-of-fold predictions are persisted keyed on (game_id, play_id), ready for Phase 1.4's own-team EPA reporting"
    - "The production refit is registered as a model version in the MLflow registry"
  artifacts:
    - path: "src/flag_football_ep/model/evaluate.py"
      provides: "Naive baseline, per-class reliability curves, per-source breakdown, out-of-fold persistence"
      exports: ["naive_baseline_logloss", "reliability_curves", "per_source_metrics", "oof_frame", "write_oof_predictions", "render_reliability_figure"]
    - path: "data/processed/oof_predictions_ep.parquet"
      provides: "Out-of-fold EP class probabilities keyed on (game_id, play_id)"
    - path: "data/processed/oof_predictions_wp.parquet"
      provides: "Out-of-fold WP probabilities keyed on (game_id, play_id)"
  key_links:
    - from: "src/flag_football_ep/model/train.py"
      to: "src/flag_football_ep/model/evaluate.py"
      via: "_train computes calibration artifacts from LogoResult and hands them to _log_run"
      pattern: "reliability_curves|naive_baseline_logloss|per_source_metrics"
    - from: "src/flag_football_ep/model/train.py"
      to: "src/flag_football_ep/model/registry.py"
      via: "_log_run registers the production refit as a model version"
      pattern: "register_production_model"
    - from: "src/flag_football_ep/model/evaluate.py"
      to: "data/processed/oof_predictions_{ep,wp}.parquet"
      via: "write_oof_predictions(config, model_prefix, ...)"
      pattern: "oof_predictions_"
---

<objective>
Turn the out-of-fold predictions from plan 01.3-04 into the calibration report REQ-S1-08 asks
for — per-class reliability curves and log-loss against a naive baseline, broken down per
source — log it all as MLflow run artifacts, persist the out-of-fold predictions for Phase 1.4,
and register the production refit as a registry model version.

Purpose: CONTEXT §"Calibration & training report" locks the class-frequency naive baseline,
MLflow run artifacts as the report form (reliability-curve PNGs plus a metrics table, browsable
in the MLflow UI), report-only miscalibration handling, and the per-source breakdown that
realises the folded IFAF-benchmarking todo. CONTEXT §"Grouped-CV protocol" additionally
requires the out-of-fold predictions to be persisted so Phase 1.4's own-team EPA is not
flattered by in-sample predictions.

Output: calibration and breakdown functions in `evaluate.py`, extended `_log_run` artifacts,
`data/processed/oof_predictions_{ep,wp}.parquet`, and registry registration of the shipped model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md
@.planning/phases/01.3-methodical-model-retraining/01.3-RESEARCH.md
@.planning/phases/01.3-methodical-model-retraining/01.3-PATTERNS.md
@.planning/phases/01.3-methodical-model-retraining/01.3-03-SUMMARY.md
@.planning/phases/01.3-methodical-model-retraining/01.3-04-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes. Extracted from the codebase and from plans 01.3-03/01.3-04. -->

src/flag_football_ep/model/evaluate.py (plan 01.3-04):
  @dataclass(frozen=True) class LogoResult:
      oof_pred: np.ndarray       # (n_rows, n_outputs); EP n_outputs=5, WP n_outputs=1
      oof_label: np.ndarray
      oof_game_id: np.ndarray
      oof_play_id: np.ndarray
      oof_source: np.ndarray
      n_folds: int
      wall_seconds: float
  class EvaluationError(ValueError)
  def run_logo(*, model_data, features, fixed_params, weight_column, group_column=LOGO_GROUP_COLUMN) -> LogoResult

src/flag_football_ep/model/registry.py (plan 01.3-03):
  CHAMPION_ALIAS: str = "champion"
  class RegistryError(ValueError)
  def registered_model_name(model_prefix: str) -> str          # "ep" -> "ep_model"
  def register_production_model(model, name: str, config: Config) -> str   # call inside an active run
  def resolve_champion(name: str, config: Config) -> str

src/flag_football_ep/model/train.py (after plan 01.3-04):
  def _train(*, plays, config, experiment, exclude_ids, prepare_fn, mutate_fn, selected_columns,
             features, fixed_params, metric_name, weight_column, model_prefix, tune, max_evals,
             export_pkl) -> str
  def _log_run(*, experiment, params, metrics, model, feature_names, config, trial_losses=None) -> str
      # currently: ensure_experiment -> start_run -> log_params -> log_metric per metric
      #            -> stepped tune_logloss -> set_tag("phase", ...) -> mlflow.xgboost.log_model(model, name="model")

src/flag_football_ep/model/hyperparams.py:
  EP_PROB_LABELS: list[str] = ["Touchdown_Prob", "Opp_Touchdown_Prob", "Safety_Prob", "Opp_Safety_Prob", "No_Score_Prob"]
  EP_PARAMS["num_class"] = 5 ; WP_PARAMS has no num_class (binary)

Verified-available scikit-learn 1.5.1 primitives (RESEARCH Patterns 2 and 3):
  from sklearn.calibration import calibration_curve
      # (y_true, y_prob, *, pos_label=None, n_bins=5, strategy="uniform") -> (prob_true, prob_pred)
  from sklearn.metrics import log_loss   # (y_true, y_pred, *, normalize=True, sample_weight=None, labels=None)

MLflow artifact logging (already a dependency, used by _log_run):
  mlflow.log_figure(figure, artifact_file)   # matplotlib Figure -> PNG artifact
  mlflow.log_text(text, artifact_file)
  mlflow.log_metric(key, value)
</interfaces>

**Scope guard — report only.** CONTEXT §"Calibration & training report" explicitly defers any
isotonic/Platt calibration *correction* to a later decision. Do not import or use
`CalibratedClassifierCV`. If the curves look bad, that is a finding for the SUMMARY, not a code
change in this phase.

**Multiclass note (RESEARCH Anti-Patterns).** `CalibrationDisplay.from_estimator` assumes one
fitted estimator; LOGO produces accumulated prediction arrays with no single estimator object.
Call `calibration_curve` directly, once per class, one-vs-rest.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add naive baseline, per-class reliability curves and per-source breakdown</name>
  <files>src/flag_football_ep/model/evaluate.py, tests/test_model_evaluate.py</files>
  <read_first>
    - src/flag_football_ep/model/evaluate.py (whole file as written by plan 01.3-04 — `LogoResult`, `EvaluationError`, `run_logo`)
    - src/flag_football_ep/model/hyperparams.py (`EP_PROB_LABELS`, `EP_PARAMS["num_class"]`, `WP_PARAMS`)
    - tests/test_model_evaluate.py (as written by plan 01.3-04 — the fixture style to extend)
    - .planning/phases/01.3-methodical-model-retraining/01.3-RESEARCH.md (Pattern 2 per-class reliability curve, Pattern 3 naive class-frequency baseline, "Don't Hand-Roll" row on reliability-curve binning)
    - .planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md (§"Calibration & training report" — naive baseline, report form, miscalibration policy, per-source breakdown)
  </read_first>
  <behavior>
    - `naive_baseline_logloss(labels, num_class)` predicts the marginal class distribution of `labels` for every row and returns its log-loss; for a perfectly balanced 2-class array the value equals `log(2)` within 1e-9.
    - `naive_baseline_logloss` computes the class frequencies from the labels passed in and never assumes 0.5 for the binary case.
    - `reliability_curves(oof_pred, oof_label, class_names, n_bins)` returns one entry per class name for a multiclass input, and exactly one entry for a single-column (binary) input.
    - Each returned entry carries `prob_true` and `prob_pred` arrays of equal length, obtained from `sklearn.calibration.calibration_curve` on the one-vs-rest binarised labels.
    - `reliability_curves` raises `EvaluationError` naming the mismatch when `len(class_names)` does not equal `oof_pred.shape[1]`.
    - `per_source_metrics(oof_pred, oof_label, oof_source, num_class)` returns one row per distinct source with columns `source`, `n_plays`, `n_games` is not required, `logloss`, `naive_logloss`, `improvement`; `improvement` equals `naive_logloss - logloss`.
    - `per_source_metrics` includes a row with source `__pooled__` carrying the pooled numbers.
    - `per_source_metrics` computes each source's naive baseline from that source's own label distribution.
    - `render_reliability_figure(curves, title)` returns a matplotlib `Figure` with one subplot per curve, each containing the perfect-calibration diagonal, and does not call `plt.show`.
  </behavior>
  <action>
    Extend `src/flag_football_ep/model/evaluate.py` with the reporting half of the evaluation
    protocol. Add, with these exact names:

    - `naive_baseline_logloss(labels: np.ndarray, num_class: int) -> float` — build
      `class_freqs = np.bincount(labels.astype(int), minlength=num_class) / len(labels)`, tile it
      to `(len(labels), num_class)`, and return `float(log_loss(labels, naive_pred, labels=list(range(num_class))))`.
      For `num_class == 1` (the binary WP case) use the marginal positive rate and the binary
      `log_loss` form. CONTEXT locks this class-frequency baseline; do not substitute a
      down/distance-bucket baseline (explicitly deferred).
    - `@dataclass(frozen=True) class ReliabilityCurve` with fields `name: str`,
      `prob_true: np.ndarray`, `prob_pred: np.ndarray`, `n_bins: int`.
    - `reliability_curves(oof_pred: np.ndarray, oof_label: np.ndarray, class_names: Sequence[str], n_bins: int = 10) -> list[ReliabilityCurve]`
      — one-vs-rest per class: `y_true_binary = (oof_label == class_idx).astype(int)`,
      `y_prob_class = oof_pred[:, class_idx]`, then `calibration_curve(y_true_binary, y_prob_class, n_bins=n_bins)`.
      For a single-column `oof_pred` treat it as the binary positive-class probability and produce
      exactly one curve. Do not hand-roll the binning — `calibration_curve` already handles empty
      and degenerate bins (RESEARCH "Don't Hand-Roll").
    - `per_source_metrics(oof_pred: np.ndarray, oof_label: np.ndarray, oof_source: np.ndarray, num_class: int) -> pl.DataFrame`
      — group the out-of-fold rows by source, compute `log_loss` and `naive_baseline_logloss` on
      each group's own rows and own label distribution, and emit the columns listed in
      `<behavior>` plus a `__pooled__` row. Pass `labels=list(range(num_class))` to `log_loss` so a
      source that never sees a rare class does not raise. This per-source table is how CONTEXT's
      folded IFAF-benchmarking todo is realised — the IFAF WM-2026 rows train like any other
      source and are simply reported separately.
    - `render_reliability_figure(curves: Sequence[ReliabilityCurve], title: str) -> "matplotlib.figure.Figure"`
      — import matplotlib with the `Agg` backend (`matplotlib.use("Agg")` before `pyplot`, so the
      function works headless in tests and CI), lay the curves out as small multiples, plot each
      curve's `prob_pred` against `prob_true` with the `y = x` perfect-calibration diagonal, label
      axes "mean predicted probability" and "observed frequency", set the supplied title, and
      return the `Figure` without showing or saving it. The caller owns logging it to MLflow.

    Raise `EvaluationError` (the module's existing named exception) for every malformed-input
    case, naming the offending shape or class-name list.

    Extend `tests/test_model_evaluate.py` with tests for every bullet in `<behavior>`, including
    a hand-built balanced binary array for the `log(2)` assertion and a hand-built two-source
    array for the per-source breakdown so the expected numbers are computable by hand. Never
    assert on model quality.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_model_evaluate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_model_evaluate.py -k "naive or reliability or per_source or figure" -q` passes and collects at least 9 tests.
    - `grep -n "from sklearn.calibration import calibration_curve" src/flag_football_ep/model/evaluate.py` returns a match.
    - `grep -c "CalibratedClassifierCV" src/flag_football_ep/model/evaluate.py` is 0 (correction is out of scope).
    - `grep -n 'matplotlib.use("Agg")' src/flag_football_ep/model/evaluate.py` returns a match.
    - `.venv/bin/python -c "import numpy as np; from flag_football_ep.model.evaluate import naive_baseline_logloss; print(abs(naive_baseline_logloss(np.array([0,1,0,1]),2)-np.log(2))<1e-9)"` prints `True`.
  </acceptance_criteria>
  <done>`evaluate.py` computes the naive baseline, per-class reliability curves and the per-source metric table from out-of-fold arrays, and renders the curves as a headless matplotlib figure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist out-of-fold predictions as a Phase 1.4-facing contract</name>
  <files>src/flag_football_ep/model/evaluate.py, tests/test_model_evaluate.py</files>
  <read_first>
    - src/flag_football_ep/model/evaluate.py (as extended in Task 1)
    - src/flag_football_ep/pipeline.py (`_atomic_write_parquet` around line 114 — the existing atomic-write helper and its temp-file-then-rename discipline)
    - src/flag_football_ep/config.py (`Paths.processed`)
    - src/flag_football_ep/model/hyperparams.py (`EP_PROB_LABELS`, `WP_PROBABILITY_COLUMN` is in `features/mutations.py`)
    - src/flag_football_ep/features/mutations.py (line 38 `WP_PROBABILITY_COLUMN = "wp"` — the column name the WP out-of-fold frame should use)
    - .planning/phases/01.3-methodical-model-retraining/01.3-RESEARCH.md (Open Question 1 — recommended location/format and the instruction to document it as a Phase 1.4-facing contract)
    - .planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md (§"Grouped-CV protocol" > Out-of-fold EPA; §Integration Points on the 1.4-facing contract)
  </read_first>
  <behavior>
    - `oof_frame(logo, prob_labels)` returns a Polars frame with columns `game_id`, `play_id`, `source` and one Float64 column per entry of `prob_labels`, with exactly `len(logo.oof_label)` rows.
    - `oof_frame` raises `EvaluationError` naming the mismatch when `len(prob_labels)` does not equal `logo.oof_pred.shape[1]`.
    - `(game_id, play_id)` is unique across the returned frame.
    - `write_oof_predictions(logo, prob_labels, config, model_prefix)` writes `data/processed/oof_predictions_{model_prefix}.parquet` and returns its path.
    - Re-running `write_oof_predictions` replaces the file and leaves no `.tmp` residue in `config.paths.processed`.
    - An interrupted write never leaves a partially-written `oof_predictions_*.parquet` in place of a previously good one.
    - The written frame round-trips: reading it back and joining it onto the training frame on `(game_id, play_id)` matches every row.
  </behavior>
  <action>
    Add to `src/flag_football_ep/model/evaluate.py`:

    - `oof_frame(logo: LogoResult, prob_labels: Sequence[str]) -> pl.DataFrame` — assemble
      `game_id` (Utf8), `play_id` (Int32), `source` (Utf8) and one Float64 probability column per
      entry of `prob_labels` from `logo.oof_pred`'s columns, in `prob_labels` order. For EP pass
      `EP_PROB_LABELS` (5 columns, matching the EP model's `predict()` output order); for WP pass
      `[WP_PROBABILITY_COLUMN]` (i.e. `["wp"]`). Raise `EvaluationError` on a column-count mismatch.
    - `write_oof_predictions(logo: LogoResult, prob_labels: Sequence[str], config: Config, model_prefix: str) -> Path`
      — build the frame, `config.paths.processed.mkdir(parents=True, exist_ok=True)`, and write
      `config.paths.processed / f"oof_predictions_{model_prefix}.parquet"` through the same
      write-to-temp-then-`Path.replace` discipline `pipeline._atomic_write_parquet` already uses,
      so a crash mid-write cannot destroy a previously good file. Reuse
      `pipeline._atomic_write_parquet` directly if importing it does not create a circular import;
      otherwise replicate its exact shape and say so in a comment referencing it.

    Document the format in the function docstring as a **Phase 1.4-facing contract**: file names
    `oof_predictions_ep.parquet` and `oof_predictions_wp.parquet` under `config.paths.processed`,
    join key `(game_id, play_id)`, EP columns exactly `EP_PROB_LABELS` and WP column exactly `wp`,
    semantics "each historical play's probability as predicted by a model that never saw that
    play's game" (CONTEXT §Out-of-fold EPA, nflfastR precedent). State that Phase 1.4 joins these
    onto `plays.parquet` for own-team EPA, and that new/unseen games are scored with the refit
    champion model via `ffep score` instead.

    Extend `tests/test_model_evaluate.py` with tests for every bullet in `<behavior>`, using a
    `tmp_path`-scoped `Config` (never the real repo `data/`).

    Do not wire this into `_train` here — Task 3 does that in one place alongside the rest of the
    reporting.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_model_evaluate.py -k oof -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_model_evaluate.py -k oof -q` passes and collects at least 5 tests.
    - `grep -n "oof_predictions_" src/flag_football_ep/model/evaluate.py` returns a match.
    - `grep -n "Phase 1.4" src/flag_football_ep/model/evaluate.py` returns a match (the contract is documented at the code, not only in planning files).
    - `grep -n "replace(" src/flag_football_ep/model/evaluate.py` returns a match inside `write_oof_predictions` (atomic rename, not a bare `write_parquet` onto the live path).
  </acceptance_criteria>
  <done>Out-of-fold predictions serialise to a documented, atomically written Parquet contract keyed on `(game_id, play_id)`, one file per model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Log the calibration report and register the production model</name>
  <files>src/flag_football_ep/model/train.py, tests/test_model_train.py</files>
  <read_first>
    - src/flag_football_ep/model/train.py (whole file as rewired by plan 01.3-04 — `_train`'s LOGO/refit block and params dict, `_log_run` lines ~349-378)
    - src/flag_football_ep/model/evaluate.py (as extended in Tasks 1 and 2)
    - src/flag_football_ep/model/registry.py (plan 01.3-03 — `register_production_model`, `registered_model_name`)
    - src/flag_football_ep/model/hyperparams.py (`EP_PROB_LABELS`, `EP_PARAMS["num_class"]`)
    - src/flag_football_ep/features/mutations.py (line 38 — `WP_PROBABILITY_COLUMN`)
    - tests/test_model_train.py (whole file as updated by plan 01.3-04)
    - .planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md (§"Calibration & training report" — "MLflow run artifacts ... browsable in the MLflow UI. No separate standalone report file required")
  </read_first>
  <behavior>
    - After `train_ep(plays, config)`, the MLflow run has metrics `logo_mlogloss`, `naive_mlogloss` and `logloss_improvement`, where `logloss_improvement` equals `naive_mlogloss - logo_mlogloss`.
    - After `train_wp(plays, config)`, the run has metrics `logo_logloss`, `naive_logloss` and `logloss_improvement`.
    - The EP run's artifacts include a reliability-curve PNG covering all five `EP_PROB_LABELS` classes; the WP run's include one for the binary case.
    - The run's artifacts include a per-source metrics table logged as text (Markdown), containing one line per distinct source in the training frame plus the pooled row.
    - The run has one metric per source of the form `logo_logloss_by_source_<source>`.
    - After `train_ep`, `registry.resolve_champion` still raises until `ffep promote` runs, but a registered model version for `ep_model` exists with a version number.
    - Training twice produces two registered versions; neither overwrites the other.
    - After `train_ep`, `data/processed/oof_predictions_ep.parquet` exists with one row per training-frame row; after `train_wp`, `oof_predictions_wp.parquet` does.
    - The `phase` tag on the run is `01.3`.
  </behavior>
  <action>
    In `_train`, after the LOGO measurement and before `_log_run`, compute the reporting payload:
    `num_class = fixed_params.get("num_class") or 1`; `naive = naive_baseline_logloss(logo.oof_label, num_class)`;
    `curves = reliability_curves(logo.oof_pred, logo.oof_label, prob_labels)`;
    `source_table = per_source_metrics(logo.oof_pred, logo.oof_label, logo.oof_source, num_class)`.
    Add a `prob_labels: Sequence[str]` keyword parameter to `_train` and pass `EP_PROB_LABELS` from
    `train_ep` and `[WP_PROBABILITY_COLUMN]` from `train_wp`.

    Call `write_oof_predictions(logo, prob_labels, config, model_prefix)` and add the returned path
    to the logged params as `oof_predictions_path`.

    Extend the `metrics` dict handed to `_log_run` with: the existing `metric_name` entry,
    `f"naive_{metric_name.removeprefix('logo_')}"` (so `naive_mlogloss` / `naive_logloss`),
    `logloss_improvement` = naive minus model, and one
    `f"logo_logloss_by_source_{source}"` entry per non-pooled row of `source_table`. MLflow metric
    keys must stay alphanumeric/underscore — sanitise any source value that is not, and note the
    sanitisation in the run params if it fires.

    Extend `_log_run`'s signature with `figures: dict[str, "Figure"] | None = None`,
    `texts: dict[str, str] | None = None` and `register_as: str | None = None`. Inside the existing
    `with mlflow.start_run() as run:` block, after the metrics loop: log each figure with
    `mlflow.log_figure(fig, artifact_file)`; log each text with `mlflow.log_text(text, artifact_file)`;
    change `mlflow.set_tag("phase", "01.2")` to `"01.3"`; and replace the bare
    `mlflow.xgboost.log_model(model, name="model")` with a call to
    `registry.register_production_model(model, register_as, config)` when `register_as` is not
    None, falling back to the current bare `log_model` call when it is None. Keep
    `model.get_booster().feature_names = feature_names`, `ensure_experiment`, `log_params`, the
    metrics loop, the stepped `tune_logloss` loop and `return run.info.run_id` unchanged.

    From `_train`, pass `figures={f"reliability_{model_prefix}.png": render_reliability_figure(curves, f"{model_prefix.upper()} reliability (out-of-fold, leave-one-game-out)")}`,
    `texts={"per_source_metrics.md": <source_table rendered as a Markdown table>}` and
    `register_as=registry.registered_model_name(model_prefix)`. Close the figure with
    `matplotlib.pyplot.close(fig)` after logging so repeated training runs do not leak figures.

    Do not add any promotion here — becoming champion stays an explicit `ffep promote` step
    (CONTEXT §Promotion, implemented in plan 01.3-03).

    Extend `tests/test_model_train.py` with tests for every bullet in `<behavior>`. Read artifacts
    back through `MlflowClient().list_artifacts(run_id)` / `download_artifacts`, never by walking
    `config.paths.mlruns` directly (plan 01.3-01 removed that assumption).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_model_train.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_model_train.py -k "naive or reliability or per_source or registered or oof" -q` passes and collects at least 8 tests.
    - `grep -n "log_figure" src/flag_football_ep/model/train.py` returns a match.
    - `grep -n "register_production_model" src/flag_football_ep/model/train.py` returns a match.
    - `grep -v '^#' src/flag_football_ep/model/train.py | grep -c '"01.2"'` is 0 (the phase tag is updated).
    - `grep -n "logloss_improvement" src/flag_football_ep/model/train.py` returns a match.
    - `.venv/bin/pytest -q` passes (full suite).
  </acceptance_criteria>
  <done>Every training run carries reliability curves, a naive-baseline comparison, a per-source breakdown and persisted out-of-fold predictions, and its production refit is a registered model version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source label -> MLflow metric key | `source` values from canonical Parquet are interpolated into metric key names |
| Out-of-fold Parquet -> Phase 1.4 | `oof_predictions_*.parquet` becomes an input to a downstream phase's reporting |
| Training run -> registry version | Every run now creates a registered model version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.3-16 | Tampering | `logo_logloss_by_source_<source>` metric keys | mitigate | Source values are sanitised to alphanumeric/underscore before being interpolated into an MLflow metric key, and the sanitisation is recorded as a run param when it fires — the same "validate before interpolating an identifier" discipline as T-1.2-08 |
| T-1.3-17 | Tampering | `write_oof_predictions` | mitigate | Write-to-temp-then-`Path.replace`, mirroring `pipeline._atomic_write_parquet`, so an interrupted run cannot leave a truncated file that Phase 1.4 would silently read as complete |
| T-1.3-18 | Spoofing | registered model versions | mitigate | `register_production_model` is called only with the name derived by `registry.registered_model_name(model_prefix)`, which is validated against `_IDENTIFIER_PATTERN` (plan 01.3-03); a training run cannot register under an arbitrary caller-supplied name |
| T-1.3-19 | Repudiation | published calibration numbers | mitigate | Reliability PNGs, the per-source table and `training_data_sha256` are logged to the same run as the metrics, so a published calibration claim is always traceable to the exact corpus and protocol |
| T-1.3-20 | Information Disclosure | reliability/per-source artifacts | accept | Artifacts contain aggregate probabilities, log-losses and source names only — no player identities; artifact root stays under `config.paths.mlruns` on the local machine |
| T-1.3-SC | Tampering | package installs | mitigate | No package installs in this plan; `matplotlib` and `scikit-learn` are already direct `pyproject.toml` dependencies and RESEARCH `## Package Legitimacy Audit` records zero new external packages for this phase |
</threat_model>

<verification>
- `.venv/bin/pytest -q` (full suite) passes.
- After `ffep train --model both` on the real corpus: `mlflow ui --backend-store-uri sqlite:///$(pwd)/mlruns/mlflow.db` shows, per run, the reliability PNG, the `per_source_metrics.md` artifact, `logo_*`/`naive_*`/`logloss_improvement` metrics and the per-source metrics.
- `data/processed/oof_predictions_ep.parquet` and `oof_predictions_wp.parquet` exist, and `(game_id, play_id)` is unique in each.
- `grep -rn "CalibratedClassifierCV" src/` returns no matches (correction stays deferred).
</verification>

<success_criteria>
- Reliability curves per class and log-loss against a class-frequency naive baseline are part of every training run (REQ-S1-08).
- Metrics and curves are additionally broken down per source, realising the folded IFAF-benchmarking todo.
- Out-of-fold predictions are persisted under a documented Phase 1.4-facing contract.
- The production refit is a registered MLflow model version (REQ-S1-11), still requiring an explicit `ffep promote` to become champion.
</success_criteria>

<output>
Create `.planning/phases/01.3-methodical-model-retraining/01.3-05-SUMMARY.md` when done
</output>
