---
phase: 01.2-repo-to-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["01.2-01"]
files_modified:
  - src/flag_football_ep/config.py
  - ffep.toml
  - .env.example
  - .gitignore
  - config/config.py
  - data/raw/.gitkeep
  - data/raw/hudl/.gitkeep
  - data/raw/sportapp/.gitkeep
  - data/raw/ifaf/.gitkeep
  - data/raw/legacy/data_raw.csv
  - data/processed/.gitkeep
  - data/reference/half_boundaries.csv
  - tests/test_config.py
autonomous: false
requirements: [REQ-S1-06]
user_setup:
  - service: sportapp.fi
    why: "Play-by-play API for tournament data (fetch-sportapp)"
    env_vars:
      - name: SPORTAPP_API_KEY
        source: "sportapp.fi provider contact — the previously committed key must be rotated first"
    dashboard_config:
      - task: "Rotate the API key that was committed in plaintext at config/config.py"
        location: "sportapp.fi provider / API key management"
  - service: cpx.studio (IFAF WM-2026)
    why: "Tournament play-by-play API (fetch-ifaf); auth requirement unverified"
    env_vars:
      - name: CPX_API_KEY
        source: "only if the first live call returns 401/403 — endpoints observed so far carry no apikey parameter"

must_haves:
  truths:
    - "`data/` holds the full raw/processed/reference layout and `data_raw.csv` no longer sits at the repo root"
    - "No API key exists anywhere in the working tree; secrets resolve from environment or a git-ignored .env"
    - "`ffep.toml` is the single checked-in source of paths and settings, loadable via load_config()"
  artifacts:
    - path: "ffep.toml"
      provides: "checked-in paths/reference/sources/train configuration"
      contains: "[paths]"
    - path: "src/flag_football_ep/config.py"
      provides: "Config dataclasses, load_config(), .env loader, secret() resolver"
      min_lines: 80
    - path: ".env.example"
      provides: "documented secret names with no values"
      contains: "SPORTAPP_API_KEY="
    - path: "data/reference/half_boundaries.csv"
      provides: "relocated half-boundary reference file"
      contains: "half2_first_play"
  key_links:
    - from: "src/flag_football_ep/config.py"
      to: "os.environ"
      via: "secret() resolving api_key_env names, never literals"
      pattern: "os\\.environ"
    - from: ".gitignore"
      to: ".env and data/raw/hudl"
      via: "ignore rules protecting secrets and PII"
      pattern: "^\\.env$"
---

<objective>
Establish the configuration layer and the `data/` layout, and remove the plaintext API key that RESEARCH found committed at `config/config.py`.

Purpose: every later plan resolves paths and secrets through this module; the ingest plans need the `data/raw/*` + `data/processed` + `data/reference` layout to exist; the committed key is a live security finding that must not survive this phase.
Output: `ffep.toml`, `src/flag_football_ep/config.py`, `.env.example`, `.gitignore` rules, the populated `data/` skeleton with `data_raw.csv` and `half_boundaries.csv` relocated, and the deletion of `config/config.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@.gitignore
@config/config.py

<interfaces>
<!-- Config contract consumed by every later plan. Attribute names are fixed here. -->

Config
  .paths.data_root, .paths.raw_hudl, .paths.raw_sportapp, .paths.raw_ifaf, .paths.raw_legacy,
  .paths.processed, .paths.reference, .paths.models, .paths.mlruns, .paths.contract   -> pathlib.Path
  .reference.half_boundaries, .reference.final_scores, .reference.team_mapping,
  .reference.sportapp_games                                                            -> pathlib.Path
  .sources.sportapp.base_url, .sources.sportapp.api_key_env                            -> str
  .sources.ifaf.base_url, .sources.ifaf.tournament, .sources.ifaf.api_key_env           -> str
  .train.ep_experiment, .train.wp_experiment                                            -> str
  .train.exclude_games_ep, .train.exclude_games_wp                                      -> list[str]

load_config(path: Path = Path("ffep.toml")) -> Config
secret(env_name: str, required: bool = True) -> str | None
load_dotenv(path: Path = Path(".env")) -> None   # never overrides an already-set env var
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ffep.toml and the config module</name>
  <files>ffep.toml, src/flag_football_ep/config.py, tests/test_config.py</files>
  <read_first>
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (config decisions: checked-in config file holds paths and settings; secrets only from environment/.env)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Don't Hand-Roll: stdlib tomllib; Standard Stack note that no pyyaml/python-dotenv dependency is wanted)
    - tests/conftest.py (existing fixtures: repo_root, tmp_data_root)
    - this plan's `<interfaces>` block (exact Config attribute names)
    - Python/fetch_sportappfi_api.py lines 6-24 (the sportapp base URL and apikey query-parameter shape that `sources.sportapp` must describe)
  </read_first>
  <behavior>
    - `load_config` on a minimal TOML returns a Config whose path attributes are `Path` objects resolved relative to the config file's directory
    - `load_config` raises `FileNotFoundError` with the attempted path when the TOML is missing
    - `load_config` raises a `ConfigError` naming the missing key when a required table or key is absent
    - `load_dotenv` sets variables from `KEY=VALUE` lines, ignores blank lines and `#` comments, strips surrounding quotes, and does NOT overwrite a variable already present in `os.environ`
    - `secret("SPORTAPP_API_KEY")` returns the env value; with the variable unset it raises an error message naming the variable and pointing at `.env.example`, and the message contains no value
    - `Config.train.exclude_games_ep` equals `["legacy-37"]` and `exclude_games_wp` equals `["legacy-35"]` for the checked-in `ffep.toml`
  </behavior>
  <action>
Create `ffep.toml` at the repo root with these tables and keys (TOML chosen over YAML: stdlib `tomllib`, zero new dependency, matches `pyproject.toml` style):

`[paths]` — data_root = "data", raw_hudl = "data/raw/hudl", raw_sportapp = "data/raw/sportapp", raw_ifaf = "data/raw/ifaf", raw_legacy = "data/raw/legacy", processed = "data/processed", reference = "data/reference", models = "models", mlruns = "mlruns", contract = "docs/data-contract.schema.json".

`[reference]` — half_boundaries = "data/reference/half_boundaries.csv", final_scores = "data/reference/final_scores.csv", team_mapping = "data/reference/team_mapping.csv", sportapp_games = "data/reference/sportapp_games.csv".

`[sources.sportapp]` — base_url = "https://main-api-1.sportapp.fi/api/v1/public", api_key_env = "SPORTAPP_API_KEY".

`[sources.ifaf]` — base_url = "https://us.cpx.studio/v1", tournament = "ffwc26-women", api_key_env = "CPX_API_KEY".

`[train]` — ep_experiment = "ep_model", wp_experiment = "wp_model", exclude_games_ep = ["legacy-37"], exclude_games_wp = ["legacy-35"]. Add a comment above the two exclusion lists recording their origin: models/ep_model.ipynb cell 3 filtered `game_id != 37`, models/wp_model.ipynb cell 3 filtered `game_id != 35`; the canonical string game ids are `legacy-37` and `legacy-35`.

Create `src/flag_football_ep/config.py` implementing the `<interfaces>` block: frozen dataclasses `Paths`, `ReferenceFiles`, `SportappSource`, `IfafSource`, `TrainSettings`, `Config`; a `ConfigError(Exception)`; `load_config()` reading with `tomllib.load` in binary mode and resolving every path relative to the config file's parent; `load_dotenv()` as a ~15-line stdlib parser; `secret()` reading `os.environ`. `load_config` calls `load_dotenv` once so that `ffep` invocations pick up `.env` automatically. No `pydantic`, no `python-dotenv`.

Write `tests/test_config.py` covering every bullet in `<behavior>` using `tmp_path` for TOML/.env fixtures and `monkeypatch.delenv`/`setenv` for the secret cases. Include an assertion that no test writes a real secret value into an assertion message.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_config.py -q` exits 0 with at least 6 tests
    - `uv run python -c "from flag_football_ep.config import load_config; c=load_config(); print(c.paths.processed, c.reference.final_scores, c.sources.ifaf.tournament, c.train.exclude_games_ep)"` prints `data/processed data/reference/final_scores.csv ffwc26-women ['legacy-37']`
    - `ffep.toml` contains the tables `[paths]`, `[reference]`, `[sources.sportapp]`, `[sources.ifaf]`, `[train]`
    - `ffep.toml` contains no API key value (`grep -v '^#' ffep.toml | grep -c "wRX0tTke" ` returns 0)
    - `grep -c "pydantic\|python-dotenv\|yaml" src/flag_football_ep/config.py` returns 0
    - `load_dotenv` does not overwrite a pre-set environment variable (asserted by test)
  </acceptance_criteria>
  <done>Config loads from a checked-in TOML, secrets resolve from env/.env only, exclusions are documented in config rather than hardcoded in training code.</done>
</task>

<task type="auto">
  <name>Task 2: data/ layout, gitignore rules, and removal of the committed API key</name>
  <files>.gitignore, .env.example, config/config.py, data/raw/.gitkeep, data/raw/hudl/.gitkeep, data/raw/sportapp/.gitkeep, data/raw/ifaf/.gitkeep, data/processed/.gitkeep, data/raw/legacy/data_raw.csv, data/reference/half_boundaries.csv</files>
  <read_first>
    - .gitignore (current content: only the data/samples PII rule from phase 1.1)
    - config/config.py (the file being deleted — confirm it holds only API_KEY before removing)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Pitfall 1: committed API key; Runtime State Inventory: which root files are live inputs vs derivable)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (data/ layout decision: raw/hudl, raw/sportapp, raw/ifaf, raw/legacy, processed, reference)
    - data/half_boundaries.csv (current location and header — currently header-only)
  </read_first>
  <action>
File moves (use `git mv` so history follows):
- `data_raw.csv` → `data/raw/legacy/data_raw.csv`
- `data/half_boundaries.csv` → `data/reference/half_boundaries.csv`

Directory skeleton: create `data/raw/hudl/`, `data/raw/sportapp/`, `data/raw/ifaf/`, `data/processed/` each with a `.gitkeep`. Do NOT copy, move or rename anything from `data/samples/` — plan 05's checkpoint owns Hudl export placement.

`.gitignore` additions (keep the existing `data/samples/` rule and its comment):
- `.env` — secrets never committed (T-1.2-01)
- `data/raw/hudl/` with a negation for `!data/raw/hudl/.gitkeep` — new-format exports carry player names, same PII policy as `data/samples/` (T-1.2-02)
- `data/processed/` with `!data/processed/.gitkeep` — regenerable pipeline output, rebuilt atomically per ingest
- `mlruns/` — local MLflow tracking store
- `__pycache__/`, `*.pyc`, `.venv/`
Explicitly do NOT ignore `data/raw/legacy/`, `data/raw/sportapp/`, `data/raw/ifaf/` or `data/reference/`: the legacy CSV is the bulk of the training corpus and the raw JSON snapshots plus the maintained reference CSVs are what make the pipeline reproducible. Add a comment stating that.

`.env.example`: create with commented documentation and empty assignments `SPORTAPP_API_KEY=` and `CPX_API_KEY=` (the latter marked optional, needed only if cpx.studio turns out to require auth). No values.

Delete `config/config.py` and the now-empty `config/` directory. Verify by grep that nothing else in the tree imports `config.config` or references the literal key; if `api_call.ipynb`/`api_fuzzing.ipynb` reference it, leave the notebooks alone (plan 17 deletes them) but record the hits in the SUMMARY.
  </action>
  <verify>
    <automated>test -f data/raw/legacy/data_raw.csv && test -f data/reference/half_boundaries.csv && test ! -e config/config.py && test -f .env.example && for d in data/raw/hudl data/raw/sportapp data/raw/ifaf data/processed; do test -f $d/.gitkeep || exit 1; done; grep -v '^#' .gitignore | grep -qx '.env' && grep -rn "wRX0tTke3DZ8RLKAMntjZ81LwgNQuSN9" --include='*.py' --include='*.toml' src pyproject.toml ffep.toml . 2>/dev/null | grep -v '^\.planning/' | grep -v '\.ipynb' | wc -l | grep -qx '0' && git check-ignore -q .env data/raw/hudl/x.csv data/processed/plays.parquet && echo "layout and secret hygiene OK"</automated>
  </verify>
  <acceptance_criteria>
    - `data/raw/legacy/data_raw.csv` exists and root `data_raw.csv` does not
    - `data/reference/half_boundaries.csv` exists with header `filename,half2_first_play`; root `data/half_boundaries.csv` does not
    - `config/config.py` does not exist
    - `.env.example` exists and contains `SPORTAPP_API_KEY=` with no value after the `=`
    - `git check-ignore .env` exits 0; `git check-ignore data/raw/hudl/anything.csv` exits 0; `git check-ignore data/processed/plays.parquet` exits 0
    - `git check-ignore data/raw/legacy/data_raw.csv` exits non-zero (legacy input stays tracked)
    - `git check-ignore data/reference/half_boundaries.csv` exits non-zero (reference data stays tracked)
    - no `.py`/`.toml` file in the tree contains the literal string `wRX0tTke3DZ8RLKAMntjZ81LwgNQuSN9`
  </acceptance_criteria>
  <done>The `data/` layout is in place, secrets and PII and regenerable output are git-ignored, and the plaintext key is gone from the working tree.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Rotate the exposed sportapp.fi API key</name>
  <files>.env, data/reference/half_boundaries.csv</files>
  <read_first>
    - .env.example (the variable names the operator must populate)
    - src/flag_football_ep/config.py (secret() and how the key is resolved at runtime)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Pitfall 1: the committed key remains in git history regardless of the code fix)
  </read_first>
  <action>Present the rotation checkpoint to the operator with the exact verification steps below and wait for the resume signal. Do not attempt to rotate the key yourself — it lives with the sportapp.fi provider. Record the answer in the SUMMARY, and when the answer is "deferred", add a STATE blocker stating that `ffep fetch-sportapp` runs with a credential that is public in git history.</action>
  <done>The operator has answered "rotated" or "deferred", and the answer plus any resulting blocker is recorded in the SUMMARY.</done>
  <what-built>
    `config/config.py` (which contained `API_KEY = 'wRX0tTke3DZ8RLKAMntjZ81LwgNQuSN9'` in plaintext) is deleted from the working tree, `.env` is git-ignored, `.env.example` documents `SPORTAPP_API_KEY`, and `config.secret()` reads the key from the environment only.
  </what-built>
  <how-to-verify>
    Deleting the file does not undo the exposure: the key remains in git history and must be treated as compromised.

    1. Contact the sportapp.fi provider and rotate/revoke the key `wRX0tTke...QuSN9`.
    2. Create a local `.env` at the repo root (already git-ignored) containing `SPORTAPP_API_KEY=<new key>`.
    3. Confirm the new key resolves: `uv run python -c "from flag_football_ep.config import load_config, secret; c=load_config(); print(bool(secret(c.sources.sportapp.api_key_env)))"` prints `True`.
    4. Confirm `git status --short` does not list `.env`.

    If rotation is not possible right now, say so explicitly — the phase can proceed, but `ffep fetch-sportapp` will be running with a compromised credential and that fact gets recorded in the SUMMARY and STATE blockers.
  </how-to-verify>
  <resume-signal>Type "rotated" (key replaced, .env populated), "deferred" (rotation pending — will be recorded as a blocker), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repository → public/backup surfaces | anything committed leaves the operator's control permanently |
| environment/.env → process | secrets enter the runtime here, and only here |
| filesystem `data/` → pipeline | operator-supplied raw exports, some containing player PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-01 | Information Disclosure | `config/config.py` plaintext sportapp.fi API key | mitigate | delete the file, resolve the key via `config.secret()` from `os.environ`/git-ignored `.env`, ship `.env.example` with empty values, assert by grep that no `.py`/`.toml` in the tree holds the literal key |
| T-1.2-09 | Information Disclosure | git history retains the exposed key | transfer | history rewrite is out of scope; rotation with the provider is the only real fix — enforced by the blocking human checkpoint in task 3, recorded as a STATE blocker if deferred |
| T-1.2-02 | Information Disclosure | `data/raw/hudl/` Hudl exports containing player names and BLITZ person names | mitigate | git-ignore `data/raw/hudl/` (same policy as `data/samples/` from T-1.1-03), verified with `git check-ignore` |
| T-1.2-12 | Tampering | `ffep.toml` path values used to build filesystem paths | accept | single-operator local tool, the config file is checked in and operator-owned; paths are resolved relative to the config file's directory rather than the process CWD to avoid surprise writes |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py -q` green
- `git check-ignore .env data/raw/hudl/x.csv data/processed/plays.parquet` all ignored; `data/raw/legacy/data_raw.csv` and `data/reference/half_boundaries.csv` still tracked
- No literal API key anywhere in `src/`, `pyproject.toml`, `ffep.toml`
- Key rotation answered by the operator (rotated or explicitly deferred)
</verification>

<success_criteria>
- Every later plan can call `load_config()` and get typed paths
- Secrets exist only in the environment or a git-ignored `.env`
- `data/` is the single home for inputs, outputs and reference files
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-03-SUMMARY.md` when done
</output>
