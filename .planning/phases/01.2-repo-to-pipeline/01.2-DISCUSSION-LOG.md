# Phase 1.2: Repo to Pipeline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-17
**Phase:** 1.2-Repo to Pipeline
**Areas discussed:** Todo triage, CLI shape & entry points, Validation report & failure policy, data/ layout & source merge, Notebook migration cut-line

---

## Todo Triage

| Option | Description | Selected |
|--------|-------------|----------|
| IFAF/cpx.studio WM API | Add the WM-2026 API as third ingest source in 1.2 | ✓ |
| Contract ratification | Fold the analyst ratification follow-up into 1.2 | |
| Neither | Keep both as tracked todos | |

**User's choice:** Fold the IFAF/cpx.studio WM API into 1.2 scope.
**Notes:** Ratification todo stays tracked; validator must tolerate a contract version bump.

---

## CLI shape & entry points

| Option | Description | Selected |
|--------|-------------|----------|
| One CLI, subcommands | Single entry point: ffep ingest / fetch-sportapp / fetch-ifaf / train / score | ✓ |
| Ingest CLI only | Only Hudl-folder→Parquet ingest gets a CLI; rest via python -m | |
| python -m per module | No installed entry point | |

**User's choice:** One CLI with subcommands.

| Option | Description | Selected |
|--------|-------------|----------|
| Typer | Type-hint-driven, minimal boilerplate | ✓ |
| argparse (stdlib) | Zero new dependencies, more boilerplate | |
| Click | Decorator-based, more verbose | |

**User's choice:** Typer.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, add `ffep run` | One command chains ingest → features → train → score | ✓ |
| No, separate commands only | Invoke steps individually | |
| You decide | Claude picks during planning | |

**User's choice:** Add `ffep run`.

| Option | Description | Selected |
|--------|-------------|----------|
| Defaults + .env for secrets | Hard-defaulted paths, flags override, secrets via env | |
| Config file (TOML/YAML) | Checked-in config for paths/settings; secrets via env | ✓ |
| Flags only | Everything explicit per invocation | |

**User's choice:** Config file (TOML/YAML) — format is Claude's discretion.

---

## Validation report & failure policy

| Option | Description | Selected |
|--------|-------------|----------|
| Quarantine the game | Failing games excluded from Parquet, run continues | ✓ |
| Hard-fail the run | Any invalid game aborts the whole ingest | |
| Include with warnings | Everything lands in Parquet with flags | |

**User's choice:** Quarantine.

| Option | Description | Selected |
|--------|-------------|----------|
| Manual reference file | Maintained CSV under data/ mapping game → final score | ✓ |
| From the filename | Extend filename convention to carry final score | |
| API lookup where possible | Pull scores from sportapp/cpx, manual fallback | |

**User's choice:** Manual reference file (same workflow as half_boundaries.csv).

| Option | Description | Selected |
|--------|-------------|----------|
| Console + Markdown file | Console summary + per-run Markdown report next to the Parquet | ✓ |
| Console only | Nothing persisted | |
| Machine-readable JSON + console | JSON report for future tooling | |

**User's choice:** Console + Markdown file.

| Option | Description | Selected |
|--------|-------------|----------|
| Same checks, warn-only | Legacy games run every check, never quarantined | ✓ |
| Fully identical gates | Legacy failures quarantined too | |
| Skip validation for legacy | Legacy path bypasses checks | |

**User's choice:** Same checks, warn-only for the grandfathered 47 games.

---

## data/ layout & source merge

| Option | Description | Selected |
|--------|-------------|----------|
| raw/ per source + processed/ | data/raw/{hudl,sportapp,ifaf,legacy}, data/processed/, data/reference/ | ✓ |
| Flat data/ with prefixes | Source encoded in filenames | |
| You decide | Claude picks a layout | |

**User's choice:** raw/ per source + processed/ + reference/.

| Option | Description | Selected |
|--------|-------------|----------|
| Snapshot first | fetch commands download raw JSON to disk; ingest reads disk only | ✓ |
| Live fetch in ingest | Ingest pulls APIs directly each run | |
| Hybrid with cache | Live fetch with local cache layer | |

**User's choice:** Snapshot first.

| Option | Description | Selected |
|--------|-------------|----------|
| One Parquet, nullable extras | Core columns required for all sources; rich Hudl fields + IFAF clock nullable | ✓ |
| Per-source Parquets + core view | Full-fidelity per source + merged core view | |
| Core-only canonical | Rich fields in a Hudl-only side table | |

**User's choice:** One Parquet with nullable extras.

| Option | Description | Selected |
|--------|-------------|----------|
| Manual mapping table | Reference CSV maps source labels/IDs to canonical team codes; fail loudly on unmapped | ✓ |
| Normalize on the fly | String heuristics in code | |
| Keep source labels | Harmonization deferred | |

**User's choice:** Manual mapping table.

| Option | Description | Selected |
|--------|-------------|----------|
| Manual game list | Maintained file of known game UUIDs | |
| Research discovery endpoint | Find a list/schedule endpoint in 1.2 research | |
| GER games only | Only Germany's WM games | |

**User's choice:** Free-text answer — user supplied real discovery endpoints: `/v1/games` (all games), `/v1/tournaments/ffwc26-women` (+`/teams`; tournament metadata, teams, fields), per-game `/events?includeReverted=false&limit=500&offset=0` alongside the known `/unified-plays`. fetch-ifaf gets real discovery, no manual list.

| Option | Description | Selected |
|--------|-------------|----------|
| Free text, defer mapping | Keep source-provided player text (+ cpx jersey/id columns) | ✓ |
| Roster mapping table now | Canonical player reference normalized in ingest | |
| You decide | Claude picks based on 1.4 implications | |

**User's choice:** Free text, defer mapping to 1.4.

| Option | Description | Selected |
|--------|-------------|----------|
| Single plays.parquet | One file, atomic rebuild, source/competition/season as columns | ✓ |
| Partitioned dataset | Hive-style partitioning | |
| You decide | Claude picks | |

**User's choice:** Single plays.parquet + small games-metadata Parquet.

| Option | Description | Selected |
|--------|-------------|----------|
| Sort into data/, delete dead | Live inputs to raw/reference; intermediates regenerated; _OLD/stray deleted | ✓ |
| Archive everything | Unclear files to data/archive/ | |
| You decide | Claude classifies during planning | |

**User's choice:** Sort into data/, delete dead.

---

## Notebook migration cut-line

| Option | Description | Selected |
|--------|-------------|----------|
| Port as-is, no improvement | Mechanical migration, flaws included | |
| Port + obvious fixes | Migrate and fix low-hanging issues | ✓ |
| Ingest only in 1.2 | Training/scoring migrate in 1.3 | |

**User's choice:** Free-text: "take 2 and make it like enterprise level with mlflow and kinda that" — port + obvious fixes, plus MLflow ambition (clarified next).

| Option | Description | Selected |
|--------|-------------|----------|
| Foundation in 1.2, full use in 1.3 | MLflow as training logging backend now; registry/experiments in 1.3 | ✓ |
| All MLflow in 1.3 | 1.2 just ports with timestamped artifacts | |
| Full MLflow now in 1.2 | Tracking + registry + versioning all now | |

**User's choice:** Foundation in 1.2, full use in 1.3.

| Option | Description | Selected |
|--------|-------------|----------|
| Pipeline-stage modules | ingest/, validation, features, model, cli; Python/ absorbed and deleted | ✓ |
| Keep helper layout | Move Python/*.py as-is | |
| You decide | Claude designs the layout | |

**User's choice:** Pipeline-stage modules.

| Option | Description | Selected |
|--------|-------------|----------|
| Delete after migration | Notebooks deleted once ffep reproduces behavior | |
| Archive in notebooks/ | Frozen exploration folder | |
| Rewrite as thin demos | Small notebooks that import the package and visualize | ✓ |

**User's choice:** Rewrite as thin demos.

| Option | Description | Selected |
|--------|-------------|----------|
| pytest for critical logic | Unit tests for derivation rules + validation checks + migration-equivalence test | ✓ |
| Equivalence test only | Single reproduce-the-notebook test | |
| No tests in 1.2 | Validation report as safety net | |

**User's choice:** pytest for critical logic.

## Claude's Discretion

- Config file format (TOML vs YAML) and structure
- Reference CSV formats/naming (final scores, team mapping)
- Typer command signatures, module APIs, Markdown report layout
- MLflow local setup details (experiment naming, artifact layout)

## Deferred Ideas

- Cross-source player identity mapping — Phase 1.4, on real report need
- MLflow model registry as REQ-S1-11 versioning mechanism — Phase 1.3
- IFAF API further-tournament coverage + usage terms — research note, non-blocking
- Contract ratification with the Videoanalyst — tracked todo, not folded
