---
phase: 01.2-repo-to-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["01.2-01"]
files_modified:
  - src/flag_football_ep/canonical.py
  - src/flag_football_ep/testing.py
  - tests/test_canonical.py
autonomous: true
requirements: [REQ-S1-06]

must_haves:
  truths:
    - "One canonical plays schema exists in code: core columns required for every source, extras materialized as typed nulls"
    - "Any source frame can be conformed to that schema with a report of what was missing and what failed to cast"
    - "The score/points/differential chain is shared code, with defensive scoring credit switchable per source"
  artifacts:
    - path: "src/flag_football_ep/canonical.py"
      provides: "CORE_COLUMNS, NULLABLE_EXTRAS, NON_NULL_COLUMNS, make_game_id, conform_to_canonical, add_scoring_play_team, add_score_columns"
      min_lines: 140
      exports: ["CORE_COLUMNS", "NULLABLE_EXTRAS", "conform_to_canonical", "add_score_columns"]
    - path: "src/flag_football_ep/testing.py"
      provides: "canonical_plays() factory building schema-valid frames for tests in every later plan"
      min_lines: 50
    - path: "tests/test_canonical.py"
      provides: "unit coverage of conform, casting, game-id rules and the score chain"
  key_links:
    - from: "src/flag_football_ep/canonical.py"
      to: "ingest/hudl.py, ingest/legacy.py, ingest/sportapp.py, ingest/ifaf.py"
      via: "conform_to_canonical() as the single convergence point for all sources"
      pattern: "def conform_to_canonical"
    - from: "src/flag_football_ep/testing.py"
      to: "CORE_COLUMNS"
      via: "factory builds frames from the schema definition, not a hardcoded column list"
      pattern: "CORE_COLUMNS"
---

<objective>
Define the canonical plays schema in code, plus the shared derivations that all four ingest paths converge on.

Purpose: RESEARCH established that Hudl RESULT parsing, sportapp.fi text parsing and cpx.studio outcome mapping cannot share code — they converge only at the canonical column contract. This plan IS that contract. It lands before any ingest plan so no ingest executor invents column names.
Output: `canonical.py` (schema + conform + score chain), `testing.py` (frame factory used by every later test module), `tests/test_canonical.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@Python/helper_add_hudl_mutations.py
@docs/data-contract.schema.json

<interfaces>
<!-- Canonical schema contract. Column names are the existing project vocabulary from -->
<!-- Python/helper_add_hudl_mutations.py and data_raw.csv — do not rename them. -->

CORE_COLUMNS (name -> polars dtype), required in every source frame:
  Utf8 : source, competition, game_id, posteam, defteam, home_team, away_team, posteam_after,
         play_type, result_raw, scoring_play_team
  Int32: season, play_id, drive_id, half, down, yards_to_go, yardline, yardline_50, yardline_50_after,
         yardline_50_simple, yards_to_go_simple, sack, interception, complete_pass, touchdown,
         def_touchdown, penalty, safety, one_point_conv_success, two_point_conv_success,
         defensive_two_point_conv, scoring_play, home_team_points, away_team_points, home_team_score,
         away_team_score, posteam_score, defteam_score, score_differential, yards_gained, first_down

NULLABLE_EXTRAS (name -> polars dtype), materialized as typed nulls when a source lacks them:
  Utf8 : gender, source_game_id, game_date, off_form, off_play, off_str, play_dir, gap, pass_zone,
         target_route, hash_mark, def_front, coverage, blitz, qb, thrown_by, received_by, target,
         tackle, description, jersey_number, player_id
  Int32: yac, drive_success
  Float64: half_seconds_remaining
  Int64: game_clock_ms

NON_NULL_COLUMNS (spec only — enforced by validation in plan 06, never raised inside conform):
  source, competition, season, game_id, play_id, drive_id, half, posteam, defteam,
  home_team, away_team, down, yards_to_go, yardline_50

Functions:
  make_game_id(source: str, key: str | int) -> str
      # hudl -> the export filename stem verbatim; every other source -> f"{source}-{key}"
  conform_to_canonical(df: pl.DataFrame, source: str) -> tuple[pl.DataFrame, ConformReport]
  add_scoring_play_team(df: pl.DataFrame, credit_defense: bool) -> pl.DataFrame
  add_score_columns(df: pl.DataFrame) -> pl.DataFrame

  ConformReport: dataclass(missing_core: list[str], materialized_extras: list[str],
                          dropped_unknown: list[str], cast_failures: dict[str, int])
  MissingCanonicalColumns(Exception)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical schema definition and conform_to_canonical</name>
  <files>src/flag_football_ep/canonical.py, tests/test_canonical.py</files>
  <read_first>
    - Python/helper_add_hudl_mutations.py (make_hudl_mutations lines 28-181 — the authoritative source of the existing column vocabulary and dtypes)
    - data/raw/legacy/data_raw.csv (header line only: the legacy derived columns yardline_50, game_id, play_id, drive_id, half, posteam)
    - docs/data-contract.schema.json (core_columns, optional_rule, unknown_rule, derived_fields — the contract this schema implements)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (merge model: one canonical plays table with nullable extras; single plays.parquet with source/competition/season as filter columns)
    - this plan's `<interfaces>` block (the exact column lists)
  </read_first>
  <behavior>
    - `conform_to_canonical` on a frame holding every core column returns a frame whose `columns` equal `list(CANONICAL_COLUMNS)` in that exact order
    - a source frame missing three extras gets them back as all-null columns of the declared dtype, and `report.materialized_extras` lists exactly those three names
    - a source frame missing a core column raises `MissingCanonicalColumns` whose message lists every missing name
    - a string value that cannot cast to Int32 (e.g. "n/a") becomes null instead of raising, and `report.cast_failures` records `{column: count_of_new_nulls}`
    - unknown source columns are dropped and listed in `report.dropped_unknown`
    - `conform_to_canonical` sets `source` to the passed value on every row
    - nulls in NON_NULL_COLUMNS do not raise (validation, not conform, owns that judgement)
    - `make_game_id("hudl", "2026-06-14_GER-vs-AUT_EM-QUALI")` returns the stem unchanged; `make_game_id("legacy", 37)` returns "legacy-37"; `make_game_id("sportapp", 981)` returns "sportapp-981"
  </behavior>
  <action>
Create `src/flag_football_ep/canonical.py` implementing exactly the `<interfaces>` block. Implementation notes:

- Declare `CORE_COLUMNS` and `NULLABLE_EXTRAS` as `dict[str, pl.DataType]` module constants, `NON_NULL_COLUMNS` as a tuple, and `CANONICAL_COLUMNS = tuple(CORE_COLUMNS) + tuple(NULLABLE_EXTRAS)` as the Parquet column order.
- `conform_to_canonical` steps in order: (1) collect missing core names and raise `MissingCanonicalColumns` if any, after populating `report.missing_core`; (2) add absent extras via `pl.lit(None).cast(dtype)`; (3) cast every canonical column with `.cast(dtype, strict=False)`, counting newly introduced nulls per column by comparing null counts before and after; (4) overwrite `source` with the given value; (5) `select(CANONICAL_COLUMNS)`, recording every dropped column name in `report.dropped_unknown`.
- Use Int32 for all count/flag columns to match the notebook's `pl.col(pl.UInt32).cast(pl.Int32)` normalization; Float64 only for `half_seconds_remaining`; Int64 only for `game_clock_ms` (real IFAF clock in milliseconds).
- `make_game_id` returns `str(key)` for source `"hudl"`, `f"{source}-{key}"` otherwise.
- Define `MissingCanonicalColumns(Exception)` and the `ConformReport` dataclass here; later plans import both.

Write `tests/test_canonical.py` covering every `<behavior>` bullet, building inputs from small literal `pl.DataFrame` dicts (the `testing.py` factory arrives in task 3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_canonical.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_canonical.py -q` exits 0 with at least 8 tests
    - `uv run python -c "from flag_football_ep.canonical import CORE_COLUMNS, NULLABLE_EXTRAS, NON_NULL_COLUMNS, CANONICAL_COLUMNS as C; assert 'result_raw' in CORE_COLUMNS and 'game_clock_ms' in NULLABLE_EXTRAS; assert C[0]=='source'; assert set(NON_NULL_COLUMNS) <= set(CORE_COLUMNS); print(len(C))"` exits 0 and prints a count of at least 60
    - a test asserts `conform_to_canonical` output column order equals `CANONICAL_COLUMNS`
    - a test asserts an uncastable string yields nulls plus a `cast_failures` entry and does not raise
    - a test asserts the `MissingCanonicalColumns` message contains every missing core column name
    - `grep -c "def make_game_id\|def conform_to_canonical" src/flag_football_ep/canonical.py` returns 2
  </acceptance_criteria>
  <done>The canonical schema is code, and any source frame can be conformed to it with a machine-readable report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared scoring-credit and score-accumulation chain</name>
  <files>src/flag_football_ep/canonical.py, tests/test_canonical.py</files>
  <read_first>
    - Python/helper_add_hudl_mutations.py lines 113-166 (make_hudl_mutations: scoring_play, the offense-only scoring_play_team branch, home/away points, cum_sum, forward_fill, posteam_score/defteam_score, score_differential)
    - Python/helper_add_hudl_mutations.py lines 261-303 (make_dsfootball_mutations: the same chain WITH the defteam credit branch for def_touchdown / defensive_two_point_conv / safety — the branch `credit_defense=True` ports)
    - .planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md ("Score-cumsum / points chain" shared-pattern section)
    - src/flag_football_ep/canonical.py (written in task 1 — extend it)
  </read_first>
  <behavior>
    - `add_scoring_play_team(df, credit_defense=False)` credits `posteam` for touchdown / one_point_conv_success / two_point_conv_success and leaves def_touchdown, defensive_two_point_conv and safety rows null — same observable output as make_hudl_mutations lines 125-129
    - `add_scoring_play_team(df, credit_defense=True)` additionally credits `defteam` on def_touchdown, defensive_two_point_conv and safety rows — same as make_dsfootball_mutations lines 261-266
    - `add_score_columns` on a game with one 6-point home touchdown ends at home_team_score 6, away_team_score 0
    - `add_score_columns` forward-fills so every play carries the running score, and play_id 1 starts both scores at 0
    - `posteam_score`/`defteam_score` follow possession and `score_differential == posteam_score - defteam_score` on every row
    - with `credit_defense=True`, a defensive safety increments the defending team's score by 2 (the case that previously scored nothing)
    - two games in one frame never leak cumulative totals into each other
  </behavior>
  <action>
Extend `src/flag_football_ep/canonical.py` with `add_scoring_play_team(df, credit_defense)` and `add_score_columns(df)`.

`add_scoring_play_team` derives `scoring_play` (1 when any of touchdown, def_touchdown, one_point_conv_success, two_point_conv_success, defensive_two_point_conv, safety equals 1) and `scoring_play_team`. Do not reproduce the original's chained-`|`-then-`== 1` precedence bug: compare each flag to 1 explicitly and combine the booleans with `|`. Add a comment recording that this makes `scoring_play` correct for every flag rather than only the last one.

`add_score_columns` ports the chain: `home_team_points`/`away_team_points` (0 on play_id 1; otherwise the point value of the scoring event when that team equals `scoring_play_team` — touchdown 6, def_touchdown 6, one_point_conv_success 1, two_point_conv_success 2, defensive_two_point_conv 2, safety 2), then `cum_sum().over(["game_id","home_team"])` and `over(["game_id","away_team"])`, then `forward_fill()`, then `posteam_score`/`defteam_score` by team match, then `score_differential`. Sort by `["game_id","play_id"]` before the cumulative window so ordering is explicit instead of inherited from read order.

Add the `<behavior>` cases to `tests/test_canonical.py`, including a two-game frame proving no cross-game leakage and a `credit_defense` True/False pair on a safety play.
  </action>
  <verify>
    <automated>uv run pytest tests/test_canonical.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_canonical.py -q` exits 0 with at least 15 tests total
    - a test proves `credit_defense=False` leaves `scoring_play_team` null on a safety row while `credit_defense=True` sets it to the defending team
    - a test proves the defensive safety adds 2 points to the defending team's running score only under `credit_defense=True`
    - a test with two game_ids proves cumulative scores do not leak between games
    - a test asserts `score_differential == posteam_score - defteam_score` on every row of a multi-play fixture
    - `grep -c 'sort(' src/flag_football_ep/canonical.py` returns at least 1 and the sort keys are game_id and play_id
  </acceptance_criteria>
  <done>Score reconstruction is shared, per-game-safe code with the defensive-credit fix available to new-format ingest and switchable off for the grandfathered legacy path.</done>
</task>

<task type="auto">
  <name>Task 3: Canonical frame factory for downstream tests</name>
  <files>src/flag_football_ep/testing.py, tests/test_canonical.py</files>
  <read_first>
    - src/flag_football_ep/canonical.py (CORE_COLUMNS, NULLABLE_EXTRAS, CANONICAL_COLUMNS — the factory derives from these, it must not restate them)
    - tests/conftest.py (existing fixture style; the factory ships as a package module so later plans never edit conftest)
  </read_first>
  <action>
Create `src/flag_football_ep/testing.py` — a small test-support module inside the package so every later test file can `from flag_football_ep.testing import canonical_plays` with no conftest edits or sys.path tricks.

API:
- `canonical_plays(n_games=1, plays_per_game=8, source="hudl", overrides=None, extras=None) -> pl.DataFrame`
  Builds a schema-valid canonical frame: `game_id` via `make_game_id`, `play_id` contiguous 1..N per game, `drive_id` non-decreasing (new drive every 4 plays, starting at 1), `half` 1 for the first half of the plays and 2 for the rest, `posteam`/`defteam` alternating between two team codes, `down` cycling 1..4, `yards_to_go` between 5 and 20, `yardline_50` between 0 and 50, all flag columns 0, `result_raw` "Complete", `play_type` "pass", every extra null.
  `overrides` maps column name → scalar or list (length must equal frame height); `extras` does the same for extra columns. Raise `ValueError` on an unknown column name or a length mismatch — silent fixture misuse is worse than a hard failure.
- `canonical_plays_with_scores(...)`: the same frame, then `add_scoring_play_team(credit_defense=True)` and `add_score_columns`.

Add tests in `tests/test_canonical.py`: the default frame passes `conform_to_canonical` with an empty report; `overrides={"down": 7}` produces the out-of-range frame validation tests will need; an unknown column name raises `ValueError`; `canonical_plays(n_games=2, plays_per_game=6)` has height 12 with contiguous play_ids per game.
  </action>
  <verify>
    <automated>uv run pytest tests/test_canonical.py -q && uv run python -c "from flag_football_ep.testing import canonical_plays; from flag_football_ep.canonical import conform_to_canonical, CANONICAL_COLUMNS; df=canonical_plays(n_games=2, plays_per_game=6); out,rep=conform_to_canonical(df,'hudl'); assert out.columns==list(CANONICAL_COLUMNS); assert not rep.missing_core and not rep.materialized_extras and not rep.cast_failures, rep; assert out.height==12; print('factory OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_canonical.py -q` exits 0
    - `from flag_football_ep.testing import canonical_plays, canonical_plays_with_scores` imports successfully
    - `canonical_plays()` output passes `conform_to_canonical` with empty `missing_core`, `materialized_extras` and `cast_failures`
    - `canonical_plays(overrides={"down": 7})` yields a frame whose `down` column is all 7
    - `canonical_plays(overrides={"not_a_column": 1})` raises `ValueError`
    - `src/flag_football_ep/testing.py` references `CORE_COLUMNS`/`NULLABLE_EXTRAS` rather than duplicating the column list
  </acceptance_criteria>
  <done>Every later plan can build schema-valid test frames in one call, and fixture drift against the schema is impossible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source frames → canonical table | untrusted, source-specific column values cross into the shared schema here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-03 | Tampering | `conform_to_canonical` casting of source values | mitigate | cast with `strict=False` so malformed values become nulls that the plan-06 validation checks catch and report, instead of crashing the run or silently coercing garbage; every cast-induced null is counted in `ConformReport.cast_failures` |
| T-1.2-13 | Tampering | unknown source columns reaching the canonical Parquet | mitigate | `select(CANONICAL_COLUMNS)` drops everything unknown and records the names, implementing the contract's `unknown_rule` (ignore with a log notice) |
| T-1.2-02 | Information Disclosure | PII-bearing charting columns (blitz, received_by, thrown_by) | accept | these are declared nullable extras because the coaching reports in phase 1.4 need them; exposure is controlled by git-ignoring `data/raw/hudl/` and `data/processed/` (plan 03), not by dropping the columns |
</threat_model>

<verification>
- `uv run pytest tests/test_canonical.py -q` green
- `conform_to_canonical` is the only function any ingest module needs to reach the canonical schema
- Score chain is proven per-game-safe and defensive-credit switchable
</verification>

<success_criteria>
- Canonical column names, dtypes and order are defined once, in code
- No ingest plan has to invent or negotiate column names
- Test frames for all later plans come from one factory that cannot drift from the schema
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-04-SUMMARY.md` when done
</output>
