---
phase: 01.2-repo-to-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture_notebook_baseline.py
  - tests/fixtures/baseline_ep_model_data.parquet
  - tests/fixtures/baseline_wp_model_data.parquet
  - tests/fixtures/baseline_manifest.json
autonomous: true
requirements: [REQ-S1-04]

must_haves:
  truths:
    - "The current notebook training frames are frozen on disk before any porting happens"
    - "A manifest records row counts, column names, label distribution and weight statistics of both frozen frames"
    - "The capture script can be re-run to reproduce the fixtures from the same inputs"
  artifacts:
    - path: "scripts/capture_notebook_baseline.py"
      provides: "Reproduces models/ep_model.ipynb cells 1+3 and models/wp_model.ipynb cells 1+3 using the current Python/ helpers"
      min_lines: 60
    - path: "tests/fixtures/baseline_ep_model_data.parquet"
      provides: "Frozen EP model_data frame (label, yardline_50, yards_to_go, down0..down4, Total_W_Scaled)"
    - path: "tests/fixtures/baseline_wp_model_data.parquet"
      provides: "Frozen WP model_data frame (label + eight WP features)"
    - path: "tests/fixtures/baseline_manifest.json"
      provides: "Ground-truth metadata for the migration-equivalence test in plan 16"
      contains: "ep_model_data"
  key_links:
    - from: "scripts/capture_notebook_baseline.py"
      to: "Python/helper_add_hudl_mutations.py"
      via: "sys.path insert + import of make_hudl_mutations/prepare_ep_data/prepare_wp_data"
      pattern: "make_hudl_mutations"
    - from: "tests/fixtures/baseline_manifest.json"
      to: "tests/test_migration_equivalence.py (plan 16)"
      via: "expected row counts and column lists"
      pattern: "n_rows"
---

<objective>
Freeze the current notebook behaviour as test fixtures BEFORE any code is ported. Without this, the CONTEXT-mandated migration-equivalence test ("package output == notebook output on `data_raw.csv`") has no ground truth to diff against.

Purpose: this is the only plan that must run while `Python/` and the root CSVs are still in place. It creates the regression baseline that plan 16 asserts against and that gives every porting plan a way to prove it did not change behaviour.
Output: a re-runnable capture script plus two frozen Parquet frames and a manifest under `tests/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@Python/helper_add_hudl_mutations.py
@Python/helper_add_model_mutations.py

<interfaces>
<!-- Exact notebook chain being frozen. Source: models/ep_model.ipynb cells 1-3, models/wp_model.ipynb cells 1-3. -->

EP chain:
  df      = pl.read_csv("data_raw.csv", separator=";", infer_schema_length=0)
  df      = make_hudl_mutations(df)
  df      = df.with_columns(pl.col(pl.UInt32).cast(pl.Int32).name.keep())
  df_wc   = pl.read_csv("data/wc24_pbp.csv", separator=",", schema_overrides={
              "posteam": pl.String, "posteam_after": pl.String, "defteam": pl.String, "defteam_after": pl.String,
              "home_team": pl.String, "home_team_after": pl.String, "away_team": pl.String, "away_team_after": pl.String,
              "scoring_play_team": pl.String, "penalty": pl.Int32,
            }).with_columns(pl.col(pl.Int64).cast(pl.Int32).name.keep())
  df      = pl.concat([df, df_wc], how="diagonal")
  df2     = prepare_ep_data(df)
  ep_data = make_ep_model_mutations(df2.filter(pl.col("game_id") != 37), EP_SELECTED_COLUMNS)

  EP_SELECTED_COLUMNS = ["label", "yardline_50", "yards_to_go", "down0", "down1", "down2", "down3", "down4", "Total_W_Scaled"]

WP chain (data_raw.csv only, no wc24 concat):
  df1     = make_hudl_mutations(pl.read_csv("data_raw.csv", separator=";", infer_schema_length=0))
  df2     = prepare_wp_data(df1)
  wp_data = make_wp_model_mutations(df2.filter(pl.col("game_id") != 35), WP_SELECTED_COLUMNS)

  WP_SELECTED_COLUMNS = ["label", "receive_2h_ko", "half_seconds_remaining", "game_seconds_remaining",
                         "Diff_Time_Ratio", "score_differential", "down", "yards_to_go", "yardline_50"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture script reproducing the notebook EP and WP training frames</name>
  <files>scripts/capture_notebook_baseline.py</files>
  <read_first>
    - Python/helper_add_hudl_mutations.py (make_hudl_mutations, prepare_ep_data, prepare_wp_data — the exact functions being frozen)
    - Python/helper_add_model_mutations.py (make_ep_model_mutations, make_wp_model_mutations, the label mappings and the Total_W_Scaled weighting chain)
    - this plan's `<interfaces>` block (the literal notebook chain, including the `game_id != 37` / `game_id != 35` exclusions and the wc24 schema_overrides)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Pitfall 4 on the Total_W_Scaled division-by-zero invariant, Pitfall 5 on the .melt/.pivot API risk in get_games)
  </read_first>
  <action>
Create `scripts/capture_notebook_baseline.py` as a standalone script (not part of the installed package — it depends on `Python/`, which plan 17 deletes).

Requirements:
- CLI arguments via argparse: `--legacy-csv` (default `data_raw.csv`, falling back to `data/raw/legacy/data_raw.csv` if the default is absent), `--wc24-csv` (default `data/wc24_pbp.csv`), `--out-dir` (default `tests/fixtures`).
- `sys.path.insert(0, "Python")` then import `make_hudl_mutations`, `prepare_ep_data`, `prepare_wp_data` from `helper_add_hudl_mutations` and `make_ep_model_mutations`, `make_wp_model_mutations` from `helper_add_model_mutations`.
- Reproduce the EP chain and the WP chain exactly as given in the `<interfaces>` block — same read options, same casts, same diagonal concat, same `filter(pl.col("game_id") != 37)` / `!= 35` exclusions, same selected-column lists. Do not add `.drop_nulls()`; the notebook drops nulls later, at fit time, and the baseline must capture the pre-drop frame.
- Write `baseline_ep_model_data.parquet` and `baseline_wp_model_data.parquet` into `--out-dir`.
- If `get_games()` raises `AttributeError` or `TypeError` on the installed polars version (the `.melt`/`.pivot(aggregate_function=...)` risk from RESEARCH Pitfall 5), do NOT patch the helper. Print the traceback, exit non-zero, and record the failure in the plan SUMMARY — this is exactly the signal plan 10 needs, and the baseline must come from unmodified helper code.
- Print row counts and null counts per column for both frames to stdout.
  </action>
  <verify>
    <automated>uv run python scripts/capture_notebook_baseline.py --out-dir tests/fixtures && uv run python -c "import polars as pl; ep=pl.read_parquet('tests/fixtures/baseline_ep_model_data.parquet'); wp=pl.read_parquet('tests/fixtures/baseline_wp_model_data.parquet'); assert ep.columns==['label','yardline_50','yards_to_go','down0','down1','down2','down3','down4','Total_W_Scaled'], ep.columns; assert wp.columns==['label','receive_2h_ko','half_seconds_remaining','game_seconds_remaining','Diff_Time_Ratio','score_differential','down','yards_to_go','yardline_50'], wp.columns; assert ep.height>3000 and wp.height>3000, (ep.height, wp.height); print('baseline frames OK', ep.height, wp.height)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/capture_notebook_baseline.py` exits 0
    - `tests/fixtures/baseline_ep_model_data.parquet` exists with exactly the nine EP selected columns in the order given in `<interfaces>`
    - `tests/fixtures/baseline_wp_model_data.parquet` exists with exactly the nine WP selected columns in that order
    - both frames have > 3000 rows (data_raw.csv holds ~3,700 legacy plays; the EP frame additionally carries the ~14,546 wc24 rows)
    - the script imports the helpers from `Python/` and contains no reimplementation of the mutation logic
    - re-running the script produces frames whose `equals()` comparison against the previous run is True
  </acceptance_criteria>
  <done>Both notebook training frames are frozen on disk, produced by unmodified `Python/` helper code.</done>
</task>

<task type="auto">
  <name>Task 2: Baseline manifest with the invariants plan 16 asserts against</name>
  <files>scripts/capture_notebook_baseline.py, tests/fixtures/baseline_manifest.json</files>
  <read_first>
    - scripts/capture_notebook_baseline.py (the script written in task 1 — extend it, do not duplicate it)
    - tests/fixtures/baseline_ep_model_data.parquet (the frame whose statistics are being recorded)
  </read_first>
  <action>
Extend `scripts/capture_notebook_baseline.py` to also write `baseline_manifest.json` into `--out-dir`. JSON keys, exactly:

- `captured_at` — ISO-8601 UTC timestamp
- `polars_version`, `python_version`
- `inputs` — object with `legacy_csv` (resolved path), `legacy_csv_sha256`, `wc24_csv`, `wc24_csv_sha256`
- `ep_model_data` — object with `n_rows`, `columns` (ordered list), `dtypes` (column → polars dtype string), `null_counts` (column → int), `label_distribution` (label value as string → count, including a `"null"` key), `total_w_scaled` (object with `min`, `max`, `mean`, `n_nan`, `n_null`)
- `wp_model_data` — same shape as `ep_model_data`, with `label_distribution` and a `half_seconds_remaining` stats object instead of `total_w_scaled`
- `excluded_games` — object `{"ep": 37, "wp": 35}` documenting the notebook's magic game exclusions (models/ep_model.ipynb cell 3, models/wp_model.ipynb cell 3)
- `notes` — free text stating that these frames were produced by unmodified `Python/` helpers before the package port

`n_nan` must be computed with `is_nan()` on the float column (guarding null-only cases) so RESEARCH Pitfall 4 (Total_W_Scaled division by a zero range) has a recorded pre-port value.
  </action>
  <verify>
    <automated>uv run python scripts/capture_notebook_baseline.py --out-dir tests/fixtures && uv run python -c "import json; m=json.load(open('tests/fixtures/baseline_manifest.json')); [m[k] for k in ('captured_at','inputs','ep_model_data','wp_model_data','excluded_games')]; assert m['excluded_games']=={'ep':37,'wp':35}; assert m['ep_model_data']['n_rows']>3000; assert set(m['ep_model_data'])>={'n_rows','columns','dtypes','null_counts','label_distribution','total_w_scaled'}; assert m['inputs']['legacy_csv_sha256']; print('manifest OK', m['ep_model_data']['n_rows'], m['wp_model_data']['n_rows'])"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/baseline_manifest.json` parses as JSON and contains keys captured_at, polars_version, python_version, inputs, ep_model_data, wp_model_data, excluded_games, notes
    - `inputs.legacy_csv_sha256` and `inputs.wc24_csv_sha256` are 64-character hex strings
    - `ep_model_data.label_distribution` sums (including the null bucket) to `ep_model_data.n_rows`
    - `ep_model_data.total_w_scaled` contains min, max, mean, n_nan, n_null
    - `excluded_games` equals `{"ep": 37, "wp": 35}`
    - re-running the script yields an identical manifest apart from `captured_at`
  </acceptance_criteria>
  <done>The manifest records every invariant plan 16's equivalence test needs, including the null and NaN counts of the weighting column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo CSVs → capture script | local input files, no network, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-02 | Information Disclosure | `tests/fixtures/*.parquet` | mitigate | the frozen frames contain only the nine model columns each (numeric features, label, weights) — no player names, no free-text charting columns; verified by the exact column-list assertion in both tasks |
| T-1.2-11 | Repudiation | baseline provenance | mitigate | manifest records input file SHA-256 hashes, polars/python versions and a capture timestamp so a later behaviour delta can be attributed |
</threat_model>

<verification>
- `uv run python scripts/capture_notebook_baseline.py` exits 0 and is idempotent
- `tests/fixtures/` contains two Parquet frames and the manifest
- The frames were produced without editing anything under `Python/`
</verification>

<success_criteria>
- Plan 16 can assert package output against a frozen, hashed, documented ground truth
- No porting plan has to guess what "today's behaviour" was
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-02-SUMMARY.md` when done
</output>
