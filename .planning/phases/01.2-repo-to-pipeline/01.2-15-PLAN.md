---
phase: 01.2-repo-to-pipeline
plan: 15
type: execute
wave: 5
depends_on: ["01.2-13"]
files_modified:
  - src/flag_football_ep/model/score.py
  - tests/test_model_score.py
autonomous: true
requirements: [REQ-S1-04]

must_haves:
  truths:
    - "`ffep score` applies the trained EP and WP models to canonical plays and writes a scored Parquet"
    - "Models are resolved from the MLflow tracking store, not from a hardcoded pickle path"
    - "EPA and WPA columns are produced by the ported feature functions, not reimplemented"
  artifacts:
    - path: "src/flag_football_ep/model/score.py"
      provides: "resolve_run, load_model, score_plays"
      min_lines: 130
      exports: ["score_plays", "resolve_run", "load_model"]
    - path: "tests/test_model_score.py"
      provides: "train-then-score round-trip coverage against a temporary MLflow store"
  key_links:
    - from: "src/flag_football_ep/model/score.py"
      to: "mlruns/"
      via: "mlflow.search_runs plus runs:/<id>/model artifact load"
      pattern: "search_runs"
    - from: "src/flag_football_ep/model/score.py"
      to: "flag_football_ep.features.mutations.add_ep_variables"
      via: "EPA derivation reusing the ported feature functions"
      pattern: "add_ep_variables"
---

<objective>
Port the scoring path: load the logged EP and WP models, predict on canonical plays, and derive EPA/WPA through the ported feature functions.

Purpose: completes the REQ-S1-04 chain (ingest, features, training, scoring runs from the package) and gives phase 1.4's reports their input. It also closes the pickle trust question: models come from the configured MLflow store, never from an arbitrary file path.
Output: `model/score.py` and `tests/test_model_score.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@src/flag_football_ep/model/train.py
@src/flag_football_ep/model/hyperparams.py
@src/flag_football_ep/features/mutations.py

<interfaces>
<!-- Scoring API declared by cli.py in plan 01. -->

flag_football_ep.model.score:
  resolve_run(experiment: str, config: Config, run_id: str | None = None) -> str
      # explicit run_id wins; otherwise the most recent FINISHED run of that experiment
  load_model(run_id: str, config: Config)
      # mlflow.xgboost.load_model(f"runs:/{run_id}/model") against the configured tracking uri
  score_plays(plays: pl.DataFrame, config: Config, ep_run: str | None = None,
              wp_run: str | None = None) -> pl.DataFrame

EP probability column order (must match the training label mapping exactly):
  ["Touchdown_Prob", "Opp_Touchdown_Prob", "Safety_Prob", "Opp_Safety_Prob", "No_Score_Prob"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run resolution and model loading from the MLflow store</name>
  <files>src/flag_football_ep/model/score.py, tests/test_model_score.py</files>
  <read_first>
    - src/flag_football_ep/model/train.py (how runs are logged: experiment names, the artifact path used by log_model, the tags and params available for resolution)
    - src/flag_football_ep/config.py (paths.mlruns, train.ep_experiment, train.wp_experiment)
    - .planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md (score.py section: the notebook's pickle.load pattern and the instruction to load from the MLflow-logged artifact instead)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (MLflow foundation decision; the model registry is explicitly deferred to phase 1.3)
  </read_first>
  <behavior>
    - `resolve_run` with an explicit run id returns it unchanged after verifying the run exists, raising `RunNotFound` when it does not
    - `resolve_run` without a run id returns the most recent FINISHED run of the named experiment
    - `resolve_run` on an experiment with no runs raises `RunNotFound` naming the experiment and the tracking path
    - `load_model` returns a model whose booster feature names match the features logged by the training run
    - `load_model` sets the tracking uri from `config.paths.mlruns` before loading, so a caller-supplied path can never point the loader outside the configured store
    - `load_model` rejects a run id that is not a plain hex identifier, so no path fragment can be injected into the `runs:/` uri
  </behavior>
  <action>
Create `src/flag_football_ep/model/score.py` with `RunNotFound`, `resolve_run` and `load_model` per `<interfaces>`.

Rules:
- Always `mlflow.set_tracking_uri("file:" + str(config.paths.mlruns))` before any MLflow call, so resolution is scoped to the configured store.
- `resolve_run` uses `mlflow.search_runs(experiment_names=[experiment], filter_string="attributes.status = 'FINISHED'", order_by=["attributes.start_time DESC"], max_results=1)`.
- Validate an explicitly supplied run id against `^[0-9a-f]{8,}$` before interpolating it into a `runs:/` uri, and raise `ValueError` otherwise. This is the mitigation for loading arbitrary pickles: the only reachable artifacts are runs inside the configured tracking directory.
- No `pickle.load` of a caller-supplied path anywhere in this module. If a dated `.pkl` export needs loading later, that is a separate, explicit function — do not add it in this plan.

Write `tests/test_model_score.py` covering the `<behavior>` bullets by first calling `train_ep`/`train_wp` against a `tmp_path` mlruns store (reusing the fixtures from `tests/test_model_train.py` patterns) and then resolving those runs.
  </action>
  <verify>
    <automated>uv run pytest tests/test_model_score.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_model_score.py -q` exits 0 with at least 6 tests
    - a test proves `resolve_run` returns the latest FINISHED run when no id is given
    - a test proves an unknown experiment raises `RunNotFound` naming the experiment and tracking path
    - a test proves a run id containing a path fragment (for example `../../etc`) raises `ValueError`
    - a test proves the loaded booster's feature names match the training features
    - `grep -c "pickle.load" src/flag_football_ep/model/score.py` returns 0
  </acceptance_criteria>
  <done>Models are resolved and loaded only from the configured MLflow store, with no arbitrary-path deserialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: score_plays producing EPA and WPA columns</name>
  <files>src/flag_football_ep/model/score.py, tests/test_model_score.py</files>
  <read_first>
    - ep_model.ipynb cells 10 and 16 (the load-predict-hstack-add_ep_variables pattern: predict into the five probability columns, build a polars frame, hstack, then apply add_ep_variables)
    - src/flag_football_ep/features/mutations.py (prepare_ep_data, prepare_wp_data, add_ep_variables, add_wp_variables and their required input columns)
    - src/flag_football_ep/model/hyperparams.py (EP_FEATURES, WP_FEATURES, EP_PROB_LABELS — feature order must match training exactly)
    - src/flag_football_ep/model/score.py (task 1 output — extend it)
  </read_first>
  <behavior>
    - `score_plays` on a canonical frame returns a frame containing the five EP probability columns, `ExpPts`, `ep`, `epa`, plus `wp`, `home_wp`, `away_wp` and `wpa`
    - the probability columns are named and ordered per `EP_PROB_LABELS`, matching the training label mapping
    - features are selected in `EP_FEATURES` / `WP_FEATURES` order before predicting, so a column-order change cannot silently corrupt predictions
    - the five EP probabilities sum to approximately 1 on every row (within 1e-6)
    - `ExpPts` lies within [-6, 6] on every scored row
    - rows with null features are passed through with null probabilities rather than dropped, so the output row count equals the input row count
    - `score_plays` raises a clear error naming the missing columns when the input frame lacks a required feature
    - scoring is deterministic: two calls on the same input and run ids produce identical output
  </behavior>
  <action>
Extend `src/flag_football_ep/model/score.py` with `score_plays` per `<interfaces>`.

Steps: `prepare_ep_data` → select `EP_FEATURES` → `model.predict` → build the probability frame with `EP_PROB_LABELS` column names → `hstack` onto the prepared frame → `add_ep_variables`. Then `prepare_wp_data` → select `WP_FEATURES` → predict → add the `wp` column → `add_wp_variables`.

Rules:
- Always `select(EP_FEATURES)` / `select(WP_FEATURES)` explicitly before `to_numpy()`; never rely on the frame's incidental column order.
- Preserve row count: split out null-feature rows, predict on the complete rows, and rejoin with null probabilities so the caller keeps a 1:1 mapping with the input plays.
- Round-trip via numpy only; do not introduce a pandas dependency in this module.
- The caller (`cli.py score`) writes the returned frame to `data/processed/plays_scored.parquet`; `score_plays` itself performs no I/O beyond loading the models.

Add the `<behavior>` cases to `tests/test_model_score.py` as a train-then-score round trip on a synthetic corpus.
  </action>
  <verify>
    <automated>uv run pytest tests/test_model_score.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_model_score.py -q` exits 0 with at least 13 tests total
    - a test proves the output carries the five probability columns plus ExpPts, ep, epa, wp, home_wp, away_wp and wpa
    - a test proves the five probabilities sum to 1 within 1e-6 on every complete row
    - a test proves `ExpPts` stays within [-6, 6]
    - a test proves the output row count equals the input row count with null-feature rows preserved
    - a test proves two identical calls produce identical frames
    - `grep -c "import pandas" src/flag_football_ep/model/score.py` returns 0
  </acceptance_criteria>
  <done>Scoring runs from the package end to end and produces EPA/WPA through the ported feature functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLflow artifact store → process | model artifacts are deserialized into the running process |
| caller-supplied run ids → artifact uri | operator input is interpolated into a `runs:/` uri |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-08 | Elevation of Privilege | deserializing an arbitrary pickle chosen by a caller-supplied path | mitigate | models load only via `runs:/<id>/model` against the tracking uri derived from `config.paths.mlruns`; run ids are validated against a hex pattern; no `pickle.load` exists in the module, asserted by a grep gate |
| T-1.2-31 | Tampering | feature-order drift between training and scoring silently corrupting predictions | mitigate | explicit `select(EP_FEATURES)` / `select(WP_FEATURES)` before prediction plus a test asserting the loaded booster's feature names match |
| T-1.2-18 | Repudiation | silently dropping rows that could not be scored | mitigate | null-feature rows are preserved with null probabilities and the row count is asserted equal to the input |
</threat_model>

<verification>
- `uv run pytest tests/test_model_score.py -q` green
- Train-then-score round trip works against a temporary MLflow store
- No pickle path loading anywhere in the module
</verification>

<success_criteria>
- The full REQ-S1-04 chain runs from the package: ingest, features, training, scoring
- Model provenance is a run id, not a filename
- Scoring output is deterministic and row-count preserving
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-15-SUMMARY.md` when done
</output>
