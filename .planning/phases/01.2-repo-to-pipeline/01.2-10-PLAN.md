---
phase: 01.2-repo-to-pipeline
plan: 10
type: execute
wave: 4
depends_on: ["01.2-02", "01.2-04", "01.2-06"]
files_modified:
  - src/flag_football_ep/ingest/legacy.py
  - tests/test_ingest_legacy.py
autonomous: true
requirements: [REQ-S1-05]

must_haves:
  truths:
    - "The 47 grandfathered games in data_raw.csv ingest into the canonical schema with their notebook semantics intact"
    - "Legacy games are never quarantined — failing checks only warn"
    - "The fragile first-drive-is-home heuristic lives only here, and produces the same home/away assignment as the notebook"
  artifacts:
    - path: "src/flag_football_ep/ingest/legacy.py"
      provides: "read_legacy, get_games, legacy_mutations, ingest_legacy"
      min_lines: 170
      exports: ["read_legacy", "get_games", "ingest_legacy"]
    - path: "tests/test_ingest_legacy.py"
      provides: "home/away heuristic, warn-only behaviour and baseline-parity coverage"
  key_links:
    - from: "src/flag_football_ep/ingest/legacy.py"
      to: "flag_football_ep.canonical.add_scoring_play_team"
      via: "credit_defense=False preserving notebook score semantics"
      pattern: "credit_defense=False"
    - from: "tests/test_ingest_legacy.py"
      to: "tests/fixtures/baseline_manifest.json"
      via: "row-count and statistic parity against the frozen notebook baseline"
      pattern: "baseline_manifest"
---

<objective>
Port the grandfathered `data_raw.csv` path into `ingest/legacy.py` with notebook-identical semantics, and wire it to warn-only validation.

Purpose: these 47 games (~3,700 plays) are the bulk of the training corpus, cannot be re-exported, and are the ground truth for the migration-equivalence test in plan 16. Their semantics must NOT be modernized — the RESULT substring behaviour, the polluted ODK column and the offense-only scoring credit all stay, because changing them would change the trained model without a methodological review (that is phase 1.3).
Output: `ingest/legacy.py` and `tests/test_ingest_legacy.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@Python/helper_add_hudl_mutations.py
@src/flag_football_ep/canonical.py
@src/flag_football_ep/validation/checks.py
@tests/fixtures/baseline_manifest.json

<interfaces>
<!-- Legacy ingest API consumed by pipeline.run_ingest (plan 14). -->

flag_football_ep.ingest.legacy:
  read_legacy(path: Path) -> pl.DataFrame          # separator=";", infer_schema_length=0
  get_games(df: pl.DataFrame) -> pl.DataFrame      # game_id, home_team, away_team (first-drive-is-home)
  legacy_mutations(df: pl.DataFrame) -> pl.DataFrame   # faithful port of make_hudl_mutations
  ingest_legacy(path: Path, team_mapping: pl.DataFrame | None) -> tuple[pl.DataFrame, IngestNotices]

data_raw.csv header (verified 2026-08-17, `;`-delimited, utf-8-sig):
  PLAY #, ODK, OFF FORM, Off Str, OFF PLAY, DN, DIST, YARD LN, RESULT, Drive Success,
  TARGET ROUTE, RECEIVED BY, GN/LS, Thrown By, YAC, QB, C, X/H, Y/CAT, Z, Target, Drop, B,
  yardline_50, game_id, play_id, drive_id, half, posteam

Note: yardline_50, game_id, play_id, drive_id, half and posteam are ALREADY present in the legacy
file (hand-maintained), so this path does not re-derive them — it casts them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Faithful legacy mutation port including the first-drive-is-home heuristic</name>
  <files>src/flag_football_ep/ingest/legacy.py, tests/test_ingest_legacy.py</files>
  <read_first>
    - Python/helper_add_hudl_mutations.py lines 4-182 (get_games and make_hudl_mutations in full — the exact behaviour being preserved, including the `str.contains` predicates and the offense-only scoring_play_team)
    - data/raw/legacy/data_raw.csv (header plus the first rows: the pre-derived yardline_50/game_id/play_id/drive_id/half/posteam columns and the polluted OFF FORM/ODK content)
    - .planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md (legacy.py section: keep the heuristic here, reuse validation with warn_only, do not duplicate check logic)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Pitfall 2 stating the legacy path may keep the old substring behaviour because its malformed RESULT variants are documented as invalid going forward but still present; Pitfall 5 on the .melt/.pivot API risk in get_games)
    - tests/fixtures/baseline_manifest.json (frozen row counts and label distribution to compare against)
    - src/flag_football_ep/canonical.py (conform_to_canonical, add_scoring_play_team, add_score_columns, make_game_id)
  </read_first>
  <behavior>
    - `get_games` assigns `home_team` = the first distinct posteam in play order per game and `away_team` = the second, reproducing the original melt/pivot result without using `.melt` or `.pivot`
    - `get_games` on a game with three distinct posteam values raises `ValueError` naming the game (the original silently produced a struct of unexpected width)
    - `legacy_mutations` keeps the `RESULT` substring semantics: "Complete Penalty" (no comma) still classifies as a complete pass, and "Penalty (declined)" still classifies as no_play
    - `legacy_mutations` uses `add_scoring_play_team(credit_defense=False)`, so defensive scores stay uncredited exactly as in the notebook
    - `game_id` becomes `legacy-{n}` via `make_game_id("legacy", n)` and the original integer is preserved in the `source_game_id` extra
    - `ingest_legacy` output columns equal `CANONICAL_COLUMNS`
    - `ingest_legacy` on the real `data_raw.csv` yields the number of rows recorded in the baseline manifest for the legacy portion, and its `posteam`/`home_team`/`away_team` values are non-null on every row
    - `team_mapping=None` leaves team labels untouched (legacy labels may not be mapped yet) while a supplied mapping maps them and raises on gaps
  </behavior>
  <action>
Create `src/flag_football_ep/ingest/legacy.py` implementing the `<interfaces>` block.

Rules:
- `read_legacy` mirrors the notebook read exactly: `pl.read_csv(path, separator=";", infer_schema_length=0)`.
- `get_games` must NOT use `.melt` or `.pivot(aggregate_function=...)`. RESEARCH Pitfall 5 flags those calls as an API-compatibility risk on the installed polars version. Reimplement with `sort(["game_id","play_id"])` → `unique(subset=["game_id","posteam"], keep="first")` → `group_by("game_id").agg(pl.col("posteam"))` → first element as `home_team`, second as `away_team`. Keep the original's comment that the team with the first drive is assumed home and that this only matters for scores and the differential.
- `legacy_mutations` is a faithful port of `make_hudl_mutations`: the same casts, the same `yardline_50_simple`/`yards_to_go_simple` bins, the same `shift(-1)` columns, the same `str.contains`-based play_type/sack/interception/complete_pass/touchdown/def_touchdown/penalty/safety/conversion derivations, the same `yards_gained` and `first_down`. Replace only: the keyword-comparison forms `pl.when(down = 0)` with explicit `pl.col("down") == 0` predicates (identical semantics, avoids a deprecated call style), and the score chain with `add_scoring_play_team(credit_defense=False)` plus `add_score_columns` from `canonical.py`. Add a docstring stating that this path is intentionally frozen at notebook behaviour and that any semantic change belongs to phase 1.3.
- Map the legacy charting columns onto canonical extras: `OFF FORM`→off_form, `Off Str`→off_str, `OFF PLAY`→off_play, `TARGET ROUTE`→target_route, `RECEIVED BY`→received_by, `Thrown By`→thrown_by, `YAC`→yac, `QB`→qb, `Target`→target, `Drive Success`→drive_success. Set `source` "legacy", `competition` "legacy", `season` 0 with a comment that the legacy file carries no season and that phase 1.3 may backfill it from a reference file.
- `ingest_legacy` finishes with `conform_to_canonical(df, "legacy")`.

Write `tests/test_ingest_legacy.py` covering the `<behavior>` bullets. Use small synthetic `;`-CSVs for the semantics cases and the `legacy_csv_path` fixture (auto-skipping) plus `tests/fixtures/baseline_manifest.json` for the real-file parity case.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingest_legacy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ingest_legacy.py -q` exits 0 with at least 10 tests
    - `grep -c "\.melt(\|aggregate_function" src/flag_football_ep/ingest/legacy.py` returns 0
    - `grep -c "credit_defense=False" src/flag_football_ep/ingest/legacy.py` returns at least 1
    - a test proves `get_games` picks the first-drive team as home and raises on three distinct posteam values
    - a test proves "Complete Penalty" still classifies as a complete pass (legacy substring semantics preserved)
    - a test proves `game_id` values match the pattern `legacy-<int>` and `source_game_id` holds the original integer
    - a test proves `ingest_legacy` output columns equal `CANONICAL_COLUMNS`
    - a test compares the real-file row count against `tests/fixtures/baseline_manifest.json` and passes (or skips when `data_raw.csv` is absent)
  </acceptance_criteria>
  <done>The legacy path runs from the package with its notebook semantics frozen and its fragile heuristic quarantined to this module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warn-only validation wiring for the grandfathered path</name>
  <files>src/flag_football_ep/ingest/legacy.py, tests/test_ingest_legacy.py</files>
  <read_first>
    - src/flag_football_ep/validation/checks.py (run_checks, partition_games, Status, warn_only_sources — reuse, do not reimplement)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (legacy path decision: the 47 games run through the same checks but warn-only, never quarantined, since they cannot be re-exported and are the bulk of training data)
    - src/flag_football_ep/ingest/legacy.py (task 1 output — extend it)
  </read_first>
  <behavior>
    - running `run_checks` on real legacy output and passing the results through `partition_games` leaves every legacy row in `accepted`
    - every FAIL on a legacy game is reported as `Status.WARN` with its detail preserved
    - `ingest_legacy` records the check outcomes in its notices so the report can show them per game, without deciding quarantine itself
    - a legacy game with a genuinely gapless, in-range, score-reconstructable shape reports all PASS (proving warn-only is not masking a broken implementation)
    - the score-reconstruction check on legacy games reports SKIPPED when no `final_scores.csv` entry exists, not FAIL
  </behavior>
  <action>
Extend `src/flag_football_ep/ingest/legacy.py` so `ingest_legacy` runs `validation.checks.run_checks` on its conformed output and stores the resulting `CheckResult` list in `IngestNotices.messages` (rendered form) plus a structured attribute the pipeline can pass straight to the report. Do not call `partition_games` here — quarantine policy belongs to `pipeline.run_ingest` (plan 14), which passes `warn_only_sources={"legacy"}`.

Add tests to `tests/test_ingest_legacy.py` for every `<behavior>` bullet, including `test_warn_only_never_quarantines` (name from the RESEARCH test map) built by feeding a deliberately broken synthetic legacy frame (gap in play_id, down 7) through `run_checks` and `partition_games` with the legacy source. Also add the positive control: a clean synthetic legacy game reports all PASS.

Record in the SUMMARY how many of the real 47 games trip which checks — that list is the operator's data-quality picture for phase 1.3 and should also appear in the run's Markdown report.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingest_legacy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ingest_legacy.py -q` exits 0 with at least 15 tests total
    - `uv run pytest tests/test_ingest_legacy.py -k "warn_only" -q` exits 0
    - a test proves a broken legacy frame stays entirely in `accepted` with WARN results
    - a test proves a clean legacy frame reports all PASS (warn-only is not hiding a broken check wiring)
    - a test proves the score check is SKIPPED, not FAIL, when the reference frame has no entry
    - `grep -c "partition_games" src/flag_football_ep/ingest/legacy.py` returns 0 (policy stays in the pipeline)
  </acceptance_criteria>
  <done>Legacy games are validated and reported without ever being excluded, and the wiring is proven not to mask real failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/raw/legacy/data_raw.csv` → canonical table | grandfathered, hand-maintained data with documented quality problems crosses into the shared dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-03 | Tampering | known-bad legacy rows (polluted ODK, empty RESULT, malformed "Complete Penalty") | accept | documented in the contract as invalid going forward but irreplaceable; mitigated by warn-only reporting so every finding is visible in the run report and available to phase 1.3, rather than by exclusion |
| T-1.2-26 | Tampering | modernizing legacy semantics would silently change the trained model | mitigate | `credit_defense=False` and the `str.contains` predicates are preserved deliberately, asserted by grep gates and tests, and the module docstring names phase 1.3 as the owner of any semantic change |
| T-1.2-18 | Repudiation | legacy rows being dropped without record | mitigate | warn-only partitioning keeps every row and `partition_games` enforces row conservation |
| T-1.2-02 | Information Disclosure | player names in legacy charting columns | accept | file is already tracked in git from project inception; canonical extras carry the columns because phase 1.4 reports need them |
</threat_model>

<verification>
- `uv run pytest tests/test_ingest_legacy.py -q` green
- No `.melt`/`.pivot(aggregate_function=...)` calls anywhere in the package
- Real `data_raw.csv` row count matches the frozen baseline
</verification>

<success_criteria>
- The 47 grandfathered games reach the canonical table with unchanged semantics
- Their data-quality findings are reported, never fatal
- The fragile home/away heuristic exists in exactly one module
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-10-SUMMARY.md` when done
</output>
