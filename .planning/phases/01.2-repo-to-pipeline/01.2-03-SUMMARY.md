---
phase: 01.2-repo-to-pipeline
plan: 03
subsystem: config
tags: [tomllib, dataclasses, dotenv, config, secrets, gitignore]

# Dependency graph
requires:
  - phase: 01.2-01
    provides: package skeleton (src/flag_football_ep) and pyproject.toml that config.py builds into
provides:
  - "ffep.toml as the single checked-in source of paths/reference/sources/train config"
  - "src/flag_football_ep/config.py: load_config(), secret(), load_dotenv(), typed Config dataclasses"
  - "data/raw/{hudl,sportapp,ifaf,legacy}, data/processed, data/reference layout"
  - ".gitignore rules for .env, data/raw/hudl, data/processed, mlruns"
  - "removal of the plaintext sportapp.fi API key from the working tree"
affects: [ingest plans that call load_config(), fetch-sportapp/fetch-ifaf CLI commands, plan 01.2-17 (deletes api_call.ipynb/api_fuzzing.ipynb)]

# Tech tracking
tech-stack:
  added: []
  patterns: ["stdlib tomllib for config, no pydantic/python-dotenv/pyyaml", "paths resolved relative to config file directory, not CWD", "secrets resolved only via os.environ / git-ignored .env, never literals"]

key-files:
  created:
    - ffep.toml
    - src/flag_football_ep/config.py
    - tests/test_config.py
    - .env.example
    - data/raw/hudl/.gitkeep
    - data/raw/sportapp/.gitkeep
    - data/raw/ifaf/.gitkeep
    - data/processed/.gitkeep
  modified:
    - .gitignore
    - data/raw/legacy/data_raw.csv (moved from data_raw.csv)
    - data/reference/half_boundaries.csv (moved from data/half_boundaries.csv)
  deleted:
    - config/config.py

key-decisions:
  - "TOML over YAML for ffep.toml: stdlib tomllib, zero new dependency, matches pyproject.toml style"
  - "load_config() calls load_dotenv() once so ffep invocations pick up .env automatically, without overwriting already-set env vars"
  - "Key rotation deferred by user decision — recorded as an open blocker rather than a completed task"

patterns-established:
  - "Config module: frozen dataclasses (Paths, ReferenceFiles, SportappSource, IfafSource, TrainSettings, Config), ConfigError for missing keys, secret() error messages never contain the value"

requirements-completed: [REQ-S1-06]

# Metrics
duration: unknown (continuation agent; Task 1-2 commits at 2026-08-17T13:44:40+02:00 and 13:46:27+02:00)
completed: 2026-08-17
---

# Phase 01.2 Plan 03: Config layer, data/ layout, and API key removal Summary

**Stdlib-tomllib config module (`ffep.toml` + `load_config()`/`secret()`/`load_dotenv()`) established; `data/raw/{hudl,sportapp,ifaf,legacy}` + `data/processed` + `data/reference` layout populated; plaintext sportapp.fi key deleted from the working tree, but key rotation with the provider remains an open, user-deferred blocker.**

## Performance

- **Tasks:** 3 (2 completed as planned, 1 resolved via the deferred-blocker path)
- **Files modified:** 13 (per plan frontmatter `files_modified`)
- **Task 1 commit:** 2026-08-17T13:44:40+02:00
- **Task 2 commit:** 2026-08-17T13:46:27+02:00
- **Task 3 (checkpoint) resolved:** 2026-08-17 (continuation agent, deferred path)

## Accomplishments

- `ffep.toml` checked in with `[paths]`, `[reference]`, `[sources.sportapp]`, `[sources.ifaf]`, `[train]` tables; training exclusions (`legacy-37`, `legacy-35`) documented in config instead of hardcoded in notebooks
- `src/flag_football_ep/config.py` implements the full `<interfaces>` contract with frozen dataclasses, `load_config()`, `secret()`, and a ~15-line `load_dotenv()` — no pydantic/python-dotenv/pyyaml
- `tests/test_config.py` covers every `<behavior>` bullet (11 tests, all green)
- `data/` layout in place: `data/raw/legacy/data_raw.csv` and `data/reference/half_boundaries.csv` relocated via `git mv`; `data/raw/{hudl,sportapp,ifaf}` and `data/processed` scaffolded with `.gitkeep`
- `.gitignore` updated: `.env`, `data/raw/hudl/*` (PII, same policy as `data/samples/`), `data/processed/*` (regenerable), `mlruns/`, `__pycache__/`, `*.pyc`, `.venv/` — while explicitly keeping `data/raw/legacy/`, `data/raw/sportapp/`, `data/raw/ifaf/`, `data/reference/` tracked
- `config/config.py` (plaintext `API_KEY = 'wRX0tTke...QuSN9'`) and the now-empty `config/` directory deleted
- `.env.example` created documenting `SPORTAPP_API_KEY=` and `CPX_API_KEY=` with no values

## Task Commits

Each task was committed atomically:

1. **Task 1: ffep.toml and the config module** - `e8f707a` (feat)
2. **Task 2: data/ layout, gitignore rules, and removal of the committed API key** - `cdb16cc` (feat)
3. **Task 3: Rotate the exposed sportapp.fi API key** - checkpoint, resolved via deferred path (no code change; see Open Blocker below)

**Plan metadata:** committed alongside this SUMMARY

## Files Created/Modified

- `ffep.toml` - checked-in paths/reference/sources/train config, loadable via `load_config()`
- `src/flag_football_ep/config.py` - Config dataclasses, `load_config()`, `.env` loader, `secret()` resolver
- `tests/test_config.py` - covers TOML loading, path resolution, missing-file/missing-key errors, dotenv parsing (no overwrite of set vars), secret error messages, train exclusions
- `.env.example` - documents `SPORTAPP_API_KEY=` and `CPX_API_KEY=` (optional) with no values
- `.gitignore` - added `.env`, `data/raw/hudl/*` + negated `.gitkeep`, `data/processed/*` + negated `.gitkeep`, `mlruns/`, `__pycache__/`, `*.pyc`, `.venv/`
- `data/raw/legacy/data_raw.csv` - moved from repo-root `data_raw.csv` via `git mv`
- `data/reference/half_boundaries.csv` - moved from `data/half_boundaries.csv` via `git mv`
- `data/raw/{hudl,sportapp,ifaf}/.gitkeep`, `data/processed/.gitkeep`, `data/raw/.gitkeep` - directory skeleton
- `config/config.py` - deleted (plaintext API key removed from working tree)

## Decisions Made

- TOML chosen over YAML for `ffep.toml`: stdlib `tomllib` support, zero new dependency, matches existing `pyproject.toml` style
- `load_config()` invokes `load_dotenv()` once internally so any `ffep` CLI invocation picks up `.env` automatically, without overriding a variable already present in `os.environ`
- Task 3 (key rotation) resolved via the plan's explicit deferred path: the operator answered "deferred," so rotation was not attempted by the agent (it requires contacting the sportapp.fi provider) and is recorded as an open blocker rather than completed work

## Deviations from Plan

None - plan executed exactly as written, including the deferred-checkpoint path defined in Task 3's own `<action>` ("when the answer is 'deferred', add a ... blocker").

## Issues Encountered

None during Task 1-2 re-verification. Task 1's test suite (`uv run pytest tests/test_config.py -q`) re-ran clean at 11/11 passing after resuming this worktree.

## Open Blocker: sportapp.fi API key rotation deferred

Task 3 was a `checkpoint:human-action` gate. The operator responded **"deferred"** — rotation with the sportapp.fi provider has not happened.

- The key `wRX0tTke...QuSN9` remains **valid and compromised**: it was committed in plaintext at `config/config.py` and remains recoverable from git history regardless of the code fix in this plan (T-1.2-09, disposition `transfer`).
- Rotation with the sportapp.fi provider is **pending user action**. No `.env` file exists in this working tree (confirmed: `test -f .env` fails), so `secret("SPORTAPP_API_KEY")` will raise until the operator populates one.
- Until rotation happens, **`ffep fetch-sportapp` will run with a compromised credential** if/when the operator manually sets `SPORTAPP_API_KEY` in `.env` to the old (unrotated) key. The pipeline code itself contains no literal key — this is a live external-provider risk, not a code defect.
- `api_call.ipynb` and `api_fuzzing.ipynb` still contain the literal key `wRX0tTke3DZ8RLKAMntjZ81LwgNQuSN9` (confirmed via grep: only these two `.ipynb` files match, no `.py`/`.toml` file matches). These notebooks are left alone per Task 2's instruction — plan `01.2-17` deletes them.

**Recommended STATE blocker (for orchestrator to record):** "sportapp.fi API key rotation deferred by user (2026-08-17) — `wRX0tTke...QuSN9` remains valid/compromised in git history and in `api_call.ipynb`/`api_fuzzing.ipynb`; `ffep fetch-sportapp` cannot safely run until rotated; notebooks holding the literal key are scheduled for deletion in plan 01.2-17."

## User Setup Required

External service action still pending:
- **Service:** sportapp.fi
- **Why:** Play-by-play API for tournament data (`fetch-sportapp`)
- **Action:** Contact the sportapp.fi provider and rotate/revoke `wRX0tTke...QuSN9`, then create a local `.env` (already git-ignored) with `SPORTAPP_API_KEY=<new key>`.
- **Verification once done:** `uv run python -c "from flag_football_ep.config import load_config, secret; c=load_config(); print(bool(secret(c.sources.sportapp.api_key_env)))"` should print `True`, and `git status --short` should not list `.env`.

## Next Phase Readiness

- `load_config()` is available for every later plan to resolve typed paths and reference file locations
- `data/` layout (`raw/{hudl,sportapp,ifaf,legacy}`, `processed`, `reference`) is in place for ingest plans
- Secrets resolve only from environment/git-ignored `.env`; no literal key remains in any `.py`/`.toml` file
- **Blocker carried forward:** sportapp.fi key rotation is still outstanding — any plan that calls `ffep fetch-sportapp` should treat the current key as compromised until the operator confirms rotation

---
*Phase: 01.2-repo-to-pipeline*
*Completed: 2026-08-17*
