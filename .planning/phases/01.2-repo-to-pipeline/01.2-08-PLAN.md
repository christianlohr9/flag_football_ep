---
phase: 01.2-repo-to-pipeline
plan: 08
type: execute
wave: 3
depends_on: ["01.2-02", "01.2-04"]
files_modified:
  - src/flag_football_ep/features/__init__.py
  - src/flag_football_ep/features/mutations.py
  - tests/test_features_mutations.py
autonomous: true
requirements: [REQ-S1-04]

must_haves:
  truths:
    - "EP and WP feature engineering runs from the package with no notebook and no Python/ import"
    - "The synthetic half_seconds_remaining behaviour is ported unchanged and clearly marked as synthetic"
    - "Total_W_Scaled is computed on the full training corpus and contains no NaN or inf"
  artifacts:
    - path: "src/flag_football_ep/features/mutations.py"
      provides: "prepare_ep_data, prepare_wp_data, add_ep_variables, add_wp_variables, make_ep_model_mutations, make_wp_model_mutations"
      min_lines: 220
      exports: ["prepare_ep_data", "prepare_wp_data", "add_ep_variables", "add_wp_variables", "make_ep_model_mutations", "make_wp_model_mutations"]
    - path: "tests/test_features_mutations.py"
      provides: "unit coverage of half_end/game_end, Next_Score_Half, synthetic clock, weights and label mapping"
  key_links:
    - from: "src/flag_football_ep/features/mutations.py"
      to: "tests/fixtures/baseline_manifest.json"
      via: "column/label-distribution parity asserted against the frozen notebook baseline"
      pattern: "Total_W_Scaled"
    - from: "src/flag_football_ep/features/mutations.py"
      to: "flag_football_ep.model.train"
      via: "make_ep_model_mutations/make_wp_model_mutations produce the training frames"
      pattern: "def make_ep_model_mutations"
---

<objective>
Port the feature-engineering layer out of `Python/helper_add_hudl_mutations.py`, `helper_add_ep_wp.py` and `helper_add_model_mutations.py` into `src/flag_football_ep/features/mutations.py`, behaviour-preserving.

Purpose: REQ-S1-04's core — no notebook, no `sys.path.insert(0, 'Python')`. The cut-line from CONTEXT is explicit: port with obvious fixes only, no methodological redo. Every evaluation change (GroupKFold, calibration, empirical PAT baselines) is phase 1.3 and must NOT appear here.
Output: `features/mutations.py` plus a test module covering the derivation rules and the weighting invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@Python/helper_add_hudl_mutations.py
@Python/helper_add_ep_wp.py
@Python/helper_add_model_mutations.py
@src/flag_football_ep/canonical.py

<interfaces>
<!-- Feature API consumed by model/train.py (plan 13), model/score.py (plan 15) and pipeline (plan 14). -->

flag_football_ep.features.mutations:
  prepare_ep_data(plays: pl.DataFrame) -> pl.DataFrame
      # adds: index, half_end, game_end, scoring_event, score_drive, start_posteam,
      #       Next_Score_Half, Drive_Score_Half, max_play_id
  prepare_wp_data(plays: pl.DataFrame) -> pl.DataFrame
      # adds: index, half_end, game_end, helper_one, play_id_half, play_time,
      #       half_seconds_remaining (SYNTHETIC), game_seconds_remaining, elapsed_share,
      #       Diff_Time_Ratio, start_posteam, receive_2h_ko
  add_ep_variables(df: pl.DataFrame) -> pl.DataFrame     # needs the five EP probability columns
  add_wp_variables(df: pl.DataFrame) -> pl.DataFrame     # needs the wp probability column
  make_ep_model_mutations(df: pl.DataFrame, selected_columns: Sequence[str]) -> pl.DataFrame
      # adds label (0..4), down0..down4, Drive_Score_Dist, Drive_Score_Dist_W, ScoreDiff_W,
      #      Total_W, Total_W_Scaled; filters null yardline_50/yards_to_go; selects columns
  make_wp_model_mutations(df: pl.DataFrame, selected_columns: Sequence[str]) -> pl.DataFrame
      # adds Winner, label (0/1); selects columns

EP probability column names (order matters for the model's predict output):
  ["Touchdown_Prob", "Opp_Touchdown_Prob", "Safety_Prob", "Opp_Safety_Prob", "No_Score_Prob"]

EP label mapping (helper_add_model_mutations lines 16-25):
  Touchdown 0, Opp_Touchdown 1, Safety 2, Opp_Safety 3, No_Score 4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port prepare_ep_data and prepare_wp_data</name>
  <files>src/flag_football_ep/features/__init__.py, src/flag_football_ep/features/mutations.py, tests/test_features_mutations.py</files>
  <read_first>
    - Python/helper_add_hudl_mutations.py lines 321-407 (prepare_ep_data and prepare_wp_data verbatim: the half_end index trick, game_end, scoring_event, score_drive, start_posteam, the backward_fill block, Next_Score_Half, Drive_Score_Half, max_play_id; then helper_one/play_id_half/play_time/half_seconds_remaining/game_seconds_remaining/elapsed_share/Diff_Time_Ratio/receive_2h_ko)
    - .planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md (features/mutations.py section: the half_end and synthetic-clock excerpts, and the explicit "port as-is" note on the synthetic clock)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (cut-line: port training/scoring with obvious fixes, NOT a methodological redo; real time data is REQ-S1-02 / phase 1.4 territory)
    - src/flag_football_ep/canonical.py (the canonical frame these functions consume: no `index` column exists, so these functions must create their own)
    - tests/fixtures/baseline_manifest.json (the frozen notebook statistics these ports must stay consistent with)
  </read_first>
  <behavior>
    - `prepare_ep_data` creates its own 1-based `index` column when absent (the canonical schema has none; the notebook inherited it from `make_hudl_mutations`' trailing `with_row_index(offset=1)`)
    - `half_end` is 1 on exactly the last row of each (game_id, half) group and 0 elsewhere
    - `game_end` is 1 only where `half_end == 1` and `half == 2`
    - `scoring_event` maps touchdown and def_touchdown to "Touchdown", safety to "Safety", one_point_conv_success to "Extra_Point", two_point_conv_success to "Two_Point_Conversion", and a non-scoring half end to "No_Score"
    - `Next_Score_Half` becomes "Opp_Touchdown" when the backward-filled scoring team is not the play's posteam, and "Touchdown" when it is
    - `Drive_Score_Half` equals the play's own `drive_id` on "No_Score" rows and the backward-filled scoring drive otherwise
    - `prepare_wp_data` produces `half_seconds_remaining` starting at 1200 per half and decreasing monotonically, computed from play counts (synthetic), and `game_seconds_remaining` from 2400
    - `receive_2h_ko` is 0 when the play's posteam equals the game's first-play posteam and 1 otherwise
    - `Diff_Time_Ratio` equals `score_differential / exp(-4 * elapsed_share)`
    - a two-game frame produces per-game half_end/clock values with no cross-game leakage
  </behavior>
  <action>
Create `src/flag_football_ep/features/__init__.py` with only a docstring.

Create `src/flag_football_ep/features/mutations.py` and port `prepare_ep_data` and `prepare_wp_data` from `Python/helper_add_hudl_mutations.py` lines 321-407. Port the logic, not the style: replace the deprecated-prone `pl.when(play_id_half=1)` keyword-comparison forms with explicit `pl.col(...) == ...` predicates, and keep every derived column name, formula and constant identical (1200 per half, 2400 per game, the `exp(-4 * elapsed_share)` factor).

Requirements:
- If `index` is absent, create it with `with_row_index(name="index", offset=1)` before the half_end computation, and keep the original `unique(subset=["game_id","half"], keep="last")` semantics so ordering behaviour matches the frozen baseline exactly.
- Mark `half_seconds_remaining` as synthetic in the docstring and in an inline comment: it is derived as `1200 / max(play_id_half)` per half because the analyst has not delivered real clock data (REQ-S1-02 pending). Phase 1.4 gates the WP charts on this flag. Do not attempt to use `game_clock_ms` here — IFAF real clock consumption is out of this phase's cut-line.
- Every window operation must be `.over(["game_id", ...])`-scoped; no global cumulative operations.
- Import nothing from `Python/`.

Write `tests/test_features_mutations.py` covering every `<behavior>` bullet, building inputs with `flag_football_ep.testing.canonical_plays_with_scores` and targeted `overrides`. Name the half-derivation test `test_half_from_boundary_file`-adjacent cases explicitly (`test_half_end_flags`, `test_game_end_flag`) and include the two-game no-leakage case.
  </action>
  <verify>
    <automated>uv run pytest tests/test_features_mutations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_features_mutations.py -q` exits 0 with at least 10 tests
    - `uv run python -c "import polars as pl; from flag_football_ep.testing import canonical_plays_with_scores as f; from flag_football_ep.features.mutations import prepare_ep_data, prepare_wp_data; d=prepare_ep_data(f(n_games=2, plays_per_game=8)); assert {'index','half_end','game_end','scoring_event','Next_Score_Half','Drive_Score_Half','max_play_id'} <= set(d.columns); w=prepare_wp_data(f(n_games=2, plays_per_game=8)); assert {'half_seconds_remaining','game_seconds_remaining','elapsed_share','Diff_Time_Ratio','receive_2h_ko'} <= set(w.columns); assert w['half_seconds_remaining'].max() <= 1200; print('prepare OK')"` exits 0
    - a test proves `half_end` is 1 on exactly one row per (game_id, half)
    - a test proves `half_seconds_remaining` is non-increasing within each (game_id, half)
    - a test proves two games do not leak clock or half_end values
    - `grep -c "sys.path\|from helper_\|import helper_" src/flag_football_ep/features/mutations.py` returns 0
    - `grep -c "synthetic" src/flag_football_ep/features/mutations.py` returns at least 1
  </acceptance_criteria>
  <done>EP and WP data preparation runs from the package with the notebook's formulas intact and the synthetic clock explicitly marked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port add_ep_variables and add_wp_variables</name>
  <files>src/flag_football_ep/features/mutations.py, tests/test_features_mutations.py</files>
  <read_first>
    - Python/helper_add_ep_wp.py lines 1-186 (add_ep_variables in full: ExpPts weighting, ep/tmp_posteam backward_fill, home_ep/home_ep_after/home_epa, the interception sign flip, the touchdown 6-point branch, and the hard-coded PAT baselines 0.5 and 0.92 at lines 57, 64, 71, 78)
    - Python/helper_add_ep_wp.py lines 187-244 (add_wp_variables in full: wp backward_fill, home_wp/away_wp/def_wp, the game_end final_value branch, wpa, home_wp_post/away_wp_post, total_home_wp/total_away_wp)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (port as-is; empirical PAT baselines are REQ-S1-10, phase 1.3)
    - src/flag_football_ep/features/mutations.py (task 1 output — extend it)
  </read_first>
  <behavior>
    - `add_ep_variables` computes `ExpPts` as 0*No_Score_Prob + 2*Safety_Prob + 6*Touchdown_Prob - 2*Opp_Safety_Prob - 6*Opp_Touchdown_Prob
    - `add_ep_variables` raises a clear error naming the missing probability columns when any of the five is absent
    - `epa` on an interception row is the negated forward difference (the sign flip at helper_add_ep_wp.py lines 37-41 is preserved)
    - a touchdown row's `epa` equals 6 - ep when the scoring team is the posteam and -6 - ep otherwise
    - the hard-coded PAT baselines 0.5 (1-point from the 5) and 0.92 (2-point from the 10) are preserved verbatim and carry a comment naming REQ-S1-10 / phase 1.3 as the replacement owner
    - `add_wp_variables` produces `home_wp`, `away_wp` summing to 1 on every row, `def_wp = 1 - wp`, and at `game_end` a `final_value` of 1 / 0 / 0.5 for a home win / loss / tie
    - `add_wp_variables` raises a clear error when the `wp` probability column is absent
    - both functions are pure: the input frame is not mutated
  </behavior>
  <action>
Extend `src/flag_football_ep/features/mutations.py` with `add_ep_variables` and `add_wp_variables`, ported from `Python/helper_add_ep_wp.py` with no formula changes.

Two additions permitted (they change nothing about the numbers):
1. A guard at the top of each function that raises `MissingFeatureColumns` (define it in this module) naming every missing required probability column — the notebook failed with an opaque polars ColumnNotFound.
2. A module-level constant `PAT_BASELINE_ONE_POINT = 0.5` and `PAT_BASELINE_TWO_POINT = 0.92` replacing the inline literals, with a comment stating these are the notebook's hard-coded assumptions and that REQ-S1-10 (phase 1.3) replaces them with empirical estimates. Same numbers, one place to change later.

Add the `<behavior>` cases to `tests/test_features_mutations.py`, constructing probability columns explicitly (five EP probabilities summing to 1, and a `wp` column) on top of `canonical_plays_with_scores` frames.
  </action>
  <verify>
    <automated>uv run pytest tests/test_features_mutations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_features_mutations.py -q` exits 0 with at least 18 tests total
    - a test proves `ExpPts` equals the documented weighted sum on a hand-computed row
    - a test proves the interception sign flip changes `epa`'s sign relative to an otherwise identical non-interception row
    - a test proves `home_wp + away_wp == 1` on every row
    - a test proves a missing probability column raises `MissingFeatureColumns` naming it
    - `uv run python -c "from flag_football_ep.features.mutations import PAT_BASELINE_ONE_POINT, PAT_BASELINE_TWO_POINT; assert (PAT_BASELINE_ONE_POINT, PAT_BASELINE_TWO_POINT)==(0.5,0.92); print('baselines preserved')"` exits 0
    - `grep -c "REQ-S1-10" src/flag_football_ep/features/mutations.py` returns at least 1
  </acceptance_criteria>
  <done>EP/WP variable application runs from the package with identical numbers and clearly labelled phase-1.3 placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port the model-mutation layer and prove the weighting invariant</name>
  <files>src/flag_football_ep/features/mutations.py, tests/test_features_mutations.py</files>
  <read_first>
    - Python/helper_add_model_mutations.py (both functions in full: the label mapping, down0..down4 dummies, Drive_Score_Dist, the three weight columns, the null filter and the select; then Winner/label for WP)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Pitfall 4: the Total_W_Scaled division-by-zero risk and the full-corpus invariant that must be preserved)
    - tests/fixtures/baseline_manifest.json (`ep_model_data.label_distribution` and `total_w_scaled` min/max/mean/n_nan — the frozen expectations)
    - src/flag_football_ep/features/mutations.py (tasks 1-2 output — extend it)
  </read_first>
  <behavior>
    - `make_ep_model_mutations` maps Next_Score_Half to label: Touchdown 0, Opp_Touchdown 1, Safety 2, Opp_Safety 3, No_Score 4, and leaves other values null
    - `down0`..`down4` are one-hot for down 0..4, with exactly one 1 per row for in-range downs
    - `Drive_Score_Dist` equals `Drive_Score_Half - drive_id`
    - `Total_W_Scaled` lies in [0, 1] and contains no NaN and no inf for a multi-game corpus
    - a single-game frame with zero-range `Drive_Score_Dist` raises `DegenerateWeightRange` (defined in this module) instead of emitting NaN, naming the full-corpus invariant
    - rows with null `yardline_50` or null `yards_to_go` are filtered out before selection
    - the returned frame's columns equal the passed `selected_columns` in that exact order
    - `make_wp_model_mutations` sets Winner from the game_end scores, backward-fills it, and labels 1 when posteam equals Winner else 0; a tie yields label 0 for both teams
  </behavior>
  <action>
Extend `src/flag_football_ep/features/mutations.py` with `make_ep_model_mutations` and `make_wp_model_mutations`, ported from `Python/helper_add_model_mutations.py`.

Preserve exactly: the label mapping, the commented-out Extra_Point / Two_Point_Conversion label branches (keep them as comments so phase 1.3 sees the history), the down dummies, `Drive_Score_Dist`, `Drive_Score_Dist_W`, `ScoreDiff_W`, `Total_W`, `Total_W_Scaled`, the null filter and the final `select(selected_columns)`.

One addition (RESEARCH Pitfall 4): before computing `Drive_Score_Dist_W`, check that `max(Drive_Score_Dist) != min(Drive_Score_Dist)` and that the absolute `score_differential` range is non-zero; raise `DegenerateWeightRange` with a message stating that these weights must be computed on the full training corpus, never per game. This converts a silent NaN-weight failure into a loud one and encodes the invariant the notebook only satisfied by accident.

Add the `<behavior>` cases to `tests/test_features_mutations.py`, including an explicit `test_total_w_scaled_has_no_nan` and a degenerate single-game case asserting the raise. Also assert the label distribution shape (five distinct labels plus nulls) on a synthetic multi-game corpus.
  </action>
  <verify>
    <automated>uv run pytest tests/test_features_mutations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_features_mutations.py -q` exits 0 with at least 26 tests total
    - `uv run pytest tests/test_features_mutations.py -k "total_w_scaled" -q` exits 0
    - a test proves `Total_W_Scaled` has zero NaN, zero inf and lies within [0, 1]
    - a test proves a zero-range single-game frame raises `DegenerateWeightRange`
    - a test proves the output columns equal the passed `selected_columns` list exactly, in order
    - a test proves rows with null `yardline_50` are dropped
    - a test proves a tie game labels both teams 0 in `make_wp_model_mutations`
  </acceptance_criteria>
  <done>Training frames are produced by the package, and the weighting invariant that used to fail silently now fails loudly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical Parquet → feature frames | already-validated local data; no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-22 | Tampering | silent NaN/inf sample weights degrading the trained model | mitigate | `DegenerateWeightRange` raises when the weight denominators collapse, and a test asserts zero NaN/inf in `Total_W_Scaled` (RESEARCH Pitfall 4) |
| T-1.2-23 | Repudiation | undocumented hard-coded PAT baselines silently shaping EPA | mitigate | the 0.5 / 0.92 literals become named constants annotated with REQ-S1-10 and phase 1.3 as the replacement owner |
| T-1.2-24 | Tampering | opaque failure when a probability column is missing | mitigate | `MissingFeatureColumns` names every missing column instead of surfacing a polars ColumnNotFound |
</threat_model>

<verification>
- `uv run pytest tests/test_features_mutations.py -q` green
- `grep -rn "sys.path" src/flag_football_ep/` returns nothing
- Feature module imports only polars/numpy and `flag_football_ep.*`
</verification>

<success_criteria>
- Feature engineering runs from the package, formula-identical to the notebook
- The synthetic clock and the hard-coded PAT baselines are explicit, not hidden
- The weighting invariant is enforced instead of assumed
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-08-SUMMARY.md` when done
</output>
