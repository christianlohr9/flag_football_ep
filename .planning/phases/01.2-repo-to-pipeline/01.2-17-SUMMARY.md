---
phase: 01.2-repo-to-pipeline
plan: 17
subsystem: repo-hygiene
tags: [polars, typer, mlflow, jupyter, pytest, cleanup]

# Dependency graph
requires:
  - phase: 01.2-repo-to-pipeline
    provides: "run_ingest/run_all (plan 14, 16), migration-equivalence test + frozen baseline fixtures (plan 02, 16), the full src/flag_football_ep package (all prior 01.2 plans) -- everything Python/ and the notebooks needed to be provably superseded before deletion"
provides:
  - "clean repository root: no CSV/xlsx sprawl, all live inputs under data/raw or data/reference"
  - "Python/ helper directory deleted (fully absorbed into src/flag_football_ep by earlier plans)"
  - "three thin demo notebooks (pipeline_demo, ep_model_demo, wp_model_demo) that only import the package and visualize data/processed output"
  - "explainable_ai_ep.ipynb / explainable_ai_wp.ipynb moved to notebooks/ with package imports instead of sys.path/Python/ hacks"
  - "tests/test_repo_hygiene.py: 14 automated gates against sprawl, Python/, sys.path, and demo notebooks growing pipeline logic"
  - "scripts/capture_notebook_baseline.py guarded to fail with clear worktree-regeneration instructions now that Python/ is gone"
  - "docs/pipeline.md: full operational reference (quickstart, data layout, reference-file maintenance, validation/quarantine semantics, config reference, MLflow tracking, known limitations)"
  - "README.md rewritten as a short entry point linking to docs/pipeline.md, docs/data-contract.md, docs/ifaf-field-mapping.md"
affects: [phase-1.3-methodological-review, phase-1.4-reporting]

tech-stack:
  added: []
  patterns:
    - "hygiene tests as the enforcement mechanism (not discipline): notebooks/*.ipynb JSON validity, no sys.path/from helper_ imports anywhere, Python//insights/ absence, root CSV/xlsx sprawl, and a code-cell cap on the three demo notebooks"
    - "scripts that depend on a since-deleted directory fail fast with the exact commit hash and git worktree add command needed to regenerate their output, instead of an opaque ImportError"

key-files:
  created:
    - notebooks/pipeline_demo.ipynb
    - notebooks/ep_model_demo.ipynb
    - notebooks/wp_model_demo.ipynb
    - tests/test_repo_hygiene.py
    - docs/pipeline.md
    - .planning/phases/01.2-repo-to-pipeline/deferred-items.md
  modified:
    - README.md
    - scripts/capture_notebook_baseline.py
    - src/flag_football_ep/testing.py
    - notebooks/explainable_ai_ep.ipynb (moved from insights/, import cells rewritten)
    - notebooks/explainable_ai_wp.ipynb (moved from insights/, import cells rewritten)
    - data/reference/roster.csv (moved from team_roster.csv)
    - data/raw/legacy/pbp_wc24_static.xlsx (moved from repo root)

key-decisions:
  - "insights/insights.ipynb deleted alongside its only inputs (model_eval_situations*.csv, df2.csv), even though not literally named in the plan's <interfaces> deletion list -- it read exactly those files (confirmed: no notebook writes them, all are derived-schema exports), so it was already permanently broken once Task 1 deleted its inputs, and the plan's own action item 4 requires insights/ to end up empty and removed. Logged as a deviation, not silently dropped."
  - "The repo-wide grep -rn \"sys.path\" src/ notebooks/ acceptance gate (task 2) caught a docstring-only mention in src/flag_football_ep/testing.py (\"no conftest edits or sys.path tricks\") -- reworded to \"import-path hacks\" rather than weakening the hygiene test's literal substring check, since a lingering mention is exactly what invites the hack back."
  - "Real ffep run executed for this plan's documentation (257 games / 21,437 plays / 1.79s total) rather than reusing plan 16's numbers verbatim, so every command and figure in docs/pipeline.md is this plan's own evidence, per the plan's closing line: every command shown must be one actually run in this phase."
  - "explainable_ai_{ep,wp}.ipynb's analysis cells (cell 2 onward) were left untouched per the plan's explicit instruction, including a pre-existing .drop(\"label\") bug that predates this plan and would fail identically against the original unmodified Python/ helpers -- logged in deferred-items.md rather than fixed, per the scope-boundary rule."

patterns-established:
  - "Demo notebooks under notebooks/ are execution-verified (not just JSON-parsed): each was run cell-by-cell against the real data/processed/*.parquet this plan produced, confirming they actually work, not just parse."

requirements-completed: [REQ-S1-04, REQ-S1-06]

# Metrics
duration: ~23min
completed: 2026-08-17
---

# Phase 01.2 Plan 17: Repo Cleanup, Thin Demo Notebooks and Pipeline Docs Summary

**Deleted the repo-root CSV/xlsx sprawl and the fully-absorbed `Python/` helper directory, replaced the logic-bearing notebooks with three execution-verified thin demos plus two import-rewritten SHAP notebooks, added `tests/test_repo_hygiene.py` as the automated enforcement mechanism, and wrote `docs/pipeline.md` (232 lines) plus a rewritten `README.md` documenting a real `ffep run` (257 games / 21,437 plays / 1.79s).**

## Performance

- **Duration:** ~23 min
- **Started:** 2026-08-17T17:13:00Z (worktree base correction + context read)
- **Completed:** 2026-08-17T17:35:22Z
- **Tasks:** 3 (all `type="auto"`), plus one same-scope corrective commit
- **Files modified:** 25 files in task 2 alone; 14 in task 1; 2 in task 3 (see Task Commits)

## Accomplishments

- Root inventory cleanup: `pbp_wc24_static.xlsx` -> `data/raw/legacy/`, `team_roster.csv` -> `data/reference/roster.csv`; 12 derivable/superseded files deleted with evidence recorded per file (see Task 1 below) -- the repository root now holds zero `.csv`/`.xlsx` files.
- `Python/` (4 helper modules, fully ported into `src/flag_football_ep` by earlier 01.2 plans) and the six logic-bearing notebooks deleted via `git rm`; `scripts/capture_notebook_baseline.py` guarded to exit 1 with the exact pre-migration commit hash and `git worktree add` command needed to regenerate its fixtures, instead of an opaque `ModuleNotFoundError`.
- Three thin demo notebooks created and **execution-verified against real pipeline output**: `pipeline_demo.ipynb` (row counts by source/competition/season + the latest validation report's quarantine section), `ep_model_demo.ipynb` (EP by field position per down + EPA leaders by team), `wp_model_demo.ipynb` (WP curve for one game, explicitly labeled as play-sequence not real time per the synthetic-clock limitation).
- `insights/explainable_ai_ep.ipynb`/`explainable_ai_wp.ipynb` moved to `notebooks/`, import cells rewritten to `from flag_football_ep.features.mutations import ...` / `from flag_football_ep.ingest.legacy import legacy_mutations as make_hudl_mutations`, analysis cells left untouched; `insights/` directory fully removed.
- `tests/test_repo_hygiene.py`: 14 tests gating notebook JSON validity, `sys.path`/`from helper_` absence, `Python/`/`insights/` absence, root sprawl, and a 15-code-cell cap on the three demos with a forbidden-token check (`write_parquet`, `.fit(`, `mlflow.`, etc.) so they can't silently grow pipeline logic.
- `docs/pipeline.md` (232 lines, all 8 required sections) and a rewritten `README.md` (68 lines): every command shown was actually executed during this plan, including a real `ffep run` (257 games, 21,437 plays, 10 quarantined, 1.79s total) and `ffep fetch-sportapp`'s clean "environment variable not set" failure path.

## Task Commits

1. **Task 1: Root inventory cleanup and reference relocation** - `3726d89` (chore)
2. **Task 2: Delete Python/ and rewrite the notebooks as thin demos** - `04970b6` (feat)
3. **Corrective: capture the explainable_ai import rewrite the task 2 `git add` missed** - `84fefd9` (fix, discovered via post-commit self-check, see Deviations)
4. **Task 3: Pipeline documentation and README** - `4dcaf78` (docs)

**Plan metadata:** committed alongside this SUMMARY.md

## Files Created/Modified

- `notebooks/pipeline_demo.ipynb`, `ep_model_demo.ipynb`, `wp_model_demo.ipynb` - thin demos, execution-verified against real `data/processed/*.parquet`
- `notebooks/explainable_ai_ep.ipynb`, `explainable_ai_wp.ipynb` - moved from `insights/`, import cells rewritten
- `tests/test_repo_hygiene.py` - 14 hygiene gates (new file)
- `scripts/capture_notebook_baseline.py` - `_require_python_dir()` guard added
- `src/flag_football_ep/testing.py` - docstring reworded to drop a `sys.path` substring match caught by the hygiene test
- `docs/pipeline.md` - new, 232 lines
- `README.md` - rewritten, 68 lines
- `data/reference/roster.csv`, `data/raw/legacy/pbp_wc24_static.xlsx` - relocated from repo root
- `.planning/phases/01.2-repo-to-pipeline/deferred-items.md` - pre-existing bug logged out of scope

## Decisions Made

See `key-decisions` in the frontmatter: `insights/insights.ipynb` deletion (implicit but required), the `testing.py` docstring reword (needed for the plan's own literal grep gate), running a fresh real `ffep run` for documentation evidence, and leaving the explainable_ai notebooks' analysis cells (including a pre-existing bug) untouched.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] `insights/insights.ipynb` deleted alongside its now-gone inputs**
- **Found during:** Task 1 evidence-gathering (confirming `model_eval_situations*.csv`/`df2.csv` were derivable) and Task 2 (removing `insights/`)
- **Issue:** the plan's `<interfaces>` deletion list names `insights/model_eval_situations*.csv` and `insights/df2.csv` but not `insights/insights.ipynb` itself, which is the only notebook that reads them. Leaving it in place would mean a permanently broken notebook sitting in a directory the plan's own action item 4 requires to end up empty and removed.
- **Fix:** confirmed via grep that no notebook writes these CSVs (all have the mutated-dataframe column schema, not the raw Hudl export schema, confirming they are derived exports) and that `insights/insights.ipynb` is the only reader; deleted it with `git rm` and removed the now-empty `insights/` directory.
- **Files modified:** `insights/insights.ipynb` (deleted)
- **Committed in:** `04970b6`

**2. [Rule 1 - Bug] Reworded a `sys.path`-mentioning docstring to satisfy the plan's own literal hygiene grep**
- **Found during:** Task 2, running `tests/test_repo_hygiene.py`
- **Issue:** `src/flag_football_ep/testing.py`'s docstring said "...with no conftest edits or sys.path tricks" -- a true statement, but it makes `grep -rn "sys.path" src/` (task 2's own literal acceptance criterion) return 1, not 0.
- **Fix:** reworded to "...or import-path hacks", preserving the meaning without the literal substring.
- **Files modified:** `src/flag_football_ep/testing.py`
- **Verification:** `grep -rn "sys.path" src/ notebooks/ | wc -l` returns 0; full suite still green.
- **Committed in:** `04970b6`

**3. [Rule 1 - Bug] Corrective commit for a `git add` staging miss**
- **Found during:** post-Task-2 self-check (diffing the commit against the working tree)
- **Issue:** the Task 2 `git add` command listed `Python` and `__pycache__` (already deleted by an earlier `git rm -r`) as its first two arguments; git's fatal "pathspec did not match any files" error aborted that entire `git add` invocation before it reached `notebooks/explainable_ai_{ep,wp}.ipynb`'s content changes (their `git mv` rename was already staged from an earlier command, but the subsequent import-cell rewrite was not). The task 2 commit therefore captured the plain rename with the *original* `sys.path.insert`/`from helper_*` imports still inside it, even though the working tree and the hygiene-test run against it were already correct.
- **Fix:** staged and committed the missing content diff in a dedicated follow-up commit rather than amending the already-pushed-to-history task 2 commit.
- **Files modified:** `notebooks/explainable_ai_ep.ipynb`, `notebooks/explainable_ai_wp.ipynb`
- **Verification:** `git show 84fefd9:notebooks/explainable_ai_ep.ipynb` confirmed the package imports are now in history; `tests/test_repo_hygiene.py` and the full suite both green after.
- **Committed in:** `84fefd9`

---

**Total deviations:** 3 auto-fixed (1 missing-critical, 1 bug/gate-compliance, 1 bug/staging-correction). No scope creep -- all three were necessary to make this plan's own stated acceptance criteria true and to make the git history match what was actually verified.

## Issues Encountered

- `plt.show()` in the demo notebooks hung when executed via a plain Python `exec()` harness under the default matplotlib backend (no `nbclient`/Jupyter available in this environment to execute the `.ipynb` files directly). Resolved by setting `MPLBACKEND=Agg` for the verification runs; this does not affect the notebooks themselves, which render normally inline under a real Jupyter kernel.
- `data/wc24_pbp_short.csv`'s claimed "row subset of `wc24_pbp.csv`" needed byte-level verification: a naive `diff` reported every line as different because the two files use different CSV quote-escaping dialects (and CRLF vs LF line endings) for the same underlying content. Verified equivalence properly with a normalizing Python comparison before deleting.

## Known Stubs

None -- the three demo notebooks read real, already-produced `data/processed/*.parquet` (from this plan's own `ffep run`), not placeholder/mock data.

## Threat Flags

None. This plan's threat register (T-1.2-34, T-1.2-01, T-1.2-02, T-1.2-35) was directly implemented: every deletion is evidence-backed and went through `git rm`/`git mv` (T-1.2-34); `docs/pipeline.md` documents only `.env`/`.env.example`, never a committed secret path (T-1.2-01); the demo notebooks read git-ignored `data/processed` and write nothing, enforced by the hygiene test's forbidden-token check (T-1.2-02); `capture_notebook_baseline.py` keeps its regeneration path documented and working (T-1.2-35). No new surface introduced.

## Reminder: the sportapp.fi key blocker is unchanged

Per `.planning/STATE.md`'s recorded blocker, the sportapp.fi API key that was previously
committed in plaintext (`api_call.ipynb`, `api_fuzzing.ipynb`, both deleted by this plan)
**remains compromised in git history regardless of this deletion** -- deleting the notebooks
is repo hygiene, not remediation. `docs/pipeline.md` section 8 restates this explicitly.
`ffep fetch-sportapp` still fails cleanly with a clear "environment variable not set" error
(verified during this plan) until the key is rotated with the provider and `.env` is updated.

## User Setup Required

None - no external service configuration required. (The unrotated `SPORTAPP_API_KEY` remains
a pre-existing, user-owned blocker unaffected by this plan -- see above.)

## Next Phase Readiness

- REQ-S1-04's endpoint is met: `Python/` is gone, nothing imports it (`grep -rn "sys.path" src/ notebooks/` returns 0, `tests/test_repo_hygiene.py` gates it going forward), and notebooks are thin demos that import the package.
- REQ-S1-06's consolidation half is met: no root CSV/xlsx sprawl; every live input lives under `data/raw/` or `data/reference/`.
- `docs/pipeline.md` gives phase 1.3/1.4 (and any future operator) a complete, evidence-based operational reference -- CLI, data layout, reference-file maintenance, validation semantics, config, MLflow, and the three still-open limitations (synthetic clock, provisional contract, hard-coded PAT baselines) they need to account for.
- One pre-existing, out-of-scope bug logged in `deferred-items.md` (`explainable_ai_{ep,wp}.ipynb` cell 2's `.drop("label")`) for whichever future phase next touches those notebooks' analysis logic.
- No blockers introduced by this plan. The sportapp.fi key rotation blocker (STATE.md, pre-existing) is unchanged.

---
*Phase: 01.2-repo-to-pipeline*
*Completed: 2026-08-17*

## Self-Check: PASSED

All 14 referenced files confirmed present on disk (notebooks/{pipeline,ep_model,wp_model}_demo.ipynb,
notebooks/explainable_ai_{ep,wp}.ipynb, tests/test_repo_hygiene.py,
scripts/capture_notebook_baseline.py, src/flag_football_ep/testing.py, docs/pipeline.md,
README.md, data/reference/roster.csv, data/raw/legacy/pbp_wc24_static.xlsx,
deferred-items.md, this SUMMARY.md). All 4 commit hashes (`3726d89`, `04970b6`, `84fefd9`,
`4dcaf78`) confirmed present in `git log --oneline --all`.
