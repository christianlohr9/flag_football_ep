---
phase: 01.2-repo-to-pipeline
plan: 11
type: execute
wave: 4
depends_on: ["01.2-04", "01.2-05", "01.2-07"]
files_modified:
  - src/flag_football_ep/ingest/sportapp.py
  - tests/test_ingest_sportapp.py
  - data/reference/sportapp_games.csv
  - data/raw/legacy/wc24_pbp.csv
autonomous: false
requirements: [REQ-S1-06]

must_haves:
  truths:
    - "sportapp.fi snapshots on disk parse into the canonical schema without any network call"
    - "The WC24 tournament data (14,546 plays) has an explicit, recorded disposition — neither silently dropped nor silently duplicated"
    - "The sportapp mutation chain stays independent from the Hudl RESULT chain, converging only at the canonical schema"
  artifacts:
    - path: "src/flag_football_ep/ingest/sportapp.py"
      provides: "load_snapshot, flatten_plays, clean_yardage, clean_play_ids, correct_posteam, add_event_columns, ingest_snapshots"
      min_lines: 240
      exports: ["load_snapshot", "ingest_snapshots"]
    - path: "data/reference/sportapp_games.csv"
      provides: "maintained game-id list for fetch and ingest"
      contains: "source_game_id"
    - path: "tests/test_ingest_sportapp.py"
      provides: "yardline, play/drive id, event-column and canonical-merge coverage"
  key_links:
    - from: "src/flag_football_ep/ingest/sportapp.py"
      to: "data/raw/sportapp/*.json"
      via: "load_snapshot reading fetched payloads from disk"
      pattern: "match-drives"
    - from: "src/flag_football_ep/ingest/sportapp.py"
      to: "flag_football_ep.canonical.conform_to_canonical"
      via: "canonical convergence"
      pattern: "conform_to_canonical"
---

<objective>
Port the sportapp.fi parsing chain into `ingest/sportapp.py` reading fetched snapshots from disk, and settle the disposition of the already-mutated `data/wc24_pbp.csv`.

Purpose: REQ-S1-06 requires Hudl and sportapp.fi/IFAF data in one canonical schema. RESEARCH found that `data/wc24_pbp.csv` is not raw data but the fully-mutated output of the old pipeline, currently concatenated post-mutation inside the notebooks — a fact CONTEXT.md does not resolve. Getting this wrong either drops 14,546 plays from the training corpus or duplicates them.
Output: `ingest/sportapp.py`, its tests, a seeded `sportapp_games.csv`, and a recorded decision on the WC24 file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@Python/fetch_sportappfi_api.py
@src/flag_football_ep/canonical.py
@src/flag_football_ep/fetch/sportapp.py

<interfaces>
<!-- sportapp ingest API consumed by pipeline.run_ingest (plan 14). -->

flag_football_ep.ingest.sportapp:
  load_snapshot(drives_path: Path, match_path: Path) -> tuple[dict, dict]
  flatten_plays(drives_data: dict, match_data: dict, game_id: str) -> pl.DataFrame
  clean_yardage(df) -> pl.DataFrame
  clean_play_ids(df) -> pl.DataFrame
  correct_posteam(df) -> pl.DataFrame
  add_event_columns(df) -> pl.DataFrame
  add_team_points_chain(df) -> pl.DataFrame        # delegates to canonical.add_score_columns
  ingest_snapshots(raw_dir: Path, team_mapping: pl.DataFrame,
                   game_ids: Sequence[str] | None = None) -> list[tuple[str, pl.DataFrame, IngestNotices]]

Snapshot files written by plan 07:
  data/raw/sportapp/match-drives_{game_id}.json
  data/raw/sportapp/match-v1_{game_id}.json
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Disposition of data/wc24_pbp.csv (14,546 already-mutated WC24 plays)</name>
  <files>data/wc24_pbp.csv</files>
  <read_first>
    - data/wc24_pbp.csv (header and row count: the 90+ already-mutated columns that prove it is pipeline output, not raw data)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Runtime State Inventory row on wc24_pbp.csv and Open Question 2)
    - .planning/phases/01.2-repo-to-pipeline/01.2-03-SUMMARY.md (whether the sportapp.fi key was rotated or the rotation was deferred — this determines whether a live re-fetch is possible)
  </read_first>
  <action>Present the decision below to the operator and wait for the selection. Before presenting, report the concrete numbers: the row count of `data/wc24_pbp.csv`, its distinct `game_id` count, and whether `SPORTAPP_API_KEY` currently resolves — those three facts determine whether re-derivation is viable today. Do not pick an option yourself; task 3 implements whatever is selected.</action>
  <done>The operator has selected re-derive, grandfather or re-derive-with-fallback, and the selection is recorded in the SUMMARY.</done>
  <decision>
    Does the WC24 2024 tournament data enter the canonical Parquet by re-deriving it from freshly fetched raw sportapp.fi JSON, or by grandfathering the existing mutated CSV as a warn-only input?
  </decision>
  <context>
    `data/wc24_pbp.csv` (14,546 rows, 90+ columns) is NOT raw sportapp.fi JSON — RESEARCH verified it is the fully-mutated output of the old `Python/fetch_sportappfi_api.py` pipeline (it already carries `complete_pass`, `drive_id`, `home_team_score`). The notebooks `pl.concat` it post-mutation with the Hudl data, which is how it reaches the EP training corpus today.

    The new snapshot-first design has `ffep fetch-sportapp` write raw JSON to `data/raw/sportapp/` and `ffep ingest` read only from disk. That leaves this file's role undefined, and CONTEXT.md does not address it. Getting it wrong means either losing 14,546 plays from the EP training corpus or counting them twice.
  </context>
  <options>
    <option id="re-derive">
      <name>Re-derive from raw JSON (recommended)</name>
      <pros>WC24 flows through the same validated canonical path as everything else; its plays get real per-game validation and quarantine; `data/wc24_pbp.csv` becomes a comparison fixture that proves the port did not change the numbers; no bespoke reader to maintain</pros>
      <cons>needs a working, rotated `SPORTAPP_API_KEY` and a successful fetch of ~60 WC24 games; if the API has changed or retired those games, the data is temporarily unavailable</cons>
    </option>
    <option id="grandfather">
      <name>Grandfather the mutated CSV as warn-only</name>
      <pros>no API dependency, no risk of losing the corpus, immediately available</pros>
      <cons>adds a second grandfathered reader for an already-mutated 90-column shape; those plays never get real validation; the file's provenance stays opaque; the sportapp JSON path then has no real data to prove it works</cons>
    </option>
    <option id="re-derive-with-fallback">
      <name>Re-derive, fall back to grandfathering if the fetch fails</name>
      <pros>gets the benefits of re-derivation when the API cooperates, with a guaranteed corpus either way; the decision degrades gracefully instead of blocking the phase</pros>
      <cons>both code paths may need to exist, which is slightly more surface than picking one</cons>
    </option>
  </options>
  <resume-signal>Select: re-derive, grandfather, or re-derive-with-fallback</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the sportapp.fi snapshot parsing chain</name>
  <files>src/flag_football_ep/ingest/sportapp.py, tests/test_ingest_sportapp.py</files>
  <read_first>
    - Python/fetch_sportappfi_api.py lines 6-97 (process_game: what the two JSON payloads contain, the drive/eventGroup nesting, the season/competition metadata extraction, the per-play dict keys)
    - Python/fetch_sportappfi_api.py lines 154-306 (convert_to_polars, extract_players_from_summary, clean_sort, clean_yardage — the yardline_50 derivation from start/end yard-line team halves)
    - Python/fetch_sportappfi_api.py lines 308-435 (correct_posteam, clean_play_ids with the drive_id/play_id diff-cumsum derivation, add_event_columns with the down/PAT and play_type/complete_pass derivations from action_title and play_result)
    - Python/fetch_sportappfi_api.py lines 437-498 (add_scoring_play_team, add_team_points, drop_cols)
    - .planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md (sportapp.py section with the yardage, event-column and play-id excerpts, plus the anti-unification warning)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Architecture Pattern 3: two independent mutation chains, one canonical output — do NOT route sportapp rows through the Hudl RESULT chain)
    - src/flag_football_ep/canonical.py (conform_to_canonical, add_scoring_play_team, add_score_columns, make_game_id)
  </read_first>
  <behavior>
    - `load_snapshot` reads the two JSON files and raises a clear error naming the missing path when either is absent
    - `flatten_plays` turns the nested drives/eventGroups payload into one row per play with `half` from the drive's `num` + 1, `drive_id` from the event-group index + 1 and `play_id` from the play index + 1 (the existing process_game shape)
    - `flatten_plays` carries season, competition_id, competition_name, competition_league, gender, home_team, away_team, home_score, away_score, summary, action_title, down, down_desc, yards_to_go and the yard-line fields through to the frame
    - `clean_yardage` derives `yardline_50` and `yardline_50_after` from the start/end yard line plus its team-half id, yielding values within 0..50 for the fixture
    - `clean_play_ids` derives contiguous per-game `play_id` and non-decreasing `drive_id` from the diff-cumsum chain
    - `add_event_columns` sets `down` 0 when `down_desc` contains "PAT", and derives play_type/complete_pass/interception/sack/touchdown/safety from `play_result` and `action_title` — never from a RESULT token
    - `ingest_snapshots` output columns equal `CANONICAL_COLUMNS` and `source` is "sportapp" on every row
    - `game_id` equals `make_game_id("sportapp", source_game_id)` and `source_game_id` preserves the sportapp id
    - a snapshot whose payload lacks the drives array is skipped with a recorded notice and does not abort the remaining games
    - team labels pass through `map_teams` for source "sportapp"; an unmapped label raises `UnmappedTeamError`
  </behavior>
  <action>
Create `src/flag_football_ep/ingest/sportapp.py` implementing the `<interfaces>` block by porting the parsing and mutation functions from `Python/fetch_sportappfi_api.py`. Two structural changes from the original, no semantic ones:

1. No network: `load_snapshot` reads the two JSON files written by `fetch/sportapp.py`. Nothing in this module may import `requests`.
2. No pandas round-trip where polars suffices: the original goes `pd.DataFrame` → `extract_players_from_summary` (regex on `summary`) → `pl.from_pandas`. Keep the regex extraction logic verbatim; using pandas only inside that one helper is acceptable if the regex behaviour depends on it, but the module's public functions take and return polars frames.

Keep the sportapp derivations strictly separate from the Hudl chain (RESEARCH Pattern 3). Add a module docstring stating that convergence happens at `conform_to_canonical`, not at shared mutation code, and that any attempt to unify with `ingest/hudl.py` is a documented anti-pattern.

Reuse `canonical.add_scoring_play_team(credit_defense=True)` and `canonical.add_score_columns` for the points/cumsum tail (PATTERNS confirms that tail is structurally identical across sources and operates on already-canonical boolean columns), replacing the original `add_team_points`.

Map remaining source fields onto canonical extras: `summary`→description, `passer`→qb, `receiver`→received_by, `rusher`→target when present, plus gender.

Write `tests/test_ingest_sportapp.py` using a hand-built JSON fixture pair (two drives, ~6 plays, one touchdown, one PAT) committed under `tests/fixtures/sportapp/`. Cover every `<behavior>` bullet, and name the canonical-merge test `test_canonical_merge_schema` (from the RESEARCH test map).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingest_sportapp.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ingest_sportapp.py -q` exits 0 with at least 12 tests
    - `grep -c "import requests" src/flag_football_ep/ingest/sportapp.py` returns 0
    - `grep -c "make_hudl_mutations\|parse_result_tokens\|RESULT" src/flag_football_ep/ingest/sportapp.py` returns 0 (chains stay independent)
    - `uv run pytest tests/test_ingest_sportapp.py::test_canonical_merge_schema -q` exits 0
    - a test proves `down` becomes 0 for a PAT play
    - a test proves `yardline_50` stays within 0..50 on the fixture
    - a test proves a payload missing its drives array is skipped with a notice, not an exception
    - a test proves `source` is "sportapp" and `game_id` matches `sportapp-<id>`
  </acceptance_criteria>
  <done>Fetched sportapp.fi snapshots parse into the canonical schema offline, with the two mutation chains kept independent.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the WC24 disposition chosen in task 1</name>
  <files>data/reference/sportapp_games.csv, data/raw/legacy/wc24_pbp.csv, src/flag_football_ep/ingest/sportapp.py, tests/test_ingest_sportapp.py</files>
  <read_first>
    - the task 1 checkpoint outcome (which option the operator selected)
    - data/wc24_pbp.csv (header and the distinct `game_id` values — the source of the game-id list to seed)
    - src/flag_football_ep/fetch/sportapp.py (fetch_games signature and the snapshot filenames it writes)
    - data/reference/sportapp_games.csv (header created in plan 05: source_game_id, competition, season, note)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Runtime State Inventory row on wc24_pbp.csv; Open Question 2)
  </read_first>
  <action>
Implement exactly the option selected in task 1.

Common to all options — seed the game list:
- Extract the distinct `game_id` values from `data/wc24_pbp.csv` and write them into `data/reference/sportapp_games.csv` with `competition` = the `competition_name` value found in that file, `season` = its `season` value, and `note` = "WC24, seeded from data/wc24_pbp.csv on <date>". This is the list `ffep fetch-sportapp` reads when no `--game-ids` is passed, and it is needed under every option because it is the only record of which sportapp games belong to the corpus.

If option `re-derive` or `re-derive-with-fallback`:
- Run `uv run ffep fetch-sportapp` to snapshot those games into `data/raw/sportapp/`.
- Run `ingest_snapshots` over the snapshot directory and compare the result against `data/wc24_pbp.csv` on the columns both carry (`game_id`, `play_id`, `down`, `yards_to_go`, `yardline_50`, `complete_pass`, `touchdown`, `home_team_score`, `away_team_score`): report row counts and per-column mismatch counts in the SUMMARY. This comparison is the evidence that the port preserved behaviour.
- `git mv data/wc24_pbp.csv data/raw/legacy/wc24_pbp.csv` and add a `note` row in `sportapp_games.csv` recording that the file is retained as a comparison fixture only and is NOT an ingest input. Add a test asserting that no ingest module reads `wc24_pbp.csv`.
- Under `re-derive-with-fallback`, if the fetch fails (missing/rotated key, non-200, retired games), stop and implement the grandfather branch instead, recording why.

If option `grandfather` (or the fallback triggered):
- `git mv data/wc24_pbp.csv data/raw/legacy/wc24_pbp.csv`.
- Add `read_mutated_sportapp_snapshot(path) -> pl.DataFrame` to `ingest/sportapp.py`: read the CSV with the notebook's `schema_overrides` (posteam, posteam_after, defteam, defteam_after, home_team, away_team, home_team_after, away_team_after and scoring_play_team as Utf8; penalty as Int32), cast Int64 to Int32, rename its columns onto the canonical names, set `source` to "legacy-sportapp", and `conform_to_canonical`. Add "legacy-sportapp" to the pipeline's warn-only source set (document it in the SUMMARY so plan 14 wires it) since these rows cannot be re-validated at source.
- Add tests proving the mutated snapshot conforms to `CANONICAL_COLUMNS`, that its row count equals 14,546, and that its `source` is "legacy-sportapp".

Record the decision, the reasoning and the resulting corpus size in the SUMMARY under a heading "WC24 disposition".
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingest_sportapp.py -q && test -f data/reference/sportapp_games.csv && test ! -f data/wc24_pbp.csv && test -f data/raw/legacy/wc24_pbp.csv && uv run python -c "import polars as pl; g=pl.read_csv('data/reference/sportapp_games.csv'); assert g.height>0, 'sportapp_games.csv not seeded'; print('games seeded:', g.height)"</automated>
  </verify>
  <acceptance_criteria>
    - `data/wc24_pbp.csv` no longer exists at its old path; `data/raw/legacy/wc24_pbp.csv` exists
    - `data/reference/sportapp_games.csv` has one row per distinct WC24 game id with a non-empty `note`
    - `uv run pytest tests/test_ingest_sportapp.py -q` exits 0
    - under re-derive: the SUMMARY reports snapshot count, ingested row count and per-column mismatch counts against the old mutated CSV
    - under grandfather: a test asserts the mutated snapshot reader returns 14,546 rows conformed to `CANONICAL_COLUMNS` with `source` "legacy-sportapp"
    - the SUMMARY contains a "WC24 disposition" section naming the selected option and the resulting corpus size
    - no module references the path `data/wc24_pbp.csv` any more (`grep -rn "wc24_pbp.csv" src/ | grep -vc "raw/legacy"` returns 0)
  </acceptance_criteria>
  <done>The WC24 corpus has a recorded disposition, the game list is maintained under `data/reference/`, and no plays are silently dropped or duplicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched JSON snapshots → canonical table | remote-origin payloads, already on disk, parsed into the shared dataset |
| pre-mutated legacy CSV → canonical table | opaque-provenance data that cannot be re-validated at source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-03 | Tampering | malformed or partial sportapp payloads (missing drives array, unexpected nesting) | mitigate | `load_snapshot` and `flatten_plays` fail per game with a recorded notice and the run continues; the canonical frame then goes through the plan-06 checks |
| T-1.2-27 | Tampering | double-counting or losing the 14,546 WC24 plays | mitigate | the blocking decision checkpoint plus the mandatory `git mv` out of `data/` root and the explicit statement that the retained file is a fixture, not an input; a grep gate asserts no module reads the old path |
| T-1.2-15 | Spoofing | sportapp team names mapping ambiguously onto canonical codes | mitigate | `map_teams` for source "sportapp" raises `UnmappedTeamError` listing every unmapped label |
| T-1.2-05 | Information Disclosure | API key reaching this module | mitigate | `ingest/sportapp.py` performs no network I/O and imports no `requests`, asserted by a grep gate; keys exist only in `fetch/` |
</threat_model>

<verification>
- `uv run pytest tests/test_ingest_sportapp.py -q` green
- `data/wc24_pbp.csv` relocated, `sportapp_games.csv` seeded
- WC24 disposition recorded in the SUMMARY with corpus size
</verification>

<success_criteria>
- sportapp.fi data reaches one canonical schema shared with Hudl and IFAF
- The two mutation chains remain independent, as RESEARCH requires
- The WC24 provenance question is answered explicitly, not by accident
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-11-SUMMARY.md` when done
</output>
