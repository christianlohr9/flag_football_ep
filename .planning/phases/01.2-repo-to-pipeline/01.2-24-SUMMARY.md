---
phase: 01.2-repo-to-pipeline
plan: 24
subsystem: testing
tags: [polars, xgboost, mlflow, integration-test, docs, gap-closure]

# Dependency graph
requires:
  - phase: 01.2-repo-to-pipeline
    provides: "score_plays row-order fix (plan 18), game_id-scoped EP/WP fills (plan 19), non-strict per-file/per-game ingest containment (plan 20), first-play scoring fix (plan 21), Source notices/Skipped files report sections (plan 22), one cross-source play_type vocabulary (plan 23)"
provides:
  - "an end-to-end run_all test on a real four-source (hudl+legacy+sportapp+ifaf) corpus containing IFAF's always-null yards_to_go rows, proving CR-01's failure mode is caught at integration level"
  - "docs/pipeline.md documents the Source notices / Skipped files report sections, per-file containment, the unmapped-team loud-abort exception, and the six-value play_type vocabulary"
  - "a full-suite green gate (449 passed, 2 skipped) with all six gap-closure plans (18-23) applied together"
affects: [01.2-verification, phase-1.3-groupkfold-evaluation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "reuse existing tree builders across test modules (test_pipeline_ingest's fixture writers) instead of duplicating fixture CSV/JSON construction for a new integration test"

key-files:
  created: []
  modified:
    - tests/test_cli_run.py
    - docs/pipeline.md

key-decisions:
  - "Built the mixed-corpus fixture by layering test_pipeline_ingest's legacy/sportapp/ifaf fixture writers on top of test_cli_run.py's existing multi-game Hudl training_tree builder, rather than writing a new set of fixtures from scratch -- training_tree's _write_training_tree already calls test_pipeline_ingest._write_reference_csvs, which pre-populates half_boundaries/final_scores/team_mapping entries for all four sources, so no new reference-CSV glue was needed."
  - "Used yards_to_go nullness (not a per-source flag) to identify the IFAF null-feature rows in assertions, since that is the actual mechanism score_plays uses to decide which rows get null EP/WP probabilities -- ties the test to the real behavioural contract rather than to source labels."
  - "Did not add a slow/integration marker: the new test runs in ~2s standalone and the whole test_cli_run.py module in ~5.6s, both well under the plan's 60s threshold for a real (untuned) XGBoost fit."

requirements-completed: [REQ-S1-04, REQ-S1-05, REQ-S1-06]

# Metrics
duration: 20min
completed: 2026-08-17
---

# Phase 01.2 Plan 24: Integration Coverage Gap Closure Summary

**End-to-end `run_all` test on a real mixed hudl+legacy+sportapp+ifaf corpus proving CR-01's row-order corruption is caught, plus operator-facing docs for the Source notices/Skipped files/play_type contract established by gap-closure plans 18-23.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-17T22:30:00+02:00 (approx.)
- **Completed:** 2026-08-17T22:48:33+02:00
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Closed the integration-coverage gap VERIFICATION called out: `test_run_all_mixed_corpus_with_null_feature_ifaf_rows` runs the real `run_all` chain (ingest → train_ep → train_wp → score) on a corpus mixing complete hudl/legacy/sportapp rows with IFAF's always-null `yards_to_go` rows, asserting row count/order preservation, non-null `epa`/`wpa` on complete rows, and null EP probability columns on the null-feature rows — the exact shape that let CR-01 survive a green suite.
- Documented the operator-facing contract in `docs/pipeline.md`: `## Source notices` / `## Skipped files` report sections, per-file containment vs. the one deliberate loud-abort exception (unmapped team codes in `data/reference/team_mapping.csv`), and the six-value cross-source `play_type` vocabulary.
- Ran the full suite as the phase gate with all six gap-closure plans (18-23) applied together: 449 passed, 2 skipped, 28.92s.

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end run_all on a mixed corpus with null-feature rows** - `6ed733d` (test)
2. **Task 2: Document the notice/containment contract and gate the full suite** - `3582ae4` (docs)

## Files Created/Modified
- `tests/test_cli_run.py` - added `mixed_corpus_tree` fixture (training_tree's Hudl games + test_pipeline_ingest's legacy/sportapp/ifaf fixtures) and `test_run_all_mixed_corpus_with_null_feature_ifaf_rows`, which runs the real ingest→train→score chain and asserts row count/order preservation, non-null `epa`/`wpa` on complete rows, and null EP probability columns on IFAF's null-`yards_to_go` rows
- `docs/pipeline.md` - extended section 4 (Validation and quarantine semantics) with the Source notices/Skipped files report contract, per-file containment behaviour, the unmapped-team loud-abort exception naming `team_mapping.csv`, and the `play_type` vocabulary

## Decisions Made
- Reused `test_pipeline_ingest`'s fixture writers (`_write_legacy_game`, `_write_sportapp_fixture`, `_write_ifaf_fixture`, `_write_reference_csvs` via `_write_training_tree`) instead of building a new four-source tree from scratch — `training_tree`'s existing reference-CSV setup already covers team mapping and final scores for all four sources.
- Identified null-feature rows in test assertions by `yards_to_go.is_null()` (the real scoring-null trigger) rather than by `source == "ifaf"`, so the assertion exercises the actual behavioural contract.
- No slow/integration marker added — measured runtime (test: ~2s, module: ~5.6s) is well under the plan's 60s guidance for a real untuned XGBoost fit.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. All acceptance criteria were met on the first implementation without needing to enlarge the fixture corpus — `training_tree`'s existing three-game Hudl shape (already tuned for real EP/WP convergence by `test_cli_run.py`) was sufficient once layered with the legacy/sportapp/ifaf fixtures.

## Gap-to-plan mapping (VERIFICATION gaps 1 and 2, plans 18-23)

| Plan | Gap closed | What it did |
|------|-----------|-------------|
| 01.2-18 | Gap 1 (CR-01): order-dependent EP/WP corruption | Sorted `_score_probabilities`' output back to `_score_row_id` order before `add_ep_variables`/`add_wp_variables` see it; added regression tests pinning the fix |
| 01.2-19 | Gap 1 (cross-game leakage variant) | Scoped every `backward_fill()`/`shift(-1)` in `add_ep_variables`/`add_wp_variables` to `.over("game_id")`, so a multi-game frame's fills never leak across game boundaries |
| 01.2-20 | Gap 2 (CR-02): silent whole-source data loss | Non-strict (`strict=False`) identity/yardage casts in `hudl.derive_identity_columns` and `sportapp.clean_yardage`; complete per-file containment in `hudl.ingest_dir` (all four documented exception types) and per-game containment in `sportapp.ingest_snapshots`; null-safe `gapless_play_ids` check |
| 01.2-21 | WR-02: first-play scoring | `add_score_columns` credits a score on a game's first play instead of silently zeroing it (Kleene-safe branch ordering) |
| 01.2-22 | Gap 2 (WR-01): notices never surfaced | `render_report` gained always-rendered `## Source notices`/`## Skipped files` sections; `run_ingest` passes `source_notices` through and echoes them to console; `cli.ingest`/`cli.run` echo `result.notices` |
| 01.2-23 | WR-11: cross-source `play_type` divergence | `canonical.PLAY_TYPE_VOCABULARY` as the single value set every ingest module converges on; IFAF defensive two-point conversions routed to `defteam`; sportapp's raw "rush" normalized to "run"; contract test guarding the emitted value set |

## Full-suite gate result

`uv run pytest -q` (all six gap-closure plans applied together): **449 passed, 2 skipped in 28.92s**, exit code 0.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 01.2's two VERIFICATION-blocking gaps (CR-01 scoring corruption, CR-02 silent source loss) are now closed and covered at integration level, not just unit level.
- The full suite is green with all 24 plans applied; the phase is ready for re-verification.
- REQ-S1-04, REQ-S1-05 and REQ-S1-06 (notebook migration, ingest CLI + validation report, cross-source schema merge) are now backed by an end-to-end test on the exact mixed-corpus shape that previously escaped detection.

---
*Phase: 01.2-repo-to-pipeline*
*Completed: 2026-08-17*

## Self-Check: PASSED

- FOUND: tests/test_cli_run.py
- FOUND: docs/pipeline.md
- FOUND: .planning/phases/01.2-repo-to-pipeline/01.2-24-SUMMARY.md
- FOUND: commit 6ed733d (test(01.2-24): mixed-corpus run_all coverage for IFAF null-feature rows)
- FOUND: commit 3582ae4 (docs(01.2-24): document source notices, containment and play_type contract)
- FOUND: commit 397d631 (docs(01.2-24): add plan summary)
