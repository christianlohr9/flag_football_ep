---
phase: 01.2-repo-to-pipeline
plan: 17
type: execute
wave: 7
depends_on: ["01.2-16"]
files_modified:
  - README.md
  - docs/pipeline.md
  - scripts/capture_notebook_baseline.py
  - notebooks/pipeline_demo.ipynb
  - notebooks/ep_model_demo.ipynb
  - notebooks/wp_model_demo.ipynb
  - notebooks/explainable_ai_ep.ipynb
  - notebooks/explainable_ai_wp.ipynb
  - tests/test_repo_hygiene.py
  - data/raw/legacy/pbp_wc24_static.xlsx
  - data/reference/roster.csv
autonomous: true
requirements: [REQ-S1-04, REQ-S1-06]

must_haves:
  truths:
    - "No CSV sprawl remains at the repository root; every live input sits under data/raw or data/reference"
    - "Python/ is gone and nothing imports it"
    - "Notebooks are thin demos that import the package instead of carrying pipeline logic"
    - "README and docs/pipeline.md describe how to run the pipeline and maintain the reference files"
  artifacts:
    - path: "docs/pipeline.md"
      provides: "CLI reference, data layout, reference-CSV maintenance workflow, validation and quarantine semantics"
      min_lines: 120
    - path: "README.md"
      provides: "install, quickstart, repository map"
      min_lines: 60
    - path: "tests/test_repo_hygiene.py"
      provides: "automated gates against sprawl, Python/ imports and notebook logic creeping back"
  key_links:
    - from: "notebooks/*.ipynb"
      to: "flag_football_ep"
      via: "package imports replacing sys.path hacks"
      pattern: "from flag_football_ep"
    - from: "docs/pipeline.md"
      to: "data/reference/*.csv"
      via: "documented maintenance workflow"
      pattern: "final_scores.csv"
---

<objective>
Finish the migration: remove the repo-root sprawl and the `Python/` helper directory, rewrite the notebooks as thin demos, and document the pipeline.

Purpose: REQ-S1-06's second half ("`data_raw.csv`/`games_plays.csv` sprawl consolidated into `data/`") and REQ-S1-04's endpoint ("pipeline runs from the package, not notebooks"). Without this, both old and new paths exist side by side and the next phase inherits the ambiguity.
Output: a clean root, `notebooks/` demos, `docs/pipeline.md`, a rewritten `README.md`, and hygiene tests that keep it that way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@README.md
@ffep.toml
@src/flag_football_ep/cli.py

<interfaces>
<!-- Root inventory and disposition. Live inputs move, derivable intermediates are deleted. -->

move to data/raw/legacy/ : pbp_wc24_static.xlsx
move to data/reference/  : team_roster.csv -> roster.csv
delete (derivable)       : games_plays.csv, games_plays_OLD.csv, model_eval_situations.csv,
                           data/wc24_pbp_short.csv, insights/df2.csv,
                           insights/model_eval_situations*.csv
delete (superseded)      : team_roster_OLD.csv, h, models.py, Python/, __pycache__/
delete (logic-bearing)   : ep_model.ipynb, api_call.ipynb, api_fuzzing.ipynb, test_sportappfi.ipynb,
                           models/ep_model.ipynb, models/wp_model.ipynb
move + rewrite imports   : insights/explainable_ai_ep.ipynb, insights/explainable_ai_wp.ipynb
                           -> notebooks/
create (thin demos)      : notebooks/pipeline_demo.ipynb, notebooks/ep_model_demo.ipynb,
                           notebooks/wp_model_demo.ipynb
keep                     : models/*.pkl (existing trained artifacts; MLflow owns new ones)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root inventory cleanup and reference relocation</name>
  <files>data/raw/legacy/pbp_wc24_static.xlsx, data/reference/roster.csv</files>
  <read_first>
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (cleanup decision: live inputs move to their data/raw or data/reference home, derivable intermediates are regenerated and deleted, `*_OLD` files and stray artifacts are deleted since git history preserves them)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Runtime State Inventory: no cron, launchd or external service references any of these paths, so relocation is safe)
    - .gitignore (which of the destination directories are ignored — the relocated files must remain tracked)
    - this plan's `<interfaces>` block (the file-by-file disposition)
  </read_first>
  <action>
Execute the `<interfaces>` inventory exactly, using `git mv` for moves and `git rm` for deletions so history stays traceable.

Before deleting each derivable file, confirm the claim rather than assuming it:
- `games_plays.csv` / `games_plays_OLD.csv`: confirm no module or notebook references them (`grep -rn "games_plays" src/ notebooks/ docs/`). If anything does, stop and record it in the SUMMARY instead of deleting.
- `model_eval_situations*.csv` and `insights/df2.csv`: confirm they are outputs by checking they are written, not read, in the deleted notebooks. Record their nature in the SUMMARY, since phase 1.3 will regenerate evaluation tables.
- `data/wc24_pbp_short.csv`: confirm it is a row subset of the relocated `wc24_pbp.csv` before deleting.

Relocations:
- `pbp_wc24_static.xlsx` → `data/raw/legacy/` (a raw static export, keep it as an input, not an intermediate).
- `team_roster.csv` → `data/reference/roster.csv` (hand-maintained reference data; note in the SUMMARY that cross-source player mapping is deferred to phase 1.4 per CONTEXT).

Verify afterwards that `data/reference/roster.csv` and `data/raw/legacy/pbp_wc24_static.xlsx` are tracked by git (not swallowed by an ignore rule) and that the repository root contains no `.csv` or `.xlsx` files at all.
  </action>
  <verify>
    <automated>test -f data/reference/roster.csv && test -f data/raw/legacy/pbp_wc24_static.xlsx && test -z "$(ls *.csv *.xlsx 2>/dev/null)" && test ! -e h && test ! -e models.py && test ! -e team_roster_OLD.csv && test ! -e games_plays.csv && git check-ignore -q data/reference/roster.csv && echo "reference file is ignored (BAD)" && exit 1; echo "root clean"</automated>
  </verify>
  <acceptance_criteria>
    - the repository root contains no `.csv` and no `.xlsx` files
    - `data/reference/roster.csv` and `data/raw/legacy/pbp_wc24_static.xlsx` exist and are tracked by git
    - `h`, `models.py`, `team_roster_OLD.csv`, `games_plays.csv`, `games_plays_OLD.csv`, `model_eval_situations.csv`, `data/wc24_pbp_short.csv` no longer exist
    - `grep -rn "games_plays\|team_roster.csv\|wc24_pbp_short" src/ docs/ notebooks/ 2>/dev/null | wc -l` returns 0
    - the SUMMARY records, per deleted file, the evidence that it was derivable or superseded
  </acceptance_criteria>
  <done>Every live input lives under `data/`, every derivable intermediate is gone, and the root is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Python/ and rewrite the notebooks as thin demos</name>
  <files>notebooks/pipeline_demo.ipynb, notebooks/ep_model_demo.ipynb, notebooks/wp_model_demo.ipynb, notebooks/explainable_ai_ep.ipynb, notebooks/explainable_ai_wp.ipynb, scripts/capture_notebook_baseline.py, tests/test_repo_hygiene.py</files>
  <read_first>
    - insights/explainable_ai_ep.ipynb and insights/explainable_ai_wp.ipynb (their import cells and which helper functions they use — only the imports are rewritten, not the analysis)
    - scripts/capture_notebook_baseline.py (it imports from `Python/`, so deleting that directory makes it unrunnable — it needs the guard described below)
    - src/flag_football_ep/cli.py (the commands the demo notebooks call)
    - src/flag_football_ep/features/mutations.py and model/score.py (the functions the demos import)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (notebooks decision: rewritten as thin demos that only import the package and visualize; the heavy logic-bearing notebooks are replaced, not archived)
  </read_first>
  <action>
1. Delete `Python/` (including `__pycache__`) and the root `__pycache__` with `git rm -r`. Delete the logic-bearing notebooks listed in `<interfaces>`.

2. Guard the baseline script: `scripts/capture_notebook_baseline.py` can no longer run because it imports the deleted helpers. Add a check at the top that exits with a clear message when `Python/` is absent, naming the commit before this phase and the `git worktree add` command needed to regenerate the fixtures from the pre-migration tree. Keep the script — the frozen fixtures under `tests/fixtures/` stay the regression baseline, and their provenance must remain reproducible.

3. Create three thin demo notebooks under `notebooks/`, each at most ~10 cells, importing only from `flag_football_ep` and plotting:
   - `pipeline_demo.ipynb`: read `data/processed/plays.parquet` and `games.parquet`, show row counts by source/competition/season, and display the latest validation report's quarantine section.
   - `ep_model_demo.ipynb`: read `plays_scored.parquet`, plot EP by `yardline_50` per down, and show EPA leaders by `posteam`.
   - `wp_model_demo.ipynb`: plot the WP curve for one game, with a visible annotation that `half_seconds_remaining` is synthetic until REQ-S1-02 delivers real clock data.
   No mutation logic, no model fitting, no file writing in any demo notebook.

4. Move `insights/explainable_ai_ep.ipynb` and `insights/explainable_ai_wp.ipynb` into `notebooks/` and rewrite only their import cells: replace `sys.path.insert(0, '../Python')` plus `from helper_add_* import ...` with `from flag_football_ep.features.mutations import ...` and `from flag_football_ep.model.score import ...`. Leave the analysis cells untouched. Remove the now-empty `insights/` directory.

5. Write `tests/test_repo_hygiene.py` with these gates: every `.ipynb` under `notebooks/` parses as JSON; no notebook code cell contains `sys.path.insert` or `from helper_`; no file under `src/` contains `sys.path`; `Python/` does not exist; the repository root contains no `.csv`/`.xlsx`; every demo notebook has at most 15 code cells.
  </action>
  <verify>
    <automated>uv run pytest tests/test_repo_hygiene.py -q && test ! -d Python && test ! -d insights && uv run python -c "import json,glob; nbs=sorted(glob.glob('notebooks/*.ipynb')); assert len(nbs)>=5, nbs; [json.load(open(n)) for n in nbs]; print('notebooks OK', len(nbs))"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_repo_hygiene.py -q` exits 0 with at least 6 tests
    - `Python/` and `insights/` no longer exist
    - `notebooks/` contains at least five parseable notebooks including the three demos
    - `grep -rn "sys.path" src/ notebooks/ | wc -l` returns 0
    - `grep -rn "from helper_" . --include=*.ipynb | wc -l` returns 0
    - `scripts/capture_notebook_baseline.py` exits with the documented regeneration instructions when `Python/` is absent
    - `uv run pytest tests/ -q` still exits 0 (deleting `Python/` broke nothing)
  </acceptance_criteria>
  <done>The old code path is gone, notebooks carry no pipeline logic, and hygiene is enforced by tests rather than by discipline.</done>
</task>

<task type="auto">
  <name>Task 3: Pipeline documentation and README</name>
  <files>docs/pipeline.md, README.md</files>
  <read_first>
    - src/flag_football_ep/cli.py (every command and option, so the CLI reference is accurate rather than aspirational)
    - ffep.toml (every configuration key to document)
    - src/flag_football_ep/validation/checks.py (the six checks and the quarantine/warn-only semantics to describe)
    - src/flag_football_ep/reference.py (the four reference files and their schemas)
    - README.md (current content, 125 bytes — being replaced)
    - .planning/phases/01.2-repo-to-pipeline/01.2-14-SUMMARY.md and 01.2-16-SUMMARY.md (the real run's counts and timings to cite)
    - docs/data-contract.md (the contract document `docs/pipeline.md` links to rather than duplicates)
  </read_first>
  <action>
Write `docs/pipeline.md` covering:
1. Quickstart: install with uv, set `.env` from `.env.example`, `ffep fetch-sportapp`, `ffep fetch-ifaf`, `ffep ingest`, `ffep train`, `ffep score`, `ffep run` — each with its real, working invocation.
2. Data layout: what lives in `data/raw/{hudl,sportapp,ifaf,legacy}`, `data/processed`, `data/reference`, and which paths are git-ignored and why (PII, regenerable output, secrets).
3. Reference-file maintenance: the schema and the workflow for `half_boundaries.csv`, `final_scores.csv`, `team_mapping.csv`, `sportapp_games.csv`, `roster.csv` — including what happens when a row is missing (quarantine for a missing half boundary, a skipped score check for a missing final score, a hard failure for an unmapped team).
4. Validation and quarantine semantics: the six checks, what quarantine means, why the legacy source is warn-only, and where the report lands.
5. Adding a new export: the contract filename pattern, the core columns, and the first-run checklist.
6. Configuration reference: every `ffep.toml` key with its meaning and default.
7. Model tracking: where `mlruns/` lives, how to browse it with `mlflow ui`, how `ffep score` resolves a run, and a pointer to phase 1.3 for the versioning and evaluation work.
8. Known limitations, stated plainly: `half_seconds_remaining` is synthetic until REQ-S1-02 delivers real clock data; the data contract is provisional pending analyst ratification; the PAT baselines are hard-coded until REQ-S1-10.

Rewrite `README.md` as a short entry point: what the project is, install, the five commands, a repository map, and links to `docs/pipeline.md`, `docs/data-contract.md` and `docs/ifaf-field-mapping.md`. Do not duplicate `docs/pipeline.md` content in the README.

Every command shown in either document must be one you actually ran successfully in this phase.
  </action>
  <verify>
    <automated>test -f docs/pipeline.md && test -f README.md && uv run python -c "
import re,pathlib
doc=pathlib.Path('docs/pipeline.md').read_text()
for s in ['ffep ingest','ffep train','ffep score','ffep run','half_boundaries.csv','final_scores.csv','team_mapping.csv','mlruns','quarantine','synthetic']:
    assert s in doc, s
rd=pathlib.Path('README.md').read_text()
for s in ['docs/pipeline.md','docs/data-contract.md','ffep']:
    assert s in rd, s
print('docs OK', len(doc.splitlines()), len(rd.splitlines()))"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/pipeline.md` is at least 120 lines and covers all eight documented sections
    - `docs/pipeline.md` mentions every CLI command: ingest, fetch-sportapp, fetch-ifaf, train, score, run
    - `docs/pipeline.md` documents all four reference files plus the consequence of a missing row in each
    - `docs/pipeline.md` states the synthetic-clock, provisional-contract and hard-coded-PAT-baseline limitations
    - `README.md` links to `docs/pipeline.md`, `docs/data-contract.md` and `docs/ifaf-field-mapping.md`
    - every command shown in either document was executed successfully during this phase (confirmed in the SUMMARY)
  </acceptance_criteria>
  <done>An operator returning after a month can run and maintain the pipeline from the docs alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion operations → irreplaceable data | a wrong deletion could destroy a live input |
| documentation → operator behaviour | wrong docs cause wrong operations, including secret handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-34 | Tampering | deleting a file that turns out to be a live input | mitigate | every deletion is preceded by an evidence check (grep for references, subset verification) recorded in the SUMMARY; all removals go through `git rm` so history preserves the content |
| T-1.2-01 | Information Disclosure | documentation instructing the operator to put a key in a tracked file | mitigate | `docs/pipeline.md` documents `.env` and `.env.example` only, and states that `ffep.toml` never holds secrets |
| T-1.2-02 | Information Disclosure | demo notebooks committing PII-bearing outputs into git | mitigate | demo notebooks read from git-ignored `data/processed`, write nothing, and the hygiene test caps their cell count so they cannot grow into pipelines |
| T-1.2-35 | Repudiation | losing the ability to regenerate the frozen notebook baseline after `Python/` is deleted | mitigate | `scripts/capture_notebook_baseline.py` is retained with a guard that prints the pre-migration commit and the worktree command needed to regenerate |
</threat_model>

<verification>
- `uv run pytest tests/ -q` green after `Python/` is deleted
- Repository root holds no data files; `Python/` and `insights/` are gone
- `docs/pipeline.md` and `README.md` describe only commands that were actually run
</verification>

<success_criteria>
- REQ-S1-06's consolidation half is complete: no root CSV sprawl
- REQ-S1-04's endpoint is complete: nothing imports `Python/`, notebooks are demos
- The pipeline is documented well enough to be operated from the docs alone
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-17-SUMMARY.md` when done
</output>
