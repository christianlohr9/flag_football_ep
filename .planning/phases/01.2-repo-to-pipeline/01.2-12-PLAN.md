---
phase: 01.2-repo-to-pipeline
plan: 12
type: execute
wave: 4
depends_on: ["01.2-04", "01.2-05", "01.2-07"]
files_modified:
  - docs/ifaf-field-mapping.md
  - src/flag_football_ep/ingest/ifaf.py
  - tests/fixtures/ifaf/unified-plays_sample.json
  - tests/test_ingest_ifaf.py
autonomous: true
requirements: [REQ-S1-06]

must_haves:
  truths:
    - "The cpx.studio unified-plays payload has a written, evidence-based field mapping to the canonical schema"
    - "IFAF WM-2026 snapshots parse into the same canonical schema as Hudl and sportapp.fi"
    - "The real IFAF game clock is preserved instead of being overwritten by the synthetic clock"
  artifacts:
    - path: "docs/ifaf-field-mapping.md"
      provides: "cpx.studio field to canonical column mapping with per-field evidence"
      min_lines: 60
    - path: "src/flag_football_ep/ingest/ifaf.py"
      provides: "load_snapshot, flatten_unified_plays, derive_outcome_columns, ingest_snapshots"
      min_lines: 200
      exports: ["load_snapshot", "ingest_snapshots"]
    - path: "tests/fixtures/ifaf/unified-plays_sample.json"
      provides: "committed payload fixture so tests run without network access"
  key_links:
    - from: "src/flag_football_ep/ingest/ifaf.py"
      to: "docs/ifaf-field-mapping.md"
      via: "the parser implements the documented mapping table"
      pattern: "game_clock_ms"
    - from: "src/flag_football_ep/ingest/ifaf.py"
      to: "flag_football_ep.canonical.conform_to_canonical"
      via: "canonical convergence"
      pattern: "conform_to_canonical"
---

<objective>
Add the IFAF/cpx.studio WM-2026 API as the third ingest source: document the payload mapping from real evidence, then implement the parser against it.

Purpose: the folded todo makes IFAF a first-class source in this phase, and CONTEXT designs the canonical schema against all three sources at once. This is also the only source carrying a real game clock (`gameClockMs`), which matters for phase 1.4 where the synthetic clock must be flagged.
Output: `docs/ifaf-field-mapping.md`, `ingest/ifaf.py`, a committed payload fixture, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@.planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md
@.planning/todos/pending/2026-08-17-tap-ifaf-wm-api-as-data-source.md
@src/flag_football_ep/canonical.py
@src/flag_football_ep/fetch/ifaf.py

<interfaces>
<!-- IFAF ingest API consumed by pipeline.run_ingest (plan 14). -->

flag_football_ep.ingest.ifaf:
  load_snapshot(plays_path: Path, tournament_path: Path | None = None) -> tuple[list | dict, dict | None]
  flatten_unified_plays(payload, game_meta: dict, game_id: str) -> pl.DataFrame
  derive_outcome_columns(df: pl.DataFrame) -> pl.DataFrame
  ingest_snapshots(raw_dir: Path, team_mapping: pl.DataFrame,
                   game_ids: Sequence[str] | None = None) -> list[tuple[str, pl.DataFrame, IngestNotices]]

Documented payload structure (CONTEXT.md, verified structurally in the folded todo):
  each play: context with gameClockMs, half, down, ballOn, possession, score; plus sequence, outcome, description
  no defense-scheme fields exist in this source

Snapshot files written by plan 07:
  data/raw/ifaf/unified-plays_{game_id}.json
  data/raw/ifaf/events_{game_id}.json
  data/raw/ifaf/tournament_{slug}.json, tournament_{slug}_teams.json, games.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evidence-based field mapping document and test fixture</name>
  <files>docs/ifaf-field-mapping.md, tests/fixtures/ifaf/unified-plays_sample.json</files>
  <read_first>
    - data/raw/ifaf/unified-plays_*.json (the live snapshots from plan 07 — primary evidence; if plan 07's live run failed this directory is empty and the CONTEXT-documented structure is the fallback)
    - data/raw/ifaf/games.json and data/raw/ifaf/tournament_ffwc26-women.json (competition name, season, team codes)
    - .planning/phases/01.2-repo-to-pipeline/01.2-07-SUMMARY.md (the live-run findings: auth, rate limits, actual context key names)
    - .planning/todos/pending/2026-08-17-tap-ifaf-wm-api-as-data-source.md (the verified play-object structure, and the note that no defense-scheme fields exist)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Open Question 4 and the LOW-confidence rating on this API — every mapping row must cite its evidence)
    - src/flag_football_ep/canonical.py (CORE_COLUMNS and NULLABLE_EXTRAS — the target column names)
  </read_first>
  <action>
Inspect the actual snapshots, then write `docs/ifaf-field-mapping.md` containing:

1. A provenance header: endpoint, snapshot file, fetch date, number of games and plays observed, and whether auth was required (from plan 07's live run).
2. A mapping table with the columns: cpx.studio path, canonical column, transform, evidence. Evidence is exactly one of `observed` (seen in the snapshot, include an example value), `documented` (from CONTEXT or the todo but not seen), or `absent` (no IFAF source; the canonical column stays null). Cover at minimum down, distance, ballOn, possession, score, half, gameClockMs, sequence, outcome, description, plus the identity columns game_id, competition, season, gender, home_team, away_team.
3. A section listing every canonical CORE column with no IFAF source and how it is filled — derived, defaulted, or null — including the validation consequence of each null.
4. An outcome-vocabulary section: the distinct `outcome` values actually observed and the canonical flag each maps to. This is the IFAF counterpart to the Hudl RESULT vocabulary and must be data-derived. List what you saw; mark expected-but-unseen values explicitly as such.
5. A ballOn-semantics section: state whether observed values are absolute field positions or team-relative, with evidence, because the `yardline_50` transform depends on it. If the snapshots do not settle it, say so and name the conservative transform the parser uses.

Then commit a trimmed, redacted payload fixture at `tests/fixtures/ifaf/unified-plays_sample.json`: at most 10 plays, structure preserved exactly, any player names replaced with placeholders. If no live snapshot exists, hand-write the fixture from the documented structure and mark every mapping row `documented` instead of `observed`.
  </action>
  <verify>
    <automated>test -f docs/ifaf-field-mapping.md && test -f tests/fixtures/ifaf/unified-plays_sample.json && uv run python -c "import json; d=json.load(open('tests/fixtures/ifaf/unified-plays_sample.json')); plays=d if isinstance(d,list) else (d.get('plays') or d.get('data') or d.get('items')); assert plays is not None and len(plays)<=10; assert 'context' in plays[0]; print('fixture OK', len(plays))" && grep -v '^#' docs/ifaf-field-mapping.md | grep -c "gameClockMs"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ifaf-field-mapping.md` exists with a provenance header naming the endpoint and fetch date
    - the mapping table covers down, distance, ballOn, possession, score, half, gameClockMs, sequence, outcome, description and every identity column, each with an evidence value of observed, documented or absent
    - the document contains an outcome-vocabulary section listing the values it maps
    - the document contains a ballOn-semantics section stating the chosen transform and its evidence
    - `tests/fixtures/ifaf/unified-plays_sample.json` parses, holds at most 10 plays, and each play has a `context` object
    - the fixture contains no real player names (redacted placeholders only)
  </acceptance_criteria>
  <done>The undocumented API has a written, evidence-tagged mapping, and tests have an offline fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IFAF snapshot parser to canonical frames</name>
  <files>src/flag_football_ep/ingest/ifaf.py, tests/test_ingest_ifaf.py</files>
  <read_first>
    - docs/ifaf-field-mapping.md (task 1 output — the mapping this parser implements, including the ballOn transform decision)
    - tests/fixtures/ifaf/unified-plays_sample.json (the fixture the tests run against)
    - Python/fetch_sportappfi_api.py lines 50-97 (the nested-JSON-to-flat-rows flattening shape; PATTERNS names this the closest structural analog)
    - .planning/phases/01.2-repo-to-pipeline/01.2-PATTERNS.md (ifaf.py section: apply the flatten-then-pl.DataFrame shape; no RESULT or action_title precedent applies, cpx outcome/description needs its own mapping)
    - src/flag_football_ep/canonical.py (conform_to_canonical, add_scoring_play_team, add_score_columns, make_game_id)
    - src/flag_football_ep/reference.py (map_teams, UnmappedTeamError)
  </read_first>
  <behavior>
    - `load_snapshot` accepts either a top-level list payload or an object wrapping the play array under `plays`, `data` or `items`, and raises a clear error naming the file when neither shape is present
    - `flatten_unified_plays` produces one row per play with contiguous per-game `play_id` derived from `sequence` order
    - `half` comes from `context.half`; `down` from `context.down`; `yards_to_go` from the payload's distance field per the mapping document
    - `yardline_50` is derived from `context.ballOn` using the transform recorded in `docs/ifaf-field-mapping.md`, and the result lies within 0..50 for the fixture
    - `posteam` comes from `context.possession` mapped through `map_teams` for source "ifaf"; `defteam` is the other team of that game
    - `game_clock_ms` preserves `context.gameClockMs` as Int64 and `half_seconds_remaining` is left null here (the synthetic clock belongs to features, and this source has real time data phase 1.4 can use)
    - `drive_id` is derived by incrementing on a `possession` change within a game, starting at 1
    - `derive_outcome_columns` maps the observed `outcome` vocabulary to touchdown, safety, interception, complete_pass, incomplete_pass, sack, penalty and the PAT flags; an unmapped `outcome` value is recorded as a notice and leaves the flags 0
    - `result_raw` carries the original `outcome` string (plus `description` in the description extra) so nothing is lost
    - `ingest_snapshots` output columns equal `CANONICAL_COLUMNS` with `source` "ifaf" and `game_id` matching `ifaf-<id>`
    - a snapshot with an unparseable payload is skipped with a recorded notice, and the remaining games still ingest
  </behavior>
  <action>
Create `src/flag_football_ep/ingest/ifaf.py` implementing the `<interfaces>` block, exactly following `docs/ifaf-field-mapping.md`.

Rules:
- Flatten with a plain Python loop into a list of per-play dicts, then build one `pl.DataFrame` (the structural pattern from `Python/fetch_sportappfi_api.py` lines 50-97). Do not attempt a lazy or nested-struct approach; the payload shape is only partially verified and an explicit loop keeps the mapping auditable against the document.
- Defensive access throughout: use `.get()` with defaults for every payload key, and count missing keys per field into the notices, so a schema drift on the provider side shows up as a report line rather than a KeyError.
- Do not reuse the Hudl RESULT parser or the sportapp `action_title` parser (RESEARCH Pattern 3). This source gets its own `derive_outcome_columns`, driven by a module-level `OUTCOME_MAP` dict whose keys are the observed vocabulary from the mapping document, so adding a value later is a one-line change.
- Identity columns: `source` "ifaf", `competition` from the tournament payload (fall back to the tournament slug), `season` parsed from the tournament payload or the slug, `gender` from the tournament payload when present, `game_id` via `make_game_id("ifaf", id)`, `source_game_id` preserved.
- Scoring: derive the flags, then use `canonical.add_scoring_play_team(credit_defense=True)` and `canonical.add_score_columns`. If the payload's `context.score` object carries authoritative running scores, additionally compare the reconstructed scores against it and record any mismatch as a notice — that is free validation this source uniquely offers.
- Finish with `conform_to_canonical(df, "ifaf")`.

Write `tests/test_ingest_ifaf.py` covering every `<behavior>` bullet against the committed fixture plus small hand-built variants (unknown outcome value, missing context key, unparseable payload, possession change for drive_id).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingest_ifaf.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ingest_ifaf.py -q` exits 0 with at least 12 tests
    - a test proves `ingest_snapshots` output columns equal `CANONICAL_COLUMNS` with `source` "ifaf"
    - a test proves `game_clock_ms` is populated as Int64 and `half_seconds_remaining` is null for this source
    - a test proves `yardline_50` stays within 0..50 on the fixture
    - a test proves an unmapped `outcome` value is recorded as a notice and leaves the outcome flags 0
    - a test proves a missing `context` key is counted in the notices instead of raising
    - a test proves `drive_id` increments on a possession change and starts at 1
    - `grep -c "parse_result_tokens\|action_title" src/flag_football_ep/ingest/ifaf.py` returns 0
    - `grep -c "OUTCOME_MAP" src/flag_football_ep/ingest/ifaf.py` returns at least 1
  </acceptance_criteria>
  <done>IFAF snapshots become canonical frames with real clock data preserved and provider schema drift surfaced as report notices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cpx.studio JSON snapshots → canonical table | payloads from an undocumented third-party API cross into the shared dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-04 | Tampering | undocumented, unversioned payload shape drifting without notice | mitigate | defensive `.get()` access with per-field missing counts recorded in the notices, an explicit evidence-tagged mapping document, and an `OUTCOME_MAP` that surfaces unmapped vocabulary as report lines instead of silently zeroing flags |
| T-1.2-28 | Tampering | wrong ballOn interpretation silently shifting every yardline | mitigate | the transform decision and its evidence are recorded in `docs/ifaf-field-mapping.md`, and a test asserts the fixture's `yardline_50` values stay within 0..50; the payload's own running score is cross-checked against the reconstruction when available |
| T-1.2-02 | Information Disclosure | player names in the committed test fixture | mitigate | fixture is trimmed to at most 10 plays with player names replaced by placeholders, asserted by the acceptance criteria |
| T-1.2-15 | Spoofing | cpx team UUIDs mapping ambiguously onto canonical codes | mitigate | `map_teams` for source "ifaf" raises `UnmappedTeamError` listing every unmapped identifier |
</threat_model>

<verification>
- `uv run pytest tests/test_ingest_ifaf.py -q` green
- `docs/ifaf-field-mapping.md` exists with per-field evidence tags
- No RESULT or action_title parsing leaks into this module
</verification>

<success_criteria>
- Three sources share one canonical schema (REQ-S1-06)
- The undocumented API's mapping is auditable, not folklore
- Real game-clock data is preserved for phase 1.4
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-12-SUMMARY.md` when done
</output>
