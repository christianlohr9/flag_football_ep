---
phase: 01.2-repo-to-pipeline
plan: 16
type: execute
wave: 6
depends_on: ["01.2-02", "01.2-14", "01.2-15"]
files_modified:
  - src/flag_football_ep/pipeline.py
  - src/flag_football_ep/cli.py
  - tests/test_migration_equivalence.py
  - tests/test_cli_run.py
autonomous: true
requirements: [REQ-S1-04]

must_haves:
  truths:
    - "`ffep run` chains ingest, features, training and scoring with sensible defaults"
    - "The package reproduces the notebook's training frames on data_raw.csv within a documented tolerance"
    - "The whole chain is exercised end to end by an automated test, and its wall-clock duration is recorded"
  artifacts:
    - path: "src/flag_football_ep/pipeline.py"
      provides: "run_all chaining ingest to features to train to score with per-stage timing"
      min_lines: 60
      exports: ["run_all"]
    - path: "tests/test_migration_equivalence.py"
      provides: "package output versus the frozen notebook baseline"
    - path: "tests/test_cli_run.py"
      provides: "end-to-end ffep run coverage on a fixture tree"
  key_links:
    - from: "tests/test_migration_equivalence.py"
      to: "tests/fixtures/baseline_ep_model_data.parquet"
      via: "frame comparison against the frozen notebook output"
      pattern: "baseline_ep_model_data"
    - from: "src/flag_football_ep/cli.py"
      to: "flag_football_ep.pipeline.run_all"
      via: "ffep run delegation"
      pattern: "run_all"
---

<objective>
Close the phase's REQ-S1-04 loop: chain the stages behind `ffep run`, and prove the ported package reproduces the notebook's training frames.

Purpose: `ffep run` is the rehearsal for phase 1.4's ten-minute end-to-end goal, and the migration-equivalence test is the CONTEXT-mandated proof that the port did not silently change the analysis.
Output: `run_all`, the `ffep run` wiring, `tests/test_migration_equivalence.py`, `tests/test_cli_run.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md
@.planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md
@src/flag_football_ep/pipeline.py
@src/flag_football_ep/cli.py
@tests/fixtures/baseline_manifest.json
@scripts/capture_notebook_baseline.py

<interfaces>
<!-- Chaining API declared by cli.py in plan 01. -->

flag_football_ep.pipeline:
  run_all(config: Config, tune: bool = False) -> RunAllResult
      # ingest -> features (inside train) -> train_ep + train_wp -> score -> write plays_scored.parquet
  RunAllResult: dataclass(ingest: IngestResult, ep_run: str, wp_run: str,
                          scored_path: Path, durations: dict[str, float])

Baseline comparison targets (frozen by plan 02 from the unmodified Python/ helpers):
  tests/fixtures/baseline_ep_model_data.parquet   # label, yardline_50, yards_to_go, down0..down4, Total_W_Scaled
  tests/fixtures/baseline_wp_model_data.parquet   # label + the eight WP features
  tests/fixtures/baseline_manifest.json           # n_rows, columns, dtypes, null_counts, label_distribution
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_all chaining with per-stage timing</name>
  <files>src/flag_football_ep/pipeline.py, src/flag_football_ep/cli.py, tests/test_cli_run.py</files>
  <read_first>
    - src/flag_football_ep/pipeline.py (run_ingest and IngestResult — run_all composes them)
    - src/flag_football_ep/model/train.py and model/score.py (train_ep, train_wp, score_plays signatures)
    - src/flag_football_ep/cli.py (the `run` command declared in plan 01: --config, --skip-fetch, --tune)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (`ffep run` chains ingest to features to train to score with sensible defaults, as a rehearsal for the phase-1.4 ten-minute goal)
  </read_first>
  <behavior>
    - `run_all` executes ingest, then EP training, then WP training, then scoring, and returns all three artifact references
    - `durations` records a wall-clock float per stage under the keys ingest, train_ep, train_wp, score
    - `run_all` writes `plays_scored.parquet` into the processed directory
    - `run_all` aborts with a clear error when ingest produced zero accepted plays, instead of training on an empty frame
    - `tune=True` is forwarded to both training calls
    - `--skip-fetch` (the default) performs no network call: `ffep run` on a fixture tree with no network available still completes
    - the CLI prints the per-stage durations and the total
  </behavior>
  <action>
Extend `src/flag_football_ep/pipeline.py` with `RunAllResult` and `run_all` per `<interfaces>`, and finish the `run` command in `cli.py` (printing the artifact paths and the per-stage durations).

Rules:
- Reuse `run_ingest`; do not reimplement source dispatch.
- Read the freshly written `plays.parquet` for the training stage rather than passing the in-memory frame, so `ffep run` exercises exactly the same path a separate `ffep train` would.
- Time each stage with `time.perf_counter` and keep the durations in the result, so phase 1.4 can measure against the ten-minute target without re-instrumenting.
- Honour `--skip-fetch` by simply not calling any fetch function; the snapshot-first design means ingest reads whatever is already on disk. When `--skip-fetch` is false, call `fetch_games`/`fetch_tournament` first, resolving secrets through `config.secret`.

Write `tests/test_cli_run.py` exercising `ffep run` through `typer.testing.CliRunner` against a `tmp_path` fixture tree (reuse the tree builder from `tests/test_pipeline_ingest.py` by importing it or re-creating it locally), covering every `<behavior>` bullet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_run.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli_run.py -q` exits 0 with at least 6 tests
    - a test proves `run_all` returns run ids for both models and a written `plays_scored.parquet`
    - a test proves `durations` carries the four documented stage keys
    - a test proves an empty ingest result aborts with a clear error instead of training
    - a test proves `ffep run` completes with no network access available
    - the CLI output contains the per-stage durations (asserted on captured output)
  </acceptance_criteria>
  <done>One command runs the whole chain, and its timing is measurable for the phase-1.4 goal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration-equivalence test against the frozen notebook baseline</name>
  <files>tests/test_migration_equivalence.py</files>
  <read_first>
    - scripts/capture_notebook_baseline.py (exactly which chain produced the frozen frames, including the wc24 diagonal concat and the game exclusions)
    - tests/fixtures/baseline_manifest.json (n_rows, columns, dtypes, null_counts, label_distribution, total_w_scaled statistics, excluded_games)
    - src/flag_football_ep/ingest/legacy.py (the package path that must reproduce the legacy portion)
    - src/flag_football_ep/features/mutations.py (prepare_ep_data, prepare_wp_data, make_ep_model_mutations, make_wp_model_mutations)
    - src/flag_football_ep/model/hyperparams.py (EP_SELECTED_COLUMNS, WP_SELECTED_COLUMNS — the exact column lists the baseline used)
    - .planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md (tests decision: a migration-equivalence test, package output equals notebook output on data_raw.csv)
    - .planning/phases/01.2-repo-to-pipeline/01.2-11-SUMMARY.md (the WC24 disposition, which determines whether the EP comparison includes the wc24 rows)
  </read_first>
  <behavior>
    - the WP comparison runs the package chain on `data/raw/legacy/data_raw.csv` (ingest_legacy, prepare_wp_data, make_wp_model_mutations with WP_SELECTED_COLUMNS, excluding legacy-35) and asserts row count, column names and column order equal to `baseline_wp_model_data.parquet`
    - the WP comparison asserts the label distribution matches the baseline exactly
    - the WP comparison asserts each numeric column's min, max and mean match the baseline within 1e-9
    - the EP comparison runs the equivalent legacy chain excluding legacy-37 and compares against the legacy subset of `baseline_ep_model_data.parquet`, with the subset boundary derived from the manifest rather than hardcoded
    - the EP comparison asserts `Total_W_Scaled` statistics match within 1e-9 when computed over the same corpus, and documents in the test's assertion message when the corpus differs because of the WC24 disposition
    - both tests skip with a clear reason when `tests/fixtures/baseline_*.parquet` or `data/raw/legacy/data_raw.csv` is absent
    - any mismatch reports which columns differ and by how much, not just that the frames are unequal
  </behavior>
  <action>
Write `tests/test_migration_equivalence.py` implementing the `<behavior>` bullets.

Rules:
- Compare the model-data frames (the notebook's `model_data`), not intermediate frames: those are the frames that actually determine the trained model, and they are what plan 02 froze.
- Do not assert byte equality of the whole frame. Assert, in order: column names and order, row count, dtypes, label distribution, and per-column min/max/mean within 1e-9. Report every difference found rather than failing on the first, so one run gives the full delta.
- The known and expected deltas are: (a) the legacy path's `game_id` is now `legacy-<n>` rather than an integer — irrelevant because game_id is not a model column; (b) `Total_W_Scaled` depends on the corpus, so when the EP corpus changed because of the WC24 disposition, compare on the legacy subset and state that in the assertion message. Any other delta is a port bug and must fail.
- If a delta cannot be explained, do not weaken the tolerance. Record it in the SUMMARY as an open finding for the phase verification step.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_equivalence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_migration_equivalence.py -q` exits 0
    - the WP equivalence test passes with row count, columns, dtypes and per-column statistics matching the frozen baseline within 1e-9
    - the EP equivalence test passes on the comparable corpus and its assertion message states the corpus basis
    - both tests skip (not fail) when the baseline fixtures or the legacy CSV are absent
    - a deliberately perturbed frame produces a failure message naming the differing column and the delta (asserted by a self-check test)
    - the SUMMARY records any unexplained delta together with its magnitude
  </acceptance_criteria>
  <done>The port is proven against the frozen notebook behaviour, with every deviation either explained or recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate and real-run timing record</name>
  <files>tests/test_cli_run.py</files>
  <read_first>
    - .planning/phases/01.2-repo-to-pipeline/01.2-VALIDATION.md (the per-task verification map and the sign-off checklist this task closes)
    - .planning/phases/01.2-repo-to-pipeline/01.2-RESEARCH.md (Validation Architecture: sampling rate and the phase-gate requirement that the full suite is green plus the equivalence test passing on real data)
    - tests/test_cli_run.py (task 1 output — extend it)
  </read_first>
  <action>
Run the full suite and record the phase's end-to-end evidence.

1. `uv run pytest tests/ -v` must be green. Fix any cross-plan interaction failures found here (for example fixtures colliding on the processed directory) in the owning module, not by weakening assertions.
2. Execute a real chain on real data: `uv run ffep run` and record in the SUMMARY the per-stage durations, the accepted play count, the quarantined game count and the two MLflow run ids. This is the phase-1.4 ten-minute baseline measurement.
3. Add one guard test to `tests/test_cli_run.py` asserting the suite's own hygiene: no test writes into the repository's real `data/processed` directory (assert that every test using the pipeline passes an `out_dir` or a tmp config), so running the suite can never clobber the operator's canonical Parquet.
4. Confirm `uv run ffep --help`, `uv run ffep ingest --help`, `uv run ffep train --help`, `uv run ffep score --help` and `uv run ffep run --help` all exit 0.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q && uv run ffep --help >/dev/null && uv run ffep ingest --help >/dev/null && uv run ffep train --help >/dev/null && uv run ffep score --help >/dev/null && uv run ffep run --help >/dev/null && echo "full suite and CLI green"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` exits 0 with zero failures and zero errors
    - all five `--help` invocations exit 0
    - the SUMMARY records the real `ffep run` per-stage durations, play count, quarantined count and both MLflow run ids
    - a guard test proves no test writes into the repository's real `data/processed` directory
    - `git status --short data/processed` shows no unexpected modification after running the suite
  </acceptance_criteria>
  <done>The whole phase is green under one command, and the ten-minute baseline is measured rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → operator's real data directories | tests must not clobber the canonical dataset |
| chained stages → each other | each stage trusts the previous stage's artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-32 | Tampering | the test suite overwriting the operator's real `data/processed/plays.parquet` | mitigate | a guard test asserts every pipeline test uses `tmp_path` or an `out_dir` override, and `git status data/processed` is checked after a full run |
| T-1.2-33 | Repudiation | an undetected behaviour change from the notebook port | mitigate | the migration-equivalence test compares the frames that determine the model against the frozen baseline within 1e-9 and reports every differing column; unexplained deltas are recorded rather than tolerated |
| T-1.2-18 | Repudiation | `run_all` training on an empty corpus after a silent ingest failure | mitigate | `run_all` aborts with a clear error when ingest produced zero accepted plays |
</threat_model>

<verification>
- `uv run pytest tests/ -q` green across every test module in the phase
- `uv run ffep run` executed on real data with recorded timings
- Migration equivalence passing or its deltas explicitly recorded
</verification>

<success_criteria>
- REQ-S1-04 fully observable: the entire pipeline runs from `src/flag_football_ep` via one command
- The port is proven equivalent to the notebook on the frames that matter
- The phase-1.4 ten-minute goal has a measured starting point
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-16-SUMMARY.md` when done
</output>
