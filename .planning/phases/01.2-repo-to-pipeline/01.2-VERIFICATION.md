---
phase: 01.2-repo-to-pipeline
verified: 2026-08-18T05:30:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "Per-file / per-source ingest containment now holds for the IFAF source: a malformed play (null/str playNumber, non-dict entry) degrades to a per-game skip notice instead of aborting the whole source (plan 01.2-25)"
  gaps_remaining: []
  regressions: []
gaps: []
---

# Phase 01.2: Repo to Pipeline Verification Report

**Phase Goal:** A repo-to-pipeline migration for the flag football analytics project: the full pipeline runs from `src/flag_football_ep` with no notebook required, the ingest CLI turns Hudl exports into canonical Parquet with a per-game validation report, and Hudl own games + sportapp.fi/IFAF tournament data share one canonical schema consolidated under `data/`.
**Verified:** 2026-08-18T05:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap-closure plan 01.2-25 executed (previous run: 4/5, one blocking gap — IFAF per-game ingest containment)

## Goal Achievement

This re-verification checks only what changed since the 2026-08-17T16:40:00Z report (preserved in git history): plan 01.2-25's IFAF containment fix. Truths 1–4 were VERIFIED in that report; plan 25's `files_modified` (`ingest/ifaf.py`, `pipeline.py`, two test files, `docs/pipeline.md`) does not intersect the code paths behind Truths 1, 2 or 4, and the full test suite — which includes the migration-equivalence, CLI-run, validation and cross-source vocabulary tests backing those truths — was independently re-run green on the merged tree in this session (exit 0). Truth 3's reliability caveat and Truth 5's FAILED status were both caused by the same IFAF gap, which is now closed with independently reproduced evidence.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The full pipeline runs from `src/flag_football_ep` — no notebook required (SC1) | VERIFIED | Unchanged since previous report; untouched by plan 25 (docs/tests aside); full suite green on merged tree. |
| 2 | Ingest CLI: Hudl exports in → canonical Parquet + per-game validation report with the six checks (SC2) | VERIFIED | Unchanged since previous report; untouched by plan 25; full suite green. |
| 3 | Hudl + sportapp.fi/IFAF share one canonical schema; raw files consolidated under `data/` (SC3) | VERIFIED | Structural verification unchanged. The previous report's reliability caveat ("a source that can vanish wholesale from one malformed row is not a dependable merge") is resolved by the Truth 5 fix below. |
| 4 | Scoring produces correct EPA/WPA/ep values even with null-feature rows | VERIFIED | Unchanged since previous report; `tests/test_cli_run.py::test_run_all_mixed_corpus_with_null_feature_ifaf_rows` part of the green suite on the merged tree. |
| 5 | Per-file / per-source ingest failures are contained and visibly reported — the pipeline never silently drops an entire source without operator-visible notice | VERIFIED | IFAF now mirrors the hudl/sportapp containment pattern. Independent evidence this session (not trusted from SUMMARY): (a) direct repro of the old failure shape — `flatten_unified_plays` on a payload mixing `playNumber` int/null/str returned 4 rows without raising, where the previous code raised `TypeError` from `_play_sort_key`; (b) `grep -c "except (TypeError, AttributeError, ValueError, KeyError" ingest/ifaf.py` = 1 (per-game guard wrapping the chain through `conform_to_canonical`); (c) `grep -c 'source-level failure' pipeline.py` = 1 (the explicit whole-source-drop notice, reachable only for genuine source-level errors); (d) `uv run pytest tests/test_ingest_ifaf.py tests/test_pipeline_ingest.py::test_run_ingest_ifaf_malformed_play_does_not_drop_the_source -q` → 39 passed, exit 0, including the new end-to-end regression test in which a second IFAF snapshot with a `playNumber: null` play does not drop the healthy game. |

**Score:** 5/5 truths verified (was 4/5 before plan 25)

### Required Artifacts

All artifacts from the previous report remain VERIFIED. The two previously deficient rows are now closed:

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/flag_football_ep/ingest/ifaf.py` | per-game containment mirroring hudl/sportapp | VERIFIED | Null/type-safe `_play_sort_key` (group-ranked tuple; unusable `playNumber` sorts last in stable payload order); per-game `try/except (TypeError, AttributeError, ValueError, KeyError, pl.exceptions.PolarsError)` produces `notices.skipped` with reason; `UnmappedTeamError` still propagates loudly (regression test added). |
| `src/flag_football_ep/pipeline.py` | `_ingest_ifaf` catch scoped to genuine source-level failures | VERIFIED | Last-resort `except Exception` retained (same shape as the other dispatchers, still reachable for `_load_games_meta`/directory failures) but its notice now reads `source-level failure, all ifaf games dropped from this run` — a per-game failure arrives as a `skipped` notice instead. |
| `tests/test_ingest_ifaf.py`, `tests/test_pipeline_ingest.py` | regression coverage for the malformed-play case | VERIFIED | New tests present, including `test_run_ingest_ifaf_malformed_play_does_not_drop_the_source` (named guard for the previous report's Truth 5 gap and REVIEW WR-01); all green. |
| `docs/pipeline.md` | containment contract documented for JSON sources | VERIFIED | Containment sentence covering the JSON sources present. |

### Key Link Verification

The two previously failing links are closed; all others unchanged-VERIFIED:

| From | To | Via | Status |
|------|-----|-----|--------|
| `ingest/ifaf.py: ingest_snapshots` | per-game skip | try/except around `flatten_unified_plays`…`conform_to_canonical` | VERIFIED (was NOT_WIRED) |
| `pipeline.py: _ingest_ifaf` | `ingest_ifaf_snapshots()` | per-source dispatch, per-game failures contained upstream | VERIFIED (was HOLLOW) |

### Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| REQ-S1-04 | SATISFIED | Unchanged from previous report. |
| REQ-S1-05 | SATISFIED | Unchanged; the "trustworthy validation report for any ingested source" spirit-level concern is resolved now that IFAF failures surface per game. |
| REQ-S1-06 | SATISFIED | The previous "SATISFIED WITH CAVEAT" is upgraded: the IFAF half of the merge can no longer silently vanish from a run. `REQUIREMENTS.md` marks all three `[x] Complete`; this verification now agrees without discrepancy. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite green on merged tree | `uv run pytest -q` | exit 0 (456 collected: 454 passed, 2 expected skips) | PASS |
| Old failure shape no longer reproduces | `flatten_unified_plays` on payload with `playNumber` int/null/str mix | 4 rows returned, no raise (previously `TypeError`) | PASS |
| Per-game guard present | `grep -c "except (TypeError, AttributeError, ValueError, KeyError" src/flag_football_ep/ingest/ifaf.py` | 1 | PASS |
| Whole-source notice literal exactly once | `grep -c 'source-level failure' src/flag_football_ep/pipeline.py` | 1 | PASS |
| Containment regression tests | `uv run pytest tests/test_ingest_ifaf.py tests/test_pipeline_ingest.py::test_run_ingest_ifaf_malformed_play_does_not_drop_the_source -q` | 39 passed, exit 0 | PASS |

### Human Verification Required

None. All items resolvable via code reading, grep, test-suite runs and a direct Python reproduction.

### Gaps Summary

No gaps remain. The single blocking gap from the previous report — IFAF whole-source loss from one malformed play — is closed by plan 01.2-25 and independently confirmed. The non-blocking Warning/Info findings from `01.2-REVIEW.md` (WR-02 memory exhaustion on a corrupt `play_id` cell, WR-03 Hudl penalty-`play_type` asymmetry, WR-04 IFAF `play_type` always null, WR-05 IFAF `drive_id` 0-start, WR-06 sportapp operator-precedence latent risk, IN-01…IN-09) remain tracked in the review report for follow-up; none individually threatens the three roadmap success criteria.

**Not treated as phase gaps (carried forward, still true):** the unrotated sportapp.fi API key is a documented, user-deferred operator action recorded in STATE.md; reference-data partial seeding and legacy placeholder team codes are documented user decisions.

---

_Verified: 2026-08-18T05:30:00Z_
_Verifier: Claude (orchestrator inline re-verification; scope limited to the plan-25 delta after two machine-sleep-interrupted gsd-verifier runs — all delta evidence independently reproduced in-session)_
