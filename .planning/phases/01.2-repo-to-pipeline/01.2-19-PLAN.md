---
phase: 01.2-repo-to-pipeline
plan: 19
type: execute
wave: 8
depends_on: []
files_modified:
  - src/flag_football_ep/features/mutations.py
  - tests/test_features_mutations.py
autonomous: true
requirements: [REQ-S1-04]
gap_closure: true
closes_gaps: ["WR-04"]

must_haves:
  truths:
    - "On a multi-game frame, one game's ep/wp/tmp_posteam never leaks into another game's rows"
    - "home_ep_after / home_wp_after never read the next game's first play"
    - "The module's documented .over('game_id') no-cross-game-leakage invariant holds for every fill and shift in the file"
  artifacts:
    - path: "src/flag_football_ep/features/mutations.py"
      provides: "game-scoped backward_fill/shift in add_ep_variables and add_wp_variables"
      contains: "backward_fill().over(\"game_id\")"
    - path: "tests/test_features_mutations.py"
      provides: "multi-game leakage tests for add_ep_variables and add_wp_variables"
  key_links:
    - from: "src/flag_football_ep/features/mutations.py: add_ep_variables"
      to: "game_id partition"
      via: "over('game_id') on every backward_fill and shift"
      pattern: "shift\\(-1\\)\\.over\\(\"game_id\"\\)"
---

<objective>
Scope the four unscoped `backward_fill()`/`shift(-1)` operations in `add_ep_variables`/`add_wp_variables` to `game_id`.

Purpose: closes Warning WR-04, the second half of the EPA/WPA correctness problem behind VERIFICATION truth 4. `score_plays` passes a multi-game frame, so an unscoped `backward_fill()` pulls `ep`/`wp`/`tmp_posteam` across game boundaries into a game's trailing null rows, and `shift(-1)` reads the next game's first play. Every other fill in this module is already `.over("game_id")` and the module docstring claims the invariant — these four are the only violations.
Output: four game-scoped operations plus tests that fail on cross-game leakage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-repo-to-pipeline/01.2-VERIFICATION.md
@.planning/phases/01.2-repo-to-pipeline/01.2-REVIEW.md
@src/flag_football_ep/features/mutations.py

<interfaces>
<!-- Exact call sites. There are exactly four unscoped operations in the file; every other -->
<!-- fill/shift/cum_sum is already `.over("game_id")` or `.over(["game_id","half"])`. -->

`add_ep_variables` (src/flag_football_ep/features/mutations.py)
- line 263: `ep=pl.col("ep").backward_fill()`
- line 264: `tmp_posteam=pl.col("tmp_posteam").backward_fill()`
- line 272: `home_ep_after=pl.col("home_ep").shift(-1)`

`add_wp_variables`
- line 419: `wp=pl.col("wp").backward_fill()`
- line 420: `tmp_posteam=pl.col("tmp_posteam").backward_fill()`
- line 442: `home_wp_after=pl.col("home_wp").shift(-1)`

Already-correct references to copy the style from
- line 114-119 (`prepare_ep_data`): `.backward_fill().over("game_id")` / `.forward_fill().over("game_id")`
- line 604 (`make_wp_model_mutations`): `pl.col("Winner").backward_fill().over("game_id")`
- lines 396-397, 477-480: `cum_sum().over("game_id")`

Consumers
- `add_ep_variables`/`add_wp_variables` are imported ONLY by `src/flag_football_ep/model/score.py` and `tests/test_features_mutations.py`. `tests/test_migration_equivalence.py` compares `prepare_*_data` + `make_*_model_mutations` only, so it does not exercise these two functions and its frozen baseline is unaffected by this change.
- `add_ep_variables` nulls `ep`/`epa` where `game_end == 1`; `add_wp_variables` nulls `wpa` where `game_end == 1`. Those branches partly mask the shift at the exact boundary row but do nothing for the backward fills.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope the four fills and shifts to game_id</name>
  <files>src/flag_football_ep/features/mutations.py</files>
  <read_first>
    - src/flag_football_ep/features/mutations.py (module docstring plus lines 100-130, 234-300, 403-460 — the invariant claim, the already-correct examples and the six lines to change)
    - .planning/phases/01.2-repo-to-pipeline/01.2-REVIEW.md section "WR-04"
  </read_first>
  <action>
    In `add_ep_variables`, change the `ep` and `tmp_posteam` `backward_fill()` calls to `backward_fill().over("game_id")`, and the `home_ep_after` derivation to `pl.col("home_ep").shift(-1).over("game_id")`.

    In `add_wp_variables`, change the `wp` and `tmp_posteam` `backward_fill()` calls to `backward_fill().over("game_id")`, and the `home_wp_after` derivation to `pl.col("home_wp").shift(-1).over("game_id")`.

    Add a comment above each pair, matching the existing style at lines 111-112, stating that the scope is what keeps a game's trailing null probabilities from inheriting the next game's values and what keeps `shift(-1)` from reading the next game's first play.

    Change nothing else: not the formulas, not the `game_end`/`half_end` null-out branches, not the PAT baselines, not the cumulative EPA/WPA totals (already `.over("game_id")`).
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*#' src/flag_football_ep/features/mutations.py | grep -c 'backward_fill()\.over("game_id")'` returns at least 6 (3 pre-existing in `prepare_ep_data` + `Winner` + the 2 new ones... count what the file actually yields and record the number in the SUMMARY).
    - `grep -v '^\s*#' src/flag_football_ep/features/mutations.py | grep -n 'backward_fill()' | grep -v 'over("game_id")'` returns no lines.
    - `grep -v '^\s*#' src/flag_football_ep/features/mutations.py | grep -n 'shift(-1)' | grep -v 'over("game_id")'` returns no lines.
    - `uv run pytest tests/test_features_mutations.py tests/test_migration_equivalence.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_features_mutations.py tests/test_migration_equivalence.py -q</automated>
  </verify>
  <done>All four operations are `.over("game_id")`-scoped, no unscoped `backward_fill()`/`shift(-1)` remains in the file, and the existing feature and migration-equivalence tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-game leakage tests for add_ep_variables and add_wp_variables</name>
  <files>tests/test_features_mutations.py</files>
  <behavior>
    - EP: on a two-game frame where game A's last play has null EP probability columns and game B's first play has non-null ones, game A's last row's `ep` must stay null (nothing later in game A to backfill from) rather than inheriting game B's first-play `ExpPts`.
    - EP: game A's last row's `home_ep_after` must be null, not game B's first-play `home_ep`.
    - WP: on a two-game frame where game A's last play has a null `wp` and game B's first play has a non-null `wp`, game A's last row's `wp` must stay null and its `home_wp_after` must be null.
    - Non-regression: on a single-game frame, every column produced by `add_ep_variables`/`add_wp_variables` is unchanged by the scoping (compare against the values the existing single-game tests already pin).
  </behavior>
  <read_first>
    - tests/test_features_mutations.py (the hand-built EP frame helper at line ~97 and the existing `add_ep_variables`/`add_wp_variables` test classes at lines 360-515 — reuse their frame builders, extend them to two games)
    - src/flag_football_ep/features/mutations.py (post-Task-1 state)
  </read_first>
  <action>
    Extend the existing `add_ep_variables` and `add_wp_variables` test groups with cross-game leakage tests. Build the two-game frame from the existing single-game helper by duplicating it with a second `game_id` (and distinct `home_team`/`away_team` values so a leaked `tmp_posteam` is observable), concatenating game A then game B, and nulling the probability columns on game A's final row.

    Add at least four tests: EP `ep` non-leak, EP `home_ep_after` null at the game boundary, WP `wp` non-leak, WP `home_wp_after` null at the game boundary. Assert on the exact boundary rows by filtering on `game_id` and `play_id`, never by positional index.

    Confirm each new test fails when the `.over("game_id")` scoping is removed (revert locally, run, restore — do not commit the revert), and record which tests failed in the SUMMARY.

    Keep `game_end`/`half` values in the fixture consistent with the existing helper so the `game_end == 1` null-out branches do not mask the assertions — if game A's last row is a `game_end` row, `ep`/`wpa` are nulled by design there, so target the leakage assertion at `home_ep_after`/`home_wp_after` and `tmp_posteam` (which the `game_end` branch does not touch) or set `half`/`game_end` so the boundary row is not a game end. State in a comment which choice was made and why.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_features_mutations.py -q` exits 0 with at least 4 more tests collected than before.
    - Each new test fails when the corresponding `.over("game_id")` is removed; the SUMMARY names the tests and the observed failures.
    - `uv run pytest -q` exits 0 (whole suite, including `tests/test_model_score.py` and `tests/test_migration_equivalence.py`).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_features_mutations.py -q && uv run pytest -q</automated>
  </verify>
  <done>Cross-game leakage in the EP/WP variable derivations is covered by tests that fail without the scoping fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-game rows → shared multi-game frame | four sources' games are concatenated into one frame before EP/WP derivation, so one game's values can reach another's rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.2-38 | Tampering | cross-game leakage via unscoped `backward_fill()`/`shift(-1)` in `add_ep_variables`/`add_wp_variables` | mitigate | all four operations scoped `.over("game_id")`; boundary-row tests fail if the scoping is removed |
| T-1.2-39 | Information Disclosure | a game's outcome leaking backwards into an earlier game's win-probability rows | mitigate | the `wp`/`tmp_posteam` fills are game-scoped, so no row can carry a value derived from a different game |

No new dependencies are installed by this plan, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `uv run pytest tests/test_features_mutations.py -q` green
- `uv run pytest tests/test_migration_equivalence.py -q` green (the frozen baseline is untouched — these two functions are not on the equivalence path)
- No unscoped `backward_fill()` or `shift(-1)` remains in `features/mutations.py`
</verification>

<success_criteria>
- The module's documented `.over("game_id")` invariant is true of the whole file, not just of most of it
- A multi-game scoring frame produces the same per-game EP/WP values it would produce if each game were scored alone
</success_criteria>

<output>
Create `.planning/phases/01.2-repo-to-pipeline/01.2-19-SUMMARY.md` when done
</output>
