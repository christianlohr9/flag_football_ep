---
phase: 01.2-repo-to-pipeline
plan: 21
subsystem: data
tags: [polars, canonical-schema, score-chain, gap-closure]

# Dependency graph
requires:
  - phase: 01.2-repo-to-pipeline
    provides: add_scoring_play_team, add_score_columns, canonical schema (earlier 01.2 plans)
provides:
  - "add_score_columns credits a score that occurs on a game's first play instead of silently zeroing it"
  - "four new TestAddScoreColumns cases pinning home/away/null/defensive-credit first-play behaviour"
  - "measured, documented zero-delta migration-equivalence outcome for the WR-02 fix"
affects: [validation, score_reconstruction, wp-model-mutations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Kleene-safe pl.when precedence: scoring branch evaluated before the play-1 seed branch, with a dedicated play_id==1 catch-all after it for the null-scoring_play_team case"

key-files:
  created: []
  modified:
    - src/flag_football_ep/canonical.py
    - tests/test_canonical.py
    - tests/test_migration_equivalence.py

key-decisions:
  - "Branch order for home/away points: (play_id==1 AND team != scoring_play_team) -> 0, then (team == scoring_play_team) -> points_value, then play_id==1 -> 0 (null-safe catch-all), then null."
  - "No test tolerance widened; the migration-equivalence docstring gained one sentence documenting the measured zero-delta outcome, per the plan's zero-count branch."

requirements-completed: [REQ-S1-05, REQ-S1-06]

# Metrics
duration: 12min
completed: 2026-08-17
---

# Phase 01.2 Plan 21: Fix WR-02 first-play scoring zeroed by seed precedence Summary

**`add_score_columns` now credits a touchdown, safety, or conversion on a game's first play instead of silently zeroing it, closing Warning WR-02 with zero measured delta on the legacy corpus.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-17T20:07:00Z
- **Completed:** 2026-08-17T20:19:18Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Reordered the `home_team_points`/`away_team_points` `pl.when` branches in `add_score_columns` so the scoring branch is evaluated before the play-1 zero seed, while keeping a null-safe catch-all so a non-scoring (or null-`scoring_play_team`) first play still seeds the cumulative score at 0.
- Added 4 tests to `TestAddScoreColumns` pinning home-touchdown, away-conversion, null-`scoring_play_team`, and defensive-safety-credit behaviour on play 1 (30 -> 34 tests in `tests/test_canonical.py`).
- Measured the legacy corpus impact directly (`ingest_legacy(data/raw/legacy/data_raw.csv)`): 0 of 47 games' first plays carry a non-null `scoring_play_team`, confirming the plan's prediction of zero delta, and recorded that count in `tests/test_migration_equivalence.py`'s known-deltas docstring.
- Confirmed `tests/test_validation_checks.py` and `tests/test_pipeline_ingest.py` are unchanged (no game's `score_reconstruction` outcome shifted), and the full suite (`uv run pytest -q`) is green.

## Task Commits

Each task was committed atomically:

1. **Task 1: Evaluate the scoring branch before the play-1 zero seed** - `cd15139` (fix)
2. **Task 2: Confirm and document the migration-equivalence outcome** - `006073d` (docs)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `src/flag_football_ep/canonical.py` - `add_score_columns` branch reorder (scoring branch before play-1 seed) plus an extended docstring recording the fixed bug, mirroring `add_scoring_play_team`'s documented-fix style
- `tests/test_canonical.py` - 4 new `TestAddScoreColumns` cases: play-1 home touchdown, play-1 away conversion, play-1 null-`scoring_play_team`, play-1 defensive safety with `credit_defense=True`
- `tests/test_migration_equivalence.py` - one sentence appended to the module docstring recording the measured zero-delta outcome (0 of 47 legacy games' first plays have a non-null `scoring_play_team`)

## Decisions Made
- Kept the plan's specified 4-branch order exactly: `(play_id==1 & team != scoring_play_team) -> 0`, `(team == scoring_play_team) -> points_value`, `play_id==1 -> 0` (Kleene-null catch-all), `otherwise -> null`. This is the same precedence style already used in `add_scoring_play_team`'s documented fix, so the two docstrings now read consistently.
- Did not touch `points_value`, `cum_sum`/`forward_fill`, `posteam_score`/`defteam_score`, or `score_differential` — scope stayed inside the two `pl.when` chains named in the plan.

## Deviations from Plan

None - plan executed exactly as written. The measured legacy first-play count (0 of 47) matched the plan's prediction exactly, so the zero-delta branch of Task 2's action block applied with no further investigation needed.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- WR-02 is closed: `score_reconstruction` will no longer quarantine a valid game solely because its opening play scored, once games with such openings enter the pipeline (e.g. via the sportapp.fi path, once the API key is rotated per the existing blocker in STATE.md).
- The migration-equivalence baseline is unaffected by this fix on the current legacy corpus; no baseline fixture regeneration needed.
- No blockers for downstream plans in this wave.

---
*Phase: 01.2-repo-to-pipeline*
*Completed: 2026-08-17*
