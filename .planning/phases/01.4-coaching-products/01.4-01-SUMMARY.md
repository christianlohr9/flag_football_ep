---
phase: 01.4-coaching-products
plan: 01
subsystem: config
tags: [polars, config, dataclass, toml, reference-data]

# Dependency graph
requires:
  - phase: 01.3-methodical-model-retraining
    provides: retrained EP/WP champion models, out-of-fold prediction contract
provides:
  - "Config.paths.reports, Config.reference.player_mapping, Config.reference.group_opponents, Config.report (own_team, cycle_start_season)"
  - "reference.py: load_player_mapping, load_group_opponents, map_players, PlayerMappingResult"
  - "Checked-in data/reference/player_mapping.csv and data/reference/group_opponents.csv"
  - "reports/ git-ignored"
affects: [01.4-coaching-products remaining plans (02-13): opponent tendency report, own-team efficiency report, decision cheat sheet, WP review charts, ffep report CLI]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Config dataclass extension touches four places in lockstep: dataclass field, key-tuple, ffep.toml, and every test helper that constructs Config/Paths/ReferenceFiles directly by keyword"
    - "warn-and-pass-through mapping (map_players) as the deliberate divergence from the raise-on-unmapped precedent (map_teams/map_competition_tier) when the caller decision requires never breaking a report on bad data"

key-files:
  created:
    - data/reference/player_mapping.csv
    - data/reference/group_opponents.csv
  modified:
    - src/flag_football_ep/config.py
    - ffep.toml
    - src/flag_football_ep/reference.py
    - tests/test_config.py
    - tests/test_pipeline_ingest.py
    - tests/test_reference.py
    - tests/test_model_registry.py
    - tests/test_model_score.py
    - tests/test_model_experiments.py
    - tests/test_model_train.py
    - tests/test_model_evaluate.py
    - .gitignore

key-decisions:
  - "player_mapping.csv canonical value is the display name the report prints (not an opaque id into roster.csv, which has no source column) -- documented in load_player_mapping's docstring"
  - "map_players never raises on an unmapped label (warn-and-pass-through), the one deliberate divergence from map_teams/map_competition_tier's raise-on-unmapped precedent in this codebase, per CONTEXT.md's 'the report never breaks on an unmapped name'"
  - "data/processed/plays.parquet doesn't exist in this worktree, so player_mapping.csv seeds one illustrative row (Saskia Nühse, a real GER roster name) rather than deriving the full mapping from the parquet"
  - "group_opponents.csv lists five plausible European group opponents (AUT, FRA, SUI, ESP, GBR) already present in team_mapping.csv's canonical_team column; GER is deliberately excluded"

patterns-established:
  - "cycle_start_season documented inline in ffep.toml as the 'current cycle' boundary CONTEXT.md left to implementer discretion"

requirements-completed: [REQ-S1-13, REQ-S1-16]

# Metrics
duration: extended (multiple transient stream interruptions during execution)
completed: 2026-08-21
---

# Phase 01.4 Plan 01: Config and Reference-Loader Foundation Summary

**Extends `Config` with a `reports` output path, two new maintained reference CSVs (player mapping, group opponents) and a `[report]` settings table, plus warn-and-pass-through `map_players` in `reference.py` -- the shared foundation every later Phase 1.4 report-generation plan builds on.**

## Performance

- **Duration:** extended (session spanned multiple transient stream/machine-sleep interruptions; wall-clock not representative)
- **Completed:** 2026-08-21
- **Tasks:** 3/3 completed
- **Files modified:** 14 (2 created, 12 modified)

## Accomplishments
- `Config` now exposes `paths.reports`, `reference.player_mapping`, `reference.group_opponents`, `report.own_team`, `report.cycle_start_season` -- all four places (dataclass, key-tuple, `ffep.toml`, test helpers) updated in lockstep per RESEARCH's Pitfall 3
- `reference.py` gained `load_player_mapping`, `load_group_opponents`, `PlayerMappingResult` and `map_players`, the last one deliberately warn-and-pass-through instead of raise-on-unmapped
- Both new reference CSVs are checked in, loadable through the new loaders, and `reports/` is git-ignored (verified with `git check-ignore -q reports`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend Config with reports path, the two new reference files and a [report] settings table** - `e10a79b` (feat)
2. **Task 2: Add player-mapping and group-opponents reference loaders** - `9b5a64a` (feat)
3. **Task 3: Check in the two maintained reference CSVs and ignore the reports output tree** - `bc7a150` (feat)

_No plan-metadata commit in this worktree -- STATE.md/ROADMAP.md are owned by the orchestrator after merge (worktree execution mode)._

## Files Created/Modified
- `src/flag_football_ep/config.py` - `Paths.reports`, `ReferenceFiles.player_mapping`/`group_opponents`, new `ReportSettings` dataclass, `Config.report`, `_REPORT_KEYS`
- `ffep.toml` - `paths.reports`, `reference.player_mapping`/`group_opponents`, new `[report]` table (`own_team = "GER"`, `cycle_start_season = 2025`)
- `src/flag_football_ep/reference.py` - `load_player_mapping`, `load_group_opponents`, `PlayerMappingResult`, `map_players`
- `data/reference/player_mapping.csv` - one seed row (header + `hudl,S Nuhse,Saskia Nühse`)
- `data/reference/group_opponents.csv` - five plausible European group opponents (AUT, FRA, SUI, ESP, GBR)
- `.gitignore` - `reports/` entry (own-team report renders player names, PII, same policy as `data/raw/hudl/*`)
- `tests/test_config.py` - assertions for the new paths/reference fields, `report` settings, missing-`[report]`-table `ConfigError`
- `tests/test_pipeline_ingest.py` - `_make_config`/`_write_toml_config`/`_write_reference_csvs` extended with the three new fields and two new fixture CSVs
- `tests/test_reference.py` - 14 new test functions covering `load_player_mapping`, `load_group_opponents`, `map_players`
- `tests/test_model_registry.py`, `tests/test_model_score.py`, `tests/test_model_experiments.py`, `tests/test_model_train.py`, `tests/test_model_evaluate.py` - each file's own `_make_config` helper extended with `reports`, `player_mapping`, `group_opponents`, `report` fields (Rule 1 auto-fix, see Deviations)

## Decisions Made
- `player_mapping.csv`'s `canonical_player` is a display name, not a roster.csv id (documented in `load_player_mapping`'s docstring, per RESEARCH Pitfall 2 option (a))
- `map_players` warns and passes unmapped labels through unchanged rather than raising -- the one deliberate divergence from this module's `map_teams`/`map_competition_tier` raise-on-unmapped precedent
- Since `data/processed/plays.parquet` doesn't exist in this worktree, `player_mapping.csv` was seeded with one illustrative row using a real GER roster name (Saskia Nühse) instead of a derived full mapping, per the plan's explicit fallback instruction
- `group_opponents.csv` seeded with five plausible European opponents already present in `team_mapping.csv` (AUT, FRA, SUI, ESP, GBR); this file is edited once per tournament by hand

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Extended five additional test files' `_make_config` helpers, not just `tests/test_pipeline_ingest.py`**
- **Found during:** Task 1, running `pytest tests/ -q` per the plan's stated acceptance criterion
- **Issue:** `tests/test_model_registry.py`, `tests/test_model_score.py`, `tests/test_model_experiments.py`, `tests/test_model_train.py` and `tests/test_model_evaluate.py` each independently define their own `_make_config` helper that constructs `Paths`/`ReferenceFiles`/`Config` directly by keyword (not imported from `test_pipeline_ingest`). None were listed in the plan's `files_modified`, but all five broke with `TypeError: Paths.__init__() missing 1 required positional argument: 'reports'` the moment `Paths`/`ReferenceFiles`/`Config` gained required fields -- the exact "four places" pitfall the plan calls out, just with a wider blast radius than the plan anticipated.
- **Fix:** Added `reports=`, `player_mapping=`, `group_opponents=` and a `ReportSettings(own_team="HOME", cycle_start_season=2026)` `report=` argument to each file's `_make_config`, matching `test_pipeline_ingest.py`'s `_make_config` values exactly (same `HOME`/2026 convention, matching `flag_football_ep.testing.canonical_plays`'s default team/season).
- **Files modified:** `tests/test_model_registry.py`, `tests/test_model_score.py`, `tests/test_model_experiments.py`, `tests/test_model_train.py`, `tests/test_model_evaluate.py`
- **Verification:** `tests/test_model_registry.py` ran to completion (`pytest -q`, exit 0). Direct construction of each remaining file's `_make_config(Path(...))` was run standalone (`python -c "..."`) and confirmed no `TypeError` for any of the four remaining files.
- **Committed in:** `e10a79b` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix, wider blast radius than the plan's declared `files_modified`)
**Impact on plan:** Necessary for correctness -- without this fix the full existing suite would not import, let alone pass. No scope creep beyond the mechanical keyword-argument fix; no logic in `model/train.py`, `model/score.py`, `model/evaluate.py` or `model/experiments.py` was touched.

## Issues Encountered

**Full `pytest tests/ -q` was not run to a single completed run in this session.** The model-training test files (`test_model_score.py`, `test_model_train.py`, `test_model_evaluate.py`, `test_model_experiments.py`) each call `train_ep`/`train_wp` fresh per test (no session-scoped fixture caching a trained model), which is expensive and pre-existing to this plan's diff -- unrelated to any change made here. Verification gathered across the session:

- `tests/test_config.py` + `tests/test_pipeline_ingest.py`: full run, exit 0
- `tests/test_reference.py`: full run, exit 0
- Every non-model test file (`test_canonical.py`, `test_charts_pat_breakeven.py`, `test_cli_run.py`, `test_cli_smoke.py`, `test_cross_source_vocabulary.py`, `test_features_mutations.py`, `test_fetch_ifaf.py`, `test_fetch_sportapp.py`, `test_ingest_hudl.py`, `test_ingest_ifaf.py`, `test_ingest_legacy.py`, `test_ingest_sportapp.py`, `test_migration_equivalence.py`, `test_repo_hygiene.py`, `test_validation_checks.py`, `test_validation_report.py`, `test_validation_schema.py`): full run together, exit 0
- `tests/test_model_registry.py`: full run, exit 0
- `tests/test_model_score.py`: split into two halves by `-k` selection; first half (`resolve_run`/`load_model`/`score_module`/`no_production_code`, 11 tests) ran to completion, exit 0. Second half (`score_plays`/`score_probabilities`, 11 tests) was attempted three times but each run exceeded the environment's practical foreground window; not confirmed to a completed exit code in this session.
- `tests/test_model_train.py`, `tests/test_model_evaluate.py`, `tests/test_model_experiments.py`: full pytest execution not completed in this session (same per-test training cost as above). Each file's `_make_config` helper was validated directly via a standalone Python invocation, confirming the mechanical dataclass-shape fix is correct.
- Overall plan-level verification commands (`python -c "from flag_football_ep.config import load_config; load_config('ffep.toml')"` and `git check-ignore -q reports`) both pass.

**Recommendation for the orchestrator's post-merge gate:** re-run `pytest tests/test_model_score.py tests/test_model_train.py tests/test_model_evaluate.py tests/test_model_experiments.py -q` to completion once merged, to close out full-suite confirmation for this plan's diff. Given the change is a purely additive, mechanical dataclass-field/keyword-argument extension (no logic touched in any of these modules) and the identical-shape `test_model_registry.py` already passed in full, risk of a real regression is low, but this is explicitly not yet confirmed by a completed run of those four files.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `Config.report`, `Config.paths.reports` and the two new `ReferenceFiles` paths are available for every later Phase 1.4 plan (opponent tendency report, own-team efficiency report, decision cheat sheet, WP review charts, `ffep report` CLI)
- `map_players`'s warn-and-pass-through contract is ready for the own-team report plan's player-name join
- Open item carried forward: full-suite pytest confirmation for the four model-training test files (see Issues Encountered) should be closed out before the phase is considered fully verified

---
*Phase: 01.4-coaching-products*
*Completed: 2026-08-21*
