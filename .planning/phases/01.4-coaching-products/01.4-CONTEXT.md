# Phase 1.4: Coaching Products - Context

**Gathered:** 2026-08-19
**Status:** Ready for planning

<domain>
## Phase Boundary

The HC opens auto-generated, tournament-ready scouting products (REQ-S1-12 … REQ-S1-16): an opponent tendency report per group opponent, an own-team efficiency report, a game-decisions cheat sheet (PAT break-even + 4th-down conversion by distance), and per-game WP review charts — all rendered as standalone German HTML files and reachable from raw exports in under 10 minutes via one batch command. This completes Milestone 1's Strand-1 half.

Not this phase: live/in-game tooling (D-08), any CV dependency, model methodology changes (1.3 delivered the retrained, versioned champion models this phase consumes).

</domain>

<decisions>
## Implementation Decisions

### Report format & delivery
- **Output format:** Self-contained HTML — one standalone `.html` file per report with all images embedded; opens in any browser offline. Browser print-to-PDF covers the PDF half of REQ-S1-12; no native PDF dependency.
- **Charts:** Static matplotlib/seaborn images (reuse the headless-Agg convention from `charts/pat_breakeven.py` / `model/evaluate.py`). No plotly/interactivity — primary consumption device is a **tablet**, so layout must be tablet-friendly: large fonts, charts sized to screen width, no horizontally-scrolling tables; pinch-zoom on images is the zoom story.
- **File structure:** One file per opponent; own-team report, decision cheat sheet, and WP game reviews are separate files. No monolithic dossier, no index page requirement.
- **Language:** German prose and labels (football terms like Blitz, Coverage, 4th down stay English as usual).
- **Density:** One-pager-first — each opponent report opens with a compact summary block (top tendencies in plain sentences, key rates) that stands alone in 30 seconds; full tables/charts follow below.
- **Decision charts:** A standalone "game decisions" cheat-sheet page — PAT break-even + 4th-down chart together, printable, camp-ready, usable without the other reports.
- **WP review chart:** Annotated WP line per game — curve with markers on the biggest swings (top-EPA plays, scores, turnovers) with short play captions. Synthetic-time flag must be visible on the chart (C-08 / REQ-S1-02 outcome).
- **Style:** Clean neutral — white background, one accent color, no logos. Details are Claude's discretion within readability-on-tablet-and-print.

### Tendency report content
- **Data scope:** All sources with graceful degradation — formation/route/coverage-dependent sections use Hudl-charted games only (the fields exist only there); down/distance, play-call, 4th-down and PAT behavior also pull from sportapp + IFAF PBP. Each report section states its game basis (which games, which source).
- **Field zones:** 3 zones + explicit red-zone bucket (~last 10 yards) over `yardline_50` ∈ [0, 50].
- **Small samples:** Show everything, always with n — every rate carries its play count (e.g. "78% Pass (n=9)"); cells under a threshold are visually muted, never hidden.
- **Score state:** Three buckets by margin — trailing / close (within one score, ±6) / leading by more.

### Own-team report & player identity
- **Player names:** Maintained roster reference CSV under `data/reference/` (same pattern as `team_mapping.csv`) mapping every source spelling/jersey to one canonical player. Unmapped names appear as-is with a prominent warning listing them — the report never breaks on an unmapped name. (This resolves the cross-source player-mapping decision deferred from Phase 1.2.)
- **Timeframe:** Current cycle by default for player tables; formation/play-call/route EPA shown for the current cycle with all-time as a comparison column. (Exact cycle definition — season boundary vs date — is Claude's discretion, documented in the report.)
- **Drive success:** Points per drive as headline plus a drive-outcome breakdown (TD / turnover / downs / half-end).
- **Defense:** Offense views per REQ-S1-13 **plus a basic defense section** — EPA/play allowed by `DEF FRONT`/`COVERAGE` and takeaways. Kept small; it extends REQ-S1-13, it does not redefine it.
- **EPA source:** Own-team EPA uses the persisted out-of-fold predictions from 1.3 for historical plays (locked 1.3 decision); new games are scored with the refit champion model.

### Report CLI & <10-minute flow
- **Command shape:** One batch command — `ffep report` builds everything by default (all group-opponent pages, own-team report, decision cheat sheet, WP charts), with flags to build a single product or team (e.g. `--opponent FRA`).
- **Timed path (REQ-S1-16):** ingest → score (promoted champion via registry alias) → report. Training/promotion stays a separate deliberate step (`ffep train` + `ffep promote`) outside the 10-minute clock — no silent model changes.
- **Outputs:** Dated run folder (e.g. `reports/2026-08-19/…`) per generation, plus a `reports/latest/` copy always holding the newest set.
- **Group opponents:** A small maintained reference file (`data/reference/` pattern) lists the upcoming tournament's group opponents; edited once per tournament, read by `ffep report`. No API coupling at report time.

### Claude's Discretion
- HTML generation mechanism (Jinja2 vs stdlib templating), CSS details within "clean neutral", accent color.
- Exact muting threshold for thin tendency cells, chart sizing, top-tendency selection logic for the summary block.
- Roster/opponents reference file formats and exact filenames (consistent with `data/reference/` CSV style).
- "Current cycle" boundary definition; WP-chart play-annotation selection rule (e.g. top-k |EPA| swings).
- How `ffep report` integrates with `ffep run` (1.2 decided report generation joins the chain — wiring is discretion).
- Handling of an opponent with zero film in some section (show an explicit "no data" block rather than failing — exact form is discretion).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase sources & requirements
- `.planning/REQUIREMENTS.md` — REQ-S1-12 (tendency report), REQ-S1-13 (own-team efficiency), REQ-S1-14 (decision charts), REQ-S1-15 (WP review charts), REQ-S1-16 (<10-minute turnaround)
- `docs/plan-1-analytics-refresh.md` §"Phase 1.4" — original phase definition (German, authoritative detail)
- `.planning/PROJECT.md` §Key Decisions — D-08 (Strand-1 non-goals: no live tooling, no CV dependency)
- `.planning/phases/01.3-methodical-model-retraining/01.3-CONTEXT.md` — locked 1.3 decisions this phase consumes: out-of-fold EPA contract, champion-alias promotion, PAT baselines, reusable chart-module intent
- `.planning/phases/01.2-repo-to-pipeline/01.2-CONTEXT.md` — `ffep run` chain design (report generation joins it here), `data/reference/` maintained-CSV pattern, player-mapping deferral this phase resolves

### Code this phase builds on
- `src/flag_football_ep/charts/pat_breakeven.py` + `ffep pat-breakeven` — existing chart module and headless-Agg convention; the decision cheat sheet reuses/extends it (part of REQ-S1-14 already delivered in 1.3)
- `src/flag_football_ep/model/score.py` — champion-alias resolution the report pipeline calls
- `src/flag_football_ep/model/train.py` (`write_oof_predictions`) — persisted out-of-fold predictions, the own-team EPA input
- `src/flag_football_ep/cli.py` — Typer app where `ffep report` lands; `ffep run` chain to extend
- `src/flag_football_ep/canonical.py` — canonical column set (incl. nullable rich Hudl fields `OFF FORM`, `OFF PLAY`, `TARGET ROUTE`, `Thrown By`, `RECEIVED BY`, `YAC`) the tendency/own-team slicing reads
- `src/flag_football_ep/config.py` — config surface for new knobs (report paths, opponents file)

### Data inputs
- `data/processed/plays.parquet` — canonical plays with source/competition filter columns
- `data/reference/team_mapping.csv` — style template for the new roster and opponents reference files

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `charts/pat_breakeven.py`: render-function returning a matplotlib Figure without showing/saving — the module shape every new chart (4th-down, WP line, tendency visuals) should follow; `Agg` backend set before pyplot import
- Out-of-fold predictions Parquet (written by `ffep train`): per-play held-out EP/WP — direct input for honest own-team EPA aggregation
- MLflow registry + `ffep promote` champion alias: the report pipeline resolves champion, never latest-run
- Validation report precedent (per-run Markdown next to the Parquet): prose+file-output conventions to stay consistent with

### Established Patterns
- Reference data as small maintained CSVs under `data/reference/` — roster mapping and group-opponents files follow this shape; ingest-style loud warnings on unmapped entries
- Config via `flag_football_ep.config.Config` — report output dir, opponents-file path, thresholds belong there
- Typer subcommands on the single `ffep` entry point; lazy imports per command
- polars one-function-per-concern helpers for aggregation logic
- matplotlib + seaborn are the only viz deps; **no HTML templating stack exists yet** — Jinja2 (or chosen alternative) is a new dependency decision for the planner/researcher

### Integration Points
- `ffep run` chains ingest → train → score today; 1.2 decided report generation joins the chain in this phase — the timed REQ-S1-16 path is `ffep run`-or-equivalent ending in reports
- `ffep score` writes scored plays to Parquet — the report generator's model-derived input alongside the out-of-fold predictions
- Champion-alias resolution speed matters: the <10-minute budget lives here (1.3 flagged it as a 1.4-facing contract)

</code_context>

<specifics>
## Specific Ideas

- Reports are consumed on a **tablet** by the HC — every layout decision traces back to that (static images, pinch-zoom, no hover-dependent info, large fonts).
- The HC gets the essentials in ~30 seconds: the plain-sentence summary block on top of each opponent page is the product's heart; tables below are for staff who scroll.
- Reports are in German; analytics terms and football vocabulary stay in their usual English form.
- The user consistently picks statistical honesty (n on every rate, out-of-fold EPA, champion-only scoring) — keep that bias when planning trade-offs.

</specifics>

<deferred>
## Deferred Ideas

- Interactive charts (plotly hover/zoom) — revisit if tablet usage shows static images falling short.
- Opponent-adjusted decision charts (cheat sheet tuned per opponent) — considered when placing decision charts; not chosen.
- Richer defense analytics beyond the basic section (coverage-level route success, per-defender metrics) — future iteration, possibly with Strand-2 tracking data.
- WP + per-play EPA panel as an extended game-review layout — annotated WP line chosen for now.

### Reviewed Todos (not folded)
- **Ratify data contract v1.0 with the Videoanalyst** (`.planning/todos/pending/2026-08-17-ratify-data-contract-with-videoanalyst.md`) — analyst-blocked and user-owned; stays a tracked todo outside this phase (same handling as 1.2/1.3).
- **Tap IFAF/cpx.studio WM API as additional data source** (`.planning/todos/pending/2026-08-17-tap-ifaf-wm-api-as-data-source.md`) — both halves already delivered (ingest in 1.2, calibration/benchmarking in 1.3); nothing left to fold here. IFAF games do enter tendency reports via the all-sources data-scope decision. Candidate for closing.

</deferred>

---

*Phase: 1.4-Coaching Products*
*Context gathered: 2026-08-19*
