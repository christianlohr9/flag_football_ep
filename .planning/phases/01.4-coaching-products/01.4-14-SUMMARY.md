---
phase: 01.4-coaching-products
plan: 14
subsystem: docs
tags: [documentation, human-verification, uat]

# Dependency graph
requires:
  - phase: 01.4-coaching-products
    plan: "01.4-13"
    provides: "ffep report CLI command this plan documents and verifies against real data"
provides:
  - "docs/coaching-reports.md: coaching-report workflow guide"
  - "README.md: report workflow section"
  - "Human-verified REQ-S1-16 timing evidence (25.1s real run) and tablet/print sign-off"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - docs/coaching-reports.md
  modified:
    - README.md
    - src/flag_football_ep/reports/aggregate.py

key-decisions:
  - "The real-data verification run exposed a null-group-key crash in share_table (all five opponent reports failed): real corpora carry null down/distance on un-charted rows, which the synthetic test fixtures never produced, and polars' default join drops null keys, so group_n came back null and int(group_n) raised. Fixed with join_nulls=True on the group_n join (f5a8896); all report suites re-pass."
  - "Champion models had never been trained/promoted on this machine; ran ffep train + ffep promote for EP and WP once as setup. This one-time setup is deliberately outside the REQ-S1-16 timed budget, which measures the recurring raw-exports-to-reports path."

requirements-completed: [REQ-S1-16]

# Metrics
duration: ~1h (docs) + verification session
completed: 2026-08-22
---

# Phase 01.4 Plan 14: Report Workflow Docs + Human Verification Summary

**Documented the coaching-report workflow, then verified REQ-S1-16 against the real corpus: `ffep report` produced the complete report set (5 opponent reports, own-team, decisions, one WP review per game) in 25.1 seconds — a ~24x margin under the ten-minute budget — after fixing a real-data-only null-join crash the run itself exposed.**

## Performance

- **Tasks:** 2/2 completed (Task 2 is the human-verify checkpoint)
- **Completed:** 2026-08-22

## Accomplishments

- `docs/coaching-reports.md`: the coaching-report workflow — prerequisites (promoted champions), the `ffep report` command and its flags, the output layout (`reports/<date>/` + `reports/latest/`), the maintained reference files (`player_mapping.csv`, `group_opponents.csv`) and how to update them before a tournament.
- README section pointing to the workflow doc.
- **Real-run verification (Task 2, checkpoint resolved):**
  - Timing: `time ffep report` → **total 25.1s** (ingest 0.45s, score 0.50s, report 24.2s). REQ-S1-16's ten-minute budget cleared by ~24x.
  - Output completeness: `reports/latest/` contains one `opponent-<CODE>.html` per row of `group_opponents.csv` (AUT, ESP, FRA, GBR, SUI), plus `own-team.html`, `decisions.html`, and one `wp-review-*.html` per game in the corpus.
  - Content checks: synthetic-clock notice present on WP review pages; own-team report renders the "Nicht zugeordnete Spielerinnen-Namen" warning section and the oof/champion EPA provenance sentence; decisions and opponent pages embed their charts as self-contained base64 PNGs.
  - Human sign-off: user reviewed the rendered pages and approved ("approved", 2026-08-22).

## Deviations

- **Rule 1 bug fix (found by the verification run itself):** first real run failed all five opponent reports with `int() argument ... not 'NoneType'`. The per-product failure-notice mechanism (T-1.4-59) worked as designed — the rest of the batch completed — but the root cause was `share_table`'s `group_n` join dropping null group keys (`down`/`distance_bucket` are null on un-charted real rows). Fixed with `join_nulls=True` in `reports/aggregate.py`, re-verified: all four previously-failing opponents build, report test suites pass, and the re-run produced the complete set.

## Task Commits

1. **Task 1: Document the report workflow and the maintained reference files** - `9f0bab7` (docs)
2. **Task 2: Verification fix surfaced by the real run** - `f5a8896` (fix)

## Files Created/Modified

- `docs/coaching-reports.md` - workflow guide (created)
- `README.md` - report workflow section (modified)
- `src/flag_football_ep/reports/aggregate.py` - null-safe `group_n` join (modified, verification fix)
