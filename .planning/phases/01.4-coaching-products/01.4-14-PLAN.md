---
phase: 01.4-coaching-products
plan: 14
type: execute
wave: 6
depends_on: ["01.4-13"]
files_modified:
  - README.md
  - docs/coaching-reports.md
autonomous: false
requirements: [REQ-S1-12, REQ-S1-16]

must_haves:
  truths:
    - "The developer can find, from the README alone, how to produce a full report set and which two reference files must be maintained per tournament"
    - "The documented workflow separates the deliberate train/promote step from the timed report path"
    - "A human has confirmed on a real tablet that the reports are legible and print to PDF correctly"
    - "The real end-to-end run has been timed against the ten-minute budget on the real corpus"
  artifacts:
    - path: "docs/coaching-reports.md"
      provides: "the report workflow, the reference-file contracts, and the documented discretion decisions"
      contains: "ffep report"
    - path: "README.md"
      provides: "a report section pointing at docs/coaching-reports.md"
      contains: "ffep report"
  key_links:
    - from: "docs/coaching-reports.md"
      to: "data/reference/group_opponents.csv and data/reference/player_mapping.csv"
      via: "documented per-tournament maintenance workflow"
      pattern: "group_opponents"
---

<objective>
Document the coaching-report workflow, then have a human confirm the two things automated tests
structurally cannot: that the reports are actually legible and printable on the HC's tablet, and
that the real corpus goes from raw exports to finished reports inside ten minutes.

Purpose: REQ-S1-16's acceptance is a wall-clock measurement on real data, and REQ-S1-12's
acceptance includes a PDF path that exists only inside a browser's print dialog. Both are listed
as manual-only verifications in the phase validation strategy. Without this plan the phase can
pass its whole test suite while failing its actual success criteria.
Output: `docs/coaching-reports.md`, a README pointer, and a recorded human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-coaching-products/01.4-CONTEXT.md
@.planning/phases/01.4-coaching-products/01.4-VALIDATION.md
@.planning/phases/01.4-coaching-products/01.4-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the report workflow and the maintained reference files</name>
  <files>docs/coaching-reports.md, README.md</files>
  <read_first>
    - README.md (current structure, heading style and command-example style to match)
    - docs/plan-1-analytics-refresh.md lines 30-44 (the German source definition of the four products and the Strand-1 success criterion)
    - src/flag_football_ep/cli.py (the `report` command's final option surface as implemented in plan 13)
    - src/flag_football_ep/reports/aggregate.py (the field-zone boundaries, the score-state band and the muting threshold to quote verbatim)
    - src/flag_football_ep/reports/build.py (the timed-path stage order and the champion-alias constraint to document)
    - .planning/phases/01.4-coaching-products/01.4-13-SUMMARY.md (what actually shipped, including deviations)
  </read_first>
  <action>
    Create `docs/coaching-reports.md` covering, in this order:

    1. The four products and the file each lands in — `opponent-<CODE>.html`, `own-team.html`,
       `decisions.html`, `wp-review-<game>.html` — that each is a standalone offline HTML file,
       and that the PDF route is the browser's print dialog: there is no PDF dependency, by
       design.
    2. The commands, as real invocations: `ffep report` for everything,
       `ffep report --opponent FRA` for one team, `--product` for one family, `--skip-ingest`
       for re-rendering from an existing `plays.parquet`.
    3. The two-step model discipline, stated explicitly: `ffep train` + `ffep promote` is a
       separate deliberate step, and `ffep report` scores with the promoted `champion` alias and
       never trains. Include the failure mode — a `RegistryError` naming `ffep promote` when no
       champion exists — and its fix.
    4. The two maintained reference files with their exact schemas:
       `data/reference/group_opponents.csv` (`canonical_team,team_name`, edited once per
       tournament) and `data/reference/player_mapping.csv`
       (`source,source_player,canonical_player`, extended whenever the own-team report's
       unmapped-name warning lists a new spelling). Describe the maintenance loop: run the
       report, read the warning block, add the rows, re-run.
    5. The output layout: a dated run folder per generation plus `reports/latest/` always holding
       the newest complete set, and that `reports/` is git-ignored because the own-team report
       contains player names.
    6. The documented discretion decisions, quoted from the code so the report footnotes and the
       docs cannot drift: the field-zone boundaries, the ±6 score-state band, the muting
       threshold, the current-cycle definition (`report.cycle_start_season` in `ffep.toml`), the
       WP annotation-selection rule, and the embedded-chart dpi.
    7. The synthetic-clock caveat: until REQ-S1-02's real clock data arrives, every WP chart is
       flagged synthetic and WP values must not be used for clock-management conclusions.

    Add a short `## Coaching-Reports` section to `README.md` with the one-line `ffep report`
    invocation and a link to `docs/coaching-reports.md`, matching the README's existing heading
    and code-block style. Do not duplicate the detail in both files — the README points, the doc
    explains.
  </action>
  <verify>
    <automated>python -c "import pathlib; d=pathlib.Path('docs/coaching-reports.md').read_text(encoding='utf-8'); r=pathlib.Path('README.md').read_text(encoding='utf-8'); req=['ffep report','group_opponents.csv','player_mapping.csv','ffep promote','reports/latest','cycle_start_season']; missing=[t for t in req if t not in d]; assert not missing, missing; assert 'ffep report' in r and 'coaching-reports.md' in r; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/coaching-reports.md` exists and contains `ffep report`, `ffep promote`, `group_opponents.csv`, `player_mapping.csv`, `reports/latest`, `cycle_start_season`
    - `README.md` contains `ffep report` and a link to `docs/coaching-reports.md`
    - The field-zone boundaries, the score-state band and the muting threshold in the doc match the values in `src/flag_football_ep/reports/aggregate.py`
    - `pytest tests/test_repo_hygiene.py -q` exits 0 (no new stray files at the repository root)
  </acceptance_criteria>
  <done>The report workflow, the per-tournament maintenance loop and every documented discretion decision are written down where the developer will look.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Tablet legibility, print-to-PDF and the real ten-minute timing</name>
  <files>(none — verification only; this task modifies no files)</files>
  <read_first>
    - docs/coaching-reports.md (task 1 output — the workflow the human follows)
    - .planning/phases/01.4-coaching-products/01.4-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
    Produce a full report run against the real corpus, then hand off to the developer for the
    device-level checks listed under `<how-to-verify>`. Before pausing: confirm a champion is
    promoted for both models, run `ffep report` from the repository root while capturing the
    total wall-clock time and the echoed per-stage durations, and list the contents of
    `reports/latest/`. Present those three results to the developer alongside the verification
    steps, then wait for the resume signal. Do not mark the phase complete on a green test suite
    alone — the ten-minute budget and the print-to-PDF path are observable only here.
  </action>
  <what-built>
    A full `ffep report` run has been produced against the real corpus. Every report family
    renders as a standalone offline HTML file with embedded charts, a dated run folder and a
    `reports/latest/` copy. The automated suite has verified structure, escaping,
    self-containment, file size and the never-retrains guarantee — it cannot verify legibility on
    a physical tablet, the browser's print-to-PDF output, or the wall-clock time on the real
    corpus. Those three are what this checkpoint covers.
  </what-built>
  <how-to-verify>
    1. Make sure a champion is promoted for both models: `ffep promote --model ep` and
       `ffep promote --model wp` if they are not already. This is deliberately outside the timed
       path.
    2. Time the real run from the repository root:
       `time ffep report`
       Record the total wall-clock time and the per-stage durations the command echoes.
       REQ-S1-16's bar: raw exports to finished reports in under 10 minutes.
    3. Confirm the output set: `ls reports/latest/` should list one `opponent-<CODE>.html` per
       row in `data/reference/group_opponents.csv`, plus `own-team.html`, `decisions.html`, and
       one `wp-review-*.html` per game.
    4. Copy `reports/latest/` onto the tablet the HC actually uses (or open the files there via
       your usual transfer route) and open an opponent report in the tablet browser. Check:
       - the summary block at the top is readable without zooming and conveys the opponent's
         tendencies in about 30 seconds
       - tables fit the screen width without horizontal scrolling
       - charts scale to screen width and pinch-zoom works on them
       - greyed thin-sample cells are visibly distinct from normal cells, and the legend
         explaining the greying is findable
    5. Open `own-team.html` on the same device. Confirm the unmapped-player warning block (if any
       names are unmapped) is prominent and above the tables, and that the EPA provenance
       sentence in the footnotes reads correctly.
    6. Open `decisions.html` and use the browser's print dialog to save it as PDF. Check the
       resulting PDF: charts are not split across pages, background shading survives the print,
       and no URLs are appended to links. This is REQ-S1-12's PDF half.
    7. Open one `wp-review-*.html` and confirm the synthetic-clock notice is visible both in the
       page text and on the chart itself.

    Report back: the measured total time, whether it cleared 10 minutes, and any legibility or
    print problem you hit — including "the summary block does not actually tell me what I need in
    30 seconds", which is a real finding, not a nitpick.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms tablet legibility, print-to-PDF output and the synthetic-clock notice, and reports the measured `ffep report` runtime on the real corpus</human-check>
  </verify>
  <acceptance_criteria>
    - The measured wall-clock time of `ffep report` on the real corpus is recorded in the plan summary, with the per-stage breakdown
    - `reports/latest/` contains one opponent page per group-opponents row, plus the own-team, decisions and per-game WP pages
    - A PDF produced by browser print-to-PDF from `decisions.html` exists and has unsplit charts
    - Tablet legibility of the summary block, tables and charts is confirmed, or specific defects are listed
    - The synthetic-clock notice is confirmed present in both the WP page text and the chart
  </acceptance_criteria>
  <resume-signal>Type "approved" with the measured runtime, or describe the legibility, print or timing problems found</resume-signal>
  <done>A human has signed off on tablet legibility and print-to-PDF, and the real end-to-end runtime is recorded against the ten-minute budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated reports → the HC's tablet and any printed copy | this is where the phase's output leaves the repository and enters coaching use |
| documentation → future maintenance of the reference files | a wrong documented schema causes a mis-edited reference CSV, which silently mis-attributes players or opponents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.4-62 | Repudiation | phase declared done on a green test suite while the real ten-minute budget was never measured | mitigate | the blocking human checkpoint requires a recorded wall-clock measurement on the real corpus, with the per-stage breakdown |
| T-1.4-63 | Tampering (documentation drift) | documented bucket boundaries or thresholds diverging from the code | mitigate | the doc quotes the values from `reports/aggregate.py`, and an acceptance criterion requires doc and code to match |
| T-1.4-64 | Information disclosure | the PII-bearing own-team report copied to a tablet and left in shared storage | accept | an operational control outside this codebase; the doc states that `reports/` is git-ignored and why, so the sensitivity is at least explicit |
| T-1.4-65 | Tampering | print-to-PDF silently dropping the shading that encodes thin-sample muting | mitigate | plan 02's `@media print` rules set `print-color-adjust: exact`; the checkpoint explicitly asks the human to confirm shading survives the print |
</threat_model>

<verification>
- `python -c "import pathlib; assert 'ffep report' in pathlib.Path('docs/coaching-reports.md').read_text(encoding='utf-8')"` exits 0
- `pytest tests/ -q` exits 0
- Human checkpoint approved with a recorded runtime
</verification>

<success_criteria>
- `docs/coaching-reports.md` documents the commands, the model discipline, both reference-file schemas, the output layout and every discretion decision, matching the code
- `README.md` points at it
- A human has confirmed tablet legibility, browser print-to-PDF output and the synthetic-clock disclosure on real generated reports
- The real end-to-end runtime is measured and recorded against REQ-S1-16's ten-minute budget
</success_criteria>

<output>
Create `.planning/phases/01.4-coaching-products/01.4-14-SUMMARY.md` when done
</output>
