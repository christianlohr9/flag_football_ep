---
phase: 01.4-coaching-products
plan: 11
type: execute
wave: 4
depends_on: ["01.4-02", "01.4-08", "01.4-10"]
files_modified:
  - src/flag_football_ep/reports/opponent.py
  - src/flag_football_ep/templates/opponent_report.html.j2
  - tests/test_reports_opponent_page.py
autonomous: true
requirements: [REQ-S1-12]

must_haves:
  truths:
    - "One standalone HTML file per opponent opens offline, leads with a plain-German summary block, and carries the four tendency sections below it"
    - "Every rate in every table shows its play count, and thin cells render greyed rather than hidden"
    - "Each section states which games and which sources it is based on"
    - "An opponent with no film renders an explicit no-data page instead of failing"
  artifacts:
    - path: "src/flag_football_ep/templates/opponent_report.html.j2"
      provides: "opponent tendency layout extending base.html.j2"
      contains: "extends"
    - path: "src/flag_football_ep/reports/opponent.py"
      provides: "build_opponent_page, opponent_filename, team_slug"
      exports: ["build_opponent_page", "opponent_filename", "team_slug"]
  key_links:
    - from: "src/flag_football_ep/reports/opponent.py"
      to: "flag_football_ep.charts.tendency.render_share_bars"
      via: "embedded through fig_to_data_uri"
      pattern: "render_share_bars"
    - from: "src/flag_football_ep/templates/opponent_report.html.j2"
      to: "summary block above the tables"
      via: "one-pager-first layout"
      pattern: "summary-block"
---

<objective>
Render plan 08's `OpponentReportData` as REQ-S1-12's deliverable: one self-contained German
HTML file per opponent, summary block first, tables and charts below, printable to PDF from the
browser.

Purpose: this is the artifact the HC actually opens before a group game. CONTEXT locks
one file per opponent, no index page, a 30-second summary block on top, n on every rate,
muted-not-hidden thin cells, and a per-section data basis.
Output: `build_opponent_page` in `reports/opponent.py` plus `templates/opponent_report.html.j2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-coaching-products/01.4-CONTEXT.md
@.planning/phases/01.4-coaching-products/01.4-RESEARCH.md
@.planning/phases/01.4-coaching-products/01.4-PATTERNS.md
@.planning/phases/01.4-coaching-products/01.4-02-SUMMARY.md
@.planning/phases/01.4-coaching-products/01.4-08-SUMMARY.md
@.planning/phases/01.4-coaching-products/01.4-10-SUMMARY.md

<interfaces>
<!-- Contracts this plan establishes. Plan 13 dispatches one page per group opponent. -->

reports/opponent.py (added by this plan):
  TEAM_SLUG_PATTERN: re.Pattern            # ^[A-Za-z0-9._-]+$
  team_slug(team: str) -> str              # allow-list sanitised; raises ValueError when empty
  opponent_filename(team: str) -> str      # f"opponent-{team_slug(team)}.html"
  build_opponent_page(data: OpponentReportData, *, generated_on: date | None = None) -> str
      # one standalone HTML document string; no filesystem writes

Consumed:
  reports.opponent.OpponentReportData / ReportSection      (plan 08)
  reports.render.render_page / fig_to_data_uri             (plan 02)
  charts.tendency.render_share_bars                        (plan 10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the opponent page assembler with a path-safe filename</name>
  <files>src/flag_football_ep/reports/opponent.py, tests/test_reports_opponent_page.py</files>
  <read_first>
    - src/flag_football_ep/reports/opponent.py (plan 08 output: `OpponentReportData`, `ReportSection`, `summary_sentences`, `sections`, `overall_basis`, `notices`)
    - src/flag_football_ep/reports/render.py (plan 02 output: `render_page`, `fig_to_data_uri`, and `write_report_run`'s filename-key rejection rule this must satisfy)
    - src/flag_football_ep/reports/wp_review.py (plan 07 output: `game_slug` — copy its allow-list sanitisation shape for `team_slug` rather than inventing a second one)
    - src/flag_football_ep/charts/tendency.py (plan 10 output: `render_share_bars` column contract)
    - src/flag_football_ep/model/score.py lines 55-74 (`_RUN_ID_PATTERN`/`_validate_run_id`, the codebase's validate-before-path-interpolation discipline)
  </read_first>
  <action>
    Add `TEAM_SLUG_PATTERN`, `team_slug`, `opponent_filename` and `build_opponent_page` to
    `src/flag_football_ep/reports/opponent.py`.

    `team_slug(team)` mirrors plan 07's `game_slug`: replace every character outside
    `[A-Za-z0-9._-]` with `-`, collapse repeated separators, strip leading dots and hyphens,
    re-validate against `TEAM_SLUG_PATTERN`, and raise `ValueError` naming the input when the
    result is empty. `opponent_filename(team)` returns `f"opponent-{team_slug(team)}.html"`.
    Document that the canonical team code reaching this function comes from a hand-edited
    reference CSV, so it is validated here even though plan 13 additionally restricts the CLI
    `--opponent` value to the closed set from `load_group_opponents`.

    `build_opponent_page(data, *, generated_on=None) -> str` converts an `OpponentReportData`
    into a render context and returns `render_page("opponent_report.html.j2", ...)`. It performs
    no filesystem writes. Conversion rules:
    - each `ReportSection` becomes a dict with its heading, its `basis.text`, its
      `empty_notice`, a list-of-dicts rendering of `table` (polars `to_dicts()`), and the
      column headers in display order with German labels
    - every rate/share cell is pre-formatted into its display string and a boolean `muted` flag,
      so the template contains no arithmetic and no formatting logic — the template's only job
      is markup. Percentages are rendered with no decimal places and always carry `(n=K)`, the
      form CONTEXT locks; a null rate renders `keine Daten`.
    - for the two share-based sections (formation tendencies, route distribution) also render a
      `render_share_bars` Figure over that section's top rows and embed it via
      `fig_to_data_uri`; a section whose table is empty contributes no chart, only its
      `empty_notice`
    - `data.summary_sentences`, `data.overall_basis.text` and `data.notices` pass through
      unchanged for the summary block, the header and the notices block

    Create `tests/test_reports_opponent_page.py` asserting: `team_slug` leaves `FRA` unchanged,
    sanitises `../etc` and a value containing a slash, and raises `ValueError` on `""` and on a
    separator-only string; `opponent_filename("FRA") == "opponent-FRA.html"` and contains no
    `/`; `build_opponent_page` returns a string starting with `<!DOCTYPE html`; the summary
    sentences all appear in the output; the output contains one `data:image/png;base64,`
    occurrence per non-empty share section; `matplotlib.pyplot.get_fignums()` is empty after the
    call; a zero-film `OpponentReportData` yields a page containing every section's
    `empty_notice` and raises nothing; the page contains no `<script` and no `http://`/`https://`
    external reference.
  </action>
  <verify>
    <automated>pytest tests/test_reports_opponent_page.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/flag_football_ep/reports/opponent.py` contains `def team_slug(`, `def opponent_filename(`, `def build_opponent_page(`, `TEAM_SLUG_PATTERN`
    - `grep -c "write_text\|savefig\|mkdir" src/flag_football_ep/reports/opponent.py` is 0
    - Behavior: `opponent_filename("FRA")` equals `opponent-FRA.html`; `team_slug("../x")` returns a value containing no `/` and no `..`, or raises `ValueError`
    - Behavior: `build_opponent_page` on a zero-film data object returns a page containing every `empty_notice` and raises nothing
    - Behavior: `matplotlib.pyplot.get_fignums()` is empty after the call
    - `pytest tests/test_reports_opponent_page.py -q` exits 0
  </acceptance_criteria>
  <done>An `OpponentReportData` becomes one self-contained HTML string with a safe filename and no template-side arithmetic.</done>
</task>

<task type="auto">
  <name>Task 2: Build the opponent report template</name>
  <files>src/flag_football_ep/templates/opponent_report.html.j2, tests/test_reports_opponent_page.py</files>
  <read_first>
    - src/flag_football_ep/templates/base.html.j2 (plan 02 output: the `title`/`subtitle`/`content`/`footnotes` blocks and the `.summary-block`, `.chart-block`, `.chart-img`, `.muted-cell`, `.warning-block`, `.section-basis` classes)
    - src/flag_football_ep/templates/decisions_cheatsheet.html.j2 (plan 06's sibling template — keep the visual language identical)
    - src/flag_football_ep/reports/opponent.py (the exact context variable names task 1 passes)
    - .planning/phases/01.4-coaching-products/01.4-CONTEXT.md §"Density" and §"Small samples"
  </read_first>
  <action>
    Create `src/flag_football_ep/templates/opponent_report.html.j2` with
    `{% extends "base.html.j2" %}`.

    Layout, top to bottom:
    - `title`: `Gegner-Report — {{ team_name }} ({{ team }})`
    - `subtitle`: generation date plus `overall_basis.text`
    - `content`, first element: the `.summary-block` rendering `summary_sentences` as a short
      list. This block must come first in document order and must stand alone — CONTEXT calls it
      the product's heart, readable in 30 seconds without scrolling.
    - a `.warning-block` for `notices`, rendered only when `notices` is non-empty
    - then one section per entry in `sections`, in the order plan 08 assembles them
      (Formation × Down & Distance × Feldzone, Ziel-Routen-Verteilung, Play-Call nach Score
      State, 4th down & PAT). Each section renders an `<h2>` heading, its `.section-basis` line,
      its chart inside a `.chart-block` as `<img class="chart-img" ...>` when one was produced,
      and its `<table>`. A section with an `empty_notice` renders that notice inside a
      `.warning-block` and no table.
    - every cell whose `muted` flag is set gets `class="muted-cell"` — shown, greyed, never
      dropped. Add a one-line legend under the first table explaining the greying, quoting the
      threshold value passed in from `aggregate.MUTED_MIN_N`.
    - `footnotes`: the field-zone boundaries, the score-state margin definition, the
      confidence-interval method (Clopper-Pearson), and a sentence stating that formation- and
      route-based sections use Hudl-charted games only while down/distance, play-call, 4th-down
      and PAT sections use all sources.

    Tables must wrap rather than force horizontal scrolling on a tablet, inheriting the base
    template's table CSS; do not add per-column min-widths.

    All prose German; `Play-Call`, `Score State`, `Coverage`, `4th down`, `PAT`, `Route` stay
    English.

    Extend `tests/test_reports_opponent_page.py`: the rendered page's index of `summary-block`
    is less than the index of the first `<h2>`; there is one `<h2>` per section; a cell derived
    from a `muted` row carries `muted-cell`; the footnotes mention `Clopper-Pearson`, the
    red-zone boundary and the `±6` score-state band; the notices block appears only when
    `notices` is non-empty; a team name containing `<b>` renders escaped as `&lt;b&gt;` and the
    output contains no `<b>` substring.
  </action>
  <verify>
    <automated>pytest tests/test_reports_opponent_page.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/flag_football_ep/templates/opponent_report.html.j2` contains `{% extends "base.html.j2" %}`, `summary-block`, `muted-cell`, `chart-img`, `section-basis`
    - Behavior: the rendered page's index of `summary-block` is less than the index of the first `<h2>`
    - Behavior: the rendered page contains exactly one `<h2>` per section in `data.sections`
    - Behavior: a `team_name` of `<b>x</b>` renders as `&lt;b&gt;` with no `<b>` substring in the output
    - Behavior: the rendered page contains no `<script` and no `src="http` / `href="http`
    - `pytest tests/test_reports_opponent_page.py -q` exits 0
  </acceptance_criteria>
  <done>The opponent report renders one-pager-first, with thin cells greyed and every section declaring its data basis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical team code (hand-edited reference CSV) → output filename under `reports/` | a caller-influenced value becomes part of a write path |
| charted labels and team names → HTML opened in the HC's tablet browser | source-derived strings interpolated into a document |
| aggregate cells → a scouting page acted on before a game | hidden sample sizes change a game plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.4-46 | Tampering | path traversal via a crafted team code in `opponent_filename` | mitigate | `team_slug` allow-lists `[A-Za-z0-9._-]`, strips leading dots, re-validates against `TEAM_SLUG_PATTERN`; plan 02's `write_report_run` rejects separator-bearing keys; plan 13 additionally restricts `--opponent` to the group-opponents set |
| T-1.4-47 | Tampering | markup injection via a team name or charted category label | mitigate | rendering goes through plan 02's `select_autoescape` environment; asserted with a `<b>` payload |
| T-1.4-48 | Tampering (misrepresentation) | a thin cell rendered indistinguishably from a solid one | mitigate | `muted` cells carry `muted-cell` and a legend quotes the threshold; asserted in tests |
| T-1.4-49 | Denial of service | an opponent with no film aborting the batch run | mitigate | empty sections render their `empty_notice`; asserted with a zero-film data object |
| T-1.4-50 | Information disclosure | an offline page phoning home through an external resource | mitigate | the page embeds every image as a data URI and contains no external `http(s)` reference and no script; asserted in tests |
</threat_model>

<verification>
- `pytest tests/test_reports_opponent_page.py -q` exits 0
- `pytest tests/ -q` exits 0
</verification>

<success_criteria>
- `build_opponent_page` returns one self-contained HTML string per opponent with no filesystem side effects and no leaked figures
- The summary block precedes every table, and every rate cell shows its play count
- Thin cells are greyed with an explained threshold, never hidden
- Each section prints its data basis and the footnotes state the Hudl-only versus all-sources split
- Filenames derived from a team code cannot escape the run folder
</success_criteria>

<output>
Create `.planning/phases/01.4-coaching-products/01.4-11-SUMMARY.md` when done
</output>
