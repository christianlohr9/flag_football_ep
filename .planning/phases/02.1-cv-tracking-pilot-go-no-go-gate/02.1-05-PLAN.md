---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
plan: 05
type: execute
wave: 3
depends_on: [02.1-02]
files_modified:
  - src/flag_football_ep/cv/schema.py
  - src/flag_football_ep/cv/export.py
  - src/flag_football_ep/testing.py
  - tests/test_cv_track_schema.py
  - tests/test_cv_export.py
autonomous: true
requirements: [REQ-S2-02]

must_haves:
  truths:
    - "The XY tracking output has one declared, typed schema — the Phase-2.3-facing contract"
    - "Tracking rows key on session_id + clip_number now (D-02) and join to plays.parquet on game_id/play_id later without a migration"
    - "A dtype mismatch against the canonical game_id/play_id can never silently produce an empty join"
    - "Tracking Parquet is written atomically, like plays.parquet"
    - "A CSV export exists for eyeballing without re-introducing dtype risk into the canonical artifact (D-14)"
  artifacts:
    - path: "src/flag_football_ep/cv/schema.py"
      provides: "TRACKING_COLUMNS, TRACKING_SCHEMA, empty_tracking_frame, conform_tracking, write_tracking_parquet"
      min_lines: 90
    - path: "src/flag_football_ep/cv/export.py"
      provides: "Parquet -> CSV export"
      min_lines: 30
    - path: "tests/test_cv_track_schema.py"
      provides: "schema gate plus the dtype-mismatch join regression"
      min_lines: 90
  key_links:
    - from: "src/flag_football_ep/cv/schema.py"
      to: "data/reference/video_sync.csv join keys"
      via: "nullable game_id/play_id columns with canonical dtypes"
      pattern: "game_id"
    - from: "src/flag_football_ep/cv/schema.py"
      to: "atomic parquet write"
      via: "tmp sibling + os.replace"
      pattern: "os.replace"
---

<objective>
Define the artifact the entire rest of Strand 2 consumes: the typed XY tracking table, its atomic
writer, and its CSV export.

Purpose: D-14 makes Parquet canonical and CSV an export; D-02 keys the pilot on clip number because
the friendly has no Hudl-PBP, while requiring forward-compatibility with the
`video_sync.csv` -> `plays.parquet` join once charted games arrive. `docs/sync-convention.md` already
warns that a dtype mismatch on `game_id`/`play_id` silently yields an empty join — this plan makes
that a test, not a hope.
Output: `cv/schema.py`, `cv/export.py`, a synthetic-track factory in `testing.py`, and the schema
gate tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-CONTEXT.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-RESEARCH.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-PATTERNS.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-02-SUMMARY.md
</context>

<interfaces>
Tracking table (`data/processed/tracking/{session_id}_tracks.parquet` from plan 02.1-12 and
`{session_id}_xy.parquet` from plan 02.1-13) — one row per (clip, frame, track):

session_id        Utf8      not null   D-02 pseudo play key, part 1
clip_number       Int32     not null   D-02 pseudo play key, part 2
frame_index       Int32     not null
timestamp_s       Float64   not null
track_id          Int32     not null
class_name        Utf8      not null   "player" | "referee"
confidence        Float64   not null
bbox_x1..bbox_y2  Float64   not null   pixel space
foot_x_px         Float64   not null   bottom-centre of the box (ground contact point)
foot_y_px         Float64   not null
team_id           Int32     nullable   filled by plan 02.1-12 (SigLIP+UMAP+KMeans), null for referees
hover_position_id Utf8      nullable   filled by plan 02.1-12 from hover_positions.csv
x_yards           Float64   nullable   filled by plan 02.1-13 (homography)
y_yards           Float64   nullable   filled by plan 02.1-13
game_id           Utf8      nullable   canonical.CORE_COLUMNS dtype; null for the pilot friendly (D-02)
play_id           Int32     nullable   canonical.CORE_COLUMNS dtype; null for the pilot friendly
detector_run_id   Utf8      not null   MLflow run id of the detector that produced the boxes
tracked_at        Utf8      not null   ISO-8601 UTC
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: tracking schema, conform, atomic writer</name>
  <files>src/flag_football_ep/cv/schema.py, tests/test_cv_track_schema.py</files>
  <read_first>
    - src/flag_football_ep/cv/schema.py (the contract stub written by plan 02.1-02)
    - src/flag_football_ep/canonical.py (lines 1-130 — the canonical plays schema, especially `CORE_COLUMNS` declares `game_id: pl.Utf8` and `play_id: pl.Int32`; note the `CANONICAL_COLUMNS` tuple + schema-dict convention)
    - src/flag_football_ep/pipeline.py (lines 108-130 — `_atomic_write_parquet`, to copy verbatim)
    - tests/test_capture_artifacts.py (lines 28-70 and 144-177 — `SYNC_COLUMNS`/`SYNC_SCHEMA` and `test_video_sync_schema_joins_plays_shaped_frame`, the regression shape to mirror)
    - docs/sync-convention.md (the dtype-safe join discipline this schema must satisfy)
  </read_first>
  <action>
Fill `TRACKING_COLUMNS` (tuple, declared order) and `TRACKING_SCHEMA` (dict of polars dtypes) with
exactly the columns in this plan's `<interfaces>` block. `game_id` and `play_id` must use the SAME
declared polars dtypes as `canonical.py` uses for those columns — read them from `canonical.py` and
mirror them; do not guess and do not widen them to Utf8 "to be safe".

Implement `empty_tracking_frame()` returning a zero-row `pl.DataFrame` with exactly
`TRACKING_SCHEMA`. Implement `conform_tracking(df)`: reorder to `TRACKING_COLUMNS`, cast to
`TRACKING_SCHEMA`, raise a named exception listing the missing column names when a required column is
absent, and raise when a not-null column contains nulls (`class_name`, `session_id`, `clip_number`,
`frame_index`, `track_id`, `detector_run_id`). Nullable columns are filled with typed nulls when
absent, which is what lets plan 02.1-12 write pixel-only rows and plan 02.1-13 fill the field
coordinates later without a schema change.

Implement `write_tracking_parquet(df, path)`: call `conform_tracking` first, then write with the
`.tmp` sibling + `os.replace` helper copied verbatim from `pipeline._atomic_write_parquet` (including
the `finally` cleanup), and return the path.

Also validate the class vocabulary: `class_name` values outside `("player", "referee")` raise a named
exception listing the offending values. The pilot detects no ball (project constraint C-12) — an
unexpected third class means an upstream mistake, not data to silently keep.

tests/test_cv_track_schema.py, mirroring `tests/test_capture_artifacts.py`'s style:
- `TRACKING_COLUMNS` and `TRACKING_SCHEMA` have identical key sets and the declared order matches
- `empty_tracking_frame()` has exactly `TRACKING_SCHEMA` dtypes and zero rows
- `conform_tracking` fills absent nullable columns with typed nulls and preserves values
- missing required column -> named exception listing that column
- null in a not-null column -> named exception
- `class_name = "ball"` -> named exception naming the value
- **dtype join regression:** a tracking frame with `game_id`/`play_id` populated inner-joins a
  `video_sync.csv`-shaped frame and a `plays.parquet`-shaped frame (built with
  `flag_football_ep.testing`'s canonical factory) on `["game_id", "play_id"]` and returns the
  expected non-zero height; the assertion message names `docs/sync-convention.md`
- the pilot case: rows with null `game_id`/`play_id` still write and re-read without dtype drift
- `write_tracking_parquet` leaves no `.tmp` file behind and leaves the previous file intact when
  `conform_tracking` raises
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_track_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cv_track_schema.py -q` exits 0 with at least 10 tests
    - `uv run python -c "from flag_football_ep.cv.schema import TRACKING_COLUMNS, TRACKING_SCHEMA; from flag_football_ep import canonical; assert set(TRACKING_COLUMNS)==set(TRACKING_SCHEMA); assert TRACKING_SCHEMA['game_id']==canonical.CORE_COLUMNS['game_id']; assert TRACKING_SCHEMA['play_id']==canonical.CORE_COLUMNS['play_id']; print('dtypes aligned')"` prints `dtypes aligned`
    - `uv run python -c "from flag_football_ep.cv.schema import empty_tracking_frame; print(empty_tracking_frame().height, len(empty_tracking_frame().columns))"` prints `0` and the full column count
    - `grep -c 'os.replace' src/flag_football_ep/cv/schema.py` >= 1
    - `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>The XY tracking contract exists as a typed, tested schema with an atomic writer; a dtype drift against the canonical join keys fails a named test.</done>
</task>

<task type="auto">
  <name>Task 2: synthetic track factory for the CV test suite</name>
  <files>src/flag_football_ep/testing.py, tests/test_cv_track_schema.py</files>
  <read_first>
    - src/flag_football_ep/testing.py (full file — the `canonical_plays` factory pattern, its docstring style and its deterministic-value conventions)
    - src/flag_football_ep/cv/schema.py (Task 1 — `TRACKING_COLUMNS`, `TRACKING_SCHEMA`, `conform_tracking`)
  </read_first>
  <action>
Add a `synthetic_tracks(...)` factory to `src/flag_football_ep/testing.py` following the existing
`canonical_plays` factory shape: deterministic values from the arguments (no randomness without an
explicit seed), a docstring stating exactly what invariants the produced frame satisfies, and a
return value that already passes `cv.schema.conform_tracking`.

Signature: `synthetic_tracks(*, n_clips=2, n_frames=10, n_tracks=4, session_id="test-session",
with_teams=False, with_field_coords=False, with_pbp_keys=False, detector_run_id="0"*32,
seed=20260516)`. Tracks move linearly across the frame so downstream continuity and homography tests
have monotone, checkable geometry. `with_teams` fills `team_id` for `player` rows and leaves it null
for the one `referee` track per clip. `with_field_coords` fills `x_yards`/`y_yards` inside the
configured field bounds. `with_pbp_keys` fills `game_id`/`play_id` with canonical-dtype values so the
join regression test can use the same factory.

Import discipline: `testing.py` is a core module — it may import `flag_football_ep.cv.schema`
(polars only) but must not pull any `cv` extras dependency. Verify with the existing contract guard.

Extend tests/test_cv_track_schema.py: the factory's default output passes `conform_tracking`
unchanged; each flag fills exactly the columns it claims and leaves the others null; two calls with
the same arguments produce identical frames.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_track_schema.py tests/test_cv_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from flag_football_ep.testing import synthetic_tracks; from flag_football_ep.cv.schema import conform_tracking; df=synthetic_tracks(); print(conform_tracking(df).height)"` prints a positive integer
    - `uv run python -c "from flag_football_ep.testing import synthetic_tracks; a=synthetic_tracks(); b=synthetic_tracks(); assert a.equals(b)"` exits 0
    - `uv run pytest tests/test_cv_contracts.py -q` exits 0 (importing `testing.py` still pulls no CV extra)
  </acceptance_criteria>
  <done>Every later CV plan can build a valid, deterministic tracking frame in one call instead of hand-rolling fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: CSV export of the tracking table</name>
  <files>src/flag_football_ep/cv/export.py, tests/test_cv_export.py</files>
  <read_first>
    - src/flag_football_ep/cv/export.py (the contract stub written by plan 02.1-02)
    - src/flag_football_ep/cv/schema.py (Task 1 — `TRACKING_COLUMNS`, `conform_tracking`)
    - src/flag_football_ep/cv/commands.py (the `ffep cv export` option surface: `--tracks`, `--out`)
    - src/flag_football_ep/testing.py (Task 2 — `synthetic_tracks`)
  </read_first>
  <action>
Implement `export_tracking_csv(parquet_path, csv_path)`: read the Parquet, run `conform_tracking`,
write a comma-dialect CSV with the columns in `TRACKING_COLUMNS` order, floats formatted to 4 decimal
places (pixel and yard precision beyond that is noise and makes the file unreadable), and return the
CSV path. Raise a named exception when the input Parquet is missing, naming the path.

Two properties this must hold, both from D-14: the Parquet stays canonical (the CSV is never read
back into the pipeline — say so in the module docstring), and the CSV must not become a second join
surface, so it is written under the tracking directory next to its Parquet with the same stem plus
`.csv`, when `--out` is not given.

tests/test_cv_export.py: round-trip a `synthetic_tracks(with_teams=True, with_field_coords=True)`
frame through `write_tracking_parquet` and `export_tracking_csv`; the CSV header equals
`",".join(TRACKING_COLUMNS)`; the row count matches; a null `team_id` renders as an empty field (not
the string `null`); floats carry at most 4 decimals; a missing input Parquet raises the named
exception naming the path.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cv_export.py -q` exits 0 with at least 5 tests
    - `uv run --extra cv ffep cv export --help` exits 0 showing `--tracks` and `--out`
    - In the round-trip test the CSV first line equals `",".join(TRACKING_COLUMNS)`
    - `grep -c 'read_csv' src/flag_football_ep/cv/export.py` == 0 (export is one-way; the CSV is never an input)
  </acceptance_criteria>
  <done>`ffep cv export` produces a readable CSV of the canonical tracking Parquet without turning the CSV into a pipeline input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracking Parquet -> Phase 2.3 consumers | schema drift here silently breaks every downstream metric |
| tracking output -> git | tracking rows describe identifiable player movement (DSGVO) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-13 | Tampering | dtype drift on `game_id`/`play_id` producing a silently empty join | mitigate | Task 1 mirrors `canonical.py`'s declared dtypes and adds the inner-join regression test against both a `video_sync`-shaped and a `plays`-shaped frame; assertion message names `docs/sync-convention.md` |
| T-2.1-10 | Tampering | half-written tracking Parquet after a mid-run failure | mitigate | Task 1 writes via `.tmp` sibling + `os.replace` with `finally` cleanup, copied from `pipeline._atomic_write_parquet`; test asserts no stray `.tmp` and an intact previous file |
| T-2.1-01 | Information disclosure | tracking Parquet/CSV under `data/processed/tracking/` | mitigate | Covered by the pre-existing `data/processed/*` gitignore block (plan 02.1-01 deliberately added no redundant rule); no player names are ever written — `track_id` and `team_id` are opaque integers |
| T-2.1-14 | Tampering | CSV export re-imported as a pipeline input, bypassing the typed schema | mitigate | Task 3 makes the export one-way by contract and asserts `read_csv` appears nowhere in `cv/export.py` |
</threat_model>

<verification>
- `uv run pytest tests/test_cv_track_schema.py tests/test_cv_export.py -q` — green
- `uv run pytest -q` — full suite green
- `uv run --extra cv ffep cv export --help` — surface wired
</verification>

<success_criteria>
- One declared, typed tracking schema keyed on `session_id` + `clip_number` with nullable canonical-dtype `game_id`/`play_id`
- Atomic Parquet writes; no `.tmp` residue; previous file intact on failure
- A dtype mismatch against the canonical join keys fails a named test citing `docs/sync-convention.md`
- `synthetic_tracks` gives every later CV plan a deterministic fixture
- `ffep cv export` writes a readable, one-way CSV
</success_criteria>

<output>
Create `.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-05-SUMMARY.md` when done
</output>
