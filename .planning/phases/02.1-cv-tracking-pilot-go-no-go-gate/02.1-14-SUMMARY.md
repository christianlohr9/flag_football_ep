---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
plan: 14
subsystem: cv
tags: [overlay, continuity, human-review, oc-sort, bot-sort, occlusion]

# Dependency graph
requires:
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 12
    provides: session tracks parquet (superseded mid-plan by the v2 BoT-SORT re-run)
provides:
  - cv/overlay.py -- per-clip track-overlay rendering (team colours, track ids, ground-contact dot, legend)
  - cv/continuity.py -- automatic continuity statistics + summarise_review (refuses partial-review headline rates)
  - data/reference/continuity_review.csv -- 61 auto-column rows, human verdicts for clips 1-20 (v2 pipeline)
  - C-09 continuity finding -- criterion measurably NOT met (upper bound 47/61 = 77% vs >= 90% target)
affects: [02.1-16, 02.1-17]

# Tech tracking
tech-stack:
  added: []
  patterns: ["human review stops when the criterion outcome becomes mathematically decided (conservative upper bound), never by shrinking the denominator"]

key-files:
  created: [src/flag_football_ep/cv/overlay.py (implementation), src/flag_football_ep/cv/continuity.py (implementation), tests/test_cv_overlay.py, tests/test_cv_continuity.py, data/reference/continuity_review.csv]
  modified: [src/flag_football_ep/cv/commands.py]

key-decisions:
  - "Overlay output goes to gitignored data/labels/<session>/overlays/ (PII), not data/processed"
  - "The v1 review (5 clips, all fail) exposed two systematic defects (team misassignment, camera-pan ID cascades) and triggered the mid-phase gap-fix iteration (BoT-SORT + torso crops) instead of completing a review of a broken artifact"
  - "The v2 review stopped at 20/61 by explicit user+orchestrator decision: 6 passes in 20 makes the >= 90% target unreachable even if all remaining clips pass (best case 77%); every unreviewed clip counted as pass -- conservative bound, no manufactured rate"

patterns-established: []

requirements-completed: []

# Metrics
duration: ~2 days wall-clock across two review rounds and the gap-fix iteration
completed: 2026-08-30
---

# Phase 02.1 Plan 14: Track Overlays and Human Continuity Review Summary

**Overlay rendering and continuity statistics implemented and run twice (v1 OC-SORT, v2 BoT-SORT after the gap-fix iteration); the human review of the v2 overlays stopped at 20/61 clips once the C-09 continuity criterion (>= 90% of plays without ID switch) became mathematically unreachable — measured outcome: criterion NOT met, dominant failure mode identity switches at player occlusions.**

## Review Results (v2 pipeline)

- Reviewed: 20/61 clips. Pass: 6 (clips 1, 2, 4, 6, 11, 13). Fail: 14.
- Conservative upper bound on the full-session pass rate: (6 + 41 unreviewed-as-pass)/61 = **77% < 90% target** — the criterion outcome is decided without reviewing further.
- Dominant failure mode: **identity switches at player occlusions** (crossings, coverage, flag pulls — inherent to flag football at this oblique camera angle). Secondary: residual team misassignments (clips 12, 16, 17 — the torso-crop fix repaired the 8 known v1 corrections but did not eliminate the class of error), one in-clip camera cut (clip 14, capture artifact).
- v1-vs-v2 context: the gap-fix iteration halved fragmentation (auto-flagged clips 26 -> 4; player tracks 1874 -> 1453) and fixed the v1 systematic defects; the residual occlusion weakness is an architecture limit of motion-only association.

## Remediation paths recorded for the gate document

1. **Appearance-based re-identification** (ReID embedding in association, or post-hoc track merging) — the identified next technical iteration.
2. **Steeper/more overhead drone angle** — occlusion frequency is a direct function of the oblique capture angle; routes to Phase 2.0 capture setup per D-06 (never to more training labels).

## Task Commits

1. **Overlay rendering** - `ff027bc` (executor; cherry-picked into the v2 branch as part of `2d353e1`)
2. **Continuity statistics + review contract** - `47acf56` (executor; same cherry-pick)
3. **v1 61-clip auto-stats + render** - `8bc95d9` (superseded by the v2 re-run; branch deleted after cherry-pick)
4. **v2 re-run record** - `547e009` (v2 runner)
5. **Human verdicts clips 1-20 + stop rationale** - `755b156` (orchestrator)

## Deviations

- **Mid-plan pipeline rework:** the v1 review was aborted after 5 all-fail clips exposed systematic defects; the plan resumed against the v2 artifacts. Both review rounds are preserved (v1 findings in the gap-fix experiment record, v2 verdicts in continuity_review.csv).
- **Review stopped at 20/61:** documented above; `summarise_review` correctly reports `pass_rate: None` for the partial review — the gate document must use the upper-bound argument, not a partial-review rate (T-2.1-31 honoured).
- **Executor close-out by orchestrator:** the plan 14 executor's worktree was superseded by the v2 re-run; its unique commits were cherry-picked, its branch deleted.

## Self-Check: PASSED

- 61/61 v2 overlays rendered and human-spot-checked; tests for overlay/continuity green in the full suite (post-merge gate exit 0)
- continuity_review.csv: 61 rows, 20 verdicts, machine-readable stop rationale in the commit message
