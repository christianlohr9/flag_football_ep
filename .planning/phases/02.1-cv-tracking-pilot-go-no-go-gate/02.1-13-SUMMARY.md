---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
plan: 13
subsystem: cv
tags: [homography, calibration, field-coordinates, foot-point, yards]

# Dependency graph
requires:
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 04
    provides: ViewTransformer, load_calibration, pick_points, reprojection_error_yards
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 12
    provides: 61-clip tracks parquet with foot points and hover_position_id
provides:
  - foot_point/add_field_coordinates in cv/coordinates.py -- bottom-center foot point per box, per-hover-position homography projection into field yards
  - data/reference/homography_calibration.csv -- real user-picked calibration (hp-01 4 fit + 1 held-out, hp-02 4 fit)
  - x_yards/y_yards columns on the session tracks parquet (341,461 rows, 100% coverage)
  - docs/homography-calibration.md -- completed calibration record with reprojection numbers and deviations
affects: [02.1-15, 02.1-16, 02.1-17]

# Tech tracking
tech-stack:
  added: []
  patterns: ["landmark vocabulary contains only physically painted lines -- never invite guessing invisible geometry", "calibration clicks captured via a local HTML picker page (data/labels/calibration/picker.html) instead of the 5s-bounded cv2 window"]

key-files:
  created: [src/flag_football_ep/cv/coordinates.py (implementation), tests/test_cv_coordinates.py, docs/homography-calibration.md, data/reference/homography_calibration.csv (real rows)]
  modified: [src/flag_football_ep/cv/homography.py (landmark vocabulary), tests/test_cv_homography.py]

key-decisions:
  - "Landmark vocabulary corrected to field reality (14 names): the pilot field paints only goallines, endzone back lines, midfield and the 5-yard no-run-zone lines; yardline_10/20/30/40 dropped, yardline_5/45 added"
  - "Held-out budget deviation accepted: hp-01 has 1 held-out (0.27 yd error), hp-02 has 0 (exactly determined) -- the field offers no more clickable intersections; hp-02 quality is measured end-to-end by plan 02.1-15 only"
  - "25.03% of track rows project out of bounds, dominated by deliberately-tracked sideline persons (65% of oob rows are <= 3 yd outside); spatial on-field filtering stays the downstream mechanism"

patterns-established: []

requirements-completed: []

# Metrics
duration: ~4h wall-clock across executor automation, user picking session and orchestrator close-out
completed: 2026-08-29
---

# Phase 02.1 Plan 13: Field Coordinates via User-Calibrated Homographies Summary

**Both hover-position homographies calibrated from real user clicks (hp-01: 0.27 yd held-out reprojection error; hp-02: exactly determined by the 4 visible landmarks), foot points projected into field yards for all 341,461 track rows, and the landmark vocabulary corrected to the lines actually painted on the pilot field.**

## Accomplishments
- `foot_point`/`add_field_coordinates` in `cv/coordinates.py` (7 tests): bottom-center foot point per bbox, per-hover-position `ViewTransformer` projection, yards output on the D-13 axis convention
- Landmark vocabulary corrected (14 names) after the user reported the field carries no 10/20/30/40 yardlines — `yardline_5_*`/`yardline_45_*` (no-run-zone lines) replace them
- Real calibration picked by the user via a local HTML picker page (built after the cv2 window's 5-second T-2.1-12 bound proved unusable for humans): hp-01 5 points, hp-02 4 points — all physically identifiable intersections in the frames
- `ffep cv coords` run over the real session parquet: `x_yards`/`y_yards` on 100% of rows; out-of-bounds share 25.03% (85,484 rows; 669 of 2,031 tracks are majority-oob = the deliberately-boxed sideline population)
- `docs/homography-calibration.md` completed: per-position point lists, reprojection numbers, oob interpretation, all deviations recorded

## Task Commits

1. **Task 1: reference frames + doc draft** - `22664a7` (docs, executor)
2. **Task 3 code: foot_point/add_field_coordinates** - `a0ae7df` (feat, executor)
3. **Vocabulary fix** - `b5cb36d` (fix, orchestrator)
4. **Task 2+3 close-out: user calibration, coords run, doc completion** - `0805ff0` (feat, orchestrator)

## Deviations

- **Landmark vocabulary change (field reality):** see key-decisions; committed as `b5cb36d`.
- **Held-out budget below plan (hp-01: 1 of 2, hp-02: 0 of 2):** the frames physically contain only 5 resp. 4 identifiable landmark intersections. hp-02's homography has no independent reprojection check; plan 02.1-15's ground-truth measurement is the (only) honest quality gate for it.
- **Interactive cv2 picking replaced by a local HTML picker** (`data/labels/calibration/picker.html`, gitignored): the production picker's deliberate 5-second wait bound (T-2.1-12) makes human picking impractical; the CSV was written from the page's output and validated via `load_calibration`/`transformer_for` before commit.
- **Executor close-out by orchestrator:** the executor's transcript was lost to an overnight session restart after its checkpoint return; the orchestrator merged its branch and completed Task 2/3 with the user, per the workflow's manual close-out path.
- **Obsolete test updated:** `test_load_calibration_header_only_checked_in_file_loads_empty` asserted the checked-in CSV stays header-only; replaced by a both-positions-calibrated assertion.

## Self-Check: PASSED

- `load_calibration` + `transformer_for` succeed for both positions; hp-01 held-out error 0.27 yd
- Tracks parquet carries `x_yards`/`y_yards` on all 341,461 rows
- `tests/test_cv_coordinates.py` (7), `tests/test_cv_homography.py` (19), `tests/test_cv_contracts.py` (73) — all green
