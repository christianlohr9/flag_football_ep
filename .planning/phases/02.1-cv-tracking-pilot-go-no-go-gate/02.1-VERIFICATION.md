---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
verified: 2026-08-30T07:38:25Z
status: passed
score: 5/5 must-haves verified (1 confirmed by human on 2026-08-31)
overrides_applied: 0
human_approved: 2026-08-31 -- showcase reel sent to the HC by the user (recorded in docs/pilot-gate-decision.md ## Demo and 02.1-HUMAN-UAT.md); analyst demo stays DEFERRED-ANALYST per D-12
human_verification:
  - test: "Confirm the showcase reel has actually been sent to and viewed by the HC, and check on the DEFERRED-ANALYST follow-up trigger"
    expected: "HC has received data/labels/2026-05-16_FRIENDLY-GER-vs-PANAMA-ROJO-DRONE/showcase/showcase.mp4 (or an explicit decision to send it); analyst demo status re-checked once the videoanalyst is available again (STATE.md Blockers/Concerns)"
    why_human: "This is an external async action (sending an email, scheduling a demo) that lives outside repo state. docs/pilot-gate-decision.md itself records it as 'vorbereitet, Versand ausstehend' (prepared, not yet sent) and 'DEFERRED-ANALYST' -- the document is honest about this, but ROADMAP Success Criterion 3 literally reads 'has been demoed to HC and analyst', which is not yet true. D-12 (decided at phase-planning time, before execution, in 02.1-CONTEXT.md) explicitly reframes the demo as evidence attached to the decision, not a gate condition, and the analyst's unavailability is a pre-existing, independently documented project blocker (STATE.md, dated 2026-08-17), not an invented excuse for this phase."
---

# Phase 02.1: CV Tracking Pilot (Go/No-Go Gate) — Verification Report

**Phase Goal:** Run the CV tracking pilot on the 61-clip drone session end to end and close it with an
explicit, evidence-backed go/no-go gate decision against the three C-09 criteria (REQ-S2-02).
**Verified:** 2026-08-30T07:38:25Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, Phase 2.1)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A fine-tuned RF-DETR-Small detects `player`/`referee`, bootstrapped from Grounding DINO zero-shot pre-labels and ~300-500 CVAT+SAM2-corrected frames | ✓ VERIFIED | `src/flag_football_ep/cv/detect.py::train_detector` (RF-DETR fine-tune, MLflow logging); `src/flag_football_ep/cv/prelabel.py` (Grounding DINO); `docs/cv-setup.md` `### Detector-Training` records machine, settings, wall-clock, per-class metrics, MLflow run id; `cv_detector_model@champion` resolves via `src/flag_football_ep/cv/registry.py`. |
| 2 | OC-SORT/tracking + SigLIP+UMAP+KMeans team clustering produces per-player tracks with team assignment on the pilot game | ✓ VERIFIED (with documented tracker substitution) | `src/flag_football_ep/cv/track.py` runs over all 61 clips with per-clip containment (D-09); `src/flag_football_ep/cv/teams.py::assign_teams` (SigLIP+UMAP+KMeans), now called through a real `ffep cv teams` verb (commit `b36b6cf`, WR-09 fix). Tracker was swapped OC-SORT -> `trackers.BoTSORTTracker` mid-phase as a gap-fix iteration after the human continuity review (`docs/cv-setup.md` `### v2`); confirmed still `trackers` (Apache-2.0) not `boxmot` (`grep` shows no `boxmot` anywhere in `pyproject.toml`/`uv.lock`/`track.py`), so C-06 (AGPL exclusion) still holds. This is an honestly documented mid-phase deviation, not a stub. |
| 3 | Manual 4-8-point homography per hover position yields an XY CSV in field coordinates, and a top-down radar clip has been demoed to HC and analyst | ⚠ PARTIAL — see human_needed item | Homography/XY CSV half fully verified: `data/reference/homography_calibration.csv` (hp-01, hp-02 committed), `docs/homography-calibration.md` (149 lines, reprojection error 0.27 yd for hp-01, hp-02 exact-fit), `src/flag_football_ep/cv/coordinates.py`, `data/processed/tracking/*_xy.parquet` (Phase 2.3 input). Radar/showcase reel rendered and frame-verified (`docs/pilot-accuracy.md` `## Showcase-Reel`, reproducible non-cherry-picked clip-selection rule cross-checked against `continuity_review.csv`: 6 pass-verdict clips, rule selects clips 11/2/6/13/4). **"Demoed to HC and analyst" is not yet true**: `docs/pilot-gate-decision.md` `## Demo` records HC send as "vorbereitet, Versand ausstehend" (prepared, not sent) and analyst demo as `DEFERRED-ANALYST` (analyst unavailable, a pre-existing project blocker per `STATE.md`). |
| 4 | The go/no-go decision is recorded against the C-09 gate criteria; a clear miss routes back to Phase 2.0, not to more labeling (D-06) | ✓ VERIFIED | `docs/pilot-gate-decision.md` (232 lines): all three C-09 criteria stated with target vs. measured; verdict **TEILWEISE**, dated, attributed to the user; consequences section written before the verdict; explicit "Angeforderte Capture-Setup-Änderung" routes criterion-1 miss to Phase 2.0 (steeper hover angle) per D-06, explicitly not more training labels; `.planning/REQUIREMENTS.md` REQ-S2-02 marked `Complete (Gate: teilweise)`. |

**Score:** 3.5/4 roadmap criteria fully verified; criterion 3's demo clause is a legitimate, pre-planned
(D-12), owner-assigned pending human action rather than a code gap — routed to human verification below.

### C-09 Gate Criteria (measured, cross-checked against committed data — not the SUMMARY narrative)

| Criterion | Target | Measured (per `pilot-gate-decision.md`) | Independently cross-checked | Verdict |
|---|---|---|---|---|
| 1. Track continuity | ≥ 90% | 6/20 reviewed clips pass; upper bound over all 61 = 47/61 = 77.0% | `awk -F, 'NR>1{print $6}' data/reference/continuity_review.csv \| sort \| uniq -c` -> `41 (blank/unreviewed), 14 fail, 6 pass` — matches the document's 6/14/41 split exactly | **NEIN** (fails even at best-case upper bound) |
| 2. Position error | ~≤ 1 m (1.094 yd) | Median 0.171 yd (0.156 m), p90 0.422 yd, n=250, match-rate 99.6% | `docs/pilot-accuracy.md` line 191-195 shows the identical v2 numbers (0.171/0.422/99.6%/249/250) | **JA** |
| 3. Inference/game | < 1 h | 47.83 min extrapolated | `docs/cv-setup.md` lines 671-680 print the *identical* formula (decode/detect/track/write factors, 0.956594x realtime, 2869.78s) verbatim in both documents | **JA** |

The phase's own framing — a TEILWEISE verdict where criteria 2 and 3 pass and criterion 1 measurably
fails at its own most-favorable upper bound — is treated as a **valid phase outcome**, per the explicit
task context: the phase goal is an evidence-backed decision, not a passing pilot. The review-stop at
20/61 is mathematically justified in the document itself (6+41=47/61 ≈ 77% even if every unreviewed clip
passed), and `continuity.summarise_review` is confirmed (via the WR-02 fix, see below) to refuse a
manipulated `pass_rate` on a partial review.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docs/pilot-gate-decision.md` | Milestone-1 closing artifact: criteria, numbers, verdict, consequences | ✓ VERIFIED | 232 lines; all sections present; verdict TEILWEISE dated 2026-08-30 |
| `docs/pilot-accuracy.md` | Position-error distribution + decomposition | ✓ VERIFIED | 392 lines; v1/v2 numbers, zone breakdown, scale check, showcase-reel selection record |
| `docs/cv-setup.md` | Environment, CVAT, training, inference-throughput record | ✓ VERIFIED | 868 lines; `### Detector-Training`, `## Inferenz-Durchsatz`, `## Tracking-Lauf` v1/v2 sections present |
| `docs/homography-calibration.md` | Per-hover-position calibration + reprojection error | ✓ VERIFIED | 149 lines; hp-01 (0.27 yd held-out), hp-02 (exact-fit) documented |
| `docs/pilot-sighting.md` | Hover-position grouping and locked inference settings | ✓ VERIFIED | 111 lines |
| `data/reference/continuity_review.csv` | 61 clips, auto stats + human verdict | ✓ VERIFIED | 62 lines (61 data rows); verdict column matches gate doc's 6/14/41 split exactly |
| `data/reference/gt_positions.csv` | Hand-labelled GT foot positions | ✓ VERIFIED | 251 lines (250 GT points), matches n=250 cited in accuracy doc |
| `data/reference/homography_calibration.csv` | Per-hover-position point correspondences | ✓ VERIFIED | Contains `hp-01`, `hp-02` rows |
| `tests/test_cv_gate_artifacts.py` | Structural gate on the decision document | ✓ VERIFIED | 9/9 tests pass (`uv run --extra cv pytest tests/test_cv_gate_artifacts.py -q`) |
| `.planning/REQUIREMENTS.md` REQ-S2-02 row | Traceability closure | ✓ VERIFIED | `- [x] REQ-S2-02 ... Gate decided TEILWEISE in Phase 2.1`; table row `Complete (Gate: teilweise)` |
| `ffep cv teams` command | Fills `team_id` through the documented CLI (post-review fix) | ✓ VERIFIED | `src/flag_football_ep/cv/commands.py:339 def teams(...)`, commit `b36b6cf` |

### Post-Review Fix Verification (commits `95483f1`..`b36b6cf`)

The 02.1-REVIEW.md flagged 2 Critical + 12 Warning findings (`status: issues`). The phase claims 2
Critical + 5 Warning were fixed post-review. Verified each fix commit against its diff, not just the
commit message:

| ID | Finding | Fix commit | Verified |
|----|---------|-----------|----------|
| CR-01 | `ffep cv benchmark` read a `stage`/`seconds`/`frames` parquet that no writer ever produced | `95483f1` | ✓ `track_session` now writes `<session>_stage_timings.json` (atomic tmp+replace); `benchmark` reads `--timings` from that artifact; 2 new tests (`test_cv_benchmark.py`, `test_cv_track.py`) |
| CR-02 | `footage_seconds = sum(frames)/30.0` inflated the denominator ~3-4x, hardcoded 30 fps, understated runtime in the gate-favorable direction | `d88d9c4` | ✓ `footage_seconds` now derived from inventory-declared clip durations (summed, with a noticed fps fallback), persisted in the timings JSON; CLI raises `BadParameter` if the field is absent/non-positive |
| WR-01 | Continuity denominator used last-tracked-frame instead of the clip's real frame count (overstates coverage exactly when tracking dies) | `bc22da0` | ✓ Denominator now `round(duration_seconds * fps)` from inventory, floored by last-tracked-frame, notice on fallback |
| WR-02 | `_VALID_VERDICTS` defined but never enforced — typo'd verdicts silently deflated `pass_rate` | `5c28a14` | ✓ `summarise_review`/`_load_existing_human_columns` now raise `ValueError` naming CSV + offending clip numbers on any out-of-set verdict |
| WR-06 | Showcase reel (PII, rendered footage) defaulted under `data/processed/`, contradicting the project's own PII-placement rule | `cf36e34` | ✓ Default now `data/labels/<session>/showcase/showcase.mp4` |
| WR-08 | CVAT errors collapsed DNS/refused-connection/timeout/401/SDK-bug into one "HTTP unknown" message | `82fdd87` | ✓ Exception type name now included in the sanitized message |
| WR-09 | `assign_teams`/`extract_track_crops` had no CLI verb; `team_id` unreachable through the documented surface | `b36b6cf` | ✓ New `ffep cv teams` command, notices echoed |

All 7 fix commits carry real diffs with matching new tests, not message-only changes. **Not** fixed
(and not claimed to be): WR-03, WR-04, WR-05, WR-07, WR-10, WR-11, WR-12, and all 10 Info items — these
are latent robustness issues for future sessions (explicitly scoped as such in the review, e.g. WR-03
"fine for the 61-clip pilot, wrong for any smaller session"), not blockers to this pilot's already-
recorded gate evidence. Listed under Anti-Patterns below as non-blocking.

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `docs/pilot-gate-decision.md` | `data/reference/continuity_review.csv` | continuity pass rate over all 61 clips | ✓ WIRED | Doc cites the exact 6/14/41 split; independently reproduced from the CSV |
| `docs/pilot-gate-decision.md` | `docs/pilot-accuracy.md` | measured position error and decomposition | ✓ WIRED | Median/p90 figures identical across both documents |
| `docs/cv-setup.md` | `cv.benchmark.extrapolate_game_runtime` | formula reproduced verbatim | ✓ WIRED | Formula text byte-identical between `docs/cv-setup.md` and `docs/pilot-gate-decision.md` |
| `src/flag_football_ep/cv/track.py` | `trackers.BoTSORTTracker` | per-frame update loop | ✓ WIRED | `from trackers import BoTSORTTracker`; no `boxmot` reference anywhere in `pyproject.toml`/`uv.lock`/source |
| `src/flag_football_ep/cv/commands.py::benchmark` | `<session>_stage_timings.json` | `--timings` option | ✓ WIRED (post-fix) | Verified via commit `95483f1` diff |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full `cv`-tagged suite is green (minus the two documented, pre-existing worktree/data deviations from `deferred-items.md`) | `uv run --extra cv pytest tests/ -q -k cv --deselect tests/test_cv_homography.py::test_pick_points_ref_jpeg_is_gitignored --deselect tests/test_cv_continuity.py::test_continuity_review_csv_uses_comma_dialect` | exit code 0, no FAILED/ERROR lines (88 `cv`-tagged tests collected) | ✓ PASS |
| Gate-artifact structural gate passes | `uv run --extra cv pytest tests/test_cv_gate_artifacts.py -q` | 9/9 passed | ✓ PASS |
| `teams` CLI verb exists on the documented surface (WR-09 fix) | `grep -n "def teams(" src/flag_football_ep/cv/commands.py` | `339:def teams(` | ✓ PASS |
| No `boxmot` anywhere (C-06) | `grep -ri boxmot pyproject.toml uv.lock src/` | no matches | ✓ PASS |
| `continuity_review.csv` verdict split matches gate doc's cited numbers | `awk -F, 'NR>1{print $6}' ... \| sort \| uniq -c` | `41 blank, 14 fail, 6 pass` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REQ-S2-02 | 02.1-01 through 02.1-17 (all 17) | Pilot on one scrimmage: zero-shot baseline -> CVAT+SAM2 correction -> RF-DETR-Small fine-tune -> tracking + team split -> manual homography -> XY CSV + radar; explicit go/no-go against gate criteria | ✓ SATISFIED (Gate: teilweise) | `.planning/REQUIREMENTS.md` line 41 (`- [x] REQ-S2-02 ... Gate decided TEILWEISE`), line 83 (table row `Complete (Gate: teilweise)`); `docs/pilot-gate-decision.md` full closing artifact |

No orphaned requirements — REQ-S2-02 is the phase's only requirement ID and every plan declares it.

### Anti-Patterns Found (non-blocking, carried from 02.1-REVIEW.md, not fixed and not claimed to be)

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `cv/frames.py:69-70,398-403` | Val-split floor gives zero train clips for sessions ≤6 clips (WR-03) | Warning | Explicitly scoped "fine for the 61-clip pilot" — latent risk for future smaller sessions (Phase 2.2+) |
| `cv/frames.py:246-274` | Frame-index rounding collisions can silently overwrite extracted frames (WR-04) | Warning | Latent data-quality risk in future frame sampling runs |
| `cv/track.py:179-192` | `_probe_fps` silently falls back to 30.0 fps with no notice (WR-05) | Warning | `timestamp_s` accuracy risk on damaged containers; does not affect this pilot's already-recorded spatial-only position-error metric |
| `cv/homography.py:237,448`, `cv/accuracy.py:213-215` | cwd-dependent `load_config()` calls inside library functions (WR-07) | Warning | Fragile if invoked outside repo root; documented convention avoids this today |
| `cv/dataset.py:165-171` | `validate_coco` raises bare `TypeError` instead of `DatasetError` on malformed COCO input (WR-10) | Warning | Edge case on hand-edited/foreign COCO exports; pilot's own dataset already passed validation |
| `cv/radar.py:387-390,426-428` | `render_showcase_reel` locks frame size/fps from the first clip; mixed-resolution sessions can crash or silently drop frames (WR-11) | Warning | This pilot's reel already rendered successfully (single resolution); latent risk for future sessions |
| `cv/prelabel.py:207-229` | `_resolve_backend` catches only `ImportError`, not `OSError` from HF downloads (WR-12) | Warning | Latent risk for future zero-shot pre-labelling runs on an offline/HF-outage machine |

None of these affect the validity of the already-recorded C-09 numbers or the gate decision document.

### Human Verification Required

#### 1. Showcase reel delivery to HC, and analyst-demo follow-up

**Test:** Confirm with the user whether `data/labels/2026-05-16_FRIENDLY-GER-vs-PANAMA-ROJO-DRONE/showcase/showcase.mp4` has been sent to (and viewed by) the head coach, and check whether the `DEFERRED-ANALYST` follow-up trigger (videoanalyst availability, per `STATE.md` Blockers/Concerns) is still being tracked.
**Expected:** Either the reel has been sent/viewed, or there is an explicit, current acknowledgment that this remains an open async action owned by the user (consistent with D-12's framing of the demo as evidence attached to the decision, not a gate condition).
**Why human:** This is an external communication/scheduling action outside repo state — cannot be verified by grep or test execution. ROADMAP Success Criterion 3 literally reads "a top-down radar clip has been demoed to HC and analyst," which is not yet true as of this verification; the codebase and docs are honest about this ("vorbereitet, Versand ausstehend" / "DEFERRED-ANALYST"), so this is a transparency gap in delivery timing, not a code or documentation gap.

### Gaps Summary

No code-level gaps block the phase goal. The pipeline runs end to end on all 61 clips, all three C-09
criteria are measured with committed, cross-checked evidence, the go/no-go decision is recorded with a
dated human verdict and pre-committed consequences, and the two Critical + five Warning review findings
claimed as fixed are verified as real, tested code changes (not message-only commits). The one open item
is that ROADMAP Success Criterion 3's "demoed to HC and analyst" clause is not yet literally true — the
reel is rendered, frame-verified, and ready, but sending it to the HC is pending (owned by the user,
async, non-blocking per D-12) and the analyst demo is explicitly deferred against a pre-existing,
independently documented project blocker (analyst unavailable since 2026-08-17). This is routed to
human verification rather than marked as a failed gap because it is a pre-planned, honestly-documented,
non-code deviation — not an incomplete implementation.

---

_Verified: 2026-08-30T07:38:25Z_
_Verifier: Claude (gsd-verifier)_
