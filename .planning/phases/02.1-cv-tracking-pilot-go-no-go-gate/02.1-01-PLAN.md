---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - ffep.toml
  - .gitignore
  - data/labels/.gitkeep
  - src/flag_football_ep/config.py
  - tests/test_capture_artifacts.py
  - tests/test_cv_config.py
  - tests/test_cv_dependencies.py
  - docs/cv-setup.md
autonomous: true
requirements: [REQ-S2-02]

must_haves:
  truths:
    - "`uv sync` (no extras) still installs a lean core with no CV/video library"
    - "`uv sync --extra cv` installs the full CV stack and every top-level CV import succeeds on the M5 Max"
    - "`boxmot` appears nowhere in pyproject.toml or uv.lock (AGPL, C-06)"
    - "Every CV path and setting resolves through `flag_football_ep.config.Config`, never hard-coded"
  artifacts:
    - path: "pyproject.toml"
      provides: "`[project.optional-dependencies] cv` group"
      contains: "rfdetr"
    - path: "ffep.toml"
      provides: "`[cv]` settings table plus video/labels/tracking paths and CV reference files"
      contains: "[cv]"
    - path: "src/flag_football_ep/config.py"
      provides: "Paths.video/labels/tracking, ReferenceFiles CV entries, CvSettings dataclass"
      contains: "class CvSettings"
    - path: "docs/cv-setup.md"
      provides: "German environment record: install outcome, per-machine torch note, fallback decisions"
      min_lines: 30
  key_links:
    - from: "ffep.toml"
      to: "src/flag_football_ep/config.py"
      via: "_PATH_KEYS / _REFERENCE_KEYS / _CV_KEYS tuple-driven load"
      pattern: "_CV_KEYS"
    - from: ".gitignore"
      to: "data/labels/"
      via: "ignore-contents-not-directory block"
      pattern: "data/labels/\\*"
---

<objective>
Lay the dependency, config and repo-hygiene foundation the whole CV pilot stands on: an optional `cv`
extras group (D-07), a config surface for every CV path and knob (D-08), gitignore coverage for the
new PII-bearing label directory (D-15), and a recorded smoke test that the stack actually installs
and imports on the primary machine.

Purpose: every later plan in this phase assumes `cfg.paths.labels`, `cfg.cv.resolution` and a working
`uv sync --extra cv`. Nothing else can start until this holds.
Output: `cv` extras group, `[cv]` config table, `data/labels/` ignore block, narrowed Phase-2.0
dependency guard, `docs/cv-setup.md` environment record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-CONTEXT.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-RESEARCH.md
@.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan establishes; every later plan in phase 02.1 reads them from Config. -->

Config additions (src/flag_football_ep/config.py):
- `Paths` gains: `video: Path`, `labels: Path`, `tracking: Path`
- `ReferenceFiles` gains: `hover_positions: Path`, `homography_calibration: Path`, `gt_positions: Path`, `continuity_review: Path`
- new `@dataclass(frozen=True) class CvSettings` with fields:
  `pilot_session_id: str`, `detector_model: str`, `detector_experiment: str`,
  `resolution: int`, `sahi: bool`, `sahi_slice: int`, `sahi_overlap: float`,
  `train_epochs: int`, `train_batch_size: int`, `train_grad_accum: int`,
  `device: str`, `label_frame_target: int`,
  `cvat_host: str`, `cvat_username_env: str`, `cvat_password_env: str`,
  `field_length_yards: float`, `field_width_yards: float`, `endzone_yards: float`
- `Config` gains: `cv: CvSettings`

Field coordinate convention (used by every later plan, D-13):
- `x_yards`: 0.0 at the west goal line, `field_length_yards` (50.0) at the east goal line;
  negative into the west end zone, > 50 into the east end zone
- `y_yards`: 0.0 at the south sideline, `field_width_yards` (25.0) at the north sideline
- gate threshold "~<= 1 m" converts to <= 1.094 yards
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: cv extras group, data/labels ignore block, narrowed dependency guard</name>
  <files>pyproject.toml, .gitignore, data/labels/.gitkeep, tests/test_capture_artifacts.py, tests/test_cv_dependencies.py</files>
  <read_first>
    - pyproject.toml (current core `dependencies`, `[dependency-groups] dev`, hatch/pytest config)
    - .gitignore (the `data/raw/hudl/*` + `!data/raw/hudl/.gitkeep` and `data/video/*` blocks and their NOTE comments)
    - tests/test_capture_artifacts.py (lines 1-100 for module constants, lines 211-229 for `test_phase_adds_no_video_python_dependency`)
    - .planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-RESEARCH.md §Standard Stack, §Package Legitimacy Audit, §Code Examples "Optional dependency group"
  </read_first>
  <action>
Add a `[project.optional-dependencies]` table to pyproject.toml with a single group named `cv`
containing exactly: `rfdetr>=1.9.3`, `trackers>=2.6.0`, `supervision>=0.30.0`, `sahi>=0.12.6`,
`autodistill>=0.1.29`, `autodistill-grounding-dino`, `transformers>=5.15.1`, `umap-learn>=0.5.12`,
`opencv-python`, `torch>=2.13`, `cvat-sdk>=2.73.0`. Do NOT add `scikit-learn` (already a core
dependency at `>=1.5.1`). Do NOT add `boxmot` under any circumstance — it is AGPL-3.0 as of
2026-08-24 and violates project constraint C-06; `trackers` (roboflow, Apache-2.0) is the OC-SORT
implementation this phase uses. Resolve the concrete current `opencv-python` version with
`uv pip index versions opencv-python` and pin the resolved major.minor as `opencv-python>=X.Y`
(RESEARCH assumption A1 asks for exactly this). Do the same for `autodistill-grounding-dino`
(RESEARCH assumption A2) and pin the resolved version.

Add a `.gitignore` block for the new COCO label directory following the existing
`data/raw/hudl/*` / `!data/raw/hudl/.gitkeep` shape, including the same NOTE comment explaining
that git cannot re-include a file inside a wholesale-excluded directory. Rationale comment must
name the PII reason (labeled frames are extracted player images, same policy as `data/video/*`,
`docs/capture-legal.md`). Create `data/labels/.gitkeep`. Do NOT add a rule for
`data/processed/tracking/` — the existing `data/processed/*` block already covers it.

Retarget the Phase-2.0 guard `test_phase_adds_no_video_python_dependency` in
tests/test_capture_artifacts.py: it currently asserts the literal token `opencv` is absent from the
whole pyproject.toml text, which Phase 2.1 legitimately breaks. Replace it with a `tomllib`-based
test named `test_core_dependencies_stay_free_of_cv_libraries` that parses pyproject.toml and asserts
the forbidden tokens (`opencv`, `decord`, `ffmpeg-python`, `imageio-ffmpeg`, `moviepy`, `rfdetr`,
`torch`, `transformers`) appear in NO entry of `project.dependencies`, while explicitly permitting
them inside `project.optional-dependencies.cv`. Keep the assertion message naming D-07 (core install
stays lean).

Create tests/test_cv_dependencies.py with the AGPL supply-chain guard: assert the string `boxmot`
appears in neither pyproject.toml nor uv.lock, and assert the `cv` optional group exists and lists
`trackers`. Message must name C-06 and the AGPL reason.
  </action>
  <verify>
    <automated>uv run pytest tests/test_capture_artifacts.py tests/test_cv_dependencies.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import tomllib,pathlib;d=tomllib.loads(pathlib.Path('pyproject.toml').read_text());print(d['project']['optional-dependencies']['cv'])"` prints a list containing `trackers` and `rfdetr`
    - `grep -c boxmot pyproject.toml uv.lock` reports 0 matches in both files
    - `grep -v '^#' pyproject.toml | grep -c 'opencv-python>='` is >= 1 (version pinned, not bare)
    - `git check-ignore -q data/labels/frame_0001.jpg` exits 0
    - `git check-ignore -q data/labels/.gitkeep` exits 1
    - `uv run pytest tests/test_capture_artifacts.py -q` exits 0 and no test named `test_phase_adds_no_video_python_dependency` remains (`grep -c test_phase_adds_no_video_python_dependency tests/test_capture_artifacts.py` == 0)
  </acceptance_criteria>
  <done>The `cv` extras group exists with pinned, license-clean packages; `data/labels/` is gitignored except `.gitkeep`; the Phase-2.0 guard permits CV deps only inside the optional group; a boxmot/AGPL guard test is green.</done>
</task>

<task type="auto">
  <name>Task 2: CV config surface (paths, reference files, [cv] settings table)</name>
  <files>src/flag_football_ep/config.py, ffep.toml, tests/test_cv_config.py</files>
  <read_first>
    - src/flag_football_ep/config.py (full file — `Paths`, `ReferenceFiles`, `TrainSettings`, `ReportSettings`, `Config`, `_PATH_KEYS`, `_REFERENCE_KEYS`, `_REPORT_KEYS`, `_table`, `_key`, `_resolve`, `load_config`)
    - ffep.toml (full file — `[paths]`, `[reference]`, `[train]`, `[report]` table shapes)
    - .planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-PATTERNS.md §`src/flag_football_ep/config.py` (extended)
    - tests/test_model_registry.py (lines 30-60 — the `_make_config` tmp_path helper shape later CV tests copy)
  </read_first>
  <action>
Extend `Paths` with three fields `video`, `labels`, `tracking` and add the matching key names to
`_PATH_KEYS`. Extend `ReferenceFiles` with `hover_positions`, `homography_calibration`,
`gt_positions` and add them to `_REFERENCE_KEYS`. Add a frozen dataclass `CvSettings` with the exact
field set listed in this plan's `<interfaces>` block, a `_CV_KEYS` tuple mirroring it, and a `cv`
field on `Config`. Load `[cv]` in `load_config` using the existing `_table`/`_key` helpers so a
missing table or key raises `ConfigError` naming it, exactly like `[train]` and `[report]` do today.
Path values go through `_resolve(base_dir, raw)` — relative to the config file's directory, never
the process CWD. No new hard-coded default paths inside `config.py`.

Add the corresponding TOML to ffep.toml:
`[paths]` gains `video = "data/video"`, `labels = "data/labels"`,
`tracking = "data/processed/tracking"`.
`[reference]` gains `hover_positions = "data/reference/hover_positions.csv"`,
`homography_calibration = "data/reference/homography_calibration.csv"`,
`gt_positions = "data/reference/gt_positions.csv"`,
`continuity_review = "data/reference/continuity_review.csv"`.
New `[cv]` table with: `pilot_session_id = "2026-05-16_FRIENDLY-GER-vs-PANAMA-ROJO-DRONE"`
(the exact `session_id` value used in data/reference/video_inventory.csv),
`detector_model = "cv_detector_model"`, `detector_experiment = "cv_detector"`,
`resolution = 672`, `sahi = false`, `sahi_slice = 640`, `sahi_overlap = 0.2`,
`train_epochs = 30`, `train_batch_size = 4`, `train_grad_accum = 4`, `device = "cpu"`,
`label_frame_target = 400`, `cvat_host = "http://localhost:8080"`,
`cvat_username_env = "CVAT_USERNAME"`, `cvat_password_env = "CVAT_PASSWORD"`,
`field_length_yards = 50.0`, `field_width_yards = 25.0`,
`endzone_yards = 10.0`. Add a comment above `resolution`/`sahi` stating both are provisional
defaults revised by the Phase 2.1 sighting review (plan 02.1-03), that `resolution` must stay a
multiple of 224 (= lcm(32, 56), valid under either documented RF-DETR divisibility rule), and that `train_batch_size = 4` / `train_grad_accum = 4` is the documented
8 GB-VRAM setting for the Dell box (D-05). `cvat_host` stays a loopback URL — the self-hosted
CVAT instance must never bind a routable interface, because the footage it serves is PII
(`docs/capture-legal.md`).

Create tests/test_cv_config.py covering: `load_config(Path("ffep.toml"))` returns a `Config` whose
`cv.pilot_session_id` matches a `session_id` actually present in data/reference/video_inventory.csv;
`cv.resolution % 224 == 0` (224 = lcm(32, 56), so the value is valid under either documented RF-DETR resolution-divisibility rule); every new `Paths`/`ReferenceFiles` attribute is an absolute resolved
`Path` under the repo root; a `[cv]` table with a missing key raises `ConfigError` naming that key
(write a trimmed TOML into tmp_path to exercise this).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class CvSettings" src/flag_football_ep/config.py` == 1
    - `uv run python -c "from pathlib import Path; from flag_football_ep.config import load_config; c=load_config(Path('ffep.toml')); print(c.cv.resolution, c.paths.labels, c.reference.homography_calibration)"` prints `672`, a path ending `data/labels`, and a path ending `data/reference/homography_calibration.csv`
    - `uv run python -c "from pathlib import Path; from flag_football_ep.config import load_config; c=load_config(Path('ffep.toml')); assert c.cv.resolution % 224 == 0"` exits 0
    - Removing any single key from `[cv]` in a tmp copy of ffep.toml raises `ConfigError` whose message contains that key name (asserted in the test)
    - `uv run pytest -q` exits 0 (no regression in the existing config tests)
  </acceptance_criteria>
  <done>Every CV path and knob resolves through `Config`; a missing `[cv]` key fails loudly at load time with the key name in the message.</done>
</task>

<task type="auto">
  <name>Task 3: Install and smoke-test the CV stack on the primary machine, record the outcome</name>
  <files>docs/cv-setup.md, uv.lock, pyproject.toml</files>
  <read_first>
    - docs/capture-protocol.md (lines 1-30 — German status-header doc convention this file follows)
    - .planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-RESEARCH.md §Common Pitfalls (Pitfall 3), §Open Questions 2, §Environment Availability
    - pyproject.toml (the `cv` group written in Task 1)
  </read_first>
  <action>
Run `uv sync --extra cv` and then probe every CV entry point in one Python process:
`import torch`, `import cv2`, `import supervision`, `from rfdetr import RFDETRSmall`,
`from trackers import ...` (the OC-SORT tracker class — read the installed package's `__init__`
to get the exact exported name and record it in the doc), `from transformers import SiglipVisionModel`,
`import umap`, `import sahi`, `from autodistill.detection import CaptionOntology`,
`from autodistill_grounding_dino import GroundingDINO`. Record `torch.backends.mps.is_available()`
and the resolved versions of every package in the group.

If `autodistill-grounding-dino` fails to install or import (RESEARCH Open Question 2), do NOT stop:
record the failure verbatim, remove it from the `cv` group, and add `transformers`-based
Grounding DINO as the pre-labeling path instead (checkpoint `IDEA-Research/grounding-dino-tiny`,
class `GroundingDinoForObjectDetection`, pure PyTorch, no custom CUDA op). The decision made here is
binding for plan 02.1-07.

Write `docs/cv-setup.md` in German, following the `docs/capture-protocol.md` status-header
convention (`# Title — subtitle` then `**Status: ...**` then a `## Zweck & Abgrenzung` section).
Sections: installed versions table (package, resolved version, license), MPS/CPU availability on the
primary machine, the per-machine torch caveat (M5 Max = MPS wheel, Dell = CUDA wheel, one
`pyproject.toml` but two environments — never assume one lock resolution serves both), the
Grounding DINO backend decision from the probe above, and an explicit note that `boxmot` is
forbidden (AGPL-3.0, C-06) with `trackers` as the replacement. Reserve an empty `## CVAT` section
with the marker `<!-- gefuellt von Plan 02.1-08 -->` for the CVAT setup record.

Also correct the hardware naming discrepancy RESEARCH flagged: record the machine identifier from
`system_profiler SPHardwareDataType | grep Chip` verbatim rather than repeating "M5 Max" from
CONTEXT.md if they disagree.
  </action>
  <verify>
    <automated>uv run --extra cv python -c "import torch, cv2, supervision, sahi, umap, trackers; from rfdetr import RFDETRSmall; from transformers import SiglipVisionModel; print('cv stack ok', torch.__version__, cv2.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `cv stack ok` with concrete torch and cv2 versions
    - `uv run python -c "import flag_football_ep.cli"` exits 0 in an environment synced WITHOUT `--extra cv` (core install stays usable)
    - `docs/cv-setup.md` contains `**Status:` on line 3 and the sections `## Zweck & Abgrenzung`, `## Installierte Versionen`, `## Grounding-DINO-Backend`, `## CVAT`
    - `grep -c 'boxmot' docs/cv-setup.md` >= 1 and the surrounding text names AGPL-3.0 and C-06
    - `docs/cv-setup.md` names the exact OC-SORT tracker class exported by the installed `trackers` version
  </acceptance_criteria>
  <done>The CV stack installs and imports on the primary machine; the Grounding DINO backend decision and the per-machine torch caveat are written down; `docs/cv-setup.md` exists as the environment record later plans extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> local environment | third-party package code executes at install and import time |
| Working tree -> git remote | PII-bearing frames/labels must never cross this boundary |
| ffep.toml -> filesystem | operator-edited path strings resolve to filesystem locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-SC | Tampering | `uv add --optional cv` package installs | mitigate | All 11 packages carry `[OK]` slopcheck + independent GitHub license verification in 02.1-RESEARCH.md §Package Legitimacy Audit; no `[ASSUMED]`/`[SUS]` entries remain, so no legitimacy checkpoint is required. Task 1 adds `tests/test_cv_dependencies.py` asserting `boxmot` (AGPL-3.0) is absent from pyproject.toml and uv.lock |
| T-2.1-01 | Information disclosure | `data/labels/` COCO export (extracted player frames) | mitigate | Task 1 `.gitignore` block ignores contents, exempts `.gitkeep`; verified by `git check-ignore` acceptance criteria |
| T-2.1-02 | Tampering | `[paths]`/`[reference]` strings in ffep.toml | mitigate | Task 2 routes every new path through the existing `_resolve(base_dir, raw)` helper (config-file-relative, not CWD-relative); repo-relative-path guards on the hand-edited CV reference CSVs land with those CSVs in plans 02.1-03/04/15 |
| T-2.1-07 | Information disclosure | `docs/cv-setup.md` environment record | accept | Records package versions and a chip identifier only; contains no secrets, no paths outside the repo, no player data |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green (core environment, no `--extra cv`)
- `uv sync` (no extras) followed by `uv run python -c "import flag_football_ep.cli"` — core CLI still imports without the CV stack
- `uv run --extra cv python -c "from rfdetr import RFDETRSmall; import trackers"` — CV stack importable
- `git status --porcelain data/labels/` shows nothing but the tracked `.gitkeep`
</verification>

<success_criteria>
- `[project.optional-dependencies] cv` exists with pinned, Apache-2.0/MIT-only packages; `boxmot` absent from pyproject.toml and uv.lock
- `Config` exposes `paths.video/labels/tracking`, `reference.hover_positions/homography_calibration/gt_positions/continuity_review` and a complete `cv` settings object loaded from `[cv]`
- A missing `[cv]` key raises `ConfigError` naming the key
- `data/labels/` is gitignored except `.gitkeep`
- `docs/cv-setup.md` records installed versions, MPS availability, the Grounding DINO backend decision, and the boxmot/AGPL prohibition
</success_criteria>

<output>
Create `.planning/phases/02.1-cv-tracking-pilot-go-no-go-gate/02.1-01-SUMMARY.md` when done
</output>
