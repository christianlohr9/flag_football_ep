---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
plan: 03
subsystem: cv
tags: [opencv, frame-extraction, hover-positions, sighting, mog2, inference-settings]

# Dependency graph
requires:
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 01
    provides: CvSettings/[cv] config surface, cv extras group, data/labels gitignore block
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 02
    provides: cv/frames.py and cv/sighting.py contract signatures, ffep cv sight CLI verb
provides:
  - cv/frames.py implementation -- clip discovery against video_inventory.csv (ClipNotFound on missing files, symlink-safe repo-root containment), deterministic frame extraction
  - cv/sighting.py implementation -- hover-position grouping (8-frame-averaged blurred fingerprints, centroid clustering) and MOG2 apparent-size measurement
  - data/reference/hover_positions.csv -- 61 rows, 2 confirmed hover positions (hp-01: 30 clips, hp-02: 31 clips), deterministic output
  - docs/pilot-sighting.md -- ratified sighting record (German): hover table, measured sizes p50=30px/p10=16px, settings derivation, 2-homography consequence for plan 02.1-13
  - ffep.toml [cv] resolution = 896, sahi = false -- human-approved inference settings (was 672 provisional)
affects: [02.1-07, 02.1-11, 02.1-13]

# Tech tracking
tech-stack:
  added: []
  patterns: ["repo-root containment checks must be syntactic (no Path.resolve()) because data/video/ may legitimately be a symlink", "hover fingerprints: 8-frame-averaged + gaussian-blurred grayscale, centroid-based clustering, threshold tuned on real footage"]

key-files:
  created: [src/flag_football_ep/cv/frames.py (implementation), src/flag_football_ep/cv/sighting.py (implementation), tests/test_cv_frames.py, tests/test_cv_sighting.py, data/reference/hover_positions.csv, docs/pilot-sighting.md]
  modified: [ffep.toml]

key-decisions:
  - "2 hover positions confirmed by measurement + human review (not RESEARCH's assumed 1) -- plan 02.1-13 must calibrate 2 homographies"
  - "resolution = 896 / sahi = false locked by user approval on 2026-08-24: measured p50=30px/p10=16px sits in the 20-40px band where higher input resolution suffices without SAHI's runtime cost against C-09's < 1 h/game criterion"
  - "Sighting output is deterministic: repeated ffep cv sight runs produce byte-identical hover_positions.csv"

patterns-established:
  - "Contact sheets and any frame-bearing artifact go under data/labels/ (gitignored PII); only aggregate CSVs and docs are committed"
  - "Human-verify checkpoints commit their reviewable automation artifacts BEFORE pausing, so review happens against committed state"

requirements-completed: []

# Metrics
duration: ~180min (across checkpoint pause and session interruptions)
completed: 2026-08-24
---

# Phase 02.1 Plan 03: Clip Discovery, Sighting Pass and Settings Review Summary

**Clip discovery and frame extraction over the registered 61-clip pilot session, hover-position grouping and MOG2 apparent-size measurement rewritten against real footage, and the human settings review: 2 confirmed hover positions, p50 player height 30 px, `resolution = 896` / `sahi = false` ratified into `ffep.toml`.**

## Performance

- **Tasks:** 3 (third was a blocking human-verify checkpoint, approved by the user on 2026-08-24)
- **Sighting runtime:** ~71 s over all 61 clips (foreground, deterministic)

## Accomplishments
- `cv/frames.py`: clip discovery validated against `data/reference/video_inventory.csv` (raises `ClipNotFound` on missing files), deterministic frame extraction; repo-root containment check made symlink-safe
- `cv/sighting.py`: hover grouping via 8-frame-averaged blurred fingerprints with centroid clustering; apparent player size via sequential MOG2 with warmup and area/fill-ratio/aspect filters — both rewritten after the synthetic-fixture versions failed on real 1080p footage
- Real sighting pass: **2 hover positions** (hp-01: 30 clips wide framing, hp-02: 31 clips zoomed/rotated), interleaved by clip number; visually spot-checked on 6 clips
- Measured apparent player height: p50 = 30.0 px, p10 = 16.0 px (all 61 clips `Brauchbar` at 1920x1080)
- `docs/pilot-sighting.md` (German) drafted, reviewed, and ratified; contact sheets rendered under gitignored `data/labels/sighting/`
- User approved settings applied: `[cv] resolution = 896` (was 672), `sahi = false`; final verify prints `2 896 False`, exit 0

## Task Commits

1. **Task 1: clip discovery and frame extraction** - `e1dde21` (feat)
2. **Task 2: hover-position grouping and size measurement** - `8a4c5ae` (feat)
3. **Fix: symlink-safe repo-root containment** - `399c231` (fix)
4. **Fix: hover grouping + MOG2 sizing against real footage** - `f8633b0` (fix)
5. **Task 3 automation: sighting pass over the real session** - `ab64f53` (feat)
6. **Task 3 approval: lock approved inference settings** - `4af8dd7` (feat)

## Deviations

- **Two real-footage bug fixes (Rule 1/3, auto-fixed):** `Path.resolve()` in the repo-root check followed the `data/video/` symlink and rejected valid clips (replaced with a syntactic `..` check); the original single-frame fingerprint (threshold 0.97) and scattered MOG2 sampling both collapsed on real compressed 1080p footage (0.1–0.5 correlations, 100k+ spurious sub-5px blobs) and were rewritten as described above.
- **Checkpoint executed across sessions:** the original executor's data-availability blocker (footage absent in the worktree) was resolved by symlinking the user's clips into the worktree; the approval remainder (config edit, ratification, verify, this summary) was completed by the orchestrator after user approval because the executor transcript was lost to a login interruption.

## Self-Check: PASSED

- `uv run --extra cv` verify command: exit 0, prints `2 896 False`
- `hover_positions.csv`: 61 rows, every row grouped
- `docs/pilot-sighting.md`: all required sections present, `## Konsequenzen` names 2 homographies
- Contact sheets exist per hover position; `git check-ignore` exits 0
