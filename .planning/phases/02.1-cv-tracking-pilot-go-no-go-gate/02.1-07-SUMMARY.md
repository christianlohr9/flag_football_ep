---
phase: 02.1-cv-tracking-pilot-go-no-go-gate
plan: 07
subsystem: cv
tags: [frame-sampling, grounding-dino, zero-shot, coco, prelabels, transformers]

# Dependency graph
requires:
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 01
    provides: cv extras group, Grounding DINO backend decision (transformers, not autodistill)
  - phase: 02.1-cv-tracking-pilot-go-no-go-gate
    plan: 03
    provides: cv/frames.py clip discovery + extraction, data/reference/hover_positions.csv (2 hover positions)
provides:
  - sample_training_frames/write_manifest/read_manifest in cv/frames.py -- deterministic clip-stratified sample (budget by clip duration, stratified per hover position, floor 3 / ceiling 12 frames per clip, jittered even grid seeded from f"{seed}:{clip_number}")
  - prelabel_frames in cv/prelabel.py -- Grounding DINO zero-shot person boxes over the sampled frames, COCO output whose images are exactly the manifest entries in manifest order
affects: [02.1-08, 02.1-09]

# Tech tracking
tech-stack:
  added: []
  patterns: ["frame sampling is manifest-driven: COCO images come from the manifest belonging to frames_dir, never a directory glob", "per-clip determinism via random.Random(f\"{seed}:{clip_number}\") so adding/removing clips never reshuffles other clips' frames"]

key-files:
  created: [tests/test_cv_prelabel.py]
  modified: [src/flag_football_ep/cv/frames.py, src/flag_football_ep/cv/prelabel.py, tests/test_cv_frames.py]

key-decisions:
  - "Backend selection tries autodistill first for contract compatibility but this project's cv extras group intentionally does not install it -- the transformers GroundingDinoForObjectDetection path (plan 01's binding decision) is the operative backend"
  - "Frame indices computed via the same ffprobe-probed fps that extract_frames uses, so sampled indices always land on extractable frames"

patterns-established:
  - "Manifest files and COCO pre-labels live under config.paths.labels (gitignored PII); only code and tests are committed"

requirements-completed: []

# Metrics
duration: ~150min (across repeated machine-sleep interruptions)
completed: 2026-08-25
---

# Phase 02.1 Plan 07: Training-Frame Sample and Grounding DINO Pre-Labels Summary

**Deterministic clip-stratified training-frame sampling (budget by duration, stratified across the 2 confirmed hover positions, 3-12 frames per clip on a jittered seeded grid) and Grounding DINO zero-shot person pre-labels written as a COCO file whose images are exactly the sampled manifest entries.**

## Performance

- **Tasks:** 2
- **Verification:** `uv run --extra cv pytest tests/test_cv_frames.py tests/test_cv_prelabel.py tests/test_cv_contracts.py -q` — exit 0, no failures

## Accomplishments
- `sample_training_frames` in `cv/frames.py`: allocates the frame budget proportionally to clip duration, stratifies per hover position first (hover_positions.csv from plan 03 names 2), enforces floor 3 / ceiling 12 frames per clip, and places timestamps on a jittered even grid seeded per clip — adding or removing clips never reshuffles other clips' samples
- `write_manifest`/`read_manifest`: the sample is persisted as a manifest under `config.paths.labels` and is the single source of truth for which frames exist
- `prelabel_frames` in `cv/prelabel.py`: loads the manifest belonging to `frames_dir` (never a directory glob), runs Grounding DINO zero-shot person detection (transformers backend per plan 01's binding decision), and writes COCO where `images` are exactly the manifest entries in manifest order
- 405 new test lines across `tests/test_cv_frames.py` (sampling determinism, stratification, floors/ceilings) and `tests/test_cv_prelabel.py` (manifest-driven COCO, backend fallback, box plausibility)

## Task Commits

1. **Task 1: deterministic clip-stratified training-frame sample** - `24dff50` (feat)
2. **Task 2: Grounding DINO zero-shot pre-labels to COCO** - `6cb589e` (feat)

## Deviations

- **Executor closed out by orchestrator:** the machine slept repeatedly during execution, killing the executor after both task commits but before SUMMARY creation. The orchestrator merged the branch, ran the plan's verify commands (exit 0), and wrote this summary — per the workflow's "close out manually" recovery path.

## Self-Check: PASSED

- Both plan verify commands pass (`test_cv_frames.py`, `test_cv_prelabel.py`), plus the 73 contract guards
- Both task commits present and atomic
- Nothing under `data/video/` or `data/labels/` committed
