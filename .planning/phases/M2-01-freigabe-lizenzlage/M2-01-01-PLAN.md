---
phase: M2-01-freigabe-lizenzlage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/freigabe-vorlage.md
  - docs/capture-legal.md
  - docs/hackathon-challenge-reid.md
  - tests/test_m2_legal_docs.py
autonomous: false
requirements: [RECHT-01, RECHT-02, RECHT-03]

must_haves:
  truths:
    - "A signable German one-pager exists that names Dev-Set, Test-Set and Transfer-Set individually as released material classes"
    - "The one-pager states a deletion path, a dated deletion deadline, and who confirms the deletion in writing"
    - "The signature date lives in exactly one machine-readable place and every other document quotes it from there"
    - "Before the signature exists, all three documents carry the literal token SIGNATUR-DATUM-TBD; after it exists, none of them does"
    - "No player name, no clip filename and no path under data/private/ appears in the release documents"
  artifacts:
    - path: "docs/freigabe-vorlage.md"
      provides: "Signable German release one-pager (material classes, purpose limitation, deletion path/deadline/confirmer, signature block)"
      contains: "<!-- signatur-datum: "
      min_lines: 50
    - path: "docs/capture-legal.md"
      provides: "Nachtrag that upgrades the 2026-08-31 blanket approval to the written release and points at the one-pager"
      contains: "docs/freigabe-vorlage.md"
    - path: "docs/hackathon-challenge-reid.md"
      provides: "Status block and §Datenschutz naming the written release with the signature date"
      contains: "docs/freigabe-vorlage.md"
    - path: "tests/test_m2_legal_docs.py"
      provides: "Automated consistency + PII guard over the three release documents"
      contains: "SIGNATUR-DATUM-TBD"
  key_links:
    - from: "docs/hackathon-challenge-reid.md"
      to: "docs/freigabe-vorlage.md"
      via: "signature date quoted from the machine-readable marker"
      pattern: "freigabe-vorlage\\.md"
    - from: "docs/capture-legal.md"
      to: "docs/freigabe-vorlage.md"
      via: "Nachtrag reference"
      pattern: "freigabe-vorlage\\.md"
    - from: "tests/test_m2_legal_docs.py"
      to: "docs/freigabe-vorlage.md"
      via: "parses the signatur-datum marker and compares it against the other two documents"
      pattern: "signatur-datum"
---

<objective>
Turn the dated, blanket, verbal federation approval of 2026-08-31 into a signable written
release that names Dev-Set, Test-Set and Transfer-Set individually and fixes a deletion path,
a deletion deadline and a named confirmer (RECHT-01, RECHT-03). Wire the signature date into
the challenge description through a single machine-readable marker so the date placeholder is
replaced exactly once, in one commit, when the real signature exists (RECHT-02).

Purpose: without the signed release the challenge is withdrawn (agreed with the hackathon
organisers). The project can prepare everything except the signature — that step is the user's.
Output: `docs/freigabe-vorlage.md`, a Nachtrag in `docs/capture-legal.md`, the release block in
`docs/hackathon-challenge-reid.md`, and `tests/test_m2_legal_docs.py` keeping the three in sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/M2-01-freigabe-lizenzlage/M2-01-CONTEXT.md
@.planning/imported/challenge-haertung/ABGLEICH.md
@docs/capture-legal.md
@docs/hackathon-bundles.md
</context>

<file_collision_guard>
Phase 2.2 waves 7–11 are executing on this tree in parallel. Plans 02.2-14..20 own:
`src/flag_football_ep/cv/bundle.py`, `src/flag_football_ep/cv/detect.py`,
`tests/test_cv_bundle.py`, `tests/test_cv_detect_train.py`, `tests/test_phase22_docs.py`,
`docs/hackathon-bundles.md`, `docs/dataset-buildout.md`, `docs/cv-setup.md`,
`docs/dataset-publication.md`, `docs/dataset-card.md`, `.dvc/config`, `ffep.toml`,
`data/labels/**`. This plan must not write to any of them — read only.

`docs/hackathon-challenge-reid.md` is shared ground and currently has uncommitted working-tree
changes. Edits from this plan are limited to exactly three places, listed verbatim in Task 2 and
Task 3. Do not reformat, reorder or revert anything else in that file, and commit promptly after
each task so the file is never held hostage.

New test file is `tests/test_m2_legal_docs.py` — do NOT add assertions to
`tests/test_phase22_docs.py` (owned by plan 02.2-19).
</file_collision_guard>

<pii_discipline>
Raw material and crops are PII (`docs/capture-legal.md`). The release documents describe material
CLASSES, never individuals: no player name, no jersey number tied to a name, no clip filename, no
path under `data/private/`. Nothing gitignored gets committed. Task 3 enforces this by test.
</pii_discipline>

<tasks>

<task type="auto">
  <name>Task 1: Write the signable release one-pager</name>
  <files>docs/freigabe-vorlage.md</files>
  <read_first>
    - `docs/capture-legal.md` — whole file (42 lines); the `## DSGVO — Einverständnis liegt beim Verband` section and the `## Nachtrag 2026-08-31 — Freigabe des Verbands` block are the factual basis this document upgrades.
    - `docs/hackathon-bundles.md` — the three `### Inhalt` tables (Dev-Set at ~line 26, Test-Set at ~line 153, Transfer-Set at ~line 263) and the `### Delivery-Regeln` blocks; these define what each material class actually contains and how it is delivered.
    - `.planning/phases/M2-01-freigabe-lizenzlage/M2-01-CONTEXT.md` — `<decisions>` block (Riegel-Charakter, RECHT-02 rule).
  </read_first>
  <action>
    Create `docs/freigabe-vorlage.md`, German prose, in the register of the existing `docs/*.md`
    (plain, factual, no marketing tone, no emojis, no em dashes). It is a document the user
    prints or sends to the federation and gets signed — write it so it can be signed as-is.

    Required structure, in this order, using exactly these H2 headings:

    `# Datenfreigabe für den BWI Data Analytics Hackathon (23.–27.11.2026)`

    `## Parteien` — releasing body (federation, name and function left as a fill-in line
    `Name, Funktion: ____________________`), and the recipient (owner of the project, the user).

    `## Gegenstand der Freigabe` — a table with the columns `Materialklasse | Inhalt | Umfang |
    Empfänger`. Exactly three rows, one per class, filled from `docs/hackathon-bundles.md`:
    `Dev-Set` (43 Drohnen-Clips des Pilotspiels, Detektionen, Baseline-Tracks, Oberkörper-Crops,
    Human-Urteile, Overlays; Empfänger: alle Hackathon-Teams),
    `Test-Set` (18 zurückgehaltene Drohnen-Clips desselben Spiels, Detektionen, Baseline-Tracks,
    Overlays, ohne Labels; Empfänger: alle Hackathon-Teams, Wertungsgrundlage),
    `Transfer-Set` (60 GoPro-Seitenlinien-Clips WM GER–MEX und 51 TV-Clips WM USA–AUS mit
    Detektionen; Empfänger: alle Hackathon-Teams). Describe classes, never individual persons.

    `## Zweckbindung` — internal, purpose-bound use in the hackathon only: developing and
    measuring re-identification methods. No publication, no further transfer outside the
    hackathon context, no cloud upload of the material by teams (mirrors the operative rules in
    `docs/hackathon-challenge-reid.md ## Datenschutz` and the `### Delivery-Regeln` blocks).

    `## Löschweg und Löschfrist` (RECHT-03) — concrete and datable:
    the hackathon ends 2026-11-27; every team deletes all local copies of every material class
    within 14 days, i.e. by `2026-12-11`; the project owner deletes the bundle objects from the
    delivery storage (Open Telekom Cloud OBS) by the same date; the label vault stays with the
    project and never left it. State the deadline as a literal ISO date.

    `## Bestätigung der Löschung` (RECHT-03, who confirms) — the hackathon organiser (BWI)
    confirms the team-side deletion in text form to the project owner; the owner records the
    confirmation as a dated Nachtrag in `docs/capture-legal.md`. Leave a fill-in line for the
    confirming person: `Bestätigende Stelle/Person: ____________________`.

    `## Geltungsdauer` — release valid from the signature date until the deletion confirmation is
    recorded; it does not replace the DSGVO consents, which stay with the federation.

    `## Unterschrift` — signature block with `Ort, Datum: ____________________` and
    `Unterschrift: ____________________`, immediately followed by the single machine-readable
    marker line, verbatim and on its own line:

    `<!-- signatur-datum: SIGNATUR-DATUM-TBD -->`

    Directly under the marker, one sentence of German prose stating that this marker is the only
    place the signature date is maintained and that `tests/test_m2_legal_docs.py` checks every
    other document against it.

    Do not invent a federation contact name and do not fill the signature date — both are the
    user's input in Task 3.
  </action>
  <verify>
    <automated>test -f docs/freigabe-vorlage.md && grep -c '^## ' docs/freigabe-vorlage.md | grep -qx '7' && grep -q '^<!-- signatur-datum: SIGNATUR-DATUM-TBD -->$' docs/freigabe-vorlage.md && grep -q 'Dev-Set' docs/freigabe-vorlage.md && grep -q 'Test-Set' docs/freigabe-vorlage.md && grep -q 'Transfer-Set' docs/freigabe-vorlage.md && grep -q '2026-12-11' docs/freigabe-vorlage.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/freigabe-vorlage.md` exists and has exactly the seven H2 sections named above, in that order.
    - The `## Gegenstand der Freigabe` table has three data rows, one each for Dev-Set, Test-Set, Transfer-Set, with all four columns filled.
    - `## Löschweg und Löschfrist` contains the literal date `2026-12-11` and names both the team-side and the owner-side deletion duty.
    - `## Bestätigung der Löschung` names BWI as the confirming body and has a fill-in line for the confirming person.
    - The file contains the marker line `<!-- signatur-datum: SIGNATUR-DATUM-TBD -->` exactly once.
    - The file contains no person's name, no `.mp4` filename and no `data/private/` path.
  </acceptance_criteria>
  <done>The federation could sign this document unchanged; the only unfilled fields are names, place, date and signature.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor the release in capture-legal.md and the challenge description, guard it with a test</name>
  <files>docs/capture-legal.md, docs/hackathon-challenge-reid.md, tests/test_m2_legal_docs.py</files>
  <read_first>
    - `docs/freigabe-vorlage.md` — the file written in Task 1, in particular the marker line.
    - `docs/capture-legal.md` — the final section `## Nachtrag 2026-08-31 — Freigabe des Verbands`; the new Nachtrag is appended after it and must not alter it.
    - `docs/hackathon-challenge-reid.md` lines 1–12 (status block) and the `### Datenschutz` section (~lines 194–209) — the two blocks that carry the approval date today.
    - `tests/` — pick up the register of an existing doc test for style, e.g. `tests/test_phase22_docs.py` (read only, never edit).
  </read_first>
  <action>
    Three edits and one new file. Touch nothing else.

    (a) `docs/capture-legal.md`: append a new section
    `## Nachtrag 2026-09-01 — Schriftliche Freigabe (Vorlage)` after the existing
    `## Nachtrag 2026-08-31` block. German prose, four to eight sentences: the 2026-08-31
    approval was verbal and blanket; for handing material to hackathon teams it is being upgraded
    to a written, signed release that names Dev-, Test- and Transfer-Set individually and fixes a
    deletion path (RECHT-01, RECHT-03); the signable document is `docs/freigabe-vorlage.md`;
    until it is signed the material stays undelivered and the signature date is carried as the
    literal token `SIGNATUR-DATUM-TBD`; the DSGVO responsibility stays with the federation
    (unchanged from the section above).

    (b) `docs/hackathon-challenge-reid.md`, status block (lines 3–5): keep the sentence about the
    2026-08-31 approval as the verbal basis and extend it with the written release, i.e. the
    block must additionally name `docs/freigabe-vorlage.md` and carry the literal token
    `SIGNATUR-DATUM-TBD` as the signature date. Do not touch the surrounding sentences about
    the BWI submission or the owner.

    (c) `docs/hackathon-challenge-reid.md`, `### Datenschutz`: after the sentence that reports the
    2026-08-31 approval, add two sentences: the written, signed release naming the three material
    classes plus deletion path is `docs/freigabe-vorlage.md`, signed on `SIGNATUR-DATUM-TBD`; the
    deletion deadline for all classes is 2026-12-11 and BWI confirms the team-side deletion in
    text form. Leave the rest of the section, including the operative rules, untouched.

    (d) Create `tests/test_m2_legal_docs.py` — stdlib + pytest only, no `cv` extra, runs in under
    a second. It reads the three documents from the repo root (resolve via
    `pathlib.Path(__file__).resolve().parents[1]`) and asserts:
      1. `docs/freigabe-vorlage.md` contains exactly one line matching
         `^<!-- signatur-datum: (.+) -->$`; capture the value as `signatur_datum`.
      2. Structure: the seven H2 headings from Task 1 are present in `docs/freigabe-vorlage.md`,
         in order; the three material class names `Dev-Set`, `Test-Set`, `Transfer-Set` each
         appear in the `## Gegenstand der Freigabe` table; `2026-12-11` appears in
         `## Löschweg und Löschfrist`.
      3. Consistency: if `signatur_datum == "SIGNATUR-DATUM-TBD"`, then that same token must
         appear in `docs/capture-legal.md` and in `docs/hackathon-challenge-reid.md`. Otherwise
         `signatur_datum` must match `^\d{4}-\d{2}-\d{2}$`, must appear literally in both other
         documents, and the token `SIGNATUR-DATUM-TBD` must appear in NEITHER of the three files.
      4. Cross-reference: the string `docs/freigabe-vorlage.md` appears in `docs/capture-legal.md`
         and in `docs/hackathon-challenge-reid.md`.
      5. PII guard over `docs/freigabe-vorlage.md`: the file contains no `data/private/`
         substring and no `.mp4` substring.
    Write each numbered assertion as its own test function with a speaking name so a failure
    names the broken invariant.
  </action>
  <verify>
    <automated>uv run pytest tests/test_m2_legal_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_m2_legal_docs.py -q` passes with at least five test functions collected.
    - `git diff --stat docs/hackathon-challenge-reid.md` shows changes confined to the status block and the `### Datenschutz` section.
    - `docs/capture-legal.md` ends with the new `## Nachtrag 2026-09-01 — Schriftliche Freigabe (Vorlage)` section and the 2026-08-31 Nachtrag above it is byte-identical to before.
    - `grep -c 'SIGNATUR-DATUM-TBD' docs/freigabe-vorlage.md docs/capture-legal.md docs/hackathon-challenge-reid.md` reports at least one hit per file.
    - No file outside `files_modified` is changed.
  </acceptance_criteria>
  <done>The three release documents point at one another, the signature date has exactly one source of truth, and a test fails the moment they drift apart.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Signature by the federation, then replace the date token (RECHT-01, RECHT-02)</name>
  <files>docs/freigabe-vorlage.md, docs/capture-legal.md, docs/hackathon-challenge-reid.md</files>
  <read_first>
    - `docs/freigabe-vorlage.md` — the document to be sent for signature.
    - `.planning/phases/M2-01-freigabe-lizenzlage/M2-01-CONTEXT.md` — Riegel-Charakter: without the signature the challenge is withdrawn; the signature is user-side.
  </read_first>
  <action>
    Pause execution and hand `docs/freigabe-vorlage.md` to the user for the federation signature —
    this is the one step in the phase that no CLI can do. Present the document, the two fill-in
    fields (signatory name/function, confirming body/person) and the deletion deadline 2026-12-11,
    then stop and wait. On resume, perform exactly the edits listed in `<on-resume>` below with the
    reported date and nothing else; if the federation requested content changes instead, edit the
    one-pager accordingly, re-run the test, and go back into this checkpoint for a second round.
  </action>
  <what-built>
    `docs/freigabe-vorlage.md` is a complete, signable German one-pager: three material classes
    named individually, purpose limitation, deletion path with the deadline 2026-12-11, a named
    confirming body (BWI) and a signature block. `docs/capture-legal.md` and
    `docs/hackathon-challenge-reid.md` reference it and carry the token `SIGNATUR-DATUM-TBD` where
    the signature date belongs. `tests/test_m2_legal_docs.py` enforces that they stay consistent.
  </what-built>
  <how-to-verify>
    1. Read `docs/freigabe-vorlage.md` end to end and check the three material class rows describe
       what you actually intend to hand out.
    2. Fill in the federation signatory (`Name, Funktion`) and the confirming body/person, then send
       or hand the document to the federation for signature.
    3. Once it is signed, report back: the signature date in ISO form (`YYYY-MM-DD`), the name and
       function of the signatory, and the confirming body/person for the deletion.
    4. If the federation asks for changes to material classes, deletion path or deadline, report
       those instead — they are edited into the document before a second signature round.
  </how-to-verify>
  <on-resume>
    With the reported signature date `<DATUM>`, perform exactly these edits and nothing else:
    1. Replace every occurrence of the literal `SIGNATUR-DATUM-TBD` with `<DATUM>` in
       `docs/freigabe-vorlage.md`, `docs/capture-legal.md` and `docs/hackathon-challenge-reid.md`.
    2. In `docs/hackathon-challenge-reid.md ## Teil 5 — Vorbereitungs-Checkliste`, tick the item
       `- [ ] Verbands-Freigabe für das Spielmaterial (blockiert die Einreichung)` to `- [x]` and
       append ` — schriftlich unterzeichnet am <DATUM>, siehe docs/freigabe-vorlage.md`.
    3. In `docs/capture-legal.md`, rename the section heading
       `## Nachtrag 2026-09-01 — Schriftliche Freigabe (Vorlage)` to
       `## Nachtrag <DATUM> — Schriftliche Freigabe (unterzeichnet)` and record signatory name,
       function and confirming person in that section.
    4. Re-run `uv run pytest tests/test_m2_legal_docs.py -q` — it must pass, and the token
       `SIGNATUR-DATUM-TBD` must be gone from all three files.
    5. Commit all three files in one commit.
  </on-resume>
  <verify>
    <automated>uv run pytest tests/test_m2_legal_docs.py -q</automated>
    <automated>! grep -rq 'SIGNATUR-DATUM-TBD' docs/ && echo TOKEN-GONE</automated>
  </verify>
  <acceptance_criteria>
    - The signature date is reported in `YYYY-MM-DD` form and is identical in all three documents.
    - `grep -r 'SIGNATUR-DATUM-TBD' docs/` returns nothing.
    - The Teil-5 checklist item for the federation release is ticked and names the signature date.
    - `uv run pytest tests/test_m2_legal_docs.py -q` passes in the post-signature branch of the consistency test.
  </acceptance_criteria>
  <done>The written release is signed by the federation, the signature date is identical in all three documents, the placeholder token no longer exists, and the Teil-5 checklist records the release as done.</done>
  <resume-signal>Report `signiert am YYYY-MM-DD, <Name, Funktion>, Bestätigung durch <Stelle>` — or describe the changes the federation requested.</resume-signal>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_m2_legal_docs.py -q` passes.
- `docs/freigabe-vorlage.md` names Dev-Set, Test-Set and Transfer-Set as separate rows (RECHT-01).
- The signature date appears identically in the one-pager, `docs/capture-legal.md` and
  `docs/hackathon-challenge-reid.md`, and the placeholder token exists nowhere under `docs/` (RECHT-02).
- `docs/freigabe-vorlage.md` states deletion path, the deadline 2026-12-11 and the confirming body (RECHT-03).
- `git status --porcelain` shows no modification to any file owned by plans 02.2-14..20.
</verification>

<success_criteria>
1. A federation-signable release document exists that a lawyer-free reader can execute without asking a question.
2. RECHT-01 and RECHT-03 are satisfied by the document's content; RECHT-02 by the token replacement in Task 3.
3. The three documents cannot silently drift: the test fails on any inconsistency in the signature date or the cross-references.
4. No PII, no file owned by phase 2.2, and no gitignored content entered version control.
</success_criteria>

<output>
Create `.planning/phases/M2-01-freigabe-lizenzlage/M2-01-01-SUMMARY.md` when done.
</output>
