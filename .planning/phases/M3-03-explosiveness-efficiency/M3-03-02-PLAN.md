---
phase: M3-03-explosiveness-efficiency
plan: 02
type: execute
wave: 2
depends_on: [M3-03-01]
files_modified:
  - scripts/explosiveness_comparison.py
  - src/flag_football_ep/charts/explosiveness.py
  - data/reference/explosiveness/calibration.json
  - data/reference/explosiveness/comparison_overall.csv
  - data/reference/explosiveness/comparison_by_player.csv
  - data/reference/explosiveness/cliff_zone.csv
  - docs/explosiveness-vorschlag.md
  - docs/hc-rueckfragen-2026-09.md
  - tests/test_charts_explosiveness.py
  - tests/test_m3_explosiveness_docs.py
  - tests/test_m3_hc_pii.py
  - .planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md
autonomous: true
requirements: [HC-04]

must_haves:
  truths:
    - "The head coach's own number and every proposed alternative are computed on the same real plays and printed next to each other, per player and for the team, each with its n"
    - "The 10-12-yard cliff zone is a number on the page, not an assertion: the reader can see how many plays sit one yard either side of the cutoff"
    - "The German proposal document states which corpus produced its numbers and says plainly that the head coach's own rows are not in it yet"
    - "No player name reaches any committed artifact of this phase; the tables use pseudonyms and the key stays local"
    - "The three things this phase could not resolve from data alone are written down as questions to the head coach, not silently decided"
    - "The document's numbers cannot drift away from the measured CSVs — a test compares them"
  artifacts:
    - path: "scripts/explosiveness_comparison.py"
      provides: "Reproducible run over data/processed/plays_scored.parquet producing the calibration artifact, the comparison CSVs, the cliff-zone table and a local-only pseudonym key"
      contains: "def main"
      min_lines: 200
    - path: "src/flag_football_ep/charts/explosiveness.py"
      provides: "Headless Agg renderers for the cliff-zone distribution and the definition comparison, returning Figures for M3-4 to embed"
      contains: "matplotlib.use(\"Agg\")"
      min_lines: 120
    - path: "docs/explosiveness-vorschlag.md"
      provides: "German coach-facing proposal: baseline vs proposals, cliff-zone evidence, recommendation, small-sample rules, M3-4 handover"
      contains: "## Vorschlag"
      min_lines: 120
    - path: "data/reference/explosiveness/comparison_overall.csv"
      provides: "Measured team-level numbers per definition, the source of truth the document is checked against"
      contains: "baseline_hc_workbook"
    - path: "tests/test_m3_explosiveness_docs.py"
      provides: "Doc-versus-CSV agreement guard plus the structural gates on both German documents"
      contains: "comparison_overall.csv"
      min_lines: 120
  key_links:
    - from: "scripts/explosiveness_comparison.py"
      to: "flag_football_ep.features.explosiveness"
      via: "imports calibrate/definition_comparison/cliff_zone_table — the script computes nothing itself"
      pattern: "from flag_football_ep.features.explosiveness import"
    - from: "docs/explosiveness-vorschlag.md"
      to: "data/reference/explosiveness/comparison_overall.csv"
      via: "every number in the Vergleich table is read back from the CSV by tests/test_m3_explosiveness_docs.py"
      pattern: "baseline_hc_workbook|Workbook-Formel"
    - from: "tests/test_m3_hc_pii.py"
      to: "docs/explosiveness-vorschlag.md and the three explosiveness CSVs"
      via: "_CHECKED_ARTEFACTS extended so the roster-name gate covers this phase's artifacts too"
      pattern: "explosiveness"
---

<objective>
Turn the metrics module into the thing the head coach actually receives: run it on our real corpus,
freeze the calibration and the measured tables as committed reference artifacts, write the German
proposal document that answers "was ist, wenn eine Spielerin nur 11 Yards erzielt?" with numbers
instead of opinion, and add the three questions this phase could not answer from the data to the
existing question list for him.

The interesting result is already known from research and must survive contact with the document:
his verbally described rule is, on our plays, almost exactly a success rate (52.8 % vs 52.2 %) and
almost nothing to do with big plays, while his workbook implements something else entirely
(16.7 %, yards only). The document's job is to say that clearly and respectfully, show both
numbers, and propose the split into efficiency and explosiveness — not to declare his metric wrong.

Purpose: this is the October-sync handout. It is also the only artifact in this phase a human other
than the developer will read.
Output: a reproducible comparison script, committed measured tables, two headless chart renderers
for M3-4 to embed, `docs/explosiveness-vorschlag.md`, three new questions in
`docs/hc-rueckfragen-2026-09.md`, and the tests that keep all of it honest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-03-explosiveness-efficiency/M3-03-CONTEXT.md
@.planning/phases/M3-03-explosiveness-efficiency/M3-03-RESEARCH.md
@.planning/phases/M3-03-explosiveness-efficiency/M3-03-01-SUMMARY.md
@docs/explosiveness-recherche.md
@docs/hc-rueckfragen-2026-09.md
@docs/hc-notes-2026-09-03.md
@src/flag_football_ep/features/explosiveness.py
@src/flag_football_ep/charts/fourth_down.py
@tests/test_m2_baseline_docs.py
@tests/test_m3_hc_pii.py
</context>

<file_collision_guard>
Owned by this plan: `scripts/explosiveness_comparison.py`,
`src/flag_football_ep/charts/explosiveness.py`, everything under
`data/reference/explosiveness/`, `docs/explosiveness-vorschlag.md`,
`docs/hc-rueckfragen-2026-09.md`, `tests/test_charts_explosiveness.py`,
`tests/test_m3_explosiveness_docs.py`, `tests/test_m3_hc_pii.py`, and the single stale count gate
inside `.planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md` described in task 3.

- `src/flag_football_ep/features/explosiveness.py` is READ-ONLY here (M3-03-01 owns it). If the
  script needs a computation the module does not offer, that is a finding for the summary, not a
  reason to edit the module or to compute it inline in the script.
- `src/flag_football_ep/reports/**`, `features/mutations.py`, `canonical.py`, `reference.py`,
  `config.py`, `cli.py`, `pipeline.py`, `ffep.toml`, `pyproject.toml`: READ-ONLY. This plan adds no
  dependency and no config key; the script resolves paths from `ffep.toml` through the existing
  `load_config`.
- `src/flag_football_ep/ingest/**` (M3-1) and `src/flag_football_ep/model/**` (M3-2): never touched.
- `scripts/hackathon/**`, `src/flag_football_ep/cv/**`, `.dvc/**`, `data/labels/**`,
  `data/video/**`, every hackathon document: never touched.
- `docs/explosiveness-recherche.md` is READ-ONLY — it is the EXP-D01 research deliverable and
  already exists. The proposal document cites it, it is not rewritten.
- `data/raw/hc_files/**` is gitignored PII and is not opened by this plan.
- No other test file may be modified. `tests/test_m3_hc_pii.py` is edited only by appending entries
  to `_CHECKED_ARTEFACTS`; its logic stays untouched.
</file_collision_guard>

<pii_discipline>
`data/processed/plays_scored.parquet` carries real charted player names in `qb`, `thrown_by`,
`received_by`, `target` and `tackle`. `data/reference/` and `docs/` are both git-tracked and
public.

Rules for this plan, non-negotiable:
- Every committed table and every document uses pseudonyms only: `QB-01`, `QB-02`, `WR-01`, ...
  assigned deterministically by descending in-scope attempt count, ties broken by the SHA-256 of
  the source label so a re-run is reproducible without embedding the name.
- The pseudonym-to-name key is written to `data/processed/m3-03/pseudonym_key.csv`, which is
  gitignored (`data/processed/*` is ignored except `.gitkeep`). Assert the file is outside
  `data/reference/` in the script itself.
- No printed stdout line, no docstring, no commit message may contain a source player label.
- `tests/test_m3_hc_pii.py`'s `_CHECKED_ARTEFACTS` is extended in task 3 so the roster-name gate
  covers `docs/explosiveness-vorschlag.md`, `docs/explosiveness-recherche.md`, all three
  `data/reference/explosiveness/*.csv` and `scripts/explosiveness_comparison.py`. That test is the
  enforcement, not this paragraph.
</pii_discipline>

<interfaces>
From `src/flag_football_ep/features/explosiveness.py` (M3-03-01; read the real file — the summary
records the final signatures):

```python
class MissingExplosivenessColumns(ValueError): ...
class InsufficientCalibrationSample(ValueError): ...
class UnknownCalibrationSchema(ValueError): ...

CALIBRATION_SCHEMA_VERSION: int
DEFAULT_EPA_QUANTILE: float          # 0.80
HC_EXPLOSIVE_YARDS_THRESHOLD: int    # 12
DEFINITIONS: tuple[MetricDefinition, ...]   # baseline_hc_workbook, baseline_hc_verbal,
                                            # success_rate_epa, explosive_epa_magnitude

@dataclass(frozen=True)
class ExplosivenessCalibration: ...

def scrimmage_plays(plays, *, require_epa=False) -> pl.DataFrame
def calibrate(plays, *, epa_quantile=DEFAULT_EPA_QUANTILE) -> ExplosivenessCalibration
def write_calibration(calibration, path) -> None
def load_calibration(path) -> ExplosivenessCalibration
def hc_workbook_explosive_rate(plays, *, group_col="thrown_by") -> pl.DataFrame
def hc_verbal_explosive_rate(plays, *, group_col="thrown_by") -> pl.DataFrame
def hc_efficiency_table(plays, *, group_col="thrown_by", drops_flag=None) -> pl.DataFrame
def definition_comparison(plays, group_cols, *, calibration, ...) -> pl.DataFrame
def cliff_zone_table(plays, *, window=(8, 16)) -> pl.DataFrame
def explosive_score(calibration) -> pl.Expr
```

From `src/flag_football_ep/config.py`:

```python
def load_config(path: Path | str) -> Config
config.paths.processed   # data/processed
config.paths.reference   # data/reference
```

From `src/flag_football_ep/charts/fourth_down.py` — the headless convention to copy exactly:
`matplotlib.use("Agg")` INSIDE the render function, before `import matplotlib.pyplot`; the function
returns a `Figure` and never shows or saves it; `reports/render.py::fig_to_data_uri` is the only
sanctioned way a figure reaches a document, and no PNG is ever written to disk anywhere in this
repo. `docs/` contains zero image files today — the German proposal therefore renders its
distribution as a Markdown table with text bars, and the chart module exists so M3-4's HTML handout
can embed the same data as a real figure.

Corpus state at planning time (verify at run time, do not assume):
`data/processed/plays_scored.parquet` is the 2026-08-21 scored corpus, 21,437 rows, of which
15,006 are run/pass scrimmage plays on downs 1-4 and 14,669 carry EPA. It does NOT contain the
head coach's workbook rows (M3-1 ingests them into `plays.parquet`; M3-2 rescoring is what will put
them into `plays_scored.parquet`) and it has no `efficiency` extra column. Both facts must be
reported by the script and stated in the document, not worked around.

Existing question document `docs/hc-rueckfragen-2026-09.md`: three `## Frage N` sections, a
`## Was wir ohne Antwort liefern` section, and an `## Antworten` section holding one empty
`### Frage N` heading per question. New questions follow that exact structure.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Comparison run on the real corpus, calibration and measured tables as committed artifacts</name>
  <files>scripts/explosiveness_comparison.py, data/reference/explosiveness/calibration.json, data/reference/explosiveness/comparison_overall.csv, data/reference/explosiveness/comparison_by_player.csv, data/reference/explosiveness/cliff_zone.csv</files>
  <read_first>
    - `src/flag_football_ep/features/explosiveness.py` in full — this script orchestrates it and computes nothing itself.
    - `.planning/phases/M3-03-explosiveness-efficiency/M3-03-01-SUMMARY.md` — the final signatures and the chosen `PRIOR_STRENGTH` / `MIN_CALIBRATION_PLAYS`.
    - `scripts/capture_notebook_baseline.py` lines 1-60 — the project's standalone-script conventions: module docstring stating it is not part of the package, `argparse`, deterministic reruns.
    - `src/flag_football_ep/reports/own_team.py` lines 288-360 — `_PLAYER_SOURCE_COLUMNS` and the `thrown_by`-coalesced-onto-`qb` identity rule, so the script groups players the same way the report layer does.
    - `.gitignore` lines 10-20 — confirm `data/processed/*` is ignored and `data/reference/` is not, before choosing where the pseudonym key goes.
  </read_first>
  <action>
    Create `scripts/explosiveness_comparison.py`, a standalone script (not part of the installed
    package, same framing as `scripts/capture_notebook_baseline.py`) with an `argparse` interface:
    `--config` defaulting to `ffep.toml`, `--plays` defaulting to the scored parquet resolved from
    `config.paths.processed`, `--epa-quantile` defaulting to the module default, `--min-attempts`
    for the per-player table, and `--recalibrate` (without it, an existing calibration is loaded and
    reused so a rerun cannot silently move the threshold under an already-published document).

    Load the corpus, run `scrimmage_plays`, and print a corpus census first: total rows, scrimmage
    rows, rows with EPA, distinct `source` values with counts, min/max season, and two explicit
    findings — whether any row has `source` starting with `hc_workbook`, and whether the frame has
    an `efficiency` column at all. Both are expected to be "no" today; the script must say so as a
    named line rather than crashing or omitting the section. This is the wiring that makes
    constraint 4 real: nothing about the script is HC-specific, so head-coach rows flow in
    automatically once M3-2 rescoring puts them into the parquet.

    Calibrate (or load), then write `data/reference/explosiveness/calibration.json` via
    `write_calibration`.

    Build three tables and write them as CSV under `data/reference/explosiveness/`:
    `comparison_overall.csv` — `definition_comparison` grouped by a constant team-level key, one row
    per definition, with `definition`, `label_de`, `scope_note`, `n`, `successes`, `rate`, `ci_low`,
    `ci_high`, `muted`; `comparison_by_player.csv` — the same by QB identity and by receiver
    identity, with a `rolle` column (`QB` / `Receiver`), a pseudonymous `spieler` column, and the
    `shrunk_rate` column alongside `rate`; `cliff_zone.csv` — `cliff_zone_table`'s output over the
    default window.

    Pseudonymise before writing: assign `QB-01..QB-nn` and `WR-01..WR-nn` by descending in-scope
    attempt count with a SHA-256-of-label tiebreak, write the mapping to
    `data/processed/m3-03/pseudonym_key.csv`, and assert in code that the key path is not under
    `config.paths.reference` and not under `docs/`. Players below `--min-attempts` are aggregated
    into a single `Sonstige (n<X)` row rather than dropped — the project never hides thin samples,
    it marks them.

    Also compute and print, for the document to quote: the share of plays satisfying only the yards
    clause of the verbal rule (the "89 plays" finding), and the correlation-free comparison of the
    two baseline rates. Write these into `comparison_overall.csv` as additional labelled rows
    (`verbal_only_yards_clause`) rather than leaving them in stdout.

    If `hc_efficiency_table` raises `MissingExplosivenessColumns` because the corpus has no
    `efficiency` column, catch it, print a named notice, and record the reason in the calibration
    JSON's sibling — do not skip silently and do not fabricate a value.

    Run the script for real and commit the four generated artifacts.
  </action>
  <verify>
    <automated>uv run python scripts/explosiveness_comparison.py --config ffep.toml && uv run python -c "
import csv, json, pathlib
base = pathlib.Path('data/reference/explosiveness')
cal = json.loads((base/'calibration.json').read_text())
assert cal['schema_version'] == 1 and cal['corpus_n'] > 1000, cal
rows = list(csv.DictReader((base/'comparison_overall.csv').open(encoding='utf-8')))
keys = {r['definition'] for r in rows}
assert {'baseline_hc_workbook','baseline_hc_verbal','success_rate_epa','explosive_epa_magnitude'} <= keys, keys
assert all(int(r['n']) > 0 for r in rows if r['definition'] in keys)
players = list(csv.DictReader((base/'comparison_by_player.csv').open(encoding='utf-8')))
import re
assert players and all(re.fullmatch(r'(QB|WR)-\d{2}|Sonstige \(n<\d+\)', r['spieler']) for r in players)
cliff = list(csv.DictReader((base/'cliff_zone.csv').open(encoding='utf-8')))
assert any(r['yards_gained']=='12' for r in cliff) and any(r['yards_gained']=='11' for r in cliff)
print('artifacts ok')" && ! git check-ignore -q data/reference/explosiveness/calibration.json && git check-ignore -q data/processed/m3-03/pseudonym_key.csv && echo OK</automated>
  </verify>
  <done>
    The script runs end to end on the real corpus and is rerunnable without moving the threshold;
    `data/reference/explosiveness/` holds the calibration JSON and three CSVs, all committed, all
    pseudonymous; the pseudonym key exists only under gitignored `data/processed/`; the absent
    `efficiency` column and the absent head-coach rows are reported as named findings rather than
    silent gaps.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Headless chart renderers for the cliff zone and the definition comparison</name>
  <files>src/flag_football_ep/charts/explosiveness.py, tests/test_charts_explosiveness.py</files>
  <read_first>
    - `src/flag_football_ep/charts/fourth_down.py` in full (~130 lines) — the Agg-inside-the-function convention, the "keine Daten" annotation rule, the Clopper-Pearson error-bar rendering, German axis labels.
    - `tests/test_charts_fourth_down.py` lines 1-60 — chart tests assert on figure structure (axes, bar containers, annotation text, axis labels), never on pixels.
    - `src/flag_football_ep/charts/__init__.py` — the package docstring's contract that every renderer returns a Figure.
    - `src/flag_football_ep/reports/render.py` lines 1-95 — `fig_to_data_uri`, and the standing rule that no report builder calls `fig.savefig` directly.
  </read_first>
  <behavior>
    - `render_cliff_zone(cliff_table, *, threshold=12)` returns a `Figure` with one bar per `yards_gained` value in the table, a vertical rule between 12 and 13, and a German x-axis label naming Yards; the bars at 10, 11 and 12 carry an annotation of their combined share so the cliff zone is readable off the chart.
    - `render_cliff_zone` on an empty table returns a Figure carrying a single `keine Daten` annotation and no bars, never raising.
    - `render_definition_comparison(comparison_frame)` returns a `Figure` with one horizontal bar per definition, in `DEFINITIONS` order, labelled with `label_de`, with asymmetric error bars from `ci_low`/`ci_high` and the `n` annotated on each bar.
    - A row with `muted == True` is rendered greyed and still annotated with its `n` — muted, never dropped, matching the project's standing convention.
    - Neither function writes a file: after calling both, no new `.png` exists in the working tree.
    - Both functions import `matplotlib.pyplot` only after `matplotlib.use("Agg")`, so the module imports cleanly with no display.
  </behavior>
  <action>
    Create `src/flag_football_ep/charts/explosiveness.py` with `render_cliff_zone` and
    `render_definition_comparison`, both following `charts/fourth_down.py` exactly: `Agg` selected
    inside the function before `pyplot` is imported, a `Figure` returned, nothing shown, nothing
    saved. German labels throughout. Module docstring must state why the chart module exists in this
    phase even though rendering is M3-4's job: the data these figures show is measured here and
    would otherwise be recomputed by the report layer, and `docs/` holds no image files, so the
    Markdown proposal shows the same distribution as a text-bar table while the HTML handout embeds
    these figures through `fig_to_data_uri`.

    Do not add a `write_*` PNG helper. `fourth_down.py` has one for a standalone-print use case; this
    phase has no such use case and the repo tracks zero PNGs.

    Create `tests/test_charts_explosiveness.py` covering every `<behavior>` bullet, asserting on
    figure structure only, mirroring `tests/test_charts_fourth_down.py`. Build input frames inline
    with polars; do not read anything from `data/`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_charts_explosiveness.py -x -q && uv run pytest tests/test_charts_fourth_down.py tests/test_charts_tendency.py -q && grep -q 'matplotlib.use("Agg")' src/flag_football_ep/charts/explosiveness.py && grep -c "savefig" src/flag_football_ep/charts/explosiveness.py | grep -qx 0 && echo OK</automated>
  </verify>
  <done>
    Two headless renderers exist, return Figures, write no files, mute rather than drop thin rows,
    and are covered by structure-only tests; the existing chart tests stay green.
  </done>
</task>

<task type="auto">
  <name>Task 3: German proposal document, three new head-coach questions, and the doc guards</name>
  <files>docs/explosiveness-vorschlag.md, docs/hc-rueckfragen-2026-09.md, tests/test_m3_explosiveness_docs.py, tests/test_m3_hc_pii.py, .planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md</files>
  <read_first>
    - `docs/explosiveness-recherche.md` in full (217 lines) — the German register, the source-citation style, the candidate names (Kandidat A/B/C) this document must reuse verbatim, and the three open questions it already lists.
    - `docs/hc-notes-2026-09-03.md` — the head coach's own wording, so the document quotes him accurately and does not put words in his mouth.
    - `docs/hc-rueckfragen-2026-09.md` in full (56 lines) — the exact section structure, the "Ohne Antwort:" closing pattern in each question, and the `## Antworten` stub convention.
    - `tests/test_m2_baseline_docs.py` lines 1-130 — the doc-versus-CSV agreement pattern (`_section`, `_table_rows`, `_csv_rows`, German decimal parsing) this task's test file mirrors.
    - `tests/test_m3_hc_pii.py` in full — `_CHECKED_ARTEFACTS` and `_ALLOWED_TOKENS`; you append to the first tuple and touch nothing else.
    - `.planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md` lines 300-315 and 360-370 — the stale `grep -qx 3` count gate and the two prose lines asserting "exactly three `## Frage` headings".
    - The three CSVs written in task 1 — every number in the document comes from them.
  </read_first>
  <action>
    Write `docs/explosiveness-vorschlag.md` in German, coach-facing, addressed to the head coach,
    no player names, every number taken from `data/reference/explosiveness/*.csv`. Required
    structure, each as a `##` heading, in this order:

    `## Worum es geht` — his question and his objection ("was ist, wenn eine Spielerin nur 11 Yards
    erzielt?") restated in one paragraph, plus one sentence on what this document delivers.

    `## Datengrundlage` — which corpus, how many scrimmage plays, which sources, which seasons, and
    two explicit sentences: his own workbook rows are not yet part of this corpus (M3-1 reads them,
    M3-2 rescores them, then these numbers update automatically), and the corpus has no `Efficiency`
    column yet, so his Efficiency reproduction is prepared in code but not yet computed on real
    rows.

    `## Deine heutige Zahl, wörtlich reproduziert` — the two baselines side by side in one table,
    with their measured rates, their n, and their denominators spelled out. State the finding
    plainly and without judgement: the workbook formula checks only yards over 12, while the rule he
    described in the sync also counts positive EPA; the two produce very different numbers on the
    same plays. Cite `docs/explosiveness-recherche.md` for the formula cells.

    `## Der Befund: zwei Fragen in einer Kennzahl` — the Connelly efficiency/explosiveness split, and
    the measured consequence: the verbal rule is satisfied almost entirely by the EPA clause, so it
    is in practice a success rate rather than a description of big plays. Give the exact number of
    plays the yards clause adds on its own, from the CSV.

    `## Die Klippe, in Zahlen` — the cliff-zone table rendered from `cliff_zone.csv` as a Markdown
    table with a text-bar column (repeated block characters proportional to the share), one row per
    yard from 8 to 16, with the cutoff marked between 12 and 13. One sentence naming the combined
    share of the 10-12 zone. This section is the direct answer to the user's objection and must
    contain no prose claim that is not backed by a cell in the table.

    `## Vorschlag` — the recommendation: Efficiency as success rate (`EPA > 0`), Explosiveness as
    Kandidat B (EPA magnitude on successful plays, threshold at the corpus quantile), plus the
    continuous score as a supporting view. Quote the calibrated threshold and the quantile level
    from `calibration.json`, and say in one sentence that the threshold is derived from our data and
    stored with the corpus it came from, so it can be re-derived rather than believed. Explain in
    coach language why this removes the cliff: an 11-yard play and a 12-yard play with comparable
    situations get almost the same score instead of opposite labels.

    `## Kleine Stichproben` — the `n`, the confidence interval and the muting rule already in use,
    plus the shrinkage column as an offered addition with one plain-language sentence on what it
    does. State the rule that no rate is ever shown without its n.

    `## Was das im Report bedeutet (Übergabe an M3-4)` — the exact public function names from
    `features/explosiveness.py` and the two chart renderers M3-4 wires into the Player-Analysis
    report, and one sentence stating that wiring these numbers into `reports/own_team.py` is
    deliberately not part of this phase.

    `## Offene Fragen` — the same three questions as the new sections in the question document,
    one line each, cross-referencing `docs/hc-rueckfragen-2026-09.md`.

    `## Quellen` — the research document, the measured CSVs by path, and the calibration JSON.

    Then append to `docs/hc-rueckfragen-2026-09.md`, in its existing style, three new sections
    placed after `## Frage 3` and before `## Was wir ohne Antwort liefern`, each ending with the
    established `Ohne Antwort:` line stating what we ship regardless:

    `## Frage 4 — Ist die Yards-only-Formel für "Explosive %" so gewollt?` — the formula cell checks
    only GN/LS over 12 while the sync description said "12 Yards und/oder positive EPA"; give both
    measured rates from the CSV and ask which of the two he means. Options (a) yards-only is the
    real rule, (b) the EPA clause should go into the workbook, (c) they are two different metrics he
    wants separately.

    `## Frage 5 — Was genau steht in der Spalte "Efficiency" (Data!O)?` — state that three
    down/distance/yards re-derivations each explained under 80 % of the charted values, so it is
    treated as a manual charting judgement; ask for the rule he applies. Include the second half of
    this question explicitly: his `Efficiency` denominator is `Attempts + Drops`, but in our
    vocabulary a dropped pass is already an incomplete attempt, so we cannot tell whether his
    `Incs` already contains his `Drops` — ask which it is, and note that the code supports both
    readings without a rewrite.

    `## Frage 6 — Sollen Läufe in "Explosive %" und "Efficiency" mitzählen?` — his per-QB
    denominator is pass-only; give our measured run and pass rates from the CSV and ask whether he
    wants one blended offensive number, two separate ones, or the pass-only number kept as is.

    Add the matching empty `### Frage 4/5/6` stubs under `## Antworten`, matching the existing
    pattern exactly.

    Then fix the now-stale count gate in
    `.planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md`: change the automated verify's
    `grep -c '^## Frage' docs/hc-rueckfragen-2026-09.md | grep -qx 3` to `grep -qx 6`, and update
    the two prose lines that say the document has "exactly three `## Frage` headings" to six. This is
    a deliberate cross-phase edit of one gate value: M3-01 plan 01 is still open at its human-action
    checkpoint, and leaving a `== 3` assertion behind would make an unrelated plan fail its own
    verification for a change it did not make. Change nothing else in that file. Note in the summary
    that the six questions now travel to the head coach in one message rather than two.

    Create `tests/test_m3_explosiveness_docs.py` following `tests/test_m2_baseline_docs.py`: assert
    that every rate quoted in the proposal's baseline table matches `comparison_overall.csv` (parse
    German decimal commas), that every yard row in the cliff-zone section matches `cliff_zone.csv`,
    that the quoted threshold and quantile match `calibration.json`, that all ten required `##`
    headings are present in order, that every percentage in the document is accompanied by an `n`,
    that the document names `docs/explosiveness-recherche.md` as a source, and that
    `docs/hc-rueckfragen-2026-09.md` has exactly six `## Frage` headings with a matching
    `### Frage N` stub each under `## Antworten`.

    Finally, extend `tests/test_m3_hc_pii.py`'s `_CHECKED_ARTEFACTS` with
    `docs/explosiveness-vorschlag.md`, `docs/explosiveness-recherche.md`, the three
    `data/reference/explosiveness/*.csv` files and `scripts/explosiveness_comparison.py`. Do not
    change the test's logic, the `_MIN_SURNAME_LEN` constant or `_ALLOWED_TOKENS`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_m3_explosiveness_docs.py tests/test_m3_hc_pii.py -x -q && grep -c '^## Frage' docs/hc-rueckfragen-2026-09.md | grep -qx 6 && grep -c '^### Frage' docs/hc-rueckfragen-2026-09.md | grep -qx 6 && grep -q '^## Vorschlag' docs/explosiveness-vorschlag.md && grep -q '^## Klippe\|^## Die Klippe, in Zahlen' docs/explosiveness-vorschlag.md && grep -q 'explosiveness-recherche.md' docs/explosiveness-vorschlag.md && grep -q "grep -qx 6" .planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md && grep -c "grep -qx 3" .planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md | grep -qx 0 && ! grep -qi "exactly three" .planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md && echo OK</automated>
  </verify>
  <done>
    `docs/explosiveness-vorschlag.md` exists in German with all ten required sections, every number
    traceable to a committed CSV, no player name; `docs/hc-rueckfragen-2026-09.md` has six questions
    and six answer stubs; the stale count gate in M3-01-01 is corrected and nothing else in that file
    changed; `tests/test_m3_explosiveness_docs.py` and the extended PII gate pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `plays_scored.parquet` → committed reference CSV | real player names would cross from local data into a public git-tracked file |
| measured CSV → German document | a hand-typed number can drift away from the measurement it claims to report |
| this phase's document → head coach | an unverified claim about his own workbook damages the working relationship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-03-05 | Information Disclosure | `data/reference/explosiveness/*.csv`, `docs/explosiveness-vorschlag.md` | mitigate | Deterministic pseudonyms assigned in the script; the key written only under gitignored `data/processed/`; `tests/test_m3_hc_pii.py` extended to cover all five new artifacts, so a leak is a failing test rather than a review miss |
| T-M3-03-06 | Tampering (semantic drift) | proposal document vs measured CSVs | mitigate | `tests/test_m3_explosiveness_docs.py` re-reads every quoted rate, the cliff-zone rows and the calibration threshold from the committed CSV/JSON and fails on any divergence (same pattern as `tests/test_m2_baseline_docs.py`) |
| T-M3-03-07 | Repudiation | calibration threshold | mitigate | `--recalibrate` is opt-in: a rerun cannot silently move the threshold under an already-published document, and the JSON carries the corpus fingerprint that produced it |
| T-M3-03-08 | Tampering | `M3-01-01-PLAN.md` cross-phase edit | mitigate | Exactly one value changed (3 → 6) in one gate plus two prose lines; the change is asserted by this task's own verify and recorded in the summary, so it can never look like an unexplained edit to another phase's plan |
| T-M3-03-SC | Tampering | package installs | accept | No install occurs in this plan; matplotlib, polars, scipy are existing project dependencies (RESEARCH § Package Legitimacy Audit: not applicable, zero new packages) |
</threat_model>

<verification>
- `uv run pytest tests/test_charts_explosiveness.py tests/test_m3_explosiveness_docs.py tests/test_m3_hc_pii.py -q` green (< 30 s).
- `uv run pytest tests -q` green (full suite).
- `uv run python scripts/explosiveness_comparison.py` is rerunnable and produces byte-identical CSVs without `--recalibrate`.
- `git status --porcelain data/processed` shows nothing tracked from `data/processed/m3-03/`.
- `git diff --name-only` lists only the twelve owned paths.
</verification>

<success_criteria>
1. The head coach's number and every proposal are measured on the same real plays and committed as
   reference CSVs with their `n`.
2. The German proposal exists, is coach-readable, cites the research document, and quotes no number
   that is not in a committed CSV.
3. The 10-12-yard cliff zone appears as a per-yard table with shares, not as an assertion.
4. Three new questions are in the head coach's question list in his document's existing style, with
   answer stubs, and the stale count gate elsewhere is corrected.
5. No roster name appears in any artifact of this phase; the extended PII gate proves it.
6. The chart renderers exist, are headless, write no files, and are ready for M3-4 to embed.
7. Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/M3-03-explosiveness-efficiency/M3-03-02-SUMMARY.md` when done. Record the
measured rates for all four definitions with their n, the calibrated EPA threshold, the cliff-zone
share, the corpus census (including the confirmed absence of head-coach rows and of the `efficiency`
column), the cross-phase gate edit, and the exact public API M3-4 has to wire.
</output>
