---
phase: M3-02-epa-refinement
plan: 07
type: execute
wave: 5
depends_on: [M3-02-05, M3-02-06]
files_modified:
  - docs/epa-refinement-2026-10.md
  - docs/hc-rueckfragen-2026-09.md
  - tests/test_m3_epa_docs.py
autonomous: true
requirements: [HC-03]

must_haves:
  truths:
    - "A German document exists that a coach can read: what the corpus now contains, how the model is evaluated, how well it is calibrated, what his data added, and where his method and the model disagree"
    - "Every number in the document comes from a committed CSV and a test proves it did not drift"
    - "The unflattering results are in the document, not omitted: which model beats its naive baseline and which does not, and which of his cells rest on a handful of plays"
    - "What we deliberately did not do — his Reg-tab polynomials, the Copy of Data tab, the team-pair games, the EC-2025 workbook — is named with the reason and with what would unlock it"
    - "The two things only he can answer (half markers, competition classification of his games) are written down as questions, not silently decided"
  artifacts:
    - path: "docs/epa-refinement-2026-10.md"
      provides: "The German October-sync write-up: corpus, method, calibration, ablation, comparison, open questions, reproducibility"
      contains: "Leave-one-game-out"
      min_lines: 150
    - path: "docs/hc-rueckfragen-2026-09.md"
      provides: "Two additional questions to the head coach, appended without renumbering the existing ones"
      contains: "Zusatzfragen"
    - path: "tests/test_m3_epa_docs.py"
      provides: "Doc-versus-CSV agreement guard so no number in the document can drift from the measured artifacts"
      contains: "epa-refinement-2026-10.md"
  key_links:
    - from: "docs/epa-refinement-2026-10.md"
      to: "data/reference/epa_refinement/*.csv"
      via: "every quoted figure is read back from the CSV by the doc guard test"
      pattern: "epa_refinement"
    - from: "docs/hc-rueckfragen-2026-09.md"
      to: "data/reference/competition_tier.csv"
      via: "the tier default the questions ask him to confirm"
      pattern: "mixed-other"
---

<objective>
Write the deliverable (EPA-D05): a German `docs/epa-refinement-2026-10.md` that the head coach can
read at the October sync, and the two questions this phase could not answer from data.

The framing is his own: "mittlerweile haben wir noch mehr Datenpunkte und können diese Berechnung
noch professioneller und nachhaltiger gestalten." The pitch is therefore reproducibility,
calibration and honest sample sizes — not a fancier model. That means the document has to carry the
uncomfortable numbers too: whether EP beats its naive baseline (it did not in phase 1.3), which of
his cells rest on a dozen plays, and how much of his corpus is still locked behind two unanswered
questions about his own spreadsheets.

**The question-numbering constraint, and why this plan does not add a `## Frage 7`.** Phase M3-3 is
executing in parallel and its plan M3-03-02 appends `## Frage 4`, `## Frage 5` and `## Frage 6` to
`docs/hc-rueckfragen-2026-09.md`, and edits the count gate in
`.planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md` from `grep -qx 3` to `grep -qx 6`, with
its own verify asserting `grep -c '^## Frage' == 6` and `grep -c '^### Frage' == 6`. Adding a seventh
numbered question would require editing another phase's plan file a second time and would break
M3-03-02's assertions if either plan is re-run. This plan therefore appends a clearly-titled
`## Zusatzfragen (M3-2)` section instead: it changes neither counter, requires no cross-phase plan
edit, and keeps M3-2's questions self-contained and visible. Record this choice explicitly in the
summary so it is a decision on the record, not an omission.

Purpose: this is the artifact the October sync is built on; M3-4 turns it into the handout.
Output: `docs/epa-refinement-2026-10.md`, an appended section in `docs/hc-rueckfragen-2026-09.md`,
`tests/test_m3_epa_docs.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/M3-02-epa-refinement/M3-02-CONTEXT.md
@.planning/phases/M3-02-epa-refinement/M3-02-RESEARCH.md
@.planning/phases/M3-02-epa-refinement/M3-02-02-SUMMARY.md
@.planning/phases/M3-02-epa-refinement/M3-02-03-SUMMARY.md
@.planning/phases/M3-02-epa-refinement/M3-02-04-SUMMARY.md
@.planning/phases/M3-02-epa-refinement/M3-02-05-SUMMARY.md
@.planning/phases/M3-02-epa-refinement/M3-02-06-SUMMARY.md
@docs/hc-notes-2026-09-03.md
@docs/hc-workbook-ingest.md
@docs/model-training.md
@docs/data-contract.md
</context>

<file_collision_guard>
Owned by this plan: `docs/epa-refinement-2026-10.md` (new), `tests/test_m3_epa_docs.py` (new), and an
APPENDED section in `docs/hc-rueckfragen-2026-09.md`. Nothing else may be written.

- `docs/hc-rueckfragen-2026-09.md` is also edited by phase M3-3 (plans M3-03-02 and M3-03-03), which
  runs in parallel. Re-read the file at execution time and append after whatever is already there.
  Do NOT renumber, reword, reorder or delete any existing `## Frage` or `### Frage` section, and do
  NOT change the intro sentence that counts the questions — that sentence is M3-3's to update.
- `.planning/phases/M3-01-hc-workbook-ingest/M3-01-01-PLAN.md`: DO NOT TOUCH. See the objective for
  why this plan needs no cross-phase plan edit at all.
- `data/reference/**`, `data/processed/**`, `src/flag_football_ep/**`, `scripts/**`: READ-ONLY. If a
  number in a CSV looks wrong, report it — never hand-correct a figure in the document to make it
  look right. A number that needs fixing is a re-measurement, not a prose edit.
- `docs/hc-workbook-ingest.md` (M3-02-04), `docs/data-contract.md` (M3-02-01): READ-ONLY. Link to
  them; do not restate their content.
- `docs/explosiveness-vorschlag.md`, `docs/explosiveness-recherche.md`,
  `tests/test_m3_explosiveness_docs.py`, `tests/test_m3_hc_pii.py`: M3-3 territory. Never touched.
- Never touch `src/flag_football_ep/cv/**`, `scripts/hackathon/**` or any hackathon document.
</file_collision_guard>

<pii_discipline>
`docs/` is committed and shared. This document reports aggregate cells, corpus counts, log-losses and
run ids only. No roster name, no jersey number, no opponent-identifying detail beyond what already
appears in the public reference data. The document names the workbook FILES (they are filenames, not
people) and never a player.
</pii_discipline>

<interfaces>
Committed artifacts every figure must come from:

- `data/reference/epa_refinement/ablation_summary.csv` — model, arm, run id, experiment, n_plays,
  n_folds, logo_wall_seconds, training_data_sha256, metric, naive baseline, improvement.
- `data/reference/epa_refinement/per_source_metrics_ep.csv` / `_wp.csv` — arm, source, n, logloss,
  naive_logloss, improvement, including a `__pooled__` row.
- `data/reference/epa_refinement/corpus_arms.csv` — game ids per arm.
- `data/reference/epa_refinement/no_play_rows.csv` — Timeout / Offsetting Penalties / penalty counts.
- `data/reference/epa_refinement/comparison_by_dd.csv`, `comparison_clustered.csv`,
  `comparison_coverage.csv` — the four-way per-cell comparison and its gaps.
- `data/reference/hc_sp_tables/*.csv` + `manifest.csv` — his snapshotted tables.
- `data/reference/competition_tier.csv`, `data/reference/hc_games.csv` — the tier defaults and the
  declared games.

Phase 1.3 baseline to compare against (`.planning/phases/01.3-methodical-model-retraining/01.3-TRAINING-REPORT.md`):
214 LOGO folds; EP `logo_mlogloss` 1.027657 vs naive 1.007274 — **does not beat naive**;
WP `logo_logloss` 0.367263 vs naive 0.691095 — beats it comfortably.

Existing German document register: `docs/hc-rueckfragen-2026-09.md` addresses the head coach
informally ("deine Workbooks", "Bitte um ..."), uses short `## Frage N — <question>` headings, states
"Ohne Antwort: ..." for each, and ends with an `## Antworten` section holding one empty
`### Frage N — ...` stub per question. Match that register.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The German write-up</name>
  <files>docs/epa-refinement-2026-10.md</files>
  <read_first>
    - All five M3-02 plan summaries listed in `<context>` — every number in this document comes from
      them and from the CSVs they name. Read the CSVs directly for any figure you quote.
    - `docs/hc-notes-2026-09-03.md` — his own words and his priority for October.
    - `.planning/phases/01.3-methodical-model-retraining/01.3-TRAINING-REPORT.md` sections 1-4 and 9 —
      the baseline numbers and the register in which this project reports an unflattering result.
    - `docs/model-training.md` sections 1-3 — the evaluation protocol and why promotion is manual,
      so the document describes the real process.
    - `.planning/phases/M3-02-epa-refinement/M3-02-RESEARCH.md` section 1.4 — the "honest framing"
      instruction: state the trainable game count as a range, never "2,128 games fixed".
  </read_first>
  <action>
    Create `docs/epa-refinement-2026-10.md` in German, addressed to the head coach in the same
    informal register as `docs/hc-rueckfragen-2026-09.md`, with these sections in this order. Every
    figure is quoted from a committed CSV named inline, so a reader can check it.

    `# EPA-Update für den Oktober-Sync 2026` — one paragraph on what this is and what it is not: a
    reproducible, kalibrierte Neuberechnung auf einem größeren Korpus, kein neues Modell und keine
    fancier Mathematik.

    `## Der Korpus` — per source and per workbook/sheet: rows and games before and after, from
    M3-02-04's table. State the trainable head-coach game count as the meaningful number and say
    plainly how much is still locked behind the two open questions. Do NOT write "2.128 Spiele
    korrigiert"; state the range and what it consists of.

    `## Was wir an deinen Daten reparieren mussten` — two short subsections, one per fix, each in
    plain language: (a) die Halbzeit-Information fehlt in den Workbooks, wir setzen einen
    dokumentierten Ersatzwert und was das kostet (kein Halbzeit-Marker, eine torlose Drive der ersten
    Halbzeit erbt den nächsten echten Score); (b) die Spielgrenzen wurden an jedem Ballbesitzwechsel
    getrennt, jetzt nicht mehr. Link to `docs/data-contract.md` and `docs/hc-workbook-ingest.md` for
    the details instead of repeating them.

    `## Methode` — leave-one-game-out über `game_id` (jedes Spiel genau einmal Testspiel, nie
    gleichzeitig Trainings- und Testdaten), Wettbewerbs-Tier als Kovariate, Kalibrierungsreport,
    MLflow-Versionierung mit manueller Freigabe. Name the run ids from `ablation_summary.csv`.

    `## Kalibrierung — schlägt das Modell die einfache Grundrate?` — the log-loss versus naive
    baseline per model and arm, with the 1.3 numbers next to them. If EP still does not beat its
    naive baseline, say so in the first sentence of this section, not in a footnote. Then the
    per-source breakdown from `per_source_metrics_*.csv`, including how his own sources score against
    their own floors.

    `## Was deine Daten beitragen` — the ablation: same models, same protocol, once with and once
    without his rows; folds, plays, log-loss and the difference. State plainly whether the difference
    is large, small or within noise.

    `## Dein Ansatz und das Modell nebeneinander` — the comparison. Explain his method in one
    paragraph first (empirische Trefferquote je Down, Distanz und Feldhälfte). Then a table excerpt
    from `comparison_by_dd.csv` with n on every column, the five largest disagreements, and the cells
    where his n is in the low teens next to cells with n in the hundreds for the same down and
    distance — that contrast is the argument for the model, and it must be visible as numbers. Also
    report the published-versus-recomputed gap on his own rows and say what a systematic offset there
    would mean (eine andere Definition von "Scoring Probability", eine Frage an ihn, kein Datenfehler).

    `## Was wir bewusst nicht reproduziert haben` — the `Reg` tab. Describe what it contains
    (hardcodierte Polynome steigenden Grades je Down und Feldhälfte, das Muster einer aus einem
    Excel-Diagramm übernommenen Trendlinie), that his values are snapshotted verbatim in
    `data/reference/hc_sp_tables/reg_formulas.csv`, and why a cross-validated model is offered instead
    of a per-cell trendline. No editorialising about his work — describe, cite, and let the
    calibration numbers make the point.

    `## Was noch nicht drin ist` — the four standing exclusions with what would unlock each: the
    team-pair block (posteam unbestimmt bis Frage 2), `Copy of Data` (anderes Spaltenlayout, Frage 2),
    das EC-2025-Workbook (Frage 1), and the three corrupted clustered bin labels which we reconstruct
    as `1-5`, `6-10`, `11-15` and mark as `[ASSUMED]` (an Excel-Autokorrektur, kein Fehler von ihm).
    Plus the Timeout / Offsetting Penalties decision with its measured count from `no_play_rows.csv`.

    `## Offene Fragen` — pointing at `docs/hc-rueckfragen-2026-09.md`, listing Frage 1, Frage 2 and
    the two Zusatzfragen this phase adds (task 2), each in one line.

    `## Reproduzierbarkeit` — the exact commands (`ffep ingest`, `scripts/hc_games_refill.py`,
    `scripts/hc_corpus_ablation.py`, `scripts/hc_sp_snapshot.py`, `scripts/epa_comparison.py`), the
    MLflow run ids, the `training_data_sha256` per arm, and the CSV paths every figure in this
    document came from. End with a `Stand:` line carrying the date.

    Write no number you have not read out of a CSV in this session.
  </action>
  <verify>
    <automated>grep -q "Leave-one-game-out\|leave-one-game-out" docs/epa-refinement-2026-10.md && grep -q "epa_refinement" docs/epa-refinement-2026-10.md && grep -q "Reg" docs/epa-refinement-2026-10.md && grep -q "^## Reproduzierbarkeit" docs/epa-refinement-2026-10.md && grep -q "^## Was noch nicht drin ist" docs/epa-refinement-2026-10.md && grep -q "Stand:" docs/epa-refinement-2026-10.md && echo OK</automated>
  </verify>
  <done>
    The German document exists with all ten sections, quotes only figures that are in committed CSVs,
    states the calibration result including the unflattering one in its opening sentence, shows n on
    every comparison column, and names every exclusion with what would unlock it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Two additional questions to the head coach, without renumbering</name>
  <files>docs/hc-rueckfragen-2026-09.md</files>
  <read_first>
    - `docs/hc-rueckfragen-2026-09.md` as it stands AT EXECUTION TIME — phase M3-3 may already have
      appended Fragen 4-6. Read the whole file before writing anything.
    - `.planning/phases/M3-03-explosiveness-efficiency/M3-03-02-PLAN.md` task 3 — the exact assertions
      M3-3 makes on this file (`grep -c '^## Frage' == 6`, `grep -c '^### Frage' == 6`), so this
      plan's addition provably changes neither.
    - `data/reference/competition_tier.csv` and `.planning/phases/M3-02-epa-refinement/M3-02-02-SUMMARY.md`
      — the tier defaults and their `[ASSUMED]` rationale.
    - `.planning/phases/M3-02-epa-refinement/M3-02-RESEARCH.md` section 2.4 — the half-marker
      heuristic's n = 2 evidence, so the question states honestly why we are asking rather than
      guessing.
  </read_first>
  <action>
    Append ONE new section, `## Zusatzfragen (M3-2, EPA-Update)`, placed after the last existing
    `## Frage N` section and before `## Was wir ohne Antwort liefern` if that section exists,
    otherwise before `## Antworten`. Its heading must NOT match `^## Frage`, and its two
    sub-questions must use `### Zusatzfrage A` / `### Zusatzfrage B` headings, which do NOT match
    `^### Frage` — so both of M3-3's counters stay exactly as M3-3 left them.

    Open the section with one sentence saying these two came out of the EPA update and that neither
    can be answered from the data itself.

    `### Zusatzfrage A — Gibt es Halbzeit-Marker für deine Workbook-Spiele?`
    In German, informal: the workbooks carry no half information, so for the games we could use we set
    a documented replacement value; the cost is that a scoreless drive before halftime is credited
    with the next actual score of the game. Ask whether he can mark the first play after halftime —
    one number per game is enough — or confirm that camp and scrimmage segments have no formal two-half
    structure at all. State honestly what we did NOT do: we tested estimating the halftime from the
    play count, it landed within four percentage points of the midpoint on the only two games where we
    have a real marker, and n = 2 is not enough to adopt it. Close with an "Ohne Antwort:" line —
    the replacement value stays, documented in `docs/data-contract.md`, and the limitation is named in
    the October document.

    `### Zusatzfrage B — Wie sollen wir deine Spiele einordnen: Camp, Scrimmage oder Länderspiel?`
    Explain in one sentence why it matters: the model uses the competition level as an input, and we
    currently classify all your games as `mixed-other`, the same category as the older legacy games —
    an assumption, not a fact. Offer him a short list to pick from per workbook (`Offense Analytics
    2026 Camps and Competitions`, `Scoring Probability by Situation 2023-2026`): Länderspiel,
    Turnier, Camp/Scrimmage, gemischt. Close with an "Ohne Antwort:" line — `mixed-other` stays as the
    documented default and the assumption is marked as such in the October document.

    Add the matching empty stubs under `## Antworten`, using `### Zusatzfrage A — ...` and
    `### Zusatzfrage B — ...` so they too match neither of M3-3's counters.

    Change nothing else in the file. Do not touch the intro sentence that counts the questions.
  </action>
  <verify>
    <automated>grep -q "^## Zusatzfragen" docs/hc-rueckfragen-2026-09.md && grep -c '^### Zusatzfrage' docs/hc-rueckfragen-2026-09.md | grep -qx 4 && grep -q "mixed-other" docs/hc-rueckfragen-2026-09.md && grep -q "Halbzeit" docs/hc-rueckfragen-2026-09.md && test $(grep -c '^## Frage' docs/hc-rueckfragen-2026-09.md) -eq $(grep -c '^### Frage' docs/hc-rueckfragen-2026-09.md) && echo OK</automated>
  </verify>
  <done>
    Two additional questions and their two answer stubs are appended under headings that match
    neither `^## Frage` nor `^### Frage`; the existing numbered questions, their stubs and the intro
    sentence are untouched; no other phase's plan file was edited.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Doc-versus-CSV agreement guard</name>
  <files>tests/test_m3_epa_docs.py</files>
  <read_first>
    - `tests/test_m2_baseline_docs.py` — the established doc-versus-measurement guard pattern in this
      repository: how it parses figures out of a Markdown document and compares them to the artifact.
    - `tests/test_m3_epa_snapshot.py` (M3-02-03) — the sibling guard for the snapshot CSVs; do not
      duplicate its assertions, reference them.
    - `docs/epa-refinement-2026-10.md` as written in task 1.
  </read_first>
  <behavior>
    - Every MLflow run id quoted in `docs/epa-refinement-2026-10.md` appears in
      `data/reference/epa_refinement/ablation_summary.csv`, and every run id in that CSV appears in
      the document — neither side may carry a run the other does not.
    - Every log-loss figure quoted in the document matches a value in `ablation_summary.csv` or
      `per_source_metrics_*.csv` to the precision it is written at.
    - The document references every CSV under `data/reference/epa_refinement/` at least once, so no
      measured artifact is silently unreported.
    - The document contains no roster player name (same roster load and skip-if-absent behaviour as
      `tests/test_m3_epa_snapshot.py`).
    - `docs/hc-rueckfragen-2026-09.md` has an equal number of `^## Frage` and `^### Frage` headings,
      and its `^### Zusatzfrage` count equals twice its `^## Zusatzfragen` section's sub-question
      count — the structural invariant that keeps M3-2's addition compatible with M3-3's counters.
    - The document's `Stand:` line parses as a date.
  </behavior>
  <action>
    Create `tests/test_m3_epa_docs.py` implementing the behaviours above. Module docstring: this guard
    exists so that a number in the coach-facing document can never drift away from the measured CSV —
    if a figure is wrong, the fix is a re-measurement, not a prose edit.

    Parse figures with a tolerant regular expression (German decimal comma AND point) and compare with
    a tolerance derived from the written precision, not a fixed epsilon — a figure written as `1,03`
    must match `1.027657`.

    `pytest.skip` with a clear reason if any required CSV is absent, rather than passing vacuously.

    Do NOT assert on wording, tone or section ordering beyond the section headings the task-1 verify
    already gates — the human checkpoint in M3-02-08 owns the wording.
  </action>
  <verify>
    <automated>uv run pytest tests/test_m3_epa_docs.py -x -q && grep -c 'data/raw' tests/test_m3_epa_docs.py | grep -qx 0 && echo OK</automated>
  </verify>
  <done>
    Run ids and log-loss figures in the document are proven to match the committed CSVs in both
    directions, every measured CSV is referenced at least once, the document carries no roster name,
    and the question-file structural invariant is asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured CSV -> coach-facing prose | a hand-typed figure can drift from the measurement it claims to report |
| this phase's question edit -> another phase's file | a numbered addition would break a parallel phase's assertions and its plan file |
| repository document -> October handout | prose written for a colleague is read as a finished statement about his work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M3-02-28 | Tampering (figure drift) | quoted numbers | mitigate | `tests/test_m3_epa_docs.py` compares every quoted run id and log-loss against the committed CSVs in both directions, at the written precision; the plan forbids correcting a figure in prose |
| T-M3-02-29 | Tampering (cross-phase) | `docs/hc-rueckfragen-2026-09.md` | mitigate | The new section matches neither `^## Frage` nor `^### Frage`, so M3-03-02's two count assertions are provably unaffected; `M3-01-01-PLAN.md` is not touched at all; the verify asserts the two counters stay equal |
| T-M3-02-30 | Repudiation (selective reporting) | calibration section | mitigate | The plan requires the unflattering calibration result in the section's opening sentence, the per-source breakdown including the head coach's own sources against their own floors, and every exclusion named with what would unlock it |
| T-M3-02-31 | Information Disclosure | committed German document | mitigate | The doc guard scans for roster names with the same mechanism as the snapshot guard; the document reports aggregate cells, counts and run ids only |
| T-M3-02-32 | Spoofing (unreviewed handout) | document status | mitigate | Until M3-02-08's human checkpoint, this is a repository document, not a handout; the checkpoint owns tone, framing and the decision to send |
| T-M3-02-SC | Tampering | package installs | accept | This plan installs nothing — pytest and polars are existing pinned dependencies (M3-02-RESEARCH Package Legitimacy Audit: zero new packages). No install task exists, so no legitimacy gate applies |
</threat_model>

<verification>
- `uv run pytest tests/test_m3_epa_docs.py tests/test_m3_epa_snapshot.py -q` green.
- `grep -c '^## Frage' docs/hc-rueckfragen-2026-09.md` equals
  `grep -c '^### Frage' docs/hc-rueckfragen-2026-09.md`, and neither changed relative to before this
  plan (`git diff` on the file shows added lines only).
- `git status --porcelain .planning/phases/M3-01-hc-workbook-ingest/` empty.
- `git status --porcelain data/ src/ scripts/` empty — this plan measured nothing and changed no code.

</verification>

<success_criteria>
1. `docs/epa-refinement-2026-10.md` exists in German with corpus, fixes, method, calibration,
   ablation, comparison, deliberate non-reproductions, exclusions, open questions and
   reproducibility.
2. Every quoted number is provably from a committed CSV, in both directions.
3. The calibration section leads with the result, including if it is unflattering.
4. Two additional questions and their answer stubs are appended without renumbering anything and
   without editing another phase's plan file.
5. No code, data or other phase's document was modified.
</success_criteria>

<output>
Create `.planning/phases/M3-02-epa-refinement/M3-02-07-SUMMARY.md` when done. Record the decision to
use `## Zusatzfragen` instead of a numbered `## Frage 7` and why, the list of figures the doc guard
now pins, and anything in the document that is a judgement call the M3-02-08 checkpoint should look
at specifically.
</output>
